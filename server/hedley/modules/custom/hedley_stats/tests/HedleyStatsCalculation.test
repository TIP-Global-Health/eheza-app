<?php

/**
 * @file
 * Test Z-Score calculations.
 */

/**
 * HedleyStats calculation tests.
 */
class HedleyStatsCalculation extends HedleyWebTestBase {

  /**
   * Info hook.
   */
  public static function getInfo() {
    return [
      'name' => 'HedleyStatsCalculation tests',
      'description' => 'Tests Stats calculations.',
      'group' => 'Hedley',
    ];
  }

  /**
   * The health center ID.
   *
   * @var int
   */
  protected $healthCenterId;

  /**
   * The clinic ID.
   *
   * @var int
   */
  protected $clinicId;

  /**
   * The nurse.
   *
   * @var object
   */
  protected $nurse;

  /**
   * Add a main health center and clinic for all the tests.
   *
   * @inheritdoc
   */
  public function setUp() {
    parent::setUp([
      'hedley_activity',
      'hedley_acute_illness',
      'hedley_device',
      'hedley_ncd',
      'hedley_nutrition',
      'hedley_patient',
      'hedley_person',
      'hedley_schedule',
    ]);

    // Create the base data for the test.
    $this->healthCenterId = $this->createHealthCenter();
    // Type FBF because stats are only for FBF Clinics.
    $this->clinicId = $this->createClinic($this->healthCenterId, 'fbf');
  }

  /**
   * Combined Stats calculations test.
   *
   * Each test method in DrupalWebTestCase reinstalls the entire hedley
   * profile (~5 min in CI). The four scenarios below operate on
   * disjoint data and the underlying hedley_stats functions all take a
   * health center id as a parameter, so creating a fresh HC + FBF
   * clinic at the start of each scenario keeps them isolated. Bundling
   * them under one method keeps the install paid once instead of
   * four times.
   *
   * Covers:
   *
   *   1. Total encounters by period (last_year / this_year split).
   *   2. Case management aggregation across moderate/severe branches.
   *   3. Session attendance (smoke check that the function runs), and what
   *      counts as having attended one (B-152a).
   *   4. Stats-blob sync with cache-hash invalidation.
   *   5. Soft-deleted measurements are excluded from stats aggregates (B-032).
   *   6. Soft-deleted persons are excluded from group education stats (B-033).
   */
  public function testStatsCalculations() {
    // 1. Total encounters.
    $this->resetHealthCenter();
    $attendance_last_year = 2;
    $attendance_this_year = 1;
    $this->createSessionsWithAttendance($attendance_last_year, $attendance_this_year);
    $total_encounters = hedley_stats_get_total_encounters($this->healthCenterId, []);
    $this->assertEqual($attendance_last_year, $total_encounters['global']['fbf']['last_year']);
    $this->assertEqual($attendance_this_year, $total_encounters['global']['fbf']['this_year']);

    // 2. Case management.
    $this->resetHealthCenter();
    $measurements = [
      HEDLEY_STATS_MODERATE => [
        'weight' => 8,
        'height' => 75,
        'muac' => 12,
      ],
      HEDLEY_STATS_SEVERE => [
        'weight' => 5,
        'height' => 72,
        'muac' => 11,
      ],
    ];
    $attendance = 4;
    $sessions = $this->createSessionsWithAttendance(0, $attendance, '0', '-5 months');

    foreach ($sessions as $session) {
      if (!isset($session['session_data'])) {
        continue;
      }

      $session_data = $session['session_data'];
      $session_nid = $session_data['session_nid'];
      $measurements_state = HEDLEY_STATS_MODERATE;

      foreach ($session_data['children'] as $child_nid) {
        // We want to reach the amount of severe and moderate distribution that
        // has been set above the loop.
        $measurements_state = $measurements_state == HEDLEY_STATS_MODERATE ? HEDLEY_STATS_SEVERE : HEDLEY_STATS_MODERATE;

        // Create measurements.
        $this->createWeight('weight', $session_nid, $child_nid, $measurements[$measurements_state]['weight'], $session['date']);
        $this->createHeight('height', $session_nid, $child_nid, $measurements[$measurements_state]['height'], $session['date']);
        $this->createMuac('muac', $session_nid, $child_nid, $measurements[$measurements_state]['muac'], $session['date']);;
      }

      $patients_details = [];
      $case_management = hedley_stats_get_case_management($this->healthCenterId, $patients_details);

      $this->assertEqual($attendance, count($case_management['this_year']['fbf']));
    }

    // 3. Session attendance (smoke check).
    $this->resetHealthCenter();
    $attendance = 2;
    $this->createSessionsWithAttendance(0, $attendance, '-1 year', '-1 week', strtotime('-24 months'));

    $this_month = [
      'start' => strtotime("-1 month"),
      'end' => time(),
    ];
    $fbf_clinics = hedley_health_center_get_clinics_of_health_center($this->healthCenterId, 'fbf');
    $patients_details = [];
    [$completed_programs, $missed_sessions] = hedley_stats_get_session_attendance_stats_by_health_center($this->healthCenterId, $fbf_clinics, $patients_details);

    $count_missed_sessions = 0;
    foreach ($missed_sessions as $missed_session) {
      $expected_date = strtotime($missed_session['expected_date']);
      if ($expected_date >= $this_month['start'] && $expected_date <= $this_month['end']) {
        $count_missed_sessions++;
      }
    }
    $count_completed_programs = 0;
    foreach ($completed_programs as $completed_program) {
      $expected_date = strtotime($completed_program['expected_date']);
      if ($expected_date >= $this_month['start'] && $expected_date <= $this_month['end']) {
        $count_completed_programs++;
      }
    }

    // 3b. What counts as attending a session.
    //
    // A session counts for a person when something was measured there. The
    // attendance record itself does not count: it carries a person and a
    // session like any measurement does, but marking someone absent updates
    // it to attended = FALSE rather than removing it, so counting it turned a
    // record of absence into proof of presence.
    $this->resetHealthCenter();
    $session_nid = $this->createSession($this->clinicId, strtotime('-1 week'));
    $nurse = $this->createNurse($this->clinicId);

    $mother_nid = $this->createMother($this->clinicId);
    $measured_child = $this->createChild($mother_nid, $this->healthCenterId, strtotime('-23 months'), strtotime('-23 months'));
    $attended_only_child = $this->createChild($mother_nid, $this->healthCenterId, strtotime('-23 months'), strtotime('-23 months'));
    $absent_child = $this->createChild($mother_nid, $this->healthCenterId, strtotime('-23 months'), strtotime('-23 months'));

    // One child was weighed, one was marked present and nothing was taken,
    // one was marked absent.
    $this->createAttendance($this->healthCenterId, $session_nid, $nurse->uid, $measured_child, TRUE, strtotime('-1 week'));
    $this->createWeight('weight', $session_nid, $measured_child, 12, strtotime('-1 week'));
    $this->createAttendance($this->healthCenterId, $session_nid, $nurse->uid, $attended_only_child, TRUE, strtotime('-1 week'));
    $this->createAttendance($this->healthCenterId, $session_nid, $nurse->uid, $absent_child, FALSE, strtotime('-1 week'));

    $persons_sessions = hedley_activity_get_persons_sessions_with_measurements([$session_nid]);

    $this->assertTrue(
      !empty($persons_sessions[$measured_child][$session_nid]),
      'A child who was weighed in the session counts as having attended it.'
    );
    $this->assertTrue(
      empty($persons_sessions[$attended_only_child][$session_nid]),
      'A child marked present, with nothing measured, does not count: the session did not run for them.'
    );
    $this->assertTrue(
      empty($persons_sessions[$absent_child][$session_nid]),
      'A child marked absent does not count. The record of the absence is not evidence of presence.'
    );

    // 4. Sync handler with cache-hash invalidation.
    $this->resetHealthCenter();
    $this->createGroupNutritionData();
    $this->createIndividualNutritionEncounterData();
    $this->createNcdEncounterData();
    $this->createPrenatalEncounterData();
    $this->createAcuteIllnessEncounterData();
    $this->createChildScorecardEncounterData();
    $this->createWellChildEncounterData();

    $health_center_wrapper = entity_metadata_wrapper('node', $this->healthCenterId);

    /** @var RestfulEntityBaseNode $handler */
    $handler = restful_get_restful_handler('sync');

    // Make sure anonymous user doesn't get the content.
    $handler->setAccount($this->nurse);
    $request = [
      'base_revision' => 0,
      'db_version' => HEDLEY_RESTFUL_CLIENT_SIDE_INDEXEDDB_SCHEMA_VERSION,
    ];

    // First request will create the AQ item and get all the data for the HC.
    $handler->get($health_center_wrapper->field_uuid->value(), $request);

    $aq_item = $this->loadAqItemByTitle(HEDLEY_STATS_CALCULATE_STATS . '_' . $this->healthCenterId);
    $this->processAqItem($aq_item);

    // Then, send the second request to get the stats.
    $results = $handler->get($health_center_wrapper->field_uuid->value(), $request);

    $stats = end($results['batch']);

    if (!is_array($stats)) {
      // No need to check the rest if it's not an array, the above test will
      // fail and this will be enough to know something went wrong.
      return;
    }

    // Check that we got the stats.
    $this->assertEqual($stats['type'], 'statistics');

    // Check that the stats are not empty.
    $this->assertEqual($stats['uuid'], $health_center_wrapper->field_uuid->value());
    $this->assertFalse(empty($stats['case_management']));
    $this->assertFalse(empty($stats['children_beneficiaries']));
    $this->assertFalse(empty($stats['good_nutrition']));
    $this->assertFalse(empty($stats['total_encounters']));
    $this->assertFalse(empty($stats['nutrition_group_data']));
    $this->assertFalse(empty($stats['nutrition_individual_data']));
    $this->assertFalse(empty($stats['ncd_data']));
    $this->assertFalse(empty($stats['prenatal_data']));
    $this->assertFalse(empty($stats['acute_illness_data']));
    $this->assertFalse(empty($stats['child_scoreboard_data']));
    $this->assertFalse(empty($stats['spv_data']));

    // Call again with cache hash to make sure we don't get the results this
    // time.
    $request['stats_cache_hash'] = $stats['stats_cache_hash'];
    $results2 = $handler->get($health_center_wrapper->field_uuid->value(), $request);

    // Check the results are empty which means that it is cached and won't sent
    // it again to the frontend.
    $stats2 = end($results2['batch']);
    // We establish this by making sure the stats are not an array and therefore
    // they are an entity from the sync, so it was not sent back this time.
    $this->assertFalse(is_array($stats2));

    // Send with a random hash to dummy an old cache hash.
    $request['stats_cache_hash'] = 'some-old-cache';
    $handler->get($health_center_wrapper->field_uuid->value(), $request);

    $aq_item = $this->loadAqItemByTitle(HEDLEY_STATS_CALCULATE_STATS . '_' . $this->healthCenterId);
    $this->processAqItem($aq_item);

    $results3 = $handler->get($health_center_wrapper->field_uuid->value(), $request);
    $stats3 = end($results3['batch']);

    // Check that we got the stats.
    $this->assertEqual($stats3['type'], 'statistics');
    $this->assertEqual($stats3['uuid'], $health_center_wrapper->field_uuid->value());

    // Remove the cache hash and expect to get the stats again.
    unset($request['stats_cache_hash']);
    $handler->get($health_center_wrapper->field_uuid->value(), $request);

    $aq_item = $this->loadAqItemByTitle(HEDLEY_STATS_CALCULATE_STATS . '_' . $this->healthCenterId);
    $this->processAqItem($aq_item);

    $results4 = $handler->get($health_center_wrapper->field_uuid->value(), $request);
    $stats4 = end($results4['batch']);

    // Check that we got the stats.
    $this->assertTrue(is_array($stats4));

    // 5. Soft-deleted measurements are excluded from stats aggregates (B-032).
    //
    // An admin can soft-delete an individual measurement (field_deleted = TRUE)
    // while its encounter stays live (VBO mark-as-deleted action, or the
    // delete-duplicate-measurements script). The dashboard builders join
    // measurement values off the field_<program>_encounter fan, which had no
    // deleted filter, so the removed values kept feeding the dashboards on
    // every recalculation. This scenario exercises the shared
    // hedley_stats_exclude_deleted_from_join() helper through the nutrition
    // individual builder as a representative of the six builders that use it.
    $this->resetHealthCenter();
    $created = $this->createIndividualNutritionEncounterData('-7 days', 11, 'edema');

    // Baseline: the MUAC measurement is present in the stats.
    hedley_stats_handle_cache(HEDLEY_STATS_CACHE_CLEAR, HEDLEY_STATS_SYNC_NUTRITION_INDIVIDUAL_DATA, $this->healthCenterId);
    $encounter_before = $this->extractFirstStatsEncounter(hedley_stats_get_nutrition_individual_data($this->healthCenterId));
    $this->assertFalse(empty($encounter_before['muac']), 'MUAC measurement is present before deletion.');

    // Soft-delete the MUAC measurement, leaving its encounter live.
    $muac_wrapper = entity_metadata_wrapper('node', $created['muac']);
    $muac_wrapper->field_deleted->set(TRUE);
    $muac_wrapper->save();

    // Recalculate: the deleted MUAC must be gone, while the encounter and its
    // still-live nutrition sign must remain. Before the fix, the deleted MUAC
    // value would still be pulled in here.
    hedley_stats_handle_cache(HEDLEY_STATS_CACHE_CLEAR, HEDLEY_STATS_SYNC_NUTRITION_INDIVIDUAL_DATA, $this->healthCenterId);
    $encounter_after = $this->extractFirstStatsEncounter(hedley_stats_get_nutrition_individual_data($this->healthCenterId));
    $this->assertFalse(empty($encounter_after), 'Encounter is retained after its measurement is soft-deleted.');
    $this->assertTrue(empty($encounter_after['muac']), 'Soft-deleted MUAC is excluded from stats after recalculation.');
    $this->assertEqual(['edema'], $encounter_after['nutrition_signs'], 'Live nutrition sign is retained.');

    // 6. Soft-deleted persons are excluded from group education stats (B-033).
    //
    // Merging a duplicate patient soft-deletes the duplicate person, but
    // nothing re-points the education sessions that reference them. Those
    // sessions kept counting the deleted person among their attendees, and
    // shipped their name and gender to the dashboard.
    $this->resetHealthCenter();
    $live = $this->createMother($this->clinicId);
    $duplicate = $this->createMother($this->clinicId);
    $this->createEducationSession([$live, $duplicate]);

    // Baseline: both attendees are counted, and both have details.
    $patients_details = [];
    hedley_stats_handle_cache(HEDLEY_STATS_CACHE_CLEAR, HEDLEY_STATS_SYNC_GROUP_EDUCATION_DATA, $this->healthCenterId);
    $before = $this->extractEducationParticipants(hedley_stats_get_group_education_data($this->healthCenterId, $patients_details));
    $this->assertEqual([$live, $duplicate], $before, 'Both attendees are counted before the merge.');
    $this->assertFalse(empty($patients_details[$duplicate]), 'The duplicate has details before the merge.');

    // Merge the duplicate away, as hedley_patient's merge flow does.
    $duplicate_wrapper = entity_metadata_wrapper('node', $duplicate);
    $duplicate_wrapper->field_deleted->set(TRUE);
    $duplicate_wrapper->save();

    // The session keeps its live attendee; the deleted person is neither
    // counted nor described. Before the fix, both assertions failed.
    $patients_details = [];
    hedley_stats_handle_cache(HEDLEY_STATS_CACHE_CLEAR, HEDLEY_STATS_SYNC_GROUP_EDUCATION_DATA, $this->healthCenterId);
    $after = $this->extractEducationParticipants(hedley_stats_get_group_education_data($this->healthCenterId, $patients_details));
    $this->assertEqual([$live], $after, 'The soft-deleted attendee is no longer counted.');
    $this->assertTrue(empty($patients_details[$duplicate]), 'The soft-deleted attendee\'s name is not shipped to the dashboard.');
  }

  /**
   * Create an education session attended by the given persons.
   *
   * @param array $participants
   *   Person node IDs attending the session.
   *
   * @return int
   *   The education session node ID.
   */
  protected function createEducationSession(array $participants) {
    $node = $this->drupalCreateNode(['type' => 'education_session']);

    $wrapper = entity_metadata_wrapper('node', $node);
    $scheduled_date = date('Y-m-d', strtotime('-3 days'));
    $wrapper->field_scheduled_date->set([
      'value' => $scheduled_date,
      'value2' => $scheduled_date,
    ]);
    $wrapper->field_education_topics->set(['malaria']);
    $wrapper->field_participating_patients->set($participants);
    $wrapper->field_shards->set([$this->healthCenterId]);
    $wrapper->save();

    return $node->nid;
  }

  /**
   * Flatten the participant IDs out of a group-education stats structure.
   *
   * @param array $data
   *   A hedley_stats_get_group_education_data() result: sessions by village.
   *
   * @return array
   *   Sorted participant node IDs, as integers.
   */
  protected function extractEducationParticipants(array $data) {
    $participants = [];
    foreach ($data as $sessions) {
      foreach ($sessions as $session) {
        foreach ($session['participating_patients'] as $participant) {
          $participants[] = (int) $participant;
        }
      }
    }
    sort($participants);

    return $participants;
  }

  /**
   * Return the first encounter from a program *_data stats structure.
   *
   * @param array $data
   *   A stats builder result: a list of participant items, each with an
   *   'encounters' list.
   *
   * @return array
   *   The first participant's first encounter, or an empty array if none.
   */
  protected function extractFirstStatsEncounter(array $data) {
    $participant = reset($data);
    if (empty($participant) || empty($participant['encounters'])) {
      return [];
    }

    return reset($participant['encounters']);
  }

  /**
   * Create a fresh HC + FBF clinic and point the test instance at them.
   *
   * Used between scenarios in testStatsCalculations to keep each
   * scenario's data disjoint -- the hedley_stats functions all scope
   * by HC id, so a new HC is sufficient for isolation.
   */
  protected function resetHealthCenter() {
    $this->healthCenterId = $this->createHealthCenter();
    $this->clinicId = $this->createClinic($this->healthCenterId, 'fbf');
  }

  /**
   * Create sessions with attendance.
   *
   * @param int $attendance_last_year
   *   The number of attendances last year.
   * @param int $attendance_this_year
   *   The number of attendances this year.
   * @param string $last_year_session_date
   *   The date in string for last year's session.
   * @param string $this_year_session_date
   *   The date in string for this year's session.
   * @param string|bool $children_bd
   *   The birthday of the children created in the sessions.
   *
   * @return array
   *   The sessions created with all the data created related to the session.
   */
  protected function createSessionsWithAttendance($attendance_last_year, $attendance_this_year, $last_year_session_date = '-18 months', $this_year_session_date = '-9 months', $children_bd = FALSE) {
    $sessions = [
      [
        'date' => strtotime($last_year_session_date),
        'number_of_attendance' => $attendance_last_year,
      ],
      [
        'date' => strtotime($this_year_session_date),
        'number_of_attendance' => $attendance_this_year,
      ],
    ];

    foreach ($sessions as $key => $session) {
      if ($session['number_of_attendance'] <= 0) {
        // No need to create any data if the number of attendance is 0.
        continue;
      }

      $session_nid = $this->createSession($this->clinicId, $session['date']);
      $this->nurse = $this->createNurse($this->clinicId);

      $children = [];
      for ($i = 1; $i <= $session['number_of_attendance']; $i++) {
        $mother_nid = $this->createMother($this->clinicId);
        $child_bd = $children_bd ? $children_bd : strtotime('-23 months');
        $child_nid = $this->createChild($mother_nid, $this->healthCenterId, $child_bd, strtotime('-23 months'));
        $children[] = $child_nid;

        // Create this because the health centers are assigned to "attendance"
        // by the "PMTCT Participant" connection.
        $this->createPMTCT($child_nid, $mother_nid, $this->clinicId, $session['date']);

        $this->createAttendance($this->healthCenterId, $session_nid, $this->nurse->uid, $child_nid, TRUE, $session['date']);
      }

      $sessions[$key]['session_data'] = [
        'session_nid' => $session_nid,
        'nurse_uid' => $this->nurse->uid,
        'children' => $children,
      ];
    }

    return $sessions;
  }

  /**
   * Load an AQ Item.
   *
   * @param string $item_title
   *   The AQ item unique title.
   *
   * @return mixed
   *   The loaded AQ item, or FALSE if none found.
   */
  protected function loadAqItemByTitle($item_title) {
    $aq_item_id = hedley_general_get_aq_item_by_title($item_title, TRUE);
    if (!$aq_item_id) {
      return FALSE;
    }

    entity_get_controller('advancedqueue_item')->resetCache();
    return entity_load_single('advancedqueue_item', $aq_item_id);
  }

  /**
   * Process AQ item.
   *
   * @param object $aq_item
   *   The AQ item.
   * @param array $queue_info_overrides
   *   Array that will override existing Queue info. Defaults to empty array.
   */
  protected function processAqItem($aq_item, array $queue_info_overrides = []) {
    $queues = advancedqueue_get_queues_info();

    $queue_name = $aq_item->name;
    $queue_info = $queue_info_overrides + $queues[$queue_name];
    $queue = DrupalQueue::get($queue_name);

    advancedqueue_process_item($queue, $queue_name, $queue_info, $aq_item);
  }

  /**
   * Create an individual Nutrition encounter with related measurements.
   *
   * @param int $timestamp
   *   The date of the encounter.
   * @param float $muac_value
   *   MUAC value to record.
   * @param string $nutrition_sign
   *   Nutrition sign value to record.
   * @param float $weight_value
   *   Weight value to record.
   *
   * @return array
   *   The created node IDs, keyed by 'encounter', 'muac', 'height', 'weight'
   *   and 'nutrition'.
   */
  protected function createIndividualNutritionEncounterData($timestamp = '-7 days', $muac_value = 11, $nutrition_sign = 'edema', $weight_value = 9) {
    $date = strtotime($timestamp);
    $mother_id = $this->createMother($this->clinicId);
    $child_id = $this->createChild($mother_id, $this->healthCenterId);
    $participant = $this->createIndividualParticipant($child_id, $date, 'nutrition');

    $encounter = (object) [
      'type' => 'nutrition_encounter',
      'field_individual_participant' => [
        LANGUAGE_NONE => [
          ['target_id' => $participant->nid],
        ],
      ],
      'field_nutrition_encounter_type' => [
        LANGUAGE_NONE => [
          ['value' => 'nurse'],
        ],
      ],
      'field_scheduled_date' => [
        LANGUAGE_NONE => [
          [
            'value' => date('Y-m-d', $date),
            'value2' => date('Y-m-d', $date),
          ],
        ],
      ],
      'field_shards' => [
        LANGUAGE_NONE => [
          ['target_id' => $this->healthCenterId],
        ],
      ],
    ];
    node_save($encounter);

    return [
      'encounter' => $encounter->nid,
      'muac' => $this->createMuac('nutrition_muac', $encounter->nid, $child_id, $muac_value, $date),
      'height' => $this->createHeight('nutrition_height', $encounter->nid, $child_id, 68, $date),
      'weight' => $this->createWeight('nutrition_weight', $encounter->nid, $child_id, $weight_value, $date),
      'nutrition' => $this->createNutrition('nutrition_nutrition', $encounter->nid, $child_id, $nutrition_sign, $date),
    ];
  }

  /**
   * Create group nutrition data (sessions, attendance, and measurements).
   */
  protected function createGroupNutritionData() {
    $measurements = [
      HEDLEY_STATS_MODERATE => [
        'weight' => 8,
        'height' => 75,
        'muac' => 12,
        'nutrition' => 'none',
      ],
      HEDLEY_STATS_SEVERE => [
        'weight' => 5,
        'height' => 72,
        'muac' => 11,
        'nutrition' => 'edema',
      ],
    ];

    $attendance = 40;
    $sessions = $this->createSessionsWithAttendance($attendance, $attendance);

    foreach ($sessions as $session) {
      if (empty($session['session_data'])) {
        continue;
      }

      $session_data = $session['session_data'];
      $session_nid = $session_data['session_nid'];
      $measurements_state = HEDLEY_STATS_MODERATE;

      foreach ($session_data['children'] as $child_nid) {
        $measurements_state = $measurements_state == HEDLEY_STATS_MODERATE ? HEDLEY_STATS_SEVERE : HEDLEY_STATS_MODERATE;
        $this->createWeight('weight', $session_nid, $child_nid, $measurements[$measurements_state]['weight'], $session['date']);
        $this->createHeight('height', $session_nid, $child_nid, $measurements[$measurements_state]['height'], $session['date']);
        $this->createMuac('muac', $session_nid, $child_nid, $measurements[$measurements_state]['muac'], $session['date']);
        $this->createNutrition('nutrition', $session_nid, $child_nid, $measurements[$measurements_state]['nutrition'], $session['date']);
      }
    }
  }

  /**
   * Create NCD encounter data to assert ncd_data stats.
   */
  protected function createNcdEncounterData() {
    $date = strtotime('-1 month');
    $mother_id = $this->createMother($this->clinicId);
    $participant = $this->createIndividualParticipant($mother_id, $date, 'ncd');

    $encounter = (object) [
      'type' => 'ncd_encounter',
      'title' => 'NCD encounter',
      'field_individual_participant' => [
        LANGUAGE_NONE => [
          ['target_id' => $participant->nid],
        ],
      ],
      'field_scheduled_date' => [
        LANGUAGE_NONE => [
          [
            'value' => date('Y-m-d', $date),
            'value2' => date('Y-m-d', $date),
          ],
        ],
      ],
      'field_shards' => [
        LANGUAGE_NONE => [
          ['target_id' => $this->healthCenterId],
        ],
      ],
    ];
    node_save($encounter);

    // Add NCD-related measurements/fields to ensure data presence.
    $this->createDangerSigns('ncd_danger_signs', $encounter->nid, $mother_id, $date);
    $this->createNcdSymptomReview($encounter->nid, $mother_id, $date);
    $this->createCoreExam('ncd_core_exam', $encounter->nid, $mother_id, $date);
    $this->createNcdVitals($encounter->nid, $mother_id, $date);
    $this->createNcdFamilyPlanning($encounter->nid, $mother_id, $date);
  }

  /**
   * Create acute illness encounter data to assert acute_illness_data stats.
   */
  protected function createAcuteIllnessEncounterData() {
    $date = strtotime('-2 weeks');
    $child_id = $this->createChild($this->clinicId);
    $participant = $this->createIndividualParticipant($child_id, $date, 'acute-illness');

    $encounter = (object) [
      'type' => 'acute_illness_encounter',
      'title' => 'Acute illness encounter',
      'field_individual_participant' => [
        LANGUAGE_NONE => [
          ['target_id' => $participant->nid],
        ],
      ],
      'field_scheduled_date' => [
        LANGUAGE_NONE => [
          [
            'value' => date('Y-m-d', $date),
            'value2' => date('Y-m-d', $date),
          ],
        ],
      ],
    ];
    node_save($encounter);

    $this->createAcuteIllnessSymptoms('symptoms_gi', $encounter->nid, $child_id, $date);
    $this->createAcuteIllnessSymptoms('symptoms_general', $encounter->nid, $child_id, $date);
    $this->createAcuteIllnessSymptoms('symptoms_respiratory', $encounter->nid, $child_id, $date);
    $this->createAcuteIllnessVitals($encounter->nid, $child_id, $date);
    $this->createAcuteIllnessCoreExam($encounter->nid, $child_id, $date);
    $this->createAcuteFindings($encounter->nid, $child_id, $date);
  }

  /**
   * Create prenatal encounter data to assert it's stats.
   */
  protected function createPrenatalEncounterData() {
    $date = strtotime('-1 week');
    $mother_id = $this->createMother($this->clinicId);
    $participant = $this->createIndividualParticipant($mother_id, $date, 'antenatal');

    $encounter = (object) [
      'type' => 'prenatal_encounter',
      'title' => 'Prenatal encounter',
      'field_individual_participant' => [
        LANGUAGE_NONE => [
          ['target_id' => $participant->nid],
        ],
      ],
      'field_scheduled_date' => [
        LANGUAGE_NONE => [
          [
            'value' => date('Y-m-d', $date),
            'value2' => date('Y-m-d', $date),
          ],
        ],
      ],
    ];
    node_save($encounter);

    $this->createPrenatalBreastfeeding($encounter->nid, $mother_id, $date);
    $this->createBreastExam($encounter->nid, $mother_id, $date);
    $this->createCoreExam('core_physical_exam', $encounter->nid, $mother_id, $date);
    $this->createDangerSigns('danger_signs', $encounter->nid, $mother_id, $date);
    $this->createPrenatalGuExam($encounter->nid, $mother_id, $date);
  }

  /**
   * Create child scoreboard encounter data to assert it's data stats.
   */
  protected function createChildScorecardEncounterData() {
    $date = strtotime('-3 weeks');
    $mother_id = $this->createMother($this->clinicId);
    $child_id = $this->createChild($mother_id, $this->healthCenterId);
    $participant = $this->createIndividualParticipant($child_id, $date, 'child-scoreboard');

    $encounter = (object) [
      'type' => 'child_scoreboard_encounter',
      'title' => 'Child scoreboard encounter',
      'field_individual_participant' => [
        LANGUAGE_NONE => [
          ['target_id' => $participant->nid],
        ],
      ],
      'field_scheduled_date' => [
        LANGUAGE_NONE => [
          [
            'value' => date('Y-m-d', $date),
            'value2' => date('Y-m-d', $date),
          ],
        ],
      ],
    ];
    node_save($encounter);

    $this->createChildScoreboardImmunization('child_scoreboard_bcg_iz', $encounter->nid, $child_id, $date);
    $this->createChildScoreboardImmunization('child_scoreboard_dtp_iz', $encounter->nid, $child_id, $date);
    $this->createNcda('child_scoreboard_ncda', $encounter->nid, $child_id, $date);
  }

  /**
   * Create well child encounter data to assert it's data stats.
   */
  protected function createWellChildEncounterData() {
    $date = strtotime('-4 weeks');
    $mother_id = $this->createMother($this->clinicId);
    $child_id = $this->createChild($mother_id, $this->healthCenterId);
    $participant = $this->createIndividualParticipant($child_id, $date, 'well-child');

    $encounter = (object) [
      'type' => 'well_child_encounter',
      'title' => 'Well child encounter',
      'field_individual_participant' => [
        LANGUAGE_NONE => [
          ['target_id' => $participant->nid],
        ],
      ],
      'field_scheduled_date' => [
        LANGUAGE_NONE => [
          [
            'value' => date('Y-m-d', $date),
            'value2' => date('Y-m-d', $date),
          ],
        ],
      ],
    ];
    node_save($encounter);

    $this->createHeight('well_child_height', $encounter->nid, $child_id, 80, $date);
    $this->createMuac('well_child_muac', $encounter->nid, $child_id, 13.5, $date);
    $this->createNutrition('well_child_nutrition', $encounter->nid, $child_id, 'none', $date);
    $this->createWeight('well_child_weight', $encounter->nid, $child_id, 12.1, $date);
    $this->createNcda('well_child_ncda', $encounter->nid, $child_id, $date);
  }

}
