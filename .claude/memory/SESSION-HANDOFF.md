---
name: session-handoff
description: Pointer to the live cursor for E-Heza improvement work — the real file is .claude/backlog/HANDOFF.md in the repo
metadata:
  type: project
---

# Session handoff — pointer, not a copy

The live cursor (what merged, what is open, the queue counts, the environment gotchas) is
`.claude/backlog/HANDOFF.md` in the repository. Read it first when resuming; do not restate its
contents here — this file exists only so a fresh station knows where to look.

The process lives in the repo's skills: `/run-backlog-discovery` (find + verify into the backlog) and
`/process-backlog` (present, build, ship). Layout of `.claude/backlog/` is in its `README.md`.
