---
name: code-review-improvement-ledger
description: Running ledger of all E-Heza code-review improvement proposals across rounds (done/parked/proposed) — read before any new "suggest N improvements" sweep so nothing is re-proposed
metadata:
  node_type: memory
  type: project
  originSessionId: 3a0f2003-dc62-444e-8130-8870758fa415
  modified: 2026-08-09T09:36:05.150Z
---

The user runs a recurring "review the code, suggest N improvements" exercise (no new features incoming; goal = robustness / readability / stability / speed / UX; **bugs are top priority**). They want a persistent record so already-suggested items — done **and** parked — are never re-pitched. **Append each new round here.** Related: [[improvement-1b-poison-batch-not-quick-fix]].

## Round 1 — June 2026 (session 3a0f2003)
Numbered proposal list #1–#5 + alt "B". User picked B first, parked rest.

| # | Proposal | Status |
|---|----------|--------|
| #1a | WhatsApp screenshot → wrong IndexedDB table (silent data loss) | ✅ Done (PR #1768, merged) |
| #1b | Poison-batch: one rejected record jams a shard's upload queue | ⛔ Parked — NOT a quick client fix; see [[improvement-1b-poison-batch-not-quick-fix]] |
| #1c | No HTTP timeout on sync uploads (stalled POST wedges lane) | ✅ Done (PR #1770, merged) |
| #2 | Make sync status visible + protect unsaved form data (UX/trust); incl. `beforeunload` handler + translate raw server errors | ⛔ Parked, untouched — biggest remaining user-facing item; #1b Option A folds into this |
| #3 | Replace O(n²) `AssocList`/`EverySet` with `elm/core` `Dict`/`Set`; move view→update computation; add `Html.Keyed` | ⛔ Parked, untouched — biggest client-perf lever |
| #4 | Eliminate N+1 / super-linear queries in reports & stats (Findings 1–7) | ✅ Done (PRs #1775/#1777/#1779/#1781/#1783/#1785/#1787, merged) |
| #5 | God-modules + 189× duplicated save-handler | ◐ Partial — save-handler extracted (PR #1772); module splits remain (Measurement/Utils.elm ~10.4k lines, Backend/Update.elm ~5k lines, boolean-blind clinical predicates) |
| B | Oracle-backed clinical-logic unit-test suite + wire elm-test into CI | ✅ Done (PR #1791 + follow-ups #1812/#1814/#1816) |

Infra side-quests done: CI build-order fix #1771, Copilot cleanup #1788, prenatal-EDD client fix #1789.
Round-1 low-confidence clinical flags (never core proposals, need a clinician's eye, NOT re-propose): `respiratoryRateElevatedByAge` returns False (non-alerting) when DOB unknown; `VaccineDTPStandalone` reads OPV3 date as proxy for DTP3.

## Round 2 — 2026-06-15 (this session) — 5 NEW proposals (proposed, awaiting user pick)
Found via 6 parallel review agents; do NOT re-propose. file:line refs are point-in-time, verify at implementation.

- **R2-1 — Cross-health-center IDOR (PHI breach).** `HedleyRestfulSync::getForHealthCenter($uuid)` (`hedley_restful/plugins/restful/node/sync/HedleyRestfulSync.class.php:217-301`) returns any HC's full patient data (persons, encounters, measurements) to ANY token-authenticated device — no device→HC scoping; `$account` fetched but unused. Every device already holds all HC UUIDs (health_center in HEDLEY_RESTFUL_ALL_DEVICES) → trivial enumeration. Amplified by 5-year tokens (`hedley.install:40`, P5Y). Module comment admits scoping was never done (:54-55). Fix: resolve account→device→assigned HCs, 403 on mismatch; revisit token lifetime. Security, M effort, HIGH confidence. **Strongest of the round.**
  - **STATUS: examined 2026-06-15, user DECLINED — do not re-propose as a quick fix.** Deeper investigation showed the original fix premise is wrong: there is NO device→HC binding in the data model (device node has only `field_pairing_code`; HC membership lives on the *nurse* via `field_health_centers`, and a device is shared across nurses by login). So enforcement must be PER-NURSE, but the server can't do it: (1) the token authenticates the device robot user, not a nurse; (2) the sync GET carries no nurse identity; (3) every nurse's PLAINTEXT pin (`HedleyRestfulNurses` exposes `field_pin_code`; client decodes `pin_code` as string for offline login) syncs to every device, so nurse identity is forgeable. Root tension: {any-nurse-any-device} + {offline login} + {least-privilege} can't all hold — today every device holds all nurses' plaintext PINs (max exposure). Options were A) per-nurse request scoping (client sends nurse UUID, server checks HC membership — raises bar, not airtight), B) token hardening (shorten 5yr token + revocation, safe/independent), C) provisioned devices (real least-privilege, sacrifices any-nurse-any-device, big). User wanted none of it for now. The 5-year token (`hedley.install:40`) remains as-is.
- **R2-2 — Storage-quota / silent save-failure handling.** Offline store fills → writes fail silently. `app.js:646-651` bulkPut catches only `Dexie.BulkError`, so `QuotaExceededError` escapes as unhandled rejection (Elm never told). `IndexDbSaveFailure` is an explicit no-op (`SyncManager/Update.elm:2321-2324`). Quota IS polled (`app.js:494-510`) but only displayed (`Pages/Device/View.elm:84-90`), no threshold/back-pressure. `storage.persist()` result ignored (`app.js:484-487`) → possible silent eviction of unsynced data. Fix: broaden catch + quota threshold banner + act on failure. Offline data-safety, M effort, HIGH confidence on code facts.
  - **STATUS: ✅ PR #1818 MERGED to develop (closed #1817).** Branch `storage-pressure-save-failures` (deleted on merge). Parts 1+2 of a 4-part plan done: (1) `app.js` bulkPut `.catch` broadened from `Dexie.BulkError`-only to catch-all + new `indexedDbErrorName()` helper that surfaces `QuotaExceededError` (even when Dexie wraps it); `sendIndexedDbSaveResult` now sends a `reason`. (2) Elm: `IndexDbSaveResult` gains `reason : Maybe String` (decoder uses `optional`, so old payloads still decode); new `IndexDbSaveError = Other String | StorageFull`; new `Model.lastSaveError : Maybe IndexDbSaveError`; pure classifier `SyncManager.Utils.indexDbSaveErrorFromReason`; `SavedAtIndexDbHandle` failure branch records the error instead of blind `noChange`, success branch clears it. 3 unit tests added to `SyncManager/Test.elm`. Verified: Main.elm compiles (548 modules), elm-format clean on all 5 Elm files, JS `node --check` OK, SyncManager tests 6/6. (Pre-existing on develop, NOT mine: `App/Test.elm` Flags staleness compile error; 4 `ZScore.Test` wfh boundary failures.) **Part 4 (eviction hardening via `persistentStorage`) still PENDING.** Part 3's banner shares a UI surface with round-1 #2 (sync-status visibility, parked).
  - **Part 3 ✅ DONE → PR #1820 MERGED to develop (closed #1819).** Branch `storage-pressure-quota-banner` (deleted on merge). Added `App.View.viewStorageWarning` injected into both page wrappers (`flexPageWrapper`/`oldPageWrapper`) so the banner shows on every screen: amber `ui warning message` when `usage*100 >= quota*90` (pure helper `storageAlmostFull`, threshold const `storageQuotaWarningThreshold=90`), red `ui error message` when `syncManager.lastSaveError == Just IndexDbSaveErrorStorageFull` (red takes priority). New `Translate` ids `StorageQuotaAlmostFull`/`StorageQuotaFull` (now translated in all 4 languages — EN/RW/KI/SO — best-effort, not native-reviewed). No SCSS (uses Semantic UI message classes + a `storage-warning` hook class). No unit test for the banner: needs a full `App.Model` and the helper `App/Test.elm` is pre-broken on develop (Flags staleness). **CI gotcha for next time:** `lint_elm_review` gates the heavy jobs; it flagged `NoPrematureLetComputation` (a `let` value used in only one branch) — run `npx elm-review` locally before pushing. Also: `gh pr checks` rows are `ci/circleci: <name>\t<state>` so parse state with `awk -F'\t' '{print $2}'` (whitespace-split misreads the name as the state), and `gh pr checks --watch` exit code is unreliable — verify with a snapshot.
- **R2-3 — `decodeEverySet` collapses whole clinical sign-set on one unknown value.** `Utils/Json.elm:52-59` wraps list-decode in `oneOf [decoder, succeed empty]`; `Json.Decode.list` fails on first bad element → entire set silently becomes empty. 148 uses in `Backend/Measurement/Decoder.elm` (nutrition/danger/breast-exam/CPE/lab signs). Offline clients lag server, so first new enum member ships → older clients drop the WHOLE set (record shows "no signs"); emptied set can be written back on next sync. Same pattern hides bad `deleted` flags. Fix: element-tolerant decode (per-element `oneOf`+`filterMap`), keep strict only where required. Clinical correctness, S–M effort, HIGH mechanism / MEDIUM trigger-freq.
  - **STATUS: ✅ IMPLEMENTED 2026-06-22 — issue #1829, PR #1830 (branch `1829-decode-everyset-element-tolerant`, base develop), CI running.** Fix in `Utils/Json.elm`: `decodeEverySet decoder = list (oneOf [ map Just decoder, succeed Nothing ]) |> map (List.filterMap identity >> EverySet.fromList) |> decodeWithFallback EverySet.empty`. Unknown elements drop individually, recognized survive, non-list still falls back to empty; signature unchanged so all 148 call sites untouched. New `client/src/elm/Utils/Json/Test.elm` (5 tests: known-kept / one-unknown-dropped-rest-kept / all-unknown→empty / empty→empty / non-list→empty). Local verify: elm-format clean, full `elm-test "src/elm/**/Test.elm"` = 2728 passed (incl. the 5), my files elm-review-clean. **Tooling gotcha:** host `elm-test` is too new (wants elm-explorations/test 2.x); use project-local `client/node_modules/.bin/elm-test` (0.19.1-revision6). Local `elm-review` 2.13.5 reports ~132 PRE-EXISTING errors in untouched files (Translate.elm etc.) — tooling-version drift, NOT real; CircleCI `lint_elm_review` stayed green/pending. `client/src/elm/Config.Deploy.elm` is a git-IGNORED local deploy artifact that breaks `elm-review` ("several modules named Config") — move it aside to run elm-review locally, then restore. The `decodeWithFallback`-on-boolean variant (bad `deleted` flags hidden) is NOT addressed by this PR — still open if wanted.
- **R2-4 — "Today" computed in UTC, not local time.** `Gizra/NominalDate.elm:83-93` `fromLocalDateTime = Date.fromPosix Time.utc` (contradicts own docstring); `currentTime` set from raw `Time.now`, no `Time.here`/zone anywhere. Rwanda/Burundi are UTC+2 → data entered 00:00–02:00 local stamps the PREVIOUS day on `dateMeasured`, encounter end/concluded dates, and date-keyed logic (vaccine due dates, EGA). Long-standing since 2019. Fix: capture `Time.here` once, thread zone into `fromLocalDateTime`. Clinical date-correctness, S–M effort, HIGH mechanism (blast radius = early-morning window).
  - **STATUS: ✅ IMPLEMENTED 2026-06-22 — issue #1831, PR #1832 (branch `1831-nominaldate-local-timezone`, base develop), CI running.** Approach A (chosen over site-fixed-offset Approach B). Changes: `App/Model.elm` gains `zone : Time.Zone` (init `Time.utc`) + `SetTimeZone Time.Zone` msg; `App/Update.elm` adds `Task.perform SetTimeZone Time.here` to init `Cmd.batch` (one-shot), a `SetTimeZone` handler, and `currentDate = fromLocalDateTime model.zone model.currentTime`; `Gizra/NominalDate.elm` → `fromLocalDateTime : Time.Zone -> Time.Posix -> NominalDate`. Threaded zone through ALL 7 call sites: App/Fetch, App/View (3 PinCode dispatches + Wellbeing + MessagingCenter), and the PinCode/MessagingCenter/Wellbeing views — **PinCode needed double threading** (`view` → top-level `viewLoggedInContent`). New `Gizra/NominalDate/Test.elm` (same Posix → 1970-01-01 UTC vs 1970-01-02 under customZone 120). **KEY LESSON: `elm-test` alone is NOT a full type-check** — it only compiles the test-reachable subgraph, so it MISSED the PinCode threading gap (undefined `zone` / unused param); only `elm make src/elm/Main.elm` (Success, 548 modules) caught it. Always run a full `elm make Main.elm` for app-wide signature changes, not just elm-test. Verified: elm make 548 modules, elm-test 2725 passed, elm-format clean, no new elm-review findings in touched files. **Out of scope (same root, left for follow-up):** other `Time.utc` uses — resilience-reminder timing in `Pages/PinCode/View.elm` (`Time.toHour Time.utc`, `resolveTomorrow8AmUTC`, `+8h`) and `Pages/MessagingCenter/Update.elm` reminder scheduling. Fix is forward-looking only (no migration of historically mis-stamped early-morning records).
- **R2-5 — Clinical-string translation gaps for Somali/Kirundi nurses.** `Translate.elm`: somali=Nothing ~48% (~1,439 genuine after excluding ~271 intentional unit symbols), kirundi=Nothing ~30%, incl. clinical/triage/danger strings shown only in English (e.g. low-risk-send-home triage, "High Risk for Preeclampsia", "Acute Malnutrition", referral instructions). Falls back to English at runtime so it's invisible in code review. Fix: tooling to audit+prioritize high-impact clinical strings, then native-speaker fills (QA by clinical staff). UX/patient-safety, M–L effort (content-heavy), counts have fallback caveat. **Strongest UX-axis pick** — swap in if a UX-flavored slot is preferred over R2-4.
  - **STATUS: examined 2026-06-22, plan proposed, user chose to HOLD — line up reviewers first. Do NOT build yet; do NOT re-propose as a quick fix.** Raw counts now: 1710 `somali = Nothing` + 1078 `kirundi = Nothing` of ~3544 keys (`TranslationSet` = `{ english, kinyarwanda, kirundi, somali }`, non-English Nothing → English fallback at runtime). Counts mix GENUINE gaps with INTENTIONAL unit-symbol Nothings (kg/cm/%/MUAC — convention: leave Nothing if identical to English); no marker distinguishes them. No existing translation tooling. **Core judgment (KEEP):** this is a content/process task, NOT a code fix — do NOT bulk machine-translate clinical strings; a fluent-but-wrong Kirundi danger-sign/triage/dosage string is MORE dangerous than the English fallback (nurse trusts it). Proposed 4-part plan when unblocked: (1) audit tool parsing Translate.elm → per-key Nothing inventory, bucket intentional-vs-genuine; (2) clinical-impact prioritization by mapping key→Pages module usage, tag Tier-1 clinical-decision strings (danger signs, triage send-home/refer, diagnosis names, referral/dosage); (3) translator worklist CSV for native Somali (Somalia) + Kirundi (Burundi) speakers w/ clinical QA; (4) CI guard so the curated Tier-1 set must stay non-Nothing + new clinical strings can't merge untranslated (makes the silent-fallback gap VISIBLE). Parts 1/2/4-scaffold are buildable WITHOUT translators; part 3 (the actual fill) is BLOCKED on native reviewer availability — **that availability is the unblock trigger.**

### Round-2 runners-up (also found, recorded so NOT re-proposed; promote if a slot opens)
- **Server-side referential-integrity validation.** No `hook_node_validate` in custom code; `field_attach_validate` ignores instance `required` → a non-conforming/old client can create orphaned measurements/encounters (missing parent FK). Current official Elm client won't trigger it (defense-in-depth). `HedleyRestfulSync::handleChanges()` ~:360-545. Backend integrity, M.
- **Migration date robustness.** `HedleyMigrateBase.php:193-249` date callbacks throw plain `Exception` (not `MigrateException`) outside the per-row try/catch → one bad CSV date aborts the whole migration, row neither logged nor skipped, re-run crashes identically; empty required dates silently coerced to ''. Migration robustness, S–M.
- **`fix-well-child-encounters-warnings.php` corrupts data.** Non-reindexed `unset()` + double-wrap (`:63-80`) → surviving warnings clobbered to 'none' or stored as invalid list-in-list. One-off correction script; may already have run (then needs an audit/repair query). S.
- **Stored XSS in admin tables.** `hedley_admin.module` `#theme=>'table'` cells unescaped (resilience survey :561-584 from synced device data; sync-state :427-446). Fix: `check_plain()`. S, MEDIUM confidence (real exploitability depends on whether upstream allows markup).
- **Service-worker version skew.** `dbVersion` triplicated as hand-synced literals (app.js:386, sw.js:34, backend constant) with only a comment guarding; stalled (non-erroring) SW update has no watchdog/recovery; `controllerchange` unconditionally reloads. Single-source the constant at build time. S–M.
- **Dependency hygiene.** Dual tracked lockfiles (package-lock.json + yarn.lock; CI uses npm). ~11 unused gulp-* devDeps; `npm audit` shows build-time vulns (@babel/traverse critical, braces/axios). Mostly devDependencies. S–M, low security impact.
- **A11y.** 31 `<img>` without alt; icon-only links/buttons without accessible names; MUAC color alert mostly OK (label is translated). S, low severity.
- **advancedqueue_release_timeout = 0** → items crashed mid-job stick in PROCESSING forever (no auto-release). Minor.

## Round 3 — 2026-06-22 — 5 NEW proposals (proposed, awaiting user pick)
Found via 5 parallel review agents (backend PHP, Elm sync, clinical calc, pages/state, perf/PWA). Clinical engines (EGA/EDD 280-day, vaccine schedule, z-score LMS, BP/anemia, Burundi MUAC mm→cm) verified LARGELY CORRECT — no high-severity clinical-math bug. file:line point-in-time, verify at implementation.

- **R3-1 — `decodeWithFallback` coerces unknown clinical ENUM values to a "no-finding" sentinel (silent false-negatives on version skew).** DISTINCT from R2-3 (which collapsed an EverySet to EMPTY); here the fallback is an affirmatively-WRONG clinical value. Per-element on load-bearing fields: `Backend/PrenatalEncounter/Decoder.elm:26` (→`NoPrenatalDiagnosis`), `Backend/NCDEncounter/Decoder.elm:26` (→`NoNCDDiagnosis`), `Backend/Measurement/Decoder.elm:1917/1928` (postpartum mother/child danger signs →`NoPostpartum…DangerSigns`), `:1299` (nutrition assessment →`NoNutritionAssessment`), `:4117` (COVID contact tracing →`[]`). Backend ships new/renamed enum before PWA updates → a real pre-eclampsia/diabetes/hemorrhage-danger-sign/malnutrition/contact-list silently becomes "none". Sibling `decodeDangerSign` (:1896) correctly `fail`s — proves intent. Fix: `fail` on unknown for clinical enums (surface via Rollbar sync path); keep `decodeWithFallback` only for genuinely tolerant fields (e.g. `deleted→False`). HIGH conf, patient-safety. **Strongest of the round.**
  - **STATUS: ✅ IMPLEMENTED 2026-06-22 — issue #1833, PR #1834 (branch `1833-clinical-enum-decode-tolerant`, base develop), CI running.** Scope: user chose **PATIENT-RECORD decoders ONLY** (dashboard-stats sites DEFERRED as a follow-up). Approach: **DROP, not fail** (agent suggested fail; rejected because failing the clinical-enum decode fails the whole entity → with R3-3 unfixed, stalls the sync lane). Two new self-contained helpers in `Utils/Json.elm`: `decodeListDroppingUnknown : Decoder a -> Decoder (List a)` and `decodeEverySetDroppingUnknown : Decoder a -> Decoder (EverySet a)` (element-by-element `oneOf [map Just decoder, succeed Nothing] |> filterMap`, non-list→empty). Applied to 5 sites: `PrenatalEncounter/Decoder.elm:26`, `NCDEncounter/Decoder.elm:26` (both `list (decodeWithFallback Sentinel d)` → `decodeListDroppingUnknown d`), `Measurement/Decoder.elm:3837/3838` (`decodeEverySet (decodeWithFallback Sentinel d)` → `decodeEverySetDroppingUnknown d`), `:4117` COVID contacts (`decodeWithFallback [] (list d)` → `decodeListDroppingUnknown d`). Also removed now-unused `list` from Prenatal/NCD Json.Decode imports. New `Utils/Json/Test.elm` (6 tests). **CORRECTED 2 of agent-5's cited sites that were NOT bugs:** postpartum danger signs (`:1905/1906` use `decodeEverySet d` + field-absent `optional` default — already tolerant) and nutrition assessment (`:1300` outer fallback handles MISSING FIELDS via `field`, not unknown enum values). **Independent of #1829/#1831** (open) — develop's `decodeEverySet` is still the old all-or-nothing one, so I did NOT rely on R2-3; self-contained helpers. (Both #1830 and #1834 add `Utils/Json/Test.elm` → trivial add/add merge to combine test sets, no logic conflict.) Verified: `elm make Main.elm` Success, elm-test 2729 passed (+6), elm-format clean, no new elm-review findings (Measurement:6/9 `exposing (..)` noise is pre-existing tooling drift). **Follow-up still open: the ~13 dashboard-stats sites in `Backend/Dashboard/Decoder.elm` (297-302/330/335/338/363/368/369/405) — same idiom, skews counts.**
- **R3-2 — Cross-patient demographic leak: abandoned create/edit-person form pre-fills the next patient.** `App/Model.elm:293` `createPersonPage` is a SINGLETON (not keyed by id); `ResetCreateForm`/`ResetEditForm` fire only on the SUCCESS path (`Backend/Update.elm:4442-4445/4632-4634`); neither `SetActivePage` nor `UrlChanged` (`App/Update.elm:1138-1183/1431-1483`) clears it; `applyDefaultValuesForPerson` (`Backend/Person/Form.elm:173-284`) is fill-if-empty so stale fields block DB refresh on re-open. Abandon "New Person" for A → "New Person" for B is pre-filled with A's data → wrong demographics saved under B; edit shows stale unsaved edits not the DB value. Fix: dispatch reset on nav INTO create/edit pages (mirror existing AcuteIllnessParticipantPage reset at App/Update.elm:1171-1175). HIGH conf.
  - **STATUS: Bug 1 (CREATE leak) ✅ IMPLEMENTED 2026-06-23 — issue #1837, PR #1838 (branch `1837-create-person-form-reset`, base develop), CI running.** Added a guarded `UserPage (CreatePersonPage _ _) -> if model.activePage == page then [] else [ResetCreateForm |> MsgPageCreatePerson |> MsgLoggedIn]` case to `App/Update.elm`'s `SetActivePage` extraMsgs (mirrors the AcuteIllnessParticipantPage reset). Scoped to SetActivePage (the registration entry point; on internal click activePage is pre-set so UrlChanged-guarding fails — SetActivePage is the right hook; browser-back-into-create isn't a new-registration path). Verified mechanism end-to-end: `applyDefaultValuesForPerson` (`Backend/Person/Form.elm`) is fill-if-empty AND for EditPerson uses the edited person as `maybeRelatedPerson` to seed identity fields (firstName/secondName/hmisNumber...) — so the agent's mechanism held once traced. elm make Success, 2734 tests, elm-format + elm-review clean. **Bug 2 (EDIT staleness) ✅ DONE 2026-06-23 — folded into the SAME PR #1838** (user asked to add it to the Bug-1 PR, not a separate one; issue #1837 + PR #1838 retitled/expanded to cover both). Added a guarded `UserPage (EditPersonPage id) -> ... ResetEditForm |> MsgPageEditPerson id |> MsgLoggedIn` case next to the create one. Mechanism: abandoned edits persist in `editPersonPages` Dict (keyed by PersonId, never cleared, reset only on PATCH success — confirmed `HandlePatchedPerson` `RemoteData.map` is success-only) and fill-if-empty blocks the DB refresh on re-open → stale unsaved edit shown as current + lost-update risk if the record was synced from another device. Reset-on-entry → view re-seeds from DB. So **R3-2 is now FULLY CLOSED** in one PR. **Lesson: `gh pr edit`/`gh issue edit` work fine? — no, used `gh api repos/.../{issues,pulls}/N -X PATCH --input -` with `jq -n --arg t ... --rawfile b file '{title:$t,body:$b}'` to safely set title+multiline-markdown body (gh pr edit is broken on this repo per [[gh-pr-edit-projectcards-workaround]]).**
- **R3-3 — Download-side poison batch: one un-decodable entity in an /api/sync response jams that HC's lane forever.** DISTINCT from #1b (upload-side). `SyncManager/Decoder.elm:432`(authority)/`:350`(general) decode `required "batch" (list <| decode…Entity)`; entity catch-all `fail`s on unknown type (`:1510-1511`/`:411-412`). Elm `list` all-or-nothing → whole response `Failure` → `lastFetchedRevisionId` advances only in the post-save handler (`SyncManager/Update.elm:270-287`) which never runs → identical poison batch re-requested every cycle forever. Trigger: backend introduces a new entity TYPE (or new enum) before clients update (routine deploy ordering). Confirmed independently by 2 agents. Fix: element-tolerant batch decode (skip+log unknowns) AND advance cursor past skipped vids. HIGH / MED-HIGH conf.
  - **STATUS: examined 2026-06-23, DECLINED — do not re-propose.** It's a transient download STALL, **not** data loss: the cursor freezes at the pre-poison position and on app-update the lane resumes and pulls the previously-undecodable entity + everything after (nothing lost, just delayed). User: clients are expected to be updated, so a poison from an un-updated client self-resolves on update — not worth a client fix. Client-side skip is unsafe anyway (**FK-dangling**: a kept entity, e.g. participant, can point to a skipped known-type entity, e.g. person — same FK problem as upload #1b). Only an UNKNOWN-new-type skip would be safe (new types are leaves, referenced by known entities only via fields the client's decoder ignores), but not worth it. Proper fix if ever needed = **server-side `db_version` filtering** (download analog of #1b Option C). **META-PATTERN (important for future sweeps): all four SyncManager "poison/loop" findings — #1b upload poison, photo-incident loop, photo back-off, R3-3 download poison — have been DECLINED.** The sync layer's apparent bugs are guarded by operational reality (clients update; failures are transient/self-resolving, not data loss) and architectural invariants (FIFO + FK ordering, photos-encode-before-nodes via `replacePhotoWithFileId`, cursor-advances-only-on-save). Be skeptical of new sync-poison/stall proposals; favor non-sync findings (state bugs, backend data loss, perf).
- **R3-4 — `GROUP_CONCAT` participant/UUID lists silently truncate at MySQL's 1024-byte default → dropped participants.** `hedley_restful/plugins/restful/node/session/HedleyRestfulEducationSession.class.php:83/112` builds `GROUP_CONCAT(DISTINCT …field_uuid_value)` then `explode(',', …)`; `group_concat_max_len` never raised (confirmed no override). UUID+comma=37 bytes → truncates past ~27 UUIDs, severs last mid-string. `field_participating_patients` cardinality=-1. Same pattern in `HedleyRestfulNurses.class.php:95/99` + 3 others. Group-education sessions >27 participants sync down with a malformed final UUID; participants silently dropped, no error. Fix: `SET SESSION group_concat_max_len` large, or per-entity multi-value fetch. HIGH conf.
  - **STATUS: examined 2026-06-23 — MOOT IN PRODUCTION, declined.** Checked the LIVE value via `terminus drush ihangane.live -- sqlq "SELECT @@global.group_concat_max_len, @@session.group_concat_max_len"` → **1048576 (1 MiB)**, both global+session. Pantheon overrides it platform-wide (1024² = standard Pantheon MySQL default). At 1 MiB the UUID cap is ~28,300 UUIDs — no realistic session/nurse-roster/topic-set/stats aggregation comes close, so the truncation NEVER fires in prod (Rwanda; vhw/Burundi runs on the same Pantheon platform → same value, not re-checked). The agent was right that the REPO has no override, but the effective value is set by Pantheon's MySQL config OUTSIDE the repo. Also checked LOCAL ddev (`ddev mysql -e "SELECT @@global.group_concat_max_len"`) → **ALSO 1048576**, MariaDB 10.5.29. ROOT CAUSE of the bad finding: the agent assumed the default is 1024, but **MariaDB's default `group_concat_max_len` is 1048576 (raised from 1024 in MariaDB 10.2.4); only MySQL still defaults to 1024.** E-Heza runs MariaDB EVERYWHERE (ddev + Pantheon), so the 1024 premise never applied — truncation manifests in NO real environment (prod, local, or CI-on-MariaDB). NOT even a dev-parity tweak needed (local already = prod = 1 MiB). Only a plain-MySQL box (which E-Heza doesn't run) would hit 1024. **LESSON: for "default MySQL/PHP/server-limit" findings, check the actual RUNTIME value (terminus for prod, `ddev mysql` for local) before proposing — AND MySQL≠MariaDB defaults differ (group_concat_max_len is one). Audit also found the pattern is systemic (~40 GROUP_CONCAT→explode sites incl. HedleyRestfulNurses health_centers/villages, counseling topics, ~30 hedley_stats) — all equally moot at 1 MiB.**
- **R3-5 — Production `Debug.toString` blocks `elm make --optimize`; the whole app ships UN-optimized.** Any `Debug.*` ref makes `--optimize` refuse to compile; gulp `elm` task (`gulpfile.js:311-317`) never passes optimize. Calls: `Pages/Dashboard/View.elm:598`(per-cell render loop)`/2223`, `Pages/Dashboard/GraphUtils.elm:117`, `Pages/Prenatal/Activity/View.elm:4362`, `SyncManager/View.elm:214/302/1011`. Shipped Main.js (embeds ~1.3MB Translate.elm) gets no DCE/field-shortening → bigger bundle + slower on low-end field tablets; also CLAUDE.md "no Debug in committed Elm" violation. Fix: replace each with explicit `*ToString`/`classFor` (small custom-type sets), then enable `optimize:true` for publish. HIGH conf on blocker. **Only strong SPEED-axis pick.**
  - **Scope correction (2026-06-23/24): `elm make --optimize` authoritatively lists 5 blocking modules** — `Pages.Dashboard.GraphUtils`, `Pages.Dashboard.View`, `Pages.Prenatal.Activity.View`, `SyncManager.View` (the `Debug.toString`), PLUS **`Utils.AllDict` (7 `Debug.todo`)** which the agent missed. So R3-5 = **Part A** (Debug.toString in 4 views) + **Part B** (Debug.todo in AllDict — the gate AND the risk: it's a near-verbatim old elm/core red-black-tree copy, `RBNode_elm_builtin`; some todos are trivial-return-a-node, but `max`-on-empty returns `(k,v)` and `reportRemBug` returns `a`, which Elm can't express without a bottom → small restructure; `min` is dead, delete) + **Part C** (gulp `optimize:true` for PUBLISH only — the single `elm` task at gulpfile.js:311 is shared by dev+publish via publish→build→pwa:dev→elm, so needs a prod variant/env-flag, not a blanket flip). 1MiB-MariaDB-style note: --optimize ≈ 10-20% bundle shrink + faster on low-end tablets; worth it but AllDict carries risk.
  - **STATUS: Part A ✅ IMPLEMENTED 2026-06-24 — issue #1839, PR #1840 (branch `1839-remove-debug-tostring-views`, base develop), CI running.** Replaced 5 Debug.toString: `nutritionStatusToClass` (Dashboard/View cell class), `encodeFamilyPlanningSignAsString` reused as sort key (Dashboard/View:2223), `monthToString` (GraphUtils axis, preserves Jan-Dec; widened `import Time exposing (Month(..))`), specialized polymorphic `viewNumberInput : Maybe a` → `Maybe Int` + String.fromInt (Prenatal/Activity/View; all callers pass Maybe Int via SetOBIntInput), `Utils.WebData.viewErrorForRollbar` reused for the 2 Http.Error renders + new `syncStatusToString` (SyncManager/View dev view). Verified: no Debug.* left in the 4 modules; `elm make Main.elm` Success; **`elm make --optimize` now blocks ONLY on Utils.AllDict** (proves Part A cleared its modules); elm-test 2734; elm-format clean; no new elm-review (the 2 Backend.Entities hits are pre-existing wildcard noise). Did NOT enable --optimize. **Part B (AllDict Debug.todo) + Part C (gulp optimize) PARKED 2026-06-24** (user: park it, proceed to Round 4). **Part B plan fully worked out this session (no behavior change — all 7 todos are unreachable guards):** (1) `min` empty:182 → DELETE the whole `min` fn (dead: unexported, only self-recursive); (2) `moreBlack` BBlack:448 → `BBlack -> BBlack`; (3) `lessBlack` NBlack:464 → `NBlack -> NBlack`; (4) `rem` :512 → `_ -> RBEmpty_elm_builtin LBlack ord` (ord in scope); (5) `redden` :659 → `RBEmpty_elm_builtin _ _ -> t`; (6) `reportRemBug` calls :520/:528 → `_ -> r` / `_ -> l` (in-scope subtrees), then DELETE `reportRemBug` (+ `showLColor`/`showNColor` if orphaned); (7) `max` empty:195 (returns `(k,v)`, no valid empty value) → replace `max` with `maxHelper key value right` (base = `(key,value)`, recurse on right), call site :540 `max l_` → `maxHelper kl vl rl`. Verify: `elm make --optimize` goes GREEN + elm-test (ZScore exercises AllDict). **Part C:** gulp `elm` task (gulpfile.js:311) is shared dev+publish (publish→build→pwa:dev→elm) → add a prod-only optimized variant/env-flag, don't blanket-flip (would slow dev).
  - **⚠️ MAJOR CORRECTION 2026-06-28 (Copilot review of #1840 + DEFINITIVE clean-cache verify): "blocks ONLY on Utils.AllDict" was WRONG, twice over.** (1) Part A MISSED a Debug usage in Dashboard/View.elm — a BARE `toString` via `import Debug exposing (toString)` (line 43) + `toString percentage` in `viewPieChartLegend` (:2326) — my grep `Debug\.` only matched QUALIFIED `Debug.`, and my `--optimize` verification grep `-iE "Debug|AllDict"` FILTERED OUT the `Pages.Dashboard.View` blocker line (it contains neither "Debug" nor "AllDict"). So BOTH my Debug-search AND my --optimize check were blind to it. Copilot caught it. (2) **After fixing it (`toString percentage`→`String.fromInt percentage`, drop the import; pushed to #1840 as a follow-up commit), a FULL CLEAN-CACHE recompile of `elm make Main.elm --optimize` SUCCEEDS — 548 modules, exit 0, ZERO blockers.** Therefore **`Utils.AllDict`'s 7 `Debug.todo` do NOT block --optimize** — they're in dead/unreachable code (min unexported+self-recursive; max/rem/redden helpers DCE'd) so Elm's dead-code elimination drops them. **⇒ PART B (the whole AllDict surgery worked out above) IS UNNECESSARY. R3-5 is effectively DONE at the Elm level after this Copilot follow-up; only Part C (gulp wiring to actually pass `--optimize` in the publish build) remains if we ever want to enable it.** LESSON: never verify a "no Debug remains" / "--optimize blockers" claim with a keyword-filtered grep — it can hide blocker module names; read the FULL list or just trust the exit code of a clean `elm make --optimize`.

## Round 4 — 2026-06-24 — 5 NEW proposals (proposed, awaiting user pick)
Found via 5 parallel agents (backend queue/cron/deletion, clinical-calc, Elm state/UX, encode/decode [rate-limited 1st run, re-run clean], perf/PWA/memory). ~17 candidates surfaced; these are the strongest. file:line point-in-time.

- **R4-1 — Prenatal Labs-History: HIV / Partner-HIV / Malaria deferred-result entry is SILENTLY DEAD.** The view was extended (commit `f91716f83`) to render full forms + Save for these 3 deferred labs, but `updateLabsHistory` (`Pages/Prenatal/RecurrentActivity/Update.elm:1217-1671`) was NOT extended — all 9 `Set{HIV,PartnerHIV,Malaria}TestResult`/`Save*Result` messages fall through the stale catch-all `_ -> (data, Cmd.none, [])` (:1669-1671, comment falsely says "not related to Labs History"). Nurse types an HIV/malaria result + hits Save → typed fields revert, Save is a no-op, screen doesn't advance (the SetActivePage redirect is in the dropped extraMsgs). Pending result can NEVER be completed there. HIGH conf, clinically significant (antenatal HIV/malaria). Fix: add the Save/Set branches mirroring the existing test branches; make the catch-all explicit so future view additions fail to compile. **Strongest of the round.**
  - **Reachability CONFIRMED (user pushed back: "maybe these 3 can't be left for later?").** Checked the recording form: all three CAN be deferred. `pendingLabs = performedTests − completedTests` (`generatePendingLabsFromPreviousEncounters`, Activity/Utils.elm:5537). HIV → `viewHIVTestUniversalForm` (universal). Malaria → `viewMalariaTestForm` and PartnerHIV → `viewPartnerHIVTestForm` BOTH run `contentAndTasksLaboratoryUniversalTestInitial` + a `prerequisiteByImmediateResultInputsAndTasks` **`immediateResult` toggle** (Measurement/Utils.elm:3984/3994 malaria, 4235/4240 partnerHIV): `immediateResult=False` = "done at lab" → performed-not-completed → pending → reaches Labs-History. (Correction logged: I'd initially guessed Malaria was RDT-only because it's absent from the lab-tech `testsWithFollowUp` list at Backend/Update.elm:9027 — WRONG inference; that list is only about nurse follow-up questions, not deferrability. Verify the FORM, not the follow-up list.)
  - **STATUS: ✅ IMPLEMENTED 2026-06-24 — issue #1842, PR #1843 (branch `r4-1-labs-history-hiv-malaria`, base develop), CI running.** Added 9 branches to `updateLabsHistory` (Update.elm): per test {Set result, Set bool/blood-smear, Save}. Labs-history `data` IS the LabResultsData → Set handlers use `data.{hiv,partnerHIV,malaria}TestForm` directly; Save handlers mirror Syphilis (`to<X>ResultValueWithDefault [isLabTech] measurement form` → `Save<X>Test personId measurementId` >> MsgPrenatalEncounter labEncounterId >> MsgIndexedDb, then `sequenceExtra … extraMsgs` to redirect to the Laboratory page). **Gotcha: these 3 forms have NO `originatingEncounter` field** (Syphilis-only) → compile error until dropped; the save updates the existing measurement by `measurementId` anyway. Malaria's `toMalariaResultValueWithDefault` takes NO isLabTech (HIV/PartnerHIV do). Also tightened the silent `_ ->` catch-all comment. Verified: elm make Main Success, elm-test 2734, format clean, no new review (Backend.Entities :5 is pre-existing develop noise). Did NOT attempt full explicit-enumeration of the catch-all (shared 100+ ctor Msg type — impractical).
- **R4-2 — Restoring (un-deleting) a person does NOT restore its descendants → permanent hidden clinical-data loss.** `hedley_person.module:1307-1341`: the encounter/measurement cascade branches are gated `&& $deleted` (only run on DELETE); the person branch resolves children via `hedley_general_get_person_content_associated_by_fields` which applies `hedley_general_apply_exclude_deleted` (excludes `field_deleted=1`). So flipping a person's `field_deleted` back to FALSE leaves every cascaded-deleted participant/encounter/measurement still deleted+hidden+unsynced, forever. Undo silently loses the whole subtree. HIGH conf. Fix: on restore, resolve children WITHOUT the exclude-deleted filter and propagate `field_deleted=FALSE` through the same recursion (ideally batched, see runner-up B2). **STATUS: PARKED 2026-06-24 (user).**
- **R4-3 — Dashboard encoder writes the STUNTING z-score into the `zscore_underweight` field (×3) → corrupted underweight nutrition KPI.** `Backend/Dashboard/Encoder.elm:420-423/508-511/554-557` read `item.zscoreStunting` while emitting `( "zscore_underweight", ... )`; decoder (`Backend/Dashboard/Decoder.elm:412/466/484`) correctly reads it into `zscoreUnderweight`. So the underweight metric is silently overwritten by stunting. Flows via `SyncManager/Utils.elm:1354` (encodeDashboardStatsRaw, used for local cache) into `Pages/Dashboard/View.elm`. Bug since `5d4600e6c` (2023-12-31). HIGH conf, clean 3-line fix (`item.zscoreStunting` → `item.zscoreUnderweight`).
  - **STATUS: ✅ IMPLEMENTED 2026-06-24 — issue #1844, PR #1845 (branch `r4-3-dashboard-zscore-underweight`, base develop), CI running.** Verified on develop: each fn has BOTH a `zscoreStunting` block (`"zscore_stunting"` ← item.zscoreStunting, CORRECT, lines 415-417/503-505/549-551) and a `zscoreUnderweight` block (`"zscore_underweight"` ← item.zscoreStunting, BUG, lines 420-422/508-510/554-556). Fixed via `replace_all` on the identical 3-line buggy block → `item.zscoreUnderweight` (3 sites; stunting blocks untouched). elm make Main Success, elm-test 2734, format clean. Pure field swap, no import/review impact.
- **R4-4 — ~43 per-page `*Pages` Dicts are insert-only (never pruned) → session-long memory growth + O(n) AssocList lookups.** `App/Model.elm:296-348`: `prenatalEncounterPages`/`prenatalActivityPages`/`patientRecordPages`/etc. populated only via `Dict.insert`; ZERO `Dict.remove`/`Dict.filter` anywhere in `App/`; `SetActivePage` (App/Update.elm:1138) never evicts the page being left; only reset to `Dict.empty` at login. Every encounter/activity/person/report visited in a shift stays in the (AssocList-backed) heap until logout — memory grows unbounded AND every get/insert becomes O(pages-ever-visited). (Backend WebData caches ARE pruned via App.Fetch.forget — these page models aren't.) HIGH conf on mechanism, MED on heap magnitude. Fix: `Dict.remove` the left page's key on SetActivePage, or a small LRU. **STATUS: PARKED 2026-06-24 (user)** — never had a field complaint; judged not a real-world problem in practice (devices reloaded/repaired often enough; theoretical, not observed). Mechanism is real if it ever surfaces, but not worth the eviction-policy risk now.
- **R4-5 — COVID-19 pneumonia diagnosis operator-precedence bug.** `Pages/AcuteIllness/Activity/Utils.elm:2519-2525`: `BP || (resp>0) || (general>0) && crackles` — Elm `&&` binds tighter than `||`, so `crackles` (lung exam) gates ONLY the general-symptoms term. A single respiratory symptom (bare cough) w/o crackles → DiagnosisPneuminialCovid19; two general symptoms w/o crackles → LowRisk. Arbitrary asymmetry, pinned by tests (Test.elm:722/731), traceable to rewrite `8719bf0c`. MED conf (precedence definite; intended rule needs clinician). Fix: parenthesize, e.g. `BP || (((resp>0)||(general>0)) && crackles)`; update Test.elm:722.
  - **DOWNGRADED 2026-06-24 after elaboration (verified the tests + git history): likely INTENDED, not a bug — confidence now LOW-as-bug.** The asymmetry is DELIBERATELY pinned by two tests in `Pages/AcuteIllness/Activity/Test.elm` (~722 "respiratory Cough + normal BP + positive COVID, no crackles → PneuminialCovid19"; ~731 "two general signs + no respiratory + no crackles → LowRiskCovid19"). Git `8719bf0c` "Align conditions to determine Covid case type" deliberately rewrote an earlier all-`&&` chain (`BP && stabbingChestPain && (general>0)`, with a `@todo: revise` + commented-out respiratory clause) into the OR chain. Plausible clinical rationale: respiratory symptoms = direct lower-airway involvement → pneumonia on their own; general symptoms (chills/night-sweats) = non-specific → only count toward pneumonia WITH crackles. So it may be intentional. **Real residual issue = readability/fragility: a clinical decision relies on implicit `&&`>`||` precedence.** If intended → parenthesize intent-preservingly `BP || (resp>0) || ((general>0) && crackles)` (no behavior change, tests still pass). If NOT intended (crackles should gate both) → `BP || (((resp>0)||(general>0)) && crackles)` + update both tests + clinical sign-off. NEEDS A CLINICIAN to rule on intent before any behavior change. **STATUS: PARKED 2026-06-24 (user)** — pending clinician ruling.

### Round-4 runners-up (recorded, do NOT re-propose; promote if a slot opens)
- **Duplicate `"deleted"` key in 3 measurement encoders** (`Encoder.elm:880` ultrasound, `:1432` medicalHistory, `:2002` symptomsRespiratory): `encodeMeasurement` already emits `("deleted", measurement.deleted)` but these value-encoders ALSO emit `("deleted", bool False)`; Json.Encode.object lets the LAST win → uploads `deleted:false`, resurrecting soft-deleted records. HIGH conf (empirically tested), MED severity. 3-line fix. **Nearly top-5.**
- **Unbatched soft-delete cascade** (`hedley_person.module:1296-1342`): synchronous multi-level node_load+save recursion, no Batch API/queue; high-volume patient can time out mid-cascade → half-deleted tree. HIGH mechanism, MED that datasets are large enough.
- **Stats recalc non-atomic** (`hedley_stats.module:649-691`): clears master hash first, writes last; any of 16 sub-calcs throwing leaves half-cleared state; serve path returns `[]` if ANY metric cache NULL → dashboard blanks for that HC until a full retry (dies after 3). MED-HIGH.
- **Stock Management signature/form leak across nav** (`App/Update.elm:977-991`, `Pages/StockManagement/Update.elm:36-40,292,457`): global `stockManagementPage`, reset only on save success, no nav reset → stale signature + confirmIdentity persist; a stock record can save with nurse B's id but nurse A's signature (audit hole). SAME CLASS as R3-2 (#1838) but a different page. MED-HIGH. Fix: reset on entry, same SetActivePage hook.
- **Shard-blind IndexedDB queries** (`nodes.js:627-629` followups, `:776-777`/`:830-831` stock): query `.where('type')` then filter `item.shard===shard` in JS; no `[type+shard]` compound index (app.js:324-325) → multi-HC tablet scans every shard's records of those types per fetch. HIGH that index is missing, MED magnitude (only multi-HC devices). Fix: add `[type+shard]` compound index (new dbVersion).
- **/api/file-upload: no extension/size limits** (`files_upload__1.1.inc`, RestfulFilesUpload.php:188-207): empty `file_validate_extensions`/`file_validate_size` → any paired device writes unlimited arbitrary-type files to private:// (not RCE; exec neutralized to .txt). MED.
- **Clinical number REST fields no range validation** (`HedleyRestfulSync.class.php:485-487`; no entity_validator plugins anywhere; `node-vitals-field_body_temperature` min/max both `''`): device can POST HR `-99999`/temp `1e9`, stored verbatim, feeds z-score/reporting. MED.
- **NCDA scorecard z-score coloring strict vs assessment inclusive** (`WellChild/ProgressReport/View.elm:2730-2738` uses `<-3/<-2` vs `NutritionEncounter/Utils.elm:680/697` `<=-3/<=-2`): disagree at exactly -3.0/-2.0. HIGH inconsistency, MED-LOW incidence (continuous z rarely lands on boundary).
- **Severe-preeclampsia BP uses AND vs OR** (`Prenatal/Activity/Utils.elm:3269/3292/3299` `dia>=110 && sys>=160`) where app/WHO use OR; MITIGATED — single-param severe BP still caught one tier lower (Moderate Preeclampsia via OR-based highBloodPressure). MED, narrow.
- **Double-tap "begin encounter" → duplicate encounters** (individual participant views, e.g. `NCD/Participant/View.elm:137-181`; no in-flight `postXEncounter` Loading guard — group path HAS one). MED-LOW.
- **errorsHash dedup table never pruned** (`app.js:280,1786-1801`) — insert-only, LOW.
- **Unmemoized dashboard/GlobalCaseManagement aggregation in view** (Dashboard/View case-mgmt/child-wellness tables; GlobalCaseManagement/View viewContentForChw — 7 generate*FollowUps + fillPersonName per render, no Html.Lazy). MED; partly overlaps declared "view→update computation".
- **advancedqueue cron deletes by `created<6mo` with NO status filter** (`hedley_general.module:564-567`) — can drop a still-QUEUED/PROCESSING item. MED, low hit-rate.
- **hedley_view_export requeue path missing `switchUserBack`** (`hedley_view_export.module:254-258`) — global user stays switched to export creator for subsequent queue items in the same cron pass (identity/privilege bleed). MED.

### Round-4 cleared-clean (verified OK; don't re-chase)
- ALL 28 Backend Encoder/Decoder **enum-string pairs round-trip cleanly** (exhaustive extract+diff) — no constructor encoded as X but decoded only from Y; symptom Dict 0-filtering is by-design; sentinel-set encodings stable.
- Clinical engines re-verified clean: z-score LMS + day/month fallback + ±3 clamp, dosing tables (Coartem/ORS/Zinc/Amoxicillin contiguous), EGA/EDD, vaccine intervals, blood-sugar units (mg/dL, RBS>=200, fasting>126 pinned-by-design), NCD staging, creatinine, BMI cm→m, MUAC.
- Group session measurement forms strictly keyed by PersonId (no cross-entity save); `resolveActiveTask`/`List.head` prefetch sound.
- Backend WebData id-caches ARE pruned via `App.Fetch.forget`.
- Refuted false-positives: entityreference wrong-bundle writes (caught by entityreference_field_validate); name-field stored-XSS (core escapes on render); "nutrition assessment always NoNutritionAssessment via custom" (false — decodeNutritionAssessment reads its own field).

## Round 5 — 2026-06-24 — proposals (proposed, awaiting user pick)
5 parallel agents over DELIBERATELY under-swept lenses (i18n correctness, date/number, under-swept backend modules [hedley_reports/migrate/queue/pairing], PWA/SW/build, non-IDOR security). Richest round — report SQL/PHP + Translate table + device/sync security never audited before. ~17 candidates. file:line point-in-time.

### Top 5 proposals
- **R5-1 — Clinical dosing/regimen TRANSLATION errors (patient safety).** All in `client/src/elm/Translate.elm`, all OBJECTIVELY provable (cross-language numeric disagreement or byte-identical copy-paste of a neighbor — NOT translation-quality judgment, so distinct from the R2-5 native-reviewer hold): (a) **`:12567` Ciprofloxacin 1000mg renders as "100mg" in SOMALI** — 10× under-dose (EN/RW/RN all 1000mg); (b) **`:26604` HIV regimen "TDF + 3TC" gains "na Dulutogaraviri (50mg)" in Kinyarwanda** — pasted from adjacent `dolutegravir` entry :26597, wrong ARV regimen; (c) **`:4370` "3×/day" reads "inshuro ebyiri" (twice) in Kinyarwanda** — byte-identical to the twice-a-day entry :4363; correct "inshuro 3" exists at :3317/19419/20433; renders for amoxicillin etc via `Pages/AcuteIllness/Activity/Utils.elm:1172`; (d) **`:4371` Kirundi drops the `++ String.fromInt days` duration** — only table entry missing an EN interpolation slot. Fixes (a)-(d) need NO native input (digit fix / remove contaminant / known-correct substitution / add interpolation). HIGH conf. **Headline — patient safety.**
  - **STATUS: ✅ IMPLEMENTED 2026-06-25 — issue #1846, PR #1847 (branch `r5-1-dosing-translation-fixes`, base develop), CI running.** Verified each against source before editing: (a) Somali `100mg`→`1000mg` pure digit; (b) removed `na Dulutogaraviri (50mg)` from the Kinyarwanda `arvs` entry → "Tenofoviri na Lamividine, ikinini kimwe ku munsi." (Kirundi already correct, EN authoritative); (c) Kinyarwanda three-times `inshuro ebyiri`→`inshuro 3`; (d) Kirundi three-times garbled `(Igitigiri)` placeholder + missing days → `"Kumira incuro 3 k'umunsi mu iminsi " ++ String.fromInt days` (reused the well-formed TWICE-entry Kirundi tail grammar). **Gotcha: the buggy three-times Kinyarwanda line was BYTE-IDENTICAL to the (correct) twice-a-day entry → could NOT use a unique single-line old_string; anchored the Edit on the differing Kirundi line. Twice-a-day entry left untouched (its ebyiri/incuro 2 are correct).** Edit tool needed a Read-tool read of each region first (sed views don't satisfy it). elm make Main Success, elm-test 2734, format clean. PR invites native RW/RN sanity-check on (b)-(d) but each is a strict improvement. **Follow-up commit (user-requested): lowercased all THREE Kirundi `ByMouth*ForXDays` leading verbs (Daily/Twice `Gucisha`→`gucisha`, ThreeTimes `Kumira`→`kumira`) — they render mid-phrase via `viewTabletsPrescription` so the leading capital was a stray mid-sentence capital vs the lowercase EN/RW/Somali. (Found there were 3 sibling entries, not 2.) Folded into #1847, CI re-run.**
- **R5-2 — Security: sync-role device token → in-app ADMIN escalation + cleartext PIN exposure.** (S1) `HedleyRestfulSync::handleChanges` forwards a `nurse` PATCH/POST's raw `data` with NO field allowlist; `pin_code`/`role` are writable public fields w/ no access_callbacks (`HedleyRestfulNurses.class.php:21,34`); `sync` role has `edit any nurse content` (`hedley_user.features.user_permission.inc`); presave validates PIN length but NOT role (`hedley_user.module:43-65`). So `{type:nurse,method:PATCH,uuid:<any>,data:{role:[admin],pin_code:9999}}` over normal sync → attacker becomes in-app Administrator / hijacks any nurse PIN. HIGH on write path. **IDOR-ADJACENT (same HedleyRestfulSync trust model the user DECLINED in R2-1) — BUT sharper (account-takeover) and the fix is feasible without device→HC binding (strip pin_code/role from device nurse writes).** (S3) `HedleyRestfulSync.class.php:511-528` sync-error catch concatenates every field key/value into the `watchdog` msg → cleartext nurse PIN in dblog on a failed nurse PATCH; clean low-risk fix = redact `pin_code`. (S2, architectural/likely-known: every nurse's cleartext 4-digit PIN syncs to every device — inherent to offline client-side PIN check; fix=salted hash or HC-scoped nurse sync.)
  - **STATUS: PARKED 2026-06-25 (user) — "nurse PINs are not very important to guard." Whole R5-2 (S1 escalation + S2 cleartext sync + S3 PIN-in-logs) parked. META: nurse-PIN/credential security and the HedleyRestfulSync device-trust model (IDOR R2-1, write-escalation R5-2) are a DECLINED CLASS — devices are trusted, PINs low-value; do NOT propose nurse-PIN/IDOR/device-trust security findings in future rounds.**
- **R5-3 — Nutrition report month-overflow: `strtotime("-N month")` drops & duplicates whole months on the 29th-31st.** TWO files, same bug class: `hedley_reports/hedley_reports.module:2562-2605` (the LIVE `nutrition_report_data` endpoint :2017 consumed by Elm dashboard) AND `hedley_admin/scripts/generate-nutrition-report.php:188/202/507`. PHP relative `-N month` overflows month-end (from May 31: keys = 05,03,03,01,12,12,10,10,08,07,07,05) → entire months silently omitted, duplicates collapse, and the incidence 3-month look-back includes the target month in its own baseline. Fires ~10% of calendar days (whenever run on 29-31). HIGH conf (reproduced in PHP). Fix: anchor to first-of-month (`new DateTime('first day of this month')->modify("-$i month")`). Quarter/year variants NOT affected.
  - **STATUS: ✅ IMPLEMENTED 2026-06-25 — issue #1848, PR #1849 (branch `r5-3-report-month-overflow`, base develop), CI running. SCOPE: hedley_reports.module ONLY (user: `generate-nutrition-report.php` is NOT in use — skipped).** Added helper `hedley_reports_current_month_start()` (`strtotime(date('Y-m-01'))`) and anchored FOUR steppers: `generate_monthly_prevalence_data`, `generate_monthly_incidence_data`, `generate_quarterly_incidence_data`, `resolve_data_set_for_quarter`. **CORRECTION to the agent's claim ("quarter NOT affected") — the QUARTERLY path IS affected:** `resolve_data_set_for_quarter` derives `[year,quarter]` from `strtotime("-3N months", time())`, so e.g. Dec 31 `-3 months` → Sep 31 → Oct 1 = Q4-not-Q3 → sums the WRONG quarter's 3 months (data corruption, not just label). The YEARLY functions only extract `date('Y')` → robust to day overflow → correctly left unchanged. **phpcs GOTCHA (important): phpcs SKIPS `.module` files unless `--extensions=php,module,inc,...` is passed (a bare `phpcs --standard=Drupal file.module` silently checks NOTHING and exits 0 — a FALSE pass). CI's ci-scripts/test_coder.sh passes the full extension list. Verified WITH extensions: Drupal + DrupalPractice both exit 0; php -l clean.** Also: this project's phpcs does NOT strictly enforce the 80-char LineLength (a 113-char line passed) — but I still broke the long resolver line with an `$anchor` local for cleanliness.
- **R5-4 — MUAC report keeps the WRONG (earliest) measurement per patient — wrong dedup key.** `hedley_admin/scripts/muac-report.php:113-114`: `$rows` is keyed by `field_person` (:122) but the recency guard reads `$rows[$row->nid][2]` (node-id key, virtually always unset) → `new DateTime(NULL)` = now → `now >= measured` always true → `continue` → never replaces → keeps first row seen; query orders by `node.nid` (:67) not date → retains lowest-nid (≈earliest) MUAC. Every patient with >1 MUAC in the 2-yr window (the normal case) gets a stale value+date in the export. HIGH conf. Fix: `isset($rows[$row->field_person])` and read `$rows[$row->field_person][2]`. **STATUS: PARKED 2026-06-25 (user) — the MUAC report is NOT in use, so no action.**
- **R5-5 — ANC "Active vs Completed pregnancies" predicate off by 60 days.** `hedley_admin/scripts/anc-report-active-pregnancies.SQL:19` (+ completed/-nurse/-chw variants): uses `date(edd) >= DATE_ADD(:limit, INTERVAL 30 DAY)` (limit+30) where the documented intent (`generate-anc-report.php:25-35`, "not >30 days past EDD") needs `EDD >= limit − 30`. Verified w/ the file's own example (limit Nov 1, EDD Nov 15 → wrongly Completed). Every pregnancy with `limit-30 ≤ EDD < limit+30` (a 60-day band of near-term/recently-due) is mis-bucketed Completed. HIGH conf. Fix: `date(edd) >= DATE_SUB(:limit, INTERVAL 30 DAY)` (DATE_ADD→DATE_SUB in 6 files: active/completed × any/nurse/chw). (District hard-coded 'Bugesera'.) **STATUS: PARKED 2026-06-25 (user) — the ANC report is NOT in use either, so no action. (META: both R5-4 MUAC and R5-5 ANC are in the `hedley_admin/scripts/*-report` family and turned out to be UNUSED — for future rounds, confirm a report script is actually run before proposing its fix as top-tier.)**

### Round-5 runners-up (recorded, do NOT re-propose; promote if a slot opens)
- **Concurrent view exports silently swallowed** (`hedley_view_export.module:171` hardcodes `$unique_id=0` → AQ dedup title always `export_view_0`): while any export is in flight, every other user's export is dropped (empty file created, "we'll email you" shown, no email). HIGH. Fix: per-export unique_id (fid/random).
- **Dashboard duplicate-person merge discards nutrition history** (`Pages/Dashboard/Utils.elm:305-316`): `mergeNutritionValueDicts candidate.X candidate.X` merges candidate with ITSELF; `current.nutrition` unused → cross-program-enrolled child loses accumulated months. HIGH bug, MED magnitude (needs cross-program dup). Fix: merge `current` with `candidate`.
- **i18n wrong-text/label errors (objective, HIGH conf, non-dosing)** all Translate.elm: `:6254` ECD milestone "4-5 words" asks "2-4" in Kinyarwanda; `:14958` prenatal "why not followed?" shows NotTaken's "no measurements taken" string in Kinyarwanda; `:11492` severity "Low" → Kinyarwanda "Confirm errors"; `:30499` November short-form = October's "Ukw" (should "Ugu"); + hardcoded English "Not Set" at `Pages/Prenatal/Activity/View.elm:471` (use Translate.EmptyString).
- **PWA `statistics.js:47` stale IndexedDB handle** — guards re-open with only `if (dbSync===null)` vs sibling `nodes.js:55` `|| dbSync.verno != dbVerno`; after a dbVersion bump mid-SW-life → stale/closed handle → VersionError → `/sw/statistics` 500 → dashboard broken till reload. MED. Fix: match nodes.js guard.
- **PWA `photos.js:46-53` returns `undefined` from respondWith on network error** (`var response` assigned in try, catch returns hoisted undefined) → mid-fetch drop faults the photo request instead of a clean 503 retry. HIGH mechanical, MED severity, offline path. Fix: return `new Response(null,{status:503})` in catch.
- **Dashboard NaN% on empty denominators** (`Pages/Dashboard/View.elm:1436,3028`): raw `/` bypasses the zero-guarded `calculatePercentage` (Pages/Utils.elm:220) → `round NaN` = "NaN%" on new HC / empty month. MED-HIGH cosmetic.
- **Device title→robot-user collision** (`hedley_device.module:81-98`): robot resolved by `$node->title.' Robot'` with no device-title uniqueness → two same-titled devices share one robot uid → pairing one invalidates the other's token + identity collision for sync/authority. MED/HIGH. Fix: resolve robot by device UUID/nid or enforce title uniqueness.
- **Pairing-code consume wipes code before token mint, no transaction** (`HedleyRestfulPairingCode.class.php:98-127`): wipes `field_pairing_code` (committed) then mints token (can throw RestfulServerConfigurationException) → transient failure permanently bricks self-service pairing (needs super_user_mode + admin recovery). MED. Fix: db_transaction() or mint-then-wipe.
- **Optional-signature stock_update dropped from sync via innerJoin** (`HedleyRestfulStockUpdate.class.php:124`): inner-joins file_managed on optional `field_signature` fid → signature-less/purged-file stock_updates entirely excluded from sync (and consume a 500-row slot) → device stock ledger diverges. MED (Elm always emits signature today). Fix: leftJoin.
- **PWA `controllerchange`→`location.reload()` no re-entrancy guard** (`app.js:1908-1926`): LOW/latent (no repro today).
- Lower-conf date items: ANC first-visit-by-trimester buckets non-exhaustive (drops EDD≤0 / ≥~320d) `anc-first-visit-all-pregnancies.SQL`; demographics 50yr boundary gap (lt50 `>` vs mt50 `<`, exact-50 falls in neither) `bootstrap-demographics-report.SQL:98/113`; ANC "Percentage" column prints raw fraction (no ×100) `generate-anc-report.php:85`; `decodeNutritionValueDict` month-key `Maybe.withDefault 1` collapses bad keys to January `Backend/Dashboard/Decoder.elm:124`.

### Round-5 cleared-clean (verified OK; don't re-chase)
- Security surface SOUND: pairing-code one-shot (deleted on use, robot scoped to sync role, can't re-pair self — sync role lacks `edit device content`); `super_user_mode` only lifts unpublish/delete+pairing-reset, server-side variable_get (not remotely settable); bulk-photos itok via `hash_equals` (constant-time); NO hardcoded secrets (all variable_get); `unserialize()` only on server-internal queue/menu data not request input; interpolated `db_query` only in drush report scripts (CLI args, not web-reachable) — not web SQLi.
- PWA SOUND: precache manifest content-hashes all Main.js/app.js/importScripts → byte change triggers fresh install; `updateViaCache:'none'`; dev+publish both skipWaiting:false+clientsClaim:true; bulkPhotos write-key (stripAccessToken) matches display read-key; 3 dbVersion constants in sync (32/32/32); SPA fallback to precached index.html.
- Date/number SOUND: stock running-balance math (covered by Backend/StockUpdate/Test.elm); MUAC mm→cm display; hedley_stats period windows (>=start AND <end, end=now+1d); age-in-months everywhere uses leap-safe DateTime::diff/Date.diff; Gizra/NominalDate + sort-by-Date.compare.
- i18n SOUND: ratio/two-var interpolations preserve arg order; month names localize w/o short/long swap; `somali=Nothing` fallback by-design; ORS dosing strings complete. hedley_migrate export-content.php bugs (height triplication :1394, nested-array lookups, children.csv col mismatch) are in an OBSOLETE script (superseded by generate-sample-db.php) — not live, downgraded out.

## Round 6 — 2026-06-25 — proposals (proposed, awaiting user pick)
5 parallel agents over FRESH lenses (encounter/activity workflow & task-gating, Elm view-rendering correctness, under-swept LIVE backend modules, NON-poison sync data-fidelity, less-audited feature programs). 6 rounds deep but fresh lenses still landed 3 HIGH-confidence bugs. Agents showed strong rigor (rejected 6 false "inverted-threshold" view findings = Elm operator-section misreads; disproved a sub-agent "conclude-early" claim; verified migration import path clean). file:line point-in-time.

### Top 5 proposals
- **R6-1 — NCD Social History saves the WRONG sign (encoder copy-paste typo).** `Pages/NCD/Activity/Utils.elm:483`: `toSocialHistoryValue` encodes `SignDifficult4TimesAYear` from `form.salt` (copy-paste of the `SignConsumeSalt` line above) instead of the dedicated `form.difficult4Times` field — which IS correctly wired into the decoder (:459), the rendered question (:704/707), and the completion-task counter (:725). So the nurse must answer "difficulty affording treatment 4×/year" to complete the activity, but the answer is silently discarded at save and overwritten by the salt answer → wrong social-history sign persisted on every NCD encounter where the two differ. Origin commit `dabd0eafe`. HIGH conf, 1-line fix (`form.salt`→`form.difficult4Times`). Same family as R4-3. **STATUS: ✅ IMPLEMENTED 2026-06-25 — issue #1850, PR #1851 (branch `r6-1-ncd-social-history-encoder`, base develop), CI running.** Verified all 4 sibling sites use `difficult4Times`; changed only line 483. elm make Main Success, elm-test 2734, format clean.
- **R6-2 — NCDA low-birth-weight flag silently dropped (wrong-field guard + misplaced break).** `hedley_ncda.module:521-528` (LIVE — reached from advancedqueue worker `hedley_ncda_calculate_aggregated_data_worker`→`_for_person`): birth-weight fallback from an NCDA questionnaire is gated on `!empty(field_birth_weight) && !empty(field_weight)` — but `field_birth_weight` and `field_weight` (CURRENT weight) are independent nullable fields (an NCDA questionnaire routinely has birth-weight without current-weight), so the `&&` fails resolution; AND the `break;` (:526) is OUTSIDE the `if` so only the first NCDA node is ever checked. Result: `$data['low_birth_weight']` never set for any child whose birth weight came from an NCDA questionnaire (not a Newborn exam) → low-birth-weight indicator silently missing from their Child Scorecard/NCDA aggregate. HIGH conf. Fix: drop the `field_weight` term + move `break` inside the `if`. **STATUS: ✅ IMPLEMENTED 2026-06-25 — issue #1852, PR #1853 (branch `r6-2-ncda-low-birth-weight`, base develop), CI running.** Confirmed live (enclosing fn `hedley_ncda_calculate_aggregated_data_for_person:439` ← worker `:145` ← AQ callback `:122`); the pregnancy-summary path above correctly reads `field_weight` (that bundle stores birth weight there), only the questionnaire path uses `field_birth_weight`. Guard now `!empty($ncda->field_birth_weight[LANGUAGE_NONE][0]['value'])` + break inside if. php -l clean, phpcs Drupal+DrupalPractice exit 0 (full CI extensions). **REFINEMENT (user, CI green): the original clause-1 `!empty($ncda->field_birth_weight)` was very likely a deliberate null-GUARD and the real typo was just clause-2 `field_weight`→`field_birth_weight` (not "two defects" — one typo + the misplaced break). The single-clause fix is notice-SAFE: `empty()`/`isset()` suppress "undefined property"/"array offset on null" notices across the whole chain (TESTED on host php 7.4.33 — single-clause emits ZERO notices for full/empty-array/unset; only DIRECT access without empty() notices). So the leading guard is redundant; user chose to KEEP the single clean clause. GENERAL PHP LESSON: a deep `empty($a['x'][0]['y'])` is the canonical notice-safe path probe — no `isset()`/`!empty()` guard needed in front of it.**
- **R6-3 — Downloaded WellChild photos never cached on secondary devices (download-dispatch omission).** `SyncManager/Utils.elm:1069-1093` `getImageFromBackendAuthorityEntity` is the SINGLE chokepoint that schedules a downloaded entity's image into the local `photos` cache (Update.elm:236-256). It handles Person avatar, Photo, FamilyNutritionPhoto, NutritionPhoto, PrenatalPhoto, StockUpdate signature — but NOT `BackendAuthorityWellChildPhoto` → falls through `_ -> Nothing`. So a well-child photo uploaded from device A (upload encoder DOES handle it, Encoder.elm:146; revision DOES, :2745) is never proactively fetched on device B → viewing it hits `photos.js:36-44` 404 (no cached copy, in-app URL carries no access_token) → permanently broken cross-device well-child photo. HIGH conf, 1-line fix (add `BackendAuthorityWellChildPhoto identifier -> getImageFromMeasurement identifier`, alphabetical before `_`). **This is the DOWNLOAD-side sibling of the [[photo-upload-fileid-encoder-wiring]] memory (which covers the UPLOAD-side dispatch) — memory now updated to cover BOTH.** **STATUS: ✅ IMPLEMENTED 2026-06-28 — issue #1854, PR #1855 (branch `r6-3-wellchild-photo-download`, base develop), CI running.** Added `BackendAuthorityWellChildPhoto identifier -> getImageFromMeasurement identifier` before the `_ -> Nothing` catch-all (the case block is grouped, not strictly alphabetical, so placed after StockUpdate/before wildcard). elm make Main Success, elm-test 2734, format clean. Pure case-branch addition reusing existing helper, no import changes.
- **R6-4 — ECD on-track/off-track status computed from the WRONG measurements (inverted filter).** `WellChild/ProgressReport/View.elm:1127-1138` `genrateDefaultECDStatus`: `firstEncounterDateAfterMilestone` filters `if not <| Date.compare milestoneDate date == LT` — but the binding NAME and the design comment (:1140-1141) both say it should pick the earliest encounter AFTER the milestone, which requires DROPPING the `not`. Inversion collapses the window → wrong `StatusOnTrack`/`StatusOffTrack` (green vs red) label in the Well-Child report for milestones reaching the default path (no dedicated status entry). Origin commit `6d87898f6`. MED-HIGH (name+comment+code 3-way disagreement; caution = status→label, effect depends on encounter timing). Fix: drop the `not`. **STATUS: ✅ IMPLEMENTED 2026-06-28 — issue #1858, PR #1859 (branch `r6-4-ecd-status-inversion`, base develop), CI running, Copilot requested.** User asked for a test first. **Approach refined from the original "expose genrateDefaultECDStatus + full WellChildMeasurements fixtures" plan** (the WellChildMeasurements record is huge w/ no empty constructor → brittle fixtures): instead EXTRACTED the buggy date-filter into a pure helper `resolveFirstEncounterDateAfterMilestone : NominalDate -> List NominalDate -> Maybe NominalDate` in `WellChild/Activity/Utils.elm` (filter strictly AFTER the milestone, `not` dropped; behavior-preserving extraction = `List.filterMap (\(date,_) -> if COND then Just date) ≡ List.map Tuple.first |> List.filter COND`), wired View.elm to it, and unit-tested the helper directly with date fixtures. New `Pages/WellChild/Activity/Test.elm` (3 tests). **DISCRIMINATION PROVEN: temporarily flipped the helper back to buggy (`not`) → test 0/3 pass; fixed → 3/3.** elm make Main Success, full elm-test 2737 (+3), format clean, no new elm-review. LESSON: when a private view helper's bug is purely in a sub-computation, extract that sub-computation into a pure named helper and unit-test IT — far cleaner than exposing the whole function + building heavy fixtures.
- **R6-5 — WhatsApp batch scripts: nid-pagination with NO ORDER BY + a broken-alias resume guard (both LIVE).** (a) `hedley_whatsapp/scripts/send-messages.php:51-96` AND `delete-processed.php:33-73` keyset-paginate via `condition('nid',$nid,'>')` + `$nid=end($ids)` but neither calls `orderBy('n.nid')` → MariaDB arbitrary order under LIMIT → cursor jumps past unprocessed rows → records SKIPPED (send: some patients' reports skipped, self-heals next run from nid 0; delete-processed: skipped delivered records keep their 1MB+ screenshots → storage leak). Deviates from the project's OWN convention (`hedley_person/scripts/update-creation-date.php:33`, `hedley_patient/scripts/graduate-participants.php:36` both `propertyOrderBy('nid')`). (b) `delete-public-files.php:30-40`: `db_select('file_managed','fm')` but the count-query resume guard uses `n.fid` (alias 'n' doesn't exist) → PDOException on the documented `--fid=N` resume path (first run OK, $fid=0 skips it). MED. Both deployed to both Pantheon envs (`.pantheon-ihangane`/`.pantheon-vhw`), live `report_to_whatsapp` pipeline via `drush scr`. Fix: add `orderBy('n.nid')`; change `n.fid`→`fid`. **STATUS: ✅ IMPLEMENTED 2026-06-28 — issue #1856, PR #1857 (branch `r6-5-whatsapp-batch-scripts`, base develop), CI running.** Verified all 3 scripts have the `end($ids)` cursor advance (send :169, delete-processed :106, delete-public-files :80), so ordering is load-bearing. Added `orderBy('n.nid')` (send/delete-processed) + `orderBy('fm.fid')` (delete-public-files — it ALSO lacked orderBy, not just the alias bug, so fixed both) + count-guard `n.fid`→`fid`. php -l clean ×3, phpcs Drupal+DrupalPractice exit 0 (full CI extensions).

### Round-6 runners-up / minor (recorded, do NOT re-propose)
- **HIV `testResultDirty` hardcoded `False`** (`Pages/HIV/Activity/Utils.elm:332`) — breaks the dirty-field pattern (sibling flags + TB module preserve `form.X`); but UNREACHABLE in practice (result picker only emits `Just`; `or (Just X) _` wins regardless; clearing-to-Nothing not UI-reachable). LOW, 1-line consistency fix only.
- **AcuteIllness subsequent-visit mandatory gate ignores covidTesting** (`AcuteIllness/Activity/Utils.elm:2412-2414`) — real predicate asymmetry (checks only malariaTesting) surfacing NextSteps early, but NO reachable harm: the conclusion gate `activityCompleted AcuteIllnessLaboratory` (:153) uses `List.all` over BOTH tests, so the encounter can't conclude with COVID skipped. LOW.
- **Kirundi text mis-filed in an `english` field** — `Translate.elm` `PrescribedMedicationsChangedQuestion` (~:19908) holds Kirundi prose in `english` (bool round-trips fine). Minor i18n content (benched i18n class).
- **Prenatal fetal-presentation red alert fires for normal Cephalic/Twins at term** (`Prenatal/Activity/View.elm:3564-3567`) — LOW, git history shows DELIBERATELY authored this way across refactors → likely intended, clinician's call, NOT a clear bug.

### Round-6 cleared-clean (verified OK; don't re-chase)
- `hedley_migrate` LIVE import handlers audited position-by-position (MigrateSourceCSV binds by column position; all 22 sourceMigration FK targets, cardinality/separators, dateProcess selection) — clean. `hedley_stats` aggregation correctly `orderBy('node.nid')` before keyset paging (:3434) — ORDER-BY bug class does NOT apply there. `hedley_stock_management`/`hedley_group_education` = feature defs only, no server logic (write path = shared handleChanges). Stock balance computed client-side (no server aggregation to corrupt).
- Clinical alert thresholds RE-VERIFIED correct (6 false "inverted" reports rejected as Elm operator-section misreads): Measurement vitals BP/temp/HR/respiratory-rate; Prenatal obstetrical BMI/MUAC/fundal-height/fetal-heart-rate.
- NCDA child-scoreboard scorecard internally consistent (nutrition-behavior, WASH, interventions, ANC-newborn <4-checkups, immunization behind-by-month, take/drop 6/19-cell splits, Vit-A≥6mo/Dewormer≥12mo). ChildScoreboard/TB/HIV/GroupEd/FamNut expect/complete/referral logic consistent.
- Sync save/upload/download core sound: post-upload delete-by-localId, cursor advances only after confirmed save (max-revision over saved entities), photo fileId keyed by shardChanges localId / photo URL, upload order photos→screenshots→general→authority→whatsApp (FK-safe), bulkPut download save. (Bulk-photos URL-echo match = unverifiable + brushes declined retry class.)

### Round-3 runners-up (recorded, do NOT re-propose; promote if a slot opens)
- **Photo-upload incident loop.** Backend-rejected (non-network) photo never quarantined/marked-synced (`app.js:715-818`, unlike cache-miss branch :798-813) → re-selected (`limit(1)`) + re-reported every cycle; NO back-off (`getSyncSpeedForSubscriptions` lacks SyncUploadPhoto/Screenshot cases → 50ms fast cycle hammers /api/file-upload + /api/report-sync-incident). One bad photo wedges the whole photo lane. HIGH conf. **Nearly top-5.**
  - **STATUS: examined 2026-06-23, REJECTED — NOT a bug, do NOT re-propose.** User pushed back with decisive operational + architectural evidence: 8+ years live, tens of thousands of photos, the sync has NEVER stuck on a photo — failures are transient (network / temporary server 5xx) and self-resolve; there is no "constant" rejection in practice (the permanent-poison-photo trigger is theoretical). And the proposed fix (quarantine/substitute placeholder so the node uploads anyway) is actively HARMFUL: the node→photo link is set by `SyncManager/Encoder.elm:85-97` `replacePhotoWithFileId`, which injects the backend `fileId` into the node's image field at NODE-ENCODE time, sourced from `record.uploadPhotos` (`maybeFileId = Dict.get localId record.uploadPhotos |> Maybe.map (.fileId) |> Maybe.withDefault null`). It's ONE-SHOT — nothing re-points an already-uploaded node to a later-uploaded photo, and a missing fileId encodes the image as `null`. So letting the node upload before its photo has a real fileId permanently ORPHANS the photo (uploads `image: null`). The current block-photos-before-nodes ordering IS the invariant that keeps nodes pointing at real files; blocking on a transient failure is correct (it self-resolves). Residual micro-point **also REJECTED 2026-06-23 (user: "too small to bother")**: SyncUploadPhoto/Screenshot lack a back-off case in `getSyncSpeedForSubscriptions` so the non-network retry window runs at ~50ms. Independent of the quarantine idea (timing only, doesn't touch the photo-link invariant), but value is negligible: its only benefit (less server hammering + incident-POST noise) lives ONLY in the online-server-5xx window; there is NO `navigator.onLine` gate, so OFFLINE the retries are local no-ops (browser fails fetch instantly → no traffic, no server load), and offline NetworkError routes to SyncIdle NOT SyncReportIncident (zero incident POSTs offline). Only conceivable upside = marginal battery on multi-hour offline stretches with a pending photo. Not worth a PR. **Lesson: don't "unblock" a dependency-ordered upload lane — the blocking is load-bearing.**
- **Dashboard follow-up `setMonth` bug.** `client/src/js/nodes.js:657` `resolutionDate.setMonth(today.getMonth()+6)` should be `resolutionDate.getMonth()+6`; for today Jan–June, follow-ups resolved late prior year are wrongly dropped → dashboard under-counts follow-ups ~half the year. One-line fix, HIGH conf (reproduced numerically).
- **IndexedDB save-failure wedges download lane in `Loading`.** `SyncManager/Update.elm:198-214/2327-2338`: on IndexDbSaveFailure the lane stays `Loading`; only escape is the 30s timeout re-issuing the SAME batch → permanent no-progress loop on storage-full. Adjacent to R2-2 (visibility added) but the control-flow wedge is unaddressed. MED-HIGH.
- **Inline activity Save buttons gated only by CSS.** AcuteIllness/Prenatal activity views attach `onClick saveMsg` unconditionally + a `disabled` class; only Semantic UI `pointer-events:none` blocks it (not keyboard; breaks on CSS regression). Unlike safe `Pages/Utils.viewSaveAction`. MEDIUM.
- **Synced stats/patient-details in default `cache` bin, wiped by `drush cc all` now run EVERY deploy** (`hedley_stats.module:3814`, used :657/683/687). Dashboards blank until per-HC AQ recalc drains. Self-healing/operational. MEDIUM. (Note: aggravated by the #1828 RoboFile fra+cc-on-deploy change.)
- **`md5(time())` stats hash** (`hedley_stats.module:685`) can suppress a same-second stats update. HIGH mechanism / LOW hit-rate.
- **Unsorted `getParticipantEncountersByEncounterType`** (`Backend/NutritionEncounter/Utils.elm:349-359`) returns AssocList insertion order; "latest encounter" relies on every caller sorting. Latent trap, no confirmed live defect. MED-LOW.
- **`photos` download Cache Storage never pruned** (`photos.js:72`, `bulkPhotos.js:97`; only `photos-upload` is deleted, app.js:1465). Unbounded growth → quota pressure (adjacent to R2-2). LOW.
- **Young-infant fast-breathing threshold** (`Pages/AcuteIllness/Activity/Utils.elm:2776-2790`): no `<2mo` bracket so <2mo evaluated at ≥50 not WHO ≥60; COVID sibling has it. Over-triage, LOW harm. MEDIUM it deviates. (Note: same function as the round-1 known DOB-unknown flag, different defect.)

### Round-3 NOTES (not proposals)
- **Write-side cross-HC IDOR** (`HedleyRestfulSync::handleChanges()` ~:490-508): PATCH/POST any node of any HC, zero `field_shards` check on write path (download checks shards :247-252); `sync` role has `edit any X content` on ~all bundles. Real and arguably worse than the read IDOR — BUT it's the write analog of **R2-1 (DECLINED)** and its fix hits the SAME device→HC-binding wall the user already rejected. Surfaced for awareness; NOT a fresh proposal.
- **NCD Stage-1 OR-vs-AND** (`Pages/NCD/Utils.elm:262-264`) is already an in-repo `[FINDING]` test annotation (NOT new) and plausibly correct (standard staging uses OR) — needs clinical-sheet owner, not a code change.

## Merge wave — 2026-06-23 (all 3 review PRs landed on develop)
- **R2-3 / #1830** (`decodeEverySet` element-tolerant) — ✅ MERGED. Now the canonical tolerant set decoder on develop. Copilot review left 1 nit (test pinned `EverySet.toList` order) → fixed by sorting (`162c53c2d`).
- **R3-1 / #1834** (clinical-enum drop) — ✅ MERGED. During its develop-merge, the redundant twin `decodeEverySetDroppingUnknown` was **collapsed into `decodeEverySet`** (114/site recommendations repointed); `decodeListDroppingUnknown` kept; the two `Utils/Json/Test.elm` test sets combined into one (`decodeEverySet` + `decodeListDroppingUnknown`). The dashboard-stats follow-up (~13 sites in `Backend/Dashboard/Decoder.elm`) is now **DONE: issue #1835 → PR #1836** (drop-unknown applied: 10 `decodeEverySet (decodeWithFallback)` → `decodeEverySet`, 3 `list (decodeWithFallback)` → `decodeListDroppingUnknown`; removed the now-dead `Backend.Measurement.Model` sentinel-type import; scalar encounter_type/sequence_number + the `dict` NutritionValue placeholder left out of scope). Verified elm make Success + 2734 tests + elm-review clean.
- **R2-4 / #1832** (local-timezone `fromLocalDateTime`) — ✅ MERGED. Copilot: no comments.
- Merged via merge-commits with `--delete-branch`. Each verified locally (`elm make Main.elm` Success + elm-test) before merge; merged on UNSTABLE (heavy e2e/simpletest jobs are not required gates and didn't exercise the changes).

## Round 7 — 2026-06-28 — proposals (proposed, awaiting user pick)
5 parallel agents over fresh lenses (Elm form-field-wiring typos / backend RESTful field-mapping / `hook_update_N`+deploy correctness / Elm report-aggregation / backend worker-cron integrity). LEAN round — 7 deep, only ~2 NEW actionable bugs + 1 promotable runner-up; two whole lenses came back structurally CLEAN. **Working-tree gotcha:** agents read branch `1826-deploy-release-skill` (NOT develop), so they re-found R4-3 (Dashboard zscore), R6-1 (NCD social-history), R6-2 (NCDA birth-weight) — all real but already implemented on unmerged PR branches (#1845/#1851/#1853); ignore as dupes. file:line point-in-time.

### Top picks
- **R7-1 — Head-circumference "previous value" reference shows the OLDEST measurement, not the latest (missing sort before `List.head`).** `Backend/NutritionEncounter/Utils.elm:429`: `headCircumferences = wellChildHeadCircumferences` is left UNSORTED while the sibling `heights`/`muacs`/`weights` (`:426-428`) each get `|> List.sortWith sortTuplesByDateDesc`. Source order: `generateIndividualWellChildMeasurementsForChild` returns DESC (`:261`, "Most recent to least recent"), then `resolveIndividualWellChildValues` ends in `|> List.reverse` (`:636`) → ASC (oldest-first). `getLatestValue` (`:524-530`) does `List.filter (date<current) >> List.head` → head of ASC = OLDEST. Surfaced via `:548` into `PreviousValuesSet.headCircumference`, displayed next to the head-circ input on the Well-Child nutrition-assessment form (`Pages/WellChild/Activity/View.elm:842` `viewHeadCircumferenceForm ... previousValuesSet.headCircumference`). So a nurse measuring head circumference sees the child's FIRST-ever value as "previous" instead of the most recent → wrong growth-trend reference at point of care. Misfires for any child with ≥2 prior well-child encounters recording head circumference. Heights/MUAC/weight are immune (re-sorted at :426-428 because they concat 3 sources; head circ has only the well-child source and was left un-re-sorted). **VERIFIED MYSELF end-to-end (source DESC → reverse → ASC → head=oldest).** HIGH conf, 1-line fix: `, headCircumferences = wellChildHeadCircumferences |> List.sortWith sortTuplesByDateDesc`. Same class as the R3 unsorted-getParticipantEncounters runner-up but a CONFIRMED live patient-facing instance. **Headline of the round.** **STATUS: ✅ IMPLEMENTED 2026-06-29 — issue #1860, PR #1861 (branch `r7-1-headcirc-previous-value`, base develop), CI running, Copilot requested.** Fix per the R6-4 extract-a-pure-helper pattern: NOT the bare 1-line sort at the call site, but centralized ordering — new exported pure `resolveLatestValue : NominalDate -> List ( NominalDate, a ) -> Maybe a` (filter date<current → `sortWith sortTuplesByDateDesc` → head → Tuple.second) replaces the inline `getLatestValue` let-binding in `resolvePreviousValuesSetForChild`, so selection no longer depends on each caller pre-sorting. Removed the now-redundant per-field sorts in `resolvePreviousMeasurementsSetForChild` (confirmed its ONLY consumer is that selection — grep verified). 5 new unit tests in `Backend/NutritionEncounter/Test.elm` (oldest-first repro / order-independence / ignores today+future / empty / all-future). **DISCRIMINATION PROVEN: reverted helper to non-sorting → 3/5 fail (oldest-first returns Just 100 vs Just 300); restored → 20/20.** Verified: `elm make Main.elm` Success, elm-test 20/20 in module, elm-format clean, no new elm-review (only pre-existing elm.json NoUnused.Dependencies noise). **Tooling note: `elm`/`elm-format` are GLOBAL at `~/.nvm/.../bin` (elm 0.19.1); only `elm-test`/`elm-review` are in `client/node_modules/.bin`. Inline expression type annotations `(x : T)` are INVALID Elm — annotate via a `let` binding instead (hit this on the empty-list test).**
- **R7-2 — `hedley_update_7046` stamps the prenatal-medications REPORTING CUTOFF with the deploy/install date instead of a fixed business date.** `hedley.install:745-748` (`variable_set('hedley_prenatal_change_medications', date('Y-m-d', time()))`) + install-time call at `:54`. The variable is a reporting cutoff consumed at `hedley_reports.module:7965-7999`: prenatal encounters with `start_date >= cutoff` are scored against the new 7-activity medication model (prenatal_mms/fefol/folate/iron/…), earlier ones against the legacy single `medication` activity — it's meant to mark the REAL date ANC meds switched, but gets stamped with whenever the code ran. Won't re-fire on existing prod (Drupal records it in `system.schema`; `updb` runs each update once/env), so prod's stored value is safe — exposure is **fresh installs / new multidev / CI / rebuilt sites**, which the now-automatic deploy `updb` provisions routinely. Commit `6ac252f3a` ("Ensure prenatal medications variable on CI") already band-aided this with a CI force-set — fragility acknowledged, not fixed. MED severity (reporting accuracy on non-prod envs; HIGH mechanism / MED that prod is mis-stamped — depends on when 7046 first ran there vs true switchover). Fix: hard-code the real ANC-meds launch date (same pattern as the GU-exam/COVID `launch_date` cutoffs) + guard `if (empty(variable_get(...)))` so it never overwrites a deliberately-set value. **STATUS: ❌ DECLINED 2026-06-29 — NOT A BUG (by design). User clarified the premise is wrong: the 7-medication model is a CLIENT feature that goes live per-environment AT DEPLOY, and different sites/envs are deployed on different dates (e.g. VHW Dec 20, UVL Jan 15). So the "switchover date" legitimately IS the per-site deploy date, and `date('Y-m-d', time())` at `updb` time correctly captures it — the environment/site-dependence is INTENDED, not a defect. Agent's "fresh/rebuild mis-scores history" worry is moot: fresh install = empty DB (no pre-cutoff encounters); multidev/DB-clone has 7046 already in system.schema so updb won't re-run it (inherits source's correct date); CI creates data "now" after the stamped cutoff (commit `6ac252f3a` ensured the var is SET so CI uses the new model — consistent with design, not a bug symptom). Only importing historical pre-switchover encounters into a freshly-INSTALLED (not cloned) DB would misbehave — not a real workflow. **LESSON: for a per-deploy feature-rollout cutoff stamped at `updb`/install time, `time()` is CORRECT — do NOT assume such a cutoff "should" be a single fixed business date; rollout dates differ per site/env. Verify the feature's rollout model (client-deploy vs fixed calendar event) before flagging a deploy-time-stamped variable.**
- **R7-3 — PROMOTE the R4 runner-up: `hedley_general_cron()` deletes Advanced-Queue rows by age with NO status filter → can drop UNPROCESSED items.** `hedley_general.module:563-571`: `db_delete('advancedqueue')->condition('created', REQUEST_TIME - 6mo, '<')` with no `status` constraint. Contrib AQ's own purge (`_advancedqueue_purge_history`, advancedqueue.module:352-374) always limits deletion to `status IN (SUCCESS, FAILURE)` — terminal only; this custom cron also deletes `QUEUED(-1)`/`PROCESSING(0)`/`FAILURE_RETRY(3)`. A genuinely backlogged QUEUED item keeps its original `created`, so once 6 months old it's eligible → permanently deletes the unprocessed NCDA/Reports/Stats recalc tasks; affected `field_ncda_data`/`field_report_data`/stats caches never refresh, only trace is a watchdog count. MED (mechanism HIGH; needs a >6-month backlog, but failure mode is unbounded/silent). Fix: add `->condition('status', [ADVANCEDQUEUE_STATUS_SUCCESS, ADVANCEDQUEUE_STATUS_FAILURE], 'IN')`. Now has a sharper write-up than the bare R4 line. **STATUS: ❌ DECLINED 2026-06-29 (user) — OVERKILL, do not re-propose.** Verified the mechanism (delete has no status filter, vs contrib `_advancedqueue_purge_history` which limits to SUCCESS+FAILURE; `created`=enqueue time, preserved across FAILURE_RETRY; claim is oldest-first — so a >6mo QUEUED/RETRY item is deletable unprocessed). BUT user's operational call: a task unprocessed after 6 months is effectively DEAD — if cron/worker were going to run it, it would have long ago; preserving the row doesn't resurrect it. And these are DERIVED-data recalc tasks (NCDA/stats/reports/view-export) — source nodes untouched, so the aggregate regenerates on the next relevant person/HC touch/sync. The unfiltered delete is reasonable cleanup (and for a perpetually-retrying poison item, beneficial — stops the loop). Same OPERATIONAL-REALITY meta-pattern that retired the sync-poison/stall class (#1b, R3-3, photo loops): apparent "silent loss" findings guarded by the fact that the trigger only fires when the system is already broken AND the lost thing self-regenerates. **LESSON: for a cron purge of DERIVED/regenerable work, don't insist on contrib's terminal-only invariant — age-based deletion of long-dead queue items is fine when the data re-derives on next event.**
- **R7-4 — PROMOTE the R5 runner-up: Dashboard duplicate-person merge discards nutrition history (self-merge).** `Pages/Dashboard/Utils.elm:~305-316` (verify line at elaboration): `mergeNutritionValueDicts candidate.X candidate.X` merges the candidate with ITSELF; `current.nutrition` is unused → when a child is enrolled across programs and appears as a duplicate person, the merge keeps only one side's accumulated nutrition months, silently dropping the other. HIGH bug / MED magnitude (needs a cross-program duplicate). Fix: merge `current` with `candidate`. Same self-merge/wrong-operand class agent-4 was hunting; recorded R5, never actioned. **STATUS: proposed (promotion).**
- **R7-5 — PROMOTE the R5 runner-up: objective i18n wrong-text/label errors (non-dosing, all `Translate.elm`).** All HIGH-conf and OBJECTIVE (wrong string copied from a neighbor, not a translation-quality judgment — so distinct from the R2-5 native-reviewer hold): `:6254` ECD milestone "4-5 words" asks "2-4" in Kinyarwanda; `:14958` prenatal "why not followed?" shows NotTaken's "no measurements taken" string in Kinyarwanda; `:11492` severity "Low" renders Kinyarwanda "Confirm errors"; `:30499` November short-form = October's "Ukw" (should "Ugu"); + hardcoded English "Not Set" at `Pages/Prenatal/Activity/View.elm:471` (should use `Translate.EmptyString`). (Verify each line at elaboration — point-in-time from R5/2026-06-24.) Same fix family as the implemented R5-1 dosing-translation fixes (objective, no native input needed). Recorded R5, never actioned. **STATUS: proposed (promotion).**

### R7 implementation notes
- **R7-1 ✅ — CI fully GREEN on re-run (all 10 jobs incl. e2e_playwright_1).** First run flaked ONLY on `e2e_playwright_1` (`e2e/lab-tech-encounter.spec.ts` — `auth.ts:50 pairDevice`: `page.goto('/')` interrupted by app's own `#pincode` redirect — a test-harness navigation race, unrelated to the Elm change; same job that flaked on R6-4). Re-triggered with an empty commit (user's standing call on this exact flake) → green. PR #1861 ready to merge.
- **R7-4 ✅ IMPLEMENTED 2026-06-29 — issue #1862, PR #1863 (branch `r7-4-dashboard-merge-duplicates`, base develop), CI running, Copilot requested.** Confirmed the bug verbatim at `Pages/Dashboard/Utils.elm:306-310` (5× `mergeNutritionValueDicts candidate.nutrition.X candidate.nutrition.X` — candidate merged with ITSELF; `current.nutrition` never read; `Dict.merge` of a dict with itself is a no-op, so `current`'s accumulated months are dropped on every duplicate merge). Fix: `current.nutrition.X` as the first arg in all 5. Exposed `caseManagementMergeDuplicates` (was unexported) + new `Pages/Dashboard/Test.elm` (2 tests: same-identifier records with different months must merge to BOTH; different identifiers stay separate). **DISCRIMINATION PROVEN: reverted to self-merge → first test fails (`[2]` vs `[1,2]`); restored → 2/2.** Types: `PersonIdentifier = Int`, `CaseManagement{identifier,birthDate,gender,nutrition}`, `CaseNutrition{stunting/underweight/wasting/muac/nutritionSigns : Dict Int NutritionValue}`, `NutritionValue{class : NutritionStatus(Good|Moderate|Neutral|Severe), value : String}`, `emptyNutritionValue` at Backend/Dashboard/Model.elm:200. Verified: elm make Main Success (56 modules), elm-test 2/2, elm-format clean, no new elm-review (only pre-existing elm.json NoUnused.Dependencies noise).
- **R7-5 ✅ IMPLEMENTED 2026-06-29 — issue #1864, PR #1865 (branch `r7-5-i18n-wrong-text`, base develop), CI running, Copilot requested.** All 5 sub-findings re-verified against CURRENT source (line numbers drifted from R5): (1) ECD `UseMediumPhrases` Translate.elm:6254 — english "4-5 word sentences" had Kinyarwanda "amagambo 2 kugera kuri 4" (copy of `UseShortPhrases` 2-4) → "4 kugera kuri 5" (correct value, established idiom); (2) `NotFollowingRecommendationQuestion`:14958 — Kinyarwanda was "Nta bipimo byafashwe" (="no measurements taken", Kirundi/Somali correct) → **Nothing** (English fallback); (3) `Low`:11492 — Kinyarwanda "Kwemeza amakosa" (≈"confirm errors", Somali "Hoose" correct) → **Nothing**; (4) month short `Nov`:30499 — Kinyarwanda "Ukw" (=October's, from Ukwakira; Kirundi "Muny" already correct) → "Ugu" (Ugushyingo); (5) `Pages/Prenatal/Activity/View.elm:471` hardcoded English `Maybe.withDefault "Not Set"` (pre-pregnancy weight) → `translate language Translate.NotAvailable` ("not available", translated all 4 langs — better than the ledger-suggested EmptyString which renders ""). **DECISION: for (2)/(3) the correct Kinyarwanda is NOT recoverable → Nothing/English-fallback (strict improvement over wrong string, per the R2-5 "fluent-but-wrong is worse than English fallback" principle); PR flags native review.** No unit test (content fix, like R5-1). Verified: elm make Main Success, full elm-test 2734, elm-format clean. **`translate`/`language` already in scope in that View.elm function (used at :473).**

### Round-7 cleared-clean (verified OK; do NOT re-chase)
- **Elm "wrong-field copy-paste" encoder/update class is EXHAUSTED.** Agent verified WellChild / Nutrition / ChildScoreboard / AcuteIllness / HIV / Tuberculosis / EducationSession / StockManagement / NCD(non-Social) / NCDA / NCD-Recurrent / Prenatal(non-labs) Utils+Update, plus shared `Measurement/Utils.elm` encoders (toCorePhysicalExam/Vitals/Nutrition/FamilyPlanning — positional `andMap` orders checked against constructors) and per-entity `Backend/*/Encoder.elm` (Person/PrenatalEncounter/StockUpdate) — ALL clean. Only live instances are the already-fixed R4-3/R6-1 (unmerged on this branch). Stop mining this class.
- **Backend RESTful field-mapping is structurally ABSENT as a bug class.** `HedleyRestfulActivityBase::publicFieldsInfo()` auto-derives the public key via `str_replace('field_','',$field_name)` (~190 measurement plugins → key can't disagree with field by construction); field names are program-namespaced (`field_ncd_*`/`field_hiv_*`/etc., defeats cross-program copy-paste); `HEDLEY_RESTFUL_SHARDED`/`ALL_DEVICES` dispatch is 1:1. All 20 lab-test classes (incl. Lipid-Panel vs RandomBloodSugar `field_sugar_count`), 49 explicit overrides, vitals/core-exam/symptom/danger-sign/anthropometric sets verified clean. Only cosmetic residue: wrong `@file` docblock in HedleyRestfulChildScoreboardNCDA.class.php:5 ("Contains HedleyRestfulNutritionNCDA"), a harmless leftover `unset()` in HedleyRestfulEducationSession. Not worth a PR.
- **`hook_update_N` mechanical health OK.** Real update surface is `hedley_update_7001..7047` in the install-PROFILE file `hedley.install` (NOT `hedley_<module>_update_N` — a grep by that pattern MISSES all 47; methodology note). Contiguous, no gaps/dupes, none use `$sandbox`/Batch (no progress/`#finished` bug class present). The only `$sandbox` code (`hedley_person_execute_batch_import`, hedley_person.module:387) is an interactive import worker, not a deploy update, and advances correctly. `hedley_update_7009` nutrition-signs spelling-fix is incomplete on the revision table but MASKED (Elm decoder maps all 3 spellings disortion/distention/distension → AbdominalDistension, Decoder.elm:1445-1454) — harmless. The `drush fra`-reverts-permissions footgun is real (user_permission is Features-managed across ~24 modules) but not traceable to a concrete wrong outcome (any missing-from-export permission would already have broken on a prior manual `fra`) — flagged, not a bug.
- **Elm cross-encounter aggregation is disciplined** — nearly every `getXEncountersForParticipant` caller re-applies `sortByStartDateDesc`/`sortTuplesByDateDesc`; NCDA scorecard distribution/merge helpers all sort internally. Cleared: dashboard `generateCaseNutritionNewCases` month-13 predecessor-carry (correct, matches hedley_stats.module:3193-3196), `viewGoodNutrition` period-over-period order, NCD `resolveHypertensionCondition` / acute-illness diagnosis `List.head` (operate on DESC lists), NCDA immunization look-back + 9-month ANC window indexing. Only the R7-1 head-circ gap.

### Round-7 dupes (agents re-found, already implemented on unmerged branches — do NOT re-propose)
- Dashboard `zscore_underweight` ← `zscoreStunting` (Encoder.elm:422/510/556) = **R4-3 / PR #1845** (open).
- NCD `toSocialHistoryValue` `form.salt`→SignDifficult4TimesAYear (NCD/Activity/Utils.elm:483) = **R6-1 / PR #1851** (open).
- NCDA low-birth-weight guard+break (hedley_ncda.module:521-531) = **R6-2 / PR #1853** (open).

## Round 8 — 2026-06-29 — proposals (proposed, awaiting user pick)
5 parallel agents over FRESH lenses (Elm update control-flow / Elm singleton page-state leaks / Drupal entity-CRUD-hook side-effects / JS IndexedDB+sync orchestration / Elm number-unit display & denominators). RICHER than expected for round 8 — these lenses hadn't been primary-swept. 2 lenses landed strong; 1 came back clean; 1 agent finding DISMISSED on my verification. file:line point-in-time.

### Top 5 proposals
- **R8-1 — AcuteIllness MUAC "previous value" rendered 10× too small at Burundi.** `Pages/AcuteIllness/Activity/View.elm:769` computes `previousValue = resolvePreviousValue assembled .muac muacValueFunc` where `muacValueFunc` (Backend/Measurement/Utils.elm:620 = `\(MuacInCm cm) -> cm`) returns RAW cm with NO ×10. It's passed to `viewMuacForm`→`muacFormInputsAndTasks` (Measurement/View.elm:4316-4357), which for `SiteBurundi` ×10's the CURRENT value and labels everything `UnitMillimeter`, then passes `previousValue` straight to `viewPreviousMeasurement` with that mm label. So a previous MUAC of 12.5 cm shows as "12.5 mm" instead of "125 mm" — 10× understated, directly under a mm current field. Nutrition/WellChild are correct because they source previous MUAC from `resolvePreviousValuesSetForChild` which ×10's for Burundi (Backend/NutritionEncounter/Utils.elm:532-545). Rwanda unaffected. **VERIFIED MYSELF end-to-end.** HIGH conf, Burundi-only clinical-reference display. Same MUAC-mm/cm + previous-value family as R7-1. Fix: make the AcuteIllness `previousValue` site-aware (×10 for Burundi), or (cleaner, bigger) move the ×10 for previousValue inside `muacFormInputsAndTasks` and have all callers pass raw cm. **Headline. STATUS: ✅ IMPLEMENTED 2026-06-30 — issue #1866, PR #1867 (branch `r8-1-acuteillness-muac-previous-burundi`, base develop d36b32e9a), CI running, Copilot requested.** Chose the clean minimal fix: added exported pure helper `muacValueFuncForSite : Site -> MuacInCm -> Float` (×10 for SiteBurundi, else identity) to `Backend/Measurement/Utils.elm` next to `muacValueFunc` (added `Site(..)` to its SyncManager.Model import + exposing entry), and changed `Pages/AcuteIllness/Activity/View.elm:769` from `muacValueFunc` → `muacValueFuncForSite site` (swapped the import too; `site` already in scope, `Site(..)` already imported there). Did NOT refactor the 2 existing inline ×10 copies (muacFormInputsAndTasks current-value `Maybe Float`, resolvePreviousValuesSetForChild `Float→Float`) — different signatures, kept scope tight (noted as follow-up). 3 unit tests in `Backend/Measurement/Test.elm` (Burundi 12.5→125, Rwanda/Somalia 12.5→12.5). **DISCRIMINATION PROVEN: broke helper's Burundi ×10 → Burundi test fails (12.5 vs 125); restored → 30/30.** **GOTCHA: `elm-explorations/test` `Expect.equal` THROWS on non-integer floats ("Do not use Expect.equal with floats. Use Float.within") — 125.0 passed (integer-valued) but 12.5 failed; switched to `Expect.within (Expect.Absolute 0.001) expected`.** Verified: elm make Main Success (71 modules), elm-test 30/30, elm-format clean. (elm-review on a SUBSET incl. Measurement/Utils.elm floods NoUnused.Exports — subset-mode artifact, CI lint_elm_review is the gate; muacValueFuncForSite is genuinely used.) **CI GREEN first run (all 10 jobs).** **FOLLOW-UP consolidation folded into the SAME PR #1867 (user: "do it all at current PR"):** the ×10-for-Burundi MUAC DISPLAY conversion was duplicated inline in 4 places — centralized behind one `muacValueForSite : Site -> Float -> Float` source of truth (Backend/Measurement/Utils.elm), with `muacValueFuncForSite` now delegating to it. Replaced the inline `case site of SiteBurundi -> Maybe.map ((*) 10)` at: Measurement/View.elm (muacFormInputsAndTasks current value — split the `(value,label)` tuple, value→muacValueForSite, kept the label `case`), Pages/Nutrition/Activity/View.elm, Pages/FamilyNutrition/Encounter/View.elm, Backend/NutritionEncounter/Utils.elm (resolvePreviousValuesSetForChild — removed the local `muacValueFunc`). Behavior-preserving. **Downgraded `Site(..)`→`Site` in Nutrition/Activity/View + NutritionEncounter/Utils** (their only constructor ref was the removed one; `Site` TYPE still used in signatures → import stays, no NoUnused). MUAC INPUT direction (string→cm ÷10) was ALREADY centralized in `setMuacValueForSite` (Pages/Utils.elm:1425) — left as-is. Verified: elm make Main Success (65 modules), full elm-test 2737, elm-format clean, no Site/unused-import elm-review findings on touched files. **MEMORY for future MUAC work: display cm→mm uses `muacValueForSite`/`muacValueFuncForSite`; input mm→cm uses `setMuacValueForSite`; the unit LABEL (UnitMillimeter/UnitCentimeter) is still chosen by an inline `case site` (not yet centralized — optional further step).**
- **R8-2 — Statistical-Queries report data goes stale on edit / soft-delete of NON-prenatal content.** `hedley_reports/hedley_reports.module` `hedley_reports_node_update`→`hedley_reports_trigger_recalculation_by_edited_content` (~410-433) only handles PRENATAL encounter + (antenatal) individual-participant bundles; every other measurement/encounter update early-returns ($participant_id empty). The INSERT path (`by_new_content` ~362-400) fires on the FULL trigger set (`get_triggering_measurement_types`/`...encounter_types`). Since a soft-delete IS a `node_update` (sets field_deleted=TRUE) and value corrections arrive as node_update, the per-person `field_reports_data` (built from `hedley_general_get_person_measurements`, which excludes deleted) is never recomputed for non-prenatal edits/deletes → Statistical-Queries report keeps counting deleted measurements / stale z-scores. Sibling NCDA (`hedley_ncda_node_update`→`_trigger_recalculation_by_measurement`, no insert/update discrimination) and stats route EVERY measurement update — reports is the lone divergence; git shows prenatal-only since introduction. Agent HIGH conf; NOT yet self-verified. Fix: mirror the insert path's trigger lists in the update hook's non-person branch. **STATUS: ✅ IMPLEMENTED 2026-06-30 (incl. gender companion) — issue #1870, PR #1871 (branch `r8-2-reports-recalc-on-edit`, base develop d36b32e9a), CI running, Copilot requested.** SELF-VERIFIED: `by_new_content` (:362) fires on full set (10 encounter types `get_triggering_encounter_types` + height/weight/MUAC/nutrition/group/immunisation `get_triggering_measurement_types`); `by_edited_content` (:410) was a 2-case switch (PRENATAL_ENCOUNTER + antenatal INDIVIDUAL_PARTICIPANT), all else → empty `$participant_id` → early return. Confirmed soft-delete = node_update (no hard delete) + NCDA sibling `hedley_ncda_node_update:113` routes ALL non-person updates through the SAME `trigger_recalculation_by_measurement` used by insert (:96) → reports is lone divergence. **Self-heals on person's next triggering INSERT (window = edit/delete → next new measurement; indefinite only for inactive persons).** Fix: `by_edited_content` keeps the antenatal-participant special case (resolve via `entity_metadata_wrapper('node',$node)->field_person`), else `return hedley_reports_trigger_recalculation_by_new_content($node)` (no-ops on non-triggering types, resolves person same as insert incl. prenatal encounters — removes the duplication). **Gender companion** (`for_edited_person` :338, was R8 lower-tier): added `field_gender` comparison alongside `field_birth_date` (gender field confirmed `field_gender`, read at :518; gender drives z-scores). No unit test (backend AQ/hook; consistent w/ R5-3/R6-2/R6-5). Verified: php -l clean, phpcs Drupal+DrupalPractice clean (full CI extensions). **Branch-switch note: `git stash -u` + checkout develop reverted the R8-5 Elm files in the working tree (they live committed on the r8-5 branch) — expected, no work lost.**
- **R8-3 — `nodes.js index()` drops the `deleted` filter on 3 query branches → soft-deleted patients reappear.** `client/src/js/nodes.js`: default path applies `.and(item => item.deleted == false)` (~914-919) but three branches REASSIGN `query = table.where(criteria)` + `countQuery = query.clone()` without re-chaining the filter: **geo_fields** (geographic/admin-area participant search ~1004-1006), **resilience_survey** by nurse (~1163-1166), **education_session** by participant (~1179-1182). 6+ sibling branches (name_contains, national_id_contains, participant/encounter, pmtct session, session-by-child) all DO re-apply it. So a soft-deleted/merged patient reappears in the location-filtered directory (both `data` and `count`). Agent HIGH conf; NOT yet self-verified. Fix: append `.and(item => item.deleted === false)` to query+countQuery in the 3 branches. **STATUS: ✅ IMPLEMENTED 2026-07-01 — issue #1874, PR #1875 (branch `r8-3-index-deleted-filter`, base develop 7ce2c6033), CI running, Copilot requested.** SELF-VERIFIED: `index()` in `client/src/js/nodes.js` — the 3 branches reassign `query = table.where(criteria)` + `countQuery = query.clone()` with NO `.and(deleted)`: geo_fields (:1004, after criteria.province..village), resilience_survey (:1164, criteria.nurse), education_session (:1180, criteria.participating_patients). ALL sibling branches DO filter: name_contains (`person.deleted === true` exclude), national_id (`person.deleted !== true`), `[type+person]`/`[type+individual_participant]`/`[type+family_participant]`/`[type+clinic]` (`=== false`), pmtct session — so these 3 are the outliers (the branches REASSIGN query, dropping the default `.and(deleted==false)` applied at ~:914). Fix: added `.and(fn => fn.deleted !== true)` to the reassigned `query` in each (countQuery.clone() inherits it); used `!== true` (lenient — excludes only explicitly-deleted, matching the person-search name/national-id siblings; safer than `=== false` if `deleted` is ever undefined). No unit test (JS layer has NO unit-test framework — tests are elm-test/Playwright/simpletest; person-search is covered by `e2e_playwright_*`). Verified: `node --check` clean. **META: this closes R8's top tier. Round 8 top-5 all done (R8-1..R8-5); only lower-tier remain (app.js date_measured UTC [R2-4 JS residual], hedley_stats shard-reassignment old-HC).**
- **R8-4 — `relationshipPages` stale family-relationship state across navigation (R3-2 class).** Structural TWIN of the fixed `editPersonPages`: `App/Model.elm:297` `relationshipPages : Dict (PersonId,PersonId) Pages.Relationship.Model.Model` (holds `relatedBy` Parent/Child/Caregiver + `assignToGroup`); reset (`Pages.Relationship.Model.Reset`) is SUCCESS-only (`Backend/Update.elm:4383-4384`, and the newborn-enrolment branch resets not at all); `App/Update.elm` SetActivePage only dispatches AssignToClinicId for RelationshipPage, never Reset; view `viewedRelationship = model.relatedBy |> Maybe.Extra.orElse savedRelationship` (Pages/Relationship/View.elm:123-125) → unsaved form value WINS over DB. Abandon a relationship selection for pair A↔B, return to the SAME pair → stale never-saved linkage pre-checked → nurse may Save the wrong family link. Same-pair scope (like editPersonPages was same-person). A `Reset Initiator` Msg already exists, unused on entry. HIGH structural / MED harm; agent-found, mechanism solid. Fix: add a `model.activePage == page`-guarded Reset on SetActivePage entry, mirroring EditPersonPage. **STATUS: ✅ IMPLEMENTED 2026-07-01 — issue #1872, PR #1873 (branch `r8-4-relationship-form-reset`, base develop 7ce2c6033), CI running, Copilot requested.** SELF-VERIFIED all 4 facts: singleton `relationshipPages : Dict (PersonId,PersonId) …` (App/Model.elm:297, reset to Dict.empty only at login :361); `Reset initiator` dispatched ONLY inside `HandlePostedRelationship`'s `data |> RemoteData.map (\relationship -> …)` (Backend/Update.elm:4380-4386 = Success-only; PrenatalNextStepsNewbornEnrolmentOrigin returns [] even then); SetActivePage's ONLY RelationshipPage case (App/Update.elm:1158) handled just GroupEncounterOrigin (clinic auto-assign), no reset; view `viewedRelationship = model.relatedBy |> Maybe.Extra.orElse savedRelationship` (Pages/Relationship/View.elm:123-125) → unsaved form value WINS over DB, drives both the checked radio (:171) and Save (:325). Literal twin of the fixed #1838 create/edit-person leak (the reset template sits right at App/Update.elm:1181-1203). Fix: generalized the case to `RelationshipPage id1 id2 initiator` → `resetMsgs` (`if model.activePage == page then [] else [Reset initiator |> MsgPageRelationship id1 id2 |> MsgLoggedIn]`) `++ clinicMsgs` (`case initiator of GroupEncounterOrigin sessionId -> <existing clinic assign>; _ -> []`). Order reset-then-assign so group-origin clinic re-sets after reset; clinic-assign unguarded (preserves prior behavior). No unit test (SetActivePage extraMsg, needs full App.Model fixture — same as #1838). Verified: elm make Main Success, full elm-test 2744, elm-format clean. **This closes the singleton-form-leak family (#1838 createPersonPage/editPersonPage + this relationshipPages); remaining singleton leaks are patient-search boxes [agent-2 finding-2, weaker/arguably-expected UX] and the recorded stockManagementPage [R4 runner-up].**
- **R8-5 — NCDA child-MUAC field not site-adjusted (cm where Burundi uses mm everywhere else).** `Measurement/Update.elm:404` `muac = String.toFloat string |> Maybe.map MuacInCm` (no `setMuacValueForSite`) + `Measurement/View.elm:3327-3336` shows raw `muacAsFloat` labeled `UnitCentimeter` for ALL sites. Every other child-MUAC entry at Burundi (group nutrition muacFormConfig, setMuacValueForSite, muacFormInputsAndTasks) is mm. The NCDA questionnaire MUAC (fillable in WellChild/Nutrition/ChildScoreboard) is the lone cm-labeled-for-all-sites field → data-entry hazard: a Burundi nurse habituated to mm types "120" → stored 120 cm → flows to `muacIndicationForChild` + WellChild scorecard MUAC color (ProgressReport/View.elm:2752/2776) → wrong nutrition-status category. Label is self-consistent so NOT a guaranteed mis-render (data-entry hazard, MED conf). Fix: use `setMuacValueForSite` + the ×10/mm display branch. **STATUS: ✅ IMPLEMENTED 2026-06-30 (full fix, option A) — issue #1868, PR #1869 (branch `r8-5-ncda-muac-site-conversion`, STACKED on `r8-1-...` since it uses muacValueForSite from #1867; base auto-retargets to develop on #1867 merge), CI running, Copilot requested.** **ARCHITECTURE DISCOVERY (bigger than agent's framing):** the NCDA MUAC field has 4 input wirings — WellChild/Nutrition/ChildScoreboard (`SetMuac*ForNCDA`, ALREADY site-aware via setMuacValueForSite) + the SHARED group-session path (`viewNCDA` config `setMuacMsg=SetMuac` → `Measurement.Update.updateChild`, NOT site-aware) — and ONE shared display (`ncdaFormInputsAndTasks`, hard-labeled cm). `Measurement.Update.updateChild` had NO `site` param. **COUPLING TRAP:** the display is shared, so making it ×10/mm would BREAK the group context (input stores 125 as cm → display ×10 = 1250) unless group input is ALSO fixed → had to do both. **Threaded `site` through the group update stack:** `Pages.Session.Update` (has site via getChildForm) → `Pages.Activity.Update.updateChild` (+`Site` after `features`) AND `Pages.Participant.Update.updateChild` (+`Site` first) → `Measurement.Update.updateChild` (+`Site` first; import setMuacValueForSite from Pages.Utils [already imported] + `import SyncManager.Model exposing (Site)`). Compile-driven (elm make caught all callers: 2 for Measurement.updateChild, 1 each for Activity/Participant via Session:105/207). DISPLAY fix: `Measurement/View.elm` ncdaFormInputsAndTasks (site in scope) → `muacValueForSite site` value + `case site` UnitMillimeter/UnitCentimeter label. `muacIndicationForChild` reads stored MuacInCm (unaffected by display). **Regression test** `Measurement/Test.elm` `updateChildSetMuacTest` (group `SetMuac "125"`@Burundi → MuacInCm 12.5; Rwanda "12.5"→12.5) using exported `emptyModelChild`+`MsgChild(..)`. **DISCRIMINATION PROVEN: reverted SetMuac to raw → Burundi stores MuacInCm 125 (test fails); restored → 34 in module / 2739 full.** Verified: elm make Main Success, full elm-test 2739, elm-format clean, no Site/unused-import elm-review on touched files. **SEVERITY NUANCE (recorded at proposal): individual-encounter display was cosmetic-only [stored value correct]; the clinically-dangerous part = group-session input wrong-storage, narrow [group NCDA + Burundi + mm-entry]. User chose full fix (A) anyway.**

### Round-8 also-surfaced (lower tier; recorded, promote if a slot opens)
- **Gender change doesn't trigger report recalc** (`hedley_reports_trigger_recalculation_for_edited_person` ~338-352, same in hedley_ncda ~1107-1128): gated on `field_birth_date` change only, ignores `field_gender`; correcting gender leaves stale gender-wrong z-scores in `field_reports_data`. NCDA self-heals (activity module re-saves measurements on gender change → re-triggers NCDA recalc) but reports doesn't (compounded by R8-2). MOD. Pairs naturally with R8-2.
- **app.js `date_measured` UTC off-by-one** (`app.js:1555` `makeProgressReportScreenshot`: `today.toISOString().split('T')[0]`): UTC date → in UTC+2, local 00:00-02:00 stamps yesterday on WhatsApp report screenshot uploads. LOW (narrow window, reporting metadata). Same R2-4 timezone class, JS-side residual (R2-4 fixed Elm side; PinCode/MessagingCenter Time.utc reminders also still open).
- **hedley_stats shard-reassignment doesn't recompute OLD HC** (`hedley_stats_trigger_stats_recalculation` ~94-126 resolves HCs from current `field_shards` only): reassigning content to new shards recalcs the NEW HC but leaves the OLD HC dashboard counting content it no longer owns. LOW/niche (depends on shard reassignment).
- **Latent dead-code misroute (NOT a current bug):** `Pages/AcuteIllness/Activity/Update.elm:1041` `SetHealthEducation value` writes `nextStepsData.isolationForm` instead of `healthEducationForm` — but UNREACHABLE (no view emits `SetHealthEducation`; views use `SetHealthEducationBoolInput`). Same real-but-unreachable category as HIV `testResultDirty`. Would become a silent-drop bug only if a future view wires the constructor.

### Round-8 DISMISSED on my verification (false positive — do NOT re-propose)
- **Photo cache key mismatch (agent 4 F2) — FALSE POSITIVE.** Agent claimed `photos.js:27` read uses raw `event.request` (with access_token) while writes (`:72`, bulkPhotos.js:95) use `stripAccessToken` → permanent cache miss. WRONG: the in-app DISPLAY request carries NO access_token (confirmed by [[photo-upload-fileid-encoder-wiring]] / R6-3: "in-app URL carries no access_token"), so its URL already equals the stripped stored key → cache HIT. The DOWNLOAD request carries the token and intentionally misses (so it fetches fresh, then caches stripped at :72). Agent conflated the two. Matches the R5/R6 cleared-clean ("bulkPhotos write-key matches display read-key") and 8yr operational reality (offline photos work). **LESSON: photos.js read at :27 uses event.request, but display requests are token-less so they match the stripped write key — the read/write keys DO agree for the path that matters.**

### Round-8 cleared-clean
- **Elm update-handler control flow** — the R4-1 silent-fall-through class is EXHAUSTED. Every `Save*` pairs with the correct task-group generator + embeds the right SetActivePage redirect across all programs; only secondary catch-all is the already-fixed `updateLabsHistory`; OutMsg/extraMsgs threading sound. (One dead-code misroute noted above.)

## Round 9 — 2026-07-01 — proposals (proposed, awaiting user pick)
5 parallel agents over FRESH lenses (backend report/stats aggregation COMPUTATION / Elm double-submit guards / Elm encode↔decode round-trip asymmetry / Elm routing URL round-trip / Elm dirty-field edit-loss). RICH round — 2 strong NEW + 1 promotable + 1 MED; routing came back CLEAN. file:line point-in-time.

### Top picks
- **R9-1 — `preeclampsiaInFamily` is decoded + drives a clinical assessment but NEVER encoded → silent data loss on every prenatal Medical History save.** `MedicalHistoryValue.preeclampsiaInFamily : OccursInFamilySign` (Backend/Measurement/Model.elm:742); decoder READS it (`optional "preeclampsia_in_family" decodeOccursInFamilySign NotKnownIfOccurs`, Decoder.elm:1984); but `encodeMedicalHistoryValue` (Encoder.elm:1426-1434) emits only medical_history/physical_condition_history/infectious_disease_history/mental_health_issues/deleted/type — **NO `preeclampsia_in_family`**, and NO `encodeOccursInFamilySign`/`occursInFamilySignToString` helper exists (only the FromString direction, Utils.elm:4913). Field is captured (Prenatal/Activity/Update.elm:748, Model.elm:884), rendered (ProgressReport/View.elm:799), and **drives preeclampsia-risk assessment** (`.preeclampsiaInFamily >> (==) DoesOccur`, Prenatal/Activity/Utils.elm:2562). Backend supports it (field_preeclampsia_in_family, public REST field). So nurse's "family history of preeclampsia = yes" influences the on-screen assessment but is dropped on sync → backend stores nothing → next download defaults to NotKnownIfOccurs → answer silently reverts/lost. **VERIFIED MYSELF end-to-end (encoder key literally absent; grep=0 for encode direction).** HIGH conf, patient-safety (preeclampsia risk factor). Same class as [[photo-upload-fileid-encoder-wiring]] (a field that MUST be added to the encoder). Fix: add `occursInFamilySignToString` + `encodeOccursInFamilySign`, append `("preeclampsia_in_family", …)` to encodeMedicalHistoryValue — parallels the sibling `encodeNCDFamilyHistoryValue` (Encoder.elm:4030-4037). **Headline. STATUS: ✅ IMPLEMENTED 2026-07-01 — issue #1876, PR #1877 (branch `r9-1-encode-preeclampsia-in-family`, base develop 7ce2c6033), CI running, Copilot requested.** Added `occursInFamilySignToString` (yes/no/do-not-know, mirroring the FromString map at Utils.elm:4915; exposed) + `encodeOccursInFamilySign = occursInFamilySignToString >> string` (Encoder.elm, after encodeMedicalHistorySign) + the `("preeclampsia_in_family", …)` line in encodeMedicalHistoryValue. Regression test in Backend/Measurement/Test.elm (`preeclampsiaInFamilyEncodeTest`): encode a MedicalHistoryValue{4×EverySet.empty, preeclampsiaInFamily=sign}, `Json.Encode.object`, decode raw `field "preeclampsia_in_family" string`, assert DoesOccur→"yes"/DoesNotOccur→"no". **DISCRIMINATION PROVEN: removed the encoder line → both tests fail (Err "Expecting … preeclampsia_in_family"); restored → 2746 full.** **GOTCHA: neither `decodeMedicalHistoryValue` (Decoder) nor `encodeMedicalHistoryValue` (Encoder) was exposed — value en/decoders are internal, only measurement-level ones exposed. Exposed `encodeMedicalHistoryValue` for the test (test-module consumer → CI lint_elm_review clean, per R7-1/R7-4/R8-1 precedent); tested the raw string field rather than widen the Decoder API.** Verified: elm make Main Success, full elm-test 2746, elm-format clean. **COPILOT (2026-07-01): #1877 got 1 nit — test omitted the `NotKnownIfOccurs→"do-not-know"` case → FOLDED IN (30 in module). #1873 (R8-4) + #1875 (R8-3) Copilot-reviewed CLEAN (overview only, 0 inline comments).**
- **R9-2 — Large-scope (district-aggregated) Nutrition report drops the `|<amount>` distribution tail → district SAM prevalence misclassified (2 variants, 1 root cause).** `hedley_reports/hedley_reports.module` `hedley_reports_generate_nutrition_report_data` (~:2017 endpoint). Encoder appends `|amount`: FBF `$session_data .= '|' . amount` (:1290) → wire `"date s,w,u,m,e|amount"`; family-nutrition `$wire = "$date $muac_part|" . amount` (:1206) → `"date |amount"` when no MUAC. But the PHP large-scope parser strips NEITHER: (a) `explode(',', $parts[1])` (:2420) → `$encounter_fields[4] = "1|500"` → `=== '1'` always FALSE → EDEMA always false → kwashiorkor child (edema, normal MUAC) bucketed normal not SAM → SAM UNDERCOUNT (:2426, categorize at :3086 `muac<11.5 || edema`); (b) `(float) "|500"` (:2452) = 0.0 (not NULL) → `0.0 < 11.5` → false SAM OVERCOUNT for an AHEZA-only visit w/o MUAC. The Elm decoders (client + `server/elm/src/Backend/Reports/Decoder.elm`) split on `|` FIRST, so small-scope (single-HC) and large-scope (district PHP) DISAGREE for the same data. **VERIFIED MYSELF (encoder appends tail :1206/:1290; parser doesn't strip :2420/:2452).** HIGH conf. Fix: single helper to isolate the anthropometry chunk (`explode('|', $parts[1])[0]`) before CSV/float parse, in both loops (2409-2427 encounters + 2439-2452 family). **STATUS: ✅ IMPLEMENTED 2026-07-02 — issue #1878, PR #1879 (branch `r9-2-nutrition-report-amount-tail`, base develop 7ce2c6033), CI running, Copilot requested.** Encounters loop (:2420): `$anthropometry = explode('|', $parts[1])[0]; $encounter_fields = explode(',', $anthropometry);`. Family loop (:2452): `$muac_raw = isset($parts[1]) ? explode('|', $parts[1])[0] : ''; $muac_value = $muac_raw !== '' ? (float) $muac_raw : NULL;` (so `"|500"`→""→NULL, `"12.5|500"`→"12.5"→12.5). No unit test (backend; consistent w/ R5-3/R8-2). Verified: php -l clean, phpcs Drupal+DrupalPractice clean (tidied one 80-char comment WARNING — LineLength warnings don't fail CI but kept clean).
- **R9-3 — PROMOTE the R4 runner-up: double-tap "begin encounter" creates DUPLICATE encounters/episodes across ALL individual-participant programs (Prenatal = two open pregnancies).** Systematic: NO `Pages/*/Participant/View.elm` reads the `db.postIndividualEncounterParticipant`/`db.post<Program>Encounter` `Loading` state; the only button disable is `encounterWasCompletedToday`/`disableAction` (blocks re-create AFTER a same-day encounter completes, NOT in-flight), applied as a CSS `disabled` class while `onClick` stays wired; `maybeSessionId` from `db.individualParticipantsByPerson` isn't updated until the POST round-trips → 2nd tap re-posts. Backend post handlers set `=Loading` but don't refuse a 2nd POST. Confirmed per-page: Prenatal (Participant/View.elm:479-486, and the app has `LabelOnePregnancyEpisodeOpen` warningPopup for EXACTLY the two-open-pregnancies state a double-tap produces — headline severity), NCD:145/169, HIV:140/164, Tuberculosis:140/164, ChildScoreboard:139/163, Nutrition:162/195 + HomeVisit button :265/289, WellChild:188/218, AcuteIllness:298-300/159. Guarded siblings to mirror: Person/Relationship/Device views gate create buttons on `RemoteData.isLoading`. This is the recorded R4 runner-up ("double-tap begin encounter … MED-LOW") but SHARPER + higher severity via the Prenatal angle. Fix: thread the post `Loading` state into each `viewXAction`, OMIT `onClick` (not just add CSS class) while in-flight. MED-HIGH. **STATUS: ✅ IMPLEMENTED 2026-07-02 — issue #1880, PR #1881 (branch `r9-3-begin-encounter-double-tap`, base develop 7ce2c6033), CI running, Copilot requested.** **CHOSE A CLEANER BACKEND-LEVEL GUARD** over the agent's per-view fix: rather than thread `db.post*` Loading into ~9 views (per-action nuance, error-prone), guarded the 2 PARTICIPANT-create handlers in `Backend/Update.elm` — `PostIndividualEncounterParticipant` (:4682) + `PostFamilyEncounterParticipant` (:5096), both keyed by `session.person`: `if Dict.get session.person model.postX |> Maybe.map RemoteData.isLoading |> Maybe.withDefault False then ( model, Cmd.none, [] ) else <original post>`. ONE view-independent guard stops the HIGH-severity duplicate-PARTICIPANT/episode case (two open pregnancies etc.) for EVERY program at once (first-encounter creates all route through PostIndividualEncounterParticipant, keyed by person; it then auto-fires the PostXEncounter internally, not double-tappable). **SCOPED OUT (lower severity, noted in PR): the 10 per-program `Post*Encounter` handlers (keyed by participant) — double-tapping a SUBSEQUENT-encounter button can still duplicate an encounter WITHIN an existing episode; same guard pattern extends there if wanted.** No unit test (update guard w/ Cmd/sw deps, like R3-2/R8-4 SetActivePage guards). Verified: elm make Main Success, full elm-test 2744, elm-format clean. **GIT GOTCHA: forgot to branch before editing → R9-3 commit landed on the r9-2 branch (local, unpushed); untangled via `git reset --hard origin/r9-2` + cherry-pick onto a fresh r9-3 off develop (R9-3 commit touched only Backend/Update.elm → clean cherry-pick). PR #1879 (r9-2) was unaffected since the stray commit was never pushed.**
- **R9-4 — Dirty-flag omitted on numeric lab-result Set handlers → CLEARING a saved value silently reverts it.** `maybeValueConsideringIsDirtyField isDirty form saved = if isDirty then form else or form saved` — the flag only matters when the form field is `Nothing` (cleared). Recurrent/NCD lab handlers update the value but NOT the `…Dirty` flag, so clearing a previously-saved value → dirty stays False → `or Nothing (Just X) = Just X` → old value persists. A/B tell: the *Activity* handler sets `…Dirty=True`, the *RecurrentActivity* one doesn't. Sites: `hemoglobinCount` (Prenatal/RecurrentActivity/Update.elm:607/1541; vs Activity:2863), `hba1cResult` (NCD/Activity/Update.elm:1744 — its ONLY numeric handler), `sugarCount` (Prenatal/RecurrentActivity:669/1578 + NCD/RecurrentActivity:276), `hivViralLoad` (Prenatal/RecurrentActivity:755/1638). HIGH mechanism / **MED-narrow harm** (only the CLEAR-to-blank action is lost; changing to another number is preserved via `or`). Fix: add `…Dirty=True` to each handler. (Agent ruled out: prePregnancyWeightDirty=deprecated/no-view; select-field testResult can't become Nothing so unreachable [= why HIV testResultDirty is unreachable]; NCD creatinine/bun/alt/ast use a separate no-dirty-flag design.) **STATUS: ✅ IMPLEMENTED 2026-07-02 — issue #1882, PR #1883 (branch `r9-4-lab-dirty-flags`, base develop 7ce2c6033), CI running, Copilot requested.** 8 handler edits: `hemoglobinCount`/`sugarCount`/`hivViralLoad` in Prenatal/RecurrentActivity/Update.elm (each ×2 → `replace_all`), `hba1cResult` in NCD/Activity/Update.elm:1744, `sugarCount` in NCD/RecurrentActivity/Update.elm:276 — each `{ form | X = String.toFloat value }` → `{ …, XDirty = True }`. Confirmed the 4 `…Dirty : Bool` fields exist in Measurement/Model.elm. No unit test (compiler-checked one-field additions; testing clear-revert needs form+WithDefault fixtures — like R6-1/R5-1). Verified: elm make Main Success, full elm-test 2744, elm-format clean. **→ R9-2/R9-3/R9-4 all implemented; Round 9 fully done (R9-1..R9-4 + routing-clean).**

### Round-9 cleared-clean / negative
- **Routing/URL round-trip is SOUND.** All 67 `UserPage` ctors have matching pageToFragment + Router.elm parser cases; no ambiguity (Elm `s` matches whole segments; the 4 `person/*` routes ordered so `parseOrigin` custom-parser rejects UUIDs); all param `…ToString`/`…FromString` enum pairs + nested `+++`-composite initiators round-trip; `startsWith` initiator parsers ordered specific-before-general. Only 2 LATENT-UNREACHABLE asymmetries (Backend/Person/Utils.elm): `IndividualEncounterOrigin HomeVisitEncounter` fragment `"home-visit"` decodes to WellChildEncounter (0 constructions), and `AcuteIllnessContactsTracingActivityOrigin`→`""` fragment (feeds backend action, never a URL). Not bugs; same real-but-unreachable category as SetHealthEducation/testResultDirty.
- **Encode↔decode round-trip otherwise SYMMETRIC** — agent diffed all 27 per-entity Encoder↔Decoder pairs; only R9-1 is a genuine asymmetry. Ruled out: `deleted` (write-only const), embedded read-only relations (`individual_participant`/`stock_update`), server-assigned `uuid`, and encoder-helper/nested-tuple keys my regex under-counted (all round-trip).
- **MUAC strict `<` (reports) vs `<=` (NCDA/canonical `muacIndicationForChild`)** at exactly 11.5/12.5 — cross-report convention difference (each internally consistent with its own Elm counterpart), LOW/likely-intentional — the recorded boundary-inconsistency class, NOT a fix.

## Forward-run regime — 2026-07-02 (rounds 10+ recorded elsewhere)
Flow changed: rounds 10+ are DISCOVERY-ONLY (Fable 5, run forward toward round 50, dry-stop after 3 empty rounds), recorded in [[improvement-backlog]] with [[improvement-coverage-map]] tracking lenses/cleared/declined classes; Opus 4.8 implements from the backlog only on user instruction. See [[improvement-forward-run-flow]]. This ledger stays authoritative for rounds 1–9 history and all PARKED/DECLINED rationale; still-open runners-up were seeded into the backlog as B-001..B-022 (2026-07-02).

### PARKED from the forward-run backlog (rationale lives here, status marker lives in [[improvement-backlog]])
- **B-146 — deploy safety cluster part 2 (human-error hardening). 🅿 PARKED by the user 2026-08-09**, immediately after I presented it as the next item with a full re-verification. Presented as three parts; the presentation itself changed two of them, which is worth remembering if it ever comes back: **(b) was largely refuted** — the filed claim that "nothing ties dist to the target site" is wrong, because `.ddev/commands/web/gulp` copies the per-site `Config.Deploy.elm` from the private infra repo keyed on `$PANTHEON_NAME` and refuses to publish without it, the same var deploy reads; **(a) had shrunk** because B-143 (#2055) and B-144 (#2057) already closed its failure branches; only **(c)** grew, and it is confirmed by running rather than reading. No fix was requested for any part. ⛔ Do NOT re-pitch — if a deploy incident makes it relevant, re-open from the corrected text in the backlog entry, not from the original filing.
- ⚠ Side effect of the park: B-146(c) — a failed `gulp publish` leaving production config in the tracked `client/src/elm/Config.elm` — now has **no home**. B-144 is what made that path reachable (before it, Elm errors were swallowed so publish always completed and always reverted). Recorded on PR #2057 as an inline comment so it is not lost.
