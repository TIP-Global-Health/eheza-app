## Seeded from rounds 1–9 ledger (2026-07-02) — pre-regime items, verification labels per entry

## Round 10 — 2026-07-03 — lenses: Fetch wiring / server deleted-filter / completion-vs-save / port contract / numeric input
All findings below SELF-VERIFIED on develop @4e37b5a69 unless noted. (Agents initially read a stale tree — user switched the shared checkout to r8-2 mid-run — so every fact was re-verified on develop; line numbers below are develop's.)

## Round 12 — 2026-07-03 — lenses: AQ worker idempotency / backend presave integrity / clinical diagnosis engines / edit-loss save-noop / i18n interpolation mechanics
(agents landing; verified findings appended here)

## Round 15 — 2026-07-03 — COVERAGE SWEEP (AcuteIllness / WellChild+NCD / Nutrition+Dashboard / SyncManager / PWA-SW)

## Round 14 — 2026-07-03 — COVERAGE SWEEP (Prenatal / FamNut+HomeVisit / ChildScoreboard+NCDA / Person+relationships / backend infra)

## Round 13 — 2026-07-03 — COVERAGE SWEEP mode (unit audits: stock / group-ed / TB / HIV / Measurement.Utils)

## Round 11 — 2026-07-03 — lenses: restful join-shape / stats SQL math / login-session state / Dexie schema / visit scheduling
Findings self-verified on develop (spot-checked; the Dexie agent itself re-verified against the main checkout on develop after its worktree was GC'd).

## Round 16 + conversion pass — 2026-07-12 — develop @2c0322070 (small-targeted-fixes-only constraint in force; NO architecture changes)
**Status: PARTIAL — 4 of 8 agents delivered (fetch-wiring lens, minors re-verification, a11y inventory, 3 test-survey designs); the other 6 (hedley_migrate, hedley_ncda, dead-code, guard-tooling, revivals items 1/3-6, test-hardening remainder ×2) were killed by a session limit and RESUMED 2026-07-12 — their entries land in a follow-up append. Do not consider R16 closed.**

## Round 17 findings (2026-08-03, @c37f2fe3c)

## Round 18 findings (2026-08-04, @c37f2fe3c)

## Round 19 findings (2026-08-11, @c8a03349f — develop after #2063/#2061/#2065 merges)

## Round 20 findings (2026-08-17, @develop post-#2093; wave 2 = parser-default + generated-artifact lenses)

## Round 21 findings (2026-08-18, DATA-INTEGRITY mode — ihangane live copy snapshot 2026-08-05, local DDEV)

## Round 22 (2026-08-19, DUPLICATION-ELIMINATION mode per approved plan; find-then-fix-on-approval)

### Round-10 minors (recorded, not queued)
- AcuteIllness symptom-duration inputs: `String.toInt "" → Maybe.withDefault noChange` (`Pages/AcuteIllness/Activity/Update.elm:186-203` ×3) keeps the previous Dict value and the box snaps back mid-edit (visible, self-correcting; EDD inputs show the correct empty-clear pattern). Polish only.
- District/province enumeration for large-dataset reports scans raw locality field tables with no bundle/deleted filter (`hedley_reports.module` ~:2278-2283 + `recalculate-large-datasets.php` ~:42-45) → junk/empty `report_data` nodes for localities existing only on deleted persons; inner data queries filter correctly so no wrong numbers. LOW (agent-verified, spot-checked only).

### Round-15 notes
- **WellChild + NCD workflow/persistence = CLEAN except B-072.** WellChild task-gating/immunisation/medication/next-steps/newborn-branching clean; NCD task-gating/initial-recurrent-split (phase-partitioned medicate/refer + shared measurement field-preservation via `or`)/medication-distribution/referral clean. MINOR (inert): WellChild HeadCircumference REST plugin (`HedleyRestfulWellChildHeadCircumference.class.php`) omits `field_zscore_age` from `$fields` though it's computed server-side (`hedley_activity.module:328-331`) + exposed by the Height/Weight sibling plugins — INERT: client never sends/decodes zscores (computes live via ZScore module), reports read field_zscore_age via direct SQL, so no consumer; consistency-only fix.
- **AcuteIllness workflow/persistence sweep = CLEAN except B-070.** Task-gating (R6 covidTesting asymmetry re-confirmed inert), multi-visit episode, contacts-tracing lifecycle (re-save guarded by `disabled = isJust measurements.contactsTracing`), medication accounting, next-steps/referral, per-form count-vs-save (malaria round-trip clean) all audited clean. SetHealthEducation→isolationForm misroute (R8) re-confirmed unreachable.
- **Dashboard sweep = CLEAN except B-069** (+ B-009 NaN class re-noted at a new line `viewGoodNutrition:1451-1454`, same recorded class). Case-mgmt totals, follow-up totals (counted paths), period/month selection, village-vs-HC scoping, stats consumption all clean; B-054 filterResolvedFollowUps inversion is elsewhere, unchanged here.
- **Nutrition (individual) workflow/count-vs-save = CLEAN.**
- **SyncManager orchestration (non-poison) = CLEAN except B-071 + one cosmetic minor.** Programmatic cross-check of all 206 authority + 8 general entity variants → ZERO upload/download type-string mismatches, ZERO dup type strings (no entity mis-decodes to wrong variant/shard). Cursor bookkeeping (max-revision-over-saved, save-success-gated, can't advance past unsaved), state-transition order (FK-safe upload sequence, no skip/loop), save-result handling, upload-lane deletions (correct localId sources), timers all CLEAN.
- **PWA service-worker / offline-request layer = CLEAN** (zero new bugs). Request routing (type/uuid/method/param branches, no cross-file regex overlap), offline write path (POST uuid/type/shard, PATCH full-node echo via sendRevisedNode + change recording), cache correctness, SW lifecycle (SkipWaiting/clientsClaim/controllerchange/sendRevisions), response construction — all audited clean. Ruled-out-as-inert: `deleteNode` records no change (DEAD — no DELETE issuer; deletions flow via PATCH deleted=true), object-key `where({type,nurse})`/`{type,participating_patients}` (Dexie 3.2.7 falls back to type index + in-memory filter, no SchemaError), photo cache-key asymmetry (by-design, display URLs token-less = stripped key), makeUuid statusText/content-type typos (cosmetic, read via .text()). Recorded items re-confirmed present (B-002/006/035/040, R8-3/B-039 deleted-filters).
- MINOR (cosmetic, self-healing): `SyncManager/Update.elm:1274` entering SyncUploadAuthority sends the PRE-update zipper (`sendSyncInfoAuthoritiesCmd zipper` vs the surrounding pattern's `zipperUpdated`, e.g. :142) → model vs localStorage disagree on the current authority's `status` label only (lastFetchedRevisionId/remaining/hash identical); next handler re-sends corrected status. No cursor/data risk. Fix: send `zipperUpdated`.

### Round-14 minors (batch 2)
- Scorecard backend `continue` on empty Vitamin A field can skip MUAC/stunting recording (`hedley_ncda.module:804-852` — `if (!$items) continue;` also skips the MUAC/stunting block below it). LATENT/masked: Vitamin A is a required task on the scoreboard NCDA so a completed save always has field_receive_option. Fragile coupling; fix = move the MUAC/stunting block out from under the receive_option `continue`. LOW.
- FamilyNutrition/HomeVisit `EatenWithWater` feeding question is computed then discarded (`Measurement/View.elm:3885` — second tuple bound to `_`), so it's never rendered/settable. INERT: no consumer reads EatenWithWater; `ifNullableTrue EatenWithWater Nothing = Just EverySet.empty` so save is unaffected; longstanding (pre-2024 elm-review autofix). Not action-required.
- Demographics-report 50yr age-band gap (`hedley_admin/scripts/bootstrap-demographics-report.SQL`: lt50y `+50yr > :limit` + mt50y `+50yr < :limit` → someone exactly 50.0y at :limit matches NEITHER band, dropped from total). ⚠ GATED-ON-LIVENESS: this is the `hedley_admin/scripts/*-report` family that R5 established is largely UNUSED (R5-4/R5-5 precedent) — confirm the demographics report is actually run before treating as actionable. Fix if live: make mt50y `<=`. LOW.

### Round-14 notes
- **Prenatal workflow/persistence sweep = CLEAN except a re-find of B-037** (dup, NOT a new item — corroborated by a 2nd agent). The agent framed it as "new / inverse of B-037" but that's a misread: B-037 IS the count(4)≠map5-requires(5) mismatch at `View.elm:3104-3108` vs `Utils.elm:4284-4291`; R9-1/#1877 touched the ENCODER (`encodeMedicalHistoryValue`→JSON), a different function, so it did NOT "flip" anything — `toMedicalHistoryValue`'s `Maybe.map5` has always required all 5 fields. Fix unchanged (add `maybeToBoolTask form.preeclampsiaInFamily`). All other Prenatal sub-areas (labs lifecycle incl. R4-1 class, task-gating, recurrent split, EGA/EDD threading, outcome/close, obstetric×2/social/followUp/ultrasound/LMP/symptomReview count-vs-save) audited CLEAN.

### Round-14 minors
- `hedley_device_assign_shards` education_session EDIT branch leaves `$shards` undefined (`hedley_device.module:307-316`) → PHP notice only; `empty()` returns cleanly + edits intentionally don't reassign shards. Non-bug (notice hygiene).
- Ops-doc drift: CLAUDE.md names the flag `stock_management`, but code reads `hedley_admin_feature_stock_management_hc/_village_enabled` (`hedley_admin.module:646-647`, matching Elm `FeatureStockManagementHC/Village`). Code is internally consistent — only the doc/flag-name would mislead an operator running `drush vset`. Doc fix, not code.

### Round-13 minors
- **HIV `testResultDirty` recorded rationale CORRECTED** (Round-6 runner-up): the ledger said "UNREACHABLE (result picker only emits Just)" — WRONG. `testResult` DOES reach `Nothing` via the resultPositive/runHIVTest bool toggles + end-encounter revert (`Pages/HIV/Activity/Utils.elm:328-332` hardcodes `testResultDirty=False`), so a stale saved `TestPositive` CAN be resurrected into the form. But it's genuinely INERT: no Elm decision logic and no backend report/stat reads `hiv_diagnostics.field_test_result` (report hits at hedley_reports.module:6913+ are prenatal/NCD hiv_test/pregnancy/syphilis; hiv_diagnostics :7249/7310/7325 are presence-only). Fix if ever touched: `testResultDirty = form.testResultDirty`. Still not worth a PR (reachable-but-inert, like SetHealthEducation dead-code).
- HIV clinician-gated observations (noted, not bugs): prophylaxis-only prescription (Bactrim/Co-Trimoxazole, no ARV) can't complete the Medication task (`recordMedicationsFormAndTasks` mandatory group = ARVs only) — likely intended (ARV always required); treatment-review becomes mandatory on the initial encounter after prescribing (`expectMedicationTask TaskTreatmentReview`) — plausibly intended for transfer-in patients already on ARV. Both clinician-gated, no code change without sign-off.
- HIV Progress Report is STUBBED — the report link (`Pages/HIV/Encounter/View.elm:124-131`) is commented `@todo`; no ProgressReport module exists. Incomplete feature, not a bug.
- Group-nutrition `SaveMotherFbf` copy-paste (`Backend/Session/Update.elm:153` reads `saveLactationRequest` tracker) — INERT: `save*Request` trackers are write-only (grep: no reader outside Session/{Update,Model}.elm). No behavioral impact.
- **`"wight-loss"` misspelling** in the TB + HIV symptom enum (`Backend/Measurement/Utils.elm:4137/4155/4515/4569`, `hedley_tuberculosis...field_base.inc:210`, `hedley_hiv...field_base.inc:135`) — INERT: symmetric write+read on both client AND server, no correctly-spelled consumer (the `"weight-loss"` at `Pages/Tuberculosis/Activity/View.elm:410` is a DOM input id, not a value compare; verified). Data-hygiene only; renaming would need a data migration of stored values so NOT worth touching. Recorded so a future dev doesn't "fix" the spelling on one side and break the round-trip.
- Stock HC-level view (`Backend/StockUpdate/Utils.elm:15-42` `generateStockManagementData`) counts ALL `measurements.stockUpdate` as receipts (incl. village-tagged, since village saves set `shard = Just healthCenterId`) but its distributions come only from group childFbf/motherFbf — village `AhezaChild/AhezaMother` consumption isn't in StockManagementMeasurements → if a site runs village stock alongside HC stock, HC balance can OVERSTATE. LOW / MED-low confidence (semantics-dependent — moot if stock updates always have `village==Nothing` at HC-run sites). Verify intended HC-vs-village stock semantics with the team before acting.
- `viewNumberInput` (`Pages/Utils.elm:704-709`) sets only HTML `min="0"`; `String.toInt "-5"` parses → a hand-typed negative receive quantity isn't blocked client-side (generic, B-016-adjacent, not stock-specific).

### Round-12 minors
- **Person shards NOT recomputed on health-center change** (`hedley_device_assign_shards` short-circuits on non-empty field_shards; `handleChanges` doesn't ignore person.health_center): a person whose HC changes keeps `field_shards=[old HC]` → syncs to the wrong HC. NOT reachable by the official client (EditPerson re-sends the ORIGINAL HC via applyDefaultValuesForPerson; backend comments confirm HC is effectively immutable). Defense-in-depth only — a future importer/client that moves a person would silently mis-shard them + all their data.
- AQ dedup "stuck task" check (`hedley_general.module:75-80`) compares `time() - $task->created` (ENQUEUE time, not processing-start) vs 20min → a task backlogged >20min in QUEUED is judged "stuck" the instant it starts, allowing a duplicate enqueue. Harmless (recompute workers idempotent) → herd/wasted work only. LOW.
- view_export returns FAILURE (never FAILURE_RETRY) if the final-batch `drupal_mail` fails (`:268-271`) → a fully-built CSV is abandoned on a transient SMTP hiccup, user never emailed, cron deletes it after 7 days. LOW (email, not data).
- super_user_mode hard-delete gap: none of hedley_ncda/reports/stats implement `hook_node_delete`, so a hard-deleted measurement (only possible under `hedley_super_user_mode`) leaves person `field_ncda_data`/`field_reports_data` + HC stats counting a gone node until the next measurement. LOW (rare admin escape hatch).
- `Translate.elm:13034` Kirundi `MissedDosesOfMedication`: `... ++ String.fromInt val ++ "y'imiti"` missing the leading space → renders "3y'imiti" (number correct, cosmetic spacing only; Kinyarwanda sibling has the space). Trivial 1-char fix; fold into any future i18n touch. (Same benign unit-abutting pattern in memory/storage-quota fields 12910/12912/26291/26292 — consistent, not defects.)

### Round-11 minors
- Case-management "month 13" cutoff (`hedley_stats.module:3195`) `strtotime("last day of -1 year") + $one_day` isn't midnight-anchored relative to measurement time-of-day → measurements dated exactly on the window's first day can mis-bucket into the hidden baseline month. LOW, systematic-but-tiny (only bites if measurement dates carry time-of-day).
- `HedleyRestfulEducationSession.class.php:108` copy-paste `unset(uuid_individual_participant)` instead of `uuid_village_ref` → raw column leaks into payload (client ignores). Cosmetic; fold into B-046.

### Round-10 minors (batch 2)
- Prenatal `toMedicationValue` syphilis guard omits `syphilisAdverseEventsHospitalization` from its `List.all isNothing` check (`Pages/Prenatal/Activity/Utils.elm:4496`) while hypertension/malaria/anemia guards include theirs — currently UNREACHABLE via UI (hospitalization input only shows after adverseEvents==Just True, and the update func clears it on change); latent copy-paste asymmetry, fix opportunistically.

### Minors PROMOTED to queue — B-075..B-091 (2026-07-12). All 23 recorded minors re-verified at 2c0322070: 23 STILL-VALID, 0 fixed, none touched by #1889–#1917. Full mechanism detail stays in the original minor bullets above; entries below carry re-verified anchors + fix. NOT promoted (stand as recorded): eatenWithWater dead-value (product), HIV testResultDirty (inert), HIV clinician-gated observations, HIV ProgressReport stub, "wight-loss" tombstone (do-not-fix), EducationSession unset (rides B-046).

### A11y batch — B-092..B-095 (2026-07-12 full inventory at 2c0322070: 40 `img` sites — only 2 with alt, both in `Utils/Html.elm` :101/:162; 72 icon-only `link-back`/`icon-back` controls; 13 other icon-only controls; ZERO `aria-label`/`title` anywhere in Elm views). Rules: decorative icons (exclamation-*, timeline dots, chevrons) get `alt ""`; content images need REAL alt (WellChild ProgressReport patient photo `View.elm:1529`, StockManagement signature `View.elm:817`, Person avatar `View.elm:498`, shared `Pages/Utils.elm:1249 viewPhotoThumb`); icon-only interactive controls get `aria-label` (attribute helper + Translate entries, English-first per policy). Site lists re-derivable by grep (`img [`, `"link-back"`, `"icon-back"`); full per-file inventory in the R16 a11y agent report (session 5c144238, 2026-07-12). Zero-logic-risk markup additions.
### Test-hardening track (TH-track; ZERO production risk — tests only; fold into EXISTING specs per the CI-budget rule). Cross-cutting assertion pattern verified: `customButton` (`Pages/Utils.elm:1318-1335`) emits `classList [("active", active), ("disabled", not active)]` and attaches onClick ONLY when active → negative assert = `toHaveClass(/disabled/)` + `not.toHaveClass(/active/)`, flips after answering. More TH-items incoming from resumed agents (prenatal reference, acute-illness, nutrition, well-child, ncd, hiv, tb, stock-management, lab-tech + engine unit-pinning).
### TH-track batch 2 (2026-07-12, from the track-parent agent). ⚠ CROSS-CUTTING: TWO Save-button DOM variants at 2c0322070 — (1) `customButton` (`Pages/Utils.elm:1318-1335`; toggles active/disabled, onClick only when active; used by Prenatal/StockManagement/lab-results) and (2) AcuteIllness INLINE variant (`AcuteIllness/Activity/View.elm:516-522/996-1002/1514-1520`; toggles ONLY `disabled`, never `active`, and onClick STAYS ATTACHED when disabled — only Semantic UI pointer-events:none blocks real clicks; a `force:true` test click on a "disabled" AI Save would persist an incomplete form). Legacy group-form variant (`Measurement/View.elm:587`, id="save-form") is also disabled-only. Universal recipe: negative = `toHaveClass(/\bdisabled\b/)`, positive = `not.toHaveClass(/\bdisabled\b/)` (+ `/\bactive\b/` only for customButton). Part-B context: NCD staging is ALREADY fully pinned (`Pages/NCD/Test.elm`, 553 lines) — no NCD pin issue; big existing suites: AcuteIllness Test.elm 920 lines (initial-encounter paths), Prenatal/Activity/Test.elm 1336 lines (staging + lab diagnoses), Backend/NutritionEncounter/Test.elm 336. Recommended order: TH-00 → TH-13 → TH-19 → TH-14 → TH-18 → TH-15 → TH-11 → TH-16 → TH-17 → (TH-12 upgrade). First-week batch: TH-00 + TH-13 + TH-14 + TH-19 (<10s added CI wall-time total).
### Guard-tooling track (G-track; CI/lint guards, ZERO production-code risk). Verified 2026-07-12 at 2c0322070: elm-review already fully wired in CI (`lint_elm_review` job, `.circleci/config.yml:9,54-60`; jfmengels/elm-review 2.16.3; `client/review/src/` has ReviewConfig only — custom rules drop straight in). NoDebug.Log + NoDebug.TodoOrToString ALREADY ACTIVE (`ReviewConfig.elm:64-72`) → a "Debug guard" is MOOT. New cheap guards slot into ONE new `lint_guards` CI job (cimg/python, `ci-scripts/test_guards.sh` dispatcher, <1 min). Delivery order: G-01+G-02+G-03 as the first PR, then G-04, G-05 (needs curation), G-06, G-07. Rejected as infeasible/moot: decoder/encoder/Revision parity guard (Elm exhaustiveness already covers it — residual risk is exactly the wildcard sites, i.e. G-01), blanket kirundi/somali coverage (Nothing is intentional for units — curated Tier-1 is the right shape).
### Ledger-revivals verification (2026-07-12, @2c0322070) — 2 queued, 2 already-fixed, 1 moot, 1 recorded-only
- ✅ ALREADY FIXED, do not build: (a) Dashboard decoder tolerant-decode follow-up — PR #1834's idiom now covers all list/set sites (`Backend/Dashboard/Decoder.elm:319/:352/:394` use decodeListDroppingUnknown; decodeEverySet itself is element-dropping at `Utils/Json.elm:60`); remaining decodeWithFallback sites are SCALAR defaults (:112 visible "X" sentinel, :281/:282/:323 enum/seq defaults) — different class, LOW, no action. (b) persistentStorage result — fully plumbed since commit f7da0a046 (app.js:485-487 → port → `App/Model.elm:133` → Device page :89); only a missing `.catch()` remains, negligible.
- 🚫 MOOT: `fix-well-child-encounters-warnings.php` — retired one-shot drush script (CLI-guarded, zero references anywhere); its unset()/double-wrap flaw is dormant. Optional: delete the script (fold into dead-code batch).
- 📓 RECORDED-ONLY (not queued; fix would be a behavior change → fails the high-certainty constraint): boolean `deleted` decode inconsistency — 14 decoders use fail-open `decodeWithFallback False bool` (resurrect direction) vs 6 strict `bool` (PmtctParticipant/IndividualEncounterParticipant/HealthCenter/FamilyEncounterParticipant/Relationship/Person); backend-controlled field, improbable trigger. Full 14-site list in the R16 revivals report (session 5c144238).

### R16 hedley_ncda audit findings (2026-07-12, @2c0322070) — B-098..B-101 + B-078 enrichment above
### R16 dead-code batch (2026-07-12, @2c0322070) — B-102..B-107, all PROVEN dead (every discovery channel checked: Drupal hook/theme/ctools/RESTful auto-discovery, features strings, JS↔Elm ports, gulp/npm/CI/ddev/Robo/docs CLI channels, dynamic gulp-load-plugins surface, dynamic Drupal variable names). ⚠ REFUTED, DO NOT DELETE: CounselingSessions RESTful plugin (the old "dead code" note is wrong — it's in HEDLEY_RESTFUL_SHARDED `hedley_restful.module:88`, serves legacy counseling_session nodes on full-sync; Elm+sw.js+migration still fully wired; only the client UI ACTIVITY is retired). Also NOT queueable: syncmetadata plumbing (old-device IndexedDB migration behavior), gh-pages `deploy` task cluster (documented README channel, PLAUSIBLE only), AllDict full remove-machinery prune (would kill the remaining 6 Debug.todo and unlock --optimize, but needs an `update` behavior narrowing = refactor; see [[client-cannot-use-elm-optimize]]).
### R16 ncda minors (recorded, not queued)
- `hedley_ncda_get_triggering_measurement_types()` (:1198-1215) includes well_child_mebendezole / well_child_vitamin_a / well_child_ecd which are loaded (:464-466) but classify into NO bucket (saved from the pregnancy-summary else-branch only by the field_weight guard — those bundles have none) → their data never reaches the aggregated scorecard, BY PARITY with the client scorecard (rows derive exclusively from CHW questionnaires, `Pages/WellChild/ProgressReport/View.elm:2484-2560`; the client-side divergence is B-067). Net effect: pointless AQ recalcs + node loads + misleading comment. Efficiency cleanup candidate, NOT a bug fix.

### R16 hedley_migrate audit findings (2026-07-12, @2c0322070) — B-108..B-112. Liveness frame: migrations run at ddev restart/CI/`./install -d` — never production runtime; severities env-capped.
### R16 migrate minors (recorded, not queued)
- `hedley_migrate.drush.inc:8` legacy `create-dummy-nurses-devices` hardcodes HC nid 7089 (dangling on fresh installs) + writes pins.txt/pairing_codes.txt to CWD — legacy one-off tooling, not queued.
- Prior "empty required dates silently coerced to ''" concern CLEARED empirically: empty optional dates produce NO field row (intended semantics); Migrate never validates requiredness.

### TH-track additions from the R18 E2E coverage map (TH-20..TH-27; full detail in the audit — key facts inlined):
- **TH-20** ⭐ must-NOT-save AI incomplete form: extend acute-illness nurse test 1 — fill 1 of 3 symptom tasks, assert save `disabled`, `dispatchEvent('click')`, assert still on activity + no symptoms node. LANDS WITH B-147's fix (fails today — that's the point). +~20s.
- **TH-21** make the NCD recurrent lab-results test falsifiable (today its Save is inside `if(isVisible)` with `ncd_labs_results` never asserted — spec defect D1, also nullifies b031's regression signal). +~10s.
- **TH-22** site-aware gate helpers: derive bad/good MUAC from the site (today 125/14 are wrong-by-construction on Burundi; CI is rwanda-always). Step 2 (separate decision): a burundi e2e job ≈ +25min CI.
- **TH-23** un-ended-encounter integrity: `endPrenatalEncounter` must assert the encounter page went hidden (spec defect D2 — a never-ending encounter looks ended); make the fundal-height/birth-weight gate checks unconditional (D3 — they vanish silently if the input stops rendering). ~0 cost.
- **TH-24** double-tap duplicate guard (delta b003): dblclick a save, assert exactly ONE node; needs a `countMeasurementNodes` drush helper (booleans-only today). +~15s.
- **TH-25** first-ever task-counter assertion (`expectTasksCount` helper; delta 42fc8ef93 heart-murmur): N-1/N → N/N around the question, save active only after. +~10s.
- **TH-26** stock form reset on re-entry (delta b004). +~20s.
- **TH-27** negative-input guards (delta b084): type `-5`, assert minus stripped, nothing negative saveable. ~0 cost.
- Spec-defect cleanups to fold in: D4 group-session blind `3000` fill + force-clicked Save whose gate popup isn't in the dismissal set (false-pass + latent flake); D5 hand-rolled gate checks weaker than shared helper; D6 list (lab-tech `toBe(false)`-on-error, reporting baseline `??0` degradations, participant2 never asserted, CSV button-visible-only).
- Suite facts worth keeping: ZERO task-counter assertions suite-wide; force-click on a disabled button of EITHER variant is a silent no-op (pointer-events hit-test) — the AI hazard is keyboard, not force-click; per-test overhead ~60-90s so extending beats new tests 10-50×; suite is rwanda-only by construction.

### TH/G tracks SCOPED at HEAD (R20 wave 3, 2026-08-18, @9dc7dd787) — implementation-ready ordered plan; NOTHING mooted, 3 premises stale
- **Stale premises — do NOT build as recorded:** (1) TH-20's "fails today" demonstrator — B-147 fixed by #2073 (fb8581fb6): the AI inline Save variant is GONE, all 13 AI+Prenatal saves ride `saveButton`/`customButton`; build TH-20 as a REGRESSION PIN (disabled + dispatchEvent no-op + no node). (2) TH-08's numbers — B-072 fixed (#1952), heartMurmur counted at `NCD/Activity/Utils.elm:304`, CoreExam **N=8** now; design (omit head-hair) intact. (3) TH-27 — B-084 fixed (`dropLeadingMinus` at `Pages/Utils.elm:722/:741`); build as regression pin. Cross-cutting: the "TWO Save-button DOM variants" recipe is stale — remaining variants are customButton (active/disabled) and legacy group `#save-form` (`Measurement/View.elm:636/:1911`, disabled-only); TH-00's docstring documents THIS landscape.
- **Shrunk:** TH-18 (hypertension hierarchy partially pinned via recheck tests `Prenatal/Activity/Test.elm:1183+`; emergency-referral aggregation still zero pins), TH-19 (MUAC describe exists at 11.0/12.0/13.0 — the ==11.5/==12.5 boundaries, MUAC+Edema combo, over-6mo asymmetry still un-pinned).
- **All else STILL-VALID** (TH-00..TH-27, D1-D6, G-01..G-07 premises re-verified individually; anchors in the wave-3 report). Notables: TH-21/D1 correction — `ncd_labs_results` IS in `queryNCDNodes`' known list (`ncd.ts:853`); the SPEC's expectedTypes omits it. TH-23/D2 — `endPrenatalEncounter` (`prenatal.ts:120-137`) still never asserts departure. G-02's site list GREW: 4× elm.json + `install_client.sh:13-14` + `test_elm.sh:17-18` + `install_elm_review.sh:12`, incl. elm-test 0.19.2-0 pins. G-04's partition anchors moved: `.circleci/config.yml:143/:200/:257`. ⚠ Implementers must RE-DERIVE task counts N per measurement form — the #2007 range work touched them; recorded Ns were re-derived only for TH-08/TH-13.
- **CI budget:** whole plan except TH-22-step-2 adds ~2-3 min spread over the two e2e shards + one new <1-min parallel `lint_guards` job — within the 25-min goal. TH-22 step 2 (Burundi job) = +25-30 min wall, gated on user.
- **ORDERED PLAN:** PR1 = G-01+G-02+G-03 (lint_guards job + `ci-scripts/test_guards.sh` dispatcher; bundle re-verified). PR2 = release-signal e2e: TH-00 + TH-20 + TH-21(D1) + TH-23(D2+D3) + TH-27 (~+35s). PR3 = unit pins TH-16/17/18/19 (elm-test only). PR4 = G-04 partition guard (BEFORE the fold-in wave). PR5 = fold-ins TH-13(gated Q1)+TH-14+TH-15+TH-08. PR6/7 = fold-in batches by shard (TH-02/04/05/10/12 | TH-01/03/06/07/09/11). PR8 = TH-25+TH-24+TH-26+D4+D6 (D5 only on Q4-yes). PR9 = G-06 (after e2e wave). Then TH-22 step 1; G-05 on curation; G-07 last if wanted.
- **USER QUESTIONS (blocking the marked items only):** Q1 TH-13/fold-in class — does starting the TH track supersede the 2026-07-05 "at this stage" decline of the B-037 e2e expansion? Q2 TH-22 step 2 — approve a Burundi e2e job (+25-30 min) or stay rwanda-only with site-aware helpers? Q3 G-05 — draft the Tier-1 clinical key list for sign-off, and who sources kirundi/somali text? Q4 D5 — consolidate the 6 hand-rolled save helpers (test churn) or skip? Q5 G-07 — want the alphabetical ratchet at all before September?

### R20 wave-3 batch — Completion report vs client `expectActivity` (Prenatal/AI/NCD/HIV/TB compared; the R19 Unit-4 pickup). ⚠ WHOLE BATCH GATED on one user answer: is the Completion admin report actually used (and how/when are the `completion-generate-*.php` scripts run)? Data exists on live so the pipeline has run, but per the B-142 lesson liveness is the user's call — a metric nobody reads makes these moot. Anchors: PHP = hedley_reports.module, CLI = client Pages/<X>, DEC = server/elm Backend/Completion/Utils.elm. All orchestrator-verified at anchors 2026-08-18 except where noted.

### R21 wave C census + clean verdicts (recorded facts, no further items)
- **Structural duplicates CLEAN**: zero undeleted duplicates per (individual encounter, type) across all 10 encounter kinds and per (session, person, type) for groups. Raw duplicates exist but every extra copy is `field_deleted=1` — the manual `delete-duplicate-measurements.php` ops cleanup is CURRENT (keeps highest-vid = exactly the copy `decodeHead` shows since nodes.js sorts vid DESC; double-POST pairs still minted, latest 2026-04-18). 📌 Data-evidence that at least ONE hedley_admin script IS actively run — consistent with the family rule (liveness = user's call, per-script).
- **Unit-confusion damage census (B-122 class; writer open on DEPLOYED, closed by unreleased #2007 — these columns are the running cost of not shipping):** NCDA birth weights kg-typed in grams field 1,821/14,076 (group_ncda 34.5%, well_child_ncda 25.2%; 732 in 2025 + 101 in 2026); pregnancy-summary birth weight **214/324 = 66% kg-typed**; grams-typed child weights in kg fields 2,617 (1,485 in 2025-26); **mm-typed MUAC 2,036 — acute-illness MUAC alone 1,721 = 13.8% of the bundle** (⚠ consumer: `muacIndicationForChild` red ≤11.5cm, so a 125 "mm" reads GREEN → SAM missed on those rows); metre-typed mother heights 432 (118 in 2025). Repair of historical values = clinically sensitive product decision, NOT proposed; census feeds the release case + any future repair decision.
- **Clinical-plausibility census** (descriptive, for clinicians — full table + samples in scratchpad/r21-waveC-checks.md): temperature ×10-typed ~730 rows; BP cmHg-typed ("12/7") 614 rows (92% 2020-21) + 288 sys<dia inversions; child weights 60-200kg INSIDE the 0.5-200 develop gate = 1,456 rows (gate-width evidence for B-029(b)); fundal-height zeros 3,363 declining (develop min=1 will block); Apgar >10 in ~25-32/129; EGA-at-birth >45wk 21/597.
- **Other invariants**: future-dated measurements 0 (CLEAN); before-birth measurements ~570 (device-clock clusters; z-score returns Nothing on negative age — TOLERATED); prenatal measurement-vs-encounter drift 24.6k rows >3d (reopened-encounter workflow — CENSUS, pairs with B-194); vaccination doses-vs-dates zip 4 legacy nodes (TOLERATED); zero/negative anthropometrics ≤12/family.

## Round 23 (2026-08-24, @c734bd4ab, COVERAGE-SWEEP — units never given a coverage row)
Units (8 worktree scouts, one wave): (1) Prenatal ProgressReport + shared Pages/ProgressReport; (2) WellChild + Nutrition + ChildScoreboard ProgressReport; (3) AcuteIllness + NCD + TB + FamilyNutrition ProgressReport; (4) hedley_prenatal + hedley_acute_illness; (5) hedley_well_child + nutrition + family_nutrition + hiv + tuberculosis + group_education + resilience + forms; (6) hedley_patient + hedley_person (consolidation); (7) Backend/Update.elm (write path, 10.5k); (8) App shell (App/Update+View, Router, Page, PatientRecord/NextSteps/Attendance/MyAccount) + photos/bulkPhotos/photoCache/lifecycle/config JS. Candidate ids start at B-210.
- **Unit 1 (Prenatal ProgressReport) COMPLETE** → B-210 (MED, tier 3), B-211 (LOW-MED, tier 4), B-212 (MED, tier 3); all three orchestrator-verified at anchor (form list vs guard list; sentinel; ARV/NCD key). Coverage row added. Units 2-8 in flight as of this write — if the session cap kills them, resume via SendMessage (context intact) rather than relaunching.
- **Unit 2 (child ProgressReports) COMPLETE** → B-213 (⭐ HIGH, tier 2 — scorecard z-scores at today's age; identical line on origin/main so LIVE), B-214 (MED, tier 3). Coverage row added.
- **Unit 4 (hedley_prenatal + hedley_acute_illness) COMPLETE — ✅ CLEAN** (Features containers; only `populate-edd.php` executes, already covered). Coverage row added.
- **Unit 6 (hedley_patient + hedley_person) COMPLETE** → B-216 (MED-HIGH server-stats, tier 3), B-217 (guard, tier 3), B-218 (tier 4), B-219 (tier 4, with the CHW-`imported` tail note). Coverage row added.
- **Unit 8 (App shell + small JS) COMPLETE** → B-215 (MED, tier 3; identical on origin/main). Coverage row added.
- **Unit 3 (AI/NCD/TB/FamNut ProgressReports) COMPLETE** → B-220 (⭐ HIGH, tier 2 — AI report drops the current encounter; `git show 04e4bd9a8` diff verified; live since v1.14.0), B-224 (PLAUSIBLE, tier 4). Coverage row added.
- **Unit 7 (Backend/Update.elm) COMPLETE** → B-221 (MED, tier 3), B-222 (tier 4), B-223 (minors ×2, tier 4). Coverage row added. Unit 5 (hedley_well_child + small program modules) still in flight.
- **Unit 5 (hedley_well_child + 7 small program modules) COMPLETE — ✅ CLEAN** (Features containers; sibling paths for all 69 bundles audited instead). B-192 tail gained two same-shape siblings. Coverage row added.
**⭐ R23 COMPLETE (2026-08-24): 15 new items B-210..B-224 — 2 tier-2 LIVE headliners (B-213 scorecard z-scores at today's age; B-220 AI progress report omits the current encounter, regression since v1.14.0), 7 tier-3, 6 tier-4 (one PLAUSIBLE); 8 coverage rows (2 ✅ clean, 6 ◒); B-192 extended. Durable lesson: the five `hedley_<program>` server modules are configuration-only — route program-logic questions to hedley_restful/hedley_activity/hedley_person/hedley_case_management. Dry-stop counter: 0. Next discovery round = R24; un-rowed candidates: hedley_restful non-sync endpoints, per-program `Backend/*/Update.elm` sub-updaters, hedley_schedule/hedley_device at unit depth, the ~40 list_text allowed_values ↔ Elm sweep.**

## Round 24 (2026-08-24, @6347d2fa2, COVERAGE-SWEEP — surfaces named un-rowed by R23 + tree-vs-table gaps)
Units (7 worktree scouts, one wave): (1) hedley_restful NON-sync endpoints + hedley_restful.module; (2) all 17 per-program `Backend/*/Update.elm` sub-updaters; (3) Pages/GlobalCaseManagement (client, 3.1k); (4) list_text allowed_values ↔ Elm encoder/decoder sweep (~40 fields); (5) participant selection / enrolment pages (Participant(s)/People/Relationship/Clinics/*EncounterParticipants/*EncounterTypes); (6) hedley_device + hedley_schedule + hedley_general + hedley_view_export + hedley_zscore at unit depth; (7) small client pages (TraceContact/EducationSession/HomeVisit/Wellbeing/MessagingGuide/Report/Activities/Activity + Session non-group parts). Candidate ids start at B-225.
- **Unit 1 (hedley_restful non-sync) COMPLETE** → B-225 (LOW-MED, tier 4; type-mismatched `!==` guard, run-verified locally). Coverage row added.
- **Unit 5 (participant/enrolment flows) COMPLETE** → B-226 (MED, tier 3; nurse branch lacks the CHW `encounterTypeButtonActive` guard). Coverage row added.
- **Unit 7 (small client pages) COMPLETE** → B-227 (MED, tier 3; `5d553d346` inverted the smear predicate — scout's "date always Nothing" leg was half wrong, corrected in the entry), B-036 extended (derived feeding signs), eatenWithWater = already-recorded inert minor. Coverage row added.
- **Unit 2 (17 sub-updaters) COMPLETE** → B-228 (LOW, tier 4), B-229 (LOW, tier 4, race not reproduced); B-082 re-found and dropped. Coverage row added.
- **Unit 6 (5 small server modules) COMPLETE — ✅ CLEAN**; F1 = B-019 (🅿 parked, not re-pitched), F2 moot (`views_ui` enabled on ihangane.live, checked), F3 minors noted on B-019. Coverage row added.
- **Unit 4 (allowed_values ↔ Elm, 209 fields) COMPLETE** → B-230 (MED, tier 3; the only encoder⊄decoder gap). Coverage row added; the durable facts in B-230's tail.
- **Unit 3 (GlobalCaseManagement; relaunched on opus after a model API error) COMPLETE** → B-232 (⭐ HIGH, tier 2), B-231 (MED-HIGH, tier 3), B-233 (MED, tier 3), B-234 (tier 4). Scout ran elm-test probes in its worktree for three of them (main tree verified clean afterwards). Coverage row added.
**⭐ R24 COMPLETE (2026-08-24): 10 new items B-225..B-234 — 1 tier-2 (B-232 immunization pane effectively empty), 5 tier-3 (B-226 nurse postpartum-while-open, B-227 blood-smear lab row inverted, B-230 gonorrhea-continued undecodable, B-231 TB pane drop, B-233 duplicate TB/HIV participants), 4 tier-4; B-036 and B-019 extended; 7 coverage rows (1 ✅ clean: the five small server modules). Re-finds dropped: B-082, B-019, eatenWithWater minor. Durable lessons: (1) copy-adapted entry generators in GlobalCaseManagement diverge where the acting and scheduling encounter are the SAME type; (2) a `comm` of encoder vs decoder string literals is a 1-line check for the B-230 class — worth a G-track guard; (3) the auto-mode classifier blocked two read-only terminus SELECTs this round (blood-smear and gonorrhea sizing queries are in B-227/B-230 for the user to run). Dry-stop counter: 0. Next round = R25; un-rowed candidates: hedley_restful sync plugins' writable-field lists per bundle (unit-2 gap), Pages/Dashboard client full unit (three partial passes, never a unit row), Pages/Person + Backend/Person/Form at unit depth, Pages/Prenatal Activity pages post-#2007 (R14 row predates the range work).**

## Round 25 (2026-08-25, @f63fe6e88, COVERAGE-SWEEP — R24's named un-rowed surfaces + largest thematic-only files)
Units (9 worktree scouts, one wave): (1) Pages/Prenatal/Activity/Utils+Model; (2) Pages/Prenatal/Activity/Update+View + RecurrentActivity/*; (3) Pages/Prenatal/Utils + Encounter/ + Participant/; (4) Pages/Dashboard + Backend/Dashboard (first unit row); (5) Pages/Person + Backend/Person (form/encoder/decoder); (6) hedley_restful sync plugins — writable-field contract per bundle; (7) hedley_reports.module (scripts/ excluded, declined); (8) hedley_stats.module (scripts/ excluded); (9) Pages/Report + Activity/Utils + Utils/* helpers.
- **Unit 1 (Pages/Prenatal/Activity/Utils+Model, 8.3k) COMPLETE** → B-235 (⭐ HIGH, tier 2), B-236 (MED-HIGH, tier 3), B-237 (MED, tier 3), B-238 (LOW, tier 4). All four orchestrator-verified at anchors. B-235's flip confirmed by `git show 45f215dbd` (one line; the same commit's `Svg.elm` half is a pure `bmi`→`weightGain` rename, so the chart oracle was NOT re-pointed) and it is an ancestor of `origin/main` = deployed; live check established `hedley_admin_feature_healthy_start_enabled` exists on **ihangane only** (absent on vhw/tip-somalia/uvl, and `variable_get(…, FALSE)` means absent = off) — its VALUE was blocked by the auto-mode classifier and is written into B-235 for the user. Coverage row added.
- **Unit 2 (Prenatal Activity Update+View + RecurrentActivity) COMPLETE** → B-239 (MED-HIGH, tier 3), B-240 (MED, tier 3), B-241 (MED, tier 3); all three orchestrator-verified at anchors. ⚠ B-239 needed care: `RecurrentActivity/View.elm` has TWO `saveMsg` blocks and only the follow-ups one (~:769) drops `TaskPartnerHIVTest` — the Lab Results one (:338) handles it correctly, so a grep for the constructor alone would have refuted the finding wrongly. B-240's precedence claim confirmed from the Elm fixity table (`<|` infixr 0 vs `||` infixr 2). Coverage row added.
- **Unit 3 (Prenatal Utils/Encounter/Participant/Outcome) COMPLETE** → B-253, B-254, B-255 (all tier 4). Two of the three are fresh instances of B-214's unsorted-`Dict.toList` selection pattern. Coverage row added.
- **Unit 4 (Pages/Dashboard + Backend/Dashboard, first full unit row) COMPLETE** → B-242 (⭐ HIGH, tier 2 — inverted `created` filter, deployed since 2023-12-28), B-243 (MED-HIGH, tier 3 — server ships nutrition weight under the stunting key), B-260/B-261/B-262/B-263/B-264 (tier 4). Orchestrator re-verified B-242 from the two correct siblings in the same file, `git log -L` to `a07da69ea`, and `merge-base --is-ancestor origin/main`; and B-243 from the two caller bundle lists plus the four constants. Coverage row added.
- **Unit 5 (Pages/Person + Backend/Person) COMPLETE** → B-244 (⭐ HIGH, tier 2 — person PATCH nulls the photo), B-256/B-257/B-258/B-259 (tier 4). B-244's five hops each verified independently, including `sw.patchFull` (all keys, not just changed) and the server's `$ignoredFields`, where B-150's guard covers only latitude/longitude. Coverage row added.
- **Unit 6 (hedley_restful writable-field contract) COMPLETE** → B-266 (MED, tier 3), B-267 (tier 4, three latent traps); B-230 corrected. Coverage row added.
- **Unit 7 (hedley_reports.module, 10.9k) COMPLETE** → B-245 (MED-HIGH, tier 3), B-246 (MED, tier 3 — ⚠ joins the gated Completion batch), B-247 (MED-HIGH, tier 3), B-265 (tier 4); B-127 +1 dead fn; **B-184 annotated — its recorded fix is inert until `:8880` stops pushing the whole field item**. Orchestrator re-diffed the diagnosis mapping independently (82 mapped of 97 real allowed values). Coverage row added.
- **Unit 8 (hedley_stats.module) COMPLETE** → B-248 (MED, tier 3 — ⭐ three simpletest scenarios dead since 2020), B-249 (MED, tier 3), B-250 (MED, tier 3). Coverage row added.
- **Unit 9 (Pages/Report + Activity + Utils micro-units) COMPLETE** → B-251 (MED, tier 3), B-252 (tier 4); **two of three sub-units ✅ CLEAN** with both briefed hypotheses actively refuted. Coverage row added.
**⭐ R25 COMPLETE (2026-08-25): 33 new items B-235..B-267 — 3 tier-2 (B-235 Healthy-Start GWG verdict inverted; B-242 dashboard Current Pregnancies counts only today's registrations, live since Dec 2023; B-244 person edit nulls the stored photo), 13 tier-3, 17 tier-4; 9 coverage rows (2 sub-units ✅ clean); B-127, B-184 and B-230 amended. Dry-stop counter: 0. Counts after R25: 167 READY — T2 8 · T3 38 · T4 87 · untiered 34.
📌 **R25 durable lessons.** (1) ⛔ **A passing CI job is not evidence a scenario ran** — B-248 found three simpletest scenarios, including the B-032/B-033 regression tests, exiting early since a 2020 commit titled "Make Travis pass"; two backlog entries claimed CI proved them. (2) **The same defect can arrive from both ends in one round** — B-260 was found by unit 4 (client decoder) and unit 8 (server emit) independently; merged into one item deliberately. (3) **Two scouts' "clean" verdicts survived adversarial re-checking and one nearly did not**: the nutrition CSV *looks* position-swapped (PHP emits `$stunting,$wasting,$underweight`, Elm reads `[stunting, underweight, wasting]`) but the PHP keys are named after the measurement rather than the indicator — always read the docblock before filing an apparent order swap. (4) **Grep by constructor name can refute a true finding**: B-239's `TaskPartnerHIVTest` IS handled in one of `RecurrentActivity/View.elm`'s two `saveMsg` blocks and missing from the other.

## Round 26 (2026-08-30, @4fb8bcfbf, COVERAGE-SWEEP — the three big programs at the depth R25 gave Prenatal, plus Backend/Measurement's first unit row)
Units (8, two waves of worktree scouts): (1) Pages/WellChild/Activity/Utils+Model; (2) Pages/WellChild/Activity/Update+View + Encounter/ + Participant/; (3) Pages/AcuteIllness/Activity/Utils+Model; (4) Pages/AcuteIllness/Activity/Update+View + Encounter/ + Participant/; (5) Pages/NCD minus ProgressReport; (6) Backend/Measurement/Model+Utils (first row ever); (7) Backend/Measurement/Decoder+Encoder (first row ever); (8) Pages/WellChild/Utils + Tuberculosis + HIV + ChildScoreboard non-report code. Out: all ProgressReport views (R23), Dashboard/Person/Prenatal (R25), Translate content, server modules. Scouts briefed on the last-wins/unsorted-selection class WITH the "check what depends on the choice" caveat, and told B-270/B-271 are already filed. Wave 1 = units 1, 3, 6, 7 (launched; user asked for four first).
- **Unit 1 (Pages/WellChild/Activity/Utils+Model) COMPLETE** → B-272 (MED-LOW, tier 3 — one-line `List.reverse` on a DESC list; the Prenatal/AI siblings are correct because THEIR lists are ASC), B-273 (LOW, tier 4 — the R12 sentinel class's first real instance). Both orchestrator-verified at anchors incl. `generatePreviousMeasurements`'s sort and `sortByDateDesc`'s argument order. Coverage row added.
- **Unit 3 (Pages/AcuteIllness/Activity/Utils+Model) COMPLETE** → B-274 (MED-LOW, tier 3 — raw-form handler; the consumer trace `resolveMedicationsNonAdministrationReasons` reads reasons only, never `distributionSigns`, so the stale reason really drives the referral gate), B-275 (LOW, tier 4), B-276 (LOW-MED, tier 4). All three orchestrator-verified at anchors. B-274 names four same-shape sibling handlers in Prenatal/NCD whose consumers were NOT traced — R25 units 2 and 5 did not report them; a wave-2 NCD scout can pick up the NCD pair. Coverage row added.
- **Unit 6 (Backend/Measurement/Model+Utils, first row ever) COMPLETE — ✅ CLEAN.** Scout ran a mechanical 76-pair ToString/FromString diff both ways against all 207 server `allowed_values` blocks; every client-only key traced to a dead or unreachable emit. Orchestrator spot-checked the three load-bearing refutations (AssocList append at `:235`, breastfeeding Yes/No tasks, no `home-birth` emit). One same-session duplicate head-pick examined and DECLINED (B-195 class). Coverage row added.
- **Unit 7 (Backend/Measurement/Decoder+Encoder) wave-1 scout DIED** — API rate limit (session limit, reset 16:30) killed it mid-audit with no result. Relaunched with wave 2 at 16:53.
- **Wave 2 launched 16:53** (scheduled by the user 2.5h ahead): units 2, 4, 5, 8 + the unit-7 rerun.
- **Unit 2 (Pages/WellChild/Activity/Update+View + Encounter + Participant) COMPLETE** → B-277 (⭐ MED, tier 3, DEPLOYED on Burundi — one-word copy-adapt slip, `dtpForm` for `dtpStandaloneForm`; live `vhw.live` query: 2 DTP-standalone records exist and BOTH are dose-less `administered-previously`, vs 361 DTP records), B-278 (LOW-MED, tier 4), B-279 (LOW, tier 4). All orchestrator-verified; B-277 also checked on `origin/main` and against the introducing commit. B-274 widened to the cross-program `SetReferToHealthCenter`/`SetProvidedEducationForDiagnosis` reason-refill family. Coverage row added.
- **Unit 4 (Pages/AcuteIllness/Activity/Update+View + Encounter + Participant) COMPLETE** → B-280 (MED-LOW, tier 3 — subsequent-visit diagnosis is first-write-wins; the first-encounter sibling compares before/after), B-281 (LOW, tier 4 — the shared Yes/No-with-reason family, same-session; corrects the B-274 note that said "after a reload"), B-282 (LOW, tier 4 — order-dependent same-day illness guard; something DOES depend on the pick). All orchestrator-verified. B-147 confirmed landed at all seven AI save sites. Minor noted, not filed: `AcuteIllnessContactsTracingRevision` is absent from the `HandleRevisions` cases that call `generateAcuteIllnessAssesmentCompletedMsgs` (`Backend/Update.elm:2745-2842`), so saving contacts tracing last leaves the user on the encounter page — navigation only. Coverage row added.
- **Unit 8 (Tuberculosis + HIV + ChildScoreboard non-report + WellChild Encounter/Utils) COMPLETE** → B-283 (LOW-MED, tier 4 — NCDA raw-form dependent sign, with scorecard consumers; two siblings), B-284 (LOW, tier 4 — joins the gated Completion batch), B-285 (MED-LOW, tier 3 — DEPLOYED: the English string of a TB/HIV question was overwritten with Kirundi in `4a4e5e838`; the coverage map's "wrong language in field" exception applies). B-277 confirmed independently from the scoreboard side and amended. ⚠ The framing named `Pages/WellChild/Utils.elm`, which does not exist; the scout audited `Pages/WellChild/Encounter/Utils.elm` (174 lines) instead — ✅ clean. Four coverage rows added.
- **Unit 5 (Pages/NCD minus ProgressReport) COMPLETE** → B-286 (MED, tier 3 — hypertension staging compares against the encounter's OWN just-written stage: double step-down + correction ratchet; deployed since 2022 but the live query finds ZERO low-BP readings in patients with a prior Stage 2/3, so unexercised so far), B-287 (MED-LOW, tier 3 — labs Case Management entry closed before the result's diagnosis exists; the prenatal sibling recomputes, NCD does not), B-288 (MED, tier 3 — point-of-care diabetes at the initial phase gets no medication/referral task), B-289 (MED, tier 3, intent-gated on `35eb194aa` — pregnancy found by test is forgotten at later encounters, ACE inhibitors offered). Amended: B-272 (NCD vitals have the same DESC `reverse|>head` — the entry's "only Well Child" claim was wrong), B-274 (NCD medication siblings INERT — no `nonAdministrationSigns` in the NCD form), B-281 (NCD `SetReferralBoolInput` + Prenatal twin join the family). Note, not filed: an RBS switched to "not performed" after being saved as performed keeps its prerequisites and shows a result task that cannot complete — cosmetic on the recurrent page (`expectUniversalTestResultTask` never checks `executionNote`). Coverage row added.
- **Unit 7 rerun (Backend/Measurement/Decoder+Encoder, first row ever) COMPLETE** → B-290 (MED, tier 3, DEPLOYED since 2024-01-18 — encoder writes `cigarettesPerWeek` under `beverages_per_week`; live: last differing pair 2023-09-13, 2,458 records since the refactor, 88 drinkers' counts lost). B-267 gained (d): two more forbidden encoder values, both unreachable. **Candidate REFUTED by live query:** the eleven `symptoms_general` period counters added two months after the bundle (read by strict `decodeInt`) — 21,420 nodes on `ihangane`, 67 on `vhw`, ZERO missing the field. ⭐ **Premise correction, durable:** `NoRedInk/elm-json-decode-pipeline` 1.0.1's `optional` wraps the inner decoder in `oneOf [ decoder, Decode.null fallback ]`, so a present `null` yields the fallback — `optional` DOES tolerate null; only a present non-null value the inner decoder rejects fails. The R24/handoff line "`optional` tolerates MISSING but never MALFORMED" needs that qualifier. Coverage row added.
**⭐ R26 COMPLETE (2026-08-30): 19 new items B-272..B-290 — 0 tier-2, 10 tier-3 (B-272, B-274, B-277 ⭐ deployed Burundi DTP-booster data loss, B-280, B-285 ⭐ deployed Kirundi-for-English, B-286, B-287, B-288, B-289, B-290 ⭐ deployed social-history swap), 9 tier-4; 13 coverage rows (2 ✅ clean: Backend/Measurement/Model+Utils and WellChild/Encounter/Utils); B-267, B-272, B-274, B-277, B-281 amended. One candidate refuted on live data (symptoms_general period nulls). Unit 7's first scout died on an API rate limit and was rerun in wave 2. Dry-stop counter: 0.**
📌 **R26 durable lessons.** (1) **A helper shared by four programs can be right in three and wrong in one purely because of the LIST ORDER it is handed** — `reverse |> head` means "latest" over an ASC list (Prenatal, AI) and "oldest" over a DESC one (Well Child, NCD); B-272. Check the producer's sort before believing an idiom. (2) **Raw-form handlers are a family, not a bug** — B-274/B-279/B-283 (after a reload the saved value survives a toggle) and B-281 (same session: the handler clears to `Nothing`, `or` refills). Two mechanisms, one idiom fix each; record per family, not per program. (3) **A copy-adapted Save arm can read a sibling's form and nothing compiles differently** — B-277 (`dtpForm` for `dtpStandaloneForm`) and B-290 (`cigarettesPerWeek` under `beverages_per_week`) both born in refactors, both invisible to types; the discriminating check is a round-trip test per value, which neither had. (4) **Diagnosis generation must not read the diagnosis it is about to write** — B-286 (NCD includes the current encounter in its history) and B-280 (AI subsequent visit is first-write-wins); the Prenatal siblings recompute before comparing and are the reference. (5) **"Deployed" and "has fired" are different columns** — B-286 and B-277 are both live, one has hit 0 patients and the other 2 of 2; size before tiering.

## Round 27 (2026-08-31 → 09-01, @5f113f8fe, COVERAGE-SWEEP — the shared Measurement form builders + surfaces with only R13–R15-era rows)
Units (7 worktree scouts, one wave): (1) Measurement/View+Model (first unit row); (2) vaccination engine internals (`vaccinationFormDynamicContentAndTasks`/`generateFutureVaccinationsData` + all consumers); (3) Pages/Nutrition full unit; (4) group session flow (Pages/Session + Backend/Session); (5) StockManagement re-read; (6) Translate.elm objective-wrongness mechanical sweep (allowed exception classes only); (7) client Backend entity layer (non-measurement codecs/utils). All seven hit the session cap on 2026-08-31 evening — unit 1 completed before it; the other six were resumed via SendMessage on 2026-09-01 with context intact. Candidate ids start at B-291.
- **Unit 1 (Measurement/View.elm + Model.elm + Update.elm, ~7.5k lines, FULL unit read) COMPLETE** → B-291 (MED-LOW, tier 4 — ANC month grid: tapping an already-recorded month silently stores a phantom ANC visit date; orchestrator re-verified every hop: CSS `display:none` + commented `pointer-events`, unconditional label `onClick`, all four dispatchers, first-fill-only gate, both consumers, backlog de-dup vs B-190/B-283). Re-find dropped: the discarded `EatenWithWater` question — **THIRD find** (R14, R24, now; recorded as inert/not-action-required at the R14 minors bullet) — scouts keep paying for it, the disposition stands. Observation not filed (clinician-gated class): `BeneficiaryCashTransfer` counseling triggers only on `receivingCashTransfer == Just False`, unlike its three siblings that also trigger on the parent question's No. Coverage row added.
- **Unit 2 (vaccination engine internals) COMPLETE** → B-292 (MED, tier 3, DEPLOYED — `mergeVaccinationProgressDicts` reverses per-vaccine dose order via the vendored AssocList's foldr+append merge, and `lastDoseData`'s `reverse |> head` then reads the FIRST dose as "last": interval gate + catch-up date range wrong for every both-program child; **3,802 such children on ihangane**, scout repl-proved + orchestrator source-derived), B-293 (LOW, tier 4 — boundary-day `LT` filter vs its own "after" comment, donut + scorecard row). B-195 amended (three more consumers picking an arbitrary duplicate participant). Verified unchanged/in place: B-263, B-271, B-164, B-055, B-052, B-064, B-154, B-277, B-131. Coverage row added.
- **Unit 3 (Pages/Nutrition full unit, 14 files) COMPLETE** → B-294 (MED, tier 3 — stored nutrition assessment stale via a computed-before-vs-computed-after recompute gate; **269 live divergent encounters on ihangane**; the `populate-nutrition-assessment.php` backfill script is standing corroboration), B-295 (MED-LOW, tier 4 — CHW "unable to take" leaves the saved measurement alive; LATENT: 0 live occurrences on ihangane AND vhw). B-281 amended (Nutrition members traced), B-283 anchor confirmed accurate. Orchestrator verified: gate code, save-time assessment source, card order, both server consumers + the backfill script, skip-branch + hydration, and ran both sizing queries. Coverage row added.
- **Unit 4 (group session flow: Pages/Session + Backend/Session + group plumbing in Backend/Update, FULL unit read) COMPLETE** → B-296 (MED-LOW, tier 3 — child enrolled under two adults checked in through an arbitrary head pick; **604 live two-adult pairs on ihangane**), B-297 (LOW, tier 4, half product-question — nothing closes a session since the 2023-06 button swap; census: closed collapsed from ~100% in 2018-19 to 2/4,807 in 2024), B-298 (MED-LOW, tier 4 — `EditableSession.update` hardcoded `NotAsked` → saveButton's double-submit guard dead; R21's "double-POST pairs still minted" is the live corroboration; lands naturally with B-082). B-082 verified unchanged, sole tracker miss. Prefetch/auto-select mismatch (missing previous-value hints on stale tab) judged display-only, coverage note. Orchestrator verified: head-pick + check-in gate + PostRelationship creation path, dialog-producer greps, `EditableSession.update` writer enumeration, and ran both sizing queries. Coverage row added.
- **Unit 7 (client Backend entity layer — 25 modules at codec/Utils depth, FULL read) COMPLETE — NO new items; two entries widened.** The headline candidate (strict `required "acute_illness_diagnosis"` null-poison) is a RE-FIND of B-177 verbatim (even the Dashboard stats copy is in that entry). B-177 amended with the widened family (resilience_survey_signs on the GENERAL feed, village geo strings, `requiredAt` expected/scheduled_date; deleted-bool sites proven safe). B-047 amended with the no-`field_deleted` bundle inventory (clinic/village/catchment_area/counseling_* — deletion cannot propagate; nurse is B-047's case). Observations recorded in the coverage row only: Nurses `[""]` health-center explode (inert — sole consumer is an `EverySet.member`), PrenatalEncounter `prenatal_indicators` omit-when-empty (unreachable drift — the set never empties), TB/HIV `not-dignosed` wire typos frozen. Coverage row added.
- **Unit 5 (StockManagement client + Backend/StockUpdate, FULL unit read) COMPLETE** → B-299 (MED-LOW, tier 3 — CHW village save invisible in-session: `StockUpdateRevision` updates only the HC caches; LIVE on tip-somalia, the sole site with `stock_management_village_enabled=1`, usage nascent at 1 record), B-300 (MED-LOW, tier 4 — data computed before village residents load → Issued zeroed), B-301 (LOW, tier 4 — gap-12 details entry bypasses the month clamp), B-302 (LOW, tier 4, LATENT — village-switch stale cache; 0 multi-village CHWs live). Verified unchanged: B-061, B-083, B-060 fix + discrimination test, B-004 reset, B-011/photo wiring. ⭐ Flag map live-checked on all four sites (hedley_admin_feature%): HC stock OFF everywhere; village stock = tip-somalia only; ncda/healthy_start/group_education = ihangane only; family_nutrition = tip-somalia. B-292's site coverage completed (Rwanda-only, 3,802). Coverage row added.
- **Unit 6 (Translate.elm objective-wrongness MECHANICAL sweep — first full sweep; 3,552 client + 448 server records) COMPLETE** → B-303 (MED, tier 3 — 6 clinical-content errors: wrong Carvedilol dose ×2, missing "3x" frequency, invented 5-day course, EPDS options collapsed, 5s→1s ECD threshold, English-and-wrong TB guidance in a kirundi field) and B-304 (MED-LOW, tier 3 — 10 copied-neighbor labels: ALT=kidney ×2, MUAC=height placeholder, ANC→NCD referral question, malnutrition=danger-signs, popup repeat, PROM=PPROM, WhatsApp prompt, Group Encounter→Group Education overwrite, dangling Wasting card). Every anchor + the two key introducing commits re-verified by orchestrator; string equalities pinned by occurrence counts. Detector: stopword/orthography language scores + verbatim-neighbor equality + digit/unit multiset diff; 245 flags, ≈93% FP, all hand-reviewed; **server/elm Translate copy CLEAN**. De-duped: B-285/B-022 site, B-089, MeasurementOutOfRangeWarning note, R12 interpolation class. ⚠ Parser trap: a commented-out record (`{- -}` block) swallowed 3,000 records until block-comment stripping was added — sweep tooling must strip Elm block comments. Script preserved at scratchpad/tsweep.py. Coverage row added.
**⭐ R27 COMPLETE (2026-09-01): 14 new items B-291..B-304 — 0 tier-2, 6 tier-3 (B-292 ⭐ DEPLOYED merge-reversed vaccination dose order, 3,802-child population; B-294 stale stored nutrition assessment, 269 live divergences; B-296 two-adult check-in head pick, 604 pairs; B-299 village stock save invisible, live tip-somalia; B-303/B-304 the first mechanical Translate sweep — 6 clinical-content + 10 copied-neighbor sites), 8 tier-4; 7 coverage rows (every unit yielded — none audited fully clean); amendments: B-195 (+3 consumers), B-281 (Nutrition members), B-177 (strict-null family incl. the general-feed resilience sibling), B-047 (no-field_deleted inventory), B-292 (site coverage), B-291 (flag map); verified unchanged: B-082, B-061, B-083, B-263, B-271, B-283 + 8 implemented fixes re-confirmed in place. Re-finds dropped: eatenWithWater (THIRD find), B-177 headline (unit 7). All 7 scouts were killed by the session cap on launch evening; 6 resumed next day via SendMessage with context intact. Live sizing ran from this seat throughout (12 terminus queries incl. the four-site feature-flag map). Counts after R27: 188 READY — T2 3 · T3 46 · T4 105 · untiered 34. Dry-stop counter: 0.**
📌 **R27 durable lessons.** (1) **AssocList `merge` REVERSES what it rebuilds** (in the coverage lessons; B-292's mechanism) — audit every `toList`-order-trusting consumer downstream of a merge. (2) **Size the FLAG before tiering a flag-gated finding** — the per-site feature map is one sqlq away (snapshot in the coverage lessons; it moved unit-5's items across tiers in both directions). (3) **Elm block comments swallow naive parsers** — the Translate sweep read 523 of 3,552 records until `{- -}` stripping was added; mechanical sweeps must prove their record count against a grep first. (4) **A widget's `disabled` may be CSS-only** — B-291's month grid takes clicks on the label regardless; check the event handler, not the class name.

## Round 28 (2026-09-03, @519c18bdb, COVERAGE-SWEEP — the oldest surviving ✅/◒ infrastructure rows re-read at R25+ depth, plus two never-rowed surfaces)
Units (6 worktree scouts, one wave; user approved the plan as proposed): (1) SyncManager/ full unit re-read (9.6k, R15 row); (2) Measurement/Utils.elm full re-read (10.9k, R13 row; vaccination-engine slice from R27 unit 2 excluded); (3) vendored collections — AssocList + Utils/AllDict + Restful/ fork — and every order-trusting consumer of Dict.union/diff/merge/foldr/toList (B-292's class); (4) server/elm admin apps re-read at R25 depth (R17 row); (5) hedley_ncda PHP re-read (R16 row; scripts/ excluded as declined); (6) ZScore/View + growth-chart SVG rendering (R23 not-read tail). Out: program pages + ProgressReports (R23–R27), Translate (R27), Backend entity layer/Update (R23/R27), group/stock/nutrition (R27), CI/deploy (TH/G tracks), all declined classes. Candidate ids start at B-308.
All six scouts were killed by the session cap (2:30 reset) minutes after launch and resumed via SendMessage with context intact — the R27 failure mode, same remedy. All six completed within ~5 minutes of the resume.
- **Unit 4 (server/elm admin apps) COMPLETE** → B-308 (⭐ MED, tier 2 — AI report rows j/k/l rotated between `hedley_reports_ai_diagnosis_mapping` and `acuteIllnessDiagnosisFromMapping`; live 603/904/1,167 mis-rowed; the e2e suite asserts the WRONG row with a contradicting comment), B-309 (MED-LOW, tier 3 — Postnatal Care immunisation rows blind to Child Scoreboard doses; 2,392 both-programme children, 175 with no WC dose at all), B-310 (LOW, tier 4). B-293 amended: a THIRD copy of the LT filter in the scoreboard, missed by PR #2201 (comment posted on the PR). Orchestrator re-read both mapping tables + the client encoder + the e2e assertion, the emitter switch + trigger list + Elm consumer, both denominator anchors; ran the three sizing queries. `REVIEW: HEAD dropped` residue in the trigger list = B-023 (already recorded — the grep-first rule caught it). Coverage row added.
- **Unit 5 (hedley_ncda PHP) COMPLETE** → B-311 (⭐ MED-HIGH, tier 2 — rolling `-3 years` cutoff vs Nov-2023 columns: 29,202 of 61,044 children absent from every historical column, drifting daily), B-312 (MED-LOW, tier 3 — Iron row one month early via `Date.diff Months` truncation toward zero; re-derived from the justinmimbs source), B-313 (LOW, tier 4). Coverage row added.
- **Unit 6 (ZScore charts) COMPLETE** → B-314 (MED-LOW, tier 3 — y ticks by index vs points by value, three charts), B-315 (MED-LOW, tier 3 — out-of-range points pinned to the frame; 5,981 children / 7,296 measurements in the 24–26-month window on ihangane), B-316 (LOW, tier 4). R23's growth-pane CLEAN verdict holds at the SVG level. Coverage row added.
- **Unit 1 (SyncManager) COMPLETE** → B-317 (⭐ MED-HIGH, tier 2 — late response after the 30-s timeout writes its cursor onto whichever authority is current; scout scratch-test reproduced, orchestrator re-read all four handlers and re-derived the sequence incl. the R6-before-R5 gap), B-318 (MED, tier 3 — stats GET has no timeout, B-025's class). Upload-response path (R20 not-checked tail) traced clean; 206-variant dispatch tables + photo dispatch mechanically clean. Coverage row added.
- **Unit 2 (Measurement/Utils) COMPLETE** → B-319 (MED, tier 3 — lab-tech blood-smear order opens pre-cancelled; 40 open orders on ihangane today), B-320 (MED, tier 3 — HIV known-as-positive correction keeps the negative result + partner signs → Discordant Partnership + PrEP for an HIV-positive woman), B-321 (LOW, tier 4, latent). B-281 amended with the Outside Care member that has a report consumer. Coverage row added.
- **Unit 3 (vendored collections + order consumers) COMPLETE** → B-322 (LOW, tier 4 — duplicate "Malnutrition signs" row: equal as sets, unequal as lists). B-292 amended (stale fork docs). ⭐ Durable: the two assoc-list implementations' opposite order semantics, repl-proven and tabled in the coverage lessons; AllDict's 7 `Debug.todo` all unreachable; `Restful/Login.elm` dead. 60+ consumer sites inventoried with verdicts in the coverage row. Coverage row added.
**⭐ R28 COMPLETE (2026-09-03): 15 new items B-308..B-322 — 3 tier-2 (B-308 ⭐ deployed AI report rows rotated; B-311 ⭐ deployed scoreboard loses half its historical cohort; B-317 ⭐ deployed sync cursor cross-write), 7 tier-3, 5 tier-4; 6 coverage rows (none fully clean — every old ✅ row re-read yielded); amendments: B-281, B-292, B-293 (third copy, PR #2201 comment). Live sizing from this seat: 7 terminus queries. Counts after R28: 192 READY — T2 4 · T3 46 · T4 108 · untiered 34. Dry-stop counter: 0.**
📌 **R28 durable lessons.** (1) **Old ✅ infrastructure rows pay like old ✅ program rows did** — SyncManager (R15 clean → MED-HIGH race), Measurement/Utils (R13 clean → two MED clinical items), server/elm mapping tables "CLEAN" in R17 → a three-way rotation live for 15 months. (2) **An e2e test can pin the bug** — B-308's assertion expects the rotated row under a comment that says the opposite; a green suite proved the wrong behaviour stable. Read the comment against the assertion. (3) **A comment true at authoring becomes a cutoff that drifts** — B-311's "-3 years is enough" was right in 2024-03 and loses ~1/24 of the historical cohort every month since. (4) **`truncate` is not `floor`** for backward month differences (B-312). (5) **Two assoc-list implementations, opposite order semantics, one codebase** (B-322 + the lessons table).

## Round 29 (2026-09-06 → 09-07, @62b029b9d, COVERAGE-SWEEP — the diagnosis engines at tier-1 depth, the never-rowed server download core + backend admin/glitchtip, and the three oldest client rows)
Units (6 worktree scouts, one wave; user approved the plan as proposed and asked for the round to run fully): (1) diagnosis DECISION engines — Acute Illness resolver (`Pages/AcuteIllness/Activity/Utils.elm:2434` on + callers + persistence/routing) and the nutrition-assessment family (`Backend/NutritionEncounter/Utils.elm` + its three copy-adapted `generateNutritionAssessment` callers + `Backend/Update` triggers) — R12 engines row; Prenatal/NCD engines excluded (R25/R26); (2) `hedley_restful` sync DOWNLOAD path server-side (`HedleyRestfulSync` GET handlers, SyncBase, ActivityBase, EntityBaseNode, `.module`, participant/encounter/session/nurse plugins — cursor/vid/ordering/batching/deleted contract vs the client) — never rowed as a unit; (3) login / nurse session / pairing / HC-switch state machine (PinCode, Device, App login arms, ServiceWorker/, sw.js, config.js, app.js caches, SyncManager HC switch, pairing + Nurses plugins, hedley_user, hedley_device) — R11 row, never re-read; (4) Elm Fetch-wiring mechanical re-sweep (all 76 `Fetch.elm` vs every `db.*` read per page) — R10/R16 rows, B-215/B-258 residue; (5) `hedley_admin` (module + views_default + access plugin + integrity test) + `hedley_health_center` + `hedley_chw` + `logs_glitchtip` (never rowed) — scripts excluded; (6) Family Nutrition + Home Visit slice end to end (client pages, Backend encounter modules, Backend/Update arms, restful family-nutrition plugins, reports consumers) — R14 row; flag = tip-somalia only. Out: Prenatal/NCD engines + workflows (R25/R26), Translate (R27), SyncManager/Measurement-Utils/ZScore/server-elm/hedley_ncda (R28), hedley_reports/stats (R25), post-merge drift `519c18bdb..HEAD` (12 reviewed PRs; R18 precedent found zero), MessagingCenter (idle), GeoLocation, hedley_migrate, TH/G tracks, all declined classes. Candidate ids start at B-324.
- **Unit 1 (diagnosis engines) COMPLETE** (scout 217k tokens / 10 min) → **B-324** (⭐ tier 1 by the diagnosis rule, MED-LOW, tip-somalia only — COVID path exits on a >2-week cough unconditionally while the TB-suspect diagnosis that exit exists for is feature-gated in the sibling path; orchestrator re-read both paths, the lab gate, the resolver, `tuberculosisManagementEnabled`, and both introducing commits). B-294 amended (gate computes both sides against the after-db). Every recorded state re-checked and holding; R12's Well Child previous-weight exclusion now spec-confirmed. Coverage row added.
- **Unit 2 (hedley_restful sync download) COMPLETE** (scout 180k tokens) → **B-325** (⭐ MED-HIGH, tier 2, all sites — cursor skips an in-flight upload's revisions; orchestrator re-read both GET queries, the render vid, the upload transaction, both worker `node_save` sites and checked `innodb_autoinc_lock_mode`/`tx_isolation` on live), **B-326** (LOW, tier 4). Coverage row added.
- **Unit 3 (login / HC-switch) COMPLETE** (scout 224k tokens, three scratch elm-tests) → **B-317 amended** (deterministic add/remove-HC trigger + stats-lane inverse; orchestrator re-read `RevisionIdAuthorityAdd`, the ack handler's `model.syncInfoAuthorities` target, the stats handler's closure zipper, `TrySyncing`), **B-327** (LOW, tier 4, latent — live: P5Y, 5,725 tokens, 0 expired, earliest 2030-03-19; contrib deletes expired tokens `RestfulAuthenticationToken.class.php:78-83`), **B-328** (LOW, tier 4). R11 verdicts hold. Coverage row added.
- **Unit 4 (Fetch wiring) COMPLETE** (scout 229k tokens) → **B-329** (MED-LOW, tier 3, ihangane — group NCDA activity fetch gap; orchestrator re-read `Session/Fetch.elm:48-61,88-93` and the `viewNCDA` read at `Measurement/View.elm:2696`; live 5,762 `group_ncda`, 565 in 2026), **B-330** (LOW, tier 4). B-215/B-024/B-066 verified fixed. Coverage row added.
- **Unit 5 (hedley_admin + glitchtip) COMPLETE** (scout 244k tokens) → **B-331** (⭐ MED, tier 3, DEPLOYED all sites — Sentry SDK absent from the deployed vendor; orchestrator listed `sites/all/vendor` in the main tree AND `origin/master` of `.pantheon-ihangane` (`6b5922ba3`, 2026-08-25): `autoload.php composer paragonie ramsey twilio`; live variables enabled=1 + DSN set), **B-332** (LOW, tier 4; 29 resets in hedley_stats counted), **B-333** (MED-LOW, tier 3; access-type lines `:29`/`:1749` with no `perm` key, permission export, ledger gate re-read; live 3 active E-ledger viewer users), **B-334** (LOW, tier 4). Coverage row added.
- **Unit 6 (Family Nutrition + Home Visit) COMPLETE** (scout 263k tokens, live sizing on tip-somalia) → **B-335** (LOW, tier 4; gate + constant re-read). B-066 verified fixed. Coverage row added.
**⭐ R29 COMPLETE (2026-09-07): 12 new items B-324..B-335 — 1 tier-1 (B-324 ⭐ diagnosis rule, tip-somalia), 1 tier-2 (B-325 ⭐ DEPLOYED sync cursor skips in-flight uploads), 3 tier-3 (B-329 group NCDA fetch gap, B-331 ⭐ DEPLOYED backend errors silently dropped, B-333 E-ledger viewer unscoped), 7 tier-4; amendments B-294, B-317 (deterministic trigger); 6 coverage rows (none fully clean — the R11/R14 rows and the never-rowed download core all yielded). Live sizing from this seat: 5 terminus queries (ihangane ×4, tip-somalia via scout). Counts after R29: 202 READY — T1 7 · T2 5 · T3 41 · T4 115 · untiered 34. Dry-stop counter: 0.**
📌 **R29 durable lessons.** (1) **A monotonic cursor over an auto-increment is only as good as the visibility order** — vids are allocated at INSERT and visible at COMMIT; any "sync everything above N" contract needs a lag or a safe-cursor (B-325). (2) **"Silently skip rather than take the site down" guards hide their own failure for months** — B-331's SDK-missing bail has been the only thing running since the release; a guard on infrastructure must surface somewhere (`hook_requirements`, a watchdog row, a dashboard). (3) **A Views export with `access type = perm` and no `perm` key means `access content`** — check who holds it (B-333). (4) **Zipper.fromList refocuses on the head** — any list edit during an in-flight request re-targets the write (B-317 amendment). (5) **Hard refresh lands on the page's own fetch** — the PinCode page's whole-table fetches never run on that path, so "masked in normal navigation" is the normal state for every un-fetched read (B-329/B-330).

## Round 30 (2026-09-14, @19a26a9b3, COVERAGE-SWEEP — the un-deployed tier-1 merge clusters read as merged surfaces, the release's server delta, and the R12–R19 infrastructure rows)
Units (7 worktree scouts, one wave; user approved the plan as proposed after confirming units 1–3 audit the MERGED result of already-implemented items, not re-finds): (1) prenatal blood-smear stack as merged (B-319/B-338/B-227 — #2224/#2228/#2229); (2) prenatal HIV/discordant-partner stack as merged (B-221/B-237/B-240/B-320/B-323/B-336); (3) NCD staging/diabetes/pregnancy stack as merged (B-286/B-288/B-289) + B-324 TB gate; (4) release server delta main..develop (features/updb for `field_blood_smear_ordered`, `HedleyRestfulSync` change, the three repair scripts shipping, mixed-fleet compatibility); (5) backend presave/CRUD hooks + shards + AQ workers (reports/stats/view_export) — R12 ✅ rows; (6) client JS layer sw.js/nodes.js/app.js/photos.js/bulkPhotos.js — R15/R19 rows; (7) hedley_case_management + hedley_activity + hedley_user + hedley_whatsapp — R18 row. Out: everything re-read R25–R29, ProgressReports + Backend/Update (R23), GlobalCaseManagement (R24), MessagingCenter, GeoLocation, hedley_migrate, TH/G tracks, all declined classes. Candidate ids start at B-339.
- **Unit 3 (NCD decision surface as merged + AI TB gate) COMPLETE** (scout 262k tokens / 12.5 min, 200-combination scratch decision table) → **B-339** (⭐ tier 1 by the diagnosis rule, MED-LOW — re-scoring encounter A after a later B exists reads B as A's history; the stage half is introduced by #2216 (B-286), un-deployed; orchestrator re-read `generatePreviousEncountersData`, the new staging line, the pre-#2216 `resolveCurrentHypertensionCondition`, `filterDiagnosesOfDeterminedConditions`, the three lab revision triggers and `generateNCDAssessmentMsgs`; 1 live exposure on ihangane), **B-340** (tier 4, gated Completion batch — server NCD completion copy not updated for B-288/B-289; orchestrator read both PHP sites against the client diff). **B-337 REFUTED** (orchestrator confirmed `viewNonRDTForm`/`viewUrineDipstickTestForm` never read `immediateResult`). Coverage row added.
- **Unit 4 (release server delta main..develop) COMPLETE** (scout 195k tokens / 16 min, 0.19.2 rebuild of elm-main.js byte-identical, live queries on all four sites) → **B-341** (tier 2, MED — `restore-wiped-photos.php` ships in #2128 but is not in its post-deploy steps; ~3,213 persons stay photo-less; 199 of ihangane's are B-098's uid-0 lost update, not B-244's; orchestrator read the #2128 and #2152 bodies, issue #2151 state and B-244's entry). No code defect in the delta. Coverage row added.
- **Unit 1 (blood-smear stack as merged) COMPLETE** (scout 295k tokens / 18 min, scratch elm-test, one live query) → **B-342** (⭐ tier 1, MED-LOW — mixed fleet: `blood_smear_ordered` outlives an old client's RDT conversion and suppresses a real positive RDT on new clients; introduced by #2228, un-deployed; orchestrator read `bloodSmearOrderedByValue`, the diagnosis guard, the released `encodeMalariaTestValue` and the sync PATCH key loop). Six "smear ordered" derivations inventoried. Coverage row added.
- **Unit 2 (prenatal HIV stack as merged) COMPLETE** (scout 346k tokens / 17.5 min, scratch elm-tests, live query) → ⛔⛔ **B-336 STRANDED**: PR #2214 merged 38 s after its parent into the parent's already-merged branch, never reached develop; orchestrator verified via `gh pr view` + `merge-base --is-ancestor` and checked EVERY PR merged since 2026-08-20 (only #2214 affected); B-336 entry corrected to READY, HANDOFF correction added, `stacked-pr-base-is-parent-branch` memory given a merge-time clause. New: **B-343** (⭐ tier 1, stale partner-positive answer, 3 live), **B-344** (⭐ tier 1, pending marker = no HIV programme), **B-345** (⭐ tier 1, health-education twin of B-336), **B-346** (tier 4, re-tap guard after reload). F6 (partner question skipped after lab marks not-run) kept as an unverified coverage note. Coverage row added.
- **Unit 7 (case management / activity / user / whatsapp) COMPLETE** (scout 241k tokens / 19 min, live queries on three sites) → **B-347** (tier 3, MED — zero/short WhatsApp numbers pass validation, told Success, fail out silently; orchestrator re-ran the delivery-by-length query on ihangane: 245 delivered all 13 chars, 34 failed all malformed), **B-348** (tier 3 — Burundi dropdown defaults to +250; orchestrator re-queried vhw prefixes: 19 × +250, 3 × +257, 0 delivered), **B-349** (tier 3, config — vhw/uvl flag on with no Twilio variables; orchestrator re-queried vhw variables). Case management / activity / user clean apart from coverage notes. Coverage row added.
- **Unit 6 (client JS layer) COMPLETE** (scout 285k tokens / 24 min, rollbar UMD run in node, live queries) → **B-350** (tier 3 — Rollbar `sync`/`sw` errors before init stored as sent; init only on the General download's last page, so jammed devices never report; orchestrator read the port branch, the sole init caller, Elm's token-only gate and the localStorage token restore), **B-351** (tier 3 — offline name search limits to 500 before sort and before Elm's CHW-village/type filters; 749 same-first-name persons in one ihangane HC), **B-352** (tier 4 — local persons stored with geo-data spelling miss the title-cased exact village index). **B-040 amended** (Authority photo blobs never deleted — `sendLocalIdsForDelete` only ever called with General). ⚠ Scout's `pkill -f "terminus remote:drush ihangane.live -- sql-query"` also matched a third PID — may have killed another session's live query. Coverage row added.
- **Unit 5 (node CRUD hooks + shards + AQ workers) COMPLETE** (scout 364k tokens / 25.5 min, live queries on ihangane/vhw/tip-somalia) → **B-353** (tier 4 — follow-ups resolved by nid order, 18/5,616 next-visits closed by earlier-dated encounters; joint with unit 7's independent measurement; orchestrator read the immunization resolver), **B-354** (tier 4, latent tip-somalia — `family_participant` missing from the deletion bundle list and the shard walk; orchestrator read both plus the device-shard sibling), **B-355** (tier 4 — stats cache trigger gaps; orchestrator read the CLI early return and the NCD trigger list). B-219 + B-020 (PARKED, evidence only) amended. Coverage row added.
**⭐ R30 COMPLETE (2026-09-14): 17 new items B-339..B-355 — 5 tier-1 (B-339 NCD re-score reads later encounters, introduced by #2216; B-342 mixed-fleet smear flag suppresses a real positive RDT, introduced by #2228; B-343 stale partner-positive answer outranks the partner's negative test, 3 live; B-344 lab-tech pending marker read as "no HIV programme"; B-345 health-education twin of B-336), 1 tier-2 (B-341 release #2128 omits the photo repair run, ~3,213 persons), 5 tier-3 (B-347/B-348/B-349 WhatsApp — 12% silent failures on ihangane, +250 default on Burundi, vhw/uvl never delivered; B-350 Rollbar pre-init suppression; B-351 people-search 500 cap), 6 tier-4 (B-340, B-346, B-352, B-353, B-354, B-355). ⛔⛔ **B-336 found STRANDED** — PR #2214 merged into its dead parent branch, never reached develop; entry back to READY (T1 READY count includes it). B-337 REFUTED. Amended: B-040, B-219, B-020 (parked, evidence only). 7 coverage rows (none fully clean). Orchestrator live queries: 3 (ihangane WhatsApp delivery by length; vhw variables; vhw record prefixes). Counts after R30: 212 READY — T1 6 · T2 6 · T3 45 · T4 121 · untiered 34. Dry-stop counter: 0.**
📌 **R30 durable lessons.** (1) **A "merged" PR is not proof its code is on develop** — a stacked child merged before GitHub's retarget lands on the dead parent branch and still shows MERGED; verify with `git merge-base --is-ancestor <head> origin/develop` (memory updated). (2) **Reading a merged cluster as one surface pays even after 3–4 per-PR review rounds** — units 1–3 found 5 new tier-1 defects on code that had each passed review, three of them introduced or exposed by those same PRs (B-339 by removing a masking head element, B-342 by adding a field old clients cannot clear, B-345/B-344 as the unfixed twins of B-336/B-323). (3) **Removing a value from the head of a history list can un-mask a wrong list** — B-339's history always included later encounters; the encounter's own stage at the head hid it. (4) **A new sticky field needs its "who clears it" traced across client versions** — PATCH writes only the keys it receives, so an old client can never unset a flag. (5) **Configuration is a finding** — B-349's flag-on/Twilio-absent state produced 22 "Success" messages and zero deliveries for 17 months.

## Round 31 (2026-09-14, @1681397f9, COVERAGE-SWEEP — the un-read client half of release #2128 plus the R23–R25 rows it touches)
Units (7 worktree scouts, one wave; user approved the plan as proposed): (1) Pages/GlobalCaseManagement full re-read + nodes.js follow-up synthesis (R24 row; largest client release delta); (2) Pages/Dashboard + Backend/Dashboard codecs down to hedley_stats payloads (R25 row); (3) Prenatal + WellChild + FamilyNutrition progress reports + shared Pages/ProgressReport (R23 rows); (4) AcuteIllness + NCD + TB + HIV + ChildScoreboard progress reports + WhatsApp screenshot path (R23 row); (5) Backend/Update.elm full re-read + SyncManager/Encoder + Backend/NutritionEncounter/Utils release deltas (R23 row); (6) Pages/Report + Pages/Utils + shared Utils helpers + Translate.elm release delta (objective-wrongness only) (R25–R27 rows); (7) hedley_restful non-sync endpoints (R24 row). Out: everything R28–R30 read, Prenatal/NCD activity pages beyond the release delta, Translate as a whole, MessagingCenter, GeoLocation, hedley_migrate, TH/G tracks, declined classes. Candidate ids start at B-356.
- **Unit 6 (Pages/Report + Pages/Utils + Backend/Utils + Translate delta) COMPLETE** (scout 234k tokens / 10 min, scratch render tests, live query) → **B-356** (tier 3, MED-LOW, DEPLOYED since 2022 — pending urine dipstick / lipid panel summary rows show "Abnormal"; orchestrator read all three verdict functions and confirmed the code is on origin/main), **B-357** (tier 4 — known-positive dated today, blank in history; orchestrator read the dating line, the empty translation and the history cell). Pages/Utils, Backend/Utils and the Translate delta clean. Coverage row added.
- **Unit 7 (hedley_restful non-sync endpoints) COMPLETE — ✅ CLEAN, no new items** (scout 198k tokens / 12.5 min, live device-robot check on four sites). 225 resources classified (214 sync / 11 non-sync), 7 hook_menu modules (no JSON callbacks); every traced defect already recorded (B-225, B-246, B-311, B-020, B-086, B-096). Coverage row added.
- **Unit 4 (AI/NCD/TB/Child Scoreboard progress reports + WhatsApp screenshot path) COMPLETE** (scout 267k tokens / 13.5 min, scratch render test, live queries) → **B-358** (tier 3, MED — Child Scoreboard report Immunization History empty without a Well Child record, 3,341 ihangane children; orchestrator read both vaccination builders in the shared view), **B-359** (tier 3 — AI report prints lemon juice/honey administered unconditionally; orchestrator read the branch and the medication-step age rule), **B-360** (tier 4 — AI physical exam graded by today's age). B-339 amended (NCD report readers), B-224 noted fixed on develop. Coverage row added.
- **Unit 3 (Prenatal/Well Child/Family Nutrition progress reports) COMPLETE** (scout 358k tokens / 16 min, scratch render test, live queries) → **B-361** (tier 3, MED-LOW — Patient Record prenatal report decides nurse/CHW placement of the viewed encounter from the viewer's role; orchestrator read the entry, the partition and both role-keyed re-insertions), **B-362** (tier 4 — WhatsApp from Review-prior-diagnosis mode drops panes; orchestrator read the mode switch and mode-independent actions), **B-363** (tier 4 minors ×4). Release delta refactors byte-equivalent. Coverage row added.
- **Unit 1 (GlobalCaseManagement full re-read) COMPLETE** (scout 248k tokens / 17 min, live queries on ihangane/vhw/uvl) → no new item; **B-233 WIDENED** to a loop (the AI-derived TB entry survives its own tap; 19 participants for one person in a day; B-233's fix shape does not cover the not-diagnosed case) — orchestrator read the entry gate, the new-participant branch and the not-diagnosed close. Unreleased delta sound. Coverage row added.
- **Unit 5 (Backend/Update.elm full re-read + release deltas) COMPLETE** (scout 402k tokens / 21 min, scratch elm-test, live queries) → **B-364** (⭐ tier 1 — Prenatal twin of B-339: an older encounter re-scored with later encounters as history; latent, 368 precondition records; orchestrator read the history builder, three adding readers and the write-back), **B-365** (⭐ tier 1 — six Prenatal diagnosis inputs missing from the assessment special cases; orchestrator enumerated the special-case list). Every other program's re-score path cleared per program. Coverage row added.
- **Unit 2 (Dashboard full re-read) COMPLETE** (scout 418k tokens / 21.5 min, scratch round-trip test of every payload field, live queries on four sites) → **B-366** (tier 3 — nurse ANC Hospital Referrals always 0; orchestrator read the card, the builder join list and re-ran a live query), **B-367** (tier 3 — COVID figures count only the obsolete suspect diagnosis), **B-368** (tier 4 minors ×3). Release Encoder delta clean. Coverage row added.
**⭐ R31 COMPLETE (2026-09-14): 13 new items B-356..B-368 — 2 tier-1 (B-364 Prenatal later-encounters-as-history, the B-339 twin; B-365 six Prenatal diagnosis inputs never re-trigger assessment), 6 tier-3 (B-356 pending dipstick/lipid "Abnormal", 1,754 pending on ihangane; B-358 Child Scoreboard immunisation history empty, 3,341 children; B-359 AI report always says lemon juice/honey given; B-361 Patient Record prenatal report places the latest visit by viewer role; B-366 nurse ANC hospital referrals always 0; B-367 COVID dashboard counts only the obsolete suspect diagnosis), 5 tier-4 (B-357, B-360, B-362, B-363 ×4, B-368 ×3). **B-233 WIDENED** to a loop (19 TB participants for one person in a day). Amended: B-339 (NCD report readers), B-224 (fixed on develop by B-289). 7 coverage rows (1 ✅ clean: hedley_restful non-sync endpoints). Orchestrator live queries: 1 (ihangane send_to_hc by encounter type vs hospital referrals). Counts after R31: 225 READY — T1 8 · T2 6 · T3 51 · T4 126 · untiered 34. Dry-stop counter: 0.**
📌 **R31 durable lessons.** (1) **A defect found in one program is a question for every program that shares the idiom** — B-339 (NCD) asked of `Backend/Update.elm` found B-364 (Prenatal) the same day and cleared the other seven programs with a reason each (same-day initial phases, navigate-only triggers). (2) **A dashboard card can be dead for years if nothing compares it to the form that writes its field** — B-366 counts a field nurses stopped writing 16 months before the card existed; B-367 counts a diagnosis the engine stopped emitting. Cross-check every card's predicate against the CURRENT writer. (3) **"Viewer role" and "record type" are different axes** — B-361 used `isChw` to decide how to place an encounter whose own type says otherwise; right on the encounter page, wrong from the Patient Record. (4) **A list entry derived from a record the server resolves needs a device-side retirement** — B-233's TB entry lives until a download, and each tap in that window creates data.

## Round 32 (2026-09-15, @e871d98b6, COVERAGE-SWEEP — the un-read structural and mixed-fleet halves of release #2128, plus the R11–R24 rows never re-read at R25+ depth)
Units (7 worktree scouts, one wave; user approved the plan as proposed): (1) R22 deduplication PRs as merged (#2108–#2116, #2120) — every removed copy vs the survivor, per caller; (2) mixed-fleet contract of release #2128, client side — every write/read change on a synced record, old v1.18.1 device ↔ new device ↔ server, plus post-deploy scripts vs old writers; (3) encounter + participant lifecycle, all ten programs — every create path vs the user's one-encounter-a-day rule, close/conclude/end-date, active predicates (R24 rows); (4) next-visit + follow-up date generation, all programs (R11 row; vaccination engine, GCM consumers, B-353 resolver excluded); (5) hedley_patient + hedley_person server re-read (R23 row); (6) App shell + encounter-type selection pages (per-site feature flag × role gating) + small pages (R23/R24 rows); (7) hedley_general + hedley_device + hedley_schedule server (R14 row). Out: everything R28–R31 read, Translate, MessagingCenter, GeoLocation, hedley_migrate, TH/G tracks, vaccination engine, Prenatal/NCD diagnosis engines, all declined classes. Candidate ids start at B-369.
- **Unit 1 (R22 dedup PRs as merged) COMPLETE — ✅ CLEAN, no new items** (scout 213k tokens / 6 min; server/elm bundle rebuilt byte-identical). ~30 consolidations across 10 PRs equivalent per caller; only #2115's decode-error display text changed. Orchestrator spot-checked #2114's `LT` filter and #2115's error-text lines. Coverage row added.
- **Unit 2 (mixed-fleet contract of release #2128, client side) COMPLETE** (scout 250k tokens / 13 min, 4 live queries) → **B-369** (tier 4, LOW — #2134's undated-pregnancy expiry lets a new device open a second pregnancy that v1.18 devices see as a second active one, B-254's arbitrary pick then applies; orchestrator read both `isPregnancyActive` versions, the `viewActions` filter/head and the nurse first-visit action). No change in the delta breaks old decoders. Beverages repair-script race kept as a coverage note (0 live crossed saves). ⚠ `origin/main` is tagged **v1.18.2** (HANDOFF says v1.18.1). Coverage row added.
- **Unit 6 (App shell + flag × role gating + small pages) COMPLETE** (scout 294k tokens / 12 min, live flag map on four sites) → **B-370** (tier 4, LOW — `handleRevision` requires a selected HC, so the location picker never sees a newly added assignment; orchestrator read the handler, `getLoggedInData` and both picker filters), **B-051 AMENDED** (inherited CHW village crosses roles: nurse Case Management shows CHW panes, lab tech locked out of Prenatal Labs; orchestrator read the gate and the village-keyed view/fetch split). Flag gating consistent at every entry point. Coverage row added.
- **Unit 4 (next-visit + follow-up date generation) COMPLETE** (scout 228k tokens / 15 min, scratch elm-test, live queries on vhw/ihangane) → **B-371** (⭐ MED, tier 3, DEPLOYED since v1.15.0 — Prenatal CHW visits 2/3 on an undated pregnancy can never be ended: Next Visit is required, its Save is `NoOp` without an LMP, and those visits have no Pregnancy Dating; orchestrator read the save msg, task expectation/completion, CHW activity lists and End gating, and re-ran the vhw live query — 38/38 undated visits since 2026-03-01 open with no next-visit date, 25 of them stuck after reaching Next Steps), **B-372** (tier 4, LOW — Well Child month-end clamp makes the next pediatric visit today; orchestrator read the ladder, scout scratch test). Coverage row added.
- **Unit 3 (encounter + participant lifecycle, all programs) COMPLETE** (scout 282k tokens / 16 min, live queries on ihangane/vhw/uvl) → **B-373** (⭐ MED, tier 3, DEPLOYED — Pregnancy Outcome saved inside a postpartum visit closes the pregnancy, the open visit drops off the participant page, and Postpartum opens a second pregnancy the same day; orchestrator read activity order, close handler, both role branches and the vhw worked example live: person 87258, open `chw-postpartum` 97475 then a new participant + 97481 25 minutes later, outcome recorded twice), **B-374** (tier 4, LOW-MED — GCM antenatal entries of expired pregnancies still create visits; orchestrator read the entry filter, the tap POST and grepped the pane for any expiry check), **B-284 AMENDED** (unconfirmed TB/HIV same-day sibling). Same-day rule otherwise holds; `isPostInFlight` covers every create handler. Coverage row added.
- **Unit 7 (hedley_general + hedley_device + hedley_schedule) COMPLETE — ✅ CLEAN, no new items** (scout 215k tokens / 17 min). Two suspicions refuted by running them (stale person cache — saved by `hedley_activity`'s `resetCache`, load-bearing; photo storage-name collisions — unique key). Coverage row added.
- **Unit 5 (hedley_patient + hedley_person server) COMPLETE** (scout 245k tokens / 19 min, three local rolled-back runs, live queries on ihangane/vhw/uvl) → **B-375** ✅ **IMPLEMENTED 2026-09-17, issue #2263, PR #2264** (MED, tier 3, latent — merge's acute-illness step looks up `'acute_illness'`, stored value is `'acute-illness'`: no participant moves and the delete cascade soft-deletes the AI measurements just moved to the original; orchestrator read both literals, the mapping constant, the lookup and the cascade), **B-376** (MED-LOW, tier 3, latent — merge picks the newest unconcluded pregnancy as "ongoing", usually an expired shell, and fuses the current pregnancy into it; orchestrator read the handler against the client's active rule and LMP head; ⚠ tier-1 question left to the user), **B-377** (tier 4 — CSV import two-digit years stored literally; orchestrator `php -r` + live count: 112 ihangane persons born 0016–0022), **B-378** (tier 4, latent — presave guard un-deletes an already-deleted person on any later save; orchestrator read the guard). Coverage row added.
**⭐ R32 COMPLETE (2026-09-15): 10 new items B-369..B-378 — 0 tier-1, 0 tier-2, 4 tier-3 (B-371 ⭐ DEPLOYED CHW visits 2/3 on an undated pregnancy can never be ended — 38/38 open on vhw since March; B-373 ⭐ DEPLOYED outcome inside a postpartum visit opens a second pregnancy the same day — 12 ihangane / 8 vhw in 2026; B-375 merge deletes the original's acute-illness measurements, latent; B-376 merge fuses the current pregnancy into an old shell, latent), 6 tier-4 (B-369 mixed-fleet second active pregnancy from #2134, B-370 location picker ignores nurse updates, B-372 Well Child month-end next visit = today, B-374 GCM antenatal entries of expired pregnancies, B-377 import two-digit birth years — 112 live, B-378 presave un-delete). Amended: B-051 (inherited CHW village crosses roles), B-284 (unconfirmed TB/HIV same-day sibling). 7 coverage rows (2 ✅ clean: R22 dedup PRs as merged; hedley_general/device/schedule). Release #2128's structural refactors verified equivalent and its mixed-fleet contract holds apart from B-369. Orchestrator live queries: 4 (vhw undated CHW visits ×2; vhw person 87258; ihangane birth years < 100). Counts after R32: 231 READY — T1 4 · T2 6 · T3 55 · T4 132 · untiered 34. Dry-stop counter: 0.**
📌 **R32 durable lessons.** (1) **A task that is "done when a stored value exists" needs a path that can always produce the value** — B-371's Save is `NoOp` when its input is missing, and the button stays enabled, so the encounter is trapped silently. (2) **An action that closes the parent inside a child still open hides the child from every "active parent" filter** — B-373 (outcome closes the pregnancy mid-postpartum-visit); check every close-from-inside flow against the page's own active filter. (3) **An expiry rule added to one entry point is a question for every entry point** — #2134 expired pregnancies on the participant page; Case Management (B-374), the merge tool (B-376) and old clients (B-369) each keep their own idea of "ongoing". (4) **A hyphen/underscore literal mismatch in admin tooling is invisible until it deletes data** — B-375 has been wrong since 2020 and only a discrimination run proved it; prefer the file's own mapping constant. (5) **Load-bearing side effects deserve a note** — shard propagation works only because `hedley_activity` resets the node cache first (R32 unit 7).

## Round 33 (2026-09-20, @3eef4b666, COVERAGE-SWEEP — the un-deployed diagnosis/report-code merges read as merged surfaces, the Translate duplicate-English fold, and the four oldest live rows never re-read at R25+ depth)
Units (7 worktree scouts, one wave; user approved the plan as proposed): (1) prenatal/NCD diagnosis-history cluster AS MERGED — B-339 (#2241) + B-364 (#2243) + B-365 (#2249) + the re-assess triggers in Backend/Update + the three new Test.elm suites, with B-345/B-343/B-344 where they touch the same functions; (2) diagnosis→report-code mapping AS MERGED — B-308 (#2255) + B-245 (#2259) + hedley_reports.module tables for every program + the new recalculate-newly-mapped-prenatal-diagnoses.php + HedleyReportsDataEndpoint.test + server/elm Reports decoder; (3) the duplicate-English Translate fold (#2238, five commits, client +735/−1071 and server +84/−98) — every folded id × every call site × all four languages, plus the new CI duplicate check; (4) GeoLocation end to end (R18 row: both data copies, gps capture path, address cascade, per-site administrative divisions); (5) MessagingCenter / Wellbeing / Resilience end to end (R19 unit 1, first-ever row, never re-read); (6) shared micro-dirs Components/ + DateSelector/ + Gizra/ + uuid.js and every call site (R19 unit 3 / R17); (7) Dexie schema and the offline storage contract (R11 row — versions, query→index pairs, dbVersion quadruple, upload-cleanup table shapes; NOT the JS logic R30 unit 6 cleared). Out: everything R28–R32 read, the vaccination engine, hedley_restful sync plugins (R29 unit 2 / R31 unit 7 re-read that surface in substance), hedley_migrate, the TH-00..TH-27 and G-01..G-07 tracks, all declined classes. Candidate ids start at B-381.
Frame: **nothing is deployed** — origin/main is still 7ece1b2d0 (v1.18.2) with release PR #2128 open, so everything R30–R32 fixed plus the 11 merges since sits unshipped; a defect found in units 1–3 is caught before it reaches a device. Queue at frame time: 220 READY — T1 0 · T2 2 · T3 52 · T4 132 · untiered 34. Dry-stop counter: 0.
- **Unit 2 (diagnosis→report-code mapping as merged) COMPLETE — ✅ CLEAN, no items** (scout 209k tokens / 15 min). Orchestrator re-derived both tables independently: prenatal 64 codes ↔ 64 Elm cases, AI 15 + `z`=`none` undecoded, zero orphans either direction, zero constructors reachable by two codes; B-308's j/k/l rotation confirmed fixed. New codes assigned BY NAME. Script verified present in #2128's post-deploy list and uses three independent `field_deleted` aliases. No mixed-fleet exposure. ⚠ Handed unit 1 a lead: `high-risk-of-preeclampsia-*` has ZERO rows on all four sites. Coverage row added.
- **Unit 6 (shared micro-dirs + every call site) COMPLETE** (scout 224k tokens / 15 min) → **B-381** (MED-LOW, tier 3 — Child Scoreboard birth-date calendar wrong at BOTH ends: today unselectable so a newborn cannot be registered, and the earliest offered day already fails `isChildUnderAgeOf2` so the child vanishes from the search; orchestrator read both bounds, the age predicate, the `Date.isBetween` gate and confirmed the widget is calendar-only and required), **B-382** (MED on uvl, tier 3 — National-ID search is digits-only while Burundi/Somalia store alphanumeric IDs; orchestrator live-counted uvl 2,434 IDs / 2,429 non-digit vs ihangane 67,318 / 2). All 16 `DateSelectorConfig` ranges checked, none inverted; the selector's disabled state proven NOT CSS-only. ⚠ `src/elm/Gizra` is globally exempt from elm-review — four modules wholly dead. Coverage row added.
- **Unit 5 (MessagingCenter / Wellbeing / Resilience) COMPLETE** (scout 229k tokens / 17 min) → **B-383** (LOW, tier 4 — the 3-survey cap counts raw nodes while its sibling dedups by `dateMeasured` with a comment saying why; orchestrator re-ran the live duplicate query: exactly one group, nurse 1627758, 3 nodes over 2 dates, permanently blocked), **B-384** (LOW, tier 4, latent — three different gates for one programme), **B-385** (LOW, tier 4 — Kinyarwanda End-Of-Period header reads "first month"; needs the author's confirmation). ⭐ Usage re-measured: per-nurse gating only, 20 nurses on ihangane alone, zero surveys in 2026, all programmes ended Apr/Aug 2025. Coverage row added.
- **Unit 7 (Dexie schema + offline storage contract) COMPLETE — ✅ CLEAN, no items** (scout 257k tokens / 19 min). Version still 32, nothing added since R11; orchestrator re-verified the quadruple independently. 214 entity types across all three registries. v32 family-nutrition slice fully wired incl. both photo dispatches. ⚠ No CI guard ties the four version constants together, and a too-LOW `dbVerno` is benign while too-HIGH is the damaging direction. Refuted by reading Dexie source: close/reopen does not strand cached Table hooks. Coverage row added.
- **Unit 3 (Translate duplicate-english fold as merged) COMPLETE — the fold is CONTENT-CLEAN** (scout 203k tokens / 23 min) → **B-386** (LOW, tier 4 — client and server now spell five clinical labels differently), **B-387** (LOW, tier 4 — the new check's exemption list is unbounded; commit `8a0099761` claims it "refuses additions" and **no such mechanism exists**; plus 106 `++`-built records silently skipped and per-file comparison that cannot see cross-copy drift). ⭐ Record count proved against grep before use (3,174 + 233 = 3,407) and the check's own 3,682 reproduced. All-negative: 0 `Just`→`Nothing`, 0 new `Just <english>`, 0 text from a different-meaning record, 0 new indistinguishable pairs, 0 new client/server conflicts. Coverage row added.
- **Unit 4 (GeoLocation end to end) COMPLETE** (scout 281k tokens / 26 min) → **B-388** (MED, tier 3, latent — a blank geo field on a village makes the general batch undecodable and stops ALL sync for the site; the health-centre presave guard 400 lines away exists for exactly this consequence), **B-389** (LOW, tier 4, latent — uvl village 21 stores sector `Nyagasaga` vs the dict's `Nyagasasa`, byte-confirmed), **B-390** (MED, tier 3 — ⬖ **SPLIT out of B-150's tail**, where it sat marked STILL OPEN inside an IMPLEMENTED item the queue never surfaces), **B-391** (LOW, tier 4 — eleven views hardcode `Translate.Village`). B-151 re-swept mechanically across 3 sites × 5 levels in both copies: 0 duplicates, 0 orphans, 0 collisions. Copy parity entry-for-entry. Coverage row added.
- **Unit 1 (diagnosis-history cluster as merged) COMPLETE** (scout 362k tokens / 30 min, scratch elm-test through the shipped harness, live sizing on ihangane + vhw) → ⭐ **B-392 (tier 1 — the duplicate-referral guard reads a history that cannot contain the encounter it reports to; introduced by the B-364 merge, NOT yet deployed).** Orchestrator traced all five links in source. ⭐ **The preeclampsia lead is ANSWERED: reachable and rare, NOT dead** — run-verified, and the zero live rows are an opportunity problem (the block became effective ~2026-03-04 as ihangane volume collapsed; the one real opportunity stored `none` correctly at EGA 8 weeks). The merge did not cause it and B-365 improves it. Otherwise clean: same predicate in both programs, every consumer filtered, trigger set complete both directions. ⛔ B-364's entry carried a factual error about labs-history results — corrected. Coverage row added.
**⭐ R33 COMPLETE (2026-09-20): 12 new items B-381..B-392 — ONE tier 1 (B-392 ⭐ the duplicate-referral guard broken by the un-deployed B-364 merge — caught before the release ships), 0 tier 2, 4 tier 3 (B-381 Child Scoreboard birth-date calendar wrong at both ends; B-382 National-ID search unusable on Burundi — 99.8% of stored IDs; B-388 a blank village geo field stops ALL sync for a site, latent; B-390 stale GPS fix saved onto the next person, split out of B-150), 7 tier 4. Amended: B-150 (sibling split out), B-364 (factual error corrected). 7 coverage rows, **3 of them ✅ CLEAN** (report-code mapping as merged; Dexie schema; and the Translate fold's content). Two un-deployed merge clusters verified — one held, one did not. Orchestrator live queries: 8 (preeclampsia/hypertension counts ×4 sites, resilience duplicate group + the blocked nurse's set, national-ID digit counts ×2, village NULL geo ×4, uvl village 21 hex). Counts after R33: 232 READY — T1 1 · T2 2 · T3 56 · T4 139 · untiered 34. Dry-stop counter: 0.**
📌 **R33 durable lessons.** (1) ⭐ **A history filter change silently invalidates every guard that reads that history for a DIFFERENT question.** B-364 correctly redefined "history" for diagnosing an encounter; B-392 is a guard asking "does the encounter I am reporting TO already know this?" — same list, different question, and the cut made it always answer no. When a shared accessor's meaning narrows, re-ask what each caller was using it to mean. (2) ⭐ **A still-open sibling recorded inside an IMPLEMENTED item is invisible to the queue.** B-150's tail said "STILL OPEN" twice and no seat ever saw it; B-390 only exists because a sweep re-read the area. Split a surviving sibling into its own READY entry at the moment the parent closes. (3) **A commit message can document a guarantee the code does not implement** — B-387's check "refuses additions" to its exemption list and nothing enforces that. Read the guard, not its description. (4) **Zero live rows is not evidence of dead code.** The high-risk-preeclampsia derivation is fully reachable; it has had ~2 qualifying opportunities in production because the code became effective exactly as site volume collapsed. Size the OPPORTUNITIES, not the outcomes, before calling a branch dead. (5) **"Tolerated blank server-side = poison record client-side" now has a second live instance** (B-388, villages) — the B-108 lesson generalises to every optional backend field whose client decoder says `required`.

## Round 34 (2026-09-20, @53b9558ed — code identical to R33's 3eef4b666, COVERAGE-SWEEP — the verification and delivery layer, never re-read since R18/R19, plus the two surfaces that have never had a row)
Units (6 worktree scouts, one wave; user approved the plan as proposed): (1) the E2E Playwright suite as it stands today — 23 specs / 9,305 lines + helpers, the spec x test-title x CI-job run matrix (the partition trap), whether each test discriminates, off-Rwanda helper assumptions, tests that lock in a defect, and the current truth of the D1-D6 / TH-20..TH-27 tracks (R18 row); (2) the unit-test layer, FIRST-EVER row — 35 Elm `*Test.elm` (13,343 lines) + the 8 custom SimpleTest classes: executability (B-248's silently-skipped class), discrimination, wrong expectations that point at app defects, fixtures that hide the behaviour; (3) CI pipeline + all 18 `ci-scripts/` at R25+ depth (R19 unit 5 row) including the two never-examined new checks `check_comments.py` (#2247) and `test_translations.js` (#2238), the e2e partition rule, exit-code propagation, the no-cache structural flake, `[ci skip]` reach, and a per-item verdict on B-157 a-g; (4) deploy and build tooling at R25+ depth (R18 row) — RoboFile, gulpfile build vs publish, pantheon.yml, `.ddev`, `server/install`, the deploy skills' cc/updb/fra order, repo-vs-deployed drift in all four Pantheon checkouts, and a per-item verdict on B-143/B-144/B-145/B-146; (5) the six server modules with no R25-depth row of their own — `hedley_device` (pairing, shards), `hedley_schedule`, `hedley_view_export` + its AQ worker, `hedley_forms`, `hedley_faker`, `hedley_zscore` — module code, Features exports, plugins and every caller; (6) Drupal config-as-code integrity repo-wide, all 31 custom modules — field cardinality vs use, `required` vs the client decoders, `allowed_values` vs both Elm copies both ways, content types vs the 214 sync entity types, report/admin views' `field_deleted` filtering, permissions/roles, `hook_update_N`. Out: everything R25-R33 read at depth; the vaccination engine; `hedley_migrate` (R16 full audit, and its degrade-gracefully class is declined via B-108); the `hedley_admin`/`hedley_reports` scripts family; the TH-00..TH-27 and G-01..G-07 tracks; all declined classes. Candidate ids start at B-393.
Frame: **nothing merged since R33** — the four commits since `3eef4b666` are backlog bookkeeping only, so the code is byte-identical to what R33 swept. **Nothing is deployed**: `origin/main` is still `7ece1b2d0` (v1.18.2), 529 commits behind `develop`, release PR #2128 open. Five backlog PRs awaiting merge (#2262, #2264, #2266, #2269, plus #2237). Queue at frame time: 232 READY — T1 1 (B-392, un-deployed) · T2 2 · T3 56 · T4 139 · untiered 34. Dry-stop counter: 0.
- **Unit 2 (the unit-test layer, FIRST-EVER row) COMPLETE** (scout 325k tokens / 14 min) → **B-402** (LOW, tier 4 — a test comment asserts a FINDING that is false; orchestrator refuted it by reading the actual strings: the Labs tab's `(F) 74-126` / `(NF) 74-200` match the code arm for arm, and `randomBloodSugarResultNormal` is their exact complement), **B-403** (LOW, tier 4 — the 214-branch `HandleRevisions` dispatch is untested AND untestable-by-construction, so B-365's six new branches have no guard). Amended **B-269** (⚠ fixing the group name would NOT restore the "cumulative offsets" coverage — the offsets are computed in `assembleContainer`, which no test calls) and **B-248** (two scenario-4 fixtures build unsharded data: `createChild($this->clinicId)` passes a clinic where a mother is expected, `createMother()` never sets `field_health_center`). ✅ Elm executability and discrimination CLEAN across all 34 suites; 2,442 z-score cases diffed against the 2,450 PHP ones; all seven `HedleyActivityZscoreUpdate` expectations recomputed exactly from the WHO LMS JSON. ⛔ Three of its candidates (fasting `>126` vs ADA `>=126`, NCD Stage-1 OR/AND, the pre-eclampsia BP gate) are the **declined boundary-convention class** and were NOT queued. Coverage row added.
- **Unit 1 (E2E suite as it stands today) COMPLETE** (scout 383k tokens / 16 min) → ⭐ **B-393** (MED, tier 3 — one test in ZERO CI jobs since 2026-03-15; orchestrator reproduced the full matrix with `playwright test --list`: 11+25+27+1 = union 64 of 65, zero tests in two jobs, and the one uncovered test is `nutrition-encounter.spec.ts:36`), **B-400**, **B-401**. Amended B-380 (a traced mechanism lead) and B-330 (corroborated from two e2e helper reload loops). CLEAN: all 65 tests carry assertions, zero lock in a defect, `forbidOnly` on under CI. D1–D6 re-verified (D3 half fixed). Coverage row added.
- **Unit 4 (deploy & build tooling) COMPLETE** (scout 198k tokens / 17 min) → ⭐ **B-394** (MED, tier 3 — `healthy_start` is ON in Rwanda production, confirmed live, and is the one available feature no dev or CI environment enables, so nothing gated on it is ever tested), **B-404** (⬖ split from B-144), **B-405** (302/302 deployed images byte-identical to source — the imagemin step produces nothing that ships), **B-406** (precache revisions are pre-minification hashes; `app.js`, on the uglify exclusion list, is a clean control whose revision DOES match), **B-407**, **B-408**, **B-409**, **B-410**, **B-411**. Verdicts: B-143 **FIXED**, B-144 **half-fixed**, B-145 **CONFIRMED + sharpened**, B-146 **CONFIRMED**. Coverage row added.
- **Unit 5 (the six unrowed server modules) COMPLETE** (scout 268k tokens / 16 min) → **B-399** (LOW, tier 4 — `field_incident_type` allows `files-upload` while the client sends `file-upload`; orchestrator counted **9 live rows** on ihangane), **B-412** (LOW, tier 4, latent — three instances permitting a shape the decoder or the sync query cannot survive; live counts taken on all four sites). ✅ CLEAN: `hedley_zscore` (harness re-run 2450/2450, both read-raised traps disproved), `hedley_faker` (inert in production **proven from the guard, not the name**), `hedley_forms`, `hedley_schedule` (4 allowed-values sets cross-checked exactly against the client). Co-found B-413 from the export side. Coverage row added.
- **Unit 3 (CI pipeline + all 18 ci-scripts) COMPLETE** (scout 222k tokens / 21 min) → ⭐ **B-395 (tier 2 — the release gate is RED on `develop` since 2026-09-16; four e2e literals still expect English strings PR #2238 changed, and all five of that PR's commits carry `[ci skip]` so nothing ever ran on it)**, **B-396** (the `&&` short-circuit silently skips 25 of 65 tests), **B-397** (`[ci skip]` + zero required status checks = merge with no CI at all), **B-398** (the CI log cap hides which SimpleTest classes ran). Co-found B-393. Per-item verdicts on **B-157 (a)–(h)** written into that entry — six fixed, (e) and (f) still open — and B-387 extended with two re-measured additions. Orchestrator verified all four string pairs, the CircleCI status of every `develop` build since the fold, the five `[ci skip]` commits and the empty branch protection. Coverage row added.
- **Unit 6 (Drupal config-as-code integrity, all 31 modules) COMPLETE** (scout 382k tokens / 32 min) → **B-413** (MED, tier 3 — admin lists and CSVs show soft-deleted content; orchestrator live-counted 159 persons / 156 relationships / 118 pmtct_participants with `field_deleted=1 AND status=1` on ihangane), **B-414**, **B-415**, **B-416** (latent, 0 live rows on all four checked values), **B-417**. ⭐ The clean both-ways diffs are the deliverable: 214/214 registry agreement, **216/216 `$multiFields` cardinality agreement with B-267 the sole exception**, zero orphan bases or instances, and **no `allowed_values` set changed in `main..develop`** — so the deployed-client enum hazard does not apply to release #2128. Coverage row added.
**⭐ R34 COMPLETE (2026-09-20 → 09-21): 25 new items B-393..B-417 — 0 tier 1, ONE tier 2 (B-395 ⭐ the release gate has been red for five days and nobody knew), 5 tier 3 (B-393 a test in no CI job for six months; B-394 a production-enabled feature no test environment turns on; B-396 the `&&` short-circuit; B-397 merge-with-no-CI; B-413 admin lists show deleted content), 19 tier 4. Amended: B-331 (live re-confirmation on all four sites + one wrong premise corrected), B-269, B-248, B-144 (→ split B-404), B-145, B-157 (eight verdicts), B-380, B-330, B-387. 6 coverage rows, **0 fully clean but three carrying large clean both-ways diffs**. Orchestrator live queries: 9 (feature flags, sentry/twilio/glitchtip ×4 sites, incident types, participant_form bodies ×4, soft-deleted-but-published by type, four allowed_values drift counts). Orchestrator independent reproductions: the full 65-test CI partition via `playwright test --list`; 302-file image byte-compare; the precache-revision control; the `preg_split` on an HTTPS remote. Counts after R34: 256 READY — T1 1 · T2 3 · T3 61 · T4 157 · untiered 34. Dry-stop counter: 0.**
📌 **R34 durable lessons.** (1) ⭐ **A CI log that starts mid-sentence is truncated, and absence from it proves nothing.** CircleCI keeps only the last 400,000 bytes of a step. The orchestrator nearly filed "a second SimpleTest class never runs" on exactly that reasoning, and B-269's own recorded evidence line rests on it (the finding survives on the code). **Read the START of a log before concluding from an absence.** (2) ⭐ **`[ci skip]` plus no required checks means the convention, not the pipeline, is what protects `develop`** — and it failed once already (B-395). A merged-untested PR is invisible until the post-merge build, which blocks nothing. (3) ⭐ **A test suite is a surface like any other and had never been audited.** Two of 65 e2e tests pass when the thing they check is absent, one has not run since March, and a test comment carried a false "FINDING" that a scout then repeated to me as fact. **Verify a comment that claims a finding exactly as hard as code.** (4) **A production feature flag that no test environment enables is a coverage hole nobody can see from the code** — `healthy_start` is on in Rwanda and off in every local and CI install, which is the environment three shipped defects came through. Size the FLAG against the test config, not just against production. (5) **Two scouts converging on one finding from opposite ends is the strongest signal in a sweep** — B-413 was found from the views exports and from the AQ CSV export independently. (6) **A scout's scope caveat can itself be wrong**: unit 6 discounted its own severity reasoning because "contrib source is not in the repository", while `server/hedley/modules/contrib/` is in the tree and unit 5 read it. Check a stated limitation before inheriting it.

## Round 35 (2026-09-21, @db0f901a9 — code identical to R33/R34's 3eef4b666, COVERAGE-SWEEP — the oldest rows never re-read at R25+ depth)
Units (5 worktree scouts, one wave; user approved the plan as proposed): (1) form completion count vs persisted payload, all forms today (R10 row, B-037 only); (2) edit/save round trip — `*FormWithDefault` hydration + `to*Value`, all forms (R12 row, clean); (3) Elm↔JS port contract (R10: 23+8 ports, now 34 declarations) + i18n interpolation mechanics after the #2238 fold (R12 row); (4) `hedley_stock_management` server + stock REST/sync arms (server last read R13; R27 was client-only); (5) `Pages/PatientRecord` (950 lines, only ever read inside the R23 app-shell unit). Out: everything R25–R34 read at depth; the vaccination engine; `hedley_migrate`; the admin/reports scripts family; TH/G tracks; all declined classes. Candidate ids start at B-418.
Frame: nothing code-side merged since R33; nothing deployed (`origin/main` 7ece1b2d0 v1.18.2, 532 behind, #2128 open); coverage table has no ☐ rows left. Queue at frame time: 257 READY. Dry-stop counter: 0.
- **Unit 4 (hedley_stock_management server) COMPLETE — ✅ CLEAN, no items** (scout 115k tokens / 5 min). B-177 amended: `stock_update` is a fourth member of the optional-on-server / `required`-in-decoder family (orchestrator re-read the three instance flags and the decoder). Live: one stock_update on all four sites. Coverage row added.
- **Unit 5 (Pages/PatientRecord) COMPLETE** (scout 113k tokens / 5 min) → **B-418** (MED-LOW, tier 3, DEPLOYED — the adult Family Planning pane never reads NCD family planning; orchestrator read pane, caller, fetch, issue #271, and counted 746 records / 545 women on ihangane). B-199 noted (a duplicate relationship shows a child twice). B-215 fix confirmed complete. Coverage row added.
- **Unit 3 (port contract + i18n mechanics) COMPLETE** (scout 201k tokens / 7 min) → **B-419** (LOW, tier 4, latent — deferred-photo IndexedDB reads have no `.catch`, so a rejected read leaves the photo lane Loading until reload; B-318's sibling), **B-420** (LOW, tier 4 — Somali stock history months fall back to English; the server copy was fixed in 2025-08, the client copy not). Port count unchanged since R10 (31). Orchestrator read both JS branches, the catch set, the Elm guard, and both Translate copies. Coverage row added.
- **Unit 1 (completion count vs persisted payload) COMPLETE** (scout 418k tokens / 11 min) → **B-421** (⭐ tier 1 — NCD pregnancy test keeps a hidden result when changed to "not performed"; the reader trusts the result before the note), **B-423** (tier 4 — stock batch number `Just ""` counts as filled). Coverage row added.
- **Unit 2 (edit/save round trip) COMPLETE** (scout 345k tokens / 11 min, scratch elm-tests through the real update) → **B-421 co-found independently**, **B-422** (⭐ tier 1, latent — HIV Diagnostics hydration hard-codes `testResultDirty = False`, so correcting Positive to "not run" still stores Positive and never closes the participant; refutes the R1-9 ledger's "unreachable"). B-283 amended (two more clearing lambdas; its fix shape cannot work because `ncdaFormWithDefault` refills at Save). Orchestrator read both mechanisms end to end and sized them live: ncd_pregnancy_test 757 / 8 with a result / 0 stale; ncd_hiv_test 1 `not-indicated`+`negative` on ihangane; hiv_diagnostics 30 total, none with a result. Coverage row added.
**⭐ R35 COMPLETE (2026-09-21): 6 new items B-418..B-423 — TWO tier 1 (B-421 NCD pregnancy status from a withdrawn test, co-found by two scouts; B-422 HIV Diagnostics correction never closes the participant, latent), 0 tier 2, 1 tier 3 (B-418 adult Family Planning pane omits NCD — 746 records / 545 women on ihangane), 3 tier 4 (B-419 photo lane no-reply, B-420 Somali stock months, B-423 stock batch number). Amended: B-177 (stock_update member), B-199 (child shown twice), B-283 (two lambdas + broken fix shape). 5 coverage rows, 1 ✅ CLEAN (hedley_stock_management server). Orchestrator live queries: 7 (stock per site, NCD family planning, NCD tests ×3 sites, node totals ×3). Counts after R35: 263 READY — T1 3 · T2 3 · T3 62 · T4 161 · untiered 34. Dry-stop counter: 0.**
📌 **R35 durable lessons.** (1) ⭐ **A form fix applied to the "universal" variant leaves its non-universal twin behind.** B-320 cleared stale results in the Prenatal lab forms; the NCD-only copies still refill them (B-421). When a fix lands in one of two sibling form families, grep the other. (2) **A hard-coded `xDirty = False` in hydration makes every clear last one message** — a mechanical scan for constant dirty flags found B-422 in one pass. (3) **"Unreachable" in an old ledger is a claim to re-test**: the R1-9 note on B-422's path was wrong. (4) **A pane built from an issue spec freezes the sources that existed that month** — NCD family planning arrived a month after the Patient Record pane and was never added (B-418).

## Round 36 (2026-09-22, @76e4d3e04 — code identical to R33–R35's 3eef4b666, COVERAGE-SWEEP — the last rows never re-read at R25+ depth: one Elm unit, three cross-side metric rows, one enrolment surface)
Units (5 worktree scouts, one wave; user approved the plan as proposed): (1) per-program `Backend/*/Update.elm` sub-updaters (17 files, 3,246 lines) + Msg types + dispatch + every close/end/discard path + Encoder⊇Decoder per patched entity (R24 unit 2 row; R27 said "NOT examined"); (2) NCDA per-indicator semantics cross-side — client Child Scoreboard ↔ `hedley_ncda.module` ↔ server/elm Scoreboard (R18 row; each side re-read alone since); (3) hedley_stats-fed dashboard metrics cross-side — ANC visit counters, SPV/CS feeds vs ECD, nutrition prevalence/incidence/z-score/MUAC pipeline, WC medication age bands (R19/R20 rows); (4) person search / relationship / group enrolment surface — Pages/People, Relationship, Participant(s), Clinics, Attendance, both EncounterParticipants pages, EducationSession, NextSteps, GroupEncounterTypes, Clinical, IndividualEncounterTypes + Backend/Relationship + PmtctParticipant + nodes.js search (R14 / R24 unit 5+7 rows); (5) client `expectActivity` vs the `hedley_reports.module` completion builders, re-diffed after the NCD/HIV/Prenatal merges, + the three R20 "never verified" ports + `hedley_ncd` server. Out: everything R25–R35 read at depth; the vaccination engine; `hedley_migrate`; the scripts family; TH/G tracks; declined classes; the five open PRs (#2262/#2264/#2266/#2269/#2237 — an "as merged" unit once they land); Prenatal DemographicsReport (302) + RecurrentEncounter (296). Candidate ids start at B-424.
Frame: nothing code-side merged since R33; nothing deployed (`origin/main` 7ece1b2d0 v1.18.2, #2128 open); coverage table has no ☐ rows. Queue at frame time: 263 READY — T1 3 · T2 3 · T3 62 · T4 161 · untiered 34. Dry-stop counter: 0.
- **Unit 1 (per-program sub-updaters) COMPLETE** (scout 265k tokens / 15 min; Opus after the Fable-limit restart) → **B-424** (MED-LOW, tier 3, DEPLOYED — the last lab result's save PATCHes the labs-results record twice and the second is built from the pre-save copy; orchestrator traced both generators, `patchNode`'s last-write-wins, every reader incl. the next encounter's pending-labs history, and fingerprinted 16 HIV records live). 174 `Save…` arms and every Encoder⊇Decoder pair mechanically clean. Coverage row added.
- **Unit 5 (expectActivity vs Completion builders) COMPLETE** (scout 340k tokens / 17 min) → **B-425** (LOW, tier 4 — referral row inverted for ARV/NCD programme enrolment; 31 speciality-care records live), **B-426** (LOW, tier 4 — vitals recheck expected at postpartum encounters; 8 live). ⭐ The hospital-referral port (token `4`) is verified for the first time; wire tokens for all five programs zero-mismatch. Coverage row added.
- **Unit 2 (NCDA cross-side) COMPLETE** (scout 287k tokens / 18 min) → **B-427** (MED-LOW, tier 3 — scorecard paints red X on months 0–5 of four rows the form never asks before six months; B-067's class), **B-428** (MED-LOW, tier 3 — aggregated % rows divide by all under-2s), **B-429** (MED-LOW, tier 3 — aggregated dose ladder rebuilt from dates; orchestrator counted 788 disagreeing child-vaccine pairs live: 179 more dates than max dose, 609 fewer). 20-indicator agreement table in the coverage row.
- **Unit 4 (person search / relationship / enrolment) COMPLETE** (scout 312k tokens / 18 min) → ⭐ **B-430** (MED, tier 3, DEPLOYED — nurse Clinics page never fetches today's sessions while the HC is `Uploading`, so the group button mints a second session per tap; orchestrator read fetch gate vs view gate, forget loop, sticky status, button default, revision path; live: 1,164 nurse clinic-days with >1 session, 582 same-device, 256 since 2025), **B-431** (LOW, tier 4 — auto check-in on enrolment always POSTs; 0 live dup pairs, 11 incl. swept ones in 2026). B-330 cross-linked. Observation: 113 persons on ihangane have no health centre (sharded nowhere).
- **Unit 3 (dashboard stats cross-side) COMPLETE** (scout 433k tokens / 22 min) → **B-432** (LOW, tier 4 — group-education attendee details cached once, never refreshed), **B-368(d)** appended (Resolved-Cases cards read the unfiltered list). Re-found and NOT re-filed: the Nutrition report's MUAC `<` — B-149(c) / ⛔ B-138 refuted. Closed the R31 `numberOfCephaly` open note as sound; `fd`-alias rule clean in hedley_stats.
**⭐ R36 COMPLETE (2026-09-22): 9 new items B-424..B-432 — 0 tier 1, 0 tier 2, 5 tier 3 (B-424 Prenatal recurrent labs record loses the last test and the next encounter asks again; B-427 scorecard false red X on months 0–5; B-428 aggregated scoreboard denominators; B-429 aggregated dose ladder, 788 live pairs; B-430 nurse group button mints a second session while uploading, 582 same-device days live), 4 tier 4 (B-425, B-426, B-431, B-432). Amended: B-368 (d), B-330 (sibling link). 5 coverage rows, 0 fully clean, every unit re-read at R25+ depth; the coverage table now has no row older than R25 that a later row does not supersede. Orchestrator live queries: 11 (labs gaps ×2, speciality care, postpartum BP, dose ladder, sessions ×3, attendance ×2, persons without HC). ⚠ All five scouts were first killed by the Fable usage limit mid-read and re-run on Opus with identical briefs. ⚠ `develop` moved during the round (nine backlog PRs merged 2026-09-22, incl. #2262 B-311, #2266 B-317, #2271 B-395) — the sweep is at 76e4d3e04; an "as merged" unit for those is R37's first pick. Counts after R36: 271 READY — T1 3 · T2 2 · T3 67 · T4 165 · untiered 34. Dry-stop counter: 0.**
📌 **R36 durable lessons.** (1) ⭐ **Two messages in one update that PATCH the same record are a lost update, not a no-op** — the second is built from the model the first has not yet changed, and `patchNode` re-reads the local row when it builds the upload change, so the later body wins everywhere (B-424). Grep for `++` joins of message generators that touch one entity. (2) **A fetch gate and a view gate on the same sync status must agree** — where the view shows content the fetch must run, or a page reads "not loaded" as "does not exist" (B-430; B-330 was the empty-fetch twin). (3) **A cross-side agreement table is the deliverable even when each side was recently read alone** — R26/R28/R31 each read one NCDA side clean; the divergences (B-428, B-429) only appear when the three are laid side by side. (4) **Sizing a duplicate needs the soft-deleted rows** — the daily sweep leaves 0 live attendance duplicates; the 11 swept ones are the rate (B-431).
