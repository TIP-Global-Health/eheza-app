## Seeded from rounds 1–9 ledger (2026-07-02) — pre-regime items, verification labels per entry

## Round 10 — 2026-07-03 — lenses: Fetch wiring / server deleted-filter / completion-vs-save / port contract / numeric input
All findings below SELF-VERIFIED on develop @4e37b5a69 unless noted. (Agents initially read a stale tree — user switched the shared checkout to r8-2 mid-run — so every fact was re-verified on develop; line numbers below are develop's.)

## Round 12 — 2026-07-03 — lenses: AQ worker idempotency / backend presave integrity / clinical diagnosis engines / edit-loss save-noop / i18n interpolation mechanics
(agents landing; verified findings appended here)

## Round 15 — 2026-07-03 — COVERAGE SWEEP (AcuteIllness / WellChild+NCD / Nutrition+Dashboard / SyncManager / PWA-SW)

## Round 14 — 2026-07-03 — COVERAGE SWEEP (Prenatal / FamNut+HomeVisit / ChildScoreboard+NCDA / Person+relationships / backend infra)

## Round 13 — 2026-07-03 — COVERAGE SWEEP mode (unit audits: stock / group-ed / TB / HIV / Measurement.Utils)

## Round 11 — 2026-07-03 — lenses: restful join-shape / stats SQL math / login-session state / Dexie schema / visit scheduling
Findings self-verified on develop (spot-checked; the Dexie agent itself re-verified against the main checkout on develop after its worktree was GC'd).

## Round 16 + conversion pass — 2026-07-12 — develop @2c0322070 (small-targeted-fixes-only constraint in force; NO architecture changes)
**Status: PARTIAL — 4 of 8 agents delivered (fetch-wiring lens, minors re-verification, a11y inventory, 3 test-survey designs); the other 6 (hedley_migrate, hedley_ncda, dead-code, guard-tooling, revivals items 1/3-6, test-hardening remainder ×2) were killed by a session limit and RESUMED 2026-07-12 — their entries land in a follow-up append. Do not consider R16 closed.**

## Round 17 findings (2026-08-03, @c37f2fe3c)

## Round 18 findings (2026-08-04, @c37f2fe3c)

## Round 19 findings (2026-08-11, @c8a03349f — develop after #2063/#2061/#2065 merges)

## Round 20 findings (2026-08-17, @develop post-#2093; wave 2 = parser-default + generated-artifact lenses)

## Round 21 findings (2026-08-18, DATA-INTEGRITY mode — ihangane live copy snapshot 2026-08-05, local DDEV)

## Round 22 (2026-08-19, DUPLICATION-ELIMINATION mode per approved plan; find-then-fix-on-approval)

### Round-10 minors (recorded, not queued)
- AcuteIllness symptom-duration inputs: `String.toInt "" → Maybe.withDefault noChange` (`Pages/AcuteIllness/Activity/Update.elm:186-203` ×3) keeps the previous Dict value and the box snaps back mid-edit (visible, self-correcting; EDD inputs show the correct empty-clear pattern). Polish only.
- District/province enumeration for large-dataset reports scans raw locality field tables with no bundle/deleted filter (`hedley_reports.module` ~:2278-2283 + `recalculate-large-datasets.php` ~:42-45) → junk/empty `report_data` nodes for localities existing only on deleted persons; inner data queries filter correctly so no wrong numbers. LOW (agent-verified, spot-checked only).

### Round-15 notes
- **WellChild + NCD workflow/persistence = CLEAN except B-072.** WellChild task-gating/immunisation/medication/next-steps/newborn-branching clean; NCD task-gating/initial-recurrent-split (phase-partitioned medicate/refer + shared measurement field-preservation via `or`)/medication-distribution/referral clean. MINOR (inert): WellChild HeadCircumference REST plugin (`HedleyRestfulWellChildHeadCircumference.class.php`) omits `field_zscore_age` from `$fields` though it's computed server-side (`hedley_activity.module:328-331`) + exposed by the Height/Weight sibling plugins — INERT: client never sends/decodes zscores (computes live via ZScore module), reports read field_zscore_age via direct SQL, so no consumer; consistency-only fix.
- **AcuteIllness workflow/persistence sweep = CLEAN except B-070.** Task-gating (R6 covidTesting asymmetry re-confirmed inert), multi-visit episode, contacts-tracing lifecycle (re-save guarded by `disabled = isJust measurements.contactsTracing`), medication accounting, next-steps/referral, per-form count-vs-save (malaria round-trip clean) all audited clean. SetHealthEducation→isolationForm misroute (R8) re-confirmed unreachable.
- **Dashboard sweep = CLEAN except B-069** (+ B-009 NaN class re-noted at a new line `viewGoodNutrition:1451-1454`, same recorded class). Case-mgmt totals, follow-up totals (counted paths), period/month selection, village-vs-HC scoping, stats consumption all clean; B-054 filterResolvedFollowUps inversion is elsewhere, unchanged here.
- **Nutrition (individual) workflow/count-vs-save = CLEAN.**
- **SyncManager orchestration (non-poison) = CLEAN except B-071 + one cosmetic minor.** Programmatic cross-check of all 206 authority + 8 general entity variants → ZERO upload/download type-string mismatches, ZERO dup type strings (no entity mis-decodes to wrong variant/shard). Cursor bookkeeping (max-revision-over-saved, save-success-gated, can't advance past unsaved), state-transition order (FK-safe upload sequence, no skip/loop), save-result handling, upload-lane deletions (correct localId sources), timers all CLEAN.
- **PWA service-worker / offline-request layer = CLEAN** (zero new bugs). Request routing (type/uuid/method/param branches, no cross-file regex overlap), offline write path (POST uuid/type/shard, PATCH full-node echo via sendRevisedNode + change recording), cache correctness, SW lifecycle (SkipWaiting/clientsClaim/controllerchange/sendRevisions), response construction — all audited clean. Ruled-out-as-inert: `deleteNode` records no change (DEAD — no DELETE issuer; deletions flow via PATCH deleted=true), object-key `where({type,nurse})`/`{type,participating_patients}` (Dexie 3.2.7 falls back to type index + in-memory filter, no SchemaError), photo cache-key asymmetry (by-design, display URLs token-less = stripped key), makeUuid statusText/content-type typos (cosmetic, read via .text()). Recorded items re-confirmed present (B-002/006/035/040, R8-3/B-039 deleted-filters).
- MINOR (cosmetic, self-healing): `SyncManager/Update.elm:1274` entering SyncUploadAuthority sends the PRE-update zipper (`sendSyncInfoAuthoritiesCmd zipper` vs the surrounding pattern's `zipperUpdated`, e.g. :142) → model vs localStorage disagree on the current authority's `status` label only (lastFetchedRevisionId/remaining/hash identical); next handler re-sends corrected status. No cursor/data risk. Fix: send `zipperUpdated`.

### Round-14 minors (batch 2)
- Scorecard backend `continue` on empty Vitamin A field can skip MUAC/stunting recording (`hedley_ncda.module:804-852` — `if (!$items) continue;` also skips the MUAC/stunting block below it). LATENT/masked: Vitamin A is a required task on the scoreboard NCDA so a completed save always has field_receive_option. Fragile coupling; fix = move the MUAC/stunting block out from under the receive_option `continue`. LOW.
- FamilyNutrition/HomeVisit `EatenWithWater` feeding question is computed then discarded (`Measurement/View.elm:3885` — second tuple bound to `_`), so it's never rendered/settable. INERT: no consumer reads EatenWithWater; `ifNullableTrue EatenWithWater Nothing = Just EverySet.empty` so save is unaffected; longstanding (pre-2024 elm-review autofix). Not action-required.
- Demographics-report 50yr age-band gap (`hedley_admin/scripts/bootstrap-demographics-report.SQL`: lt50y `+50yr > :limit` + mt50y `+50yr < :limit` → someone exactly 50.0y at :limit matches NEITHER band, dropped from total). ⚠ GATED-ON-LIVENESS: this is the `hedley_admin/scripts/*-report` family that R5 established is largely UNUSED (R5-4/R5-5 precedent) — confirm the demographics report is actually run before treating as actionable. Fix if live: make mt50y `<=`. LOW.

### Round-14 notes
- **Prenatal workflow/persistence sweep = CLEAN except a re-find of B-037** (dup, NOT a new item — corroborated by a 2nd agent). The agent framed it as "new / inverse of B-037" but that's a misread: B-037 IS the count(4)≠map5-requires(5) mismatch at `View.elm:3104-3108` vs `Utils.elm:4284-4291`; R9-1/#1877 touched the ENCODER (`encodeMedicalHistoryValue`→JSON), a different function, so it did NOT "flip" anything — `toMedicalHistoryValue`'s `Maybe.map5` has always required all 5 fields. Fix unchanged (add `maybeToBoolTask form.preeclampsiaInFamily`). All other Prenatal sub-areas (labs lifecycle incl. R4-1 class, task-gating, recurrent split, EGA/EDD threading, outcome/close, obstetric×2/social/followUp/ultrasound/LMP/symptomReview count-vs-save) audited CLEAN.

### Round-14 minors
- `hedley_device_assign_shards` education_session EDIT branch leaves `$shards` undefined (`hedley_device.module:307-316`) → PHP notice only; `empty()` returns cleanly + edits intentionally don't reassign shards. Non-bug (notice hygiene).
- Ops-doc drift: CLAUDE.md names the flag `stock_management`, but code reads `hedley_admin_feature_stock_management_hc/_village_enabled` (`hedley_admin.module:646-647`, matching Elm `FeatureStockManagementHC/Village`). Code is internally consistent — only the doc/flag-name would mislead an operator running `drush vset`. Doc fix, not code.

### Round-13 minors
- **HIV `testResultDirty` recorded rationale CORRECTED** (Round-6 runner-up): the ledger said "UNREACHABLE (result picker only emits Just)" — WRONG. `testResult` DOES reach `Nothing` via the resultPositive/runHIVTest bool toggles + end-encounter revert (`Pages/HIV/Activity/Utils.elm:328-332` hardcodes `testResultDirty=False`), so a stale saved `TestPositive` CAN be resurrected into the form. But it's genuinely INERT: no Elm decision logic and no backend report/stat reads `hiv_diagnostics.field_test_result` (report hits at hedley_reports.module:6913+ are prenatal/NCD hiv_test/pregnancy/syphilis; hiv_diagnostics :7249/7310/7325 are presence-only). Fix if ever touched: `testResultDirty = form.testResultDirty`. Still not worth a PR (reachable-but-inert, like SetHealthEducation dead-code).
- HIV clinician-gated observations (noted, not bugs): prophylaxis-only prescription (Bactrim/Co-Trimoxazole, no ARV) can't complete the Medication task (`recordMedicationsFormAndTasks` mandatory group = ARVs only) — likely intended (ARV always required); treatment-review becomes mandatory on the initial encounter after prescribing (`expectMedicationTask TaskTreatmentReview`) — plausibly intended for transfer-in patients already on ARV. Both clinician-gated, no code change without sign-off.
- HIV Progress Report is STUBBED — the report link (`Pages/HIV/Encounter/View.elm:124-131`) is commented `@todo`; no ProgressReport module exists. Incomplete feature, not a bug.
- Group-nutrition `SaveMotherFbf` copy-paste (`Backend/Session/Update.elm:153` reads `saveLactationRequest` tracker) — INERT: `save*Request` trackers are write-only (grep: no reader outside Session/{Update,Model}.elm). No behavioral impact.
- **`"wight-loss"` misspelling** in the TB + HIV symptom enum (`Backend/Measurement/Utils.elm:4137/4155/4515/4569`, `hedley_tuberculosis...field_base.inc:210`, `hedley_hiv...field_base.inc:135`) — INERT: symmetric write+read on both client AND server, no correctly-spelled consumer (the `"weight-loss"` at `Pages/Tuberculosis/Activity/View.elm:410` is a DOM input id, not a value compare; verified). Data-hygiene only; renaming would need a data migration of stored values so NOT worth touching. Recorded so a future dev doesn't "fix" the spelling on one side and break the round-trip.
- Stock HC-level view (`Backend/StockUpdate/Utils.elm:15-42` `generateStockManagementData`) counts ALL `measurements.stockUpdate` as receipts (incl. village-tagged, since village saves set `shard = Just healthCenterId`) but its distributions come only from group childFbf/motherFbf — village `AhezaChild/AhezaMother` consumption isn't in StockManagementMeasurements → if a site runs village stock alongside HC stock, HC balance can OVERSTATE. LOW / MED-low confidence (semantics-dependent — moot if stock updates always have `village==Nothing` at HC-run sites). Verify intended HC-vs-village stock semantics with the team before acting.
- `viewNumberInput` (`Pages/Utils.elm:704-709`) sets only HTML `min="0"`; `String.toInt "-5"` parses → a hand-typed negative receive quantity isn't blocked client-side (generic, B-016-adjacent, not stock-specific).

### Round-12 minors
- **Person shards NOT recomputed on health-center change** (`hedley_device_assign_shards` short-circuits on non-empty field_shards; `handleChanges` doesn't ignore person.health_center): a person whose HC changes keeps `field_shards=[old HC]` → syncs to the wrong HC. NOT reachable by the official client (EditPerson re-sends the ORIGINAL HC via applyDefaultValuesForPerson; backend comments confirm HC is effectively immutable). Defense-in-depth only — a future importer/client that moves a person would silently mis-shard them + all their data.
- AQ dedup "stuck task" check (`hedley_general.module:75-80`) compares `time() - $task->created` (ENQUEUE time, not processing-start) vs 20min → a task backlogged >20min in QUEUED is judged "stuck" the instant it starts, allowing a duplicate enqueue. Harmless (recompute workers idempotent) → herd/wasted work only. LOW.
- view_export returns FAILURE (never FAILURE_RETRY) if the final-batch `drupal_mail` fails (`:268-271`) → a fully-built CSV is abandoned on a transient SMTP hiccup, user never emailed, cron deletes it after 7 days. LOW (email, not data).
- super_user_mode hard-delete gap: none of hedley_ncda/reports/stats implement `hook_node_delete`, so a hard-deleted measurement (only possible under `hedley_super_user_mode`) leaves person `field_ncda_data`/`field_reports_data` + HC stats counting a gone node until the next measurement. LOW (rare admin escape hatch).
- `Translate.elm:13034` Kirundi `MissedDosesOfMedication`: `... ++ String.fromInt val ++ "y'imiti"` missing the leading space → renders "3y'imiti" (number correct, cosmetic spacing only; Kinyarwanda sibling has the space). Trivial 1-char fix; fold into any future i18n touch. (Same benign unit-abutting pattern in memory/storage-quota fields 12910/12912/26291/26292 — consistent, not defects.)

### Round-11 minors
- Case-management "month 13" cutoff (`hedley_stats.module:3195`) `strtotime("last day of -1 year") + $one_day` isn't midnight-anchored relative to measurement time-of-day → measurements dated exactly on the window's first day can mis-bucket into the hidden baseline month. LOW, systematic-but-tiny (only bites if measurement dates carry time-of-day).
- `HedleyRestfulEducationSession.class.php:108` copy-paste `unset(uuid_individual_participant)` instead of `uuid_village_ref` → raw column leaks into payload (client ignores). Cosmetic; fold into B-046.

### Round-10 minors (batch 2)
- Prenatal `toMedicationValue` syphilis guard omits `syphilisAdverseEventsHospitalization` from its `List.all isNothing` check (`Pages/Prenatal/Activity/Utils.elm:4496`) while hypertension/malaria/anemia guards include theirs — currently UNREACHABLE via UI (hospitalization input only shows after adverseEvents==Just True, and the update func clears it on change); latent copy-paste asymmetry, fix opportunistically.

### Minors PROMOTED to queue — B-075..B-091 (2026-07-12). All 23 recorded minors re-verified at 2c0322070: 23 STILL-VALID, 0 fixed, none touched by #1889–#1917. Full mechanism detail stays in the original minor bullets above; entries below carry re-verified anchors + fix. NOT promoted (stand as recorded): eatenWithWater dead-value (product), HIV testResultDirty (inert), HIV clinician-gated observations, HIV ProgressReport stub, "wight-loss" tombstone (do-not-fix), EducationSession unset (rides B-046).

### A11y batch — B-092..B-095 (2026-07-12 full inventory at 2c0322070: 40 `img` sites — only 2 with alt, both in `Utils/Html.elm` :101/:162; 72 icon-only `link-back`/`icon-back` controls; 13 other icon-only controls; ZERO `aria-label`/`title` anywhere in Elm views). Rules: decorative icons (exclamation-*, timeline dots, chevrons) get `alt ""`; content images need REAL alt (WellChild ProgressReport patient photo `View.elm:1529`, StockManagement signature `View.elm:817`, Person avatar `View.elm:498`, shared `Pages/Utils.elm:1249 viewPhotoThumb`); icon-only interactive controls get `aria-label` (attribute helper + Translate entries, English-first per policy). Site lists re-derivable by grep (`img [`, `"link-back"`, `"icon-back"`); full per-file inventory in the R16 a11y agent report (session 5c144238, 2026-07-12). Zero-logic-risk markup additions.
### Test-hardening track (TH-track; ZERO production risk — tests only; fold into EXISTING specs per the CI-budget rule). Cross-cutting assertion pattern verified: `customButton` (`Pages/Utils.elm:1318-1335`) emits `classList [("active", active), ("disabled", not active)]` and attaches onClick ONLY when active → negative assert = `toHaveClass(/disabled/)` + `not.toHaveClass(/active/)`, flips after answering. More TH-items incoming from resumed agents (prenatal reference, acute-illness, nutrition, well-child, ncd, hiv, tb, stock-management, lab-tech + engine unit-pinning).
### TH-track batch 2 (2026-07-12, from the track-parent agent). ⚠ CROSS-CUTTING: TWO Save-button DOM variants at 2c0322070 — (1) `customButton` (`Pages/Utils.elm:1318-1335`; toggles active/disabled, onClick only when active; used by Prenatal/StockManagement/lab-results) and (2) AcuteIllness INLINE variant (`AcuteIllness/Activity/View.elm:516-522/996-1002/1514-1520`; toggles ONLY `disabled`, never `active`, and onClick STAYS ATTACHED when disabled — only Semantic UI pointer-events:none blocks real clicks; a `force:true` test click on a "disabled" AI Save would persist an incomplete form). Legacy group-form variant (`Measurement/View.elm:587`, id="save-form") is also disabled-only. Universal recipe: negative = `toHaveClass(/\bdisabled\b/)`, positive = `not.toHaveClass(/\bdisabled\b/)` (+ `/\bactive\b/` only for customButton). Part-B context: NCD staging is ALREADY fully pinned (`Pages/NCD/Test.elm`, 553 lines) — no NCD pin issue; big existing suites: AcuteIllness Test.elm 920 lines (initial-encounter paths), Prenatal/Activity/Test.elm 1336 lines (staging + lab diagnoses), Backend/NutritionEncounter/Test.elm 336. Recommended order: TH-00 → TH-13 → TH-19 → TH-14 → TH-18 → TH-15 → TH-11 → TH-16 → TH-17 → (TH-12 upgrade). First-week batch: TH-00 + TH-13 + TH-14 + TH-19 (<10s added CI wall-time total).
### Guard-tooling track (G-track; CI/lint guards, ZERO production-code risk). Verified 2026-07-12 at 2c0322070: elm-review already fully wired in CI (`lint_elm_review` job, `.circleci/config.yml:9,54-60`; jfmengels/elm-review 2.16.3; `client/review/src/` has ReviewConfig only — custom rules drop straight in). NoDebug.Log + NoDebug.TodoOrToString ALREADY ACTIVE (`ReviewConfig.elm:64-72`) → a "Debug guard" is MOOT. New cheap guards slot into ONE new `lint_guards` CI job (cimg/python, `ci-scripts/test_guards.sh` dispatcher, <1 min). Delivery order: G-01+G-02+G-03 as the first PR, then G-04, G-05 (needs curation), G-06, G-07. Rejected as infeasible/moot: decoder/encoder/Revision parity guard (Elm exhaustiveness already covers it — residual risk is exactly the wildcard sites, i.e. G-01), blanket kirundi/somali coverage (Nothing is intentional for units — curated Tier-1 is the right shape).
### Ledger-revivals verification (2026-07-12, @2c0322070) — 2 queued, 2 already-fixed, 1 moot, 1 recorded-only
- ✅ ALREADY FIXED, do not build: (a) Dashboard decoder tolerant-decode follow-up — PR #1834's idiom now covers all list/set sites (`Backend/Dashboard/Decoder.elm:319/:352/:394` use decodeListDroppingUnknown; decodeEverySet itself is element-dropping at `Utils/Json.elm:60`); remaining decodeWithFallback sites are SCALAR defaults (:112 visible "X" sentinel, :281/:282/:323 enum/seq defaults) — different class, LOW, no action. (b) persistentStorage result — fully plumbed since commit f7da0a046 (app.js:485-487 → port → `App/Model.elm:133` → Device page :89); only a missing `.catch()` remains, negligible.
- 🚫 MOOT: `fix-well-child-encounters-warnings.php` — retired one-shot drush script (CLI-guarded, zero references anywhere); its unset()/double-wrap flaw is dormant. Optional: delete the script (fold into dead-code batch).
- 📓 RECORDED-ONLY (not queued; fix would be a behavior change → fails the high-certainty constraint): boolean `deleted` decode inconsistency — 14 decoders use fail-open `decodeWithFallback False bool` (resurrect direction) vs 6 strict `bool` (PmtctParticipant/IndividualEncounterParticipant/HealthCenter/FamilyEncounterParticipant/Relationship/Person); backend-controlled field, improbable trigger. Full 14-site list in the R16 revivals report (session 5c144238).

### R16 hedley_ncda audit findings (2026-07-12, @2c0322070) — B-098..B-101 + B-078 enrichment above
### R16 dead-code batch (2026-07-12, @2c0322070) — B-102..B-107, all PROVEN dead (every discovery channel checked: Drupal hook/theme/ctools/RESTful auto-discovery, features strings, JS↔Elm ports, gulp/npm/CI/ddev/Robo/docs CLI channels, dynamic gulp-load-plugins surface, dynamic Drupal variable names). ⚠ REFUTED, DO NOT DELETE: CounselingSessions RESTful plugin (the old "dead code" note is wrong — it's in HEDLEY_RESTFUL_SHARDED `hedley_restful.module:88`, serves legacy counseling_session nodes on full-sync; Elm+sw.js+migration still fully wired; only the client UI ACTIVITY is retired). Also NOT queueable: syncmetadata plumbing (old-device IndexedDB migration behavior), gh-pages `deploy` task cluster (documented README channel, PLAUSIBLE only), AllDict full remove-machinery prune (would kill the remaining 6 Debug.todo and unlock --optimize, but needs an `update` behavior narrowing = refactor; see [[client-cannot-use-elm-optimize]]).
### R16 ncda minors (recorded, not queued)
- `hedley_ncda_get_triggering_measurement_types()` (:1198-1215) includes well_child_mebendezole / well_child_vitamin_a / well_child_ecd which are loaded (:464-466) but classify into NO bucket (saved from the pregnancy-summary else-branch only by the field_weight guard — those bundles have none) → their data never reaches the aggregated scorecard, BY PARITY with the client scorecard (rows derive exclusively from CHW questionnaires, `Pages/WellChild/ProgressReport/View.elm:2484-2560`; the client-side divergence is B-067). Net effect: pointless AQ recalcs + node loads + misleading comment. Efficiency cleanup candidate, NOT a bug fix.

### R16 hedley_migrate audit findings (2026-07-12, @2c0322070) — B-108..B-112. Liveness frame: migrations run at ddev restart/CI/`./install -d` — never production runtime; severities env-capped.
### R16 migrate minors (recorded, not queued)
- `hedley_migrate.drush.inc:8` legacy `create-dummy-nurses-devices` hardcodes HC nid 7089 (dangling on fresh installs) + writes pins.txt/pairing_codes.txt to CWD — legacy one-off tooling, not queued.
- Prior "empty required dates silently coerced to ''" concern CLEARED empirically: empty optional dates produce NO field row (intended semantics); Migrate never validates requiredness.

### TH-track additions from the R18 E2E coverage map (TH-20..TH-27; full detail in the audit — key facts inlined):
- **TH-20** ⭐ must-NOT-save AI incomplete form: extend acute-illness nurse test 1 — fill 1 of 3 symptom tasks, assert save `disabled`, `dispatchEvent('click')`, assert still on activity + no symptoms node. LANDS WITH B-147's fix (fails today — that's the point). +~20s.
- **TH-21** make the NCD recurrent lab-results test falsifiable (today its Save is inside `if(isVisible)` with `ncd_labs_results` never asserted — spec defect D1, also nullifies b031's regression signal). +~10s.
- **TH-22** site-aware gate helpers: derive bad/good MUAC from the site (today 125/14 are wrong-by-construction on Burundi; CI is rwanda-always). Step 2 (separate decision): a burundi e2e job ≈ +25min CI.
- **TH-23** un-ended-encounter integrity: `endPrenatalEncounter` must assert the encounter page went hidden (spec defect D2 — a never-ending encounter looks ended); make the fundal-height/birth-weight gate checks unconditional (D3 — they vanish silently if the input stops rendering). ~0 cost.
- **TH-24** double-tap duplicate guard (delta b003): dblclick a save, assert exactly ONE node; needs a `countMeasurementNodes` drush helper (booleans-only today). +~15s.
- **TH-25** first-ever task-counter assertion (`expectTasksCount` helper; delta 42fc8ef93 heart-murmur): N-1/N → N/N around the question, save active only after. +~10s.
- **TH-26** stock form reset on re-entry (delta b004). +~20s.
- **TH-27** negative-input guards (delta b084): type `-5`, assert minus stripped, nothing negative saveable. ~0 cost.
- Spec-defect cleanups to fold in: D4 group-session blind `3000` fill + force-clicked Save whose gate popup isn't in the dismissal set (false-pass + latent flake); D5 hand-rolled gate checks weaker than shared helper; D6 list (lab-tech `toBe(false)`-on-error, reporting baseline `??0` degradations, participant2 never asserted, CSV button-visible-only).
- Suite facts worth keeping: ZERO task-counter assertions suite-wide; force-click on a disabled button of EITHER variant is a silent no-op (pointer-events hit-test) — the AI hazard is keyboard, not force-click; per-test overhead ~60-90s so extending beats new tests 10-50×; suite is rwanda-only by construction.

### TH/G tracks SCOPED at HEAD (R20 wave 3, 2026-08-18, @9dc7dd787) — implementation-ready ordered plan; NOTHING mooted, 3 premises stale
- **Stale premises — do NOT build as recorded:** (1) TH-20's "fails today" demonstrator — B-147 fixed by #2073 (fb8581fb6): the AI inline Save variant is GONE, all 13 AI+Prenatal saves ride `saveButton`/`customButton`; build TH-20 as a REGRESSION PIN (disabled + dispatchEvent no-op + no node). (2) TH-08's numbers — B-072 fixed (#1952), heartMurmur counted at `NCD/Activity/Utils.elm:304`, CoreExam **N=8** now; design (omit head-hair) intact. (3) TH-27 — B-084 fixed (`dropLeadingMinus` at `Pages/Utils.elm:722/:741`); build as regression pin. Cross-cutting: the "TWO Save-button DOM variants" recipe is stale — remaining variants are customButton (active/disabled) and legacy group `#save-form` (`Measurement/View.elm:636/:1911`, disabled-only); TH-00's docstring documents THIS landscape.
- **Shrunk:** TH-18 (hypertension hierarchy partially pinned via recheck tests `Prenatal/Activity/Test.elm:1183+`; emergency-referral aggregation still zero pins), TH-19 (MUAC describe exists at 11.0/12.0/13.0 — the ==11.5/==12.5 boundaries, MUAC+Edema combo, over-6mo asymmetry still un-pinned).
- **All else STILL-VALID** (TH-00..TH-27, D1-D6, G-01..G-07 premises re-verified individually; anchors in the wave-3 report). Notables: TH-21/D1 correction — `ncd_labs_results` IS in `queryNCDNodes`' known list (`ncd.ts:853`); the SPEC's expectedTypes omits it. TH-23/D2 — `endPrenatalEncounter` (`prenatal.ts:120-137`) still never asserts departure. G-02's site list GREW: 4× elm.json + `install_client.sh:13-14` + `test_elm.sh:17-18` + `install_elm_review.sh:12`, incl. elm-test 0.19.2-0 pins. G-04's partition anchors moved: `.circleci/config.yml:143/:200/:257`. ⚠ Implementers must RE-DERIVE task counts N per measurement form — the #2007 range work touched them; recorded Ns were re-derived only for TH-08/TH-13.
- **CI budget:** whole plan except TH-22-step-2 adds ~2-3 min spread over the two e2e shards + one new <1-min parallel `lint_guards` job — within the 25-min goal. TH-22 step 2 (Burundi job) = +25-30 min wall, gated on user.
- **ORDERED PLAN:** PR1 = G-01+G-02+G-03 (lint_guards job + `ci-scripts/test_guards.sh` dispatcher; bundle re-verified). PR2 = release-signal e2e: TH-00 + TH-20 + TH-21(D1) + TH-23(D2+D3) + TH-27 (~+35s). PR3 = unit pins TH-16/17/18/19 (elm-test only). PR4 = G-04 partition guard (BEFORE the fold-in wave). PR5 = fold-ins TH-13(gated Q1)+TH-14+TH-15+TH-08. PR6/7 = fold-in batches by shard (TH-02/04/05/10/12 | TH-01/03/06/07/09/11). PR8 = TH-25+TH-24+TH-26+D4+D6 (D5 only on Q4-yes). PR9 = G-06 (after e2e wave). Then TH-22 step 1; G-05 on curation; G-07 last if wanted.
- **USER QUESTIONS (blocking the marked items only):** Q1 TH-13/fold-in class — does starting the TH track supersede the 2026-07-05 "at this stage" decline of the B-037 e2e expansion? Q2 TH-22 step 2 — approve a Burundi e2e job (+25-30 min) or stay rwanda-only with site-aware helpers? Q3 G-05 — draft the Tier-1 clinical key list for sign-off, and who sources kirundi/somali text? Q4 D5 — consolidate the 6 hand-rolled save helpers (test churn) or skip? Q5 G-07 — want the alphabetical ratchet at all before September?

### R20 wave-3 batch — Completion report vs client `expectActivity` (Prenatal/AI/NCD/HIV/TB compared; the R19 Unit-4 pickup). ⚠ WHOLE BATCH GATED on one user answer: is the Completion admin report actually used (and how/when are the `completion-generate-*.php` scripts run)? Data exists on live so the pipeline has run, but per the B-142 lesson liveness is the user's call — a metric nobody reads makes these moot. Anchors: PHP = hedley_reports.module, CLI = client Pages/<X>, DEC = server/elm Backend/Completion/Utils.elm. All orchestrator-verified at anchors 2026-08-18 except where noted.

### R21 wave C census + clean verdicts (recorded facts, no further items)
- **Structural duplicates CLEAN**: zero undeleted duplicates per (individual encounter, type) across all 10 encounter kinds and per (session, person, type) for groups. Raw duplicates exist but every extra copy is `field_deleted=1` — the manual `delete-duplicate-measurements.php` ops cleanup is CURRENT (keeps highest-vid = exactly the copy `decodeHead` shows since nodes.js sorts vid DESC; double-POST pairs still minted, latest 2026-04-18). 📌 Data-evidence that at least ONE hedley_admin script IS actively run — consistent with the family rule (liveness = user's call, per-script).
- **Unit-confusion damage census (B-122 class; writer open on DEPLOYED, closed by unreleased #2007 — these columns are the running cost of not shipping):** NCDA birth weights kg-typed in grams field 1,821/14,076 (group_ncda 34.5%, well_child_ncda 25.2%; 732 in 2025 + 101 in 2026); pregnancy-summary birth weight **214/324 = 66% kg-typed**; grams-typed child weights in kg fields 2,617 (1,485 in 2025-26); **mm-typed MUAC 2,036 — acute-illness MUAC alone 1,721 = 13.8% of the bundle** (⚠ consumer: `muacIndicationForChild` red ≤11.5cm, so a 125 "mm" reads GREEN → SAM missed on those rows); metre-typed mother heights 432 (118 in 2025). Repair of historical values = clinically sensitive product decision, NOT proposed; census feeds the release case + any future repair decision.
- **Clinical-plausibility census** (descriptive, for clinicians — full table + samples in scratchpad/r21-waveC-checks.md): temperature ×10-typed ~730 rows; BP cmHg-typed ("12/7") 614 rows (92% 2020-21) + 288 sys<dia inversions; child weights 60-200kg INSIDE the 0.5-200 develop gate = 1,456 rows (gate-width evidence for B-029(b)); fundal-height zeros 3,363 declining (develop min=1 will block); Apgar >10 in ~25-32/129; EGA-at-birth >45wk 21/597.
- **Other invariants**: future-dated measurements 0 (CLEAN); before-birth measurements ~570 (device-clock clusters; z-score returns Nothing on negative age — TOLERATED); prenatal measurement-vs-encounter drift 24.6k rows >3d (reopened-encounter workflow — CENSUS, pairs with B-194); vaccination doses-vs-dates zip 4 legacy nodes (TOLERATED); zero/negative anthropometrics ≤12/family.

## Round 23 (2026-08-24, @c734bd4ab, COVERAGE-SWEEP — units never given a coverage row)
Units (8 worktree scouts, one wave): (1) Prenatal ProgressReport + shared Pages/ProgressReport; (2) WellChild + Nutrition + ChildScoreboard ProgressReport; (3) AcuteIllness + NCD + TB + FamilyNutrition ProgressReport; (4) hedley_prenatal + hedley_acute_illness; (5) hedley_well_child + nutrition + family_nutrition + hiv + tuberculosis + group_education + resilience + forms; (6) hedley_patient + hedley_person (consolidation); (7) Backend/Update.elm (write path, 10.5k); (8) App shell (App/Update+View, Router, Page, PatientRecord/NextSteps/Attendance/MyAccount) + photos/bulkPhotos/photoCache/lifecycle/config JS. Candidate ids start at B-210.
- **Unit 1 (Prenatal ProgressReport) COMPLETE** → B-210 (MED, tier 3), B-211 (LOW-MED, tier 4), B-212 (MED, tier 3); all three orchestrator-verified at anchor (form list vs guard list; sentinel; ARV/NCD key). Coverage row added. Units 2-8 in flight as of this write — if the session cap kills them, resume via SendMessage (context intact) rather than relaunching.
- **Unit 2 (child ProgressReports) COMPLETE** → B-213 (⭐ HIGH, tier 2 — scorecard z-scores at today's age; identical line on origin/main so LIVE), B-214 (MED, tier 3). Coverage row added.
- **Unit 4 (hedley_prenatal + hedley_acute_illness) COMPLETE — ✅ CLEAN** (Features containers; only `populate-edd.php` executes, already covered). Coverage row added.
- **Unit 6 (hedley_patient + hedley_person) COMPLETE** → B-216 (MED-HIGH server-stats, tier 3), B-217 (guard, tier 3), B-218 (tier 4), B-219 (tier 4, with the CHW-`imported` tail note). Coverage row added.
- **Unit 8 (App shell + small JS) COMPLETE** → B-215 (MED, tier 3; identical on origin/main). Coverage row added.
- **Unit 3 (AI/NCD/TB/FamNut ProgressReports) COMPLETE** → B-220 (⭐ HIGH, tier 2 — AI report drops the current encounter; `git show 04e4bd9a8` diff verified; live since v1.14.0), B-224 (PLAUSIBLE, tier 4). Coverage row added.
- **Unit 7 (Backend/Update.elm) COMPLETE** → B-221 (MED, tier 3), B-222 (tier 4), B-223 (minors ×2, tier 4). Coverage row added. Unit 5 (hedley_well_child + small program modules) still in flight.
- **Unit 5 (hedley_well_child + 7 small program modules) COMPLETE — ✅ CLEAN** (Features containers; sibling paths for all 69 bundles audited instead). B-192 tail gained two same-shape siblings. Coverage row added.
**⭐ R23 COMPLETE (2026-08-24): 15 new items B-210..B-224 — 2 tier-2 LIVE headliners (B-213 scorecard z-scores at today's age; B-220 AI progress report omits the current encounter, regression since v1.14.0), 7 tier-3, 6 tier-4 (one PLAUSIBLE); 8 coverage rows (2 ✅ clean, 6 ◒); B-192 extended. Durable lesson: the five `hedley_<program>` server modules are configuration-only — route program-logic questions to hedley_restful/hedley_activity/hedley_person/hedley_case_management. Dry-stop counter: 0. Next discovery round = R24; un-rowed candidates: hedley_restful non-sync endpoints, per-program `Backend/*/Update.elm` sub-updaters, hedley_schedule/hedley_device at unit depth, the ~40 list_text allowed_values ↔ Elm sweep.**

## Round 24 (2026-08-24, @6347d2fa2, COVERAGE-SWEEP — surfaces named un-rowed by R23 + tree-vs-table gaps)
Units (7 worktree scouts, one wave): (1) hedley_restful NON-sync endpoints + hedley_restful.module; (2) all 17 per-program `Backend/*/Update.elm` sub-updaters; (3) Pages/GlobalCaseManagement (client, 3.1k); (4) list_text allowed_values ↔ Elm encoder/decoder sweep (~40 fields); (5) participant selection / enrolment pages (Participant(s)/People/Relationship/Clinics/*EncounterParticipants/*EncounterTypes); (6) hedley_device + hedley_schedule + hedley_general + hedley_view_export + hedley_zscore at unit depth; (7) small client pages (TraceContact/EducationSession/HomeVisit/Wellbeing/MessagingGuide/Report/Activities/Activity + Session non-group parts). Candidate ids start at B-225.
- **Unit 1 (hedley_restful non-sync) COMPLETE** → B-225 (LOW-MED, tier 4; type-mismatched `!==` guard, run-verified locally). Coverage row added.
- **Unit 5 (participant/enrolment flows) COMPLETE** → B-226 (MED, tier 3; nurse branch lacks the CHW `encounterTypeButtonActive` guard). Coverage row added.
- **Unit 7 (small client pages) COMPLETE** → B-227 (MED, tier 3; `5d553d346` inverted the smear predicate — scout's "date always Nothing" leg was half wrong, corrected in the entry), B-036 extended (derived feeding signs), eatenWithWater = already-recorded inert minor. Coverage row added.
- **Unit 2 (17 sub-updaters) COMPLETE** → B-228 (LOW, tier 4), B-229 (LOW, tier 4, race not reproduced); B-082 re-found and dropped. Coverage row added.
- **Unit 6 (5 small server modules) COMPLETE — ✅ CLEAN**; F1 = B-019 (🅿 parked, not re-pitched), F2 moot (`views_ui` enabled on ihangane.live, checked), F3 minors noted on B-019. Coverage row added.
