<!-- id:B-361 status:READY tier:3 round:31 -->
### B-361 — Antenatal progress report opened from the Patient Record: whether the viewed encounter counts as a nurse or a CHW visit follows the VIEWER's role, not the encounter's type — a nurse loses the latest CHW visit from the CHW Activity pane and gets it counted as a nurse ANC visit; a CHW loses the latest nurse visit from the diagnosis, medication, progress and photo panes and sees an invented CHW row dated today — READY · CONFIRMED (Patient Record entry, the previous-encounter partition and both role-keyed re-insertions read by the orchestrator; live sizing scout-measured) · MED-LOW · tier 3 · R31 unit 3 · DEPLOYED, all sites; ihangane: 467 pregnancies whose latest encounter is CHW with nurse visits + 5,255 CHW-only (nurse viewer); 32,877 whose latest is nurse (CHW viewer — how often CHWs open Patient Record not measured)
- **Mechanism.** The Patient Record opens the report on the latest prenatal encounter of ANY type (`Pages/PatientRecord/View.elm:545-579`: `getPrenatalEncountersForParticipantDesc` → `List.head` → `ClinicalProgressReportPage (InitiatorPatientRecord personId)`), and `App/View.elm:480-495` passes the logged-in `isChw`. The viewed encounter is removed from both previous lists regardless of type:
  ```elm
  -- client/src/elm/Pages/Prenatal/Encounter/Utils.elm:291-302
      getPrenatalEncountersForParticipantDesc db participantId
          |> List.filter (\( id, _ ) -> id /= currentEncounterId)
          ...
                      ( nurseEncounters, chwEncounters ) =
                          List.partition (Tuple.second >> .encounterType >> isNurseEncounter) previousEncounters
  ```
  and put back by role:
  ```elm
  -- client/src/elm/Pages/Prenatal/ProgressReport/View.elm:3314-3327
  generateAllNurseEncountersData isChw assembled =
      assembled.nursePreviousEncountersData
          ++ (if isChw then [] else [ { startDate = assembled.encounter.startDate, ..., measurements = assembled.measurements } ])
  -- :1247-1254 (viewChwActivityPane)
              assembled.chwPreviousMeasurementsWithDates
                  ++ (if isChw then [ ( currentDate, assembled.encounter.encounterType, assembled.measurements ) ] else [])
  ```
  `getFirstNurseEncounterMeasurements isChw` (`Encounter/Utils.elm:402-413`) has the same shape.
- **Reachability.** Participant directory or prenatal participant page → patient-record icon (no role gate: `People/View.elm:324`, `App/View.elm:1198`) → Antenatal pane → forward arrow. From the encounter's own page the role and the encounter type agree, which is why the in-encounter report is right.
- **Impact.** Nurse viewer, latest visit CHW: CHW Activity pane drops that visit (a one-visit CHW-only pregnancy shows an empty pane); Patient Progress counts it as a nurse ANC visit in the trimester checks/timeline. CHW viewer, latest visit nurse: Medical/Obstetrical diagnoses, Medication History, Patient Progress and Photos drop that nurse encounter; CHW Activity gains a row dated today built from the nurse's measurements. CHW encounters store no diagnoses (all `none` live), so no extra diagnosis lines appear.
- **Backlog de-dup.** B-210/B-211/B-212 (prenatal report panes), B-255 (CHW alert re-grading). Not recorded.
- **Fix shape (guess).** Decide nurse-vs-CHW placement inside the report from `isNurseEncounter assembled.encounter.encounterType`; keep `isChw` only for role-specific parts (alerts).
