<!-- id:G-04 status:READY tier: round:16 -->
### G-04 — E2E spec-partition guard (CI script) — READY · S/M · SOLID
- `.circleci/config.yml:144` (job 1: `playwright test prenatal tuberculosis` + a grep-INVERT run), :198 (job 2: FIXED filename list), :252 (reporting). Trap: a NEW spec whose describe title matches the invert regex (any new "HIV …"/"NCD …"/"Prenatal …" spec) is excluded from job 1 and, unless its filename is added to job 2's list, is SILENTLY NEVER RUN while looking green. Deliverable: `ci-scripts/check_e2e_partition.py` — parse job commands + `test.describe` titles/filenames, simulate Playwright selection, fail if any spec is selected by zero jobs (warn on double-run).
