<!-- id:B-352 status:READY tier:4 round:30 -->
### B-352 — People in village (offline): a person registered or edited on the device is missing from "people in village" lists until re-downloaded, when any geo level has a multi-word name — the device stores the geo data's own spelling ("Umujyi wa kigali") while the village query uses the decoder's title-cased one ("Umujyi Wa Kigali") in an exact compound-index lookup — READY · CONFIRMED (the index lookup, both decoders, the form's geo source and the encoder read by the orchestrator) · LOW · tier 4 · R30 unit 6 · DEPLOYED; self-heals when the person is next downloaded; ihangane: 96 of 370 CHW villages have a multi-word geo level (overlap with local names not measured)
- **Mechanism.**
  ```js
  // client/src/js/nodes.js:992-1004 (geo_fields branch)
                              criteria.province = fields[0];
                              ...
                              query = table.where(criteria).and(function (person) {
  ```
  ```elm
  -- client/src/elm/Backend/Person/Decoder.elm:157-160
  decodeGeoField =
      string
          |> map (String.toLower >> toTitleCase)
  -- Backend/Village/Decoder.elm:15-19 — the village fields the query is built from use decodeGeoField
  -- Backend/Person/Form.elm:566-575 — the saved name comes straight from GeoInfo
                  Dict.get (toEntityId id) geoInfo.provinces
                      |> Maybe.map (.name >> Just >> succeed)
  -- GeoLocation/Utils.elm:58, :571
                  , ( toEntityId 4847, GeoLocation "Umujyi wa kigali" Nothing )
                 , ( toEntityId 5378, GeoLocation "Rukiri ii" (Just <| toEntityId 5363) )
  ```
  Downloaded rows are re-encoded from decoded (title-cased) values, so they match; `encodePerson` (`Person/Encoder.elm:38-42`) writes the form's raw spelling, so a locally saved or edited person does not. Scout: 61 Rwanda geo names change under `toTitleCase`.
- **Reachability / consumers.** CHW registers or edits a person → `FetchPeopleInVillage` (`Backend/Update.elm:1654`) → GlobalCaseManagement, CHW Dashboard, village stock (B-300 sibling), education-session participants. An offline CHW can miss the person for days.
- **Backlog de-dup.** B-150/B-151 (GeoLocation), B-300 (village stock data before residents load). Not recorded.
- **Fix shape (guess).** Normalise geo fields in `encodePerson` (or on the form's output), or compare case-insensitively in the village query.
