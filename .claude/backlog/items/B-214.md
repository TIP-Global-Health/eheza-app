<!-- id:B-214 status:IMPLEMENTED tier:3 round:23 -->
### B-214 — Child progress report: "the last Well Child encounter" is whichever sorts first by UUID, so the Next Appointment pane can show a past visit's dates — ✅ IMPLEMENTED · CONFIRMED (orchestrator-read at anchor + Dexie query + sibling sorts) · MED · tier 3 · both sites · R23 unit 2
- **Mechanism.** `Pages/WellChild/ProgressReport/View.elm:369-374` `lastWellChildEncounterId = … getWellChildEncountersForParticipant db >> (List.head >> Maybe.map Tuple.first)`. That helper is `getParticipantEncountersByEncounterType` (`Backend/NutritionEncounter/Utils.elm:350-360`) — a plain `Dict.toList`, no sort. The dict is `Dict.fromList` over the IndexedDB result (`Backend/Update.elm:853-862`); vendored `AssocList.insert` appends, and the Dexie query (`nodes.js:~1087`, `where('[type+individual_participant]').anyOf(tuples)`, no `sortBy`) returns compound-index-then-primary-key order = encounter UUID order. Every `WellChildEncounterRevision` refetches, so it stays UUID order. Every OTHER "latest" consumer sorts first: `resolveDateOfLastNutritionAssessment` (`:841/:850`), `generatePartitionedWarningEntries` (`:878`), `GlobalCaseManagement/View.elm:~1576`, `resoloveLastScheduledImmunizationVisitDate` (`Measurement/Utils.elm:9186+`, via `generatePreviousMeasurements` DESC). `assembleProgresReportData` is the copy not updated.
- **Downstream.** `maybeAssembled` (`:376-382`) is built for that arbitrary encounter; `individualWellChildMeasurementsWithDates` (`:434-441`) = `(thatEncounter.startDate, …) :: previousMeasurementsWithDates`, head ≠ latest. `viewNextAppointmentPane` (`:1549-1552`) reads `List.head individualWellChildMeasurements |> .nextVisit`, so the Immunisation / Pediatric Visit dates come from whichever encounter sorts first (probability ≈1/n of being the real latest). Secondary: `generateECDMilestonesWithStatus` (`:1072-1095`) `Dict.fromList` over the same unsorted list — same-window encounters resolve by UUID, minor.
- **Not affected (checked):** immunisation history (`generateVaccinationProgressDictsForWellChild` folds ALL encounters), growth charts + scorecard (bucketed by date), end-encounter gating (`:157-175` uses the real `id`).
- **Reachability.** `assembleProgresReportData` from `viewContent` (`:468`) for every initiator; pane renders in `ModeActiveDiagnosis` (`:531-535`) and in the WhatsApp report (`ComponentWellChildNextAppointment`).
- **Fix shape (first guess):** sort with `sortEncounterTuplesDesc` before `List.head` at `:369-374`, or take the head of `generateIndividualWellChildMeasurementsForChild` (already DESC).
- ✅ **IMPLEMENTED 2026-08-30 — issue #2169, PR #2170** (built together with B-253 — one PR, same defect shape), branch `B-214-latest-encounter-sort` off `b3e15df07`.
- **All FIVE call sites of `getWellChildEncountersForParticipant` were checked, not the one the entry named.** Three were already right: `ProgressReport/View.elm:876` and `GlobalCaseManagement/View.elm:1566` both sort DESC (the latter with a hand-rolled comparator), and `WellChild/Participant/View.elm:113` only looks for the *active* encounter, which is order-independent. `sortEncounterTuplesDesc` was **already imported** in the defect's own file (`:112`), so the fix is one `List.sortWith` line in the existing pipeline, with the file's own `-- Sort DESC` comment idiom.
- ⛔ **`reportData.wellChildEncounters` (`:427`) was deliberately LEFT UNSORTED, and sorting it would have been a regression.** It feeds `viewECDPane` → `generateECDMilestonesWithStatus`, which does `Dict.fromList` over `(milestone, status)` pairs; vendored `AssocList.fromList` is `List.foldl insert`, so the **last** entry for a key wins. A DESC sort would therefore make the OLDEST encounter in a milestone window decide its ECD status. The other consumer, `viewDiagnosisPane` → `resolveDateOfLastNutritionAssessment` (`:848-851`), sorts the list itself and is unaffected. Which encounter should win a shared milestone window is a real open question — filed as **B-270**.
- ⭐ **`elm-review` caught a real regression this run** (a genuine one, unlike the stale-cache phantom on B-212): dropping `List.reverse` in the Outcome page left `List.map Tuple.first |> List.head` adjacent, which the `Simplify` rule rejects. Rewritten as `List.head |> Maybe.map Tuple.first`, which also matches the B-214 site's own idiom. Without the pre-push run this would have turned CI red.
- **Verified.** `elm-format --validate` clean · `elm make src/elm/Main.elm` → *Success! Compiled 548 modules* · `elm-test` **3,030 passed / 0 failed** · `elm-review` *I found no errors!* **on a cleared `elm-stuff`**.
- 🔍 **Reviewed (medium) 2026-08-30 — four findings, two of them REFUTED on verification.** ⭐ **The headline finding did not hold, and checking it is the keeper:** the review reported `Measurement/Utils.elm:9268`/`:9298` (`behindOnVaccinationsByProgressFrom{WellChild,ChildScoreboard}`) as the same defect on a second path — an arbitrary anchor encounter whose `vaccinationProgress` would mis-report a child as behind. But `Backend.Measurement.Utils.generatePreviousMeasurements` excludes **only** the anchor and keeps every other encounter, and the progress fold is `assembled.measurements :: previousMeasurements`, so anchor + others = all encounters **for any anchor**. The set is anchor-independent; `assembled.person` does not vary; the scoreboard assembler has the identical structure. ⭐ **An arbitrary `List.head` is not automatically a defect — check whether anything downstream depends on the choice.** What survives is narrow and was filed as **B-271**: the per-dose `Dict.fromList` last-wins tie-break does depend on list order, hence on the anchor, when two encounters record the same dose with different dates. The refutation is posted on the PR.
- The two low findings: (a) the sort was inlined rather than made a wrapper — **addressed in `da31ec1d2` from the naming angle** (below); (b) `sortEncounterTuplesDesc` compares only `startDate` and `List.sortWith` is stable, so same-day encounters are still resolved by storage order, and now by the *opposite* element than before (a stable sort's head is the first inserted; `List.reverse |> List.head` was the last). Not a correctness regression, but the "latest" guarantee does not hold for same-day encounters. Posted inline, left unfixed.
- 🔧 **Follow-up `da31ec1d2` (user-approved, folded into the same PR): the sorted prenatal accessor is renamed `getPrenatalEncountersForParticipantDesc`.** Two functions shared the name `getPrenatalEncountersForParticipant` — the raw accessor in `Backend.NutritionEncounter.Utils` (storage order, one of nine generated over `getParticipantEncountersByEncounterType`) and the `Pages.Prenatal.Encounter.Utils` wrapper that adds the DESC sort. Identical signatures, so which behaviour a call site got was decided by its import line, invisibly — **that is exactly how B-253 happened**. History: the prenatal accessor has sorted since 2019; `d7391886b` (2023-07-03, *"Improve code"*) factored the generic accessor into Backend and hollowed the Pages one out to `raw |> sort`, inheriting the collision. 5 files, 9 references. No file imported both, so Elm never had to complain.
- 📌 **Audited for further B-253s while renaming: none.** The four remaining raw-accessor callers — `HIV/Activity/{Fetch,View}`, `Prenatal/Encounter/Fetch`, `PatientRecord/Fetch` — all consume the whole list (`List.concatMap` over every encounter, or fetch-all-measurements), so order is irrelevant to them. ✅ **Done in `eb37046ab`** (user-approved follow-up): `Prenatal/Participant/View.elm` imported the raw accessor and hand-rolled the DESC sort — a duplicate of the wrapper's body. It now calls `getPrenatalEncountersForParticipantDesc`, which collapses a 8-line `Maybe.map` to one line and leaves **two imports unused** (`Backend.NutritionEncounter.Utils` and `Utils.NominalDate`), both removed. ⭐ The rename is what made this visible: the duplication was invisible while both functions had the same name.
- 🤖 **Copilot reviewed 2026-08-30 — 🟢 approval recommended, one comment, FIXED in `7438bfedf`:** `GlobalCaseManagement/View.elm:731` sorted the list a second time after `getPrenatalEncountersForParticipantDesc` had already sorted it. Behaviour-preserving to remove — `List.filter` keeps relative order and Elm's sort is stable, so re-sorting a sorted list is identity. The redundancy predates the rename (`origin/develop` has the same block under the old name); renaming is what made it visible. ⚠ The two blocks that look identical at `:876` and `:995` MUST keep their sorts — they call the raw `getTuberculosisEncountersForParticipant` / `getHIVEncountersForParticipant`, which have no sorted wrapper, so the import stays for them.

