<!-- id:B-010 status:READY tier:4 round: -->
### B-010 — Shard-blind IndexedDB queries scan every shard on multi-HC tablets — READY · CONFIRMED (perf; verification sweep 2026-07-06)
- `nodes.js:627-629` (follow-up-measurements: 13 types incl. labs-results/hiv-tests), :776-778 / :830-832 (stock: 3 types each): `.where('type').anyOf(types).and(item => deleted!==true && item.shard===shard)` — the `and` is a post-deserialization JS filter. v32 schema (app.js:324-326) has NO `[type+shard]` (full index list verified v1→32); `[shard+vid]` can't help (would deserialize a whole shard). Precision: cost = all rows of the queried TYPES across all shards (not whole-table); single-HC devices waste nothing; multi-HC tablets deserialize+discard every other HC's rows per request. Callers: GlobalCaseManagement + Dashboard(village filter) fetch follow-ups; StockManagement both modes; isNotAsked-guarded → once per session per HC.
- Fix (verified sites): app.js add `dbSync.version(33).stores({shards: '<v32 string>,[type+shard]'})` + bump `dbVersion` :386; sw.js:34 dbVerno→33; `hedley_restful.module:8` constant→33; rewrite the 3 query sites to `.where('[type+shard]').anyOf(types.map(t => [t, shard]))` keeping `.and(deleted!==true)`. Note: version bump rides the B-007(b) upgrade window — land B-007's eager-open fix first or together.
