<!-- id:B-128 status:IMPLEMENTED tier:1 round:17 -->
### B-128 — ✅ IMPLEMENTED 2026-08-04 (issue #2033, PR #2034, branch `b128-scoreboard-month-offset`, base develop @c37f2fe3c, commit 959f230a1) — aggregated NCDA Scoreboard double-shifted the age/month offset
- **Fix:** all SIX sites (the entry said ~7; it is exactly 6) drop the `- gapInMonths` correction and test `ageInMonths` directly; the EDD-based ANC row becomes `negate ageInMonths` (renamed `monthsBeforeDelivery`); `ageInMonthsForIndexCell` binding deleted at the Universal-Intervention site so `resolveLastDayForMonthX` also gets the unshifted age. Invariant documented on `resolveTargetDateForMonth`. After the change EVERY remaining `gapInMonths` use in the file feeds `resolveTargetDateForMonth` and nothing else — a cheap grep-level guard.
- **Verification:** `elm make src/Main.elm` Success · elm-format clean · elm-review COLD `I found no errors!` · scratchpad discrimination sim (`b128_sim.py`, faithful ports of justinmimbs `Date.diff/add Months`, `Date.floor Month`, Gizra `toLastDayOfMonth`) running all 6 sites pre/post: column k admitted ages k..k+24 instead of 0..24; Jan-2026 column read 1-24 / 4-27 / 7-30 depending on the viewing date (invariant violation), post-fix always 0-23; newborn row swapped 2026-01-15 ↔ 2025-06-15; vaccination cutoff moved 2025-06-30 → 2026-01-31.
- ⚠ **`server/elm` HAS NO TEST SUITE** (no test-dependencies, no Test modules; CI runs only elm-review on it — `ci-scripts/test_elm_review.sh:27`). Stated plainly in the PR body rather than implying test coverage. Building test infra for that app is its own piece of work, NOT folded in (rule 6).
- 📌 **Minor spun out (not fixed):** `childrenUnder2` (:94) ages from the RAW `record.birthDate`, the five pane sites use `Date.floor Month record.birthDate` — denominator and numerators use different age definitions at month boundaries. Recorded in the PR's "deliberately not in this PR" section; needs its own decision.
- 📌 No stored data affected (view-layer arithmetic over `field_ncda_data`) — no recompute needed, UNLIKE B-149. Historical percentages WILL move on deploy → release-note line.

Original entry:
- `server/elm/src/Pages/Scoreboard/View.elm` — pattern self-verified in `childrenUnder2` (~:93-117): `targetDateForMonth = resolveTargetDateForMonth gapInMonths currentDate` (shifts k months back), `ageInMonths = diffMonths record.birthDate targetDateForMonth` (age AT the column — k already applied), then `gap = ageInMonths - gapInMonths` subtracts k AGAIN; test `gap >= 0 && gap < 24`. Correct test is on `ageInMonths` directly. Same double-shift in Demographics rows 2-3 (~:188-218), ANC-Newborn row 2 (~:492-512, reversed sign), Universal-Intervention row 1 (~:569-645 via `ageInMonthsForIndexCell`), Nutrition-Behavior row 1 (~:775-803), Targeted-Interventions rows 2-3 (~:944-970).
- Root cause: commit `5ec9bad0d` (2024-03-03, "Fix calculations fir first of the month...") moved `ageInMonths` to the column's target date but kept the old `- gapInMonths` correction. **Deployed since ~March 2024 — this is live-production-wrong, unlike most queue items.**
- Consequences (agent empirically replicated with justinmimbs diff-Months semantics; sims at session scratchpad `scoreboard_sim.py`/`anc_sim.py`): `childrenUnder2` — the denominator for every percentage cell in all 8 panes — counts ages k..k+23 months for the column k months back (Jan column viewed in Aug counts 7-30-month-olds); "newborns this month" at k months back counts births from k months earlier still; a July-born child viewed in August appears in NO column; ANC iron-during-pregnancy counts ~5 columns stepping by 2 instead of ~9, and a historical cell CHANGES VALUE as the viewing date advances (invariant violation proving the formula wrong independent of spec).
- Fix shape: use `ageInMonths` (age at target date) directly — delete the `- gapInMonths` correction at each site (and the reversed-sign ANC variant). Small diff, but touch all ~7 sites consistently + tests if extractable.
- Note: `equalByYearAndMonth` measurement-date matching is calendar-correct; only birth/EDD-age gates are broken, which is why current-month spot checks always looked right.
