<!-- id:B-009 status:IMPLEMENTED tier:3 round: -->
### B-009 — Dashboard "NaN%" on empty denominators — ✅ IMPLEMENTED 2026-07-20 (issue #1965, PR #1966, branch `b009-dashboard-nan-percentage`, base develop @c9d08268a, commit f1ba05438, Copilot requested). ⚠ BACKLOG FIX WAS WRONG (re-verify caught it): `calculatePercentage now before` computes percent CHANGE ((now-before)/before), NOT a proportion — routing the two sites through it would change the numbers. CORRECT fix: new zero-guarded `percentageOfTotal part total` (Pages/Utils.elm, if total==0→0 else round(part/total*100)); swapped the 2 raw-division PROPORTION sites in Pages/Dashboard/View.elm (:1451 goodThisYear/allThisYear Good-Nutrition card; :3044 goodNutritionEncounters/totalEncountersCompleted). Both NaN when denom 0 (new HC / empty month) → rendered 'NaN%'. `percentageLastYear` (a real change metric) correctly LEFT on calculatePercentage. 5 NEW tests in NEW Pages/Utils/Test.elm (module Pages.Utils.Test coexists fine with Pages/Utils.elm file); DISCRIMINATION: drop the total==0 guard → exactly the 2 zero-total tests fail (NaN/Infinity), 3 proportion/round guards pass. elm make Main 0, elm-test 2843, elm-format+elm-review cold clean. Local /code-review medium: no findings.
- `Pages/Dashboard/View.elm` ~:1436, ~:3028: raw `/` bypasses zero-guarded `calculatePercentage` (`Pages/Utils.elm` ~:220) → "NaN%" on new HC / empty month.
- Fix: route through `calculatePercentage`.
