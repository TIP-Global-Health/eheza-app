<!-- id:B-162 status:UNKNOWN tier:3 round:19 -->
### B-162 — Two contradictory definitions of "attended a group session" feed the same Dashboard: Total Encounters counts ONLY attended=TRUE rows, missed-sessions counts measurement-or-TRUE — CONFIRMED definitions (self-verified) · agent-measured live scale · MED-LOW · ⚠ adjacent to the #2065 user decision — PRESENT, don't just fix · R19 Unit 4 · READY
- Definition A (post-#2065, `hedley_activity_get_persons_sessions_attended`, hedley_activity.module:467-511): attended = any non-attendance measurement OR attendance row with attended=TRUE. Definition B (`hedley_stats_get_total_encounters_by_patients_at_clinic`, hedley_stats.module:3006-3014, self-verified): encounter EXISTS only where an attendance row has `field_attended_value = TRUE` — a person with measurements but no TRUE row contributes ZERO group encounters.
- Agent-measured (ihangane.live, window since 2025-09-01): **6,085** measured-without-TRUE-attendance pairs vs **6,221** attended=TRUE pairs — Total Encounters misses roughly HALF the attendances the missed-sessions definition counts. Same client Dashboard renders both: a child measured at every session can read as never-missing while contributing 0 encounters to the totals card.
- ⚠ Context that gates this: B-152a's review (2026-08-10) surfaced definition B and the user chose to NARROW the missed-sessions fix so the two would not contradict on the attendance-row side — but the measurement-only asymmetry was left standing, undiscussed. This entry is the explicit follow-up question: align B to the #2065 predicate (left-join attendance + OR, same idiom), or record a written decision that Total Encounters intentionally counts only explicit attendance. Cross-metric within one PHP engine, not cross-side — recorded under the B-152a semantic class, NOT the declined boundary class.
