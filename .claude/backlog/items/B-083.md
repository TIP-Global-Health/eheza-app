<!-- id:B-083 status:READY tier: round:16 -->
### B-083 — HC-level stock view counts village-tagged stock updates as receipts — READY · CONFIRMED-mechanism · S · ⚠ GATED on product semantics (confirm intended before building)
- `Backend/StockUpdate/Utils.elm:15-42` `allStockUpdates = Dict.values measurements.stockUpdate` (:39-40, no village filter) while distributions come only from group childFbf/motherFbf (:21-37) → HC balance can overstate when village stock runs alongside. Fix if confirmed: filter HC mode to `village == Nothing`.
