<!-- id:B-298 status:READY tier:4 round:27 -->
### B-298 — Group measurement forms: the save button's double-submission guard and error indication are dead — `EditableSession.update` is hardcoded `NotAsked` and never written — READY · CONFIRMED (all writers enumerated) · MED-LOW · tier 4 · R27 unit 4
- **Mechanism.** `makeEditableSession` sets `update = NotAsked` (`Backend/Update.elm:10210-10222`); the only other writer, the attendance fast-path (`:2585`), resets it to `NotAsked` again; nothing anywhere assigns `Loading`/`Failure` (the real request state lives in `Backend/Session/Model`'s write-only per-person dicts — B-082's orphaned-tracker family). Yet `getChildMeasurementData`/`getMotherMeasurementData` thread it into every group form (`Backend/Session/Utils.elm:76, :110, :136`) and the shared `saveButton` gates on it (`Measurement/View.elm:604-615`: `isLoading = measurement.update == Loading`, `isFailure = RemoteData.isFailure measurement.update`; form-save twin `:1863-1875`) — its doc-comment promises double-submission prevention and error indication, neither of which can happen.
- **Impact.** A double-tap on Save before the service-worker revision echo (~tens of ms, the B-062 timing analysis) fires two POSTs → two measurement rows for one (person, session, type). Live corroboration already recorded: R21 wave C found double-POST pairs still minted (latest 2026-04-18); the daily `delete-duplicate-measurements.php` sweep deletes them (keeps highest vid), so the damage is operational dependence on the sweep plus brief UI duplication. The read-side symptom of such duplicates is the R26-DECLINED `splitChild/MotherMeasurements` head-pick — this entry is the write-side hole that mints them from a single device; failure indication being dead is near-moot offline-first.
- **Fix shape (guess).** Wire the per-person request dicts (the B-082 trackers) into `MeasurementData.update`, or disable the button optimistically on tap. Fixing B-082's wrong-dict insert alone changes nothing user-visible until this wiring exists — the two items land naturally together.
