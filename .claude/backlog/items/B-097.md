<!-- id:B-097 status:READY tier:4 round:16 -->
### B-097 — check_plain() hardening for the 2 hedley_admin tables (4 cells) — READY · CONFIRMED-unescaped · LOW (defense-in-depth ONLY — verified NOT currently exploitable)
- D7 theme('table') doesn't escape #rows. Only 2 such renders exist (hedley_reports has none): devices-sync-state `hedley_admin.module:441-446` (cells :427-438) + resilience-survey `:579-584` (cells :561-566). Exploitability honestly LOW: every cell is numeric/date/enum (list_text allowed-values validated) or backend/migration-generated node titles (sync strips `label` via ignoredFields `HedleyRestfulSync:407,416`; person names sanitized `preg_replace` :477). Fix: `check_plain()` on `name` :429 + `nurse`/`health_center` :562-563. Frame the PR as hardening, not vuln.
