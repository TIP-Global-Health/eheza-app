<!-- id:B-343 status:IMPLEMENTED tier:1 round:30 -->
### B-343 — Prenatal HIV: a "partner is HIV positive" answer stored on the patient's own HIV test still diagnoses a discordant partnership after the partner is TESTED negative — the form hides the question once the partner result is known, but never clears the answer, and four readers still use it — ✅ IMPLEMENTED 2026-09-14 (issue #2244, PR #2245) · CONFIRMED (form gate, the diagnosis reader, the OR at the diagnosis and the introducing commit read by the orchestrator; scout scratch elm-test for both cases) · MED · tier 1 (diagnosis rule) · R30 unit 2 · pre-existing since 2024-10-11, reachable on the released code; live ihangane: 3 HIV tests carry `partner-hiv-positive` beside a same-encounter partner test run today with a NEGATIVE result (nids 4505031, 5262774, 5283390; encounter 4505014 has `partner-hiv-recurrent` stored)
- **Mechanism.** The question is asked only while the partner's result is unknown (`b16108a58`, 2024-10-11, "Logic fixes"):
  ```elm
  -- client/src/elm/Measurement/Utils.elm:4285-4288
                  TestNegative ->
                      let
                          ( partnerHIVPositiveSection, ... ) =
                              if partnerHIVTestResult == TestIndeterminate then
  ```
  but the diagnosis reads the stored sign unconditionally:
  ```elm
  -- client/src/elm/Pages/Prenatal/Activity/Utils.elm:2312-2323
          discordantPartnershipByHIVTest =
              hivNegative
                  && (getMeasurementValueFunc measurements.hivTest
                          |> Maybe.andThen .hivSigns
                          |> Maybe.map
                              (\hivSigns ->
                                  EverySet.member PartnerHIVPositive hivSigns
                                      && partnerNotSurpressed hivSigns
  -- :2797
              (discordantPartnershipByPartnerHIVTest || discordantPartnershipByHIVTest)
  ```
  Same stale read in `resolveDiscordantCoupleStatus`'s `byHIVTest` fallback, `partnerSurpressedViralLoadByHIVTest` (HIV health education) and `partnerTakingARVsByHIVTest` (`resolveDiscordantPartnershipSet`). B-320 gated these on the patient's own result, not on the partner's.
- **Reachability.** Initial Laboratory, tabs in any order: HIV tab saved first (negative, "partner HIV positive: Yes, not on ARVs") → Partner HIV tab saved (tested today, negative); since B-221 that save re-assesses. Once the partner test exists the nurse has no screen showing the old answer.
- **Impact (scout, run).** Partner tested negative → `[DiagnosisDiscordantPartnershipInitialPhase]` → TDF3TC (PrEP) prescribed; report prints "Discordant Couple: partner not taking ARVs" while `resolvePartnerHIVTestResult` is `TestNegative`. Partner known positive + on ARVs + suppressed, with an old "not on ARVs" answer → diagnosis still fires while the report line says suppressed. All sites.
- **Backlog de-dup.** B-221, B-237, B-320, B-323 (all IMPLEMENTED, none gates on the partner test); symptom not recorded.
- **Fix shape (guess).** Read the partner answers on the patient's own test only while the partner result is `TestIndeterminate` — the form's own condition — in all four readers; consider whether the 2022–2024 records' report lines should change with it.
- ✅ **IMPLEMENTED 2026-09-14 — issue #2244, PR #2245, branch `B-343-partner-signs-on-hiv-test` (base `8d6d870ae`).** One function, `partnerSignsByHIVTest` in `Pages/Prenatal/Utils.elm`, returns the signs on the patient's HIV test only while `resolvePartnerHIVTestResult` is `TestIndeterminate` (the form's own condition); all four readers go through it. `resolvePartnerHIVTestResult` now takes `PrenatalMeasurements`. **User decision (2026-09-14): "follow the form"** — a partner test sent to the lab (result pending, which `resolvePartnerHIVTestResult` reads as `TestNegative`) also stops the patient-side answer counting. Intent from issue #1349 (PR #1353). 9 new elm-test cases in `Pages/Prenatal/Activity/Test.elm`; 6 fail with the gate removed, 3 controls pass both ways; full suite 3,204 green, elm make + elm-review clean. Stored answers and saved diagnoses on the 3 live records are unchanged.
- **E2E (2026-09-14, commit `4e6e580da`).** The existing nurse test "partner HIV positive after a negative HIV test offers TDF + 3TC, and withdraws it once the partner is tested negative or the patient is known as positive" (`prenatal-encounter-nurse.spec.ts`, CI job `e2e_playwright_1` by path) now corrects the Partner HIV test to tested-today-negative (report has no Discordant Couple; Next Steps, where listed, no TDF + 3TC), restores it to known positive (TDF + 3TC offered again), then runs the existing patient-known-positive step. New shared helper `correctLabTest` behind `correctHIVTestToKnownPositive` and the new `correctPartnerHIVTest`. Discrimination-run locally on the main tree: FAILS on develop + e2e only (at "a partner tested negative is not a discordant couple", 1 found), PASSES on the fix (1.5 m).
- **Review round 1 (medium, 2026-09-14): no bugs, 1 style finding** — the new fixture `withHIVTestPartnerSigns` repeats `withHIVTestPartnerPositiveSigns` ([thread](https://github.com/TIP-Global-Health/eheza-app/pull/2245#discussion_r4008167751)). Awaiting the user's decision.
