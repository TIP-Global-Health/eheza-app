<!-- id:B-067 status:IMPLEMENTED tier:2 round:14 -->
### B-067 — Scorecard Dewormer & ECD rows fold in nurse (HC) questionnaires → false red "X" where Vitamin A correctly shows "-" — ✅ IMPLEMENTED 2026-07-12 (issue #1925, PR #1926, branch `b067-scorecard-dewormer-ecd-source`, base develop @38d950127, commits 84e443608 + 75c1b3e0f, ALL 10 CI CHECKS GREEN on head, Copilot round addressed: 2 comment-only findings — doc-comment grammar + pre-existing `questioneer`→`questionnaire` typo, both fixed). User confirmed the data-source caveat → dash is intended. Fix: dewormer + ecd now read `chwQuestionnairesByAgeInMonths` (mirroring vitaminA); ongeraMNP keeps the nurse+CHW merge. Row resolution extracted from the view into top-level `generateUniversalInterventionsValues` (exposed) so it's testable. NEW test module `Pages/WellChild/ProgressReport/Test.elm` (7 tests). Verification: elm make Success 548, elm-test 2768 passed, elm-format + elm-review clean; DISCRIMINATION confirmed — pre-fix the nurse-only month printed `[Dash, X, X]` (vitaminA, dewormer, ecd) vs `[Dash, Dash, Dash]` after. Tests also pin: CHW genuine negative still X, CHW wins when both exist, ongeraMNP still merges + its OngeraMNP-distributed filter, age gating (VitA 6mo / dewormer 12mo).
- ⚠ MERGE-ORDER NOTE: #1926 and #1924 both edit this module's exposing list (3 lines) → whichever merges SECOND needs a trivial conflict resolution. Test files do NOT collide (b067 used a new test module deliberately).
- `Pages/WellChild/ProgressReport/View.elm:2498-2502` (dewormer) / :2552-2553 (ECD) source from `questionnairesByAgeInMonths` (the nurse+CHW MERGE built for Ongera-MNP, which BOTH ask), whereas Vitamin A (:2485-2493) uses a CHW-only dict. But dewormer/ECD/vitaminA are asked ONLY at the CHW/child-scoreboard encounter (`Measurement/View.elm:3159-3171`: the `atHealthCenter=True` nurse branch of NCDAStepUniversalInterventions collects only `[OngeraMNP]`; atHealthCenter=False only for ChildScoreboard). So at a month with only a nurse NCDA and no OngeraMNP set, the nurse entry survives into dewormer/ECD → `.signs >> member ChildReceivesDewormer` = False → cell = X (red "did not receive"), while Vitamin A correctly shows "-" for the same month.
- Fix: give dewormer + ECD their own CHW-only dict (mirror vitaminA), not the Ongera-MNP merge. Severity MED (false-negative on a clinical intervention row; depends on nurse-only months existing — the vitaminA/dewormer asymmetry is strong evidence it's unintended). Confirm the intended data source before implementing.
