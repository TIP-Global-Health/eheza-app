<!-- id:B-084 status:IMPLEMENTED tier:3 round:16 -->
### B-084 — Shared `viewNumberInput` accepts hand-typed negatives (HTML min="0" only) — ✅ IMPLEMENTED (PR #1979, extended by #1993)
- Verified on develop 2026-08-03: `dropLeadingMinus` exists in `Pages/Utils.elm` and is wired into the input handlers (lines 741, 760). The tier-3 line listing this as open was stale.
- `Pages/Utils.elm:697-712` `inputAttrs = [ type_ "number", Html.Attributes.min "0", onInput setMsg, value currentValue ]`; `String.toInt "-5"` parses at consumers. Fix: reject/clamp negatives in the shared helper.
