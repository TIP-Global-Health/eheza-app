<!-- id:B-071 status:IMPLEMENTED tier:4 round:15 -->
### B-071 — SyncManager General save-handler reports errors against the wrong response var (copy-paste from the authority twin) → misattributed/stale Rollbar telemetry — ✅ IMPLEMENTED 2026-08-20 (issue #2126, PR #2127, branch `b071-general-lane-error-var`, base develop @4cf741074, commit 12672f741). Fix: `maybeHttpError model.downloadGeneralResponse` at `SyncManager/Update.elm:863` (anchor drifted from :843), matching the authority twin at :321. NEW test in the existing `SyncManager/Test.elm` harness (which calls `SyncManager.Update.update` directly): general response Success + authority response `Failure Http.NetworkError` → `.error` must be Nothing. DISCRIMINATION: reverting the one-liner fails exactly that test, printing `Just { error = Http NetworkError, location = "BackendGeneralFetchedDataSavedHandle" }` — the misattribution itself. ⚠ `Http.BadStatus` here takes a `Response String` (elm-http-builder 6.0.0 era), not an Int. elm make 548, elm-test 2979, elm-format clean, elm-review (fresh clone, committed state) clean.
- `SyncManager/Update.elm:843`: the `BackendGeneralFetchedDataSavedHandle` handler (maps over `downloadGeneralResponse` at :846) calls `maybeHttpError model.downloadAuthorityResponse … "BackendGeneralFetchedDataSavedHandle"` — wrong var (the authority twin at :305 correctly uses downloadAuthorityResponse for ITS handler). Reached only on general-download Success (so correct slot is always Nothing); instead it inspects the authority response, which still holds the PREVIOUS cycle's value (general runs before authority) → if that ended in Failure, this emits a stale authority Http.Error misattributed to the general location. Telemetry/dbErrors corruption ONLY — no cursor/data/state impact.
- Fix: `maybeHttpError model.downloadGeneralResponse …`. Severity LOW (observability hygiene, clean 1-line fix).
