<!-- id:B-360 status:READY tier:4 round:31 -->
### B-360 — Acute Illness progress report, physical exam pane: earlier readings are judged against the patient's age TODAY — a respiratory rate recorded at 11 months turns red after the first birthday, and the whole MUAC column disappears once the child turns 5 — READY · CONFIRMED (age bindings and the threshold read by the orchestrator) · LOW · tier 4 · R31 unit 4 · DEPLOYED; same class as B-213 (implemented) and B-255, not recorded for Acute Illness
- **Mechanism.**
  ```elm
  -- client/src/elm/Pages/AcuteIllness/ProgressReport/View.elm:441-445
          maybeAgeMonths =
              ageInMonths currentDate assembled.person
          showMuac =
              isChildUnderAgeOf5 currentDate assembled.person
  -- used per row at :507 (respiratoryRateAbnormalForAge maybeAgeMonths …) and :535 (muacIndicationForPerson currentDate …)
  -- client/src/elm/Pages/AcuteIllness/Activity/Utils.elm:2798-2806
  respiratoryRateElevatedByAge maybeAgeMonths rate =
      ... if ageMonths < 12 then rate >= 50
          else if ageMonths < 5 * 12 then rate >= 40
  ```
- **Reachability.** An earlier illness opened from Patient Record / Well Child / NCD / Child Scoreboard report history (`Pages/Report/View.elm:1261` forward arrow). Opening from the encounter itself uses the encounter day, so it is right there.
- **Fix shape (guess).** Compute age from each row's `encounterData.startDate`.
