<!-- id:B-131 status:IMPLEMENTED tier:1 round:17 -->
### B-131 — ✅ IMPLEMENTED 2026-08-06 (issue #2046, PR #2047, branch `b131-server-vaccination-engine-drift`, base develop @417df78d2, commits 4f85debf8 + 793eda287) — server vaccination-engine drift ×3
- **Built in the safe order** (see the ⛔ note below, which the user surfaced): allVaccineTypes site-gate → special cases in `nextVaccinationDataForVaccine` (birthDate threaded in) → Rwanda IPV branch in `initialVaccinationDateByBirthDate` → `getLastDoseForVaccine` site param LAST → client's placeholder comment carried across. New `laterOf` helper shared by both special cases.
- **Impact restated for the PR (worse than the entry said):** DTP-SA made **every Rwandan child 18-24 months with an OPV-3 record read off track**, since the consumer takes the EARLIEST outstanding dose. Children without OPV-3 unaffected (999-year fallback).
- **Verification:** elm make · elm-format · elm-review COLD clean · simulation old-vs-new (IPV-2 naive port = same day as dose 1 for every case; OPV-2 moves ~2 weeks later; Rwanda loses dtp_sa, Burundi keeps it, neither gains HPV) · **bundle regenerated AND byte-compared against a fresh build of the committed source — identical.**
- ⭐ **NEW STANDING VERIFICATION (from the #2045 review, adopted): for any server/elm change, rebuild the bundle from the COMMITTED source and `cmp` it against the committed `elm-main.js`.** Asserting "I regenerated it" is not evidence; this is. Cheap, catches a stale or hand-edited artifact.

Original entry:
- `server/elm/src/Pages/Scoreboard/Utils.elm` vs `client/src/elm/Measurement/Utils.elm`; consumed by Scoreboard Universal-Intervention row 1 AND Reports Postnatal-Care immunization rows (`Pages/Reports/View.elm` ~:3103-3195).
- (a) **DTP-standalone unconditional**: server `allVaccineTypes` (~:263-273, no site param — self-verified) includes `VaccineDTPStandalone` for ALL sites; client + this repo's own `Pages/Reports/Utils.elm` (~:556-574) gate it to Burundi. Rwanda children ≥18mo never have dtp_sa → marked off-track → undercounts up-to-date.
- (b) **Rwanda IPV second dose missing**: client gives Rwanda IPV 2 doses (issue #1426; client ~:9633-9651/9749-9755); server copy caps IPV at `VaccineDoseFirst` (~:117) → children behind on IPV2 counted up-to-date (overcount).
- (c) **OPV second-dose special case missing**: with a birth-dose OPV, client schedules dose 2 at max(age 6w, +4w); server adds a flat 4w (~:87) → expects OPV2 ~2 weeks early → premature off-track.
- (Server copy also shares the client's DTPStandalone-reads-OPV3 quirk = existing clinician-gated **B-055** — same fix should hit both copies when B-055 is ever ruled on.)
- ⛔ **(b) IS NOT A ONE-LINE PORT — the obvious fix breaks worse than the bug (2026-08-06).** Both copies return `( 0, Days )` from `getIntervalForVaccine` for IPV; the CLIENT labels that a placeholder and puts the real rule in special cases at the call sites, and **the server copied the placeholder without the comment or the compensating logic**. Adding `Site` to `getLastDoseForVaccine` alone would schedule IPV-2 at `lastDoseDate + 0 days` → every Rwandan child with IPV-1 permanently off track. **Order matters: (1) `allVaccineTypes` takes Site — DTP-SA for Burundi only, NO HPV (see below); (2) thread `birthDate` into `nextVaccinationDataForVaccine` and port BOTH special cases there (OPV dose 2 + Rwanda IPV dose 2 = max(36 weeks old, dose1 + 28 days)); (3) port the Rwanda IPV branch into `initialVaccinationDateByBirthDate` (client `:9566-9600`); (4) ONLY THEN `getLastDoseForVaccine` takes Site — it is the switch that turns the feature on; (5) carry the client's comment across so the placeholder is labelled in both copies.**
- ⛔ **DO NOT merge the two `allVaccineTypes` into one definition (user, 2026-08-06).** They differ ON PURPOSE: the scoreboard covers children to 24 months, so it deliberately omits `VaccineHPV` (given at 12 years) — its own docblock says so — while `Pages/Reports/Utils.elm` adds HPV for non-Burundi. The scoreboard's copy needs the Site gate for DTP-SA and NOTHING ELSE. (I proposed unifying them; that would have put a 12-year vaccine into an under-2 report.)
- 📌 **(a)'s real impact, from the consumer at `Pages/Scoreboard/View.elm:617-641`:** it takes the EARLIEST outstanding due date and marks the child off track unless it falls after the reference month. DTP-SA is scheduled at max(28 days after OPV-3, 18 months old) — inside the window — and Rwandan children never receive it, so **every Rwandan child aged 18-24 months with an OPV-3 record reads off track regardless of what they had.** Not a partial undercount: the whole top age band. (Children with no OPV-3 are unaffected — the fallback is `Date.add Years 999`.) HPV, by contrast, would be harmless even if included, since at 12 years it can never be the earliest date.
- Fix direction: port the client logic (or better: note this is ANOTHER vendored-copy drift — same class as the R16 lesson; consider whether the engine can be shared. Small-scope rule says: port the 3 behaviors, don't restructure.)
