<!-- id:G-05 status:READY tier: round:16 -->
### G-05 — Tier-1 clinical-translation guard (CI script + curated list) — READY · M (parser ~150 lines; CURATION is the real work) · SOLID mechanics
- `Translate.elm` 30,804 lines; kirundi=Nothing ×1,071, somali=Nothing ×1,710 — but Nothing is INTENTIONAL for unit strings → curated `ci-scripts/tier1_translation_keys.txt` (danger signs/triage/referral/dosage). Two checks: (1) Tier-1 records have kirundi/somali ≠ Nothing; (2) NEW TranslationIds matching a clinical-name regex not in list/waiver → fail (forces a conscious decision at PR time). elm-format-normalized layout is line-parseable (8-space branch indent; follow one level of `translationSet OtherKey` delegation, e.g. :5490-5494). ⚠ each curated key must pass TODAY or the fill must be sourced first (content, not code).
