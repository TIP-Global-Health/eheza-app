<!-- id:B-340 status:READY tier:4 round:30 -->
### B-340 — NCD completion report: the server copy of the NCD next-steps and lab rules was not updated for B-288 or B-289 — health education still expected for Stage 1 with recurrent diabetes, the pregnancy test expected only at the first encounter, and pregnancy read from this encounter's test only — READY · CONFIRMED (both PHP sites and the client diff read by the orchestrator) · LOW · tier 4 · R30 unit 3 · ⚠ joins the gated Completion-report batch (B-179…B-187, B-246, B-284: "is the Completion report used?") · the client half is merged, NOT deployed — the drift starts at the next release
- **Mechanism.** `git diff 62b029b9d 19a26a9b3 -- server/hedley/modules/custom/hedley_reports` is empty; the client changed in `Pages/NCD/Activity/Utils.elm`:
  ```elm
  -- client/src/elm/Pages/NCD/Activity/Utils.elm:145-148 (after #2220 / #2222)
                  && (not <| diagnosedAnyOf (DiagnosisRenalComplications :: diabetesDiagnoses) assembled)
                  ...
                     (not <| patientIsPregnant assembled)
  -- :896-899
          TaskPregnancyTest ->
              -- Pregnancy status can change between encounters, so the test
              -- is offered at every one of them.
              isPersonAFertileWoman currentDate assembled.person
  ```
  The server still has the old rules:
  ```php
  // server/hedley/modules/custom/hedley_reports/hedley_reports.module:7051-7053
        case 'ncd_pregnancy_test':
          if (empty($last_date)) {
  // :7110-7117 — pregnancy from THIS encounter's test only
    $pregnancy_test = $measurements_by_type['ncd_pregnancy_test'];
  // :7131-7136
      empty(
        array_intersect(
          $current_encounter_diagnoses,
          ['renal-complications', 'diabetes-initial']
  ```
  (1) Stage 1 + `diabetes-recurrent` at the current encounter (the B-288 point-of-care case): client offers no health education, server expects it → encounter permanently incomplete. (2) Pregnancy test at a later encounter: client offers it, server does not expect it → performed tests uncounted. (3) Earlier positive pregnancy carried by the client (9-month rule) with today's test not positive: client suppresses health education and refers to ANC, server expects health education.
- **Reachability.** The completion generators run over every NCD encounter (drush scripts feeding the Completion report).
- **Impact.** Completion report only, not care. (1) produces false incompletes from deploy on; (3) has ~zero live exposure (scout: 1 positive NCD pregnancy test fleet-wide, none with a later encounter).
- **Backlog de-dup.** B-182 (implemented — fixed the previous drift in this same function: 45-49 fertility window + pregnancy-test validity); the batch items B-179…B-187 are prenatal/other programs.
- **Fix shape (guess).** Add `diabetes-recurrent` to the `:7134` list; drop the `empty($last_date)` gate for `ncd_pregnancy_test` (keep the fertile-woman check); carry pregnancy across earlier encounters with the client's rule. Only after the batch's gate is answered.
