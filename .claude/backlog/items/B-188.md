<!-- id:B-188 status:IMPLEMENTED tier:3 round:20 -->
### B-188 — Dashboard ANC: the "# of Women with 4 Visits or more" card never counts visits — any pregnancy with ONE nurse encounter counts — ✅ IMPLEMENTED 2026-08-19 (issue #2104, PR #2105, branch `b188-four-visits-counts-visits` off origin/develop) · CONFIRMED · was tier 2 · R20 wave 4
- `countPregnanciesWith4VisitsOrMoreForSelectedMonth` (`Pages/Dashboard/Utils.elm:1034-1060`): the predicate is `facilityFilter && dateConcludedFilter` where facilityFilter = `List.any isNurseEncounter` over encounters-to-month-end — **no `>= 4` anywhere**. Label "# of Women with 4 Visits or more" (Translate.elm:30167), rendered on the nurse ANC dashboard (View.elm:~1287). Every OTHER "4 visits" consumer (child scorecard, ANC report) actually counts.
- ⭐ **THE SPEC EXISTS AND SETTLES THE OPEN QUESTION — issue #966 "Health Center Dashboard - ANC":** *"Number of women with 4 visits or more (include CHW and HC visit) | Number of women who have had 4 ANC visits or more **(in any context)** AND who have had an encounter at the HC"*. TWO conditions; the HC one was implemented, the ≥4 one never written. So "nurse-only vs all-type" was NOT a product decision to make — **all types count**, and I was wrong to present it as open. ⭐ **Look for the originating issue before calling something a product question.**
- 📌 **Provenance (`git log -S`):** introduced in `a37d13628` *"WIP: adjust second row [ci skip]"*, 2023-12-06, on the **issue-968 branch (an NCD dashboard)**, merged via **PR #976 "HC Dashboard - NCD"**. An ANC card written inside a WIP commit on an NCD branch — #966's definition table was never in front of that PR's reviewer. Wrong for ~20 months.
- ✅ **Shipped (user-authored shape, refined):** `encountersFilter` = `List.length filtered >= 4 && List.any isNurseEncounter filtered`, where `filtered` excludes postpartum. New local `isPostpartumEncounter` (`[NursePostpartumEncounter, ChwPostpartumEncounter]`) sits beside `isNurseEncounter`; no such predicate existed.
- ⚠ **User decision 2026-08-19: postpartum excluded from BOTH halves** — "The condition of at least one HC encounter also meant to be non postpartum." (Note `isNurseEncounter` itself INCLUDES `NursePostpartumEncounter`, so filtering first is what makes the HC clause non-postpartum.)
- ✅ **Checked, per the user's question, that nothing filters postpartum upstream — nothing does.** `hedley_stats_get_prenatal_data()` conditions on the PARTICIPANT type (`field_encounter_type = 'antenatal'`), never on `field_prenatal_encounter_type`; the encounter query filters only by participant; decoder takes `encounters` verbatim; `generateFilteredData` filters by VILLAGE only. Corroborating tell: `isNurseEncounter` has to name `NursePostpartumEncounter` explicitly, which it would not need if postpartum were already gone.
- 🅿 **Dedup by date NOT included** (raised, not taken): the scoreboard counts raw dates while the client dedups — that is B-190, still open, and both should move together. NOT completion-gated: this is the in-app dashboard, so it corrects itself on deploy with no regeneration.
