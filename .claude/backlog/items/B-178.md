<!-- id:B-178 status:IMPLEMENTED tier:3 round:20 -->
### B-178 — Completion report: prenatal MMS and Mebendazole rows show EACH OTHER's numbers (wire-token swap) — ✅ IMPLEMENTED 2026-08-18 (issue #2100, PR #2101, branch `b178-mms-mebendazole-swap` off origin/develop) · CONFIRMED · tier 3 · affects every post-medications-change prenatal encounter, rwanda+burundi
- PHP emits `prenatal_mms => 'd1'`, `prenatal_mebendazole => 'e1'` (PHP:7656-7657); decoder maps `"d1" -> PrenatalMebendazole`, `"e1" -> PrenatalMMS` (DEC:~608-613). MMS (nearly every first encounter) vs Mebendazole (EGA≥24, once) have very different profiles — both rows far off. Fix: swap the two decoder cases — stored blobs already use the PHP convention, so NO data regeneration needed. (Do not "fix" the PHP side: that WOULD need regeneration.)
- ✅ **Verified MECHANICALLY, not by eye: all 41 tokens the two sides share were compared** (regex the PHP `'prenatal_x' => 'tok'` map against the Elm `"tok" -> Just PrenatalX` cases, normalising snake/camel). **Exactly 2 mismatched — d1 and e1, swapped with each other**; after the fix, 0. That ruled out the thing worth ruling out: a systemic off-by-one across the whole list rather than a two-case swap. ⭐ Reusable check whenever a wire-token map is suspected.
- 📌 Second decoder site is a RED HERRING: `server/elm/src/Backend/Reports/Decoder.elm:394/397` also has `"d1"`/`"e1"`, but that is the DIAGNOSIS token space (`DiagnosisPPROM` / `DiagnosisHyperemesisGravidum`) — untouched.
- ⚠ **`hedley_general/js/elm-main.js` REBUILT** — `Backend/Completion/Utils.elm` is in the server/elm app, and CI never rebuilds or compares that committed bundle. Diff +2/−2, i.e. only the two case arms. Same trap as B-151.
- 📌 `server/elm` has NO test suite at all (no `tests/`, no `*Test*.elm`), so the token comparison is the verification; compile is 84 modules + elm-review clean.
