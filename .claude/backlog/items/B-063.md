<!-- id:B-063 status:READY tier:4 round:14 -->
### B-063 — Malformed clinic list: subsequent same-type clinics wrapped in a nested array — READY · CONFIRMED-malformation, ❌ RUNTIME IMPACT REFUTED · LOW (one-line cleanup; the original "PMTCT HC-stats undercount" headline is WRONG — D7 expandArguments flattens the nested element and it matches)
- `hedley_health_center.module:39-46` `hedley_health_center_get_clinics_of_health_center`: seeds `$clinics_by_type[type] = [$row->nid]` (scalar) but appends `$clinics_by_type[type][] = [$row->nid]` (a nested `[nid]` array) → for N same-type clinics: `['pmtct' => [nid1, [nid2], [nid3], …]]`; docblock + seed both intend a FLAT list. No `orderBy` → which clinic is the scalar index-0 is arbitrary.
- ❌ RUNTIME IMPACT REFUTED (verification sweep 2026-07-06, empirically tested against D7.105 core code): `DatabaseCondition::compile` gives each IN element its own placeholder with the value AS-IS, then `DatabaseConnection::expandArguments` (database.inc:793-826) EXPANDS any array-valued argument — the nested `[nid2]` flattens to `nid2` and MATCHES. Test run: final args all scalars, boundary `\b` guard verified. The EFQ/FBF consumer also tolerant (per-element fieldCondition; `is_array → IN` default). No 'Array' cast, no notice (except a cosmetic watchdog '@clinic' stringification in the truncation-warning path).
- DOWNGRADED to trivial cleanup: malformation itself is real (`hedley_health_center.module:39-46`, self-verified) and latent-fragile (any future consumer doing strict compares breaks). Fix: `$clinics_by_type[$row->field_group_type_value][] = $row->nid;` (drop the `[...]` wrapper) — one line, LOW.
