<!-- id:B-330 status:READY tier:4 round:29 -->
### B-330 — Group "Begin new encounter" page: its fetch is empty, so after a reload at that URL the Group Assessment button is dead, and with a forgotten `sessionsByClinic` it creates a second session for the day — READY · CONFIRMED (fetch, view reads and PostSession path read) · LOW · tier 4 · R29 unit 4
- **Mechanism.** `client/src/elm/Pages/GroupEncounterTypes/Fetch.elm:6-8` `fetch = []`, but the view reads `db.clinics` via `getVillageClinicId` and `db.sessionsByClinic` (`View.elm:72-79`): no clinic → `NoOp` (`:107`); no session today → `PostSession` (`:91-104`). R16 traced the page as masked by `Pages/Clinical/Fetch.elm` (`FetchClinics`, `FetchSessionsByClinic`). Unmasked: (a) reload at `#group-encounter-types` (auto-login) → `clinics` never fetched → silent dead button; (b) `clinics` kept alive by other pages while `FetchSessionsByClinic` — last wanted on Clinical — is forgotten after the 5-minute window (`App/Update.elm:1420-1478`), or still `Loading` on a fast tap → `clinicSessions = []` → a second `PostSession` for the day; `PostSession`'s in-flight guard (`Backend/Update.elm:4694`) does not cover it (the B-195 db-lag shape).
- **Fix shape (guess).** Emit `FetchClinics` and `FetchSessionsByClinic` for the village clinic from this page's fetch, mirroring `Pages/Clinical/Fetch.elm`.
- ⭐ **R34 unit 1 — corroborated from outside the Elm.** Two e2e helpers carry reload loops written against exactly this symptom: `client/e2e/helpers/group-session.ts:71-73` comments *"The GroupEncounterTypesPage fetch doesn't load clinics data. Navigate to ClinicsPage first to trigger FetchClinics"* and runs a 5-attempt loop ending `'AttendancePage not reached after clicking Child Nutrition. Village clinic data may not be synced.'`; `client/e2e/helpers/education-session.ts:29-48` does the same. That is empirical evidence the dead-button branch (`View.elm` `groupAssessmentButtonAction … |> Maybe.withDefault NoOp`) fires in practice, not only after a reload.
- **R36 unit 4 — nurse-side sibling recorded as B-430:** the nurse Clinics page has a fetch, but it is suppressed while the authority is `Uploading`, so the same "no session today" default fires there; live-sized on ihangane (582 same-device duplicate clinic-days).
