<!-- id:B-250 status:READY tier:3 round:25 -->
### B-250 — Dashboard stats: soft-deleted people still appear in case management, beneficiaries and attendance, so a merged-away duplicate keeps being listed and counted — READY · CONFIRMED (four unfiltered sites + the soft-delete writer verified) · MED · tier 3 · R25 unit 8
- **This is the explicitly-deferred residual of B-032.** That item's scope note says *"person fields … = B-033 (person-side)"*, and B-033 closed it for **one** place only: the education-session participants join (`hedley_stats.module:2344-2347`, via `hedley_stats_exclude_deleted_from_join`).
- **Mechanism.** Four other sites load person nodes and ship their details with no `field_deleted` filter: `hedley_stats_get_case_management_for_period` (`:3228-3240`, `node_load_multiple($ids)` → `hedley_stats_add_patient_details()`), `…_children_beneficiaries_group_stats` (`:2516-2536`, adults and children), `…_children_beneficiaries_individual_stats` (`:2603-2615`), and `…_session_attendance_stats_by_health_center` (`:2805-2824`). Soft delete only sets the flag and leaves `status` published (`hedley_admin_mark_as_deleted_action`, verified), so `node_load_multiple()` returns the node and `hedley_stats_add_patient_details()` (`:3969-3979`) emits its name and gender. The base queries exclude deleted *measurements* and *participations* — never the referenced person.
- **Reachability.** A patient merge or the VBO mark-as-deleted action soft-deletes the duplicate; any measurement or `pmtct_participant` still pointing at them brings them back on the next recalculation, permanently. B-199 measured **41 un-deleted measurements of soft-deleted persons on ihangane**, so the producing state is present in live data.
- **Impact.** The duplicate a merge was meant to remove is listed as its own row in Case Management and counted again in the beneficiaries and missed-session tables, with their name shipped in `patients_details`. Tens of records, all sites.
- **Fix shape (guess).** Reuse `hedley_stats_exclude_deleted_from_join()` on the `field_person` / `field_adult` joins, or filter after `node_load_multiple()`. The case-management and beneficiaries loops already skip rows with no birth date, so a skip path exists to hang it on.
