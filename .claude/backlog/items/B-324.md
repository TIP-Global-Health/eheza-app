<!-- id:B-324 status:READY tier:1 round:29 -->
### B-324 — Acute Illness diagnosis: on a site without Tuberculosis Management, a COVID-suspect patient with a cough of more than two weeks can never be diagnosed with COVID — the COVID path exits "to diagnose TB suspect" unconditionally, but TB suspect is only diagnosed when the feature is on, so the encounter falls through to Fever of Unknown Origin / complicated RI / GI with a positive COVID RDT on record — READY · CONFIRMED (both paths, the lab gate, the writer and both introducing commits read by the orchestrator) · MED-LOW · tier 1 (diagnosis rule) · R29 unit 1 · ⭐ DEPLOYED, tip-somalia only
- **Mechanism.** The initial-encounter resolver (`client/src/elm/Pages/AcuteIllness/Activity/Utils.elm:2434-2446`) tries `covid19DiagnosisPath` first and falls back to `nonCovid19DiagnosisPath`. The COVID path (`:2503-2514`) returns `Nothing` when `isChw || not covid19SuspectDiagnosed || coughForMoreThan2Weeks measurements`, with the comment *"we must diagnose Tuberculosis suspect. Therefore, we need to exit COVID19 path"* — it does not receive `features`. The non-COVID path (`:2616-2624`) diagnoses `DiagnosisTuberculosisSuspect` only when `tuberculosisManagementEnabled features && coughForMoreThan2Weeks`; otherwise, because COVID suspicion requires fever (`covid19SuspectDiagnosed :2491-2497`), it goes to `resolveAcuteIllnessDiagnosisByMalariaRDT` (`:2653-2672`), whose RDT is non-positive by construction → `DiagnosisRespiratoryInfectionComplicated` / `DiagnosisGastrointestinalInfectionComplicated` / `DiagnosisFeverOfUnknownOrigin`. The COVID RDT task is still offered (`LaboratoryCovidTesting -> not isChw && covid19SuspectDiagnosed`, `:2021-2023`), so the nurse can record a positive COVID RDT the engine cannot act on. History: the exit was added unconditionally in `ab3d0f982` (2024-03-13); the feature gate was added to the sibling path only, eight days later, in `b05e10417` (2024-03-21) — a copy-adapted pair where one half was not updated.
- **Reachability.** Nurse, initial encounter: fever (or temp ≥ 37.5) + cough with duration "more than 2 weeks" + one more respiratory symptom or two general symptoms → physical exam → malaria RDT non-positive → resolver runs from `Pages/AcuteIllness/Encounter/Utils.elm:91-93` and on each of the 14 measurement revisions (`Backend/Update.elm:9865-9876`); the first-encounter writer (`:9886-9906`) persists via `SetAcuteIllnessDiagnosis`. CHW cannot diagnose COVID, so nurse-only.
- **Impact.** Site-specific: `hedley_admin_feature_tuberculosis_management_enabled` is set on ihangane, vhw and uvl and **absent on tip-somalia** (`hedley_admin.module` reads `variable_get(…, FALSE)`; R27 flag snapshot in the coverage lessons). On tip-somalia the stored diagnosis is wrong; Next Steps route to Send-to-HC with explicitly no follow-up (`:2140-2143`), no contact tracing, no COVID severity grading; the progress report carries the wrong label. On the three TB-on sites the behaviour is as designed (TB suspect wins). Not live-sized: needs acute-illness encounters on tip-somalia with `symptoms_respiratory` cough duration = 14 and a `covid_testing` result — write the query when building.
- **Evidence.** Both paths, `resolveAcuteIllnessDiagnosis`, the lab gate, `tuberculosisManagementEnabled` (`Backend/Utils.elm:388`) and both commits read by scout and orchestrator at `62b029b9d`. `Pages/AcuteIllness/Activity/Test.elm:617-631` pins only the TB-on case; nothing pins TB-off + chronic cough. Backlog: `covid19DiagnosisPath` appears only in TH-17 (test hardening); the symptom is unrecorded.
- **Fix shape (guess).** Thread `features` into `covid19DiagnosisPath` and make the exit `tuberculosisManagementEnabled features && coughForMoreThan2Weeks`, or hoist the TB check above both paths in `resolveAcuteIllnessDiagnosis`. Discriminating test: `EverySet.empty` features + the COVID fixture (`Test.elm:641-649`) + cough at `symptomMaxDuration`, expecting a COVID diagnosis (fails on develop).
- **Clinician-gated observation, not an item.** On TB-on sites the same chronic cough also outranks a *positive* malaria RDT (`:2620` precedes `:2623`) while the malaria RDT task is still offered on the first encounter — whether TB suspect should trump confirmed malaria is a precedence question for a clinician (declined class), noted here only.
