<!-- id:B-037 status:IMPLEMENTED tier:1 round:10 -->
### B-037 — Prenatal Medical History: `preeclampsiaInFamily` required by the payload but NOT counted → Save at "4/4" silently discards the ENTIRE measurement — ✅ IMPLEMENTED 2026-07-05 (issue #1890, PR #1891, branch `b037-medical-history-preeclampsia-task`, base develop @3cd2676a5, CI running, Copilot requested). E2E-expansion follow-up (partial-fill "Save must stay disabled at 4/5" assertion in `prenatal-encounter-nurse.spec.ts` — would guard the whole count-vs-save class incl. B-072) was analysed and DECLINED "at this stage" by user 2026-07-05 — do not re-pitch unprompted; the analysis (helper always answers preeclampsia at `helpers/prenatal.ts:399-404`, suite never asserts Save-disabled on omitted required field) is in session fe67129f tail. Fix: added `maybeToBoolTask form.preeclampsiaInFamily` as the 5th task in `medicalFormInputsAndTasks` (View.elm). elm make Main Success (548), elm-format clean, elm-test 2749 passed. No bespoke test (mechanical 1-line list append, compiler-verified; count helper unexposed View fn).
- RENDERED: `Pages/Prenatal/Activity/View.elm` ~:3064 (2nd of 5 questions in `medicalFormInputsAndTasks`, select `[DoesOccur, DoesNotOccur] [NotKnownIfOccurs]`, nothing forces an answer).
- COUNTED: same function's tasks list has only FOUR `maybeToBoolTask` entries (signs/physicalConditions/infectiousDiseases/mentalHealthIssues) — preeclampsiaInFamily absent → `tasksCompleted == totalTasks` at 4/4 → Save enables.
- SAVED: `Pages/Prenatal/Activity/Utils.elm:4286-4291` `Maybe.map5 MedicalHistoryValue … form.preeclampsiaInFamily` — Nothing ⇒ whole value Nothing; `saveMeasurementMsgs` (`Pages/Utils.elm:62-64`) `|> Maybe.withDefault []` ⇒ NO backend msg, NO error, and `sequenceExtra … (generateHistoryMsgs nextTask)` (Update.elm:831) still navigates as if saved.
- Trigger: FIRST-time Medical-History entry (no saved measurement to backfill the field) with the preeclampsia question left untouched → all four answered question-groups silently discarded; task never completes; data gone on encounter exit. On edit-of-existing the decoder default (`NotKnownIfOccurs`) backfills, so first visits only.
- INDEPENDENT of R9-1/#1877 (that fixed only the ENCODER for this field; verify at impl whether #1877 merged — no overlap in lines). Fix (preferred): add `maybeToBoolTask form.preeclampsiaInFamily` as the 5th task (forces an answer, matches payload contract, consistent with siblings); alt: default the 5th map5 arg to `NotKnownIfOccurs`. Severity HIGH (silent clinical data loss, prenatal first visits).
