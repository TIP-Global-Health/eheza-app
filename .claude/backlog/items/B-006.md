<!-- id:B-006 status:UNKNOWN tier:4 round: -->
### B-006 — photos.js catch returns `undefined` from respondWith — ❌ IMPACT REFUTED 2026-07-06 (verification sweep) · ⚠️ DO NOT "FIX" NAIVELY · ❌ REFUTED (impact; do-not-fix tombstone)
- Code smell confirmed verbatim (photos.js:46-53 catch returns hoisted undefined) BUT impact refuted: undefined respondWith → browser network error → elm/http `Http.NetworkError` → `BackendDeferredPhotoFetchHandle` (SyncManager/Update.elm:1786-1789) treats NetworkError as "offline, doesn't qualify as an attempt" → clean retry, attempts NOT incremented. The recorded fix (`return Response 503`) would be WORSE: non-network Failure increments attempts and photos with attempts>2 are permanently skipped (app.js:1056-1058). Only reachable path is the SyncManager single-photo fallback (token-carrying `/system/files/` GET; display `<img>` requests get a clean 404 at :36-44). Residual harm: one SW-console TypeError per drop. Do not touch without pairing an Elm-side attempts change; effectively working-as-desired by accident.
