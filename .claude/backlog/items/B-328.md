<!-- id:B-328 status:READY tier:4 round:29 -->
### B-328 — Role gating: `viewUserPage` authorises by location only, so a lab technician or CHW reaches every nurse page through the URL or the Back history left by a previous nurse session — READY · CONFIRMED (gate and URL handling read) · LOW · tier 4 · R29 unit 3 · ⚠ adjacent to the declined device-trust class — park if judged so
- **Mechanism.** `client/src/elm/App/View.elm:449-452` checks `nurseAuthorizedForLocation` and then dispatches every `UserPage` unconditionally; `isLabTech` is only passed into a few pages to *shape* them. Role is enforced solely by which cards the main menu renders (`Pages/PinCode/View.elm:284-296`). `UrlChanged` (`App/Update.elm:1494-1546`) sets `activePage` straight from the fragment; logout clears neither `activePage` nor history.
- **Reachability.** Lab tech logs in on a device whose previous nurse session left `#clinical`/encounter entries in history; Back lands on a fully functional nurse page with `isChw = False`. Traced, not run. Role values themselves are consistent: server `field_role` `admin|nurse|chw|lab-tech` = `decodeRole` exactly.
- **Fix shape (guess).** A per-role page allow-list in `viewUserPage`, falling through to the PinCode view as the location check does.
