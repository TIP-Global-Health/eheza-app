<!-- id:B-252 status:IMPLEMENTED tier:4 round:25 -->
### B-252 — NCD progress report: the Partner HIV lab row ignores its own display flag, so every NCD report shows a permanently blank Partner HIV line — ✅ IMPLEMENTED (issue #2198, PR #2200, branch `B-252-ncd-partner-hiv-row`, base `bff5c5233`) · CONFIRMED (anchor + both consumers verified) · LOW · tier 4 · R25 unit 9
- **Mechanism.** `LabsResultsDisplayConfig` (`Pages/Report/Model.elm:12`) carries `partnerHIV : Bool`, but the entry at `Pages/Report/View.elm:992` is the only configurable row in the `LabResultsCurrentMain` list with **no** `|> showIf displayConfig.partnerHIV` — its neighbours (`hivPCR` at :991, `syphilis` at :994) both have theirs. The field is written by both consumers and read by nobody.
- **Reachability.** `Pages/NCD/ProgressReport/View.elm:210` sets `partnerHIV = False` and `:862` sets the results list to `[]`, but the row renders anyway: `viewLabResultsEntry` short-circuits to `emptyNode` only when `isResultsReviewer && totalResults == 0`, and NCD hard-codes `isResultsReviewer = False` (:200-206). It is inside the WhatsApp-shared `ComponentNCDLabsResults` block, so exported reports carry it too. Prenatal is unaffected (`Pages/Prenatal/ProgressReport/View.elm:355` sets `True`).
- **Impact.** Every NCD encounter's lab pane shows `Partner HIV | (blank) | (blank) | Negative` — a nurse could read the blank as "not done" for a test NCD does not have at all. All sites where NCD is enabled. `f0062e800` (2023-02-06) added the config field, both assignments and the row — and omitted the `showIf`.
- **Fix shape (guess).** Append `|> showIf displayConfig.partnerHIV`, matching its siblings.
- ✅ **Built 2026-09-03.** One line: `|> showIf displayConfig.partnerHIV` appended, matching `hivPCR` above and `syphilis` below. ⚠ **No regression test** — the row is built inside `viewLabResultsPane`, a view function returning `Html`; the [[B-124]] class. Verified: elm-format clean · elm-test 3,062 passed · `elm make src/elm/Main.elm` 548 modules · elm-review clean on the committed state via a fresh clone.
- ✅ **Reviewed 2026-09-03 — no findings.** The review confirmed this line is the field's ONLY read, so nothing else changes; both construction sites already set it (`Pages/NCD/ProgressReport/View.elm:210` False, `Pages/Prenatal/ProgressReport/View.elm:355` True), so prenatal is byte-identical and only NCD changes; both `viewLabResultsPane` call sites in each file share the same `labResultsConfig`, so the confirmation / lab-tech path is gated the same way; and ⭐ **`LabResultsHistoryPartnerHIV` is reachable only by clicking the now-hidden row**, so hiding it strands no navigable history mode — the one thing worth checking that the build had not.
