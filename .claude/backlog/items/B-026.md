<!-- id:B-026 status:IMPLEMENTED tier:1 round:10 -->
### B-026 — Deferred-photo download lane wedges permanently on mid-body network drop — ✅ IMPLEMENTED 2026-07-06 (issue #1898, PR #1899, branch `b026-bulk-photos-reply-guarantee`, base develop @91fb401fb, CI running, Copilot requested). Fix: try/catch on the 2 unguarded awaits in bulkPhotos.js returning `{batchError: 0, error: String(e)}` + belt-and-braces `.catch` in the app.js subscriber. node --check clean; discrimination harness drove the REAL handler (stubbed fetch/caches): OLD rejected unreplied on mid-body drop + caches.open failure, NEW returns decodable batchError, happy path identical. [HIGH REVIEW 2026-07-20: no findings — `var x; try{x=await…}catch{return{batchError:0,error}}` scoping correct (early-return on throw ⇒ never uninitialized); decodeBulkPhotoFetchHandle = oneOf[field batchError int|>Err, field results…|>Ok] so extra `error` ignored → Err 0; Elm subscription wraps in Result.withDefault (Err 0) = its own belt-and-braces; handler already treats Err N as retryable.]
- JS: `bulkPhotos.js:49` `var buf = await response.arrayBuffer();` and `:70` `caches.open` are the ONLY un-caught awaits (fetch/manifest-parse/cache.put all caught; file even documents hardening this exact abort class for RangeError — these two were missed). `app.js:598-601` subscriber: `const outcome = await handleBulkPhotoFetch(params); …send(outcome)` — rejection skips the send.
- Elm: `SyncManager/Update.elm:936/:957/:989/:1009` `DownloadPhotosBatch/All → if isLoading … then noChange`, no timeout → `backendRemoteData` Loading forever.
- Trigger: connection drop while a multi-MB 100-photo batch body streams — routine on rural networks. Fix: try/catch around arrayBuffer+caches.open returning `{batchError: 0}` (decoded as retryable) + belt-and-braces catch in the app.js subscriber sending `{batchError: 0}`. Severity MED-HIGH.
