<!-- id:B-062 status:IMPLEMENTED tier:2 round:13 -->
### B-062 — Education-session PATCHes rebuild the full node from the async-updated cache, not the authoritative Pages ViewMode → timing-race lost update (most plausibly drops the last-checked-in participant on immediate finalize) — ✅ IMPLEMENTED 2026-07-12 (issue #1927, PR #1928, branch `b062-education-session-lost-update`, base develop @2c0322070, commits 354ee396c + a39fc0207, ALL 10 CI CHECKS GREEN on head, Copilot round addressed: 1 comment-only finding, resolved by the refactor). User chose **Option 1 (optimistic cache write)**. Fix: new `Backend/EducationSession/Utils.applyUpdateToSessions sessionId subMsg sessions` (on `Update updateFunc` → `Dict.insert sessionId (Success <| updateFunc session)`; `HandleUpdated _` → unchanged), called from the `MsgEducationSession` arm of `Backend/Update.elm` — every later patchFull then rebuilds from a current cache; closes all fields/interleavings at once. NEW test module `Backend/EducationSession/Test.elm` (5 tests). Verification: elm make Success, elm-test 2794 passed, elm-format + elm-review (COLD) clean; DISCRIMINATION = reverting the helper's `Update` branch fails exactly the 3 race tests, while "non-Update msg leaves cache as is" + "update for an unheld session is a no-op" still pass.
- 🔥 **CI LESSON (cost me a red run): a Test.elm that imports `Backend/Update.elm` OOM-kills the `test_elm` CI job** (`elm make failed with exit code null`). Backend.Update pulls in ~the whole app (548 modules); elm-test otherwise compiles only test-reachable modules, and `ci-scripts/test_elm.sh` does NOT add the swap that `install_client.sh` does. FIX = extract the logic under test into a small pure helper module and test THAT — never import Backend.Update (or App.Update) from a test. Importing a big `Pages/*/View.elm` is fine (B-067's test does, CI green).
- ⚠ Elm gotcha hit: `SyncManager.Model.SyncInfoStatus(..)` exports a `Success` that collides with `RemoteData.Success` — import Flags/Site only and qualify `SyncManager.Model.NotAvailable`.
- `Backend/EducationSession/Update.elm` `Update updateFunc` applies updateFunc to `maybeSession` (= `Dict.get sessionId model.educationSessions`, Backend/Update.elm:4054) then `sw.patchFull` (FULL entity replace); `HandleUpdated` records only status, never writes the entity back. Each Pages msg is a one-field read-modify-write: `SaveTopics → {session|topics}`, `SaveAttendance → {session|participants}` (fired from every `ToggleAttendance` with the full ViewMode set), `EndEncounter → {session|endDate}` — so each PATCH carries the OTHER fields from the cache.
- **CORRECTED the agent's "offline never advances / finalizes empty every time" (that would be a guaranteed visible break):** the cache DOES advance after each offline PATCH — nodes.js:180 (PATCH) → :250 `sendRevisedNode` → `sendRevisions([item])` echoes the written node → `EducationSessionRevision` (Backend/Update.elm:5774) refreshes `model.educationSessions`. But asynchronously (SW write → postMessage → Elm sub → handleRevision, ~tens of ms, local). So it's a RACE: a user action that fires before the prior PATCH's echo lands rebuilds from the pre-echo cache. Most realistic manifestation: tap last participant (SaveAttendance) → immediately tap "Record Group Education" (EndEncounter) before the echo → EndEncounter PATCHes participants = cache-before-last-toggle → LAST participant dropped. Topics-loss variant is less likely (human gap after topic selection lets its echo land). Participants aren't lost by staleness alone because ToggleAttendance re-sends the full set each time — only the finalize-beats-echo window and cross-field (topics↔participants) interleaving lose data.
- Fix (agent's (c), sound): have EndEncounter (and each save) carry the full intended state from the Pages ViewMode instead of rebuilding from the async cache; or optimistically apply updateFunc into `model.educationSessions` in HandleUpdated. Severity MED (narrow race, local echo usually wins; single-participant/topic loss, silent). Note B-046 phantom `""` participant is threaded through the same ViewMode and would be finalized here.
