<!-- id:B-034 status:WONTFIX tier:4 round:10 -->
### B-034 — GWG backfill script computes/writes indicators from soft-deleted prenatal content — ❌ DROPPED 2026-07-03 (user: `backfill-prenatal-gwg-indicators.php` is a RETIRED ONE-SHOT, already ran, not re-runnable → code gap is real but MOOT; do NOT re-propose). Confirms the R5 lesson: the hedley_reports/hedley_admin `scripts/*` backfill+report family are frequently retired one-shots or unused — verify liveness FIRST.
- `hedley_reports/scripts/backfill-prenatal-gwg-indicators.php`: base pregnancy query :60 uses the excluding-deleted builder, but the 4 inner db_selects (:183/:209/:231/:260 — encounters, LMP, ultrasound, prenatal_nutrition) have no deleted join (soft-delete doesn't unpublish, so `status=1` doesn't help). Deleted encounters both poison the pregnancy baseline (LMP/EDD/booking weight) used to classify LIVE encounters AND get verdicts written onto them.
- Fix: add the leftJoin+isNull-or-0 pattern (as `hedley_reports.module:1869-1876`) to all four queries. GATE: confirm this Healthy-Start backfill is still run/re-runnable (R5 lesson: verify script liveness first).
