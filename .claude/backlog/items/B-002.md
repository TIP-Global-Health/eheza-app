<!-- id:B-002 status:IMPLEMENTED tier:2 round: -->
### B-002 — Dashboard follow-up `setMonth` bug under-counts follow-ups ~half the year — ✅ IMPLEMENTED 2026-07-07 (issue #1906, PR #1907, branch `b002-followup-setmonth`, base develop @502d1cd73, CI running, Copilot requested). Fix: one-line `today.getMonth()` → `resolutionDate.getMonth()` at nodes.js:657. Anchor held at :657. node --check clean; scratchpad discrimination harness (b002_discriminate.js) reproduced the exact keep/drop block → OLD drops a within-window follow-up (concluded 2025-11-15, today 2026-02-10), NEW keeps it AND still drops genuinely-expired >6mo ones; year-sweep showed OLD wrongly drops a 3-mo-old follow-up in Jan/Feb/Mar.
- `client/src/js/nodes.js` ~:657: `resolutionDate.setMonth(today.getMonth() + 6)` — should be `resolutionDate.getMonth() + 6`.
- Mechanism: for today in Jan–June, follow-ups resolved late in the prior year are wrongly dropped from dashboard counts. One-line fix; `node --check` only (no JS unit framework).
