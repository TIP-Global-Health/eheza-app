<!-- id:B-133 status:UNKNOWN tier:4 round:17 -->
### B-133 — Reports: Acute-Illness "no diagnosis" row reads the LATEST encounter's diagnosis; initial encounter is where diagnosis lives — PLAUSIBLE (frequency depends on follow-up re-derivation; re-verify before coding) · LOW · READY
- `server/elm/src/Pages/Reports/View.elm` ~:2357-2372: `sortByDateDesc .startDate >> List.head`. An illness diagnosed at visit 1 whose last follow-up derived no diagnosis counts in "No diagnosis" AND in its diagnosis row (per-encounter `diagnosesCountDict`); "Total" also double-counts re-diagnosed follow-ups. Agent-traced into client `Backend/Update.elm` ~:9840-9990 diagnosis derivation; not confirmed how often follow-ups carry empty diagnosis in practice.
- **R17 server/elm observations (spec-dependent, NOT queued — decline-class unless a stakeholder rules):** Completion's `eliminateEmptyEncounters` inflates completion % and is inconsistent with the nutrition-group report; demographics "unique" totals double-count nurse+CHW; prenatal-contacts exact-equality rows; z-score `<=` vs PHP `<=`-vs-`<` MUAC boundary conventions; edema-without-MUAC not classified SAM; `record.created == targetDate` excluded by strict LT.
