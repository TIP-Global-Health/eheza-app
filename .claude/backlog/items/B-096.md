<!-- id:B-096 status:SKIPPED tier:2 round:16 -->
### B-096 — advancedqueue_release_timeout=0: crashed PROCESSING items are never re-claimed — ⏭ SKIPPED by user 2026-07-13 ("No, lets skip this") — do NOT re-pitch. Mechanism CONFIRMED and **OBSERVED ON PRODUCTION** (see live evidence below); the fix is understood but was declined.
- 🔎 **LIVE EVIDENCE (read-only check, 2026-07-13 — user asked "did you check the live site?"; the code-grep answer was insufficient):** variable is OFF on all 3 sites — `ihangane.live` has it EXPLICITLY set to `'0'` (almost certainly from someone saving `admin/config/system/advancedqueue`, a system_settings_form that writes the default straight back), `vhw.live` + `tip-somalia.live` have no row at all (default 0). ihangane.live carries **6 zombie rows** (status=PROCESSING, expire≠0, ALL at attempt=1 — proving max_attempts only fires on re-claim): 3× hedley_stats_calculate_stats stuck 135/143/**154 days** (since Feb 2026), 1× hedley_reports_calculate_reports_data + 2× views_bulk_operations_active_queue_* stuck 62 days (since 2026-05-12). vhw/tip-somalia are clean.
- ⚠ **The fix is NOT the recorded one-liner alone.** Enabling the release path would, on the next cron, release ALL existing zombies back into their queues and RE-RUN them — including the two stale `views_bulk_operations_active_queue_*` items from May, whose originating batch is long gone and whose operations can mutate content (mark-as-deleted etc.). Any revival MUST pair `variable_set('advancedqueue_release_timeout', 1800)` with retiring the pre-existing zombie rows (mark FAILURE) in the same update hook.
- 📌 Method lesson (generalize): `variable_get()` reads the runtime `variable` table — a grep of the repo can NEVER answer "is this variable set?". Check live with `ddev terminus-auth` + `ddev auth ssh` (interactive — ASK the user to run `! ddev auth ssh`), then `terminus remote:drush <site>.live -- vget <name>`. Sites: `ihangane` (Rwanda), `vhw` (Burundi), `tip-somalia`.
- Original entry (kept for reference):
- `contrib/advancedqueue/advancedqueue.module:340` `variable_get('advancedqueue_release_timeout', 0)` — release path gated off; set NOWHERE in hedley code (grep-verified). `claimItem()` (`advancedqueue.queue.inc:112`) selects only `status=-1 AND expire=0` → stuck PROCESSING (status 0, expire set) never re-claimed; max_attempts guard (:192-214) fires only on re-claim → unreachable ("stuck-items patch" note reconciled: that path is INERT at timeout=0). Partial mitigation: `hedley_general_advanced_queue_task_exists` (`hedley_general.module:75-81`) lets a REPLACEMENT task enqueue after 20 min (see B-086 for its own bug); zombie rows linger until 6-month cron prune (:563-567).
- Fix: `variable_set('advancedqueue_release_timeout', 1800)` in a hedley_general update hook (30 min > largest configured lease 120s). No code change; activates the existing `_advancedqueue_release_stale_items()` cron path.
