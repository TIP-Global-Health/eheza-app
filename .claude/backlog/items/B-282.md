<!-- id:B-282 status:READY tier:4 round:26 -->
### B-282 — Acute Illness participant page: with two illnesses started today, whether "New illness" is disabled, opens the existing encounter, or creates a third illness depends on dict order — READY · CONFIRMED (anchors verified; something depends on the pick) · LOW · tier 4 · R26 unit 4
- **Mechanism.** `viewManageIllnessesContent` (`Pages/AcuteIllness/Participant/View.elm:121-123`, copy at `:210-212`): `lastActiveSession = List.reverse activeSessions |> List.head`. `activeSessions` (`:90-97`) is `Dict.toList` of an AssocList (REST insertion order) sorted by `startDate` only (`sortEncounterTuples`, `Utils/NominalDate.elm:204-206`, stable). Two active illnesses started today keep insertion order, and everything at `:125-163` is computed from that ONE session: `encounterWasCompletedToday` (disables the button) and whether the action navigates to the open encounter or posts a new illness.
- **Reachability.** The ordinary same-day CHW-referral flow: CHW starts illness A today; the nurse opens the page, `:138-139` filters A's encounters to nurse types → none → "Acute Illness New" posts illness B with a nurse encounter (by design, comments `:386-389`). Back on the page, A and B are both today's: if B sorts last the button is disabled / navigates to her open encounter; if A sorts last it is enabled and posts illness C.
- **Impact.** An extra empty illness + encounter per re-tap; nurse in practice; both sites. Needs the nurse to use "New" rather than "existing" — which the page invites (first button; "existing" appears only with a diagnosis, `:176-188`). Different mechanism from B-195 (db-lag race, on hold) with the same outcome shape.
- **Fix shape (guess).** Evaluate the completed-today guard and the navigate-vs-create choice over ALL sessions started today (filtered by role), not the head of a date-only sort.
