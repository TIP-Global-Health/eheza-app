<!-- id:B-089 status:IMPLEMENTED tier:4 round:16 -->
### B-089 — Kirundi `MissedDosesOfMedication` missing leading space ("3y'imiti") — ✅ IMPLEMENTED (issue #2202, **folded into PR #2200** at the user's direction, branch `B-252-ncd-partner-hiv-row`) · CONFIRMED · trivial · user-visible micro
- `Translate.elm:13034` `… ++ String.fromInt val ++ "y'imiti"`. Fix: `" y'imiti"`.
- ✅ **Built 2026-09-03**, one character, at `Translate.elm:13118` (the anchor had drifted from :13034). Only the `val /= 0` branch is affected; the zero case takes the other branch and has no interpolation. ⚠ **Folded into an already-reviewed PR**, so `/code-review medium B-252-ncd-partner-hiv-row` no longer covers the head commit `3fc68fd84`. PR retitled and its body given both `Fixes` lines. 📌 Left alone deliberately: `String.fromInt val` is evaluated three times in that `else` branch (english, kinyarwanda, kirundi) — the repo's repeated-expression rule would put it in a `let`, but that is not this fix and would touch lines the defect does not.
