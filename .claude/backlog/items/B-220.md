<!-- id:B-220 status:IMPLEMENTED tier:2 round:23 -->
### B-220 — Acute Illness progress report: the encounter it was opened for is never rendered — a first visit's report shows no diagnosis, symptoms, vitals, treatment or actions — CONFIRMED (orchestrator-read split + view + `git show 04e4bd9a8`; identical on origin/main → LIVE since v1.14.0) · HIGH · tier 2 · both sites, CHW + nurse · R23 unit 3 — ✅ **IMPLEMENTED 2026-08-25 by the user (issue #2143, PR #2144, branch `ai-progress-report-missing-current-encounter`, merge `7ece1b2d0`) — not built from this seat.**
- **Mechanism.** Every clinical pane of the report (`Pages/AcuteIllness/ProgressReport/View.elm:126-131`: assessment, symptoms, physical exam, nutrition signs, treatment, actions taken) is fed ONLY `assembled.firstInitialWithSubsequent ++ assembled.secondInitialWithSubsequent`. Those are a split of `data.previousEncountersData` alone (`Pages/AcuteIllness/Encounter/Utils.elm:95-116`), and `generatePreviousMeasurements` (`:133-140`) explicitly drops the current encounter (`if encounterId == currentEncounterId then Nothing`). The current encounter contributes only `assembled.measurements.followUp`/`encounter.startDate` (Next Steps pane) and `assembled.initialEncounter` (treatment-pane branch).
- **Per pane, first encounter of an illness (previous list empty):** assessment → `emptyNode` (`:209-211`), no diagnosis, no "Began:"; symptoms → heading only (`:274/:384`); physical exam → header row only; nutrition signs hidden; treatment → `treatmentReview` of `List.head [] ` → nothing; actions → empty. **Subsequent encounters:** all EARLIER encounters shown, today's vitals / medication / referral / health-ed actions / assessment entry omitted; the previous encounter is labelled as current.
- **History — a regression, not a design.** `aad475e42` (2021) built the lists from `allEncountersData = data.previousEncountersData ++ [ currentEncounterData ]`. `04e4bd9a8` ("Client side adjustments", 2024-08-20, issue-1287 completion-stats branch, v1.14.0) replaced `allEncountersData` with `data.previousEncountersData` in the split (diff verified: the three `allEncountersData` uses → `data.previousEncountersData`, `currentEncounterData` left unused); `8f0f4d75b` (elm-review --fix-all) deleted the dead binding. The consumers the change targeted are in `Pages/AcuteIllness/Activity/Utils.elm:~96/~2006` ("prescribed at any of *previous* encounters") — the report was not re-wired.
- **Reachability.** `Backend/Update.elm:~10049` auto-navigates to `AcuteIllnessProgressReportPage InitiatorEncounterPage id` when Next Steps completes on an initial encounter (and on subsequent ones with no improvement); the Encounter page "Progress Report" tab (`Pages/AcuteIllness/Encounter/View.elm:~313/~329`); Patient Record / NCD / nutrition / scoreboard reports open it with the LAST encounter id via `viewAcuteIllnessDiagnosisEntry` (`Pages/Report/View.elm:1258-1275`) — excluded too. WhatsApp screenshot carries the same omission. No e2e asserts report content.
- **Fix shape (first guess):** re-introduce the current encounter into the two lists FOR THE REPORT ONLY (append an `AcuteIllnessEncounterData` built from `assembled.id/encounter/measurements`, with `diagnosis` from `assembled.diagnosis` as the 2021 code did) before splitting, while `Activity/Utils` keeps the previous-only lists; re-check the `nurseEncounterIndex` split semantics when the current encounter is the nurse initial. Discriminating e2e: first-encounter report shows the diagnosis.
- ✅ **Shipped as the entry described.** The report now builds its own pair of sequences from ALL encounters of the illness: `generateAllEncountersData` (`Pages/AcuteIllness/Encounter/Utils.elm`) appends the viewed encounter to `previousEncountersData`, and `ProgressReport/View.elm:87-89` splits that with the extracted `splitByInitialNurseEncounter`. The assembled-data lists stay previous-only and are renamed `previousFirstInitialWithSubsequent` / `previousSecondInitialWithSubsequent`, so the Malaria RDT rule cannot read its own result and Ongoing Treatment cannot be raised by medication given in the same encounter. 8 files, +245/−43, including the discriminating e2e the entry asked for in both `acute-illness-encounter-chw.spec.ts` and `-nurse.spec.ts`.
