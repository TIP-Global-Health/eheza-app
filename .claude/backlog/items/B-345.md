<!-- id:B-345 status:READY tier:1 round:30 -->
### B-345 — Prenatal recurrent Next Steps: health education counts as complete once ANY phase-2 education was saved, so education a LATER diagnosis requires (discordant couple: positive-HIV + family planning) is never shown and reads as "not given" — the health-education twin of B-336 — READY · CONFIRMED (completion predicate and form hydration read by the orchestrator; scout scratch elm-test: task grows from 2 to 4 answers, completion stays True) · MED · tier 1 (diagnosis rule — care pathway) · R30 unit 2 · all sites; unfired so far (the 110 lab-tech partner results on ihangane are all negative)
- **Mechanism.**
  ```elm
  -- client/src/elm/Pages/Prenatal/RecurrentActivity/Utils.elm:445-448
          NextStepsHealthEducation ->
              getMeasurementValueFunc assembled.measurements.healthEducation
                  |> Maybe.map (.signsPhase2 >> isJust)
                  |> Maybe.withDefault False
  -- healthEducationFormWithDefault (same file): every phase-2 question is hydrated from the saved set
                  , positiveHIV = or form.positiveHIV (Maybe.map (EverySet.member EducationPositiveHIV) value.signsPhase2)
                  , familyPlanning = or form.familyPlanning (Maybe.map (EverySet.member EducationFamilyPlanning) value.signsPhase2)
  ```
  A question that was never asked is hydrated as `Just False` from any saved `signsPhase2`, and completion only asks whether something was saved.
- **Reachability.** Lab tech enters the patient's HIV negative and the partner's positive (partner answers pending) → recurrent Next Steps offers health education with the two questions then required (safer sex, partner testing) → nurse saves → nurse answers the partner follow-up "not on ARVs" → `DiagnosisDiscordantPartnershipRecurrentPhase` → the education task now needs `positiveHIV` + `familyPlanning`, pre-filled No, and `nextStepsTaskCompleted … NextStepsHealthEducation` is still True.
- **Impact.** Counselling for a discordant couple never offered; the record says it was not given. Same "any save completes it forever" shape as B-336 (medication); scout checked the other recurrent families — malaria / syphilis / hypertension use per-family `recommendedTreatmentMeasurementTaken`, anemia `isJust reinforceTreatmentSigns`, so only medication (B-336) and this one have the shape.
- **Backlog de-dup.** B-336 (medication twin — ⚠ its fix is STRANDED, not on develop), B-323 (the pending-marker window that opens this door). Not recorded.
- **Fix shape (guess).** Complete when every question the current diagnoses require has an answer in the saved set (not merely a saved set), mirroring B-336's `requiredMedicationsAddressed`; land with B-336.
