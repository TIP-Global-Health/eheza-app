<!-- id:B-115 status:IMPLEMENTED tier:2 round: -->
### B-115 — Patient merge never re-points whatsapp_record.field_person — ✅ IMPLEMENTED 2026-08-03 (issue #2025, PR #2026, branch `b115-b117-merge-remaining-person-refs`)
- Fix: `_consolidate_patient_whatsapp_records()` via the B-114 helper. **No shard handling: `whatsapp_record` has NO `field_shards` field at all** (nor `field_deleted`, which is why the cascade leaves it in place rather than soft-deleting it). A first draft cleared shards there and would have thrown.
- Live at implementation: 275 records Rwanda / 22 Burundi, **0 orphans** against 159 soft-deleted persons. Closed the class, repaired nothing.
- `whatsapp_record` carries `field_person` but has no encounter-indicator field → not a measurement type → invisible to the merge. Records of reports shared over WhatsApp keep pointing at the merged-away person.
