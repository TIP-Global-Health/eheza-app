<!-- id:B-086 status:READY tier:4 round:16 -->
### B-086 — AQ dedup "stuck task" check uses ENQUEUE time, not processing-start — READY · CONFIRMED · S · efficiency-only
- `hedley_general.module:75-81` PROCESSING branch compares `time() - $task->created` to the 20-min window → task backlogged >20min in QUEUED is "stuck" the moment it starts → duplicate enqueue (idempotent workers; wasted recompute only). Fix: compare against a processing-start marker.
