<!-- id:B-042 status:IMPLEMENTED tier:2 round:11 -->
### B-042 — Nutrition-report prevalence %: denominator deduped by child but numerator keeps per-encounter duplicates → percentages inflate, can exceed 100% — ✅ IMPLEMENTED 2026-07-13 (issue #1933, PR #1934, branch `b042-prevalence-dedup`, base develop @8e19e166a, commit 6968b111b, Copilot requested, CI running). **User chose PLAIN DEDUP** for the flagged product decision (moderate-then-normal child still counts in the moderate numerator; latest-status / exclude-changers = clinical redefinition, NOT done). Fix: `array_unique` on BOTH args inside `hedley_reports_calculate_percentage` (single site → all 8 prevalence rows) + mirrored in server/elm `generatePrevalenceNutritionMetricsResults`'s local calculatePercentage (`unique` both sides). Incidence path = verified NO-OP (its numerators are array_intersect/array_diff over array_unique inputs → already unique; demonstrated `count === count(array_unique)`). DISCRIMINATION (drush php-script vs real function): 100%→50%, **200%→100%**, 75%→50% (severe stays 25%), no-repeat case unchanged 50%→50%. Regression test FOLDED into existing `HedleyReportsDataEndpoint.test` (no new class = no extra CI install). elm make server/elm 84 modules, elm-format + elm-review clean.
- 📌 **server/elm has NO test infrastructure** (elm.json test-dependencies empty, package.json only elm-review; `ci-scripts/test_elm.sh` is client-only) → any server/elm logic fix is compile-verified only. Adding elm-test there = a candidate G-track item.
- 📌 elm-review on server/elm: no local node_modules; run `client/node_modules/.bin/elm-review --compiler <0.19.2>` FROM the `server/elm` dir (it reads the cwd's elm.json), which is what CI's `test_elm_review.sh` does via npx.
- `hedley_reports.module:2722-2739` (`hedley_reports_generate_prevalence_nutrition_metrics_results`, anchor re-located): totals `array_unique(array_merge(moderate,severe,normal))` (:2723, self-verified) but `hedley_reports_calculate_percentage` (:2819-2825, self-verified) does `count($nominator)/count($total)` with NO dedup. Mirror: `server/elm/src/Pages/Reports/Utils.elm` `sumNutritionMetrics` :170-189 (`++` append) + `generatePrevalenceNutritionMetricsResults` :272-313 (`unique` on total only) — byte-for-byte same logic.
- ✅ DUPLICATES GATE CONFIRMED (verification sweep 2026-07-06): accumulation IS per-encounter — `hedley_reports_categorize_z_score` (:3066-3080) buckets person_id once PER ENCOUNTER; month merge via `hedley_reports_sum_nutrition_metrics` (:3032-3055) = plain array_merge, no dedup; family-nutrition path same; "impacted" (2+ visits) variant equally affected AND concentrates dups by construction (`hedley_reports_filter_impacted_ids` :3151-3155 preserves them). Worked example: child A measured 2× moderate + child B 1× normal in one month → 2/2 = 100% instead of 50%; degenerate 2/1 = 200%. Incidence path unaffected (uses array_unique/intersect).
- Fix: dedup numerator BOTH sides (PHP count(array_unique(...)) + Elm List.length (unique ...)) — both must land together (PHP precomputes report_data nodes that server/elm Decoder displays, while the same Elm app also computes live). ⚠ Product decision to flag at impl: a child moderate-then-normal within one month still counts in the moderate numerator under plain dedup — intersect semantics is a clinical call. Severity MED.
