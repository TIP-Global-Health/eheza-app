<!-- id:B-185 status:UNKNOWN tier:4 round:20 -->
### B-185 — Completion generation: `--exclude_set` incremental runs would corrupt encounter history (subsequent encounters classify as initial) — PLAUSIBLE · ops-gated (unknown whether production uses the flag) · tier 4 · READY
- Every builder drops already-generated encounters from `$encounters_data` BEFORE building per-encounter context (e.g. PHP:7681-7684, :7319-7321), so an incremental run sees a new encounter with EMPTY history: HIV diagnostics instead of symptom review, prenatal first-visit history/labs re-expected, supplied mosquito net forgotten. Full runs (default) unaffected. Fix if incremental is real: skip only the WRITE for already-set encounters, keep them in the history array. Gate: ask how production runs the scripts (same user question as the batch header).
- 📌 F10 systemic note (no item): PHP flattens the client's "mandatory activities before Next Steps/labs" gates across all 5 programs (NCD none, AI initial+subsequent, HIV/TB Medication-sub-task) — biases only encounters saved incomplete, understating direction; revisit only if incomplete-encounter volume is shown to matter.
