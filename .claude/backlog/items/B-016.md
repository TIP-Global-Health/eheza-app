<!-- id:B-016 status:READY tier:4 round: -->
### B-016 — No server-side range validation on clinical numerics — READY · CONFIRMED (verification sweep 2026-07-06, self-verified)
- Sync-POST path `HedleyRestfulSync.class.php:449-508`: `$data[$key] = $value` verbatim (:485-487, self-verified) → sub-handler post/patch via entity wrapper; D7 does NOT run field_attach_validate on programmatic save. No entity_validator plugin, no hook_node_validate anywhere in custom modules (grep-verified); vitals field settings empty (`field_heart_rate` number_integer no settings; instance min/max both '' — hedley_activity features). Client-side Elm constraints exist but a modified/replayed client bypasses them.
- Concrete consumer: `hedley_stats.module:993` GROUP_CONCATs body_temperature verbatim into AI stats (plus z-score/report scripts consume height/weight). MED, defense-in-depth.
- Fix (minimal single site): per-field range map applied in the sync-POST foreach where `$data[$key]` is assigned (:485-487), reject/clamp out-of-range clinical numerics; alternative is entity_validator plugins per bundle (more idiomatic, many sites).
