<!-- id:B-164 status:READY tier:3 round:19 -->
### B-164 — Vaccination "administered previously" flow on the day a dose first becomes due: inverted empty date range, dead calendar, and TODAY committed through the path built to forbid it — ✅ IMPLEMENTED (issue #2074, PR #2075, MERGED) · CONFIRMED (all 3 host hops orchestrator-verified; widget behavior agent-proven by running) · MED · WellChild + ChildScoreboard + Prenatal · R19 Unit 3
- Chain (each hop verified): the "was it administered previously?" question shows when `Date.compare expectedOnDate currentDate == GT` is FALSE — includes the EQ day (`Measurement/Utils.elm:1449-1454`); the selector config sets `dateFrom` = the dose's due date, `dateTo = Date.add Days -1 currentDate` (yesterday), `dateDefault = Just dateFrom` (:1550-1567) — due-today ⇒ inverted range with default = today; opening the popup COMMITS the default into the form: `SetVaccinationUpdateDateSelectorState` sets `vaccinationUpdateDate = Maybe.Extra.or form.vaccinationUpdateDate (Maybe.andThen .dateDefault state)` (`Pages/WellChild/Activity/Update.elm:894-908`; ChildScoreboard :401-415; Prenatal ~:4012) — bypassing the widget's `Date.clamp` (which guards only EMITTED selections, agent-verified vs justinmimbs 4.1.0).
- The widget renders the inverted range as month list empty + every day cell Disabled (`DateSelector/Selector.elm:110-111/:357-358`, proven by running) — nurse sees a dead calendar, cannot change the date; Save gates only on `isJust form.vaccinationUpdateDate` (`Measurement/Utils.elm:1584-1590`) and inserts the dose dated TODAY via the "previously" path (no AdministeredToday note).
- Trigger is a real calendar coincidence: child reaches eligibility age today, or last-dose + interval lands exactly today. Impact: wrong-path administration record dated today + broken-looking UI, in all three immunisation programs.
- Fix shape (small): gate the question on `== LT` (strictly before today), or `dateFrom = Date.min expectedOnDate (Date.add Days -1 currentDate)` + skip the dateDefault commit when the range is empty. Fix the shared config builder once — all three programs ride it.
