<!-- id:B-001 status:IMPLEMENTED tier:1 round: -->
### B-001 — Duplicate `"deleted"` key in 3 measurement encoders resurrects soft-deleted records — ✅ IMPLEMENTED 2026-07-06 (issue #1900, PR #1901, branch `b001-duplicate-deleted-key`, base develop @91fb401fb, CI running, Copilot requested). Fix: deleted the 3 hardcoded lines (anchors :880/:1433/:2008 held; exactly 1 `"deleted"` key remains = the shared encodeMeasurement one). elm make Success (548), elm-format clean, elm-test 2752 passed (own-elm-stuff, authoritative).
- `client/src/elm/Backend/Measurement/Encoder.elm` ~:880 (ultrasound), ~:1432 (medicalHistory), ~:2002 (symptomsRespiratory).
- Mechanism: `encodeMeasurement` already emits `("deleted", measurement.deleted)`; these 3 value-encoders ALSO emit `("deleted", bool False)`; `Json.Encode.object` last-wins → uploads `deleted:false` → soft-deleted record resurrected on sync.
- Fix: delete the 3 hardcoded lines. Test: encode a deleted measurement of each type → JSON `deleted == true`.
