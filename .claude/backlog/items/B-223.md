<!-- id:B-223 status:READY tier:4 round:23 -->
### B-223 — Backend/Update revision minors ×2: (a) `PrenatalFefolRevision`/`PrenatalAspirinRevision` ignore `deleted`; (b) removing a participant from an education session leaves it in their `educationSessionsByPerson` index — CONFIRMED (orchestrator-read both) · LOW · tier 4 · R23 unit 7
- **(a)** `Backend/Update.elm:7266-7272` (`fefol`) and `:7175-7181` (`aspirin`) insert `Just ( uuid, data )` with no `if data.deleted then Nothing` guard that the other ~174 measurement branches carry. Sync ships deleted rows (server soft-deletes via `delete-duplicate-measurements.php`, proven CURRENT in R21). A deleted fefol row lands in (or replaces the survivor in) the in-memory `prenatalMeasurements` of a loaded encounter until the next fetch (≤5-min forget window). Fefol live (`Pages/Prenatal/Activity/Update.elm:~1809`); aspirin's client save path is dead (B-136).
- **(b)** `Backend/Update.elm:5875-5903` `EducationSessionRevision` updates the per-person index only by folding over `data.participants` (current set) — a person just removed is never visited, so their cached dict keeps the session. Reachable via `Pages/EducationSession/Update.elm:~80-108` `ToggleAttendance` → `SaveAttendance`. Consumer: Patient Record Group Education pane (`Pages/PatientRecord/View.elm:~248`, fetched only when `NotAsked`). Shows for ≤ the forget window if the index was already loaded.
- **Fix shape (first guess):** (a) add the sibling guard; (b) drop `uuid` from every loaded per-person dict not in `data.participants` before re-inserting.
