<!-- id:B-429 status:READY tier:3 round:36 -->
### B-429 — Aggregated NCDA scoreboard immunisation row: the server rebuilds each vaccine's dose ladder from distinct administration dates and ignores the recorded dose numbers, unlike the device — READY · CONFIRMED (orchestrator read both builders and counted 788 disagreeing child-vaccine pairs live) · MED-LOW · tier 3 · R36 unit 2 · DEPLOYED (Rwanda admin report)
- **Server.** The aggregator emits dates only, pooling Well Child and Child Scoreboard bundles, and never reads `field_administered_doses`:
  ```php
  // server/hedley/modules/custom/hedley_ncda/hedley_ncda.module:972-984
    foreach ($immunisations as $immunisation) {
      $items = field_get_items('node', $immunisation, 'field_administration_dates');
      ...
          case HEDLEY_ACTIVITY_WELL_CHILD_BCG_IMMUNISATION_CONTENT_TYPE:
            $data['ncda']['pane2']['row1']['bcg'][] = $vaccination_date;
  ```
  The dose is then invented from sort position:
  ```elm
  -- server/elm/src/Backend/Scoreboard/Utils.elm:52-62
  generateVaccinationProgressForVaccine dates =
      EverySet.toList dates
          |> List.sortWith Date.compare
          |> List.indexedMap (\index date -> vaccineDoseFromOrder index |> Maybe.map (\dose -> ( dose, date )))
  ```
- **Client.** The same-named function zips the recorded doses with the dates per record and merges programmes by dose (`client/src/elm/Measurement/Utils.elm:1270-1288`, `mergeVaccinationProgressDicts`). Everything downstream (`nextVaccinationDataForVaccine`, intervals, IPV/OPV special cases) is byte-equivalent between the copies, so the ladder is the only place the row can disagree.
- **Live (orchestrator, 2026-09-22, ihangane):** per person × vaccine across both programmes, 788 pairs where the number of distinct dates ≠ the highest recorded dose: 179 with more dates (server counts extra doses, a behind child reads on track), 609 with fewer (server counts fewer, an up-to-date child reads behind). The device grades those children on the recorded doses.
- **Fix shape (first guess):** emit the doses beside the dates in `pane2.row1` and key the server-Elm dict by recorded dose (payload shape change — needs the rollout care the backlog records for payload changes).
