<!-- id:B-054 status:IMPLEMENTED tier:2 round:11 -->
### B-054 — `filterResolvedFollowUps` comparison is INVERTED: keeps already-resolved follow-ups, drops still-open ones (same class as the fixed R6-4 ECD `not`) — ✅ IMPLEMENTED 2026-07-06 (issue #1904, PR #1905, branch `b054-filter-resolved-followups`, base develop @c036957f8 — includes user-merged #1893, CI running, Copilot requested). Fix: dropped the `not`; masking story re-verified inline (SW retains 5 dashboard types ≤6mo; case-mgmt limit=currentDate; :226 dueDate guard correct, untouched); function exposed + new Pages/GlobalCaseManagement/Test.elm (4 boundary tests, 3 discriminating vs old code — verified failing pre-fix). elm make Success (548), elm-review clean, elm-test 2756.
- `Pages/GlobalCaseManagement/Utils.elm:462-471`: `not <| Date.compare limitDate date == LT` → True (KEEP) iff `resolutionDate ≤ limitDate` (already resolved as of limitDate); items resolved AFTER limitDate (open during the window) → dropped. Used in 10 keep-when-True `List.filter`/`Dict.filter` sites all commented "Filter out resolved follow ups" (:283). Name + comment + purpose (show PENDING follow-ups) all say the code is backwards; `Nothing` resolutionDate correctly kept via `Maybe.withDefault True`.
- Ground truth: server sets `field_date_concluded = time()` only at resolution (hedley_case_management.module:560), always past/now → for a current view EVERY item with a resolutionDate has it ≤ today → all KEPT (should be dropped). Masked in practice: SW (nodes.js:603-663) drops resolved follow-ups before Elm EXCEPT 5 dashboard types retained ≤6mo; prenatal/AI panes further masked by `participant.dateConcluded` guards; nutrition pane + TB AI-derived entries are NOT masked (resolved items linger as OverDue). Dashboard `getFollowUpsTotals` (limitDate = month end) wrong BOTH directions (open-during-month dropped = undercount; resolved-before-month kept = overcount).
- Fix: drop the `not` (keep only when `Date.compare limitDate date == LT`, i.e. resolution strictly after limit). Severity MED. **REQUIRE a discrimination test + careful review (like R6-4): magnitude is masked, which is likely why a 10-call-site inversion survived in prod — confirm the masking story before/while fixing so the fix doesn't surface a flood of correctly-dropped items unexpectedly.**
