<!-- id:B-240 status:IMPLEMENTED tier:1 round:25 -->
### B-240 — Prenatal recurrent labs: an operator-precedence slip makes a **positive** lab HIV result suppress the HIV follow-up, so "is she enrolled in the HC HIV programme?" is never asked — ✅ IMPLEMENTED 2026-09-06 (issue #2207, PR #2208, branch `B-240-hiv-followup-task-gate`, base develop @33b7d383b) · CONFIRMED (precedence + consumers verified) · MED · tier 1 · R25 unit 2
- **Mechanism.** `Pages/Prenatal/RecurrentActivity/Utils.elm:299-311`, `expectLaboratoryResultFollowUpsTask` / `TaskHIVTest`: `wasFollowUpScheduled TestHIV && (not <| expectLaboratoryResultFollowUpsTask assembled TaskPartnerHIVTest || (hivTest result == TestPositive))`. `<|` is `infixr 0` and `||` is `infixr 2`, so this parses as `not (partnerFollowUpExpected || hivPositive)` = `not partnerFollowUpExpected && not hivPositive`. The comment directly above states the intent — *"So we either do not expect TaskPartnerHIVTest follow up, or, only when test HIV result is positive"* — i.e. `(not partnerFollowUpExpected) || hivPositive`. The `hivPositive` term therefore **suppresses** the task that it was written to force.
- **Reachability.** Lab tech enters HIV result `Positive` in the recurrent Lab Results. `toHIVResultValue` with `isLabTech = True` (`Measurement/Utils.elm:8086-8095`) stores `hivSigns = {PrenatalHIVSignPendingInput}`; `Backend/Update.elm:9128` adds `TestHIV` to `testsWithFollowUp`. The nurse then sees the task nowhere: Lab Results hides it (follow-up was scheduled) and Labs Results Follow Ups drops it because `not (False || True) = False`. Unlike B-239 this does **not** block the encounter — the task simply disappears, and `laboratoryResultFollowUpsTaskCompleted`'s `(not <| taskExpected …) || …` short-circuits to complete.
- **Impact.** The only question that follow-up form asks for a positive result is `HIVProgramHC` (`Measurement/Utils.elm:4245-4263`, verified), and on the nurse's recurrent path it exists nowhere else — the initial-phase form asks it only for point-of-care results. So `hivProgramAtHC` (`Pages/Prenatal/Utils.elm:2955-2960`) stays False for every lab-diagnosed HIV-positive pregnancy, which means (a) `referToARVProgram` (`RecurrentActivity/Utils.elm:724`) is False → the ARV-programme referral is never offered in the recurrent Next Steps, and (b) `resolveHIVPositiveSet` (`Pages/Prenatal/Utils.elm:1801-1812`, `hivDiagnosed && not hivProgramHC`) always prescribes TDF3TC + Dolutegravir, including for a woman already enrolled on an HC programme. All sites. Written in `80fa884d4` (2024-10-11), unchanged since.
- **Fix shape (guess).** Make the expression match its comment: `(not <| expectLaboratoryResultFollowUpsTask assembled TaskPartnerHIVTest) || hivPositive`. ⚠ Confirm the intent with the clinical spec before flipping — it changes which of the two HIV tasks appears in the partner-positive case, and B-239 sits on the other side of that same choice.
- ⬆ **Re-tiered 3 → 1 on 2026-09-03 (user rule):** a defect in how a diagnosis or its care pathway is determined is tier 1 regardless of how many patients hit it. Severity label unchanged — it describes blast radius, the tier describes priority.
- ✅ **IMPLEMENTED 2026-09-06.** `Pages/Prenatal/RecurrentActivity/Utils.elm`: the `TaskHIVTest` arm's two terms are named in a `let` — `partnerHIVTestFollowUpExpected` and `hivTestResultPositive` — and the gate reads `wasFollowUpScheduled TestHIV && (not partnerHIVTestFollowUpExpected || hivTestResultPositive)`. Named bindings rather than re-parenthesising `not <| … || …`, which is the shape that misparsed. Verified: elm-format clean · `elm make Main` 548 modules · elm-test 3,068 pass · elm-review clean on a cleared `elm-stuff`.
- 🧪 **New test module `Pages/Prenatal/RecurrentActivity/Test.elm`** — six cases over `resolveLaboratoryResultFollowUpsTasks` (already exposed; `expectLaboratoryResultFollowUpsTask` is not, and did not need to be), covering both HIV results against both states of the Partner HIV task, plus the no-HIV-follow-up-scheduled case. **Discrimination run: exactly 2 of the 6 FAIL against `origin/develop`** — the two positive-result rows — and the other four are regression pins that pass either way.
- ⚠ **The AssembledData fixture is local to the new module, deliberately.** `Pages/Prenatal/Activity/Test.elm` has `testAssembled`, but it is tuned for the EGA-sensitive diagnosis tests and depends on that file's own `currentDate`/`lmpDate`/`testEncounter`/`testPerson`; hoisting it would make the shared `TestFixtures` module depend on `Pages.Prenatal.Model`, which it does not today and which every non-prenatal test module would then carry.
- ⭐ **Live sizing, run from this seat 2026-09-06 (5 terminus queries). The defect has never fired.** `ihangane.live`: 200 `prenatal_labs_results` carry `hiv` in `testsWithFollowUp` (i.e. 200 lab-tech-entered HIV results) and joining each to its encounter's `prenatal_hiv_test` shows **all 200 negative** — not one positive. Its 49 positive HIV tests are all point-of-care, where `viewHIVTestForm` asks the question inline: 46 stored `hiv-program-hc`, 3 stored `none` (asked, answered No). `vhw.live`, `uvl.live` and `tip-somalia.live` have **zero** HIV follow-ups scheduled and **zero** positive prenatal HIV tests. Same shape as [[B-237]]: a real, deployed, two-year-old inversion with no live victims yet.
- 📝 **No written spec for this gate.** `80fa884d4` → PR #1353 → issue #1349 ("Remove question about partner HIV Status from HIV Labs screen") specifies moving the partner questions onto the Partner HIV lab and what the report shows; it says nothing about which follow-up task appears for a positive patient result. The basis is the in-file comment plus the positive/negative split in `hivResultFollowUpInputsAndTasks` (`Measurement/Utils.elm:4244-4267`), where positive asks `HIVProgramHC` alone and negative asks the partner block that `TaskPartnerHIVTest` also asks.
- ⭐ **The rest of the chain was already correct and needed nothing.** `RecurrentActivity/View.elm:718-728` renders `TaskHIVTest` through `hivResultFollowUpsFormAndTasks` and its save arm at :770-771 has always existed (unlike [[B-239]]'s partner arm); the nurse's `toHIVResultValue` (`Measurement/Utils.elm:8097-8104`) replaces `PrenatalHIVSignPendingInput` with the real signs, so `laboratoryResultFollowUpsTaskCompleted` closes.
