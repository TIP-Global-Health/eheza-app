<!-- id:B-159 status:READY tier:4 round:19 -->
### B-159 — client JS glue minors batch (3 independent one-liners) — READY · CONFIRMED (each self-verified at anchor) · LOW · one small PR · R19 Unit 2
- (a) `IndexDbQueryGetShardsEntityByUuid` crashes with no reply when the uuid is absent from local shards: `app.js:1193` `entities = [result[0]]` then `:1200` `entities[0].type` (TypeError on empty result); guard `if (entities)` at :1367 is ALWAYS truthy (array); the async IIFE (:1370) is the only replying handler with no `.catch`. Consumer is fire-and-forget `BackendReportIncidentDetails` (SyncManager/Update.elm:921-933) → no stall, cost = a silently lost incident report exactly in the most diagnostic-worthy subset (uuid unknown locally AND rejected by backend). Path is live-exercised: 797 users on ihangane.live carry non-empty field_incident_details. Fix: early-return empty result + `.catch` mirroring the upload lanes (:836/:933/:970). (Borderline with the declined sync-robustness class; recorded as a plain missing-null-check crash in a replying handler.)
- (b) `resolveShardByClinicId` rejects with ReferenceError instead of its message: `nodes.js:1275` references `session.clinic` — no `session` in scope (function param is `clinicId`; file is strict-mode) → thrown ReferenceError becomes the rejection. Outer catch re-rejects either way; only the diagnostic text is wrong. Fix: use `clinicId`.
- (c) Person-search tokenization diverges from index-time `gatherWords`: `nodes.js:931-934` `nameContains.split(' ')` keeps empty tokens (index side app.js:368-377 uses `/\s+/` + blank drop) → an internal double space ("Jean␣␣Bosco") makes `startsWithAnyOf([''])` span the ENTIRE name_search multiEntry index (results stay correct — the `words.every` filter still enforces real words; perf only; Elm trims only ends). Fix: mirror gatherWords' tokenization.
