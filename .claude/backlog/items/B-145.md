<!-- id:B-145 status:UNKNOWN tier:2 round:18 -->
### B-145 — ⚠ DECISION NEEDED: repo `pantheon.yml` is NEVER deployed — live envs have NO workflows, and **DB clones to multidev/test carry UNSANITIZED patient data** — CONFIRMED · HIGH (privacy) · release-prep — 🅿 PARKED (user 2026-08-21, no reason given — do not re-pitch)
- `RoboFile.php:86-87` excludes `pantheon.yml`/`pantheon.upstream.yml` from the rsync, so the repo's yml (declaring the sync_code → `revert_all_features.php` workflow) has never reached Pantheon: BOTH live checkouts (`.pantheon-ihangane`, `.pantheon-vhw`) have NO `workflows:` section and no `scripts/pantheon/` dir. Consequences: (1) no auto-fra on non-robo code pushes (e.g. dashboard Test→Live promotes) — only the robo path runs fra; (2) `db_sanitization.php` is wired NOWHERE (commented out even in the repo yml) → every clone-to-multidev/test carries live PHI. Intact: private-file web protection (upstream yml), php 7.4.
- This is a product/ops DECISION (deploy the yml? which workflows? sanitize what?), not a code fix I should pick — surface to the team before September, since the release process will create clones.
