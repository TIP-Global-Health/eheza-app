<!-- id:B-247 status:IMPLEMENTED tier:3 round:25 -->
### B-247 — Nutrition report: the Elm half throws away edema on well-child encounters, so the same child is SAM at district scope and not-SAM at sector scope — ✅ IMPLEMENTED · CONFIRMED (both decoders + the wire emitter verified) · MED-HIGH · tier 3 · R25 unit 7
- **Mechanism.** `hedley_reports_nutrition_metrics_to_string` (`hedley_reports.module:1452-1471`) emits edema as the 5th CSV token for **all three** encounter families, well-child included. The PHP report consumes it for all three (`:2447` `'edema' => (isset($encounter_fields[4]) && $encounter_fields[4] === '1')`) and `hedley_reports_categorize_acute_malnutrition` (`:3117-3131`) classifies SAM on `muac < 11.5 || $has_edema`. The Elm report consumes it for group and individual-nutrition encounters but **discards it for well-child**, in two places: `server/elm/src/Backend/Reports/Decoder.elm:790` destructures `[ stunting, underweight, wasting, muac, _ ]`, and `server/elm/src/Pages/Reports/Update.elm:150-156` hard-codes `hasEdema = False` under a comment claiming *"well-child wire doesn't carry edema"* — which is false. `WellChildEncounterData` (`Model.elm:230-235`) has no `hasEdema` field, unlike `NutritionEncounterData`.
- **Why the scope matters.** `hedley_reports_build_statistical_queries_results_app` (`:1741-1745`) attaches the PHP-precomputed `nutrition_report_data` only for `global / province / district / health-center`; for **sector, cell and village** the Elm app computes the report itself (`Pages/Reports/Update.elm:31-40`). So the same child in the same month is SAM in the district table and MAM-or-normal in the sector table.
- **Impact.** SAM undercount, with a matching MAM/normal overcount, at the three narrow scopes, for any child whose bilateral oedema was recorded at a well-child/SPV encounter with MUAC ≥ 11.5 cm. Oedema is a stand-alone SAM criterion, so the error runs in the clinically dangerous direction. All sites; hits the `acute_malnutrition_sam` prevalence and incidence rows.
- ✅ **`familyNutritionEncounterToMetrics` passing `False` is correct** — family nutrition records MUAC only and the PHP sets `'edema' => FALSE` at `:2482`. Well-child is the only mismatched path.
- 📌 **Adversarially checked and CLEAN, recorded so nobody re-chases it:** the wire looks like it swaps positions 2 and 3 (PHP emits `"$stunting,$wasting,$underweight,…"` while Elm reads `[stunting, underweight, wasting, …]`), but the PHP **local variable names are historically misnamed after the measurement rather than the indicator** — `$values['w']` is weight-for-age = underweight and `$values['u']` is weight-for-length = wasting. The docblock at `:1424-1434` states this explicitly. Wire order and both decoders agree.
- **Fix shape (guess).** Give `WellChildEncounterData` a `hasEdema` field, return the 5th token instead of `_`, and drop the hard-coded default. No PHP change and **no data regeneration** — the token is already on the wire and in the stored blobs.
- ✅ **IMPLEMENTED 2026-08-30 — issue #2163, PR #2164**, branch `B-247-wellchild-edema` off `72573cca8`. `WellChildEncounterData` gained `hasEdema`; `decodeWellChildEncounterData` now parses through `parseAnthropometryPayload`, the helper the individual-nutrition payload already used, so the 5th token is read instead of discarded; `Pages/Reports/Update.elm` takes `item.hasEdema` instead of hard-coding `False`. ⭐ **The entry's fix shape said "return the 5th token"; the better move was to delete the token-dropping helper entirely** — `nutritionDataFromString` had exactly one caller, so switching to the sibling parser left it with none and it went with the change (−26 lines in the decoder). Same shape as B-243: adopt the module's own existing idiom rather than extend the copy.
- **Verified.** Wire path re-read end to end first: `hedley_reports.module:705-711` writes `['e']` for `HEDLEY_ACTIVITY_WELL_CHILD_NUTRITION_CONTENT_TYPE`, and `:1025-1027` (the `HEDLEY_STATS_SPV_ENCOUNTER_TYPE` arm) emits the same 5-token `$nutrition` string as the nutrition arm — so edema was on the well-child wire and only the Elm side dropped it. `elm-format --validate` clean · `elm make src/Main.elm` → *Success! Compiled 82 modules* · `npx elm-review` on `server/elm` → *I found no errors!* · **the committed bundle was rebuilt** (`server/hedley/modules/custom/hedley_general/js/elm-main.js`) and `ci-scripts/test_server_elm_bundle.sh` passes — ⚠ CI compares it byte-for-byte, so any `server/elm` change must ship the rebuilt bundle. Discrimination: `elm repl` decoded `"2026-03-04|-1.2,-0.4,-0.3,12.4,1|"` → `hasEdema = True` and the `,0` variant → `False`, with `muacCm = Just 12.4` — i.e. exactly the MUAC ≥ 11.5 case that `categorizeAcuteMalnutrition` (`Pages/Reports/Utils.elm:193-205`) now sends to SAM and previously sent to normal. (The decoder is not exposed; it was exposed temporarily for the repl and the module header restored before committing.)
- 🔍 **Reviewed (medium) 2026-08-30 — no findings.** Its one observation, posted inline: the comment block deleted with `nutritionDataFromString` carried *"do not reorder without updating the PHP encoder/decoder in lockstep"*, and the surviving comment had not kept it, so the constraint was stated nowhere. Restored in `ef9d90b13`; the bundle was rebuilt and still matches byte-for-byte, as a comment-only change should. ⚠ Left as raised: `HedleyReportsDataEndpoint.test` has NO well-child wire coverage at all, so nothing guards this format on the PHP side — pre-existing, not introduced here.

