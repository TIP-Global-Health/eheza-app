<!-- id:B-088 status:READY tier:4 round:16 -->
### B-088 — No `hook_node_delete` in hedley_ncda / hedley_reports / hedley_stats → hard-delete (super_user_mode only) leaves stale aggregates — READY · CONFIRMED · S · hardening
- grep `_node_delete(` across the 3 modules = zero matches. Stale until next triggering measurement. Fix: add hooks enqueuing the same recalcs as insert/update.
