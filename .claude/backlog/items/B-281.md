<!-- id:B-281 status:READY tier:4 round:26 -->
### B-281 — Shared Yes/No-with-reason forms: flipping the answer back to Yes persists the stale "why not" reason alongside it, in the same session (four Acute Illness handlers; the SendToHC/HealthEducation halves are shared by every program) — READY · CONFIRMED (handler + hydration + persistence verified) · LOW · tier 4 · R26 unit 4 · family
- **Mechanism — not B-274's.** The handler clears the dependent field to `Nothing`; the `*FormWithDefault` refills it with `or form.x (Just saved.x)` (no dirty flag); `to*Value` persists it unconditionally. Resolving the form first would not help — it fires on the same device in the same session. Verified on `SetReferToHealthCenter` (`Pages/AcuteIllness/Activity/Update.elm:768-783`, `reasonForNotSendingToHC = Nothing`) → `sendToHCFormWithDefault` (`Measurement/Utils.elm:1022`, `or form.reasonForNotSendingToHC (value.reasonForNotSendingToHC |> Just)`) → `toSendToHCValue` (`:1046-1049`, `Maybe.withDefault NoReasonForNonReferral |> Just`). Stored: `{ReferToHealthCenter}` + `ClientRefused`.
- **The four Acute Illness handlers.** (1) `SetReferToHealthCenter` above. (2) `SetProvidedEducationForDiagnosis` (`:1088-1103`) → `healthEducationFormWithDefault` (`Measurement/Utils.elm:972`) → `:991-994`; stored `MalariaPrevention` + a reason. (3) `SetTreatmentReviewBoolInput` (`:717-730`) with `feverPast6HoursUpdateFunc` etc. (`Activity/Utils.elm:638-660`) clearing `*Helped` → `treatmentReviewFormWithDefault` (`:1612-1618`) → `toTreatmentReviewValue` (`:1630-1638`, `ifNullableTrue FeverPast6HoursHelped`); stored `FeverPast6HoursHelped` without `FeverPast6Hours` (three pairs). (4) `SetCovidTestingBoolInput` (`:661-676`) with the test-performed func (`Activity/Utils.elm:603-611`) clearing `testPositive/isPregnant/administrationNote` → `:1922-1925` → `:1963-1965`; stored `RapidTestPositive` + `NonAdministrationLackOfStock`.
- **Reachability.** Answer No + reason, Save; reopen the task, answer Yes, Save. The reason section renders only on the No branch (`Measurement/View.elm:1198-1216`, `:1012-1027`; `Activity/Utils.elm:1204-1224`, `:646-660`, `:562-585`), so nothing on screen shows the stale value and the counters are unaffected.
- **Impact.** Stored records only. No client consumer found: the progress report renders "helped" only when the parent sign is present (`ProgressReport/View.elm:663-689`); covid `administrationNote` is read by no report; server-side `reason_not_sent_to_hc` / `reason_not_given_education` appear only in `.info` files and REST exposure. Both sites. The SendToHC and HealthEducation halves live in shared `Measurement/Utils`, so Nutrition, Well Child, HIV, TB (and Prenatal via its own copy at `Pages/Prenatal/Utils.elm:6852`) store the same shape — not traced beyond this unit. Contrast B-274, whose stale reason HAS a consumer (the referral gate).
- **Fix shape (guess).** Have `to*Value` drop the dependent answer when the parent is Yes (e.g. `reasonForNotSendingToHC = NoReasonForNonReferral` when `referToHealthCenter == Just True`), or add dirty flags the way `OngoingTreatmentReviewForm.reasonForNotTakingDirty` already does. One idiom fix in the shared helpers covers every program.
- **R26 unit 5 adds NCD `SetReferralBoolInput` to the family (2026-08-30).** `Activity/Update.elm:1917-1929` and `RecurrentActivity/Update.elm:614-626` work on the raw form, and the update functions clear dependent fields (`referralFormHospital = Nothing` / `referralFormANCServices = Nothing, accompanyToANCServices = Nothing`, `Pages/NCD/Utils.elm:829-834, 846-852`); `referralFormWithDefault` (`:436-453`) has no dirty flags, so `or` refills them on Save — same-session, same mechanism as above. Stored: `referralSigns = {ReferralFormHospital}` beside a non-referral reason, or a stale reason beside `ReferToHospital`. Consumers checked: `referralToFacilityCompleted` (`Backend/Measurement/Utils.elm:3546-3588`) tests the `ReferTo…` sign or a reason; the progress report (`ProgressReport/View.elm:589-617`) tests the `ReferTo…` sign first — neither reads the form/accompany signs, so contradictory record only. Prenatal's handler (`Pages/Prenatal/Activity/Update.elm:3367-3377`) and `referralFormWithDefault` (`Pages/Prenatal/Utils.elm:3351-3382`) have the identical shape — not traced to consumers.
- 📌 **R27 unit 3 amendment — Nutrition members traced end-to-end:** `SetReferToHealthCenter` clears `reasonForNotSendingToHC = Nothing` (`Pages/Nutrition/Activity/Update.elm:582-597`) and `sendToHCFormWithDefault`'s `or` refills it at Save (`Measurement/Utils.elm:1011-1027` → `toSendToHCValue` stores it with `Maybe.withDefault NoReasonForNonReferral`); same for `SetProvidedEducationForDiagnosis` (`:644-659`) via `healthEducationFormWithDefault`. No Nutrition-side consumer of either reason — contradictory record only, matching the family's assessment.
