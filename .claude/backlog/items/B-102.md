<!-- id:B-102 status:IMPLEMENTED tier:4 round:16 -->
### B-102 — Delete dead `hiv_dot` entry from sw.js entity table — ✅ IMPLEMENTED 2026-08-17 (issue #2098, PR #2099, branch `b102-dead-sw-entity-row` off origin/develop @9dc7dd787) · PROVEN
- ✅ **Re-verified 2026-08-17 before deleting, not taken on trust:** `hiv_dot` appears **exactly once in the whole tree** (`client/src/js/sw.js:93` — line moved from :95), while its sibling `tuberculosis_dot` appears in 5+ Elm modules. Nothing server-side has the bundle.
- ✅ **The July "all keys live" claim re-run against the CURRENT tree:** after the deletion, all **213** remaining `tableForType` keys are referenced somewhere outside that table (sweep over `client/src/elm`, `client/src/js`, `server/hedley`, `server/elm/src` — `.elm/.js/.module/.inc/.php/.install/.csv`). **Zero** unreferenced ⇒ `hiv_dot` was the only dead key and the cleanup is complete. (Table is 213 now, not the 215 the July sweep counted — it has moved since, which is why re-running mattered.)
- `client/src/js/sw.js:93` `hiv_dot: 'shards',`. All 215 tableForType keys cross-checked against Elm — every other key live (incl. `syncmetadata` via nodes.js:219/906 and legacy `resource` = MalariaPrevention, Measurement/Decoder.elm:141). No hiv_dot bundle/plugin/field/CSV exists server-side; added symmetrically with tuberculosis_dot but HIV shipped without DOT.
