<!-- id:B-052 status:IMPLEMENTED tier:1 round:11 -->
### B-052 — WellChild immunisation "next visit" date returns Nothing for on-schedule children: the 6-month long-interval branch is dead code — ✅ IMPLEMENTED 2026-07-06 (issue #1896, PR #1897, branch `b052-immunisation-next-visit-date`, base develop @91fb401fb, CI running, Copilot requested). Fix: dates extracted before the emptiness test (revives long branch) + fallback to earliest pending dose across both groups when nothing due <30d; within-month bundling + tail clamp unchanged. elm make Success (548), elm-format clean, elm-test 2749 passed. No discrimination test yet (user reviewed diff pre-PR; test deferred — offer at merge time).
- `Pages/WellChild/Activity/Utils.elm:1982-2015` `generateNextDateForImmunisationVisit`: `List.partition` splits `futureVaccinationsData` into MR/HPV (long) vs rest (short). `generateFutureVaccinationsData` (Measurement/Utils.elm:9028+) emits one tuple PER vaccine type (`(type, Nothing)` when that cycle is complete), so `shortIntervalVaccinesData` — the list of non-MR/HPV TUPLES — is NEVER empty. Thus `if List.isEmpty shortIntervalVaccinesData` (:1990) is always False → the 6-month branch (:1991-1997) is DEAD, and the else branch extracts short-interval Just-dates, filters to `Date.diff Days currentDate d < 30` (:2007) with NO fallback → when the only pending doses are >30 days out (or only MR/HPV/long remain), the function yields `Nothing`.
- Verified worked example (Rwanda girl born 2024-01-01, on schedule): at the 14-week and 9-month encounters the next pending dose (IPV2/MR1 at 9mo, then MR2 at 15mo) is >30 days out → `Nothing` at essentially every encounter after 14 weeks. → NextVisit measurement saved with `immunisationDate = Nothing`; "Next Appointment" pane omits the immunisation row; `resoloveLastScheduledImmunizationVisitDate` (Measurement/Utils.elm:8748+) degraded. Partially mitigated: `generateASAPImmunisationDate` (correct, drives behind-child case-mgmt) + the separate pediatricVisitDate still recall the child.
- Fix (agent's, sound): extract Just dates BEFORE the emptiness test; when the <30-day filter empties the short list, fall back to the earliest pending date across all remaining vaccines with matching (interval,unit). Severity MED-HIGH (immunisation-specific forward recall silently lost for 9/15/18-month visits; child still recalled via pediatric-visit date). Discrimination test feasible (fixture on-schedule child → expect a date, not Nothing).
