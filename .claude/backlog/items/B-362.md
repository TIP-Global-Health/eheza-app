<!-- id:B-362 status:READY tier:4 round:31 -->
### B-362 — Child progress report (Well Child / Nutrition / Child Scoreboard / group): sending via WhatsApp while "Review prior diagnosis" is open sends only Patient Information + diagnoses — every other selected pane (immunisation, ECD, growth, next appointment) is left out — READY · CONFIRMED (mode switch and the mode-independent actions read by the orchestrator; scout scratch render) · LOW · tier 4 · R31 unit 3 · DEPLOYED, every site with `report_to_whatsapp`
- **Mechanism.** `client/src/elm/Pages/WellChild/ProgressReport/View.elm:480-543`: `derivedContent = case diagnosisMode of ModeActiveDiagnosis -> [ vaccination, ECD, growth, next appointment ] ; ModeCompletedDiagnosis -> []`. The action buttons (incl. Send via WhatsApp) are added whenever `selectedComponents` is `Nothing`, whatever the mode (`:264-270`, `:600-626`), and the dialog never resets the mode; `viewForReportToWhatsApp` forces the diagnosis pane to the active list, so the screenshot looks plausible.
- **Evidence.** Scout scratch render, 10-month-old, components {ActiveDiagnoses, ImmunizationHistory, ECD, NextAppointment}: `ModeActiveDiagnosis` → 1 `.vaccination-history`; `ModeCompletedDiagnosis` → 0 `.vaccination-history`, 0 `.next-appointment`; with no components in completed mode the WhatsApp button renders.
- **Reachability.** Report → "Review prior diagnosis" → Send via WhatsApp → pick panes → send.
- **Backlog de-dup.** B-266/B-347/B-348/B-349 (WhatsApp delivery), B-358 (empty immunisation pane — different cause). Not recorded.
- **Fix shape (guess).** Build `derivedContent` whenever `selectedComponents` is `Just`, or switch back to active mode when the dialog opens.
