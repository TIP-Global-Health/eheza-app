<!-- id:B-301 status:READY tier:4 round:27 -->
### B-301 — Stock Management: the oldest history row opens month details at gap 12 while the clamp is 11, and the back arrow then pages into never-computed months forever — READY · CONFIRMED (all three anchors read) · LOW · tier 4 · R27 unit 5
- **Mechanism.** History rows are the reversed tail of the data dict (`Pages/StockManagement/View.elm:246-251` — the vendored AssocList appends, so the tail drops the current month; intended per the original `List.range 1 12` skeleton). Each row's Details button passes its own `monthYearDiff` — 12 for the oldest row (`:269-284`) — but `maxMonthGap = 11` (`Model.elm:47-49`), and `viewMonthSelector` hides the back arrow only on `monthGap == maxGap` (`Pages/Utils.elm:502-509`); `ChangeDetailsMonthGap` is unclamped (`Update.elm:92-106`). From that entry point the user pages back to 13, 14, … each rendering a header-only table that reads as "no activity" for months that were simply never computed.
- **Fix shape (guess).** Clamp the details gap to `maxMonthGap` (or `>=` in the hide condition, dropping the gap-12 button).
