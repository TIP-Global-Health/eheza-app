<!-- id:B-322 status:READY tier:4 round:28 -->
### B-322 — Child progress report, Diagnosis pane: "Malnutrition signs" is listed twice for one date, because the nutrition-derived and follow-up-derived entries hold the same signs in different list order and the dedupe is structural `==` — READY · CONFIRMED (merge, both filter functions, sort, encoder/decoder round-trips read; scout repl-proved the order) · LOW · tier 4 · R28 unit 3
- **Mechanism.** `mergeNutritionAssessmentEntries` (`client/src/elm/Measurement/Utils.elm:10102-10119`) dedupes same-date assessments with `(assessments1 ++ assessments2) |> EverySet.fromList |> EverySet.toList` — structural equality. For `AssesmentMalnutritionSigns (List ChildNutritionSign)` the two sides order the list differently: the nutrition side (`filterNutritionAssessmentsFromNutritionValue`, `:10077`) uses `EverySet.toList value.signs` — a set built by `EverySet.insert` per tap, which after the encode/decode round trip (`decodeEverySet` = `fromList`, `Utils/Json.elm:60-64`) comes back in **tap order**; the follow-up side sorts (`generateNutritionAssessment`, `Backend/NutritionEncounter/Utils.elm:123-131`, "Maintain constant order, to avoid problems when comparing 2 assessments"), the encoder writes `EverySet.fromList signs`, and the decoder rebuilds `AssesmentMalnutritionSigns (EverySet.toList …)` (`Backend/Measurement/Decoder.elm:4353-4368`) — **string-sorted order**. Repl: `(["edema","abdominal-distension"], ["abdominal-distension","edema"])` → `EverySet.size 2`. The form lists Edema before Abdominal distension (`Measurement/View.elm:690-694`), so ticking top-to-bottom is the non-sorted order.
- **Reachability / impact.** Group session Nutrition signs + Next Steps follow-up → `viewDiagnosisPane` (`Pages/WellChild/ProgressReport/View.elm:673-681`, `:954-968` renders two "Malnutrition signs: …" rows); individual Nutrition/WellChild encounters go through `generateIndividualNutritionAssessmentEntries` with the same filters. Precondition: `AssesmentMalnutritionSigns` is only produced when MUAC/weight give no assessment; under 6 months all ticked signs are listed. Display only (also in the WhatsApp-sent report). No site split. Adjacent, intent question: for ≥ 6 months the follow-up carries `[Edema]` only while the nutrition side carries every sign, so with Edema + anything the two rows never dedupe regardless of order — which list the pane should show needs the spec. Also in this function (R28 unit 2): two same-day encounters from ONE source collapse to the last one's assessments (`Dict.fromList` by date before the merge) — display only, same fix site.
- **Fix shape (guess).** Normalise the sign list once at the boundary (sort in both `filterNutritionAssessmentsFrom…Value` functions or in `postProcessNutritionAssessment`), or dedupe by `nutritionAssessmentToComparable` instead of structural equality.
