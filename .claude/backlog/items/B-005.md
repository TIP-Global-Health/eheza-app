<!-- id:B-005 status:IMPLEMENTED tier:2 round: -->
### B-005 — Concurrent view exports silently swallowed — ✅ IMPLEMENTED 2026-08-03 (issue #2029, PR #2030) — parking REVERSED by the user
- **Unparked 2026-08-03** (user: *"Lets work on B-005"*), having been parked on 2026-07-13. The parking reason still holds as context — `file_managed` shows **3 exported files on Rwanda, newest 2025-06-23, none on Burundi** — so the fix was kept to the size the defect deserved.
- Fix shipped: the dedup id names what is being exported (`{uid}_{view}_{display}_{md5 of filters}`) instead of a hardcoded `0`; the return is checked; a refusal says so and deletes the file that nothing would otherwise collect. **23 lines.**
- ⚠ **This PR was written twice.** The first attempt was 152 lines — a notification helper, an item-data persistence helper, an attempt counter, retry semantics, try/catch — and three review passes found 13 issues, almost all in code that version had added. User: *"You can't have ten review bugs for PR with 5 lines changed. That does not make any sense."* The branch was reset and rewritten small. See [[pr-first-review-workflow]] for the rule that came out of it.
- ⚠ **B-087 was dropped from that PR and is now 🅿 PARKED** (user 2026-08-03) — a separate defect in a separate function, parked for the same rarely-used reason. Issue #2029 and PR #2030 were rewritten to cover B-005 only.

### B-005 original entry — 🅿 was PARKED by user 2026-07-13 (verbatim: "park - this export is used very rearly") — do NOT re-pitch. Mechanism stays CONFIRMED + LIVE (below); the traced fix (pass `$file->fid` instead of hardcoded `0` as the dedup id at :171 + check the FALSE return → delete the orphaned file + honest error instead of the fake success message) remains valid if ever revived. Adjacent same-file items B-019 (missing switchUserBack) and B-059 (worker FILE_APPEND) are SEPARATE queue entries — this parking does NOT cover them (but note the same low-usage argument may apply; ask before building).
- `hedley_view_export.module:171` passes `0` as unique id → dedup title always `export_view_0` (`hedley_general.module:27-41`; union `['title' => $task_title] + $data` means the fixed title WINS over the human-readable one; dedup blocks on QUEUED always + PROCESSING <20min). Return FALSE discarded; success message shown unconditionally (:173); email only sent by worker's final batch → swallowed request = no email ever + orphaned empty managed file (:151, never cleaned — cron :363-399 only deletes files referenced by AQ items).
- Blocked window = the WHOLE export lifetime (multi-batch requeue flips back to QUEUED between batches; processing only on the external 5-min drush run). LIVE: `historical_child` view (`hedley_admin.views_default.inc:1735`, page at admin/measurement-historical-child + hedley_view_export_csv display :2101), permission `hedley export views`.
- Fix: pass `$file->fid` (or random key) at :171 AND check the FALSE return → error message instead of fake success. Distinct from B-059 (worker FILE_APPEND) and B-019 (switchUserBack).
