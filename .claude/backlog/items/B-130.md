<!-- id:B-130 status:IMPLEMENTED tier:1 round:17 -->
### B-130 — ✅ IMPLEMENTED 2026-08-06 (issue #2044, PR #2045, branch `b130-scoreboard-calendar-month-buckets`, base develop @417df78d2, commits 278576035 + b337cab64) — decoder bucketed by months-before-today
- **Impact was BIGGER than the entry said.** Simulated with real `Date.diff Months` + `AssocList.fromList` semantics: a child measured once a month loses HALF their measurements — input May 28 / Jun 3 / Jul 20 / Aug 1 keeps only Jun 3 + Aug 1. Collision rule: any pair where the earlier date is late in its month and the later early in the next. Plus the same-month leak (severe Jul 2 + normal Jul 25 → counted as BOTH for July, violating the module's own docblock).
- **Fix:** 4 keying sites → `calendarMonth date = ( Date.year date, Date.monthNumber date )`; `sainitze…` typo corrected.
- ⚠ **The `currentDate` parameter existed ONLY to compute the broken key**, so `NoUnused.Parameters` cascaded its removal up 8 levels: `decodeMonthlyValues` → 5 pane decoders → `decodeNCDAData` → `decodePatientData` → `decodeSyncResponse` → `Backend.Scoreboard.Update.update` → `Backend.Update.updateBackend` → `App/Update.elm` call site. **Removed rather than `_`-silenced, because the end state matches the siblings: `Backend.Reports.Decoder`/`Backend.Completion.Decoder` `decodeSyncResponse` and their `update`s never took a date — the scoreboard was the outlier, and only because of this bug.** Expect this shape whenever a threaded value's only consumer is the bug being fixed.
- **Verification:** elm make 84 · elm-format · elm-review COLD clean (it drove the cascade) · old-vs-new bucketing simulation. NO test — `server/elm` has no test suite. Bundle regenerated in a second commit per the standing rule.
- 📌 No recalculation needed (decode-time arithmetic over the stored blob) — unlike B-149.

Original entry:
- `server/elm/src/Backend/Scoreboard/Decoder.elm` `sainitzeCriterionBySeverities` (~:70-127, keys at :76/:83/:90) + `sanitizeSingleValuePerMonth` (~:227-234): dedup dicts keyed on `diffMonths date currentDate` — a months-difference truncation, NOT a calendar bucket (Jul 20 + Aug 1 share bucket 0; Jul 2 + Jul 25 land in different buckets). View matches by calendar month → cross-month collapse (a normal Aug 1 measurement dropped because a severe Jul 20 one shares its bucket → column undercounts) and same-month leak (child counted in BOTH severe and no-stunting for July in the Stunting pane, violating the module's own stated one-value-per-month rule).
- Fix: key on `(Date.year date, Date.monthNumber date)`. Also fix the `sainitze` typo while there.
