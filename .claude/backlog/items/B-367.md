<!-- id:B-367 status:READY tier:3 round:31 -->
### B-367 — Acute Illness dashboard: every COVID figure counts only the OBSOLETE "COVID suspect" diagnosis — confirmed COVID (pneumonia / severe / low-risk) is counted nowhere, so the COVID donut slice and both COVID pages read 0 for current data; CHWs cannot diagnose COVID at all since 2025-02 — READY · CONFIRMED (the donut filter and the obsolete marker read by the orchestrator; card call sites and live counts scout-traced) · MED-LOW · tier 3 · R31 unit 2 · DEPLOYED, all sites; ihangane nurse encounters since 2025-09: `covid19-pneumonia` 89, `covid19-severe` 32, `covid19-low-risk` 2; stored `covid19` (suspect) 335 (2024) → 20 (2025) → 0 (2026)
- **Mechanism.**
  ```elm
  -- client/src/elm/Pages/Dashboard/View.elm:915-916
          covidCases =
              countAcuteIllnessCasesByPossibleDiagnosises [ DiagnosisCovid19Suspect ] True encounters
  -- client/src/elm/Backend/AcuteIllnessEncounter/Types.elm:30-31
        -- Obsolete. Used as a step toward determinate COVID diagnosis by nurse.
      | DiagnosisCovid19Suspect
  ```
  Same filter on the nurse COVID page's only card "Managed at Home" (`View.elm:1016-1020` → `Utils.elm:641-647`), all three CHW COVID cards (`Utils.elm:619-638`) and the CHW "currently under care" breakdown (`Utils.elm:1207-1208`). The nurse engine resolves to `DiagnosisSevereCovid19` / `DiagnosisPneuminialCovid19` / `DiagnosisLowRiskCovid19` (`Pages/AcuteIllness/Activity/Utils.elm:2538-2549`); CHW: `-- CHW may not diagnose COVID anymore.` (`:2513-2516`, `33d987ade`). Confirmed-COVID encounters carry fever (via `covid19SuspectDiagnosed`) but land in no donut slice — the respiratory/GI/malaria slices come from the non-COVID branch.
- **Impact.** Nurse and CHW dashboards on every site show 0 COVID; fever-by-cause donut under-counts fevers.
- **Backlog de-dup.** B-069 (dashboard sign), B-324 (COVID path gate, implemented). Not recorded.
- **Fix shape (guess).** Count the three confirmed diagnoses (with or instead of the suspect one). Whether the CHW COVID page should remain is a product question.
