<!-- id:B-140 status:UNKNOWN tier:3 round:18 -->
### B-140 — ❌ **CLOSED 2026-08-13 — NOT REQUIRED (user decision). ⛔ Do NOT re-propose the transfer.**
**Why:** a measurement-less `individual_participant` holds nothing worth carrying over. `eddDate` and `dateConcluded` — the only clinical content the bundle can hold — are written by `SetEddDate`, dispatched from exactly ONE place (`Pages/Prenatal/Activity/Update.elm:315-324`) in the SAME batch that saves `prenatal_last_menstrual_period` as a measurement. So an EDD implies a measurement, and a participant with a measurement is found by the encounter-type loop and transferred normally. What is left on a measurement-less one is `field_expected` (the enrolment date) and `field_encounter_type`. ⚠ I argued the opposite first, inferring from the field EXISTING on the bundle instead of checking who writes it — the user corrected it.
**Live check (ihangane, 2026-08-13, 159 soft-deleted persons / 149,594 participants):** exactly **2** participants whose person is soft-deleted, both with 1 encounter and **0 measurements** under it. 411097 (nutrition, 2020-10-15) is itself soft-deleted = the mechanism below, and holds only an enrolment date. 4772838 (2025-04-14) is NOT deleted — a live participant pointing at a deleted person, i.e. created after the merge and synced later (same explanation as the July audit's 5 orphaned relationships); a merge-time re-point would not prevent it. vhw: 0.
📌 **Still open from this entry, unrelated to the transfer:** `_consolidate_patient_newborn_references` (`hedley_patient.drush.inc:485-495`) has NO self-reference guard, so merging a mother with her own newborn yields `field_person == field_newborn`; the sibling transfers guard their analogous cases at `:322` and `:586`. Operator-error precondition, VERY LOW — worth folding into any future edit of that file.

### B-140 (original entry) — Patient merge loses a duplicate's ZERO-MEASUREMENT individual_participant (registration soft-deleted instead of transferred) — was READY · CONFIRMED (agent-found, self-verified in code 2026-08-04) · MED-LOW · merge-gap family
- `_consolidate_patients_execute` (hedley_patient.drush.inc:208-225) discovers content ONLY via `hedley_general_get_person_measurements($duplicate)` — an individual_participant with no measurements (e.g. a fresh antenatal registration) triggers no encounter-type sweep, still references the duplicate via field_person, and the delete cascade (hedley_person.module:1567-1571, bundles pmtct_participant/relationship/individual_participant) soft-deletes it. Encounters under it (possible with zero measurements) are left pointing at a soft-deleted participant. Pre-existing (not introduced by the b113-b117 fixes); sibling of the already-documented pmtct zero-measurement edge (2026-07-14 audit).
- Minor same-entry note (VERY LOW, operator-error precondition): `_consolidate_patient_newborn_references` (drush.inc:485-495) lacks a self-reference guard — merging a mother with her own newborn yields field_person == field_newborn. Sibling transfers guard their analogous cases (:322, :586). Add the guard while in the file.
- Fix shape: in the execute path, re-point the duplicate's measurement-less individual_participants (and their encounters) to the original before the field_deleted set — the registry + `hedley_general_get_person_content_associated_by_fields` make discovery trivial now. Fold a case into HedleyPatientConsolidation.test (1-install rule).
