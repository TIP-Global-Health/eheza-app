<!-- id:B-030 status:IMPLEMENTED tier:3 round:10 -->
### B-030 — Newborn-exam Pregnancy Summary: dirty flags set but NEVER read; birthWeight has no flag → contradictory/unclearable records — ✅ IMPLEMENTED 2026-07-19 (issue #1957, PR #1958, branch `b030-pregnancy-summary-dirty-flags`, base develop @c9d08268a, commit c090d6c92, Copilot requested). Fix: `pregnancySummaryFormWithDefault` now uses `maybeValueConsideringIsDirtyField form.apgarDirty/.birthLengthDirty` (flags already SET by View: number inputs + availability-No toggle which also clears the numeric) instead of plain `or`; ADDED `birthWeightDirty` (Model field + emptyPregnancySummaryForm + View birthWeight input setter + rebuild). Same helper the codebase uses for headCircumference (Utils:691) + NCD lipid panel. Exposed `emptyPregnancySummaryForm` from Model (emptyModel already exposed) for the test. NEW 6 tests in existing Pages/WellChild/Activity/Test.elm (refactored `all` to aggregate 2 describes); DISCRIMINATION: reverting fails exactly the 3 'cleared stays cleared' (apgar/birthWeight/birthLength), guards (untouched→saved, edited→wins) pass. Traced availability-toggle→save→reload consistency (signs built from availability, so old code left signs disagreeing with numeric). elm make 548, elm-test 2844, elm-format+elm-review clean. Local /code-review medium: no findings. This continues the dirty-flag class (B-031 is the NCD-recurrent sibling — creatinine/BUN/ALT/AST, still open).
- Flags set in `Pages/WellChild/Activity/View.elm` :280/:293/:316 (apgar incl. availability-toggle No → clears values + sets dirty) and :353/:375 (birthLength); `Pages/WellChild/Activity/Utils.elm:245/:249` merely COPIES the flags; grep proves zero consumers — `pregnancySummaryFormWithDefault` (:212+) resolves the numerics with plain `or` → switching "available? Yes→No" saves the old Apgar/birthLength anyway; a saved birthWeight (no dirty flag exists) can never be cleared.
- birthWeight drives the `< 2500` LBW flag (`ProgressReport/View.elm` ~:1767) and NCDA prefill. Fix: use `maybeValueConsideringIsDirtyField` for apgarOneMin/apgarFiveMin/birthLength (flags exist!), add `birthWeightDirty`. Severity MED-LOW.
