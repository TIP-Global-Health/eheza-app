<!-- id:B-167 status:UNKNOWN tier:4 round:19 -->
### B-167 — Wellbeing/MessagingCenter minors batch — mixed CONFIRMED / PLAUSIBLE · LOW · R19 Unit 1 · READY
- (a) **Cross-device last-writer-wins on the message-flags JSON blob** — PLAUSIBLE (dual-device frequency unweighed) · design-inherent: every read/favorite/snooze toggle PATCHes the FULL `resilience_messages` snapshot (`Backend/Nurse/Update.elm:29` patchFull; `Encoder.elm:20-29` whole-array), server field-overwrites (`HedleyRestfulSync.class.php:495-497`) — two devices on one nurse account in one sync epoch clobber each other's flags (read/favorite/snooze state only; same-device races closed in ms via the revision broadcast). Fix would be server-side merge by message id — bigger than the harm at current usage; recorded so the design cost is known if the program scales.
- (b) Duplicated unread predicate diverges `>` vs `>=` (self-verified: partition `MessagingCenter/View.elm:462-464` strict vs badge `Utils.elm:106-108` inclusive; mark-unread sets `nextReminder = currentTime`) → ≤10s badge/list disagreement per 10s tick. Fix: reuse `isMessageUnread` in the partition.
- (c) End-of-month-4 message renders `ResilienceMessageEndOfFourthMonthBullet4` in TWO consecutive `li`s (self-verified `View.elm:1465-1470`) — a fifth bullet was evidently intended; objectively-wrong-text class (qualifies under the declined-translation exception). Flag for author: EndOfSixthMonth closing with `EndOfFifthMonthParagraph3` (:1497) and StressManagement12 with `StressManagement8Paragraph2` (:1030) — possibly intentional reuse, ask before "fixing".
- Design observations, NOT defects (recorded to prevent re-finding): surveys hard-block the inbox until completed (intended 0/3/6-month protocol); reminder times computed in UTC so "8 AM" fires 10 AM Kigali — consistent skew, likely accepted.
