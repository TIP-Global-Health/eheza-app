<!-- id:B-182 status:IMPLEMENTED tier:3 round:20 -->
### B-182 — NCD completion: women 45-49 invisible for Family Planning + Pregnancy Test, and pregnancy-test validity reads the HIV test's fields — ✅ IMPLEMENTED 2026-08-18 (issue #2102, PR #2103, branch `b182-fertile-age-band` off origin/develop) · CONFIRMED · tier 3
- (a) PHP bands both to age 13-44 (`>= 13 && <= 44`, PHP:6866 family-planning, :~7052 pregnancy-test); client uses `isPersonAFertileWoman` = 13-49 (Backend/Person/Utils.elm:56-68; CLI NCD:61-62/:897-899). Live rwanda: 441 of 7,864 NCD encounters (~5.6%) are women 45-49 (age-NOW approximation — slight overcount). Fix: `>12 && <50`.
- ✅ **Shipped as `> 12 && < 50`, deliberately mirroring the client's wording** (`isPersonAFertileWoman`) rather than inventing a third phrasing of the same band. Truth table run: ages 12/13/20/44 unchanged, **45/47/49 flip to expected**, 50 still excluded.
- ✅ **The (b) fix is a pure variable substitution, and two facts confirm it before touching anything:** `ncd_pregnancy_test` DOES have `field_test_result`, and that field's allowed values are positive/negative/**indeterminate** — so the `!= 'indeterminate'` clause is meaningful for a pregnancy test too and is kept. The neighbouring blocks establish the intended shape beyond doubt: lipid panel reads `field_total_cholesterol`, liver function `field_alt_result`, urine dipstick `field_protein` — each its OWN measurement. The HIV block above is untouched and legitimately keeps `$hiv_test`.
- ⛔ **Review found a THIRD defect in the same block, and the USER'S CHALLENGE narrowed the fix (`6aa188c13`).** The corrected predicate still diverged from the client for an INVALID result: client `resolveLabTestDate` discards a test outright when a result exists and is invalid (**no expiry fallback**), PHP had `$valid_results_exists || $diff_days <= 35` ⇒ an **indeterminate result within 35 days counted as done while the app asked for the test again**.
- ⭐ **The user asked the right question: "are these delayed lab tests or RDTs?" — and it changed the scope.** `viewHIVTestForm` + `viewPregnancyTestForm` both route through **`prenatalRDTFormInputsAndTasks`**, which REQUIRES a result from `[TestPositive, TestNegative, TestIndeterminate]` once the test is performed+dated. So for these two the expiry clause was **vestigial**, and indeterminate is an ORDINARY outcome, not a corner case. I had recommended a blanket 3-site sweep; that was wrong.
- ✅ **The two NCD sites are written DIFFERENTLY on purpose**, because the client supplies `resultsExistFunc` differently (`Pages/NCD/Activity/Utils.elm:931-932`): `TaskHIVTest` passes **`always True`** ⇒ absent-result rows unreachable ⇒ PHP is just `if ($valid_results_exists)`; `TaskPregnancyTest` passes `.testResult >> isJust` ⇒ keeps the expiry case as `$valid || (!$exists && $diff < 35)`. ⚠ Note `<` not `<=`: the client discards at `>= labExpirationPeriod` (both constants are 35). Verified cell-by-cell with a truth table.
- 🅿 **`:8384` (prenatal loop) DELIBERATELY UNTOUCHED and NOT FILED (user, 2026-08-18).** It covers the LAB-TECH tests (blood_gprs, hepatitis_b, random_blood_sugar, syphilis, hiv, partner_hiv, hiv_pcr) where delayed results are real ⇒ the expiry clause is LOAD-BEARING there, and the discard rule applies to only 5 of 7 (client passes `isTestResultValid` for HIV/syphilis/hepatitis B/malaria/partner HIV, `always True` for the rest). Different work; not queued.
- ⚠ PHP only — `server/elm` is not involved, so NO bundle rebuild (unlike B-151/B-178).
- ⚠ Expectations are computed at GENERATION time, so stored report data keeps the old bands until regenerated (B-100 class).
- 📌 ⛔ **Verification trap hit and caught:** my first `grep`/`php -r` checks ran in `/var/www/html/ihangane` (the MAIN tree, on another branch) instead of the worktree, and reported "0 bands left" about code I had not edited. **Always `cd` into the worktree for verification greps** — a check run against the wrong tree is worse than no check.
- (b) PHP:6992-6995: `$valid_results_exists = !empty($hiv_test->field_test_result) ...` inside the `$pregnancy_test` block — copy-paste; affects suppression of re-expectation for result-less >35-day-old pregnancy tests. Fix: substitute `$pregnancy_test`.
