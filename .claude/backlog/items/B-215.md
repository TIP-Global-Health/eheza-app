<!-- id:B-215 status:IMPLEMENTED tier:3 round:23 -->
### B-215 — Patient Record (adult): the Acute Illness pane — the DEFAULT filter — never fetches its encounters, so an adult's illness history renders empty — ✅ IMPLEMENTED (issue #2196, PR #2197, branch `B-215-patient-record-acute-illness-fetch`, base `f0a3eaf34`) · CONFIRMED (orchestrator-read fetch module + default filter + dict writers; identical on origin/main) · MED · tier 3 · both sites · R23 unit 8
- **Mechanism.** `Pages/PatientRecord/Fetch.elm:43-74` `fetchForAdult` emits `[FetchIndividualEncounterParticipantsForPerson, FetchMotherMeasurements, FetchPrenatalEncountersForParticipants …] ++ prenatal measurements ++ children` — no `FetchAcuteIllnessEncountersForParticipants`. The pane (`Pages/PatientRecord/View.elm:~230/~377`) renders rows via `Pages.Report.View.viewAcuteIllnessDiagnosisEntry` (`Pages/Report/View.elm:1248`), which reads `getAcuteIllnessEncountersForParticipant db participantId` → `Backend.NutritionEncounter.Utils.getParticipantEncountersByEncounterType .acuteIllnessEncountersByParticipant` → `[]` when the dict has no entry → `Maybe.map2` yields `Nothing` → row dropped. The dict's only writers are the two explicit fetch handlers (`Backend/Update.elm:909-959`); `AcuteIllnessEncounterRevision` (`:5364`) only REMOVES the key. `FilterAcuteIllness` is `emptyModel`'s default (`Pages/PatientRecord/Model.elm:21`). The child branch is unaffected (`Backend.NutritionEncounter.Fetch.fetchForNCDAScoreboard` fetches them).
- **History (agent-traced in git):** `7603dd8bd` (2022-11) replaced the shared `Pages.AcuteIllness.Participant.Fetch.fetch` with a helper that still ran for everyone; `4231c5cd7` moved that helper into the child-only `Backend.NutritionEncounter.Fetch` and nothing restored the adult fetch.
- **Reachability.** Participant directory → patient-record icon (`Pages/People/View.elm:~324`) → `App/Fetch.elm:~476` → adult branch. Masked only if the same device previously opened that person's AcuteIllness participant page / progress report and no revision invalidated the entry since (the R10/R16 "masked in normal nav, unmasked on hard refresh" class — B-073/B-074 are the systemic siblings).
- **Impact.** The first pane a nurse sees on an adult's record shows "no entries" for someone with real acute-illness history. Display-only.
- **Fix shape (first guess):** in `fetchForAdult`, resolve `AcuteIllnessEncounter` participants with `resolveIndividualParticipantsForPerson` (already imported) and emit `FetchAcuteIllnessEncountersForParticipants ids`, mirroring `Pages/AcuteIllness/Participant/Fetch.elm`; a `List.member` test in the B-024 style discriminates it.
- ✅ **Built 2026-09-03.** `fetchForAdult` now resolves the acute-illness participants with the already-imported `resolveIndividualParticipantsForPerson` and emits `FetchAcuteIllnessEncountersForParticipants`, mirroring the antenatal binding beside it. The pane needs nothing else — `viewAcuteIllnessDiagnosisEntry` reads `encounter.diagnosis` and `encounter.startDate` off the encounter record, no measurements. The pane's participant set is `individualParticipantsByPerson` filtered on `AcuteIllnessEncounter`, exactly what the helper returns, so the fetch covers it. New `Pages/PatientRecord/Test.elm` (2 tests). ⭐ **Discrimination test run:** against the pre-fix `Fetch.elm` exactly the acute-illness test fails; the antenatal control passes either way. ⚠ **elm-review caught a leftover `ModelIndexedDb` import on the committed state** — the [[elm-review-before-every-push]] rule earned its keep again; amended before pushing. Verified: elm-format clean · elm-test 3,059 passed · `elm make src/elm/Main.elm` 548 modules · elm-review clean on the committed state via a fresh clone.
- ✅ **Reviewed 2026-09-03 — no correctness findings.** The review independently confirmed two things the build had left implicit: `Backend/Fetch.elm:247` already guards the empty-participant-list case, and the adult/child split in `fetch` lines up with the `PatientChild`/`PatientAdult`/`PatientUnknown` split in the view — **only the non-child branch renders the acute-illness pane**, so no branch is left unfetched. Its two LOW findings were both in the new test file and both **fixed in `a7b4ad69b`** at the user's direction, threads answered and closed: (1) the second test was named for a property it did not assert — renamed to *"and still asks for the antenatal encounters it asked for before"*, which is what it checks; the acute-illness leak it was nominally guarding is covered by the first test's exact-list comparison; (2) a comment about the fixture's 1985 birth date sat above the `db` binding rather than the `people` field it describes.
