<!-- id:B-048 status:IMPLEMENTED tier:2 round:11 -->
### B-048 — CHW village authorization is enforced only on the PinCode page; every UserPage checks the HC but never the village → reassigned CHW keeps working in the old village mid-session — ✅ IMPLEMENTED 2026-07-09 (issue #1919, PR #1920, branch `b048-chw-village-auth`, base develop @1ca6a4662, commit 6d32a5218, **ALL 10 CI CHECKS GREEN**, Copilot round addressed). Anchors held (App/View.elm:412 HC-only gate; PinCode/View.elm:51 role-aware check).
- COPILOT ROUND (1 comment, VALID): flagged pre-existing typo `isAuthorithedNurse` → `isAuthorizedNurse`. Initially deferred to keep scope tight; user pointed back at the review 2026-07-09 → DID the rename (commit bd82b67b5, 6 occurrences across Utils + Session/View + Clinics/View, pure rename). PR #1920 now = 2 commits (B-048 fix + rename), CI re-running.
  - ⚠ elm-review STALE-CACHE gotcha (new, cost ~5 min): after editing files that were previously cache-hit, elm-review surfaced 3 phantom `NoUnused.Variables: Backend.Entities is not used` (the module IS used — SessionId/HealthCenterId/ClinicId via `exposing (..)`; these live in the SUPPRESSED baseline). A COLD re-run (`rm -rf elm-stuff && mkdir elm-stuff` then elm-review) correctly applied suppressions → 0. LESSON: on a spurious elm-review finding after partial edits, re-run cold before believing it.
- Fix = DEDUP root cause: the role-aware check existed inline in PinCode + (Clinic-keyed) in `isAuthorithedNurse`, but App/View reimplemented a weaker HC-only version. Extracted `nurseAuthorizedForLocation : Maybe VillageId -> Maybe HealthCenterId -> Nurse -> Bool` into Backend/Nurse/Utils; routed ALL THREE sites through it (App/View = the fix; PinCode/View + isAuthorithedNurse = behaviour-preserving dedup, verified identical expression; isAuthorithedNurse's 2 callers Session/View + Clinics/View unaffected). Optional `SetVillage Nothing` DROPPED as unnecessary — PinCode renders `selectVillageOptions` (fresh selector) whenever location unauthorized, doesn't depend on villageId being cleared.
- Product caveat RESOLVED in-code (not a policy call): PinCode already treats village as authorizing (blocks unauthorized CHW), so this is inconsistent enforcement, not a new policy. User approved implement 2026-07-09.
- Verify: elm make Main exit 0; elm-format clean; **full-project elm-review 0 errors** (hit `NoPrematureLetComputation` first — `isChw` doesn't belong in the outer let since the helper computes the role internally; restored the original `( isChw, isLabTech )` tuple in the inner let); elm-test 2768 pass (7 new in NEW `Backend/Nurse/Test.elm`). Discrimination: mutate helper's CHW branch → HC-only (the old bug) → fails exactly the 2 CHW tests (revoked-village + no-village); restored → 2768/0.
- GOTCHA repeat: `loggedInNurse` outer-let binding first collided (SHADOWING) with pre-existing `( nurseId, nurse )` bindings deeper in viewUserPage — renamed mine to `loggedInNurse` (same class as B-052's `resolve` prefix).

ORIGINAL ENTRY (verified 2026-07-06):
- `App/View.elm:409-415` `viewUserPage` gate = `EverySet.member healthCenterId nurse.healthCenters` ONLY (no village check). `Pages/PinCode/View.elm:51-60` DOES check `isChw → EverySet.member villageId nurse.villages`. CHW nurse carries the parent HC in field_health_centers, so the HC check passes. Nurse revisions update `loggedInModel.nurse` mid-session (`UpdateNurseData`), but `model.villageId` is never re-validated off the PinCode page → after V1 is removed from field_villages, patient lists / village dashboards / registrations continue under V1 until the CHW happens to land on PinCodePage.
- Fix: compute the role-aware (village-for-CHW) check in `viewUserPage`, fall through to PinCode view on failure; optionally `SetVillage Nothing` when a revision drops the selected village. Severity MED (product Q: is village authorizing or just working-context? — the PinCode-page check implies authorizing).
