<!-- id:B-068 status:CLOSED tier:3 round:14 -->
### B-068 — `consolidate-patients` merge tool never migrates the duplicate's relationships → surviving patient loses family links — ✅ DONE via B-113 (PR #1946, 2026-07-15) — DUPLICATE of B-113 (same bug, same fix: re-point relationships before delete, dedup vs existing). The secondary "move pmtct_participant rows" note = the pre-existing narrow "duplicate enrolled in a group with zero measurements" edge case documented in the patient-merge audit (out of B-113 scope). Do NOT re-implement.
- `hedley_patient.drush.inc:208-233` `_consolidate_patients_execute` re-points measurements (field_person) + individual/family participants to `original`, then marks `duplicate` `field_deleted=TRUE`, but touches NO `relationship` nodes (field_person/field_related_to) and doesn't move pmtct_participant rows → any parent/child/caregiver relationship pointing at the duplicate now references a deleted person; original doesn't inherit those links (e.g. a merged child loses its link to its mother).
- Fix: load relationships where field_person/field_related_to == duplicate, re-point to original (dedup vs existing), before marking deleted. Severity LOW-MED (manually-run admin merge tool — verify whether it's an accepted limitation; merges DO happen per #1863's ecosystem).
