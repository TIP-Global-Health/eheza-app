<!-- id:B-358 status:READY tier:3 round:31 -->
### B-358 — Child Scoreboard progress report: the Immunization History pane is EMPTY for a child with no Well Child record — the pane reads vaccinations only from the latest Well Child encounter, while the NCDA scorecard in the same file falls back to the scoreboard's own doses — 3,341 children on ihangane — READY · CONFIRMED (both builders in the shared view read by the orchestrator; scout scratch render: heading, 0 rows) · MED · tier 3 · R31 unit 4 · DEPLOYED since `9415befab` (2023-11-27), ihangane only (Child Scoreboard); also sent in the WhatsApp report when "Immunization history" is selected
- **Mechanism.** The shared Well Child report view builds the pane's data from `maybeAssembled`, which exists only when a Well Child participant + encounter exist:
  ```elm
  -- client/src/elm/Pages/WellChild/ProgressReport/View.elm:362-383
          individualWellChildParticipantId =
              List.filter
                  (\( _, participant ) ->
                      participant.encounterType == Backend.IndividualEncounterParticipant.Model.WellChildEncounter
                  )
                  individualParticipants
                  |> List.head
          ...
          maybeAssembled =
              Maybe.andThen (\id -> generateAssembledData site id db |> RemoteData.toMaybe) ...
  -- :488-490
                          vaccinationProgress =
                              Maybe.map .vaccinationProgress reportData.maybeAssembled
                                  |> Maybe.withDefault Dict.empty
  ```
  The NCDA scorecard beside it already has the fallback:
  ```elm
  -- :1642-1648
          vaccinationProgressDict =
              Maybe.Extra.or
                  (Maybe.map .vaccinationProgress reportData.maybeAssembled)
                  (Maybe.map (generateVaccinationProgressDictByChildScoreboard site db)
                      reportData.individualChildScoreboardParticipantId
                  )
                  |> Maybe.withDefault Dict.empty
  ```
- **Reachability.** CHW Child Scoreboard encounter → Progress Report (report tab, not the scorecard tab) for a child with no Well Child participant; or Send via WhatsApp with Immunization history selected.
- **Evidence.** Scout scratch test: a child with only a Child Scoreboard participant and one BCG dose — `generateVaccinationProgressDictByChildScoreboard` returns the dose; rendering `viewProgressReport … (InitiatorChildScoreboard id)` finds `.pane.vaccination-history` with 0 `.entry.vaccination`. `child-scoreboard-encounter-chw.spec.ts:75,151` only asserts the pane is visible. Live ihangane (scout): 11,200 Child Scoreboard children; 3,341 have a scoreboard vaccine record with an administration date and no Well Child participant.
- **Backlog de-dup.** B-309 (server/elm Postnatal Care rows blind to scoreboard doses — the admin-report twin), B-292 (both-programme merge order), B-214 (checked the WC history folds all encounters). Not recorded.
- **Fix shape (guess).** Use the `:1642` `Maybe.Extra.or` fallback when building `vaccinationProgress` at `:488` (one shared binding for both panes).
