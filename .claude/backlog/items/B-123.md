<!-- id:B-123 status:UNKNOWN tier:1 round:16 -->
### B-123 — ✅ FIXED on develop (issue #1982 CLOSED 2026-07-26; verified in code 2026-08-03/R17: input converts via `setMuacValueForSite site` in `Pages/Prenatal/Activity/Update.elm` SetNutritionAssessmentMuac, display via `muacValueForSite`, unit label via `muacUnitTransIdForSite`, previous value converted; both alerts compare the STORED cm value so they were never site-wrong)
- MUAC is stored in cm everywhere. Burundi UI converts: `muacValueForSite` (×10, display mm), `setMuacValueForSite` (×0.1, on input), label `Translate.UnitMillimeter`. Reference implementation with comment: `Measurement/View.elm:3326-3337`.
- `Pages/Prenatal/Activity/View.elm:3442-3447` renders raw `form.muac` with `Translate.UnitCentimeter` **unconditionally**; alert thresholds hardcoded cm (18.5 / 22 at `:3434-3437`). **grep for `muacValueForSite` or `SiteBurundi` in that file = 0 hits.**
- ⇒ In Burundi a nurse habituated to mm meets one cm-labelled field; entering 220 stores 220 cm. No range check on it either (B-121/#1980), so nothing catches it.
- **LATENT: vhw.live has 1,405 prenatal encounters but ZERO prenatal nutrition-assessment measurements** — the Examination→NutritionAssessment task isn't in use there. Cheap now, awkward later (once records exist you can't tell a correct 22 from an intended 220).
- Burundi MUAC is otherwise healthy: `nutrition_muac` 1,685 recs avg 15.1cm, `acute_illness_muac` 26 recs avg 18.2cm, `well_child_muac` 104/107 correct (avg skewed to 119.2 by ONE garbage record of 11,111.2 — exactly what an entry range check would catch).
- Fix: apply the standard treatment (convert display + input, switch unit label by site) AND site-adjust the two alert thresholds.
