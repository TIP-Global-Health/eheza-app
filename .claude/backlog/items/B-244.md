<!-- id:B-244 status:IMPLEMENTED tier:2 round:25 -->
### B-244 — ✅ IMPLEMENTED 2026-08-26 (issue #2151, PR #2152, branch `b244-person-edit-wipes-photo`, base develop @99093f9e4, commit cff8df052) — all three parts in ONE PR per user instruction ("all three parts")
**What shipped (3 parts):** (1) **client** `SyncManager/Encoder.elm` — `replacePhotoWithFileId` now DROPS the image key when the change row has no upload row, instead of inserting `null`. Removal, not "leave it alone": the untouched key holds a URL string, which the backend cannot store in a file field. Fixing the shared helper also closes the untraced tail — Photo, NutritionPhoto, PrenatalPhoto, WellChildPhoto, FamilyNutritionPhoto and the StockUpdate signature all route through it. The person branch then became identical to `doEncode` (its `isJust avatarUrl` guard was doing what the helper now does), so it was collapsed into that call and the `Maybe.Extra`/`Json.Encode.Extra` imports went with it. (2) **backend** — `photo` joined `latitude`/`longitude` in the person-PATCH ignore guard in `HedleyRestfulSync.class.php`. This is the part that matters first: offline tablets run whatever build they last received. (3) **repair script** `hedley_person/scripts/restore-wiped-photos.php` — restores from the newest revision holding a photo, taking fid+alt+title+width+height from that one revision, and skipping + reporting any whose file is gone.
**Verification (all by running):** script proven end-to-end on the local ddev copy — wipe a person's photo via `node_save` → dry run reports 2 and writes nothing → real run restores fid 1 with its dimensions → re-run reports nothing (idempotent). The backend guard driven with five payload shapes (edit with the key dropped, an OLD client's `photo: null`, an edit carrying a new fid, registration POST, a measurement PATCH) — the first two ignored, the last three applied. ⚠ The guard is person-only, so a measurement PATCH with a null photo is protected by the client fix ALONE. Two regression tests added to `SyncManager/Test.elm` (3017 tests, was 3015); **discrimination proven** — against the pre-fix encoder "an edit that did not re-take the photo sends no photo key" FAILS. elm make 545 modules, elm-review clean, elm-format clean; php -l clean, phpcs Drupal + DrupalPractice exit 0 with a bad-file discrimination test (exit 2, 10 findings).
- ⚠ The `photo` field column is `field_photo_fid` and an image item carries alt/title/width/height alongside it — restoring only the fid would drop the dimensions.

### B-244 (original entry) — Person edit: saving a person whose photo was not re-taken sends `"photo": null`, and the backend deletes the stored photo — READY · CONFIRMED (full chain traced, both ends verified) · HIGH · tier 2 · R25 unit 5
- **The exact shape B-150 fixed for GPS coordinates, on a key that fix did not cover.**
- **Mechanism, in five verified hops.** (1) The edit form re-seeds the photo from the stored person — `Backend/Person/Form.elm:341` `applyDefaultTextInput photo defaultAvatarUrl identity` — so `avatarUrl = Just "<remote url>"`. (2) `Backend/Person/Encoder.elm:28` emits `( "photo", maybe string person.avatarUrl )`. (3) `PatchPerson` uses `sw.patchFull` (`Backend/Update.elm:4657`), whose full-value encoder puts **every** key in the change row — not just changed ones. (4) `SyncManager/Encoder.elm:112-120` then overwrites that key unconditionally whenever `isJust identifier_.entity.avatarUrl`: `replacePhotoWithFileId` (:85-97) resolves `Dict.get localId record.uploadPhotos |> Maybe.map (…fileId) |> Maybe.withDefault null`, and the upload row exists only if the service worker made one — which it refuses to do for a non-local URL (`client/src/js/nodes.js:1330`, `photosUploadUrlRegex = /\/cache-upload\/images/`, `sw.js:21`). A photo that came *down* from Drupal is a `/sites/default/files/styles/…` URL, so no row → **`"photo": null`**. (5) Server-side, `photo` is **not** in `$ignoredFields` (`HedleyRestfulSync.class.php:400-424`) and B-150's PATCH guard at `:455-459` covers only `latitude` and `longitude`, so the null reaches `setPropertyValues` and empties the image field.
- ⚠ This mechanism is already documented in [[photo-upload-fileid-encoder-wiring]] — *"a missing fileId encodes the image field as null (orphaned photo)"*. What is new is that a plain **edit** reaches it, because `patchFull` re-sends a photo key the user never touched.
- **Reachability.** Nurse → person page → edit → Save (`Pages/Person/Update.elm` `generateMsgsForPersonEdit` → `PatchPerson InitiatorEditForm`). Also every *secondary* patch carrying a DB-loaded person: the child-address propagation in the same function (so a parent's address edit can wipe the **children's** photos) and the WhatsApp/progress-report `PatchPerson` calls. Creation is unaffected — the photo-upload stage runs before entity upload (`SyncManager/Utils.elm:117-129`), so a freshly captured photo does have its row.
- **Impact.** Silent loss of patient identification photos on the backend, then on every device once the null syncs back down. The editing device keeps showing the photo until then (`nodes.js patchNode` writes the URL into local IDB), which is why this can run unnoticed. All sites, no feature flag.
- **Sizing is ANSWERED (terminus, 2026-08-26).** Persons whose photo field is empty while a revision still holds one: **ihangane 3,164**, **vhw 50**, **uvl 1**, **tip-somalia 0**. On all three affected sites every one of those old fids still has a live `file_managed` row, so all **3,215** are recoverable from the last non-empty revision. For scale: 119,342 Rwanda persons ever had a photo and 116,178 still do (~2.7% lost). B-150's GPS wipe was 77 persons.
- **Fix shape (guess).** Rewrite the `photo` key only when the change row actually has an upload row, and otherwise drop the key entirely so PATCH leaves the stored file alone. ⭐ A backend null-photo guard for person PATCH — mirroring the coordinates guard right beside it — should go in too: B-150's lesson is that old client builds keep sending the bad payload long after the client fix ships.
- 📌 Untraced tail, same shape: `doEncode` is unconditional for `Photo`, `NutritionPhoto`, `PrenatalPhoto`, `WellChildPhoto`, `FamilyNutritionPhoto` and StockUpdate `signature` (`SyncManager/Encoder.elm:128-162`), so any **re-save** of one of those measurements without retaking the image may null its image too. Only the person path was traced end to end.
