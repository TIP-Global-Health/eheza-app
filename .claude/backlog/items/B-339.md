<!-- id:B-339 status:IMPLEMENTED tier:1 round:30 -->
### B-339 — NCD diagnosis: re-scoring an older encounter after a later one exists reads the LATER encounter's stage (and diabetes/renal) as its history — a lab result entered for encounter A after encounter B was started rewrites A's hypertension stage to B's, and drops A's diabetes/renal finding when B already has it — ✅ IMPLEMENTED (issue #2240, PR #2241, branch `B-339-ncd-history-before-encounter`, base `1bd37a26d`, commit `c0ae3b50b`, 2026-09-14) · CONFIRMED (history builder, staging, both filter sites, the revision trigger and the pre-#2216 line read by the orchestrator) · MED-LOW · tier 1 (diagnosis rule) · R30 unit 3 · ⚠ the stage half is INTRODUCED by #2216 (B-286), merged, NOT deployed — live-sized: 1 lab result on ihangane saved after a later encounter of the same participant existed
- **Mechanism.** `generatePreviousEncountersData` (`client/src/elm/Pages/NCD/Utils.elm:78-102`) returns every NCD encounter of the participant except the one being scored — including encounters that started AFTER it — sorted newest first:
  ```elm
  -- client/src/elm/Pages/NCD/Utils.elm:78-102
  generatePreviousEncountersData currentEncounterId participantId db =
      getNCDEncountersForParticipant db participantId
          |> List.filterMap
              (\( encounterId, encounter ) ->
                  if currentEncounterId == Just encounterId then
                      Nothing
                  ...
          |> List.sortWith sortByStartDateDesc
  ```
  Since #2216 staging reads that list directly:
  ```elm
  -- client/src/elm/Pages/NCD/Utils.elm:121-127
          currentHypertensionCondition =
              ...
              resolveHypertensionCondition assembled.previousEncountersData
  ```
  and `resolveHypertensionCondition` takes the first entry carrying a hypertension stage — B, when B is later. Before #2216 the call was `resolveCurrentHypertensionCondition assembled`, which consed the encounter's OWN stored diagnoses onto the head (`git show 62b029b9d:client/src/elm/Pages/NCD/Utils.elm:320-328`), so A's own stage masked B. `filterDiagnosesOfDeterminedConditions` (`:182-202`) reads the same unfiltered list for `diagnosedPreviouslyWithDiabetes` / `diagnosedPreviously DiagnosisRenalComplications` — that half pre-dates #2216. The date-limited filter exists and is used by other readers in the same file (`filterPreviousEncountersDataToDate encounterDate assembled.previousEncountersData`, `:1013`, `:1081`; defined `Pages/Utils.elm:269-274`, strict `LT`), but not by generation.
- **Reachability.** Encounter A (e.g. Stage 1, creatinine / RBS / urine dipstick sent to lab) → a later encounter B for the same participant (only same-day is blocked) → A's lab result entered from Case Management (entry open 35 days) or A's recurrent phase. `NCDCreatinineTestRevision` / `NCDRandomBloodSugarTestRevision` / `NCDUrineDipstickTestRevision` (`Backend/Update.elm:3568-3621`) → `processRevisionAndAssessNCD A` → `generateNCDAssessmentMsgs` (`:9335-9357`) → `generateAssembledData A` → `generateNCDDiagnoses` → `SetNCDDiagnoses` when the set differs.
- **Impact.** A's stored `field_ncd_diagnoses` changes after the fact: progress-report row for A shows B's stage beside A's Stage-1 treatment; dashboard / reports hypertension counts; A's recurrent-phase tasks computed against B's history; a diabetes/renal finding on A removed when B carries it. Care at later visits is mostly unaffected (B already leads C's history). All sites. Scout scratch elm-test: A own `{Stage1}`, 145/95, creatinine 2.0, later B `{Stage3}` → `{Renal, Stage3}` (expected `{Renal, Stage1}`); A RBS 250 with later B `{DiabetesRecurrent}` → `{}`. The pre-#2216 contrast for the stage half is derived from the old source, not run. Live (ihangane, scout): 1 NCD lab result saved after a later encounter of the same participant was created (urine dipstick, 1 participant).
- **Backlog de-dup.** B-286 (own just-written stage — the fix that exposed this), B-287 (labs Case Management entry closed before the diagnosis exists — stale-set reader, different mechanism). Not recorded elsewhere.
- **Fix shape (guess).** Filter history to `assembled.encounter.startDate` (`filterPreviousEncountersDataToDate`) before staging and before `filterDiagnosesOfDeterminedConditions` — likely in `generateNCDDiagnoses` or in the assembly itself; check every `diagnosedPreviously*` reader in `Pages/NCD/Activity/Utils.elm` and `RecurrentActivity/Utils.elm` for the same question. Discriminating test: the scout's two fixtures above in `Pages/NCD/Test.elm`.

- **R31 unit 4 addition (2026-09-14) — more readers of the same shape in the NCD progress report.** Opened from an OLDER encounter (recurrent-encounter initiator), `generatePreviousEncountersData` still includes later encounters, and: `viewPatientProgressPane` puts `assembled.measurements` at the head (`Pages/NCD/ProgressReport/View.elm:700-702`), so the older encounter plots as the NEWEST point on the BP / glucose / HbA1c charts; for each later encounter `updateChronicDiagnoses` / `patientIsPregnantAtEncounter` filter `previousEncountersData` to dates before it (`Pages/NCD/Utils.elm:1082`, `:1022`) — a list that never holds the encounter the report was opened from, so that encounter's chronic stage and pregnancy answer are missing from later lines. Same root and trigger — fold into the fix (scout-traced; the anchors were not re-read by the orchestrator).

- ✅ **Built 2026-09-14 (issue #2240, PR #2241).** The cut is in `generateAssembledData`, not in `generateNCDDiagnoses` as the fix-shape guessed: `generatePreviousEncountersData … |> filterPreviousEncountersDataToDate encounter_.startDate`. That fixes every reader at once — generation (stage + determined-conditions filter), recurrent Next Steps (`medicateForHypertension` / `referForRenalComplications` recurrent arms also read the later encounter), the progress report opened from the older encounter (R31 addition), and the three `Backend/Update.elm` NCD paths that assemble. The latest encounter is unaffected. ⚠ Accepted edge: two same-day NCD encounters of one participant no longer see each other as history — same strict `LT` the report's per-encounter cut already uses. Live ihangane 2026-09-14: 217 same-day NCD pairs, but **210 are same-device doubles uploaded within 60 s (1 with measurements on both, last 2026-06-12 — the B-195 double-tap race, guarded by `isPostInFlight` since v1.18.1)**, 2 same-device on 2024-11-14, and **5 two-device pairs with data on both** (latest 2026-09-10) — the offline case. User 2026-09-14: *"this should only be possible for Acute Illness. In all other cases, there should be one encounter a day. It is possible to have this if 2 offline devices work on same patient, but it should not happen."* ⛔ A raw same-day pair count is not evidence that same-day encounters are a real workflow — break it down by device and upload gap first. Verified: 4 new `Pages/NCD/Test.elm` cases through a real `ModelIndexedDb` fixture (`assembledFromDb`) — 3 FAIL on pre-fix `Utils.elm` (Stage3 instead of Stage1; `{}` instead of DiabetesRecurrent; renal referral False), 1 guard (earlier encounter still history) passes both; full suite 3199 pass; `elm make Main` OK; elm-review clean. No e2e added. Not done: dropping the now-redundant `Maybe NCDEncounterId` parameter of `generatePreviousEncountersData` (single caller). B-364 (Prenatal twin) is a separate PR — different builder and readers.

- **Review round 1 (2026-09-14, `/code-review medium`).** One finding, posted inline (https://github.com/TIP-Global-Health/eheza-app/pull/2241#discussion_r4003438826): the progress report opened from an older encounter (`InitiatorRecurrentEncounterPage`, reached via Case Management pending labs) now hides every later encounter. Confirmed; the pre-branch report did show them but drew the older encounter as the newest chart point and dropped its chronic diagnoses from later rows (`ProgressReport/View.elm` `viewPatientProgressPane`: current `::` history, then `List.reverse`). Spec trail `167ef856c` → PR #465 → issues #448/#466: a Loom video + pane screenshots, silent on this. **User decision 2026-09-14: "We'll go with (a)"** — the report shows the patient as of the encounter it was opened from. No code change; thread answered and resolved. CI fully green on `c0ae3b50b` (10 checks incl. both e2e jobs and simpletest). Awaiting merge.
