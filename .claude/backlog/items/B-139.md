<!-- id:B-139 status:READY tier:4 round:18 -->
### B-139 — Labs-history sub-updater will silently swallow 18 lab-tech messages the day labs-history is opened to lab techs — READY · CONFIRMED-latent (agent-traced; unreachable today) · S · hardening
- From the R18 dead-save sweep (which found ZERO reachable dead-saves — the #1843 class is otherwise clean). `updateLabsHistory` (`Pages/Prenatal/RecurrentActivity/Update.elm`, catch-all at ~:1842) handles all 35 messages its view can dispatch TODAY, but the forms are built with `contentAndTasksLaboratorResultsConfig` (`View.elm:801-827`) which wires 20 messages, 18 of them UNHANDLED (all 10 `Set*TestExecutionNote` + 8 `Set*TestFormBoolInput` except HIV/PartnerHIV). They are unreachable ONLY because `viewLab` hardcodes `isLabTech = False` (`View.elm:878-880`) and every `*ResultFormAndTasks` renders config msgs behind `if isLabTech` (verified for all ten). Flipping labs-history on for lab techs reproduces #1843 exactly.
- Fix options: backfill the 18 handlers now (mechanical), or make the catch-all defensive (route unknown msgs to the main `update`, which is exhaustive), or at minimum extend the invariant comment at the catch-all naming the 18. Zero user-visible change today — pure regression insurance.
