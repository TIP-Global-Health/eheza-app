<!-- id:B-229 status:READY tier:4 round:24 -->
### B-229 — Trace contact: an edit issued while the contact is not in the cache is dropped silently with the request stuck at `Loading` — READY · CONFIRMED mechanism, race NOT reproduced · LOW · tier 4 · R24 unit 2
- **Mechanism.** `Backend/TraceContact/Update.elm:16-27` `EditTraceContact`: sets `saveTraceContact = Loading`, then `Maybe.map (editMeasurementCmd …) traceContact |> Maybe.withDefault Cmd.none`. `traceContact` comes from `Dict.get traceContactId model.traceContacts |> Maybe.andThen RemoteData.toMaybe` (`Backend/Update.elm:4103-4105`); when it is not cached, nothing is sent and nothing is reported. Same discard shape as B-196's `updateIndividualEncounterParticipant`, which now at least reports to Rollbar (`IndividualEncounterParticipant/Update.elm:110-129`); this sibling did not get that treatment. `saveTraceContact` has no reader in `Pages/`, so the stuck `Loading` is invisible.
- **Reachability.** `Pages/TraceContact/Update.elm:96-101` (no-contact outcome) and `:331-334` (symptoms recorded). Both derive the value from a contact the page already holds, so the cache is normally populated; the window is a forget/refetch race (the same one B-196 relies on). No deterministic trigger found.
- **Impact.** CHW's contact-tracing outcome/symptoms lost without error. Narrow.
- **Fix shape (guess).** Mirror the participant branch: Rollbar with cache state, and do not set `Loading` when no command is issued. Cheap to fold into any B-196 follow-up.
- 📌 Unit-2 clean notes: all 11 close paths write date-only `endDate`; Encoder⊇Decoder for all 14 patched entities (no dropped-patch); `sw.patchFull` sends full records; every `HandleSaved*` calls `triggerRollbarOnFailure`; `Session` passes `healthCenter = Nothing` but the server ignores it for non-person/stock types; `SetWellChildEncounterWarning` else-branch unreachable; `HandleSavedContactsTracing` cannot double-create (form disabled once measurement exists).
