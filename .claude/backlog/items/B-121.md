<!-- id:B-121 status:CLOSED tier:1 round:16 -->
### B-121 — ✅ SUPERSEDED/DELIVERED by the range-gate programme (PR #2007, merged 2026-08-02; issue #1980 closed) — re-checked against develop code 2026-08-03 (R17), per the handoff's "may be moot" flag. Constraint constants now exist for height/weight/MUAC(site-aware)/birth weight (grams — `getInputConstraintsBirthWeight`, with a comment warning off the kg constant)/birth length/fundal height/Apgar; Prenatal gates via `PreSaveNutritionAssessment` out-of-range check + `WarningPopupMeasurementOutOfRange` (incl. the carried-over-height branch). **Sole residue: HEAD CIRCUMFERENCE is still ungated and has NO constraint constant anywhere (grep 2026-08-03 = 0 hits; re-verified 2026-08-19 — `RangedMeasurement` has 8 members, HC absent)**. ⭐ R21 wave C gives clinicians the evidence to close this: **57 implausible non-zero HC values live** (33 in 0-25cm, 23 >60cm incl. 3700/5200/439, arriving every year 2022→2026); the save path (`PreSaveHeadCircumference` → `CloseHeadCircumferencePopup`, Pages/WellChild/Activity/Update.elm:536-550) shows a macro/microcephaly WARNING then saves anyway, so a mm/grams-typed entry produces a FALSE MACROCEPHALY referral prompt. Note: 29,129 HC zeros are NOT damage — 29,124 carry `measurement_notes='not-taken'` (convention; 5 stragglers). Fix on sign-off: add `MeasurementHeadCircumference` to `RangedMeasurement` + a constraint (~20-60cm suggestion for the clinician ask). — the lone exception among its gated siblings; unchanged from sub-case (a) below, still clinician-gated (a clinical range must be chosen first). The parked branch below (`b121-wire-range-gates`, unpushed 88ecbac35) is obsolete — do NOT resume it.
<details><summary>Obsolete parked-branch state (pre-#2007)</summary>
**What's on the branch (verified green: 548 modules, 2882 tests, elm-format + cold elm-review clean):** new exposed helper `outsideConstraintsWhenTaken : (Maybe Float -> Bool) -> Maybe Float -> Bool = isJust value && check value` in `Measurement/Utils.elm` + 6 tests; **Prenatal** nutrition assessment gated by making an out-of-range height/weight count as an INCOMPLETE TASK in `Pages/Prenatal/Activity/Utils.elm` (`heightTask`/`weightTask` substituted into `tasks_` and `tasksForBmi`); **WellChild birth length** gated at the save button in `Pages/WellChild/Activity/View.elm`.
**⚠ SCOPE WAS CUT TWICE, both times for good reason — do NOT re-add without reading:** NCDA weight+MUAC dropped → **issue #1983** (3 distinct failure modes: age-conditional MUAC rendering, step-tab bypass, `or`-hydration with no dirty flag resurrecting values that "not taken" cleared). Prenatal MUAC dropped → **B-123/#1982** (not site-aware). Birth weights dropped → **B-122/#1981** (grams, no gram constant).
**KEY DESIGN LESSON (cost 4 iterations):** do NOT bolt a save-block onto the button. Compute the violation where COMPLETION and RENDERING are decided. At the button it desyncs from what's on screen (dead Save on fields the nurse can't see) and from the task count (`resolveNextTask` skips a "complete" blocked task → **the whole nutrition measurement is silently lost**). Making it an incomplete task fixes both and makes the counter drop, which is the only on-screen feedback.
**OUTSTANDING findings from the medium review (not yet actioned):** (1) WellChild still blocks at the button while Prenatal blocks via task count — two mechanisms for one purpose; same task-based fix applies (`birthLengthTasks` → Nothing when out of range). (2) The Prenatal task-completion change is UNTESTED — reverting the `heightTask`/`weightTask` substitution leaves all tests green while restoring both the ungated save and the data loss; needs an `AssembledData` fixture. (3) MUAC sits ungated beside the two gated entries with nothing marking it deliberate. (4) The `-->` docblock example isn't run by anything (no elm-verify-examples).
</details>

### B-121 (original entry) — MUAC/height/weight range gates missing on roughly half the input sites (issue #1980) — READY · CONFIRMED (full sweep 2026-07-22) · ⚠ CLINICIAN-GATED, changes what can be SAVED · MED
**Direct descendant of B-029** (✅ PR #1917), which built `outsideConstraints` / `muacOutsideConstraints` (`Measurement/Utils.elm:280-297`, ranges at `:82-107` — height 25-250, weight 0.5-200, MUAC 5-99cm / 50-999mm Burundi) and wired 4 gates. B-029's own notes flagged two follow-ups that were **never queued**; this item absorbs them plus the wider gap found by the 2026-07-22 sweep.
- **GATED today:** Nutrition encounter h/M/w (`Pages/Nutrition/Activity/View.elm:258,287,448`), WellChild nutrition assessment h/M/w (`Pages/WellChild/Activity/Utils.elm:475,483,491`), AcuteIllness MUAC (`Pages/AcuteIllness/Activity/Utils.elm:384`), FamilyNutrition MUAC (`Pages/FamilyNutrition/Encounter/View.elm:556`), group session (`Measurement/View.elm:383-390`, nulls `saveMsg`).
- **NOT GATED:** ⚠ **Prenatal** Examination→NutritionAssessment height/weight/MUAC (`Pages/Prenatal/Activity/View.elm:3357/3392/3442`) — grep for any constraint identifier across `Pages/Prenatal/` = **0 hits**; ⚠ **shared NCDA form** weight/MUAC/birth-weight (`Measurement/View.elm:3272/3341/3665`) — **widest gap, reached from 4 call sites** (Nutrition, WellChild, ChildScoreboard, group session); **ChildScoreboard** (its only capture IS the NCDA form; 0 constraint hits); **WellChild** pregnancy-summary birth weight/length (`Pages/WellChild/Activity/View.elm:327/348`); **WellChild head circumference** (`Utils.elm:515`). Adjacent + ungated: Prenatal fundal height (`:3505`). ❌ **Prenatal BMI (`:3422`) is NOT affected** — same shared input and no range check, but `bmiUpdateFunc _ form_ = form_` is a no-op and the displayed value is always `calculateBmi form.height form.weight`, so nothing typed is stored; its only exposure is inherited from height/weight. (It is NOT html-disabled — `"bmi disabled"` is the inputClass, landing as a CSS class on the wrapper; `.disabled input { pointer-events: none }` blocks the mouse, not the keyboard. Inert because of the setter, not the styling.)
- **Head circumference is a distinct sub-case (B-029's candidate (a)):** there is NO `getInputConstraints*` for it ANYWHERE — a clinical range must be chosen before it can be gated at all. It feeds `zScoreHeadCircumferenceForAge`. Its 3 sibling tasks are all gated; it is the lone exception (`Pages/WellChild/Activity/Utils.elm:479-480`, `tasksIncomplete` only).
- **B-029's candidate (b) still open:** weight 0.5-200 kg is too wide to catch a child's dropped decimal (8.5→85 passes; range is shared with adult encounters). Age-aware constraints need clinician sign-off. ⚠ **Therefore "just wire the existing constants into Prenatal/NCDA" buys less than it sounds like** — verify the constants are right for each context before wiring.
- **Two structural facts:** (1) every gate is **save-only, never entry** — the shared inputs set `min "0"` and **no `max` at all**, so 1050cm is typeable everywhere (partially addressed by B-084/#1978, which only removes the SIGN); (2) `taskCompleted` is `isJust` (`Pages/Utils.elm:1177-1183`), so an out-of-range value **marks the task complete** — in ungated flows that is the ONLY condition, so it both completes the form and enables Save; in gated flows it produces "N/N tasks" beside a disabled Save with nothing explaining why.
- **Why clinician-gated:** this changes what a nurse is ALLOWED TO SAVE. Blocking a save that works today needs sign-off, and the bounds need confirming per context. Decide the intended behavior first (gate the save / warn but allow / bound entry), then wire consistently. Worth considering whether the shared inputs should take the constraint record directly, so a new measurement input cannot be added without one.
- **B-029 lesson that applies here:** when a fix's motivating example is a number, CHECK IT AGAINST THE ACTUAL CONSTANT — the guard's real reach may be narrower than the story.
- ⚠️ **UNITS DIFFER ACROSS THESE FIELDS — the 3 existing constants do NOT cover them all** (checked 2026-07-22): Prenatal height cm ✓ / weight kg ✓ / MUAC `MuacInCm` but NOT site-aware (→ B-123/#1982); NCDA weight `WeightInKg` ✓ / MUAC site-aware ✓; **NCDA + WellChild birth weight is `WeightInGrm` — GRAMS, and NO gram constant exists** (→ B-122/#1981, where wiring the kg constant would reject every real newborn and accept every wrong kg entry); WellChild birth length `HeightInCm` but 25-250 is adult-ranged for a ~45-55cm newborn.
- Two child items split out and PRIORITISED above this one: **B-122** (#1981, live-confirmed indicator failure) and **B-123** (#1982, latent Burundi unit mismatch). B-121 itself remains the clinician-gated "wire the range gates" work.
