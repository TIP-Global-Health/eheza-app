<!-- id:B-426 status:READY tier:4 round:36 -->
### B-426 — Prenatal Completion report: a vitals recheck is expected at postpartum (and any later nurse) encounters with borderline blood pressure, where the app never schedules one — READY · CONFIRMED (orchestrator read both sides; sized live) · LOW · tier 4 · R36 unit 5 · DEPLOYED (Completion data regenerates by manual script)
- **Server.** The rule has no encounter-type or first-encounter gate and the driver calls it for every encounter:
  ```php
  // server/hedley/modules/custom/hedley_reports/hedley_reports.module:8659-8688
  function hedley_reports_prenatal_add_vitals_reckeck_activity(array $encounter_data, array &$expected) {
    ...
    if (($sys >= 140 && $sys < 160) || ($dia >= 90 && $dia < 110)) {
      $expected[] = 'vitals_recheck';
    }
  }
  // :7833   hedley_reports_prenatal_add_vitals_reckeck_activity($encounter_data, $expected);
  ```
  Completion needs `field_sys_repeated` + `field_dia_repeated` (`:8838-8853`).
- **Client.** Scheduling is refused once any nurse encounter precedes, and postpartum encounters count as nurse encounters:
  ```elm
  -- client/src/elm/Backend/Update.elm:9006-9014 (generatePrenatalLabsTestAddedMsgs)
                  if
                      -- We allow Vitals recheck only during initial encounter.
                      -- At subsequent encounter, no need to retake blood preasure.
                      (test == Backend.Measurement.Model.TestVitalsRecheck)
                          && (not <| List.isEmpty assembled.nursePreviousEncountersData)
                  then
                      []
  ```
  (`isNurseEncounter` includes `NursePostpartumEncounter`, `Backend/PrenatalEncounter/Utils.elm:128-130`.) The repeated-vitals form exists only on the recurrent page (`RecurrentActivity/Utils.elm:584`). At postpartum encounters the client produces no blood-pressure diagnoses (`resolveLabResultsAndExaminationDiagnoses`, `Activity/Utils.elm:3462-3471`), so PHP's diagnosis early-returns do not fire either. The three BP predicates themselves are string-for-string identical on both sides.
- **Impact.** Completion report only (not in the postpartum mandatory list, so Next Steps are unaffected): those encounters stay below 100 % forever. Live ihangane (2026-09-22): 8 postpartum encounters with a borderline BP. Subsequent ANC nurse encounters with all point-of-care labs are the weaker second arm, not sized.
- **Fix shape (first guess):** restrict the PHP rule to `nurse` encounters with no previous nurse encounter, mirroring the client guard.
