<!-- id:B-354 status:READY tier:4 round:30 -->
### B-354 — Family Nutrition (server): two person-content walks skip `family_participant` — deleting a person leaves their family participant (and its encounters) live, and a person's shard change is not applied to their family encounters, so devices at the new health centre get measurements without the encounter — READY · CONFIRMED (both bundle/branch lists and the shard sibling read by the orchestrator) · LOW · tier 4 · R30 unit 5 · LATENT, tip-somalia only (family_nutrition flag): 0 deleted persons, 991 family encounters with 0 missing shards
- **Mechanism (deletion).**
  ```php
  // server/hedley/modules/custom/hedley_person/hedley_person.module:1542-1546 (hedley_person_content_affected_by_deletion)
    $bundles = [
      'pmtct_participant',
      'relationship',
      'individual_participant',
    ];
  ```
  `family_participant` references the adult through `field_person`, so the association query finds it and this filter drops it. `hedley_person_process_deleted_update` cascades a participant's deletion to encounters only for `elseif ($node->type == 'individual_participant' && $deleted)` (`:1295`) — no `family_participant` branch.
- **Mechanism (shards).**
  ```php
  // server/hedley/modules/custom/hedley_patient/hedley_patient.module:184-194
    // by individual_participant field.
    if (!empty($individual_participants)) {
      $query = hedley_general_create_db_select_query_excluding_deleted();
      $query->join('field_data_field_individual_participant', 'ip', 'ip.entity_id = n.nid');
  ```
  while the assignment sibling gives family encounters their participant's person's shards (`hedley_device/hedley_device.module:240-244`, `elseif (in_array($node->type, $family_encounter_types))`). A pmtct_participant insert at another HC's clinic re-shards the person, family participant and measurements, but not `family_nutrition_encounter`.
- **Reachability.** Deletion: admin "Mark as deleted" on a person or family_participant (patient merge is safe — it moves family encounters first). Shards: sync upload of a pmtct_participant → `hedley_patient_node_insert` → recalculate for person → this walk.
- **Backlog de-dup.** B-085 (person shards on HC change), B-218 (un-delete does not reverse the cascade), B-219 (consolidation shards), B-192 (family plugins' join siblings). This bundle omission is not recorded.
- **Fix shape (guess).** Add `family_participant` to the deletion bundle list plus a `family_participant && $deleted` branch using `hedley_person_load_family_participant_encounters_ids`; collect family participant ids in the shard walk and join on `field_family_participant`.
