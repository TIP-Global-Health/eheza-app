<!-- id:B-222 status:IMPLEMENTED tier:4 round:23 -->
### B-222 — Person edit: a failed PATCH is recorded as `Success`, so the edit form shows no error — ✅ IMPLEMENTED 2026-09-17 (issue #2268, PR #2269, branch `B-259-B-222-person-save-request`, commit 48a18fb56; one PR with B-259 and B-222 at the user's call; entering the create/edit person page also sends the new `ResetPostPersonRequest`, and `HandlePatchedPerson` stores `RemoteData.map (always personId) data`; verified by elm make (548 modules), elm-review clean, elm-test 3217 passed — no test added, page-entry wiring has no unit seam; the contact-tracing form's reader of `postPerson` left alone (obsolete feature)) · CONFIRMED (orchestrator-read handler) · LOW · tier 4 · both sites · R23 unit 7
- **Mechanism.** `Backend/Update.elm:4662-4691` `HandlePatchedPerson`: `postPerson = Success personId` unconditionally; `triggerRollbarOnFailure data` fires but the RemoteData never becomes `Failure`. Sibling `HandlePostedPerson` (`:~4606`) stores `data` as-is. `Pages/Person/View.elm:~547/~803-826` renders `Failure` as a warning and `Success` as a banner only when the form has no changed fields — after a failed patch the form still has changes, so nothing is shown.
- **Reachability.** `Pages/Person/Update.elm:~317/~324` → `PatchPerson InitiatorEditForm`. Failure must come from `sw.patchFull` (local SW/IndexedDB write) — rare.
- **Impact.** Nurse's edit silently not saved, no error on screen (Rollbar sees it).
- **Fix shape (first guess):** store `RemoteData.map (always personId) data`.
