<!-- id:B-036 status:READY tier:4 round:10 -->
### B-036 — NutritionFeeding `sachetsPerDay` deliberately cleared on supplement-type change, silently resurrected at save — READY · CONFIRMED
- Clear: `Pages/HomeVisit/Activity/Update.elm:45` + `Pages/WellChild/Activity/Update.elm:1695` (`supplementType = Just value, sachetsPerDay = Nothing, …`). Resurrect: `Measurement/Utils.elm:10084` `sachetsPerDay = or form.sachetsPerDay (Just value.sachetsPerDay)` → non-RUTF record keeps stale sachet count. Fix: dirty flag, or clear to `Just 0`. Severity LOW.
- ⬆ **Extended R24 unit 7 (2026-08-24): the same resurrection hits the three derived booleans.** Saving with `receiveSupplement = False` persists no `RationPresentAtHome/EnoughTillNextSession/SupplementShared` signs; on re-edit `nutritionFeedingFormWithDefault` (`Measurement/Utils.elm:~10524-10534`) merges them as `Just False` (absent sign → `EverySet.member … |> Just`). `receiveSupplementUpdateFunc` (`Measurement/View.elm:~3963-3970`) resets them to `Nothing` when the answer flips to Yes, the merge restores `Just False`, so the three tasks count complete (`View.elm:~3954-3956`) pre-answered "No" — and `sachetsPerDay` likewise comes back as `Just 0` (`maybeToBoolTask (Just 0)` = complete). `supplementType` is the one field guarded (`Just NoNutritionSupplementType` treated as incomplete); its siblings are not. Same fix (dirty flags, as `ongoingTreatmentReviewFormWithDefault` uses). Reachable from Home Visit → Feeding (completed tab) and Well Child. Severity stays LOW-MED; ✏ retitle at build time: "Feeding form: re-editing a saved 'no supplement' answer pre-fills the follow-up questions as No".
