<!-- id:B-129 status:IMPLEMENTED tier:1 round:17 -->
### B-129 — ✅ IMPLEMENTED 2026-08-04 (issue #2037, PR #2038, branch `b129-wash-rows-swapped`, base develop @c37f2fe3c, commits 11fb515ed + 83eb4e2c7) — WASH rows swapped
- **FINAL SHAPE (user, 2026-08-05, third iteration): the LABELS are listed in STORED-ROW ORDER** — pane now reads clean water first, toilets second (`[ HasCleanWater, HasToilets, ... ]`). Rejected my intermediate `values = [row2, row1, ...]` pairing because that leaves a compensation someone must maintain; label order is self-evident and cannot come apart. **Cost, accepted knowingly: row order differs from #720's mockup and from the child scorecard (both toilets-first) — labels stay correct, only two adjacent rows swap places.** Lesson: when a positional coupling is wrong, prefer re-ordering ONE side to match the other over compensating in the middle.
- **Fixed in the VIEW, not the backend** (user's call, and it is the right one). First draft swapped the PHP `row1`/`row2` case targets; the user asked whether moving it client-side would avoid a regeneration, and it does something stronger: **renumbering the backend rows creates a MIXED-SEMANTICS WINDOW**. The blob is recalculated per person by the AQ worker on any measurement change, so between deploy and the end of a full regeneration some people carry the new numbering and the rest the old — and the pane SUMS ACROSS BOTH, showing a blend of toilets and clean water. That is worse than today's consistent-but-mislabelled state. Fix = label order matches stored row order in `Pages/Scoreboard/View.elm` + a comment on BOTH ends (the PHP where rows are written, the Elm where they are read). Zero regeneration, zero revisions, zero device re-sync. Displayed order unchanged (toilets first, per #720 + the child scorecard).
- ⛔ **NEW STANDING RULE (user, 2026-08-04): a `server/elm` change MUST include the regenerated bundle in the PR.** `elm make src/Main.elm --output ../hedley/modules/custom/hedley_general/js/elm-main.js` (as `server/elm/elm-watch.sh` does); the artifact is COMMITTED. Found while applying it: the bundle was last built at `e93c88858` (2026-05-31) with **Elm 0.19.1** (project pins 0.19.2 → ~200 lines of `\n`→`\u000A`, `'`→`\u0027` escape churn on every rebuild), and **`6968b111b` (2026-07-13, nutrition prevalence percentages) had NEVER been compiled in** — that fix has been inert on develop. ⚠ Two PRs rebuilding the bundle CONFLICT on merge: resolve by REBUILDING, not by picking a side.
- **Provenance:** `702353677` → PR #721 → issue #720, whose body is the design mockup: toilets, clean water, handwashing, kitchen garden, bed nets. (Rows 4/5 render bed nets before kitchen garden in BOTH Elm surfaces — internally consistent, vertical-order-only difference from the mockup, deliberately NOT changed.)
- **Verification:** elm make (server app, 84 modules) · elm-format · elm-review COLD clean · php -l + phpcs clean. No test — `server/elm` has NO test suite at all.
- ⭐ **DURABLE RULE learned here — apply to every future `field_ncda_data` change:** a fix that changes what the STORED blob means needs a full regeneration to be correct, and until it finishes the aggregate mixes both meanings. A fix in the reading view never does. **Prefer the view when the choice exists.** Corollary discovered while costing it: `hedley_ncda_calculate_aggregated_data_for_person()` ends in an UNCONDITIONAL `node_save($person)` (`:1000-1001`), and device sync selects on `node.vid > base_revision` (`HedleyRestfulSync.class.php:142`) — so a full regeneration mints a new revision for EVERY person and re-downloads the entire person table to every device in the field. **Proposed follow-up (not built, not yet a B-item): compare-before-save in that function** so regenerations only write what actually changed.
- 📌 B-149's targeted regeneration is now the ONLY one the release needs. Affected persons = those whose birth weight is `< 300` or in `[2000, 2500)`; query in the B-149 entry's session notes (join `field_data_field_weight` bundle `well_child_pregnancy_summary` + `field_data_field_birth_weight` to `field_data_field_person`).

Original entry:
- Elm labels pane-5 rows `[HasToilets, HasCleanWater, ...]` (`server/elm/src/Pages/Scoreboard/View.elm` ~:1058) but PHP fills `pane5.row1` from **`has-clean-water`** and `row2` from **`has-toilets`** (`hedley_ncda.module` ~:780-787). Rows 3-5 align. One-line fix on either side (swap the Elm label order to match PHP, or the PHP case targets — pick the side that keeps other consumers consistent; client-side scorecard reads signs directly and is unaffected).
