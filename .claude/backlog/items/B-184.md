<!-- id:B-184 status:READY tier:4 round:20 -->
### B-184 — Prenatal completion: postpartum SpecialityCare triggers omit hypertension — READY · CONFIRMED (anchor verified) · tier 4 (bounded: 893 postpartum encounters)
- PHP:8879-8886 merges HIV + diabetes + preeclampsia diagnoses; client's NCD section triggers on hypertensionLIKE (chronic/gestational hypertension + preeclampsia + eclampsia) + diabetes (Pages/Prenatal/Utils.elm:3105-3110). PHP has the right list one function over (`hedley_reports_prenatal_hypertensionlike_diagnoses()`, used :9029-9031). Fix: use it here too.
- ⚠ **R25 unit 7 — THE RECORDED FIX IS INSUFFICIENT ON ITS OWN.** `hedley_reports.module:8880` pushes the whole field item (`$diagnoses[] = $diagnosis;`) where the sibling loop at `:7703` correctly pushes `$diagnosis['value']`. The `array_intersect($triggering_diagnoses, $diagnoses)` at `:8893` therefore compares diagnosis strings against `"Array"` and can never match — the scout confirmed this by running it. Adding hypertension to the triggering list changes nothing until that line is fixed too. (Script-only reachability, so it is not filed as its own item — it lives in the completion-generation family, and the whole Completion batch is gated on "is the report used?")
