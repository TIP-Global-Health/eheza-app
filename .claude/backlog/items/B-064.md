<!-- id:B-064 status:IMPLEMENTED tier:2 round:14 -->
### B-064 — Child scorecard immunization "behind-by-month" row mis-buckets for children born on day 29/30/31 (month-indexing off-by-one) — ✅ IMPLEMENTED 2026-07-09 (issue #1923, PR #1924, branch `b064-scorecard-immunization-bucket`, base develop @1ca6a4662, commit 2f6102163, CI running, Copilot requested). User chose Option 1.
- Fix (Option 1, index-keying): in `immunizationByAgeInMonths` (View.elm ~:2413) `Maybe.andThen`→`Maybe.map` + `|> distributeByAgeInMonths child` → `|> distributeByListIndex` (NEW top-level helper keying by list position). Lifted `resolveLastDayForMonthX` to top-level (unchanged logic — still needed INSIDE the lambda for the reference-date vaccine filter) + exposed both for testing. Sibling `distributeByAgeInMonths` usage (~:2336, acute-illness ORS/Zinc row) uses REAL encounter.startDate → NOT the bug, left alone.
- Verify: elm make 548 exit 0; elm-format clean; **elm-review 0 (cold)**; elm-test 2764 pass (3 new in `Pages/WellChild/Test.elm`). EMPIRICALLY confirmed the collision: day-31 birth → old reference-date bucketing yields <25 distinct buckets; distributeByListIndex → all 25; mid-month (day-15) → exactly 0..24. Discrimination: mutate distributeByListIndex to `index // 2` → the day-31 "all 25 months" test fails; restored → 2764/0.
- PR body first line = `Fixes #1923` per new rule [[pr-description-issue-link-first-line]].

ORIGINAL ENTRY (verified 2026-07-06):
- `Pages/WellChild/ProgressReport/View.elm:2455-2476`: `immunizationByAgeInMonths` builds synthetic `(referenceDate, cellValue)` tuples via `resolveLastDayForMonthX index` then `|> distributeByAgeInMonths child`, which RE-buckets each by `diffMonths birthDate referenceDate`. `resolveLastDayForMonthX` anchors to `Date.floor Date.Month childBirthDate` (FIRST day of birth month, loses the birth day-of-month) → +（monthX+1) months −1 day = last day of a month whose day-count may be < the birth day.
- VERIFIED against justinmimbs/date 4.1.0 source (`Date.elm:1180-1210`): `toMonths = 12*(year-1)+(month-1) + day/100`, `diff Months = truncate(Δ)`. Birth Jan 31: `resolveLastDayForMonthX 1` = Feb 29; `diffMonths(Jan31, Feb29) = truncate(24277.29 − 24276.31) = 0` → index-1 collides into bucket 0, bucket 1 renders "-". Fires at every reference month with fewer days than the birth day-of-month (worst for day-31 births — Feb/Apr/Jun/Sep/Nov all collide → ~⅓ of the row wrong/gapped). Real-date rows (other scorecard rows keyed by actual measurement dates via distributeByAgeInMonths) are UNAFFECTED — only this synthetic-referenceDate row.
- Fix: key directly by the synthetic index (`List.indexedMap (\i (_,v) -> (i,v)) |> Dict.fromList`) instead of round-tripping through a date; or anchor resolveLastDayForMonthX to the birthdate day, not its month-floor. Severity MED (a full clinical row renders wrong values + spurious dashes for ~10%+ of children).
