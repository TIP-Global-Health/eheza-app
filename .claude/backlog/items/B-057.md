<!-- id:B-057 status:IMPLEMENTED tier:3 round:12 -->
### B-057 — pmtct_participant / relationship shards: post-insert recalc SETs one person's shards, clobbering the presave two-person UNION — ✅ IMPLEMENTED 2026-07-20 (issue #1963, PR #1964, branch `b057-shard-union-linking-records`, base develop @c9d08268a, commit 6191cc4d5, Copilot requested). Fix: extracted `hedley_general_two_person_node_shards($node)` (union of both linked persons; relationship=field_person+field_related_to, else pmtct=field_person+field_adult) and used it in BOTH the presave (deduped the 2 identical cases in hedley_device_assign_shards) AND `hedley_patient_recalculate_shards_for_person_content` (:201 loop — two-person bundles get union, else person_shards). Both persons' recalcs now compute same union → last-writer race gone. KEY: presave `hedley_device_assign_shards` RETURNS EARLY when field_shards already populated (:227-230), so post-insert `_content` is the SOLE maintainer → the clobber genuinely sticks (bug confirmed real). Recompute (not merge) so it contracts when a person leaves an HC. NEW SimpleTest testLinkingRecordShardsSurviveRecalculation in HedleyPatientConsolidation (relationship, drives _content directly; relationship has no node_insert recalc so setup is clean); DISCRIMINATION: middle assert fails pre-fix (adult recalc→[hc1] drops child-only hc2), passes post-fix — verified by logic (simpletest runs in CI, not locally: ddev-from-worktree spawns stray project). php -l + phpcs Drupal+DrupalPractice clean. Local /code-review high: no findings.
- Presave `hedley_device.module:284-289/294-298` correctly sets `field_shards = array_unique(array_merge(shards_child, shards_adult))` for pmtct_participant/relationship. But `hedley_patient_node_insert:84-85` recalcs BOTH persons; each fires `hedley_patient_recalculate_shards_for_person_content` which walks association-field content (incl. these two bundles) and `:196 $wrapper->field_shards->set($person_shards)` — SET, not merge → last-writer (adult, :85 after :84) overwrites the participant with the adult's shards only, dropping a shard the child carries but the adult doesn't.
- Concrete: child C in [HC_A,HC_Y], adult A in [HC_A], new participant P links C+A → presave [HC_A,HC_Y] → recalc overwrites to [HC_A] → P never syncs to HC_Y devices. Reachable by official client (asymmetric multi-HC enrollment; uncommon shape). Fix: for the 2 two-person bundles, merge (`array_merge(existing, person_shards)`) or recompute via the device union logic. Severity LOW-MED (affects only linking records, not measurements).
