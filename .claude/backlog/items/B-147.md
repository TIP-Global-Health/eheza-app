<!-- id:B-147 status:READY tier:3 round:18 -->
### B-147 — AcuteIllness inline save buttons: view-only gating, handler always attached, NO update-side completeness guard → keyboard/programmatic activation saves incomplete forms — READY · CONFIRMED (E2E-audit trace: `Pages/AcuteIllness/Activity/View.elm:526-529` + 5 sibling sites; handlers e.g. SaveSymptomsGeneral `Update.elm:298-325` unguarded; Semantic CSS pointer-events:none blocks mouse only) · MED
- The long-known "two Save-button DOM variants" fact, now traced to a real defect: `customButton` attaches onClick only when active, but the AI inline variant always attaches it and relies on CSS `pointer-events:none` — keyboard Enter / dispatchEvent fires the save with the form incomplete. Fix: either guard the 6 view sites (attach onClick only when enabled, matching customButton) or add update-side completeness checks. Pairs with TH-20 below (a must-not-save e2e that would fail today).
