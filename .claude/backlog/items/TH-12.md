<!-- id:TH-12 status:READY tier: round:16 -->
### TH-12 — Lab-tech prenatal LabResults fold-in — READY · S
- `lab-tech-encounter.spec.ts` test 'nurse orders labs, lab tech enters results' (:34), `completeLabResultsAsLabTech` :111 (helper `helpers/prenatal.ts:936-~1030`; nurse phase routes tests to Lab via `completeLaboratoryNurseForLab` :60). Elm (isLabTech=True, dispatch `Pages/Prenatal/RecurrentActivity/View.elm:191-299`): RandomBloodSugar `Measurement/Utils.elm:5137-5166` — confirmation task + result section only after `runConfirmedByLabTech == Just True` → N=2. Fold-in (cleanest tab, single numeric): confirm Yes → 1/2, assert disabled BEFORE entering result → fill `sugar-count` → 2/2 active → save. Richer alternative: UrineDipstick VariantShortTest :4745-4786 (confirm + protein/ph/glucose → N=4; assert at 3/4). Must special-case ONE tab (helper fills selects/numerics in generic loops). Backend assert exists: `nodes['prenatal_labs_results']` :137-139. Note: page is `div.page-activity.prenatal` reached from Case Management.

### TH-12 upgrade note (from track parent): instead of special-casing one lab tab, the per-tab loop variant asserts the gate on ALL ~8 lab tabs (inside the `testPerformed.isVisible()` branch: after Yes-click assert disabled, after fill-loops assert active) — ~3-4s CI, M effort; covers the whole result-form family incl. lab-tech-only confirmation counting (`hemoglobinResultFormAndTasks` :5395-5402, `syphilisResultFormAndTasks` :5790 conditional symptom tasks). TH-11 note: parent's independent stock design converged on the same omit-signature shape; only the negative half is new (clickSave already requires .active).
