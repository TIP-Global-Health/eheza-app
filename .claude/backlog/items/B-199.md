<!-- id:B-199 status:READY tier:3 round:21 -->
### B-199 — Group enrolment/relationship duplicates from unconditional posts and local-dedup races: 1,101 children double-enrolled in the same clinic + 65 duplicate relationships — READY · CONFIRMED (writer anchors agent-verified; counts sampled) · tier 4 · ongoing (2026: 25) · R21 wave B
- (a) `Backend/Update.elm` `PostRelationship` posts a PmtctParticipant UNCONDITIONALLY when `addGroup` is set — the relationship is deduped against existing rows, the enrolment is NOT (`isPostInFlight` only guards concurrent Loading). 1,101 (child, clinic) pairs with >1 un-deleted enrolment, identical expected-windows, same-burst adjacent nids. Client group UI dedups by person-id dicts (mostly invisible); `hedley_stats_get_pmtct_data` emits one dashboard item PER ENROLMENT ROW (PLAUSIBLE inflation — downstream dedup unverified). Fix: check existing participation before posting, mirroring the relationship dedup.
- (b) 65 duplicate same-direction relationships (both un-deleted, latest 2025-10) — local dedup runs before the earlier copy synced down. Self-relationships 0, contradictory reversed 0. PATCH takes List.head → edits touch one copy. Same fix family.
- 📌 Wave-B referential census notes (no items): 193 parent-younger-than-child + 84 gap<8y (data-entry census); 306 cross-HC relationships where the partner's person node never syncs to the other HC (locally-dangling by shard design — TOLERATED); soft-delete cascade violations all ≤2023-01 (cascade holds today); shard consistency effectively CLEAN (12 extra/1 missing of 5.1M rows); 4,395 duplicate clinic-day sessions TOLERATED (offline multi-device design). **B-120 sizing inputs**: 8 un-deleted link nodes at soft-deleted persons, 41 un-deleted measurements of soft-deleted persons, 70 duplicate-person splits (measurements on copy A, enrolment on copy B).
