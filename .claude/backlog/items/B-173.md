<!-- id:B-173 status:READY tier:4 round:20 -->
### B-173 — Sync + Dashboard decoders: blanket `oneOf [decode, succeed EMPTY]` fallbacks read a decode FAILURE of present data as "no data" — READY · CONFIRMED-anchors (all 9 sites verified; triggering input latent) · LOW · tier 4 · hardening
- Sync photo/file lanes, `SyncManager/Decoder.elm`: `:77-83` (deferred photo), `:86-101` (deferred batch), `:123-129` (photo upload Success branch), `:164-170` (screenshot/file upload) — each `oneOf`'s second branch is meant for "no rows" but equally swallows a malformed PRESENT row: photo sync then reports done forever, row never retried, never Rollbar'd (bypasses the loud decoderError path at `Update.elm:2281-2297`). Trigger requires a malformed persisted IndexedDB row (schema evolution / JS write-path change) — latent; rows persist across app upgrades so the hazard compounds at every future decoder change.
- Dashboard sections, `Backend/Dashboard/Decoder.elm`: `:191-194` (villages encounters), `:243-247` (villages residents), `:481-493` (patients details), `:506-510` (group education) — fallback exists because PHP serializes empty assoc arrays as `[]` (which `dict` rejects), but one malformed entry empties the WHOLE section indistinguishably from "no data". Producer well-formed today — latent.
- Same class, single site: `Backend/Measurement/Decoder.elm:5487-5494` `decodePregnancyByNewborn` `oneOf [..., succeed Nothing]` reads a failed decode of a present participant as "pregnancy not tracked".
- Fix direction per site: make the EMPTY case explicit (match `[]`/`null` shapes only) so real decode failures reach the `Err`/Rollbar path. ⚠ B-006 tombstone does not apply (that is the sw.js fetch catch, accidentally load-bearing); B-026's bulkPhotos oneOf is deliberate and reviewed — leave both alone.
- 📌 B-148(a) UPGRADED by this sweep: independently re-traced (app.js catches `:970/:1005/:1067` → Decoder `:214-255` never reads `error` → `Update.elm:1435-1449` empty ⇒ `Success Nothing`, `remaining=0`) — now CONFIRMED, entry updated. NOT a new item.
