<!-- id:B-304 status:IMPLEMENTED tier:3 round:27 -->
### B-304 — Translate: copied-neighbor label batch — a lab/service/category label answers the WRONG English source (10 sites, each verbatim-equal to a different entry's translation) — ✅ IMPLEMENTED (issue #2182, PR #2183, 2026-09-01) · was READY · CONFIRMED (every duplicate-string equality re-verified mechanically by orchestrator; occurrence counts pin each pair) · MED-LOW · tier 3 · R27 unit 6 · one mechanical PR; correct translation recoverable from git for one, `Nothing` (English fallback) is the objective fix elsewhere
All class-(b) copied-neighbor paste slips (the coverage map's allowed exception), hand-verified with introducing commits by the scout and string-equality re-proven by the orchestrator:
1. **ALT lab label (`Translate.elm:10163-10168`)** + 2. **ALT history heading (`:10888-10893`)**: `kinyarwanda` = the BUN entries' "kidney test" strings ("Impyiko"); the AST siblings correctly say liver ("Umwijima"). Rwanda lab reports label a liver enzyme as a kidney test. `8d3331813`/`68e86e430`. Consumers: `Pages/Report/View.elm:381`, `:1235`.
3. **MUAC placeholder (`:16322-16327`)**: `kinyarwanda` = "Andika uburebure hano…" — the HEIGHT placeholder, inside the MUAC field. `3a056ffb5` (2018). Consumer: `Measurement/View.elm:196`.
4. **Accompany-to-ANC question (`:2403-2408`)**: `kirundi` = the NCD-program entry's ("ingwara zitandukira" = NCDs) — the CHW referral question names the wrong service. `ca14acec3`. Consumers: `Pages/Prenatal/Utils.elm:3670`, `Pages/NCD/Utils.elm:878`, `Measurement/View.elm:1250`.
5. **Malnutrition Signs assessment (`:15245-15257`)**: `kinyarwanda` = `AssesmentDangerSignsPresent`'s — a malnutrition assessment presented as danger signs, a different category on the SAME screens. `333518af7`. Consumers: `Pages/Nutrition/Activity/View.elm:201-204`, `Pages/GlobalCaseManagement/View.elm:494-497`.
6. **Treatment-review popup (`:26904-26916`)**: instructions line = the message line in Kirundi — the popup says "patient non-adherent" twice and never "further evaluation necessary". `d37506810`.
7. **PROM diagnosis (`:17548-17560`)**: `kirundi` = PPROM's preterm description verbatim (+ stray leading space) — the two distinct diagnoses render identically and PROM is described as preterm. `bb56a123d`; sibling dispatch `:17112-17124` same family. Consumer: `Pages/Prenatal/ProgressReport/Utils.elm:47`.
8. **WhatsApp update-profile prompt (`:25411-25416`)**: `kirundi` = the previous step's verification question — the composed sentence (prefix + name + suffix + number) is a garbled repeat. `d37506810`. Consumer: `Components/ReportToWhatsAppDialog/View.elm:218-221`; `report_to_whatsapp` flag.
9. **"Group Encounter" menu button (`:7565-7570`)**: `321b9397e` OVERWROTE the existing correct Kirundi "Kubonano y'umurwi" with the string it was adding to the separate `GroupEducation` entry (diff re-verified) — Burundi's group-encounter entry point is labeled as the distinct, flag-gated group-education feature. Consumer: `Pages/Clinical/View.elm:75`. Fix: restore from git.
10. **CHW "Total Beneficiaries Wasting" card (`:30259-30264`)**: `kinyarwanda` AND `kirundi` = `TotalBeneficiaries`' strings — legitimate there (a chart caption completes the sentence, `Pages/Dashboard/View.elm:2053`) but rendered standalone here (`:3184`), so the sentence dangles and "wasting" is never said. Borderline (incompleteness, not wrong statement) — included with that caveat.
- **Notes, not in scope:** dead text in unreachable site arms (`VaccineRotarix` SiteRwanda somali claiming "1st cause in Somalia"; `VaccineDTP` `_`-arm Kirundi) — would surface only if `languagesBySite` changes.
- **Implementation (2026-09-01, issue #2182, PR #2183, branch `B-303-B-304-translate-corrections`, base `d8c4a9d3f`, one PR with B-303).** 9 restored from git (`Kubonano y'umurwi`, per `321b9397e^`); 1-8 and 10 → `Nothing` (English fallback), including both fields of 10. Verified: anchors re-verified verbatim pre-edit, `elm make` 548 modules, 3,039 tests, fresh-clone `elm-review` clean.
