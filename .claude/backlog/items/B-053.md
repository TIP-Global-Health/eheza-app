<!-- id:B-053 status:IMPLEMENTED tier:3 round:11 -->
### B-053 — WellChild ECD next-visit ladder skips the 18-month rung → 18-month developmental screening late — ✅ IMPLEMENTED 2026-07-19 (issue #1961, PR #1962, branch `b053-ecd-18-month-visit`, base develop @c9d08268a, commit 943419e97, Copilot requested). Fix: added `else if ageMonths < 18 then Just <| Date.add Months 18 birthDate` before the years block; EXTRACTED the pure ladder into `resolveNextDateForECDVisit currentDate birthDate noRemainingSigns` (exposed) so it's unit-testable without AssembledData — mirrors `resolveNextDateForImmunisationVisit` precedent. 5 NEW tests in Pages/WellChild/Activity/Test.elm (`all` refactored to aggregate 2 describes — SAME refactor as unmerged B-030 #1958 → trivial conflict expected on exposing line + `all`, flagged in PR). DISCRIMINATION: remove rung → exactly the 15mo+17mo tests fail (→24mo), 3 guards pass. elm make Main OK, elm-test 2843, elm-format+elm-review cold clean. Local /code-review medium: no findings. CONFLICT-RESOLVED 2026-07-20 (user merged #1958 B-030 + #1960 B-031 first): merged origin/develop into branch (merge commit fcb1dcd8e), only Pages/WellChild/Activity/Test.elm conflicted (both B-030+B-053 rewrote imports/`all`/added test fns) → combined all 3 test groups (resolveFirst.../resolveNextDateForECDVisit.../pregnancySummary...), Utils.elm auto-merged clean. elm make 0, elm-test 2856 (2838+6+7+5), elm-format+elm-review clean. HISTORY CHECK (user-asked 2026-07-19): the 18mo rung was NEVER in `generateNextDateForECDVisit` and removed — `git log --follow -S 'Date.add Months 18'` on the ladder file is empty pre-fix, and in-develop WIP a1928ba4c already had the 15→24 gap. All historical `Date.add Months 18`/`ageMonths < 18`/`Milestone18Months` hits belong to the SEPARATE milestone code (now `Pages/WellChild/Encounter/Utils.elm`: resolvePediatricCareMilestoneOnDate/resolveDateForPediatricCareMilestone/pediatricCareMilestoneToComparable) which has ALWAYS treated 18mo as first-class → the visit scheduler is the lone inconsistency, so the fix aligns it (does NOT reverse a deliberate removal). 646b0337a #2689-revert removed a milestone CLASSIFIER branch (old PediatricCare* naming), reintroduced 2023 — not the visit ladder.
- `Pages/WellChild/Activity/Utils.elm:1899-1905` `generateNextDateForECDVisit`: ladder is `<6mo→6mo`, `<15mo→15mo`, then `ageYears<2→2y` — a child at 15-23 months jumps straight to 24 months. But `ecdSignsFrom18Months` (:1218) become assessable at 18mo and `Milestone18Months` exists in `pediatricCareMilestones` (Backend/WellChildEncounter/Model.elm). Every other sign-group start has a matching visit rung; only 18mo is missing (skipping 9/12mo is benign — no new signs there). Verified: born 2024-01-01, ECD done at 15mo (2025-04-10) → code returns 2026-01-01 (24mo); correct = 2025-07-01 (18mo). Propagates into `pediatricVisitDate = min(ECD, medication)`.
- Fix: add `else if ageMonths < 18 then Just <| Date.add Months 18 birthDate` before the years block. Severity MED. Confirm 18mo is an intended pediatric-care visit before implementing (it's a schedule question — but the sign-group ladder + Milestone18Months + function's own "following milestone" comments all imply yes).
- ✅ SCHEDULE CONFIRMED 2026-07-19 via the clinical schedule sheet (Google Sheet 1awAwPTMxRMhb2CSP3ocJvbOJravIaoPb2Q-RERe9KQ8): 18 Months (1.5 Years) IS a distinct scheduled well-child visit (ladder 6/10/14wk→6mo→9mo→1yr→15mo→**18mo**→2yr→3yr→4yr→5yr); its 5 ECD questions match `ecdSignsFrom18Months` exactly, AND it carries Vitamin A 200,000 + Mebendazole 500 mg.
- ⚠ SEVERITY DOWNGRADE to LOW-MED: `pediatricVisitDate = min(ECD, medication)` (generateNextVisitDates: Maybe.Extra.values→sort→head=earliest). The medication ladder (`nextMedicationAdmnistrationData`) schedules each drug 6mo-after-last-dose, so a 15-17mo child's next VitA/deworm lands ~18-21mo and (being < the buggy ECD 24mo) WINS the min → child usually pulled back ~18-21mo anyway, where the 18mo ECD signs unlock. So the ECD gap is USUALLY MASKED (practical delay 0-3mo); full 6mo delay only when medication date is Nothing/later than ECD. Fix still correct: books 18mo ECD on its OWN cadence, independent of medication timing.
