<!-- id:B-321 status:READY tier:4 round:28 -->
### B-321 — NCD Laboratory (urine dipstick, creatinine, liver function, lipid panel): re-saving a test task after its results were entered rewrites the record with empty results — READY · CONFIRMED (value builders read; reachability traced through the Case Management labs pane, not run) · LOW · tier 4 · latent · R28 unit 2
- **Mechanism.** The NCD (non-universal) `to*TestValue` builders rebuild the record with empty results: `toUrineDipstickTestValue` (`client/src/elm/Measurement/Utils.elm:2751-2770`, `protein = Nothing … bilirubin = Nothing`), `toNonRDTValueWithDefault` (`:3329-3335`) via `toCreatinine/LiverFunction/LipidPanelTestValueWithEmptyResults` (`:3338-3350`). The universal siblings Prenatal uses carry results through dirty-aware hydration (`:2796-2813`); `urineDipstickFormWithDefault` (`:2713-2742`) hydrates nothing result-related.
- **Reachability.** NCD results are entered from Case Management → NCD Labs pane, which filters on `resolutionDate` only (`Pages/GlobalCaseManagement/View.elm` labs entries) — the encounter need not be ended. So: tests ordered, results entered the same day, nurse returns to the still-open encounter → Laboratory → the test → Save. Needs an explicit Save on an already-completed task.
- **Impact.** Results lost and the test re-queued for the lab. Both NCD sites. LOW, latent. **Fix shape (guess).** Hydrate and pass results through in the non-universal path as the universal forms do, or refuse re-save when results exist.
