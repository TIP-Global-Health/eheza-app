<!-- id:B-152 status:IMPLEMENTED tier: round:18 -->
### B-152 — Small-backend audit batch (case_management / activity / user / whatsapp) — mixed CONFIRMED-minor + PLAUSIBLE · LOW-MED · one cleanup PR + one stats question now ANSWERED
- **(a) ✅ IMPLEMENTED 2026-08-10/11 — issue #2064, PR #2065 (5 commits, all 10 CI checks green), branch `b152a-attendance-false-counts-as-attended`.**
  **Shipped rule:** a measurement means the person was there, and so does an attendance record *that says they came*; one saying they did not is no evidence. Implemented as a `db_or()` — `node.type <> attendance` OR `field_attended = 1` — with a hand-written `leftJoin` (NOT `hedley_general_join_field_to_query`, which `addField()`s the value into the SELECT and so into the `distinct()`, doubling rows for anyone with both a measurement and an attendance record). Function renamed `hedley_activity_get_persons_sessions_attended`; 3 sites.
  ⚠ **The first pass excluded attendance entirely** (user's own reasoning: a session where nothing was measured did not run). Review killed it: `hedley_stats_get_total_encounters_by_patients_at_clinic()` (`hedley_stats.module:3004-3012`) counts attendance with `attended = TRUE` as an encounter — the same child would have been 1 FBF encounter AND a missed session. **Measured: on the 50 most recent sessions on ihangane.live, 39 of 141 ticked attendances (28%) have NO measurement** — the common case, not the corner one. User chose to narrow. If this is ever revisited, that 28% is the number that decides it.
  📊 **Live data (2026-08-10):** 187,676 attendance nodes on ihangane, **6,748 with `attended = 0`** (vhw has 75 attendance nodes total, tip-somalia 0). Every attendance node on every site HAS a `field_attended` row (187,676/187,676; 75/75) and `deleted` is always 0 — so the "NULL attended" defensive case was refuted by data, not reasoned away.
  ⚠ **Deploy note:** missed-session counts RISE at every health centre when this lands. Correct, but reads as a regression on a dashboard.
  ⚠ **Known gap, deliberately not taken:** the new assertions exercise the helper, not `hedley_stats_get_session_attendance_stats_by_health_center()` where the defect surfaced — a regression one level up would still pass. Not written blind because the DDEV router was down and period arithmetic could not be verified locally.
- **(a) original finding — CONFIRMED (upgraded 2026-08-04 by client-side check): "sessions with measurements" counts attended=FALSE attendance nodes → missed-session stats inflated toward "attended".** `hedley_activity_get_persons_sessions_with_measurements()` has no bundle condition and no field_attended filter; the `attendance` bundle carries field_person+field_session; consumer `hedley_stats_get_session_attendance_stats_by_health_center()` (hedley_stats.module:2745) uses it to decide missed sessions. CLIENT VERIFIED: un-checking a checked-in mother dispatches `SetCheckedIn attendanceId motherId False` (`Pages/Attendance/View.elm:159-161`) — the attendance node is UPDATED to attended=false, not deleted → counted as attended. Fix: exclude bundle `attendance` (or require field_attended=1) in the helper — check the legacy per-person COUNT twin for the same hole.
- (b) CONFIRMED minor ×2 in `hedley_whatsapp/scripts/`: `send-messages.php:141` falls back to the whatsapp_record node's label instead of the PATIENT's when both names empty; `delete-processed.php:101` resetCache uses the pagination cursor instead of `$node->nid` (intended per-node reset never happens).
- (c) PLAUSIBLE-low set: case_management update-trigger empty-IN crash (insert path guards `count < 2`, update path doesn't — poison-batch class, low realism); `drush rzs` recalc walks only height/weight bundles (misses nutrition_*/well_child_* — matters only after a WHO-table change); hedley_user presave `json_decode` unvalidated (PHP8 TypeError, contained — save aborts, nothing persisted) + multi-value entityref illegal-offset in incident auto-heal; whatsapp send-messages unguarded file_load/copy + only TwilioException caught.
- **Keeper VERIFIED NON-finding (agent ran it in ddev per verify-by-running):** bare-created whatsapp_record DOES materialize the field_delivery_attempts row with default 0 — the `< 5` LEFT-JOIN condition does NOT orphan new records (looked like total breakage until run).
- Audited-clean: all four modules' queries soft-delete-excluded + bounded; person refs all inside the canonical registry; whatsapp private→public→sweep file chain verified end-to-end (PII world-readable only in the bounded 1h window, by design); PIN-uniqueness-includes-deleted is CORRECT (deleted nurse's PIN must keep blocking).
