<!-- id:B-335 status:READY tier:4 round:29 -->
### B-335 — Family Nutrition: the "add child" tile disappears once the adult has 5 related people of ANY kind — own parents, caregivers and PMTCT co-participations included — not 5 children — READY · CONFIRMED (gate, dict construction and constant read; no other consumer) · LOW · tier 4 · R29 unit 6 · tip-somalia only (`family_nutrition` flag; 990 families live)
- **Mechanism.** `client/src/elm/Pages/Person/View.elm:253-262` — for `FamilyEncounterOrigin NutritionEncounter` the tile is shown when `Dict.size people < maxChildrenAtFamilyEncounter` (`Backend/Person/Utils.elm:265-267`, = 5), where `otherPeople` (`:186-195`) is every relationship in either direction unioned with every PMTCT group co-participation. Nothing else caps children: `generateAssembledData` (`Pages/FamilyNutrition/Encounter/Utils.elm:57-80`) lists every `isChildRelation` child.
- **Fix shape (guess).** Count only `isChildRelation` relationships — the predicate the encounter itself uses.
