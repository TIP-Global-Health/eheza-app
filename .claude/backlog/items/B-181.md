<!-- id:B-181 status:READY tier:3 round:20 -->
### B-181 — Completion report: Ultrasound activity (healthy_start) does not exist server-side — READY · CONFIRMED (grep: 0 hits in the whole builder) · tier 3 (healthy_start ON at rwanda; 147 live ultrasound measurements)
- Client offers Ultrasound in every nurse encounter until one is recorded (CLI Prenatal/Encounter/Utils.elm:25-68); PHP builder + Completion decoder/model have no token for it. B-161 class: skipped ultrasounds invisible (overstates completion), done ones uncounted. Fix: PHP token + expected logic + server/elm decoder/model/Translate rows.
- 📌 Same-class minors folded here, no separate items (F9): prenatal nurse **NextVisit** (client-always, CLI:558-564) and **Wait** (labs pending, :540-556), CHW-postpartum **NewbornEnrolment** (:486-488) have no PHP tokens; their state lives on nodes PHP already loads (the pregnancy_outcome dummy mechanism would fit). Effect: encounters abandoned at Next Steps look more complete than the app showed.
