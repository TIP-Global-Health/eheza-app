<!-- id:B-040 status:READY tier:4 round:11 -->
### B-040 — IndexedDB/Cache-Storage housekeeping: WhatsApp screenshot PNGs never deleted from Cache Storage (+3 dead-row accumulations) — READY · CONFIRMED
- `screenshots-upload` cache: `whatsAppUploads` ROWS are deleted after upload (`deleteEntitiesThatWereUploaded`, app.js:1379-1408) but no `cache.delete` against `'screenshots-upload'` exists anywhere (only photos-upload at app.js:1465) → every WhatsApp report permanently leaves a full-page PNG. The one real per-use growth item.
- Also: `syncMetadata` table dead since v14 (rows never cleared; ~60 lines of dead endpoint plumbing at nodes.js:219/:906, sw.js:204, zero Elm callers); `deferredPhotos` attempts>2 rows orphaned forever (comment-acknowledged); `dbErrors` marked isSynced=1 but never deleted. Fix: delete screenshot blob inside deleteEntitiesThatWereUploaded; drop syncMetadata plumbing + clear-table at next version bump; opportunistic deletes for the other two. Severity LOW.
