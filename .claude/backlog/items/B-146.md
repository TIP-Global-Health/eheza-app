<!-- id:B-146 status:READY tier:3 round:18 -->
### B-146 — Deploy safety cluster, part 2 (human-error hardening) — 🅿 **PARKED (user 2026-08-09)** · was READY · CONFIRMED (agent-traced; re-verified 2026-08-09) · MED
**Parked by the user after a full re-verification presentation on 2026-08-09. Do NOT re-pitch.** The sub-findings below stay recorded because two of them were CORRECTED during that re-verification — if this is ever revived, start from the corrections, not the original text.
- (a) App-wipe window — **STILL CONFIRMED** (re-checked 2026-08-09: `server/www/` has no `app/`, and `app` is absent from the `$rsyncExclude` list in `RoboFile.php:78-89`, so rsync#1 `--delete` removes the checkout's whole app/ and rsync#2 rebuilds it from client/dist). **Scope has SHRUNK since filing:** B-143 (PR #2055) now throws on either rsync's non-zero exit, and B-144 (PR #2057) stops `publish` from producing a broken dist. Residue = the case where nothing fails: a *stale or empty dist that rsyncs successfully* (operator skipped publish, or published in a different tree) still replaces the live app. Fix would be a dist sanity-check in RoboFile before rsync#2.
- (b) Site/dist mismatch — ⚠ **LARGELY REFUTED 2026-08-09, the filed premise is wrong.** `.ddev/commands/web/gulp:33` does `cp ../../eheza-infra/elm/${PANTHEON_NAME}/Config.Deploy.elm ${CLIENT_PATH}/src/elm/`, and refuses to run publish at all without `PANTHEON_NAME` (and without `EHEZA_INFRA_REPO_REMOTE`). Deploy reads that SAME var from the same ddev config, so publish-for-ihangane + deploy-to-vhw needs a deliberate `PANTHEON_NAME` change plus a `ddev restart` — not a slip. Narrow residue: running gulp OUTSIDE ddev (host `./node_modules/.bin/gulp publish`) skips the infra copy entirely and bakes whatever stale gitignored `Config.Deploy.elm` is on disk; and nothing stamps dist with the site it was built for, so a mismatch stays silent. Per-site configs live in the private infra repo at `eheza-infra/elm/<PANTHEON_NAME>/Config.Deploy.elm` (README.md:117-124).
- (c) Interrupted publish leaves prod `Config.elm` in the tracked working tree — **CONFIRMED BY RUNNING 2026-08-09**, not just by reading: a deliberately-failed `gulp publish` aborts at `build` and `deploy:revert` never starts (0 occurrences in the run log), leaving the tracked `client/src/elm/Config.elm` holding deploy config. ⚠ **B-144 made this path REACHABLE** — before it, an Elm compile error was swallowed so publish always completed and always reverted. Now a dev whose publish fails on a typo is left with production config in a tracked file. Reported on PR #2057 (inline, `gulpfile.js:520`) and parked with the rest of B-146, so **this currently has NO home** — revisit if it bites.
- Minors from the same audit (entry-tail, no separate items): CI never validates the publish pipeline (`install_client.sh` `$DEPLOY` branch dead); `.pantheon-tip-somalia`/`-eheza-site` checkouts missing on this machine (deploy fails with a misleading dirty-tree message); `generateReleaseNotes` tag grep is substring-match; `server/scripts/reset` lacks set -e.
