<!-- id:B-234 status:READY tier:4 round:24 -->
### B-234 — Case Management (CHW): village scoping skips the TB and HIV follow-ups, so their encounters and participants are fetched for the whole health centre — READY · CONFIRMED (anchor verified; display leak conditional) · MED-LOW · tier 4 · R24 unit 3
- **Mechanism.** `Pages/GlobalCaseManagement/Utils.elm:684-698` `filterFollowUpsOfResidents` rewrites six of the eight person-keyed dicts in `FollowUpMeasurements` (`Backend/Measurement/Model.elm:~3549-3550` has `tuberculosis` and `hiv` too); the two added later with their feature flags pass through unfiltered. Both `View.elm:115-116` and `Fetch.elm:45-46` use it, so `Fetch.elm:128-196` issues `FetchTuberculosisEncounters` / `FetchHIVEncounters` / participant fetches for every TB/HIV follow-up in the authority.
- **Reachability / impact.** Over-fetch is unconditional on every CHW Case Management visit. The display leak (a non-resident in the CHW's TB/HIV pane) is gated by `fillPersonName` (`Utils.elm:475-485`) dropping entries whose person is not in `db.people` — seeded with the village roster — so it shows only once the person was pulled by a search or session; mechanism traced, not observed. `Pages/Dashboard/Utils.elm:1119/:1161` share the function but read only nutrition/AI/prenatal — inert there.
- **Fix shape (guess).** Add `tuberculosis` and `hiv` to the record update.
