<!-- id:B-341 status:IMPLEMENTED tier:2 round:30 -->
### B-341 — Release #2128: the person-photo repair script ships but is not in the post-deploy steps — the backend guard stops NEW wipes only, so ~3,213 persons whose photos B-244 already wiped stay photo-less after the release unless someone runs `restore-wiped-photos.php` — ✅ IMPLEMENTED 2026-09-23 (issue #2151, no PR — release #2128 post-deploy steps) · CONFIRMED (release PR body, PR #2152 body, issue #2151 and B-244's entry read by the orchestrator; live counts scout-measured with the script's own query) · MED · tier 2 · R30 unit 4 · no code change — a release-checklist line and a user decision
- **Mechanism.** The only post-deploy steps in the release PR body are the B-290 cleanup:
  ```
  # gh pr view 2128 --json body — lines 4-5
      - `drush scr profiles/hedley/modules/custom/hedley_ncd/scripts/clear-crossed-beverages-per-week.php --dry_run`\
      - `drush scr profiles/hedley/modules/custom/hedley_ncd/scripts/clear-crossed-beverages-per-week.php`
  ```
  `server/hedley/modules/custom/hedley_person/scripts/restore-wiped-photos.php` came in with PR #2152 (B-244, merged 2026-08-27, after v1.18.2), whose body says it *"restores the photos already lost"* and takes `--dry_run`, `--batch`, `--nid`. Issue #2151 is OPEN with zero comments; B-244's entry records the script as proven locally but no decision to run it on any site. The release's backend half (`HedleyRestfulSync.class.php`, person PATCH ignores a null `photo`) only prevents further wipes.
- **Impact (scout, live today, the script's own selection: no current photo, a revision still holds one).** ihangane 3,162 · vhw 50 · uvl 1 · tip-somalia 0. On ihangane: 2,963 dropped by `sync`-role users (B-244's edit wipe); **199 by uid 0 with a revision timestamp EARLIER than the photo revision it replaced — B-098's NCDA-worker stale-node lost update** (Dec 2023 → Jul 2026, none after B-098's fix). The script restores the newest photo for those too, which is correct; its docblock just undercounts what it repairs.
- **Side effects to plan for.** Each restored person gets a new revision → every device at the affected health centres re-downloads ~3.2k person records (`hedley_device_disable_revisions` false on ihangane, unset elsewhere). Run after `fra` on Live so the guard is in place first.
- **Related release note, same PR.** PR #2174 (B-290) says to re-run the beverages cleanup later, once devices on old builds have updated — v1.18.x clients keep writing crossed values until then. #2128's body does not carry that reminder.
- **Backlog de-dup.** B-244 (parent, IMPLEMENTED), B-098 (the uid-0 share), B-125 (the previous release-prep item, same class). `restore-wiped-photos` appears nowhere else in `.claude/`.
- **Fix shape (guess).** Add dry-run + real-run lines for ihangane, vhw, uvl to #2128's post-deploy list (and the beverages re-run reminder), or have the user decide not to restore and record that on B-244 / #2151.
- ✅ **Done 2026-09-23 (user: "Implement").** Release PR #2128's post-deploy steps gained a #2152 block (dry run + real run of `restore-wiped-photos.php`, after `drush fra`, re-download note) and a beverages re-run reminder under #2174. Decision to restore recorded on issue #2151 (left open for whoever runs it on release).
