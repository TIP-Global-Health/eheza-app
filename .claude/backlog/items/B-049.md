<!-- id:B-049 status:PARKED tier:2 round:11 -->
### B-049 — "Stop Syncing" a health center silently strands its queued unsynced measurements; the pending-upload counter vanishes with it — 🅿 PARKED by user 2026-07-09 — verbatim: "park - with the way we use the app, I don't see a design flaw here." Do NOT re-pitch. (Mechanism stays CONFIRMED on develop @1ca6a4662: View.elm:214-218 unconditional StopSyncing → Update.elm:646 zipper-filter-only → app.js:996-1001 shard-scoped upload never revisits removed authority; `remainingToUpload` counter disappears with the card. Fix shape if ever revived: Option A Elm-only warn-and-confirm on `authorityInfo.remainingToUpload`, or Option B fresh JS shardChanges count.)
- `Pages/Device/View.elm:211-219` renders `StopSyncing`→`RevisionIdAuthorityRemove uuid` unconditionally; handler `SyncManager/Update.elm:638-660` just filters the authority out of the zipper — no pending-upload check, no confirmation, no flush. Uploads iterate only zippered authorities (`app.js:989-1018` queries `shardChanges.where('shard').equals(authorityId) && isSynced!=1`), so removed-authority `shardChanges` rows (isSynced=0) are never uploaded; the per-HC "Remaining for upload: N" block only renders for still-zippered authorities, so the sole indicator disappears too. Recoverable only by re-adding the HC (which also forces a full re-download).
- Fix: count `shardChanges` for the shard before removing → block/warn "N records not yet uploaded", or keep uploading removed authorities until their queue drains (remove only from download rotation). Severity MED (silent indefinite withholding of recorded clinical data + truth-lying status).
