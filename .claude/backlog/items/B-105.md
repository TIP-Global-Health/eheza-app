<!-- id:B-105 status:READY tier: round:16 -->
### B-105 — Delete AllDict dead internals: `min` (:172-182, unexported + only self-recursive) + `showFlag` (:327-337, zero callers) — READY · PROVEN · trivial
- Removes 1 of the 7 Debug.todo. `max` is unexported BUT used by `rem` :540 — KEEP. Importers verified: only `empty/insert/get/toList` + type consumed (ZScore family), no `exposing (..)`.
