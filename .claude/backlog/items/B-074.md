<!-- id:B-074 status:READY tier:3 round:16 -->
### B-074 — GroupEncounterOrigin `db.sessions` reads never fetched on People / Relationship / CreatePerson pages (systemic: initiator DISCARDED at fetch dispatch) — READY · CONFIRMED (R16 fetch lens) · MED-LOW · one PR
All three masked in normal nav (Attendance/Person fetches load the session first); all unmasked on hard refresh:
- (a) WORST: `Pages/Relationship/View.elm:245-248` `getSession … .clinicId == clinicId`; session missing → initiatorCondition False for every clinic AND emptyOption is emptyNode (:234-239) → "Add to Group" select renders ZERO options — nurse dead-end. `Pages/Relationship/Fetch.elm` has no FetchSession; `App/Fetch.elm:273-274` discards the initiator.
- (b) `Pages/People/View.elm:189-200` childAgeCondition graduating-age filter silently off (over-age children listed for regular group sessions).
- (c) `Pages/Person/View.elm:723-733` create-person birth-date limit falls back to `defaultMaximalAge` 13y (over-age child registrable). `fetchForCreateOrEdit` (`Pages/Person/Fetch.elm:53-62`) lacks the initiator; contrast plain PersonPage fetch :36-38 which handles it.
- Fix (one PR): thread the initiator into `Pages.People.Fetch` / `Pages.Relationship.Fetch` / `fetchForCreateOrEdit` (dispatch already has it in scope) and emit `FetchSession sessionId` for GroupEncounterOrigin.
