<!-- id:B-060 status:IMPLEMENTED tier:3 round:13 -->
### B-060 — "Months of Stock" projection dead in Receive-Stock form: current-month consumption average always 0 — ✅ IMPLEMENTED 2026-07-20 (issue #1967, PR #1968, branch `b060-months-of-stock-current-month`, base develop @c9d08268a, commit 986d775eb, Copilot requested). Fix = the ONE-LINER `List.range 1 18`→`List.range 0 18` (Backend/StockUpdate/Utils.elm:~117). ⚠ My PRESENTATION CAVEAT WAS WRONG (I forgot `distributionsByMonth |> List.reverse` → list is OLDEST-first): adding gap 0 lands current month at the END (index 18, not 0), so `splitAt(18-6)=splitAt 12 |> take 6` = indices 12-17 = gaps 1-6 = the 6 COMPLETED preceding months, EXCLUDING the partial current month; past months keep identical indices (0-17 unchanged) → their windows untouched. Confirmed via test fixture: currentDate=Feb15/2026, distributions Jul-Dec2025 each 10 → Jan2026 avg=10 (window=indices 11-16=Jul-Dec). distributionsByMonth used ONLY as window lookup (:278/:281), NOT to build result Dict (separate List.range 0 12 fold at :207 already includes current month). NEW discrimination test in Backend/StockUpdate/Test.elm (current month Feb2026, 6 preceding=12 each + a Feb=999 that MUST be excluded → avg 12); revert→only that test fails (past-month gap-1 test still passes), so fix is current-month-only. elm make Main 0, elm-test 2839, elm-format+elm-review cold clean. Local /code-review medium: no findings.
- `Backend/StockUpdate/Utils.elm:114-137` `distributionsByMonth = List.range 1 18 |> …` starts at monthGap 1 → the CURRENT month (gap 0) is never in the list. `consumptionSixPastMonths` (:273-287) does `List.Extra.findIndex (key == monthYear) distributionsByMonth` then sums the 6 preceding — for `monthYear = current month` findIndex → Nothing → `Maybe.withDefault 0` → `consumptionAverage = 0`. Receive-Stock form (`Pages/StockManagement/View.elm:150-154`) reads exactly `Dict.get (dateToMonthYear currentDate) data |> Maybe.map .consumptionAverage` (always 0), and `viewModeReceiveStock` (:566-579) / month-detail (:473-480) do `if average == 0 then [] / ""` → the Months-of-Stock indicator NEVER renders in the current month (the screen the nurse actually uses to decide receive quantity). Past-month detail views work.
- Test blind spot: `Backend/StockUpdate/Test.elm:189-215` tests only monthGap 1 (`(1,26)`), never gap 0. Fix: `List.range 0 18` and re-verify the `splitAt (index-6)` window still selects the 6 months preceding the current month. Severity LOW-MED (decision aid silently non-functional; no data corruption; discrimination test easy — add a gap-0 case).
