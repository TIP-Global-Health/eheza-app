<!-- id:B-046 status:IMPLEMENTED tier:2 round:11 -->
### B-046 — `explode(',', NULL)` on empty multi-value fields emits `[""]` → phantom empty participant UUID — ✅ IMPLEMENTED 2026-07-14 (issue #1939, PR #1940, branch `b046-empty-multivalue-explode`, base develop @d23647c56, commit 16fa63a36, Copilot requested, CI running). Fix: ActivityBase guard (`!empty(...) ? explode(...) : NULL`) in HedleyRestfulEducationSession (topics + participants) AND HedleyRestfulIndividualEncounter ($multiFields loop), + fixed the `unset(uuid_individual_participant)` copy-paste → `unset(uuid_village_ref)` (raw field was leaking). VERIFIED SAFE on the client: all 7 encounter multiFields decode via `optional`, and `null` reads IDENTICALLY to the old `[""]` (NCD diagnoses → NoNCDDiagnosis, skipped_forms → empty) — pinned by 4 new tests in Backend/EducationSession/Test.elm. DISCRIMINATION (real RESTful handler, separate processes): empty session OLD `[""]`/`[""]`+leak → NEW null/null/no-leak; session WITH data unchanged ("malaria" + real UUID both before and after).
- 📌 HARNESS TRAP (cost 2 rounds): a subclass whose copied method calls `parent::` resolves parent to the UNPATCHED class → it re-transforms an already-transformed item (silently wrong: NEW==OLD, then `explode(array)` warnings). For RESTful classes copy the WHOLE class file and rename the class (keep its real `extends`), and render each variant in its OWN drush process — RESTful/entity caches otherwise hand the 2nd handler the 1st one's output.
- 📌 NOT widened (deliberate): HedleyRestfulNurses (role/health_centers/villages), HedleyRestfulCounselingSchedule, HedleyRestfulCounselingSessions share the unguarded pattern but have no reachable empty case; flagged in the PR, not changed.
- `HedleyRestfulEducationSession.class.php:110/:112`: `explode(',', $item->education_topics)` / `explode(',', $item->uuids_participating_patients)` with NO empty-guard (contrast the guarded canonical `HedleyRestfulActivityBase`/_base: `!empty(...) ? explode(...) : NULL`). Same in `HedleyRestfulIndividualEncounter.class.php:122-125`. Sessions are POSTed empty by the client the instant "begin encounter" is tapped (`emptyEducationSession`: topics/participants = `EverySet.empty`). GROUP_CONCAT over zero rows → NULL → `[""]`: `decodeEverySet` drops the `""` topic, but `decodeEntityUuid` ACCEPTS `""` → phantom `""` participant in the set (scrubbed only on next upload); on stricter/older batch decoders (`SyncManager/Decoder.elm:432` list has no per-item fallback) `""` poisons the whole authority batch.
- Also cosmetic: `:108` unsets `uuid_individual_participant` (copy-paste) instead of `uuid_village_ref` → raw `uuid_village_ref` leaks into payload (client ignores). Fix: apply the ActivityBase empty-guard in both classes + fix the unset. Severity LOW-MED.
