<!-- id:B-111 status:READY tier:4 round:16 -->
### B-111 — Counseling-session demo migration links no session ("today" filter vs static 2019-2020 session dates) — READY · CONFIRMED (empirically: all 3 nodes have field_session NULL) · LOW (demo/CI fidelity)
- `HedleyMigrateCounselingSessions.php:94-102` `fieldCondition('field_scheduled_date','value', date('Y-m-d'))` never matches `csv/session.csv` (2019-2020). Client decoder tolerates (session optional, `Measurement/Decoder.elm:82-89`) but counseling demo content never attaches to a group session. Fix: drop the scheduled-date condition (pick any session). Method self-labelled "Temporary".
