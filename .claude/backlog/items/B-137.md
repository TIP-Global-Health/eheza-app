<!-- id:B-137 status:READY tier:4 round:18 -->
### B-137 — server/elm dead code: 3 unconstructable language variants + a dead SendSyncRequest path ×3 apps — READY · PROVEN (self-verified) · LOW · small cleanup
- From the R18 server/elm NoUnused one-off (rules never active there; CI DOES lint server/elm, hypothesis-cleared, but with the standard ruleset only).
- `App/Types.elm:21-23` `Kinyarwanda`/`Kirundi`/`Somali` never constructed — `App/Model.elm:73` hardcodes `language = English`, no decoder/setter exists. The variants AND any non-English content behind them in server Translate.elm are unreachable (bundle weight only; keep the type if a language switch is ever planned — ask user).
- `SendSyncRequest` in `Backend/{Completion,Reports,Scoreboard}/Model.elm` (:300/:311/:195): handlers exist in each Update.elm (:46/:37/:38) but NOTHING constructs it — the sync loop is driven elsewhere (R17 audited the cursor loop clean). ~45-60 lines total with handlers.
