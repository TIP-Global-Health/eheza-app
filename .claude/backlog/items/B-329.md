<!-- id:B-329 status:READY tier:3 round:29 -->
### B-329 — Group session NCDA: the activity page never fetches the child's individual data, so the "behind on vaccinations" sign can never be recorded there, the newborn-exam birth-weight questions are re-asked, and the "first fill" steps reappear — the same child gets the full question set when NCDA is opened from their own page — READY · CONFIRMED (fetch arm, `viewNCDA` reads and each `NotAsked` fallback read) · MED-LOW · tier 3 · R29 unit 4 · ihangane only (`ncda` flag); 5,762 `group_ncda` records live, 565 in 2026 (route split unmeasured)
- **Mechanism.** `client/src/elm/Pages/Session/Fetch.elm:48-61` fetches individual data (`Backend.NutritionEncounter.Fetch.fetch`) for the pending head child only for `ChildActivity Height | Muac | Weight`; `ChildActivity NCDA` falls to `_ -> Nothing`, so `fetchIndividualDataMsgs` (`:90-92`) is `[]`. The group NCDA form is the shared `viewNCDA` (`Measurement/View.elm:139-140`), which reads: `isBehindOnVaccinationsByProgress currentDate site personId db` (`:2696`; with `individualParticipantsByPerson` `NotAsked` both fallbacks in `Measurement/Utils.elm:9231-9317` end in `withDefault False`, so the question is not rendered), `getNewbornExamPregnancySummary childId db` (`:3863` → `Nothing` → `showNCDAQuestionsByNewbornExam Nothing = True`, `Measurement/Utils.elm:555-565`), `resolveNCDANeverFilled`/`resolveNCDANotFilledAfterAgeOfSixMonths` (`:3864-3866`, see only the group `childMeasurements`), `ancVisitsInpustAndTasks … db` (`:3176`, pregnancy/prenatal dicts empty), `resolveAllWeightMeasurementsForChild personId db` (`:3987`, group weights only).
- **Reachability.** Nurse → Clinics → FBF session → Activities → NCDA → child (`Activity/Utils.elm:265-268` lists NCDA under FBF optional activities; `expectNCDAActivity` `Backend/Measurement/Utils.elm:3943-3953`: `ncda` feature, nurse, child < 24 months). The sibling routes `ChildPage`/`NextStepsPage` go through `fetchForChild` (`Pages/Session/Fetch.elm:35-40`), which does fetch the set — hence the discrepancy. R27 unit 4 recorded only the Height/Muac/Weight display-only previous-value note; NCDA is the untouched sibling.
- **Impact.** Every group-route NCDA lacks the vaccination sign (scorecard reads "not behind"), may duplicate or skip steps, and has empty ANC history. Rwanda only in practice.
- **Fix shape (guess).** Include `ChildActivity NCDA` in the activities that trigger `Backend.NutritionEncounter.Fetch.fetch` — better, thread the activity page's `selectedParticipant` into `fetch` so the fetched child is the viewed child (also closes R27's display-only note).
