<!-- id:B-055 status:UNKNOWN tier: round:11 -->
### B-055 — ✅ **CLINICIAN GATE LIFTED 2026-08-06 — the requirement is written down and says DTP.** Traced `29a578fde` ("Logic for suggesting DTP (standalone vaccine)") → PR #927 → issues #926 + #920. **#926 (adamhstewart, 2023-10-25), the requirement for this very feature: "Add new vaccine called DTP for one dose at 18 months. It should only appear after 3 doses of Pentavalent and with a minimum 28 day interval from the last Pentavalent dose."** #920 gives the naming (Burundi calls the DTP-HepB-Hib combo **Pentavalent** = `VaccineDTP`) and the schedule table (Pentavalent 3 doses, Oral Polio **4** — not interchangeable). anvmn's own comment on #926 confirms the mapping. ⇒ FOUR sources agree (requirement, code comment, variable name `fourWeeksAfterThirdDTPDose`, PHP `$vaccination_progress['dtp']['dose-3']`); only the Elm code says OPV. **This is a deviation from a written spec, not a clinical judgement — nothing for a clinician to decide.** ⚠ Fix BOTH Elm copies together (client `Measurement/Utils.elm` + `server/elm .../Scoreboard/Utils.elm`); the PHP already matches and does not move. — ✅ IMPLEMENTED (issue #2050, PR #2051, commit `ac1e408c0`, merged 2026-08-09, in develop). Both Elm copies read `VaccineDTP`; the PHP already did. Found closed during the 2026-08-21 index pass, not by a fresh implementation.
- ⭐ **METHOD, now 3 for 3:** `git log -S<distinctive-token>` → `gh api repos/.../commits/<sha>/pulls` → PR body → linked issues **and their comments**. Settled the birth-weight threshold (#543/#687), the Rwanda IPV dose (#1426) and this. Each looked like a judgement call and was written down in the issue behind the commit. **Do this BEFORE gating anything as clinician-needed.**

Original entry (gate now lifted): DTPStandalone booster due-date reads OPV dose-3 instead of DTP dose-3 — CONFIRMED-mechanism · QUEUED 2026-07-03 (user queued for tracking; the clinician question — is reading OPV3 a deliberate proxy, or is the comment/var-name DTP3 the true intent? — before flipping the 1-word fix `VaccineOPV`→`VaccineDTP`. Re-surface of a Round-1 parked flag; Burundi-only, catch-up children only.)
- `Measurement/Utils.elm:9159-9180` `VaccineDTPStandalone` branch: comment + variable `fourWeeksAfterThirdDTPDose` say "3rd DTP dose", but code does `Dict.get VaccineOPV vaccinationProgress |> Maybe.andThen (Dict.get VaccineDoseThird)`. Burundi-only (only site with VaccineDTPStandalone). Effect: catch-up child with DTP3 but incomplete OPV → lookup Nothing → `never` (year 3022) → booster NEVER due (excluded from expected tasks + scheduling); or OPV3 earlier than DTP3 → booster offered inside the 28-day post-DTP3 window. On-schedule children unaffected (18mo bound dominates).
- This is the Round-1 low-confidence clinical flag "VaccineDTPStandalone reads OPV3 as proxy for DTP3" (ledger: "need a clinician's eye, NOT re-propose"). NOW strengthened to a name/comment/code THREE-WAY disagreement (per our own rule, that's a bug not a convention) — but WHICH is authoritative (code=OPV3 intentional, or comment=DTP3 intended) is a clinical/vaccine-schedule question. Fix IF confirmed: `Dict.get VaccineDTP vaccinationProgress`. Gate on clinician confirmation.
