<!-- id:B-118 status:IMPLEMENTED tier:2 round: -->
### B-118 — ROOT CAUSE: no canonical "person association fields" list — ✅ IMPLEMENTED 2026-07-14 (issue #1941, PR #1942, branch `b118-person-association-fields`, base develop @d23647c56, commit 16bbdf9b5, Copilot requested, CI running). Fix: `hedley_general_get_person_association_fields()` (all 6 fields, documents the 3 consumers) + shard-recalc and delete-cascade now derive via `array_diff(canonical, [explicit exclusions])` with rationale comments. BEHAVIOUR-NEUTRAL — verified: derived list === the old hardcoded `[field_person, field_adult, field_related_to]` exactly. GUARD = scenario 5 folded into `HedleyAdminIntegrityTests::testIntegrityInvariants()` (no new test method): walks `field_info_fields()`, collects every entityreference field whose target_bundles include `person`, asserts it equals the canonical list. DISCRIMINATION: dropping one declaration makes the guard FAIL (build stops). Runtime check confirmed exactly 6 such fields and ZERO unrestricted entityreference fields (so the guard is exact, not heuristic).
- 📌 phpcs Drupal standard rejects GENDERED PRONOUNS in comments ("her pregnancy" → "Unnecessarily gendered language in a comment"). Nouns like "mother" are fine; pronouns are not.
- NEXT: B-113 (relationships) now has a single place to work from — extend the merge to re-point, do NOT add a 4th hardcoded list.
- `hedley_general_get_person_content_associated_by_fields($person_id, $fields)` exists as a generic helper, but every caller passes its own hardcoded list: shard recalc (`hedley_patient.module:154-163`) and the delete cascade (`hedley_person.module:1544-1552`) both use `[field_person, field_adult, field_related_to]`; `hedley_person_pmtct_participants_for_person` uses `[field_person, field_adult]`; the MERGE uses none of them (it discovers by measurement bundle instead).
- The six fields that actually reference a person (config-verified against every content type): `field_person`, `field_adult`, `field_related_to`, `field_participating_patients` (education_session), `field_newborn` (individual_participant), `field_referred_person` (acute_illness_trace_contact). **The last three are in NO list** — so they escape the merge, the shard recalc AND the delete cascade.
- Fix: one `hedley_general_get_person_association_fields()` returning all six, used by all callers; then the per-bundle gaps below become a matter of handling, not discovery. A comment in that function should say: adding a new person-reference field means adding it here.
