<!-- id:B-059 status:WONTFIX tier:2 round:12 -->
### B-059 — view_export batch worker's `FILE_APPEND` is non-idempotent → re-processing a batch duplicates rows in the delivered CSV — ❌ WON'T-FIX 2026-07-16 (triple-gated, all three shut): (1) 🅿 low usage (user 2026-07-13: export "used very rarely"); (2) release_timeout=0 confirmed on all 3 live sites (B-096 check) → the crash-re-append path is dead; (3) OPS ANSWER 2026-07-16 — the Jenkins AQ job has concurrent builds DISABLED (see B-058 part 2) → no parallel report-group workers → the parallel-claim path can't fire either. Mechanism stays CONFIRMED but has no live trigger. Do NOT re-pitch.
- `hedley_view_export.module:245` `file_put_contents($file->uri, $output, FILE_APPEND)`; worker requeues the next batch (`:250-257`, QUEUED). Re-processing the same `from` offset re-appends: (a) parallel-claim race — `requeueItem` (:253) then `advancedqueue_process_item`'s unconditional `deleteItem` (`advancedqueue.queue.inc:185`, keyed on item_id only, no expire/status guard) can stomp a 2nd worker's PROCESSING lease → same batch appended thrice; (b) if `advancedqueue_release_timeout` were ever set non-zero, a crash-after-append re-appends. NO self-heal — dup rows baked into the emailed CSV. DISTINCT from B-005 (unique_id=0) and the switchUserBack bleed.
- Fix: write batch N to `part-N` then concatenate, or truncate-from-`from` on entry, or skip if `from <= last_committed`. Severity MED-mechanism/LOW-hit (needs parallel `report`-group workers or the timeout config change).
