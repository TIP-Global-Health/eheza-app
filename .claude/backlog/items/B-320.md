<!-- id:B-320 status:IMPLEMENTED tier:1 round:28 -->
### B-320 — Prenatal HIV test: correcting to "known as positive" after a negative RDT keeps the result and the partner answers, so the encounter is diagnosed Discordant Partnership and offered TDF3TC (PrEP) for a woman who is HIV-positive — ✅ IMPLEMENTED · CONFIRMED (update arm vs its sibling, value builder, diagnosis consumer, medication set all read) · MED · tier 1 · R28 unit 2 · ⭐ DEPLOYED (all sites)
- **Mechanism.** The known-as-positive update function (`contentAndTasksLaboratoryUniversalTestKnownAsPositive`, `client/src/elm/Measurement/Utils.elm:6464-6482`) clears only `testPerformed`, `executionNote`, `executionDate` (with dirty flags). The `TaskPartnerHIVTest` arm directly below (`:6491-6513`) is the copy that WAS extended — it also clears `partnerTakingARV`/`partnerSurpressedViralLoad` with dirty flags. The HIV arm leaves `immediateResult`, `testResult`, `hivProgramHC`, `partnerHIVPositive`, `partnerTakingARV`, `partnerSurpressedViralLoad` set; after a reload `hivTestUniversalFormWithDefault` (`:2571-2589`) refills them. `toHIVTestValueUniversal` (`:2599-2620`) then stores `executionNote = TestNoteKnownAsPositive` beside `testResult = Just TestNegative`, `testPrerequisites` from the stale `immediateResult`, and `hivSigns = {PartnerHIVPositive, …}`.
- **Consumer.** `Pages/Prenatal/Activity/Utils.elm:2299-2360`: `discordantPartnershipDiagnosed = byPartnerHIVTest || byHIVTest`, where `byHIVTest` reads `hivSigns` alone — no gate on execution note or result — unlike `byPartnerHIVTest`, which requires a performed negative test (`TestNoteRunToday/RunPreviously && testResult == Just TestNegative`). `:2787` `DiagnosisDiscordantPartnershipInitialPhase -> discordantPartnershipDiagnosed && immediateResult .hivTest` (prerequisites refilled → True). `Pages/Prenatal/Utils.elm:1821-1848` `resolveDiscordantPartnershipSet` → `[ TDF3TC ]`. Nothing on screen shows the partner answers once known-as-positive is Yes (`viewHIVTestUniversalForm :3711` hides the derived section).
- **Reachability.** Nurse records HIV RDT performed, immediate result Negative, partner positive / not on ARVs, Save; then corrects to "known as positive: Yes", Save — same session (`SetHIVTestFormBoolInput` works on the raw form, `Pages/Prenatal/Activity/Update.elm:2278-2290`) or after reload. A correction path, so rare; when hit, a wrong diagnosis on the record and a wrong PrEP recommendation. No site split. Not live-sized (needs `known-as-positive` + a stored `testResult` on `prenatal_hiv_test` — write the query when building).
- **Fix shape (guess).** Give the `TaskHIVTest` arm its own update function that clears result/prerequisite/sign fields with dirty flags (mirroring the partner arm), and/or gate `byHIVTest` on a performed negative test as `byPartnerHIVTest` does.
- ⬆ **Re-tiered 3 → 1 on 2026-09-03 (user rule):** a defect in how a diagnosis or its care pathway is determined is tier 1 regardless of how many patients hit it. Severity label unchanged — it describes blast radius, the tier describes priority.
- ✅ **IMPLEMENTED 2026-09-07 — issue #2211, PR #2212, branch `B-320-known-positive-stale-result`, base `B-323-partner-hiv-labtech-note` (`f023d3fca`), stacked under #2210 → #2206.**
  - ⛔ **A stacked PR's GitHub base is the PARENT BRANCH, not `develop`** (user, 2026-09-07). Opened against `develop` first, which showed 6 commits and 9 files — the union of #2206, #2210 and this one — and would have dragged both parents in on a merge. Retargeted to `B-323-partner-hiv-labtech-note`: the diff collapsed to 1 commit / 6 files, out-of-order merge became impossible, and GitHub retargets to `develop` on its own when #2210 merges. Checks are attached to the commit sha, so none re-ran.
  - **Three call sites, not the one the entry named.** The ungated `byHIVTest` / gated `byPartnerHIVTest` asymmetry is copied in `Pages/Prenatal/Activity/Utils.elm` (the diagnosis), `Pages/Prenatal/ProgressReport/View.elm` (the discordant-couple line) and `Pages/Prenatal/Utils.elm` (the HIV health-education questions, twice). One shared `patientHIVNegative : PrenatalMeasurements -> Bool` in `Pages/Prenatal/Utils.elm` now gates all of them.
  - **Both halves built**, as the entry's fix shape offered: the form clears `testResult` / `hivProgramHC` / the three partner fields with dirty flags, and the diagnosis gates on the stored result — the second is what covers records the deployed app already wrote.
  - The three known-as-positive update lambdas became one base (`knownAsPositiveUpdate`) plus two extensions (`knownAsPositiveUpdateHIVTest`, `knownAsPositiveUpdatePartnerHIVTest`), top-level and exposed, which is also the test seam.
  - ⚠ `immediateResult` has **no dirty flag** on `HIVTestUniversalForm`, so it cannot be cleared by the form and the saved `testPrerequisites` keeps `PrerequisiteImmediateResult`. Inert: with `hivSigns` and `testResult` cleared, nothing reads it for a known-positive record. Adding the flag would be a model change — not taken.
  - **Verified:** `elm make src/elm/Main.elm` (548 modules), `elm-test` 3099 pass, `elm-review` clean on a wiped `elm-stuff`. Discrimination run with both fixes reverted in place: exactly the 4 new tests fail, the other 3095 pass.
  - Tests: 2 cases in `Pages/Prenatal/Activity/Test.elm` (+ a `withHIVTestKnownAsPositiveCarryingSigns` builder), 5 in `Measurement/Test.elm`.
  - ⛔ Not live-sized. The query still to run when convenient: `prenatal_hiv_test` rows with `field_execution_note = known-as-positive` AND a non-empty `field_test_result`.
