<!-- id:B-119 status:WONTFIX tier:2 round: -->
### B-119 — Legacy `child` / `mother` bundles carry field_person and are invisible to the merge — ❌ WON'T-FIX 2026-08-03 (liveness check done: they are frozen)
- **Verified read-only against `ihangane.live`** (via `terminus connection:info --field=mysql_command`): `child` 3,143 nodes, `mother` 3,184 nodes; **newest `created` 2019-06-06, newest `changed` 2020-12-27** on both — untouched for over five years. `vhw.live` has none at all.
- The gate the entry itself set ("confirm they are frozen historical data; if frozen, close as won't-fix") is therefore answered: frozen. The merge not reaching them costs nothing, because nothing is being merged into or out of them.
- Corroborating: the legacy person-migration scripts that produced these bundles (`migrate-mothers`, `migrate-children`, `migrate-field-mother`, `migrate-field-child`) were all deleted as unused in PR #2024 (2026-08-02), user-confirmed.
- ⛔ Do NOT re-pitch. If it ever comes back, re-run the two-column query above first — a changed `MAX(changed)` is the only thing that would reopen it.
