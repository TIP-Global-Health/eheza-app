<!-- id:B-293 status:IMPLEMENTED tier:4 round:27 -->
### B-293 — Dashboard immunization donut + NCDA scorecard row: a dose administered ON the reference day is read as not given, so the child reads Behind for that month — ✅ IMPLEMENTED (issue #2199, PR #2201, branch `B-293-dose-on-reference-day`, base `bff5c5233`) · CONFIRMED (both copies read; comment/code mismatch is objective) · LOW · tier 4 · R27 unit 2 · DEPLOYED (identical filter on `origin/main:2860`)
- **Mechanism.** Two copies of the same filter use strict `LT` under a comment that says "after": `Pages/Dashboard/View.elm:2846-2848` — `Date.compare administeredDate dateLastDayOfSelectedMonth == LT` under `-- Filter out vaccinations that were performed after the reference date.` — and `Pages/WellChild/ProgressReport/View.elm:2412-2420` (Universal Interventions "Immunization" row, against `resolveLastDayForMonthX`). A dose administered exactly on the reference day is excluded from progress, `generateFutureVaccinationsData` re-schedules it with a due date ≤ the reference, and the on-track test (`closestDate` strictly after reference; donut `:2861-2868`, scorecard `:2433-2442`) classifies the child Behind / `NCDACellValueX` for that month — permanently, in past-month views.
- **Worked example.** MR1 due 2026-03-01, administered 2026-03-31; viewing March 2026 → the 03-31 dose is filtered out, MR1 reads overdue → Behind for March. April view: on track.
- **Impact.** Display/statistics only, both sites; ~1-in-30 per dose per boundary, biased upward because catch-up doses cluster on visit days. The file's own maintained "on or before" idiom exists two screens away: `withinOrBeforeSelectedMonth = not <| Date.compare date ref == GT` (`Pages/Dashboard/Utils.elm:1935-1937`), the helper B-242 consolidated onto.
- **Fix shape (guess).** `/= GT` (on-or-before) in both copies, or route them through `withinOrBeforeSelectedMonth`.
- **De-dup.** B-263 is the `== 3` DTP-standalone gate in the same donut builder; B-064 was the bucketing fix whose comment sits below this filter; neither touches this comparison (backlog grepped for the filter name and symptom).
- ✅ **Built 2026-09-03.** Both copies now keep on-or-before. The dashboard copy calls `withinOrBeforeSelectedMonth` — `Pages/Dashboard/View.elm` **already imports it** (checked before writing anything), so the reuse cost nothing. The Well Child copy states the test inline as `/= GT`: the helper lives in `Pages.Dashboard.Utils`, and a Well Child progress report taking a dependency on the dashboard's utils module for a two-token predicate is the worse trade. ⭐ **The comments needed no change** — both already said *"Filter out vaccinations that were performed after the reference date"*, which is what the code now does. ⚠ **No regression test** — `isImmunizationOnTrack` is a `let` binding inside `viewChildWellnessOverviewPage`, a view returning `List (Html Msg)`; the [[B-124]] class, and the Well Child copy is the same shape. Verified: elm-format clean · elm-test 3,062 passed · `elm make src/elm/Main.elm` 548 modules · elm-review clean on the committed state via a fresh clone.
