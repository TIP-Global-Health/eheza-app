<!-- id:B-365 status:READY tier:1 round:31 -->
### B-365 — Prenatal diagnosis: saving or correcting medical history, obstetric history (both steps), obstetrical exam, nutrition (BMI) or the ultrasound EDD never re-runs the nurse assessment — the preeclampsia-risk diagnoses that decide Aspirin (and every EGA gate, via the EDD) stay as computed before the correction — READY · CONFIRMED (the special-case list in `HandleRevisions` enumerated by the orchestrator; readers scout-traced; scratch test for one input) · LOW · tier 1 (diagnosis rule) · R31 unit 5 · DEPLOYED, all sites; unmeasurable live (all 12 ihangane encounters since 2025-08 with risk signs saved them before the last triggering save)
- **Mechanism.** `HandleRevisions` special-cases 60+ revisions that call `processRevisionAndAssessPrenatal` (`client/src/elm/Backend/Update.elm:2979-3320`); `MedicalHistoryRevision`, `ObstetricHistoryRevision`, `ObstetricHistoryStep2Revision`, `ObstetricalExamRevision`, `PrenatalNutritionRevision` and `PrenatalUltrasoundRevision` are not among them (orchestrator grep of the `[ …Revision` patterns in `:2960-3880`) and fall to `_ -> processRevisions`. The scorer reads them: preeclampsia risk from medical history, obstetric history step 2 (`Pages/Prenatal/Activity/Utils.elm:2460-2600`, e.g. `byPreviousPreeclampsia` from `.obstetricHistoryStep2`), twins from the obstetrical exam, BMI from nutrition; the ultrasound EDD resets the pregnancy dates (`Pages/Prenatal/Encounter/Utils.elm:246-252`). Risk diagnoses gate Aspirin (`Pages/Prenatal/Utils.elm:1854-1866`); scout scratch test: `HypertensionBeforePregnancy` alone → `[DiagnosisHighRiskOfPreeclampsiaInitialPhase]`.
- **Reachability.** The assessment runs only on a triggering save after mandatory activities are complete, so it bites when one of these inputs is saved or CORRECTED after the last triggering save — nothing enforces activity order (default order: History before Danger Signs at the first visit; Nutrition / Obstetrical Exam followed by Core / Breast Exam later). Correcting History at the end leaves the stored diagnoses and the Aspirin decision stale in either direction.
- **Backlog de-dup.** B-221 (same class, the partner-HIV trigger — implemented); R23's cross-check covered every other program but not these Prenatal inputs. Not recorded.
- **Untraced oddity (scout, for whoever builds this).** `high-risk-of-preeclampsia-*` has NEVER been stored on ihangane while `moderate-risk-of-preeclampsia` has 16 rows since 2026-03-04; the one post-launch encounter with `hypertension-before-pregnancy` (2026-03-05) stores `none` — possibly an old app build, not confirmed. Worth a live look before building.
- **Fix shape (guess).** Add these revisions to the Prenatal assessment special cases (`processRevisionAndAssessPrenatal … False`).
