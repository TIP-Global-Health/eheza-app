<!-- id:B-069 status:IMPLEMENTED tier:1 round:15 -->
### B-069 — Dashboard "Total Beneficiaries Wasting" & "# of Stunting" use `< 2` instead of `< -2` → grossly over-count (~98% of a normal cohort flagged) — ✅ IMPLEMENTED 2026-07-05 (issue #1894, PR #1895, branch `b069-dashboard-zscore-sign`, base develop @91fb401fb, CI running, Copilot requested). Fix: both predicates → `< -2` (View.elm :3054/:3086 anchors held). elm make Success (548), elm-format clean, elm-test 2749 passed.
- `Pages/Dashboard/View.elm:3054` `totalBeneficiariesWasting = countCurrentlyDiagnosedByValue .zscoreWasting (\zscore -> zscore < 2)` and `:3086` `numberOfStunting = … .zscoreStunting (\zscore -> zscore < 2)`. Malnutrition is z < −2; `< 2` counts every child below +2 SD.
- DEFINITIVE typo (not a convention): the SAME field `.zscoreWasting` is tested `< -2` at `:3067` (incidentsOfWasting) and `>= -2` at `:3143` (isGoodNutritionEncounter); backend stores raw signed z-scores (hedley_stats extract, no abs/negation). Renders on Dashboard→Child Wellness→Nutrition (cards TotalBeneficiariesWasting :3176 / NumberOfStunting :3180). Since `a818e8069` (2024-01-04).
- Fix: both predicates → `(\zscore -> zscore < -2)`. Severity MED (decision-support metric grossly wrong, not a clinical gate). Same objective-typo family as B-041/R4-3.
