<!-- id:B-363 status:READY tier:4 round:31 -->
### B-363 — Antenatal / child progress report minors ×4: continued-malaria diagnoses never listed (latent); the end-encounter dialog flag survives CloseEncounter; the group-session report's WhatsApp button ignores the feature flag (latent); an older encounter's Patient Progress data is out of date order — READY · CONFIRMED at the anchors by the orchestrator for (a)(b)(c); (d) scout-traced · LOW · tier 4 · R31 unit 3
- **(a) Continued-malaria diagnoses missing from the report's lists — latent.** `DiagnosisMalariaMedicatedContinuedInitialPhase`/`…RecurrentPhase` and `DiagnosisMalariaWithAnemiaMedicatedContinuedInitialPhase`/`…RecurrentPhase` appear in neither `medicalDiagnoses` nor `obstetricalDiagnoses` (`client/src/elm/Pages/Prenatal/ProgressReport/Model.elm:31-137` — grep finds none), so the panes filter them out and the View branches for them (`View.elm:2883-2899`, `referredToHospitalMessage`) never run; the engine produces them (`Pages/Prenatal/Activity/Utils.elm:2836-2860`). Since `4fb902051` (2022). Live 0 on ihangane/vhw/uvl. Report pane → not the tier-1 rule. Fix: add the four to `medicalDiagnoses`.
- **(b) End-encounter dialog flag not reset.**
  ```elm
  -- client/src/elm/Pages/Prenatal/ProgressReport/Update.elm:18-26
          CloseEncounter id ->
              ( model
              , Cmd.none
              , [ Backend.PrenatalEncounter.Model.CloseEncounter ... , App.Model.SetActivePage PinCodePage ]
  ```
  Siblings reset it (`Pages/NCD/ProgressReport/Update.elm:17-18` `{ model | showEndEncounterDialog = False }`; WellChild, Nutrition, AI, TB, ChildScoreboard likewise); `Pages/Prenatal/Encounter/Update.elm:17` has the same omission. The page model is cached per encounter until next login (`App/Update.elm:945-949`), so reopening that report in the same session shows "End encounter?"; Yes re-closes with `endDate = currentDate`. Fix: reset the flag like the siblings.
- **(c) Group-session report WhatsApp button ungated — latent.** `Pages/WellChild/ProgressReport/View.elm:600-611` `InitiatorNutritionGroup` renders `viewEncounterActionButton … Translate.ReportToWhatsApp` directly; every other initiator uses `viewEndEncounterMenuForProgressReport`, which checks `reportToWhatsAppEnabled features` (`Pages/Utils.elm:1121`). tip-somalia (only flag-off site) has no `session` nodes, so no exposure today.
- **(d) Older-encounter Patient Progress order (scout-traced).** `nursePreviousEncountersData` includes LATER nurse encounters sorted ascending and `generateAllNurseEncountersData` appends the viewed encounter last (`View.elm:3314-3327`), so older B with newer C yields `[A, C, B]`; `viewPatientProgressPane` does not sort — first-detected fetal movement/heart rate (`:1377-1397`) can show C, `currentFetalPresentation` (`:1543-1549`) picks B's older exam, BMI / fundal height / weight-gain columns, trimester icons and photos render in list order. Reached via Case Management labs → recurrent encounter → completion opens the report (`Backend/Update.elm:9280`). Live ihangane: 66 of 22,117 `prenatal_labs_results` concluded early on/after a newer nurse encounter's date. Same later-encounters-as-history family as B-339. Fix: sort by `startDate` ascending.
