<!-- id:G-06 status:READY tier: round:16 -->
### G-06 — Custom elm-review rule: NoMultiSelectWithoutSentinel — READY · M · WEAK-BUT-USEFUL
- Builder `Pages/Utils.elm:614` `viewCheckBoxMultipleSelectInput` (+ CustomInput :623; 62 view call sites; update-side protocol `setMultiSelectInputValue` :857 with explicit noValueIndicator, 84 sites). Sentinel = `noneOption` arg OR a `No*`/`Normal*` constructor in the lists (prefix heuristic validated). Fully-robust static rule INFEASIBLE (dynamic lists e.g. `TraceContact/View.elm:353/372/391`; gating-bool idiom is semantic). Weaker rule: flag call sites where BOTH option args are literal lists with no `No*`/`Normal*` member AND noneOption is literal Nothing — ~8-9 initial hits, all legit (gated e.g. `WellChild/Activity/View.elm:462`, `Measurement/Utils.elm:1644`, `NCD/Activity/Utils.elm:813`; by-design `ReportToWhatsAppDialog/View.elm:360/412`) → seed via `elm-review suppress` baseline so only NEW sentinel-less forms fail. First custom rule in `client/review/src/` (no new infra). Guards the dormant "cleared is unexpressible" class (coverage-map R12 lesson).
