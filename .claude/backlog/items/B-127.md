<!-- id:B-127 status:IMPLEMENTED tier:4 round:17 -->
### B-127 — ✅ IMPLEMENTED 2026-08-05 (issue #2039, PR #2040, branch `b127-php-dead-code`, base develop @50c26c37c, commit 680287a98) — PHP function-level dead code
- **17 deleted, not 19: `HedleyWebTestBase::createAdministrator` + `::createFamilyPlanning` KEPT** (user 2026-08-05: "leave") — the base class exists so tests don't hand-roll fixtures. Single PR (user's call). **440 lines removed** across 9 files (the entry's ~266 estimate was low — it excluded docblocks).
- **Re-derived the deadness rather than trusting the R17 sweep** (standing rule): every name resolves to exactly 1 occurrence in `server/hedley`+`server/www`+`client` (its own definition); the lone 2nd hit is the `hedley_activity` docblock mention, removed with it. Also checked: the only `call_user_func` in scope is INSIDE `query_in_batches` calling its own param; no name decomposes into `{module}_{hook}`; `hedley_install_tasks()` really doesn't register `hedley_setup_blocks`. ⚠ **Naive repo-wide grep shows 4 hits per name — 3 are the untracked `.pantheon-ihangane`/`.pantheon-vhw` deploy checkouts + a `.claude/worktrees` agent tree. ALWAYS scope greps to `server/hedley server/www client`.**
- 📌 **CI's phpcs only sniffs `server/hedley/modules/custom/*/`** (`ci-scripts/test_coder.sh:41`) — `hedley.profile` is NOT scanned and carries 3 pre-existing Drupal-standard errors (lines 1, 42; verified pre-existing by stashing). Don't chase them in an unrelated PR.
- ⛔ geolocation cluster still EXCLUDED (see below) — unchanged.

Original entry:
- From the R17 sweep (801 top-level functions, 466 methods, 301 classes in scope; hook universe of 699 names from every *.api.php + all module_invoke/drupal_alter sites; every candidate got a final manual read for same-file dynamic dispatch). **No dead classes, files, or modules.**
- **hedley_schedule** (orphaned 2019): `hedley_schedule_clinic_has_sessions` (.module:54), `hedley_schedule_clinic_get_sessions_by_date` (:80), `hedley_schedule_get_today_sessions` (:104).
- **hedley_restful** (orphaned 2019, d9412f913/e43c6955b): `hedley_restful_output_from_handler` (.module:326), `hedley_restful_output_for_bundles` (:347), `hedley_restful_query_in_batches` (:462).
- **hedley_reports** (orphaned 2026-03-30 by 7eb014408): 4× `hedley_reports_prenatal_{medical,obstetric}_history_*_sign_recorded_{previously,today}` (.module:10108/10136/10165/10193).
- **hedley_person**: `hedley_person_get_child_relationships` (.module:1140) — freshly orphaned by #2024's script deletions (415a693b9).
- **hedley_activity**: `hedley_activity_count_person_measurements_taken_during_sessions` (.module:461; only residue is a docblock mention at :483).
- **hedley.profile**: `hedley_setup_blocks` (:71) — install-task callback NEVER registered in `hedley_install_tasks()`; dead since the 2017 starter-kit copy.
- **Methods:** `HedleyWebTestBase::createAdministrator` (:54) + `::createFamilyPlanning` (:368); `HedleyMigrateBase::avatarProcess` (:178); `HedleyRestfulEntityBaseNode::renderCounselingSchedule` (:95) + `::renderParticipantForms` (:108); `HedleyStatsCalculation::assertTotalBeneficiaries` (:482, unused protected test helper).
- ⛔ **SUSPECT — DO NOT DELETE:** the hedley_migrate geolocation cluster (5 fns, 142 lines: `hedley_migrate_generate_geo_terms_vocabulary` + 4 helpers) — zero in-repo callers BUT it is the drush-php-eval ops tool that generates the client GeoLocation structure for new-site onboarding, and it was **already deleted once and deliberately restored** (0366aedf2, 2022-01-27 "restore accidentally deleted functions"). Needs a product/ops answer, not a grep.
- Traps documented for future sweeps: `_consolidate_*` dynamic dispatch, 6 query-TAG-alter fns (note `hedley_ncda_query_exclude_set_reports_data_alter` LIVES in hedley_reports.module — cross-module hook registration that works only because both are enabled), faker hooks, views plugin overrides, SimpleTest `test*` dispatch.
- 📌 **R25 unit 7 adds one more proven-dead function:** `hedley_reports_family_nutrition_metrics_to_string` (`hedley_reports.module:1487`) — a single occurrence across `server/hedley`, `server/www` and `client` (its own definition). Added by `b7270b4b5` and orphaned by `9d95e6209`, which inlined the MUAC handling into `hedley_reports_calculate_aggregated_data_for_person` (`:1181-1197`). Also newly noted as live-dead (script-only, adjacent to this inventory but not previously listed): `hedley_ncda_query_exclude_set_reports_data_alter` (`:3182`) and `hedley_ncda_resolve_nurses_ids` (`:3196`) — both sit in `hedley_reports.module` despite the `hedley_ncda_` prefix, and the latter's sole caller is a declined-class script.
