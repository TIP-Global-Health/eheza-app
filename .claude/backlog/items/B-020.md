<!-- id:B-020 status:PARKED tier:1 round: -->
### B-020 — Stats recalc non-atomic → dashboard blanks/stale for an HC — 🅿 PARKED by user 2026-07-06 — do NOT implement without explicit instruction. (Was CONFIRMED with impact nuances by the verification sweep; the traced fix shape below stays valid if revived.)
- CONFIRMED core: `hedley_stats_calculate_stats_for_health_center` (:649-691) clears the master hash FIRST (:653, self-verified), 15 sub-calcs each CACHE_SET internally, hash written LAST (:685-687) → any exception leaves hash-missing + mixed metric caches. Serve path (:706-805) has 16 identical `if ($cache_data === NULL) return [];` guards (self-verified :707-710; only PATIENTS_DETAILS partial-serves) → one missing metric empties the whole stats payload; sync endpoint silently returns empty batch. AQ: retry_after 120 / max_attempts 3, worker catches Exception→FAILURE_RETRY; PHP fatals (OOM in node_load_multiple 500-chunks) bypass the catch (stuck-items patch eventually fails them).
- NUANCE 1 (softens "forever"): dead FAILURE items don't block the dedup (QUEUED/PROCESSING only) and the sync endpoint schedules a FRESH task on every stats poll while the hash is empty → transient failures self-heal next poll+cron at the cost of repeated full recalcs; only DETERMINISTIC throwers (bad data/OOM) blank permanently. NUANCE 2: devices that synced before show STALE stats (old timestamp), not blank; fresh devices/cleared IndexedDB show an ENDLESS SPINNER (Pages/Dashboard/View.elm:163-215 withDefault spinner — no error, no retry UI).
- Fix shape VERIFIED compatible: sub-calcs already return their values; hoist CACHE_SETs to the caller (or staging keys), write all metrics then hash ONLY on full success, and STOP clearing the hash at :653 (old data stays servable during recalc). No reader changes. MED-HIGH.
