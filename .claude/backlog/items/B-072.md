<!-- id:B-072 status:IMPLEMENTED tier:3 round:15 -->
### B-072 — CorePhysicalExam `heartMurmur` required by save but not counted (B-037 class, SHARED → BOTH NCD and Prenatal) — ✅ IMPLEMENTED 2026-07-16 (issue #1951, PR #1952, branch `b072-core-exam-heart-murmur-count`, base develop @15275c54f, commit 42fc8ef93, Copilot requested). Fix: `+ taskCompleted form.heartMurmur` + total `7→8` in BOTH count blocks (`examinationTasksCompletedFromTotal` in Pages/NCD/Activity/Utils.elm :301 + Pages/Prenatal/Activity/Utils.elm :5205, byte-identical). Placed after `heart` to match toCorePhysicalExamValue field order. NOT data loss (unlike B-037): value=Nothing → isJust coreExam false → encounter can't complete → dead-end, not silent discard. Mechanical count fix (compiler-verified, no bespoke test — B-037/#1891 precedent, count is unexposed-in-case); elm make 548, elm-test 2830, elm-format + elm-review (cold) clean. Local /code-review medium: no findings. **This + B-037 exhaust the count-vs-save class.** Level was medium (Pages/NCD + Pages/Prenatal, NOT Measurement/ path → not high-risk despite touching a Measurement concept).
- SHARED save `Measurement/Utils.elm:1308` `toCorePhysicalExamValue … |> andMap form.heartMurmur` (Nothing ⇒ whole value Nothing). Counts omit it: NCD `Pages/NCD/Activity/Utils.elm:301-311` (`extremities + neck + lungs + abdomen + heart + [brittleHair,paleConjuctiva]`, total 7) and the identical Prenatal twin `Pages/Prenatal/Activity/Utils.elm:5203-5215` (total 7). The heart-murmur yes/no IS a rendered question (`Measurement/View.elm:2278-2280` `Translate.HeartMurmur` label + viewBoolInput → `heartMurmur = Just value`); starts `Nothing` (Model.elm:541), set only by answering it.
- Trigger: answer the 7 counted sub-fields, skip the always-rendered heart-murmur question → count shows 7/7, Save enables → `toCorePhysicalExamValue`=Nothing → `saveMeasurementMsgs`=`[]` (no-op). NCD `activityCompleted Examination` requires `isJust coreExam` → Examination stays pending → `allowEndingEncounter` blocks the encounter with a green task card + a Save that "does nothing" (confusing dead-end, not silent loss since it can't complete). Prenatal same shared gap.
- Fix: `+ taskCompleted form.heartMurmur` in both counts (total→8), or drop the `andMap form.heartMurmur` hard-requirement. Severity LOW-MED. Same family as B-037; both are the last two count-vs-save gaps (the completion-vs-persistence class is otherwise exhausted per R10/R12/R13/R14/R15).
