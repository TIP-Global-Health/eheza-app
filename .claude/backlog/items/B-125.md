<!-- id:B-125 status:UNKNOWN tier:2 round:17 -->
### B-125 — ✅ **CLOSED 2026-08-09 — the data does not contain the risk. Swept all four production sites; zero hits.**
- **The sweep the entry says was never run, run:** published villages with no `field_health_center` — **ihangane 0 of 370 · vhw 0 of 18 · tip-somalia 0 of 5 · eheza-site 0 (no village content at all, though it has a live env)**. So the presave guard will deploy harmlessly and no data needs fixing first. Mechanism re-verified in code (`hedley_chw_node_presave`, `hedley_chw.module:24-33`, throws on empty health centre for any save — form, API or programmatic — from code-deploy + cache clear, before `fra` runs).
- ⚠ **True as of 2026-08-09 only.** The guard is not live yet, so an operator can still create a village without a health centre before the deploy. **Re-run the sweep on release day** — it costs one query per site (`terminus connection:info <site>.live --field=mysql_command`, then count villages with no `field_data_field_health_center` row).
- 📌 **The rehearsal shortlist from the same audit stays as RELEASE-RUNBOOK items, not backlog items:** (1) this sweep on release day; (2) full `cc→updb→fra→cc` rehearsal on an ihangane prod-clone multidev, confirming `hedley_admin_update_7003` and that `admin/config/hedley-reporting` 404s; (3) time the 6 dashboard-stats builders on that clone (the delta adds `NOT EXISTS field_data_field_deleted` to million-row plans — indexed, expected fine, but the only plan-touching change); (4) leave an old-format `export_view_0` item QUEUED through the rehearsal. ⚠ **Item 2 overlaps B-145: the rehearsal needs a clone, and clones currently carry UNSANITIZED patient data.**

Original entry: RELEASE-PREP — production villages with NO health center will start THROWING on every save once the release ships
- From the R17 pending-release delta audit (full report summarized in the entry; agent traced the whole main..develop server delta). The delta's ONLY schema/updb payload is `hedley_admin_update_7003` (variable_del + menu_rebuild — SAFE, idempotent, O(1)); features delta is one line (`hedley_chw` village `field_health_center` instance `required 0→1`, storage-harmless). Deploy order confirmed from `RoboFile.php` `deployPantheonSync()`: `cc all ×2 → updb -y → fra -y → cc all` — nothing in this delta is order-sensitive.
- **The red flag:** new `hedley_chw_node_presave()` (shipped with #1910/#1911 on develop) throws an Exception on ANY save of a village with empty `field_health_center` — form, API, or programmatic — active from code-deploy + cache clear, before fra even runs. Such villages have existed (the very commit says they were "silently dropped from the all-devices sync feed"). After the deploy, each one becomes a hard save-failure with rolled-back transaction.
- **Action (ops, not code):** on each prod site (ihangane, vhw, tip-somalia, eheza-site), run read-only: `SELECT nid FROM node n WHERE type='village' AND NOT EXISTS (SELECT 1 FROM field_data_field_health_center h WHERE h.entity_id=n.nid)` and assign health centers to hits BEFORE the release. ⚠ Not yet run — my terminus query attempt was blocked by the session permission classifier; needs the user (or an allowed session) to run it.
- **Rehearsal shortlist from the same audit (fold into the release runbook):** (1) the village sweep above; (2) full `cc→updb→fra→cc` rehearsal on an ihangane prod-clone multidev, confirm 7003 + `admin/config/hedley-reporting` 404s; (3) measure the 6 dashboard-stats builders' per-HC duration on the clone — the delta adds `NOT EXISTS field_data_field_deleted` to million-row query plans (indexed, expected fine, only plan-touching change); (4) leave an old-format `export_view_0` QUEUED item through a rehearsal deploy — verified compatible by code-read, cheap to confirm. AQ payloads otherwise unchanged; deleted scripts have zero in-repo references (only residual risk: an out-of-repo Jenkins job calling one).
