<!-- id:B-011 status:IMPLEMENTED tier:4 round: -->
### B-011 — Signature-less stock_updates dropped from sync (innerJoin on optional field) — ✅ IMPLEMENTED 2026-07-08 (bundled into issue #1914, PR #1915 with B-045). Fix: innerJoin→leftJoin at HedleyRestfulStockUpdate.class.php:124 (postExecuteQueryForViewWithDbSelect already null-checks signature/uri → safe). Verified with the same SQL-demo + phpcs pass as the 5 photo classes.
- Mechanism CONFIRMED and worse than recorded: `HedleyRestfulStockUpdate.class.php:124` innerJoin file_managed; the 500-row window + revision_count are computed on a node-only query (HedleyRestfulSync :135-153/:239-265) BEFORE viewWithDbSelect runs the join → dropped node consumes a slot AND client `lastFetchedRevisionId` (max of RECEIVED entities, SyncManager/Update.elm:278-284) advances past it → permanently skipped, ledger diverges.
- BUT no live trigger: Elm encoder sends signature UNCONDITIONALLY (`Backend/StockUpdate/Encoder.elm:24`), cache-miss fallback still sets fileId 5002 (app.js:802), and `delete-unused-files.php:74` skips node-referenced files (no purge path). 5 photo handlers share the same innerJoin pattern in prod. field_signature instance required=0, so a future non-Elm writer could still trigger it.
- Fix: innerJoin→leftJoin at :124 (postExecuteQueryForViewWithDbSelect already null-checks signature/uri at :157 → safe).
