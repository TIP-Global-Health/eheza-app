<!-- id:B-024 status:IMPLEMENTED tier:2 round:10 -->
### B-024 — 4 of 5 progress-report pages never fetch health centers → Next-Appointment "Location" renders blank — ✅ IMPLEMENTED 2026-07-14 (issue #1937, PR #1938, branch `b024-progress-report-health-centers`, base develop @d23647c56, commit c0b052058, Copilot requested, CI running). Fix: `FetchHealthCenters ::` added to the 4 fetch modules (Nutrition/ChildScoreboard ProgressReport, Pages/ProgressReport, Pages/PatientRecord), mirroring the WellChild variant; guarded by isNotAsked in Backend/Fetch.elm so it's free when already loaded. NEW test module `Pages/ProgressReport/Test.elm` (5 tests — fetch fns are PURE `ModelIndexedDb -> List MsgIndexedDb`, so `List.member FetchHealthCenters` is directly assertable). DISCRIMINATION: reverting the Nutrition fetch fails exactly `✗ Nutrition` (2819/1). elm make 548, elm-test 2820, elm-format + elm-review (cold) clean.
- 📌 Anchor re-check corrected a STALE backlog assumption: the module now has MORE importers (Tuberculosis, AcuteIllness, FamilyNutrition ProgressReport views), but they import other helpers — only these 5 pages CALL `viewProgressReport`, so the affected set really is 5. Verify callers, not importers.
- `Pages/WellChild/ProgressReport/View.elm:1563` `getHealthCenterName child.healthCenterId db |> Maybe.withDefault ""` in `viewNextAppointmentPane` (rendered at :527, incl. in WhatsApp-shared reports via `ComponentWellChildNextAppointment`).
- `FetchHealthCenters` is issued by `Pages/WellChild/ProgressReport/Fetch.elm` but NOT by `Pages/Nutrition/ProgressReport/Fetch.elm`, `Pages/ChildScoreboard/ProgressReport/Fetch.elm`, `Pages/ProgressReport/Fetch.elm`, `Pages/PatientRecord/Fetch.elm` (child branch) — nor anywhere in their chains (verified transitively). All four render `viewProgressReport` (Nutrition :88, ChildScoreboard :88, ProgressReport :59, PatientRecord :179).
- Mechanism: `CheckDataWanted` (App/Update.elm:1385-1441, 50ms tick) forgets data not "wanted" for >5 min → `Backend/Fetch.elm:546` resets `healthCenters = NotAsked`; nothing on these pages re-requests → blank Location after >5 min of navigation away from PinCode/Clinical/etc. Severity LOW-MED (clinical document, silent).
- Fix: add `FetchHealthCenters ::` to the 4 fetch files (explicit, mirrors WellChild variant), or hoist into `Backend.NutritionEncounter.Fetch.fetch` (all four chains pass through it; extra fetches harmless).
