<!-- id:B-138 status:UNKNOWN tier:2 round:18 -->
### B-138 — ❌ **REFUTED 2026-08-09 — the premise is BACKWARDS. ⛔ Do NOT re-propose.**
- The entry claimed one rule copied five times with the server/elm copy drifting to `<`. In fact **the `<` sites are the ones following the written specification**, and the five sites answer **three different questions** that happen to share two numbers.
- **Requirement, traced via `9c7fdc2a2` → issue #1718** (adamhstewart, 2026-04-08), which defines the classification outright: *"MAM: MUAC >= 115mm and < 125mm. SAM: MUAC < 115mm OR edema. Normal: MUAC >= 125mm."* So exactly 11.5 is MAM and exactly 12.5 is Normal. **WHO agrees** (SAM = MUAC < 115mm). `Pages/Reports/Utils.elm:199,202` and `hedley_reports.module:3122,3126` implement it literally. **Flipping them to `<=` as this entry proposed would break conformance with both the requirement and WHO** — the same trap as B-131(b), where the obvious fix was the wrong direction.
- **The `<=` sites are not the same question:** client `muacIndicationForChild` (`Backend/Measurement/Utils.elm:48-61`) reads *the colour band on the physical MUAC tape* — its docblock says "classify according to the measurement tool shown at ihangane#282", and the 2017 tests lock it as a tape-reading oracle, not a statistical rule. `hedley_reports.module:4493` is `$send_to_hc_by_muac`, a precautionary acute-illness **referral trigger**. Changing either is a clinical/UX decision with no defect behind it.
- 📌 **Loose end RESOLVED 2026-08-11 (R19): `hedley_ncda`'s `<=` is NOT an outlier — it answers the tape question, consistently with its own paired consumer.** The sites (`hedley_ncda.module:628/:631` muac-measurement path + `:849-858` NCDA-questionnaire path, both added by PR #1382 "[Hotfix] Aggregated NCDA: Account for MUAC and Stunting level taken at NCDA activity", commit e6c8defcd) use `<= 11.5` severe / `<= 12.5` moderate — and the CLIENT child scorecard classifies the same row via `muacIndicationForChild` (the tape-colour oracle) at `Pages/WellChild/ProgressReport/View.elm:2774/:2798`. So the NCDA family (client scorecard + PHP aggregated blob) is a CONSISTENT pair on tape semantics; the reports family is a consistent pair on #1718/WHO statistics semantics. Whether the scorecard family SHOULD move to statistical boundaries at exactly 11.5/12.5 is the declined boundary-convention clinician-gated class. No finding; nothing left to chase here.
- ⭐ **Lesson: "N copies of a constant disagree" is not automatically drift.** Ask what question each site answers before calling one a copy of another. Three of these five were correct for their own purpose.

Original entry (refuted): Admin Nutrition report misclassifies MUAC at exactly 11.5 / 12.5 cm — CONFIRMED-label was wrong
- `server/elm/src/Pages/Reports/Utils.elm:192-206` `categorizeAcuteMalnutrition`: SAM if `muacCm < 11.5 || hasEdema`, MAM if `< 12.5`. Client convention (locked by tests since 2017, `Backend/NutritionEncounter/Test.elm:187,228`): `client/src/elm/Backend/Measurement/Utils.elm:54-61` — red `<= 11.5`, yellow `<= 12.5`. Server copy written 2026-04-09 (9c7fdc2a2, #1718) — the later copy diverged.
- Impact: MUAC records to one decimal, so exact band-edge values are ordinary. In the admin Nutrition report's prevalence/incidence tables (`Pages/Reports/View.elm` ~:1280/:1307/:1359/:1403 + raw buckets :1253): a child at exactly 11.5 counts MAM instead of SAM; at exactly 12.5, normal instead of MAM — while the CLIENT simultaneously shows the same child red/yellow. **Third divergent copy**: PHP `hedley_ncda` uses `<= 11.5` (agrees with client), server/elm disagrees with both.
- Fix: flip the two comparisons to `<=`. 2 characters + a test if extractable. Sibling of B-131 (same vendored-copy drift class; both PRs could share a branch with B-128..B-132).
- (Adjacent client-internal observation from the same report, NOT queued: `ZScore/Utils.elm:163` + `Pages/WellChild/ProgressReport/View.elm:2755` use `< -3`/`< -2` while `Backend/NutritionEncounter/Utils.elm:683,700` uses WHO `<= -3`/`<= -2` — boundary-convention class, clinician-gated per policy.)
