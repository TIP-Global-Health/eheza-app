<!-- id:B-056 status:WONTFIX tier:4 round:12 -->
### B-056 — NCD isolated diabetes is medicated but never referred: `referForDiabetes` requires coexisting hypertension, `medicateForDiabetes` does not — ❌ DROPPED 2026-07-03 (user: not pursuing — the hypertension gate is applied consistently to both diabetes AND renal referral = intended design, isolated diabetes managed at the HC by protocol; NOT a bug. Do NOT re-propose.)
- `Pages/NCD/Utils.elm:962` `referForDiabetes` = `(isJust <| resolveCurrentHypertensionCondition assembled) && (diabetes diagnosed)`; `:575` `medicateForDiabetes` = `(diabetes diagnosed)` with NO hypertension gate. Consumers: `TaskReferral = referForDiabetes || referForHypertension || referForRenalComplications`; `TaskMedicationDistribution = medicateForDiabetes || medicateForHypertension`. Input: first NCD encounter, diabetes comorbidity fires `DiagnosisDiabetesInitial`, BP normal, no hypertension → medication task shown, Referral task absent.
- **LIKELY INTENDED**: `referForRenalComplications` (:975) is ALSO hypertension-gated → the gate is applied consistently to both non-hypertension referrals, not a copy-paste slip → suggests design ("manage isolated diabetes at HC; refer to hospital only when combined with hypertension"). Gate on clinician confirmation of the NCD referral spec before any change. Fix IF confirmed: drop the `resolveCurrentHypertensionCondition` prefix from referForDiabetes (+ renal). Low-med confidence as a bug.
