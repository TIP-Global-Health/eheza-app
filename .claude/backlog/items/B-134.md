<!-- id:B-134 status:READY tier:3 round:17 -->
### B-134 — ChildScoreboard progress report: end-encounter dialog CANNOT be cancelled, and the diarrhea→acute-illness referral popup never fires there — READY · CONFIRMED (self-verified 2026-08-03) · MED
- Found via the R17 NoUnused run (`TriggerAcuteIllnessEncounter` reported unused). Since `9415befab` (2023-11-27, "Wire common Nutrition progress report view"): `Pages/ChildScoreboard/ProgressReport/View.elm` ~:68-72 passes `setEndEncounterDialogStateMsg = always ShowAIEncounterPopup` into the shared progress-report config. The shared dialog's Cancel is `setEndEncounterDialogStateMsg False` (`Pages/WellChild/ProgressReport/View.elm` ~:634), and `ShowAIEncounterPopup` unconditionally sets `showAIEncounterPopup = True` (`ProgressReport/Update.elm:39-43`) — **Cancel re-opens the dialog; only confirming (CloseEncounter) exits.**
- Second half: the Encounter page has a ChildGotDiarrhea check → popup → `TriggerAcuteIllnessEncounter` flow (`Encounter/View.elm` ~:46/:146/:157-167); the progress report's end-encounter path never runs that check, and the page's `TriggerAcuteIllnessEncounter` handler (`Update.elm:45-50`) is unreachable.
- Fix shape: give the page a real `SetEndEncounterDialogState Bool` (or reuse the popup msg with the Bool respected) + wire the diarrhea check into the progress-report end path like the Encounter page. Small; discrimination-testable if the dialog-state logic is extracted.
