<!-- id:B-366 status:READY tier:3 round:31 -->
### B-366 — Antenatal dashboard (nurse): "Hospital Referrals" has read 0 every month since the card was added — it counts `send_to_hc` = refer-to-health-centre on nurse encounters, but nurses record hospital referrals in `referrals`, which the stats payload never includes — READY · CONFIRMED (card filter, the builder's join list and the nurse referral writer read by the orchestrator; orchestrator live query: `send_to_hc` rows exist only on CHW encounter types, while 510 `referrals = 'hospital'` rows exist) · MED-LOW · tier 3 · R31 unit 2 · DEPLOYED since `d30270cfb` (2023-12), all sites
- **Mechanism.**
  ```elm
  -- client/src/elm/Pages/Dashboard/Utils.elm:1111-1123
  countHospitalReferralsForSelectedMonth dateLastDayOfSelectedMonth =
      List.filter
          (.encounters
              >> List.any
                  (\encounter ->
                      isNurseEncounter encounter.encounterType
                          && withinSelectedMonth dateLastDayOfSelectedMonth encounter.startDate
                          && EverySet.member ReferToHealthCenter encounter.sendToHCSigns
  ```
  ```php
  // server/hedley/modules/custom/hedley_stats/hedley_stats.module:1190-1193 — the only referral data pulled
    $query->leftJoin('field_data_field_send_to_hc', 'send_to_hc', 'send_to_hc.entity_id = prenatal_encounter.entity_id');
    $query->addExpression("GROUP_CONCAT(DISTINCT send_to_hc.field_send_to_hc_value)", 'send_to_hc');
  ```
  (`field_referrals` appears nowhere in `hedley_stats.module`.) Nurses write `referToFacilitySigns` — `ifNullableTrue ReferToHospital form.referToHospital` (`Pages/Prenatal/Utils.elm:3434-3437`), since `376ac77ef` (2022-08); the `send_to_hc` form is CHW-only (`Pages/Prenatal/Activity/View.elm:4171-4174`).
- **Evidence.** Orchestrator, ihangane.live 2026-09-14: `field_send_to_hc` joined to encounter type → only `chw-1` 77, `chw-2` 15, `chw-3` 13, `chw-postpartum` 30 (no nurse type); `field_referrals_value = 'hospital'` → 510 rows. Scout: nurse hospital referrals by year 48/127/104/128/24 (2022–2026); vhw CHW rows only; uvl no `field_referrals` rows.
- **Impact.** Nurse → Dashboard → Antenatal Care, second row (`View.elm:1299-1304`): 0 hospital referrals every month on every site.
- **Fix shape (guess).** Emit the `referrals` values from the prenatal builder as a new optional payload field (a new FIELD is safe in one release — memory `enum-variant-breaks-deployed-clients`) and count `ReferToHospital` on nurse encounters.
