<!-- id:B-299 status:READY tier:3 round:27 -->
### B-299 — Stock Management (CHW village): a saved stock update never reaches the village view in-session — the revision path updates only the HC-keyed caches, so the save looks like nothing happened and invites double entry — READY · CONFIRMED (revision arm + every cache writer enumerated by orchestrator) · MED-LOW · tier 3 · R27 unit 5 · LIVE on tip-somalia only (`stock_management_village_enabled=1` there, sole site; HC flag 0/absent everywhere) — usage nascent: **1** village-tagged stock update exists (2026-09-01)
- **Mechanism.** Local saves do push revisions instantly (`postNode` → `sendRevisedNode`, `nodes.js:329/:352` → SW `NewRevisions` → `Backend.Model.HandleRevisions`), but the `StockUpdateRevision` arm (`Backend/Update.elm:7854-7878`) merges only into the HC caches — `mapStockManagementMeasurements healthCenterId …` plus `stockManagementData … NotAsked` — touching neither `villageStockManagementMeasurements` nor `villageStockManagementData`. The ONLY writer of the village measurements is `HandleFetchedVillageStockManagementMeasurements` (`:1259-1263`), fetched only when `NotAsked` (`Backend/Fetch.elm:430-433`) and evicted only by the 5-minute forget (`:691`). Sync-download-complete re-marks the village *data* for recalculation, but the recompute (`:1265-1290`) folds the STALE measurements, so nothing changes. `AhezaChildRevision`/`AhezaMotherRevision` (`:5464-5487`) have the same gap (update only `familyNutritionMeasurements`).
- **Worked example.** CHW saves "Received 50": the revision fires, but on a CHW device `stockManagementMeasurements[hc]` was never fetched (village context fetches only village endpoints, `Pages/StockManagement/Fetch.elm:15-20`) so the merge no-ops; the form resets to `ModeMain` showing the old MTD-In, old balance, no new history row. The entry appears only after ≥5 min off the page (forget → refetch reads the local node from IndexedDB) or a reload.
- **Impact.** Every CHW stock save on tip-somalia (the Aheza program site) visibly "does nothing" — the natural reaction is to enter it again, producing duplicate receipts/corrections in a real stock ledger. HC nurses unaffected — their sibling revision path was adapted; the village half is the unadapted copy.
- **Fix shape (guess).** In `StockUpdateRevision` (and the two aheza arms), also merge into `villageStockManagementMeasurements` and mark `villageStockManagementData`, mirroring the HC pattern.
