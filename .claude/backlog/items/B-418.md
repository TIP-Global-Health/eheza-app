<!-- id:B-418 status:READY tier:3 round:35 -->
### B-418 — Patient Record: the adult Family Planning pane leaves out every method recorded in an NCD encounter — READY · CONFIRMED (orchestrator read pane, caller, fetch and the issue spec, and counted live rows) · MED-LOW · tier 3 · R35 unit 5 · DEPLOYED (Rwanda)
- **Mechanism.** The pane has two sources only: group `motherMeasurements.familyPlannings` and each prenatal encounter's `familyPlanning`. The caller hands it only the antenatal participants.
  ```elm
  -- client/src/elm/Pages/PatientRecord/View.elm:244, 601-631
                  FilterFamilyPlanning ->
                      viewFamilyPlanningPane language personId (List.map Tuple.first pregnancies) db
  ...
          entries =
              groupFamilyPlannings
                  ++ prenatalFamilyPlannings
  ...
          prenatalEncountersIds =
              List.concatMap (getPrenatalEncountersForParticipantDesc db >> List.map Tuple.first)
                  prenatalParticipantsIds
  ```
  `fetchForAdult` (`Pages/PatientRecord/Fetch.elm:43-79`) has the same gap: mother measurements, prenatal encounters + measurements, acute-illness encounters — no NCD encounters or measurements.
- **The missing source.** NCD records the same sign set: `NCDMeasurements.familyPlanning : Maybe ( NCDFamilyPlanningId, NCDFamilyPlanning )` (`Backend/Measurement/Model.elm:3620`), asked of fertile women with `FamilyPlanningCurentlyQuestion` ("which methods do you use") — closer to the pane's purpose than the prenatal form's `FamilyPlanningInFutureQuestion`.
- **Intent.** Issue #271 (the pane's spec, built 52902381d 2022-07-25): "a list of the encounter dates where we asked a patient what method of family planning they are using and then the option they selected." NCD family planning arrived a month later (f6ad107f8, 2022-08-25) and the pane was never extended.
- **Live (orchestrator, 2026-09-21):** ihangane.live **746** live `ncd_family_planning` nodes over **545** women, none shown. Scout: ≥416 of them have no group/prenatal record, so the pane says "No matches found". vhw/uvl: 0. Display only; no data affected.
- **Reach.** Participant Directory → patient-record icon for an adult woman → Family Planning filter. Any role, no flag.
- **Fix shape (first guess):** resolve NCD participants in `fetchForAdult`, fetch NCD encounters + measurements; add a third source to the pane (`getNCDEncountersForParticipant` → `ncdMeasurements` → `.familyPlanning`); adult case in `Pages/PatientRecord/Test.elm`.
