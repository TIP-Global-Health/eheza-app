<!-- id:B-166 status:STALE tier:3 round:19 -->
### B-166 — Wellbeing notification "Ignore" reschedules from the STALE timestamp → instant-reappear modal loop — READY · CONFIRMED (self-verified verbatim) · MED-LOW (usage-capped: 20 enrolled nurses, one site, program idle since 2025-11) · 1-line fix · R19 Unit 1
- `Pages/PinCode/Update.elm:42-58` (self-verified): `HandleNotificationResponse` computes `nextNotification = Maybe.map (Time.Extra.add Hour 1 utc) model.nextNotification` — **+1h from the OLD scheduled time, not from now** (the `utc` param is cosmetic per the recorded time-extra lesson — pure ms arithmetic). Armed at login via `SetNextNotification model.currentTime`; dialog shows whenever `nextNotification <= currentTime` and unread > 0 (`PinCode/View.elm:427-441`).
- Trigger: enrolled nurse returns to the main menu >2h after the last schedule (any normal workday) with unread messages (near-always — 93-message corpus). After Ignore, the recomputed time is STILL in the past → modal re-renders immediately; agent harness: a 5h gap needs 6 consecutive Ignore clicks to escape. No data loss — pure UX.
- Fix: `Just (Time.Extra.add Hour 1 utc currentTime)` — schedule from the wall clock.
- **Usage verdict for the whole program (Unit 1, live-measured all 3 sites):** per-nurse gating (`field_resilience_program`, no site flag); ihangane 87 resilience_survey nodes (44 adoption/43 quarterly, 2024-08-26→2025-11-06, ZERO in 2026), 20 nurses enrolled (all kicked off, 18 with message blobs ≤13.8KB); vhw 0; tip-somalia 0. Real ~15-month single-site program, quiescent ~9 months — mechanisms stay armed for any nurse an admin enrolls.
