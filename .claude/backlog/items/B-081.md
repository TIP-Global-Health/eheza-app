<!-- id:B-081 status:READY tier:4 round:16 -->
### B-081 — CLAUDE.md doc drift (one doc PR): feature-flag names + missing flag + stale CI-job count — READY · CONFIRMED · trivial · doc-only
- (a) `hedley_admin.module:646-647` defines `stock_management_hc`/`stock_management_village`; CLAUDE.md:111 lists bare `stock_management` (operator vset from doc would no-op). (b) `healthy_start` flag missing from CLAUDE.md's list entirely (canonical list `hedley_admin.module:638-649`). (c) CLAUDE.md "Four parallel jobs" in the CI section is stale — 7 exist (+ lint_elm_review, test_zscore, test_elm). One doc-only PR.
