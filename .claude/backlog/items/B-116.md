<!-- id:B-116 status:IMPLEMENTED tier:2 round: -->
### B-116 — Patient merge never re-points individual_participant.field_newborn (pregnancy → newborn link) — ✅ IMPLEMENTED 2026-07-15 (issue #1949, PR #1950, branch `b116-merge-transfer-newborn`, base=**b114 branch** (stack: b113←b114←b116; retarget to develop as they merge), commit 4ed4f9284, Copilot requested). Fix: new `_consolidate_patient_newborn_references($original,$duplicate)` called from execute(); REUSES `_hedley_patient_content_referencing_person($dup,['field_newborn'],'individual_participant')` (b114) + existing `_associate_content_by_field` to set field_newborn=original. field_newborn is single-value (cardinality 1) → no dedup; participant shards derive from field_person (parent) → no shard handling. DISCRIMINATION (drush, real fn): DuplicateChild→OriginalChild; OLD-path proof (participant keeps stale ref, participant NOT deleted — cascade doesn't follow field_newborn). Test cases 10-11 folded into HedleyPatientConsolidation single method. Local /code-review medium: NO findings (reuses both helpers, exemplary). **This completes the merge-gap group** (B-113 relationships / B-114 education sessions / B-116 newborn + B-118 canonical-list root cause). Remaining merge audit items are Tier-4/low: B-115 (whatsapp_record), B-117 (trace-contact field_referred_person, latent), B-119 ❌ WON'T-FIX (frozen, verified 2026-08-03), B-120 (retroactive backfill of past-merge losses, needs scoping).
- If the DUPLICATE is the newborn, the mother's pregnancy participant keeps pointing at the deleted child. Note the merge handles participants OF the duplicate, but not references TO the duplicate from someone else's participant. Live check already shows 1 such orphan.
