<!-- id:B-285 status:IMPLEMENTED tier:3 round:26 -->
### B-285 — TB/HIV follow-up medication task: the "still taking the prescribed medications?" question shows Kirundi text to every user in every language — ✅ IMPLEMENTED · CONFIRMED (commit diff verified; on `origin/main`) · MED-LOW · tier 3 · R26 unit 8 · ⭐ DEPLOYED all sites
- **Mechanism.** `Translate.elm:19991-19996`: `PrescribedMedicationsChangedQuestion -> { english = "Suzuma ko umurwayi agifise imiti yandikiwe kandi ko ayifata uko vyategekanijwe", kinyarwanda = Nothing, kirundi = Nothing, somali = Nothing }`. Commit `4a4e5e838` (2025-08-20, "add the rest of translations") REPLACED the English string — *"Please check the medications that were prescribed. Is the patient still taking the medications listed above"* — with the Kirundi translation instead of filling `kirundi`; it is the only `english =` line that commit changed. Every other language falls back to `english`, so Rwanda nurses (and Somalia) see Kirundi. Semantics stay correct (Yes → `medicationsNotChanged = True`).
- **Not the declined translation-quality class.** The coverage map's exception is explicit: objectively wrong text — wrong language in a field — qualifies. The English is gone, not merely unpolished.
- **Consumers.** `Tuberculosis/Activity/Utils.elm:698`, `HIV/Activity/Utils.elm:567`. Identical line on `origin/main:19983-19984`.
- **Fix shape (guess).** Restore the English string and move the Kirundi one into `kirundi`. Two lines.
- ✅ **IMPLEMENTED 2026-08-31 — issue #2175, PR #2176**, branch `B-285-prescribed-medications-english` off `origin/develop @ 4449d2b40`. English restored, Kirundi moved to the `kirundi` field; elm-format clean, full `elm make` success, elm-review run on the committed state.
- **"Only instance?" answered by a mechanical scan (2026-08-31), on the user's question.** All 3,524 `Translate.elm` records parsed; the `english` field was screened for Kinyarwanda/Kirundi/Somali stopwords, Bantu apostrophe prefixes, and equality with the record's own translation fields. **This entry was the only one whose `english` field holds non-English text.** Ten records show the OPPOSITE, harmless shape — a translation field carrying an exact copy of the English string (e.g. "Nitrite"/"Amitriptyline"/"Glibenclamide"/"Hydralazine"/"Lower Back"/"Child"/"cm" kirundi; "Please contact" somali; "Severe Preeclampsia" kirundi; "Has anyone in your family been told they have hypertension" kirundi) — display-identical to the `Nothing` fallback, only a convention nit against the repo's leave-it-Nothing rule. Not fixed here.
