<!-- id:B-148 status:READY tier:4 round:18 -->
### B-148 — Sync observability: upload-lane error replies decode as empty successful batches (+ storage-full banner flicker) — READY · **(a) upgraded to CONFIRMED 2026-08-17 (R20 wave 2: independently re-traced end-to-end, anchors current)** · LOW · telemetry
- (a) #1889's catch handlers reply `{entities: [], remaining: 0, error: String(e)}` for the General/WhatsApp/Authority upload queries (app.js catches at `:970/:1005/:1067`; Authority is the lane carrying all patient measurements), but the Elm decoders (`SyncManager/Decoder.elm:214-255`) never read `error` — `Update.elm:1435-1449` maps the empty list to `RemoteData.Success Nothing` with `remaining = 0` in the sync counters. An IndexedDB read failure shows as "nothing to upload". ⚠ R20 nuance: "self-heals next cycle" holds only if the Dexie read recovers — a PERSISTENT failure (`DatabaseClosedError`, private-mode `InvalidStateError`) is a permanent false-green, invisible on-device AND in Rollbar (the outer decoder succeeds, so nothing is logged). Photo/screenshot lanes DO have tagged `UploadError` decode (:145/:186) — port that pattern.
- (b) `SavedAtIndexDbHandle` success branch (`SyncManager/Update.elm` ~:2300) clears `lastSaveError` on ANY table's success → storage-full banner can blink off between failing batch saves. Consider clearing per-table or only on the failing table's success.

- **Client `NoUnused.CustomTypeConstructorArgs` one-off: RUN, ZERO real items** — 12 reports, 10 verified FALSE POSITIVES (same alias-shadowing rule bug as R17's, now proven to break ARG tracking too: every flagged Revision variant's args ARE extracted in Backend/Update.elm handlers, e.g. Call114Revision :5530, PrenatalUrineDipstickTestRevision :7712; Relationship Save's 3 args all used at Pages/Relationship/Update.elm:39), `BadJson` extracted at SyncManager/Utils:156, `IndexDbSaveErrorOther`'s string used via equality only (benign). ⚠ Rule-lesson recorded in the coverage map: CustomTypeConstructorArgs is NOT trustworthy in this codebase.
