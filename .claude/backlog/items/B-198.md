<!-- id:B-198 status:UNKNOWN tier:3 round:21 -->
### B-198 — Group enrolments SILENTLY LOST in upload: 742 persons have group measurements on the server but NO pmtct_participant anywhere — ⚠ the writer defect is PARKED B-158, which now has a measured live data trail — CONFIRMED (writer anchor orchestrator-verified verbatim; forensics agent-verified) · tier 3 (data half) · ongoing (87 rows in 2026 under deployed code) · R21 wave B · READY
- Data: 3,986 group measurements whose person has no enrolment in the session's clinic — **3,595 rows / 742 persons with NO enrolment record ANYWHERE** (2022: 1,096 → 2026: 87, ongoing); 391 rows / 68 persons enrolled only in a different clinic (legacy-dominated). CHW groups worst: ~1.06% of all CHW group measurements. Samples: 5592703-06 (2026-07-18), 3890864, 4446552.
- Forensics: every sampled nid-stream shows child+adult+relationship+**auto-attendance**+measurements in one upload burst, enrolment absent. Auto-attendance is created ONLY by `HandlePostedPmtctParticipant` on POST **success** (`Backend/Update.elm:4212-4234`, verified) — so the enrolment succeeded locally and was lost before upload. Server-side `handleChanges` is one transaction (cannot drop one item) ⇒ loss is client-side.
- Writer defect — **THIS IS B-158 (🅿 PARKED 2026-08-11, "do not re-pitch")**: `nodes.js` postNode (~:388-404, orchestrator-verified verbatim) + patchNode (~:298-315): change-row add fails twice → inner catch BUILDS a 400 Response → the chained `.then(function (localId) { return Promise.resolve(response); })` unconditionally replaces it with the 200; comment claims Rollbar logging that does not exist. Entity exists locally (device looks fine), never queues for upload, dies with the device. Identical on deployed main and develop. The IndexedDB-failure trigger is unproven (no client logs) but it is the only path matching the burst forensics.
- Consumer consequence: `ExpectedParticipants` (Session/Model.elm) builds group membership EXCLUSIVELY from pmtct_participants — on the server and every other/re-paired device these children/mothers don't exist in their group: absent from attendance lists, unmeasurable, undercounted in group dashboards, their synced measurements unreachable from any group view.
- ⚠ **Decision for the user, NOT auto-reopened**: B-158 was parked without a stated reason; this finding gives its mechanism a measured, ongoing field impact (742 persons). Present: does this evidence reopen B-158 (fix = drop the overriding `.then`, surface the 400 + Rollbar)? The DATA half stands regardless: backfill shape = derive enrolments from existing group measurements (clinic from session, expected-window from measurement dates).
