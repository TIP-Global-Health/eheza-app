<!-- id:B-191 status:READY tier:3 round:20 -->
### B-191 — Case management: prenatal follow-ups arriving at/after pregnancy conclusion are never auto-resolved — permanent invisible residents of every device's payload — READY · CONFIRMED (trigger hop orchestrator-verified: fires only on empty→set of field_outcome) · tier 4 · LOW (315 live rows, invisible to users) · R20 wave 4
- `hedley_case_management.module:516-517`: `$outcome_set = empty($outcome_orig) && !empty($outcome)` — a follow-up synced after conclusion (incl. postpartum-encounter ones) misses every trigger until the woman's next pregnancy activity. Client hides them (participant dateConcluded / postpartum rules, GlobalCaseManagement/View.elm:721-744), nodes.js keeps all unconcluded nodes forever. Live: **315** unresolved prenatal follow-ups on concluded pregnancies (ihangane); acute illness = 0 (that chain works). Fix: in the prenatal insert trigger, stamp resolved when the pregnancy already has `field_date_concluded`; optional one-off backfill.
- 📌 Hygiene notes (no items): `FollowUpNotNeeded` entries hidden client-side, unresolved server-side (~464 >6mo, negligible); TB-suspect pane partition would hide entries on a TB-flag-OFF site but both live sites have TB=1; dashboard AI totals vs CM pane TB-suspect bucketing = DIFFERENT-QUESTION labeling nit.
