<!-- id:B-073 status:READY tier:4 round:16 -->
### B-073 — PersonsPage (relation mode): `relationshipsByPerson` never fetched → "already related" filter silently off — READY · CONFIRMED (R16 fetch lens) · LOW
- `Pages/People/View.elm:211` `Dict.get personId db.relationshipsByPerson` in `personRelationCondition`; NotAsked → `unwrap True` → filter passes everyone (already-related people listed; duplicate-relationship risk, no crash). `Pages/People/Fetch.elm` issues only search-form fetch + `FetchPerson relation` + HC/villages; dispatch `App/Fetch.elm:174-181` adds nothing.
- MASKED in normal nav (entry `Pages/Person/View.elm:244`; `Pages/Person/Fetch.elm:47` fetches relationships first); UNMASKED on hard refresh at `/#relations/<uuid>/<origin>` (`Router.elm:422`) — ModelIndexedDb is in-memory only.
- Fix: `Maybe.map FetchRelationshipsForPerson relation |> Maybe.Extra.toList` appended in `Pages/People/Fetch.elm`.
