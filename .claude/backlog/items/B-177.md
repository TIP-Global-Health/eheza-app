<!-- id:B-177 status:READY tier:4 round:20 -->
### B-177 — Sync: an `acute_illness_encounter` with no diagnosis value poisons the main authority download batch — READY · CONFIRMED-mechanism · latent (live: 0 such encounters on ALL THREE sites — ihangane, vhw, tip-somalia checked 2026-08-18) · tier 4 · hardening · R20 wave 2
- Server emits `acute_illness_diagnosis: null` when the field row is absent (LEFT JOIN); client `Backend/AcuteIllnessEncounter/Decoder.elm:22` `required "acute_illness_diagnosis" decodeAcuteIllnessDiagnosis` has NO fallback while its immediate siblings do (`ai_encounter_type` and `deleted` both ride `decodeWithFallback`) — null fails → authority batch poison (B-170 class). Also the stats copy: `Backend/Dashboard/Decoder.elm:285` `required "diagnosis"`.
- Fix: `decodeWithFallback NoAcuteIllnessDiagnosis` to match the siblings. (B-108 rule satisfied: the fallback is a real domain value, not a blank.)
- 📌 **R27 unit 7 amendment — the strict-`required`-on-server-null family, widened; fix family-wide when built.** (a) `Backend/ResilienceSurvey/Decoder.elm:18` `required "resilience_survey_signs" (list … |> map Dict.fromList)` — the server emits NULL when the multi-value field is empty (`HedleyRestfulResilienceSurvey` postExecute `!empty(...) ? explode(...) : NULL`), and `resilience_survey` rides the **general** feed (`SyncManager/Decoder.elm` `decodeBackendGeneralEntity`), so one such node would jam EVERY device of the site, not one shard. (b) `Backend/Village/Decoder.elm:14-19` — five geo fields `required` through plain `string`. (c) `requiredAt [ "expected", "value" ]` (`Backend/IndividualEncounterParticipant/Decoder.elm:16`), every encounter's `requiredAt [ "scheduled_date", "value" ]`, and `Backend/PmtctParticipant/Decoder.elm:16`. All anomaly-conditioned: live counts were 0 at the R20 value-shapes sweep and R21 wave A (no poison rows in ~4.6M). The `required "deleted" bool` sites are SAFE — `HedleyRestfulSyncBase` unconditionally emits a real boolean.
- 📌 **R35 unit 4 amendment — a fourth member: `stock_update`.** `field_stock_update_type`, `field_quantity` and `field_execution_date` are `'required' => 0` (`hedley_stock_management.features.field_instance.inc` ~679/424/184). But `Backend/StockUpdate/Decoder.elm:18-20` has `required` for all three with no fallback, and the server sends NULL for a blank value (`HedleyRestfulStockUpdate` `:154` sets an empty date to NULL; value columns come from a LEFT join). The only way to get there is the Drupal `node/add/stock_update` form, since the client encoder always sends all three. Live 2026-09-21: 1 stock_update on all four sites (tip-somalia nid 5588), with all three fields set. Orchestrator re-read the decoder and all three instance flags.
