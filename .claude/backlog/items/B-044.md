<!-- id:B-044 status:IMPLEMENTED tier:2 round:11 -->
### B-044 — Villages without a health center are silently dropped from the all-devices sync feed (innerJoin field_uuid via optional field_health_center) + cascades to a shard-less CHW clinic — ✅ IMPLEMENTED 2026-07-08 (issue #1910, PR #1911, branch `b044-village-require-hc`, base develop @502d1cd73, CI running, Copilot requested). User chose the PREVENT-SAVE fix (not join change). Fix: (1) `node-village-field_health_center` instance required 0→1 in hedley_chw.features.field_instance.inc:121 (label 'Health Center' unique; applied on deploy via RoboFile fra #1828 + fresh install for CI); (2) new `hedley_chw_node_presave` throws if a village saved w/o field_health_center (covers programmatic/migration paths that bypass form validation — field_get_items empty-check). RESTful inner join LEFT AS-IS on purpose (client decoder does `required "health_center"` → emitting NULL-HC village would poison batch; the join is a backstop). NOT a leftJoin change (that would be the naive-wrong fix). Verify: php -l + phpcs Drupal+DrupalPractice CLEAN; SQL demo on real field tables (rolled-back txn) showed the inner join drops the HC-less village + keeps the HC one; guard-logic php check (no-field/empty-arr/null-target all THROW, with-hc saves). Cascade note: process_new_village guards type==village + no-ops on empty residents; process_new_participation guards type==pmtct_participant. ⚠ CI-BUDGET LESSON (user 2026-07-08): a SimpleTest was written (HedleyChwVillageValidation) then REMOVED — every DrupalWebTestCase CLASS = a fresh install + features_revert in HedleyWebTestBase::setUp, and test_simpletest_linux already runs ~23min vs a 25min goal (only ~2min headroom; 7 existing classes). Adding a class risks breaching. For simple invariants prefer out-of-band verification (SQL demo + guard-logic) over a new test class; if a committed test is needed, FOLD it as a scenario into an existing class's existing METHOD (per-method also reinstalls). See [[ci-simpletest-time-budget]].
- `HedleyRestfulVillages.class.php:52-60`: `field_health_center` LEFT-joined (optional, `node-village-field_health_center` required=0) but then `field_uuid` INNER-joined keyed off `field_health_center.field_health_center_target_id` (require=TRUE). A village saved without an HC → target_id NULL → inner join matches nothing → row absent from every device's general batch while still counted in revision_count (phantom "remaining"; if it's the newest revision the general feed sticks). `hedley_chw_process_new_village` then auto-creates a shard-less CHW clinic (also unreachable). Villages are admin/migrate-created (client never uploads them).
- Fix: make the instance required=1 + presave validation (client decoder `Backend/Village/Decoder.elm` does `required "health_center"`, so emitting a NULL-HC village would poison the batch — requiring the field is the safe fix, keep the join as backstop). Severity MED (admin one-field slip → invisible sync hole). Confirm frequency: are villages ever hand-created post-migration?
