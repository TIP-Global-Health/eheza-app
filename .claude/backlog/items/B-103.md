<!-- id:B-103 status:READY tier:4 round:16 -->
### B-103 — Delete 7 unimported client Elm modules (1,926 lines) + truqu/elm-base64 dep — READY · PROVEN · S
- `Restful/Login.elm` (1,335), `Gizra/Dom.elm` (202), `Utils/Confirmation.elm` (193), `Gizra/WebData.elm` (116), `Gizra/List.elm` (49), `Gizra/Maybe.elm` (23), `Utils/Upgrade.elm` (8). Zero-importer sweep over EVERY module incl. Test roots; only legit roots excluded (Main, LocalConfig.Example, Config.Deploy, *.Test). Also remove `truqu/elm-base64` from client/elm.json (sole importer was Restful/Login; active NoUnused.Dependencies would flag it post-delete). Verify: `elm make src/elm/Main.elm` + elm-test (elm-test alone is NOT a full type-check).
