<!-- id:B-326 status:READY tier:4 round:29 -->
### B-326 — Sync shards: a measurement is sharded by the person's health centres, not by the clinic that performed it, so an FBF distribution or follow-up for a child enrolled at two HCs is counted in the other HC's stock/case management, and a two-HC device keeps only one `shard` per node — READY · CONFIRMED (shard assignment, client shard column and the stock consumer read) · LOW · tier 4 · R29 unit 2 · likely decline-level, recorded for the census
- **Mechanism.** `hedley_device.module:311-316` — the `default:` arm for individual participants and all measurements assigns `hedley_general_shards_by_person($person_id)`; a `child_fbf` given at HC A's clinic for a child also enrolled at HC B carries shards `[A, B]`. `app.js:634-637` stores a single `entity.shard = info.shard` per node — whichever lane fetched it last. `nodes.js:774-778` (`viewStockManagementMeasurements`, filter `item.shard === shard`) and `Backend/StockUpdate/Utils.elm:21-37` (no session/clinic filter) count it in HC B's stock; follow-ups at `nodes.js:627-629` likewise.
- **Live (ihangane, scout-measured).** 234 device users report more than one synced HC; multi-shard `child_fbf`/`mother_fbf` in the last 12 months: 14 of 5,623; multi-shard follow-up-type nodes: 5. `hedley_admin_feature_stock_management_enabled` is unset on ihangane (HC stock is off on every site per the R27 flag map), so the stock consumer is inert today.
- **Fix shape (guess).** Shard measurements by the session's clinic HC where one exists, or store all shards client-side. Not worth a build at these numbers — record, do not pitch.
