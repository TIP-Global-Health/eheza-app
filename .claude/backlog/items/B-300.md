<!-- id:B-300 status:READY tier:4 round:27 -->
### B-300 — Stock Management (CHW village): the monthly data is computed as soon as measurements load, without waiting for the village residents list — losing the race zeroes every "Issued" figure and overstates balances — READY · CONFIRMED-mechanism (guards read; race frequency not demonstrated) · MED-LOW · tier 4 · R27 unit 5 · tip-somalia only (flag)
- **Mechanism.** `FetchVillageStockManagementData` (`Backend/Update.elm:1265-1290`) computes when the measurements are `Success`, with no check on `peopleInVillage`: `generateVillageStockManagementData` (`Backend/StockUpdate/Utils.elm:51-80`) filters aheza distributions by `List.member … villageResidents`, and `resolveVillageResidents` (`Backend/Village/Utils.elm:85-90`) returns `[]` while people are still loading — so every distribution is dropped: `issued = 0` all months, `consumptionAverage = 0`, balances = starting + received. The result is cached `Success` (guard `:1268-1271`) and survives until a sync-download-complete marking (online only) or the 5-minute forget; offline, the wrong figures hold for the whole visit.
- **Fix shape (guess).** Treat `peopleInVillage` as a compute dependency, or mark the data `NotAsked` in `HandleFetchedPeopleInVillage`.
