<!-- id:B-165 status:IMPLEMENTED tier:4 round:19 -->
### B-165 — PatientsSearchForm minors ×2 (shared search widget: People / IndividualEncounterParticipants / FamilyEncounterParticipants) — ✅ IMPLEMENTED 2026-08-13 (issues #2080 + #2082, PR #2081 "Stop the patient search losing what was typed") · CONFIRMED (self-verified at anchors) · LOW · R19 Unit 3
- (a) Tapping the ALREADY-ACTIVE "Name"/"National ID" label wipes the typed query: `viewCustomBoolInput` attaches `label [ onClick <| setMsg value ]` unconditionally to both options (`Pages/Utils.elm:598`), and `SetMode` resets `input = "", search = Nothing` unconditionally (`Components/PatientsSearchForm/Update.elm:31`). Also `SetMode` leaves `model.debouncer` untouched — a query typed ≤500ms before the switch still emits `SetSearch "<old text>"` under the NEW mode (no consistency check → a name string can land as a national-ID search, or a just-wiped search resurrects). Fix: no-op when the mode is unchanged + reset the debouncer on switch.
- (b) National-ID input rejection desyncs the visible field from the model: the ModeSearchByNationalId `SetInput` branch returns the model UNCHANGED when `String.toInt` fails — including the EMPTY string (`Update.elm:70-75`) — so identical vdom ⇒ no DOM patch: select-all+delete leaves a visually empty box with stale results still rendered from the old `model.search`; a stray non-digit stays visible and freezes input handling until deleted. Same-class sibling noted: `ReportToWhatsAppDialog` `SetPhoneNumber` (lower impact, length gating reads the model). Fix: accept empty explicitly (reset input+search); echo rejected input back into the model without searching.
- **What shipped** (`Components/PatientsSearchForm/Update.elm`): `SetMode` no-ops when the mode is unchanged, and on a real switch feeds the debouncer `SetSearch ""` (the debouncer has no cancel API — `provideInput` with an empty string is how you neutralise a pending emit, since the accumulator is last-input-wins). Empty/whitespace input clears `search` IMMEDIATELY in BOTH branches instead of 500ms later, and the National-ID branch stores the typed `input` rather than dropping it.
- 📌 Emptiness is `String.isEmpty (String.trim input)`, not `String.isEmpty input` — a space-only query otherwise counts as content and keeps the stale result list up. Review round 2 found this; both of its findings were taken, nothing else.
- ⚠ **The debouncer-reset half was ADDED after the first review round** — the user asked for it explicitly once the PR was already up. Scope grew by one branch; it stayed small because the fix reuses the existing `sequence update [ MsgDebouncer … ]` idiom already in the file rather than adding cancel plumbing.
- ⛔ **CI on this branch failed twice on package-download flakes, not the diff** (`Gizra/elm-debouncer 2.0.0`, then `ccapndave/elm-update-extra 4.0.0`, both `NoResponseDataReceived` from GitHub zipballs) — see the CI-flake note in [[improvement-coverage-map]].
