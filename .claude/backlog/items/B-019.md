<!-- id:B-019 status:PARKED tier:3 round: -->
### B-019 — hedley_view_export requeue path missing switchUserBack — 🅿 PARKED (user 2026-07-13, with B-005: "this export is used very rearly") — do NOT re-pitch. Parked together with [[B-005]] on the low-usage argument, even though its blast radius escapes the export feature (the bled uid lands on unrelated node_saves in the same 5-min drush run — surfaced to the user, who parked anyway). Mechanism stays CONFIRMED + LIVE; fix (add `$manager->switchUserBack();` before the requeue return, ideally try/finally around the worker body) remains valid if revived.
- Worker switches global $user to export creator (:188-193 via RestfulAuthenticationManager). Exit paths: file-load fail/write fail/mail fail → `$failure()` closure restores (:199); success restores (:283); REQUEUE (:249-258) returns WITHOUT restoring — and requeue is the NORMAL path for every export >1 batch (range default 500). Bonus gap: exceptions from views render (:218-234) also escape switched.
- Blast radius: the 5-min drush run (`infrastructure_setup/advancedqueue.sh:19`, `--all --timeout=300`, one PHP process, no --user → baseline anonymous) processes hedley ncda/reports/stats workers AFTER the bleed → their node_saves get the export creator's uid (attribution) + privilege vs anonymous. Compounding: next batch's fresh manager snapshots the ALREADY-SWITCHED user as "original", so even final-batch switchUserBack restores to the creator — bleed persists to end of run even for clean exports. drupal_save_session(FALSE) also left set (inert under drush).
- Fix: `$manager->switchUserBack();` before the requeue return; ideally try/finally around the worker body. LIVE (same Historical Child surface as B-005).
