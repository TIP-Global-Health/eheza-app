<!-- id:B-416 status:READY tier:4 round:34 -->
### B-416 — `allowed_values` drift both ways: five values the app writes today are undeclared, and three prenatal fields still declare values the app abandoned — READY · CONFIRMED (orchestrator read every set and counted live rows) · LOW · tier 4 · R34 unit 6 · LATENT (0 live rows on ihangane)
- **Direction A — written but not declared.** Values current UI code produces that the field's option set does not contain:
  | field | missing value | writer |
  |---|---|---|
  | `field_breastfeeding_signs` (`prenatal_breastfeeding`) | `none` | `Pages/Prenatal/Activity/Utils.elm:6565` — `ifEverySetEmpty NoBreastfeedingSigns` |
  | `field_hypertension_predecessors`, `field_heart_problem_predecessors`, `field_diabetes_predecessors` (`ncd_family_history`) | `none` | `Pages/NCD/Activity/Update.elm:733, 756, 779` — `setMultiSelectInputValue … NoPredecessors …` |
  | `field_dot_meds_distribution_sign` (`tuberculosis_dot`) | `negative-not-indicated` | `Pages/Tuberculosis/Activity/Utils.elm:558-566` offers `DOTNegativeNotIndicated` in the "why not" list |
  Verified: `field_hypertension_predecessors` declares only `father`, `mother`, `grand-father`, `grand-mother`; `field_dot_meds_distribution_sign` declares only `positive`, `negative-taken-today`, `negative-unavailable`, `negative-side-effects`, `negative-patient-refused`; `field_breastfeeding_signs` ends at `breastfed-first-hour` — no `none` in any of them, while `NoPredecessors -> "none"` (`Backend/Measurement/Utils.elm:3210`) and `NoBreastfeedingSigns -> "none"` (`:2519`).
- **Direction B — declared but abandoned.** `field_medical_history` still lists `bowed-legs`, `hiv`, `tuberculosis-past`, `tuberculosis-present`; `field_previous_delivery` still lists six removed outcomes; `field_obstetric_history` is still instanced on `obstetric_history_step2` though no REST resource exposes it. The client dropped all of these in `1dac72459`, paired with `72d87e51e` which deleted `hedley_prenatal_node_presave()`. The config was never updated with them.
- **It is not a sync jam, and that was checked rather than assumed.** RESTful writes through `entity_metadata_wrapper::set()`, which type-checks but does not consult the options list (list fields register no validation callback — R34 unit 5 read the contrib `entity` wrapper directly), and `list_field_validate()` only runs through Form API. On the read side, `Utils/Json.elm:60-64`'s `decodeEverySet` absorbs an unknown member *and* a null payload, and the 45 strictly-decoded enum keys were re-checked: **none has an `allowed_values` entry its `*FromString` rejects.**
- **What is real.** (1) The Drupal node-edit form silently drops an undeclared value — an administrator opening and saving an `ncd_family_history`, `prenatal_breastfeeding` or `tuberculosis_dot` node erases the `none` / `negative-not-indicated` marker, because the options widget only renders declared values. (2) Direction B is the mirror: an administrator is still offered "HIV" / "Bowed legs" / "Stillborn"; if ticked, the value stores and every device then silently drops it, so it is invisible in the app and lost on the next device save. (3) The config no longer documents the contract, which is what every audit of this kind reads first.
- **Evidence (orchestrator live query, ihangane.live, 2026-09-21).** `breastfeeding 'none'` **0**, `hypertension_predecessors 'none'` **0**, `dot 'negative-not-indicated'` **0**, `medical_history` legacy values **0**. Latent on the largest site. ⚠ Per the R33 lesson, zero rows is not evidence of dead code — these are recent-ish paths with few opportunities. ⚠ The commit that removed the client cases verified "zero legacy rows" against **ihangane only**; the other three sites were never checked.
- **Fix shape (guess).** Add the five values and re-export with `drush fu`. Whether to remove the abandoned prenatal values is a data question, not a code one — it needs the per-site row counts first.
