<!-- id:B-289 status:READY tier:1 round:26 -->
### B-289 — NCD: a pregnancy found by test at the first encounter is forgotten at every later encounter — the full antihypertensive list (ACE inhibitors) is offered, referral goes to hospital instead of ANC, health education is shown — ✅ **IMPLEMENTED 2026-09-09, issue #2221, PR #2222** (branch `B-289-ncd-pregnancy-across-encounters`, base `B-288-point-of-care-diabetes-tasks` @9249f6dbc — stacked behind #2216 and #2220) · MED · tier 1 · R26 unit 5
- **Mechanism.** `patientIsPregnant measurements` (`Pages/NCD/Utils.elm:955-963`) reads only the CURRENT encounter's pregnancy test (`executionNote == TestNoteKnownAsPositive || testResult == Just TestPositive`). The test is offered once: `TaskPregnancyTest -> isPersonAFertileWoman … && initialTestRequired TaskPregnancyTest` (`Activity/Utils.elm:897-899`), and `initialTestRequired` is false as soon as a previous encounter has a performed test with a valid result (`:918-938`, `resolveLabTestDate`; "run today" stores `executionDate = currentDate`). So from the second encounter on `patientIsPregnant` is false for a woman who tested POSITIVE last month. Consumers: `generateRecommendedTreatmentSignsForHypertension` (`:398-406`) falls back to `allRecommendedTreatmentSignsForHypertension` (`:409-419`, includes `TreatmentCaptopril`, `TreatmentLisinopril`); `resolveReferralInputsAndTasks` facility (`:813-820`) → `FacilityHospital`; `referForHypertension` loses the "pregnant" arm (`:946-947`); `expectNextStepsTask TaskHealthEducation` (`Activity/Utils.elm:147-148`) no longer suppressed; `medicateForHypertension` Stage-1 arm (`:606-607`) loses the pregnancy trigger.
- **The asymmetry that shows it is unintended.** "Known as positive" is NOT a performed note (`testPerformedByExecutionNote`, `Measurement/Utils.elm:8591-8593`), so a woman who answers that way is re-asked at every encounter and stays pregnant in the logic; only the woman found pregnant BY THE TEST is forgotten.
- **Reachability.** Fertile woman (12 < age < 50), first NCD encounter with a positive on-the-spot pregnancy test; any hypertension diagnosis at the next encounter. A pregnancy outlasts ~8 monthly follow-ups.
- **Impact.** Contraindicated medication options and the wrong referral facility during a pregnancy, on both sites. Distinct from B-224 (report row visibility); this is the activity side.
- **Gate.** `35eb194aa` ("Implement Labs Frequency for NCD", 2022-12-05) set the once-only rule; its issue may state whether pregnancy was meant to be re-asked. Read it first.
- **Fix shape (guess).** Either re-expect the pregnancy test while a positive result is recent (within ~9 months), or make `patientIsPregnant` consult the latest positive result within that window across `previousEncountersData`.
- ⬆ **Re-tiered 3 → 1 on 2026-09-03 (user rule):** a defect in how a diagnosis or its care pathway is determined is tier 1 regardless of how many patients hit it. Severity label unchanged — it describes blast radius, the tier describes priority.

- ✅ **Intent gate DISCHARGED.** `35eb194aa` → PR #560 → issue #535 ("NCD - Subsequent Visit - Labs Frequency"), whose table says **"Pregnancy Test - Initial Visit"**. The once-only rule was deliberate, so the entry's first fix guess (re-expect the test while a positive is recent) contradicted the written spec as it stood.
- ⭐ **The user reframed the item and it grew a second half.** Asked what happens to a woman whose test was NEGATIVE and who becomes pregnant later: `resolveLabTestDate` only expires a test that has NO result, so a negative result silences the task permanently — and the "Is this patient known to be pregnant" question lives INSIDE that task (`viewPregnancyTestForm` → `contentAndTasksLaboratoryTestKnownAsPositive`), so once the test has been performed there is no route left to record a pregnancy at all. Decision (user, 2026-09-09): **offer the pregnancy test at every encounter**, and **stop treating a positive as current 9 months after it was recorded**.
- **What shipped.** `patientIsPregnant` now takes `AssembledData` and delegates to a new `patientIsPregnantAtEncounter encounterDate encounterMeasurements assembled`, which reads the most recent encounter that ANSWERED the question (positive / known-as-positive / negative; not-indicated and indeterminate are skipped) and treats a positive as current only while `diffMonths < pregnancyTestValidityPeriod` (9). `expectLaboratoryTask TaskPregnancyTest` drops `initialTestRequired`; the now-unread `TaskPregnancyTest` row was removed from `generatePreviousLaboratoryTestsDatesDict`.
- ⚠ **The entry named 5 consumers; there are 7.** It missed `Pages/NCD/Utils.elm:657` (the "Hypertension and Pregnant" form header) and `Pages/NCD/ProgressReport/View.elm:539`. The report site is NOT B-224's — it picks the referral facility phrase (`FacilityANCServices` vs `FacilityHospital`), so fixing only the activity side would have made the report claim "not referred" against a correctly-written ANC referral. It is called per historical row, so it takes that row's own date and measurements.
- **Live sizing (2026-09-09, all four sites).** ihangane only — vhw / uvl / tip-somalia have ZERO `ncd_pregnancy_test` nodes. 749 live records: `not-indicated` 707, `no-equipment` 27, **`known-as-positive` 6**, `run-today` negative 4, `run-previously` negative 3, `broken-equipment` 1, **`run-today` positive 1**. Women found pregnant by the test: **1**, with a later NCD encounter: **0** — the positive half has never fired. ⭐ "Known as positive" is used SIX TIMES more often than a positive test result; only 8 of 749 records are performed tests, so the once-only suppression itself is rare.
- **Verified.** `elm make src/elm/Main.elm` Success 548 · elm-test 3,138 pass · elm-format · elm-review clean (elm-stuff cleared first). Discrimination run: `patientIsPregnantAtEncounter` reverted to current-encounter-only + `initialTestRequired` restored → **exactly 7 of the new tests fail**, the other 5 hold as guards.
- **Review round 1 (2026-09-09, `medium`).** Four findings, all minor, zero correctness findings against the fix; all four posted inline (review 5151485418) and answered. Fixed: the negative branch's dead date (`9dd293a79` — resolved once as `answerDate`, which also removed a repeated `Maybe.withDefault`). Recorded in the PR body rather than in code: historical progress-report rows are re-read under the new rule, so a row within 9 months of a positive test now shows the ANC wording and says "no referral recorded" where none was required — zero affected data. **Declined, with reasons on the threads:** (1) anchoring the window on `currentDate` rather than `encounter.startDate` — the codebase carries both idioms and this follows the closer sibling `updateChronicDiagnoses`; `currentDate` is in scope at none of the six consumers, so it would be **52 references** across the NCD pages for a gap that only opens on an encounter left open across a month boundary. (2) `filterPreviousEncountersDataToDate`'s strict `LT` hiding a same-day earlier encounter — `Pages/NCD/Participant/View.elm:124-128` makes two same-day encounters under one participant unreachable, and the strict compare is exactly `updateChronicDiagnoses`'s idiom.
- ✅ **E2E added (`4ffb3c418`)**, folded into the existing "Female, Stage 3 Hypertension" test rather than a new run: it already performs the pregnancy test with a NEGATIVE result — the live-exposed case — so part 2 backdates, starts a second encounter and asserts the **Pregnancy** lab tab is still offered. ⭐ **Partition measured**: excluded from both of `e2e_playwright_1`'s commands (path tokens `prenatal tuberculosis` → 0; the CI `--grep-invert` → 0, the title carries "NCD"), and `e2e_playwright_2`'s `ncd` path token matches the filename ⇒ exactly one job. Both measurements are path-independent, so the worktree's name did not poison them.
- 📌 **NOT covered by e2e: the carry-forward half.** `expectActivity NextSteps` requires `mandatoryActivitiesForNextStepsCompleted`, i.e. EVERY other activity done, so asserting the ANC-vs-hospital referral at encounter 2 costs a full second encounter on a job already at ~30 min. It has zero live exposure (1 positive test fleet-wide, 0 with a later encounter) and 15 unit tests. Add it only if the CI budget allows.
- ✅ **E2E DISCRIMINATION-VERIFIED 2026-09-09** (user granted the app). Main tree borrowed, branch checked out, gulp rebuilt. Pre-fix: the `expectLaboratoryTask` gate restored in the working tree → the served bundle carried `initialTestRequired` **×3** → the test **FAILED at the new assertion and nowhere else** (`element(s) not found`, spec line 272); the whole first encounter, sync, backdate and second-encounter start all ran. Fix restored → bundle back to `initialTestRequired` **×2** → **1 passed (1.1m)**. Main tree returned to `develop`.
- ⭐ **Cheap served-bundle check for this class:** count `initialTestRequired` occurrences in the served `Main.js` (3 pre-fix / 2 post-fix). Dev-mode Elm keeps let-binding names, so a call site added or removed is countable without reading code.
- ⚠ **The main tree was detached to `4b9948d18` by a PARALLEL SESSION seconds after I returned it** (that session is working B-319, issue #2223 / PR #2224, and borrowed the app for its own e2e). Its working tree is that session's; this record was written from a throwaway worktree on `origin/develop` instead. ⛔ Do not `git checkout` the main tree back while another session holds it — read the reflog first, it names who moved it.
- **Review round 2 (2026-09-09, `medium`, whole branch).** Three findings, all posted (review 5153264230; the third file-level, its line being outside the diff) and answered. ⭐ **One was a REGRESSION THIS PR INTRODUCED, on the dominant live path** — fixed in `74eb886ba`.
  - **MEDIUM.** The three-way answer (`positive` / `negative` / skip) discarded an explicit "not known to be pregnant". `toPregnancyTestValue` (`Measurement/Utils.elm:3279-3288`) stores only note/date/result — there is **no `knownAsPositive` field** — and every why-not note is reachable ONLY through the section that renders when `form.knownAsPositive == Just False` (`viewPregnancyTestForm`, `:4573-4581`). So **any note other than `TestNoteKnownAsPositive` IS the nurse answering "not pregnant"**, and skipping it inherited a stale positive for up to 9 months (Methyldopa-only list, ANC referral, health education suppressed). ⛔ **This PR made it routine, not rare:** `laboratoryTaskCompleted TaskPregnancyTest` is `(not <| taskExpected …) || isJust measurements.pregnancyTest` and the task is now always expected for a fertile woman, so EVERY completed encounter carries a saved measurement — and 707 of 749 live records are `not-indicated`. Fix: the three-way became a two-way; only an encounter with **no** pregnancy test at all falls back, which is the mid-encounter window before Laboratory completes plus historical report rows.
  - **LOW (fixed, same commit).** Answers are now sorted by their own date before `List.head`, so a back-dated `TestNoteRunPreviously` answer cannot outrank a genuinely more recent one.
  - **LOW (PR body).** `pregnancy = expectLaboratoryTask currentDate assembled TaskPregnancyTest` gates `LabResultsHistoryPregnancy` via `showIf displayConfig.pregnancy`, so the progress report's pregnancy row now shows at every encounter instead of vanishing after the first performed test — intended, and now stated in the description.
  - Round 1's two declined findings were correctly not re-raised.
- **Verified after round 2.** elm make Success 548 · elm-test **3,140** pass · elm-format · elm-review clean. Discrimination: reverted to round-1 semantics (three-way restored, sort removed) → **exactly the 4 new/changed tests fail**.
- ⛔⛔ **Durable lesson: making a task MANDATORY changes what "no answer" means.** The round-1 design read an absent-or-inconclusive measurement as "this encounter says nothing"; the same PR's other half made the measurement compulsory, so the "says nothing" branch became the common case and silently outranked fresh clinical answers. **When a change makes a form compulsory, re-ask what every stored shape of that form now means.**
- **Review round 3 (2026-09-09, `medium`, whole branch).** Three findings, posted (review 5153497225) and answered. ⭐ **Its MEDIUM CONTRADICTED round 2's MEDIUM** — round 2 said a not-performed test still carries the nurse's "not known to be pregnant" answer; round 3 said a note recording only an INABILITY to test proves nothing and now expires a carried positive, offering **Captopril/Lisinopril (ACE inhibitors, teratogenic) to a pregnant woman**. User chose the synthesis (option B) over either; fixed in `9770ff21b`.
  - ⭐ **What settled it: the stored shape carries the distinction.** The why-not options (`Measurement/Utils.elm:6703-6711`) are `LackOfReagents`, `LackOfOtherSupplies`, `BrokenEquipment`, `NoEquipment`, `NotIndicated` — the first four are supply failures, `NotIndicated` is a clinical judgment ("Not Indicated" / Kinyarwanda *Ikizamini nticyasabwe*, the test was not required). Final rule: **positive** = `KnownAsPositive` or a positive result; **answer of not-pregnant** = a negative result or `TestNoteNotIndicated`; **no answer, earlier answer stands** = the four inability notes and `TestIndeterminate`. `NotIndicated` stays an answer against the reviewer's suggestion — it is 707 of 749 live records, and silencing it would reinstate round 2's stale-positive path; the inability notes are 28.
  - ⛔ **The tie-breaker for ambiguous cases is HARM ASYMMETRY, not likelihood:** dropping a true pregnancy offers a teratogen; carrying a stale positive gives Methyldopa and a wasted ANC referral. Skip when unsure.
  - **LOW, recorded not fixed.** `generatePreviousEncountersData` keeps only encounters whose measurements are `Just (Success …)` while `generateAssembledData` guarantees that only for the CURRENT encounter, so a fresh page load has a window where a prior positive is invisible. Real, and this PR widens it to the pregnancy pathway — but the same exposure already governs `diagnosedPreviouslyAnyOf` and every cross-encounter read here; closing it means gating page render on all measurements loading, which belongs to that whole class.
  - **LOW, fixed same commit.** Every fixture used `executionDate = Nothing`, so the `Maybe.withDefault startDate value.executionDate` fallback was barely covered. Two expiry-boundary cases now use `withPregnancyTestOn`. ⭐ Verified by MUTATION: replacing the fallback with plain `startDate` leaves the suite green except "a positive dated 9 months back has expired when recorded today".
- **Verified after round 3.** elm make Success 548 · elm-test **3,143** pass · elm-format · elm-review clean. Two discrimination runs: reverting to round-2 semantics fails exactly the two new skip tests; mutating the date fallback fails exactly the new expiry test.
- ⛔⛔ **Durable lesson (three rounds, two reversals on ONE predicate): a three-way "yes / no / no answer" over stored enum values is where this class of bug lives.** Round 1 skipped too much, round 2 skipped nothing, round 3 found the middle. **Enumerate every stored variant and say out loud what each one PROVES** — the answer is in the form that writes it, and the labels (not the type names) carry the semantics.
