<!-- id:B-283 status:READY tier:4 round:26 -->
### B-283 — NCDA questionnaire (Child Scoreboard, Nutrition, Well Child): after a reload, switching a parent question to "No" keeps the saved dependent sign, and the scorecard rows read the dependent sign alone — READY · CONFIRMED (handler, lambdas, hydration, consumers verified) · LOW-MED · tier 4 · R26 unit 8 · B-274 shape
- **Mechanism.** `SetNCDABoolInput` (`Pages/ChildScoreboard/Activity/Update.elm:129-132`) runs `formUpdateFunc value model.ncdaData.form` — the raw form — while the two ANC handlers just above it use the resolved `ncdaForm` built in the same `let` (`:43-51`). Three update lambdas in the shared questionnaire clear a dependent field: `Measurement/View.elm:2661` (`supplementsDuringPregnancy … takenSupplementsPerGuidance = Nothing`), `:2739` (`ongeraMNP … takingOngeraMNP = Nothing`), `:2868` (`childReceivesFBF … childTakingFBF = Nothing`). With a fresh page model (`childScoreboardActivityPages` is in-memory; `App/Update.elm:791-796` falls back to `emptyModel`), clearing `Nothing` does nothing and `ncdaFormWithDefault` (`Measurement/Utils.elm:8719, 8730, 8732`, all `or form.x (member Sign value.signs)`) restores the saved dependent sign; `toNCDAValue` (`:8822, :8832`) writes it back. The dependent question is rendered only when the parent is `Just True`, so nothing on screen shows it.
- **Consumers.** `Pages/WellChild/ProgressReport/View.elm:1928-1936` (iron row: ANY questionnaire with `TakenSupplementsPerGuidance` → ✓ for all nine months), `:2326` (FBF row from `ChildTakingFBF`), `:2596` (Ongera row from `TakingOngeraMNP`) — none checks the parent sign.
- **Siblings (same handler, not traced past the anchor).** `Pages/Nutrition/Activity/Update.elm:422-425` and `Pages/WellChild/Activity/Update.elm:1569-1572` apply the same lambdas to the raw form.
- **Reachability.** Save NCDA → reload / re-login → reopen the completed NCDA card → flip a parent to No → Save. Both sites. Rare.
- **Fix shape (guess).** Apply `formUpdateFunc` to the resolved `ncdaForm`, as `SetUpdateANCVisits` already does; the same one-liner in the two siblings.
