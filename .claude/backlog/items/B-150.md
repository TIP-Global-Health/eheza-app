<!-- id:B-150 status:IMPLEMENTED tier:3 round:18 -->
### B-150 — ✅ IMPLEMENTED 2026-08-09 (issue #2058, PR #2059, branch `b150-person-edit-wipes-gps-coordinates`, base develop @220a6b1e2, commit caa52be0a) — all three parts in ONE PR per user instruction ("Implement 1, 2 and 3 at same PR")
**Pending live check #4 is ANSWERED (2026-08-09, terminus):** `hedley_admin_feature_gps_coordinates_enabled` is **1 on vhw and tip-somalia**, **UNSET (=off) on ihangane**; `eheza-site` could NOT be read — its drush has no `vget` command ("Command \"vget\" is not defined"), so that site's flag state is still unknown (worth a different probe if it ever matters).
**The wipe is not theoretical — it already happened.** vhw.live: 18,331 persons, 10,599 with coordinates; 10,676 distinct entity_ids in `field_revision_field_latitude` vs 10,599 in `field_data_field_latitude` → **77 still-existing person nodes lost their coordinates**, and **76 of the 77 have `changed > created`** (edited after creation = the mechanism). tip-somalia: flag on but only 1 coordinate record, so nothing at risk there.
**What shipped (3 parts):** (1) **backend guard** in `HedleyRestfulSync.class.php` — for `person` + `method == 'PATCH'`, null/empty `latitude`/`longitude` are appended to the existing per-type `$ignored` list (the same idiom that already drops `save_gps_location` a few lines above). ⚠ Deliberately PATCH-only and null-only: filtering unconditionally would break the legitimate "tick the box while editing" update, and filtering POST would stop registration recording coordinates at all. **This part is the one that matters first — offline devices run whatever build they last received, so a client-only fix leaves un-updated devices erasing coordinates for months.** (2) **client** `Backend/Update.elm` PatchPerson — carries stored coords from `model.people` (`Dict PersonId (WebData Person)`, in scope there) instead of the form's `Nothing`. Put at the patch site, NOT in the form: `validatePerson` reporting Nothing is defensible (no coord inputs exist), treating that as "clear the field" is not. (3) **repair script** `hedley_person/scripts/restore-wiped-gps-coordinates.php` — restores from `field_revision_field_latitude`, taking lat+long from the SAME revision, skipping+reporting a person with only one.
**Verification (all by running):** script proven end-to-end on the local copy — record coords → wipe via `node_save` with emptied fields → dry run reports and writes nothing → real run restores the exact values → re-run finds nothing (idempotent); test person returned to no-coords afterwards. The script's SELECT run against vhw.live returns exactly **77**. php -l clean; phpcs Drupal+DrupalPractice exit 0 with a discrimination test proving the standards were loaded (bad file → exit 2, 12 findings); `elm make` 548 modules; elm-format clean; elm-review = 1 pre-existing unused import in the touched file, identical on develop.
- ⚠ Anchors had DRIFTED from the original filing: re-located to `Form.elm:452-453` (unchanged), `Backend/Update.elm:4632` (was filed as 4628-4641/:10454), `Encoder.elm:43-44` (unchanged). Public REST field names are `latitude`/`longitude` (`HedleyRestfulPeople` derives them via `str_replace('field_', '', ...)`).
- STILL OPEN from this entry: the **stale-fix sibling** (geolocation error sends no reply, `model.coordinates` not cleared per request) — deliberately left out of #2059.

### B-150 (original entry) — Editing a person WIPES previously saved GPS coordinates (locally + backend) — READY · CONFIRMED (agent 6-hop trace; load-bearing hops self-verified 2026-08-04) · MED (destroys the data the gps feature exists to collect; ⚠ check flag state on live before prioritizing)
- `validatePerson` builds edit output with `succeed Nothing` ×2 for registrationLatitude/Longitude (`Backend/Person/Form.elm:452-453`, verified — form has no lat/long inputs); edit submit → `PatchPerson` (`Pages/Person/Update.elm:324`); coords re-attached ONLY `if gpsCoordinatesEnabled && person.saveGPSLocation` (checkbox defaults unchecked) (`Backend/Update.elm:4628-4641`/:10454); `sw.patchFull` wipes local IDB immediately; full encoder sends `"latitude": null` (`Backend/Person/Encoder.elm:43-44`, verified `maybe string`); backend keeps the null (`HedleyRestfulSync.class.php:485-494`, only save_gps_location is ignored) and restful PATCH `array_key_exists` → `set(NULL)` — field emptied.
- One ordinary edit (name fix) silently destroys household coordinates; re-checking the box instead overwrites with the device's CURRENT position. NOT affected: WhatsApp-dialog + child-address-propagation patches (DB-loaded persons, coords ride along).
- No server consumer reads the fields yet (grepped) — impact is destruction of collected-for-the-future data. **Pending live check #4:** `hedley_admin_feature_gps_coordinates_enabled` state per site (weigh-usage rule). Fix shape: hydrate the form's coords from the existing person on edit (or exclude the two fields from the PATCH when no new fix exists).
- Sibling (same audit, separate small fix — record here): **stale-fix display** — geolocation error/timeout sends NO reply to Elm (`app.js:533-551` error → `() => {}`) and `model.coordinates` isn't cleared on request (`App/Update.elm:1511/:1523`), so a previous household's fix displays as current and is saveable onto the next person; `maximumAge: 600000` compounds. Fix: clear on request + reply-on-error with a tagged failure.
