<!-- id:B-110 status:WONTFIX tier:3 round:16 -->
### B-110 — Native `./install -d` demo import silently yields 0 people/HCs/villages — ❌ WON'T-FIX 2026-08-03 (user: *"the project is only meant to be installed with DDEV"*)
- The mechanism below is real, but the native install path is not supported, so a defect only reachable through it is not worth fixing. Do NOT re-pitch. If anyone ever wants this closed properly, the honest options are documenting DDEV-only in the README or removing the native branch from `helper-functions.sh` — not adding the missing CSV copy step to a path nobody should use.
- `csv/` has only `person_<site>.csv` etc.; bare `person.csv` is created transiently by the DDEV post-start hook (`.ddev/config.local.yaml.example:31-33`) and REMOVED at :51-53; native path (`helper-functions.sh:246-268`) has no copy step. Missing CSV → `MigrateSourceCSV::performRewind` (`csv.inc:392-407/:456-462`) prints an error, yields 0 rows, migration marked COMPLETE → dependents run with NULL sourceMigration lookups → empty person/HC references. Fix: add the `cp …_$EHEZA_SITE.csv` (default rwanda) into `import_demo_content` before drush mi.
