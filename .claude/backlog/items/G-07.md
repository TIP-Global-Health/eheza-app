<!-- id:G-07 status:READY tier: round:16 -->
### G-07 — Alphabetical-ordering RATCHET (CI script) — READY · S · WEAK-BUT-USEFUL
- ⚠ SURPRISE: the convention is ALREADY violated at HEAD — `Backend/Model.elm` Revision (:588): BreastExam<BirthPlan inversion, AcuteIllnessEncounter after AcuteIllnessNutrition, ChildScoreboard* order; `SyncManager/Model.elm` BackendAuthorityEntity (:63): same inversions; `Translate.elm` TranslationId (:327): AcuteFindings*<ActiveDiagnosis. A STRICT guard fails immediately; a one-time reorder is a merge-conflict bomb (out of scope under the small-changes constraint). Published rules don't cover declaration order (SiriusStarr/elm-review-no-unsorted 1.1.8 sorts cases to DECLARATION order — which is itself wrong here). Deliverable: `ci-scripts/check_alpha_ratchet.py` — for Revision/BackendAuthorityEntity/TranslationId, count adjacent case-insensitive inversions vs a committed baseline; fail only on INCREASE (suggest lowering on decrease). NOT recommended: adopting NoUnsortedCases globally.
- Side-note for B-081's doc PR: CLAUDE.md "four parallel CI jobs" is also stale (there are 7: + lint_elm_review, test_zscore, test_elm) — bundle the correction.
