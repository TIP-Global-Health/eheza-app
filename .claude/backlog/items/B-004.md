<!-- id:B-004 status:IMPLEMENTED tier:3 round: -->
### B-004 — Stock Management signed-form leak across navigation (R3-2/R8-4 class) — ✅ IMPLEMENTED 2026-07-21 (PR #1973, issue #1969)
- Shipped: `Reset` msg → `emptyModel` in the page Update, dispatched from a guarded `UserPage StockManagementPage` arm; plus the 4×-repeated entry guard extracted to a named `onEntry`. Fixes the IN-APP navigation path only.
- ⚠ **Known remaining gap, split to #1972:** `UrlChanged` assigns `activePage` and runs NO entry messages, so browser/device Back still restores the stale signed form — and the same gap affects the pre-existing CreatePerson/EditPerson/Relationship resets. A fix WAS built (call `entryMsgs` from `UrlChanged`, echo-guarded) and then **reverted after review**: the client has ~no `<a href>` nav, so it amounted to "Back/Forward wipes forms" app-wide, destroying in-progress work (incl. drawn signatures, attached photos) and turning #1970 into an ordinary-navigation bug. Do NOT re-attempt naively — see #1972 for the suggested narrow direction (clear signature + confirmIdentity only).
- Spawned issues: #1970 (relationship form unsavable when session uncached, PLAUSIBLE), #1971 (abandoned signatures orphan `photos-upload` cache blobs).
- User decision 2026-07-21: whole-model reset KEPT despite it also discarding partly-filled unsigned forms — matches the CreatePersonPage precedent (#1837) and save-success behavior in the same file.

Original entry:
- CONFIRMED: `stockManagementPage` is a global singleton (App/Model.elm:302/:360), reset ONLY inside validated save (`SaveReceiveStock` :292 / `SaveCorrectEntry` :457 emptyModel), page's own SetActivePage (:36-40) is a passthrough, and App/Update.elm SetActivePage extraMsgs (:1150-1229) has NO StockManagementPage arm → a signed-but-abandoned form (signature ImageUrl + confirmIdentity + quantity + dates + displayMode sub-page) persists indefinitely across navigation; re-entry renders the saved signature IMG (View.elm:810-821), one click from saving stale data with fresh dateMeasured. Same stale-form class as #1838/#1873.
- ❌ REFUTED headline: "nurse B saves with nurse A's signature" is impossible in-model — every nurse change passes `SetLoggedIn` (App/Update.elm:1376-1387) which rebuilds LoggedInModel via `emptyLoggedInModel` (resets the page); `UpdateNurseData` is same-nurse-guarded. Severity: stale-form/data-hygiene (MED-LOW), NOT an audit hole. (Same-login human handoff is out of scope.)
- Fix (agent-traced): add `Reset` msg to Pages/StockManagement/Model.elm Msg (alphabetical), handle as `( emptyModel, Cmd.none, [] )` in its Update, add guarded arm in App/Update.elm extraMsgs near :1203 copying the CreatePersonPage template (`if model.activePage == page then [] else … MsgPageStockManagement`, no id arg). Whole-model reset matches save-success behavior.
