<!-- id:B-334 status:READY tier:4 round:29 -->
### B-334 — Admin sidebar: the "Create PMTCT Participation" link points at `node/add/pmtct-paticipation`, a path that does not exist (the type is `pmtct_participant`), so administrators get a 404 — READY · CONFIRMED (menu export, content-type export, block builder read) · LOW · tier 4 · R29 unit 5
- **Mechanism.** `hedley_admin.features.menu_links.inc:271-275` exports `link_path = 'node/add/pmtct-paticipation'`; the content type is `pmtct_participant` (`hedley_schedule.features.inc:102`), so core registers `node/add/pmtct-participant`. `hedley_admin_block_view` (`:182-191`) renders the menu with `l()` straight from `menu_links`, and the block is placed with `status 1` in `sidebar_first` (`hedley.install:174-181`).
- **Fix shape (guess).** Correct `link_path` in the export or drop the link (the type is creatable from `admin/content`).
