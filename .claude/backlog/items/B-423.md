<!-- id:B-423 status:READY tier:4 round:35 -->
### B-423 — Stock management Receive Stock: an erased batch number still counts as filled — READY · CONFIRMED (orchestrator read handler and count) · LOW · tier 4 · R35 unit 1
- **Mechanism.**
  ```elm
  -- client/src/elm/Pages/StockManagement/Update.elm:172-178
          SetBatchNumber value ->
              ...
                  updatedForm =
                      { form | batchNumber = Just value }
  -- client/src/elm/Pages/StockManagement/View.elm:629
                            , maybeToBoolTask form.batchNumber
  ```
  Typing then erasing leaves `Just ""`: the task counts done, Save is enabled and an empty batch number is stored. Prenatal's text handlers map empty to `Nothing` (`Prenatal/Activity/Update.elm:401,440`). The widget is a free-text input, so the declined "widget not traced" class does not apply.
- **Live:** one stock_update exists on all four sites (tip-somalia).
- **Fix shape (first guess):** map an empty string to `Nothing` in `SetBatchNumber`.
