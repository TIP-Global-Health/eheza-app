<!-- id:B-194 status:IMPLEMENTED tier:3 round:21 -->
### B-194 — Prenatal: EDD-null pregnancies are ACTIVE FOREVER, and years-old open encounters get resurrected for new pregnancies — ✅ IMPLEMENTED (issue #2133, **PR #2134** `b194-edd-null-pregnancy-expiry`, base develop @c5888f169) · CONFIRMED (anchor orchestrator-verified verbatim; counts reproduced) · tier 3 · ongoing · R21 wave B
- Anchor: `Pages/Prenatal/Participant/Utils.elm` `isPregnancyActive` — active unless (endDate AND outcome), else `session.eddDate |> Maybe.map (... < 92) |> Maybe.withDefault True` — **EDD NULL ⇒ active forever** (verified verbatim; the docstring says so). `Pages/Prenatal/Participant/View.elm` `viewActions` reuses the still-open encounter and offers NO way to start a fresh pregnancy while one is "active".
- Data (reproduced 2026-08-19): **4,603** antenatal participants with EDD NULL; **2,979 unconcluded and >1 year old** (traps). Smoking gun: 66 LMP measurements dated AFTER their encounter's scheduled date (36 by >1yr, 21 created since 2025 — ongoing). Worked sample: encounter 1170209 scheduled 2021-08-24, nurse recorded an LMP 2025-09-16 into it; participant 1170208 now shows pregnancy start 2021, EDD 2026-06-23.
- Consequence: a real new pregnancy lives on a years-old participant — progress-report timeline, ANC visit counts, `field_expected` start, every per-encounter EGA = garbage; NCDA + reports consume these.
- Fix direction: consumer tolerance — fallback expiry for EDD-null pregnancies (e.g. startDate + ~10 months) in `isPregnancyActive`, and don't resurrect open encounters whose scheduled date is far past.
- ✅ **IMPLEMENTED 2026-08-23** — `isPregnancyActive` now falls back to `lmpToEDDDate session.startDate` when `eddDate` is `Nothing`, so an undated pregnancy expires 280 + 92 = 372 days after registration. Reuses the existing helper and `pregnancyDurationInDays`; no new constant. 7 tests added to `Pages/Prenatal/Test.elm` on the existing `TestFixtures.testParticipant`. Discrimination-tested: against pre-fix code exactly the two expiry cases (372 days, 5 years) fail and the grace-period/concluded pins pass. `elm make src/elm/Main.elm` 548 modules, elm-test 2993/0, elm-review clean on the committed state in a fresh clone.
- 📌 **Live counts re-measured on ihangane.live 2026-08-23** (deployed code carries the identical function — the defect is live): 38,585 antenatal participants, 4,614 with EDD null, **3,458** of those unconcluded and registered over a year ago. ⭐ **Exactly ONE EDD-null participant in the whole database has an LMP measurement on any encounter** — EDD-set and LMP-recorded coincide 33,971/33,971. So the trap pool is entirely UNDATED SHELLS that no existing mechanism can ever clear (the nightly `populate-edd.php` needs an LMP), and the fallback had to key on `startDate`. It also explains why the entry's 61-66 LMP-into-a-stale-encounter cases are absent from the trap set: recording the LMP sets EDD, which discharges the trap — the 3,458 are the UNSPRUNG ones.
- ⛔ **Deliberately NOT in the PR:** (a) repair of the 3,458 existing shells — forward-only fix, a concluding sweep is a separate backfill shaped like B-120; (b) the nurse-side resumption of an open encounter inside a still-active pregnancy. `viewActionsForNurse` gates resumption on `isJust encounter.endDate` alone while `viewActionsForChw` also requires `encounter.startDate == currentDate`; matching the CHW rule would stop a nurse finishing yesterday's encounter and orphan it open, which is a workflow decision. Recorded as a minor below.
- 📝 **Review 2026-08-24 (`/code-review medium`), 2 low findings, both verified before acting.** (1) *Outcome button lost on expiry* — REAL: `viewActions` derives both `maybeSessionId` and `completedEncounts` from `activePregnancyData`, so once a pregnancy expires "Record Pregnancy Outcome" is permanently disabled, and the only other route (`Pages/Prenatal/Encounter/View.elm:398`, from a postpartum encounter) is reachable only through the same page. ✅ **Accepted as the trade, measured first: only 39 of the 3,458 traps have a completed encounter**, i.e. 39 have a working button today; the other 3,419 already had it disabled. Rewiring the outcome section to the most recent unconcluded pregnancy would add a second "which pregnancy" concept for 39 records a backfill must conclude anyway. Sub-claim softened: PatientRecord already rendered `--/--/----` in both date columns for these (start date derives from EDD) — only the status label flips Ongoing→Resolved. (2) *Under-expiry for late registrations* — observation correct and deliberate, but the proposed remedy is BACKWARDS: the latest encounter's `startDate` is ≥ the registration date, so anchoring there expires LATER, widening the window it means to close. No tighter anchor exists (no LMP, no EDD, no ultrasound on these records). Actioned via the reviewer's fallback: the rationale is now in the doc comment (commit `8929405f8`), no logic change.
