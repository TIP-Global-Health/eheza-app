<!-- id:B-065 status:PARKED tier:1 round:14 -->
### B-065 — Editing a person's address cascades onto their PARENT/CAREGIVER (not just children), and fires on every edit (not just address changes) → cross-patient demographic corruption — 🅿 PARKED by user 2026-07-06 ("app has been working for several years, no complaints") — do NOT implement without explicit instruction. Elaboration notes from 2026-07-06 (kept for if ever revived): correct domain vocabulary is mother/caregiver (no grandparent relation); the damaging path is a CHILD edit pushing the child's address up onto mother+caregiver; the code comment's premise ("we do not allow children to edit address") is FALSE — address section is gated only on isChw (View.elm:1162), included unconditionally (:1465).
- `Pages/Person/Update.elm:273-320` `generateMsgsForPersonEdit` → `updateChildrenMsgs` maps `Dict.get personId db.relationshipsByPerson |> RemoteData.map (Dict.values >> List.map .relatedTo)` with NO `relatedBy` filter, then PATCHes each with the editor's province/district/sector/cell/village. `toMyRelationship` (`Backend/Relationship/Utils.elm`): `.relatedTo` is the CHILD for MyChild/MyCaregiven (downward, intended) but the PARENT/CAREGIVER for MyParent/MyCaregiver (upward). So editing person M who is a child of grandmother G (M has a MyParent→G entry) overwrites G's address with M's. `fetchFamilyMembers` pre-loads all related persons into db.people + `PatchPerson`→patchFull persists → reachable.
- Secondary: the guard is `List.any (.value >> isJust)` (address POPULATED) not "changed" — contradicts its own comment ("at least one address field was changed"); edit forms pre-populate address so the cascade fires on EVERY save (e.g. editing only a phone number re-pushes the address onto relations).
- Fix: filter `Dict.values` to `MyChild`/`MyCaregiven` before `.relatedTo`; gate on actual change vs the stored person, not `isJust`. Severity MED (silent cross-patient address corruption; needs edited person to have an upward relationship + differing addresses). Same demographic-integrity family as the fixed create/edit-person leaks but a distinct write path.
