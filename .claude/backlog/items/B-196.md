<!-- id:B-196 status:STALE tier:3 round:21 -->
### B-196 — Prenatal: participant EDD goes STALE when LMP is edited/re-dated — the B-140 dropped-patch mechanism has a live data trail (~44 stale + 8 lost-LMP), and develop still drops the patch — READY · CONFIRMED (deployed discard verified silent; develop = Rollbar + still discards) · tier 3 · ongoing (latest 2026-03-11) · R21 wave B
- Anchors: `Pages/Prenatal/Activity/Update.elm` `SavePregnancyDating` writes LMP + participant-EDD as a NON-ATOMIC pair over two entities; `Backend/IndividualEncounterParticipant/Update.elm` `updateIndividualEncounterParticipant` DISCARDS the patch when the participant isn't cached (deployed v1.18.0: silent `unwrap (model, Cmd.none, [])`; develop: reports to Rollbar but still discards). `hedley_prenatal/scripts/populate-edd.php` fills only NULL EDDs — never repairs stale ones.
- Data: 95 participants whose EDD ≠ any current LMP+280d; 67 explained by ultrasound EDD (TOLERATED — invariant refined); remainder = **20 matching an old LMP revision + 24 matching an older LMP node while a newer disagrees (~44 stale, up to ~2 months) + 8 where the LMP write was lost + 1 EDD-with-no-LMP**.
- Consequence: wrong auto-expiry (EDD+92) and wrong `edd_date` into `hedley_ncda` → gestational-age-at-birth / preterm classification on the scorecard.
- Fix direction: server-side recompute of participant EDD in a node-save hook on `last_menstrual_period` (ultrasound EDD wins when present) — closes the cache-race class permanently AND is the repair shape.
