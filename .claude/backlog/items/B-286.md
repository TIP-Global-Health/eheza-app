<!-- id:B-286 status:IMPLEMENTED tier:1 round:26 issue:2215 pr:2216 -->
### B-286 — NCD hypertension staging compares against the encounter's OWN just-written stage: a Stage-3 patient with one low reading is stepped down twice in one visit, and a mistyped BP cannot be corrected to a lower stage — ✅ IMPLEMENTED 2026-09-08 (issue #2215, PR #2216, branch `B-286-ncd-staging-own-diagnoses`, base develop @f1453f897) · CONFIRMED (generation, re-run trigger and PATCH→revision path verified) · MED · tier 1 · R26 unit 5 · ⭐ DEPLOYED (since `929a3f43b`, 2022-09-14; identical on `origin/main`)
- **Mechanism.** `generateNCDDiagnoses` (`Pages/NCD/Utils.elm:104-109`) → `applyHypertensionDiagnosesLogic` (`:112-157`) takes the "current condition" from `resolveCurrentHypertensionCondition` (`:320-328`), which puts `{ diagnoses = assembled.encounter.diagnoses, … } :: assembled.previousEncountersData` through `resolveHypertensionCondition` — so the encounter being re-assessed is the head of the list and its own previously written stage is what the new stage is compared against. `generateNCDAssessmentMsgs` (`Backend/Update.elm:9324-9346`) — whose comment says *"At this stage new diagnoses were not updated yet, therefore, we can use the diagnoses set for the encounter"* — re-runs on EVERY Vitals, CoMorbidities, RandomBloodSugar, UrineDipstick and Creatinine revision (`:3562-3654`, `processRevisionAndAssessNCD`), and `SetNCDDiagnoses` (`Backend/NCDEncounter/Update.elm:29-30`) replaces the set via `sw.patchFull` (`:309-320`), whose local write comes straight back as an `NCDEncounterRevision` (`:6465-6473`), so `db.ncdEncounters` already carries the new set before the nurse's next save.
- **(a) Double step-down.** History `{Stage3}`, today's vitals 95/70. Vitals save → today matches no stage (`hypertension = []`), `lowerHypertensionStageCondition` (`:277-279`, `sys < 100`) true, current = Stage3 from history → writes `{Stage2}` (`:124-142`). The next Random Blood Sugar save (expected at every encounter, `Activity/Utils.elm:876-877`; even "not performed" is a revision) re-runs the same branch with current = the encounter's OWN Stage2 → writes `{Stage1}`. A third trigger (urine dipstick; creatinine at the recurrent phase) stays at Stage1. `Pages/NCD/Test.elm:355-388` pins "steps down by one" (Stage3 + low → Stage2), but every fixture has `encounter.diagnoses = EverySet.empty` (the file's own comment at `:141-147`), so the second pass is never exercised.
- **(b) Ratchet on correction.** First encounter, nurse types 185/85 (meant 145/95) → `{Stage3}`. Corrects to 145/95 → `hypertension = [Stage1]`, current = own Stage3, `1 > 3` false → `current :: others` (`:145-157`) → stays Stage3. Only a correction to a normal reading (sys ≥ 100 and below Stage 1) clears it. Same shape, smaller: `matchNCDDiagnosis DiagnosisDiabetesRecurrent` (`:219-221`) short-circuits on `diagnosed DiagnosisDiabetesInitial assembled` — the stale set — so unticking Diabetes in co-morbidities hides a sugar-count diabetes for one pass.
- **Reachability.** Any nurse NCD encounter. (a) needs a prior Stage 2/3 and a systolic < 100 today followed by any lab save — the normal Examination → Laboratory order. (b) needs a BP re-edit while still hypertensive.
- **Impact.** The stage written to the encounter is what `resolveHypertensionCondition` reads at every later encounter (chronic — ratchets up, or one step down on sys < 100), what `medicateForHypertension`/`referForHypertension` gate on (`:586-628`, `:939-949`: Stage3 → mandatory hospital referral; "choose two medications" vs one, `:672-676`), and what the progress report prints. (a) silently demotes a Stage-3 patient to Stage 1 for good; (b) leaves a Stage-3 referral requirement the nurse cannot remove. Both sites.
- **Fix shape (guess).** Make generation a pure function of measurements + history: in `applyHypertensionDiagnosesLogic` use `resolveHypertensionCondition assembled.previousEncountersData` instead of `resolveCurrentHypertensionCondition assembled` (and test the co-morbidities VALUE, not the stale set, in the `DiagnosisDiabetesRecurrent` guard). ⚠ Check every other caller of `resolveCurrentHypertensionCondition` first — `medicateForHypertension` and `referForHypertension` legitimately want the stage INCLUDING today's, once it is final. Pin with a `Test.elm` case that seeds `encounter.diagnoses`.
- **Live sizing (`ihangane.live`, 2026-08-30) — why tier 3, not 2.** Diagnoses written to NCD encounters: Stage1 3,379 · Stage2 1,392 · Stage3 559 · diabetes-initial 152 · diabetes-recurrent 62 · renal 30 · none 2,457. Of 7,587 NCD vitals, 65 have sys < 100 — and **none of those 65 belongs to a patient with a prior Stage 2/3 encounter**, so the double step-down (a) has not yet fired on Rwanda data. The ratchet (b) cannot be sized from data (a corrected typo leaves no trace). Deployed, mechanically certain, unexercised so far; promote to tier 2 if the user weighs (b) alone as enough.
- ✅ **IMPLEMENTED 2026-09-08 — issue #2215, PR #2216, branch `B-286-ncd-staging-own-diagnoses`, base `develop` (`f1453f897`), commit `178189968`.**
  - **Two lines in `Pages/NCD/Utils.elm`.** `applyHypertensionDiagnosesLogic` now takes the current condition from `resolveHypertensionCondition assembled.previousEncountersData`; `matchNCDDiagnosis DiagnosisDiabetesRecurrent` guards on `matchNCDDiagnosis assembled DiagnosisDiabetesInitial` instead of `diagnosed … assembled`.
  - ⭐ **The intent question was answered by the blame, not by reasoning.** `git log -S` put the diabetes guard on `7c38c3a35` (2022-10-19), whose message links issue #405 comment 1265102335 point 4. That commit's real fix was un-nesting `byUrineGlucose` from inside the `randomBloodSugarTest` `Maybe.map`; in the same edit it replaced `(not <| matchNCDDiagnosis currentDate assembled DiagnosisDiabetesInitial)` with `if diagnosed DiagnosisDiabetesInitial assembled`. **The fix restores the subject the code itself used before, keeping the un-nesting** — so it is not the "revert the offending commit" shape the [[read-the-issue-for-requirements]] rule warns about.
  - **The other five callers of `resolveCurrentHypertensionCondition` were read and deliberately left alone** (`medicateForHypertension` :590, the medication-form header :663, `referForHypertension` :943, `referForDiabetes` :968, `referForRenalComplications` :981). Every one is a consumption site that legitimately wants the stage *including* today's, once it is final. Only generation was wrong.
  - **Test fixture axis added.** Every fixture in `Pages/NCD/Test.elm` had `encounter.diagnoses = EverySet.empty` (the file said so itself), which is exactly why `hypertensionHierarchyTest` passed against the defect. New `assembledWithHistory` takes the encounter's own written set; `expectDiagnosesWithHistory` now delegates to it rather than repeating the record. 7 new cases in `reassessmentTest`.
  - **Discrimination-verified:** with only `Utils.elm` stashed back to develop, elm-test is **3114 passed / 3 failed** — the double step-down, the 185→145 correction, and the diabetes untick. The other four new cases (escalation and persistence under re-assessment) pass on both, which is what makes them guards.
  - **Verified:** `elm-test "src/elm/**/Test.elm"` 3117 passed; `elm make src/elm/Main.elm` Success 548 modules; `elm-format --validate` clean; `elm-review` clean on cleared `elm-stuff`.
  - **E2E extended, not added** — `client/e2e/ncd-encounter-nurse.spec.ts`'s "NCD Subsequent Encounter" case now records 180/110 (Stage 3) at the first encounter and 95/70 at the second, and asserts `field_ncd_diagnoses` on each. Fixed code writes `hypertension-stage2`; the defect writes `hypertension-stage1`, because `completeLaboratory`'s random blood sugar save is the second trigger. ⚠ **Stage 3 history is mandatory for discrimination** — with Stage 2 history both code paths land on Stage 1 (the Stage-1 arm is a fixed point) and the test would prove nothing.
  - **New `queryNCDDiagnoses` in `client/e2e/helpers/common.ts`**, extracted with `queryPrenatalDiagnoses` onto a shared `queryEncounterDiagnoses(label, personName, encounterType, diagnosesField, options)` rather than copied ([[code-duplication-rule]]).
  - **CI partition unchanged** — no new spec file and no new describe block; the file still matches job 2's `ncd` token and the title still carries "NCD" so job 1's `--grep-invert` excludes it. ⚠ Not measured locally: the worktree is named `B-286-ncd-staging-own-diagnoses`, and `playwright test <token>` matches the **absolute** path, so a measurement from here would report every spec as matching `ncd` ([[e2e-ci-partition-trap]]).
  - 📌 **E2E not yet run.** It borrows the single-instance app ([[e2e-local-run-procedure]]); the user was asked.
- ⬆ **Re-tiered 3 → 1 on 2026-09-03 (user rule):** a defect in how a diagnosis or its care pathway is determined is tier 1 regardless of how many patients hit it. Severity label unchanged — it describes blast radius, the tier describes priority.
