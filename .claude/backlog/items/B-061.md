<!-- id:B-061 status:READY tier:4 round:13 -->
### B-061 — Stock summary folds subtraction corrections into "Received" → negative/net "Received" inconsistent with the detail view's "Issued" — READY · CONFIRMED · LOW
- `Backend/StockUpdate/Utils.elm:243-246` `received = List.map .quantity stockUpdates |> List.sum` includes `UpdateCorrection` entries (subtractions stored as `-1*quantity`, `Pages/StockManagement/Update.elm:438-439`); `issued` (:248-250) sums only distributions. So a month with a −30 correction shows "Received: −30" / "MTD In: −30" (View.elm:228/276/288) while the month-DETAILS view (:352-357) renders the same correction under "Issued". Balance is CORRECT in both (`starting + received − issued` reconciles because the correction rides in `received`) — only the Received/Issued labelling diverges between the two screens.
- Fix: in the summary, route negative corrections into `issued` (mirror the detail view), keep the balance formula intact. Severity LOW (display/labelling; totals right).
