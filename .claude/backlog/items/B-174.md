<!-- id:B-174 status:IMPLEMENTED tier:3 round:20 -->
### B-174 — ✅ IMPLEMENTED 2026-08-20 (issue #2121, PR #2122) — Dashboard stats: `date_create(NULL)` fabricates TODAY for missing dates — every resolved acute illness "concluded" the day stats were computed, and missing birth dates read as "born today" — READY · CONFIRMED (orchestrator-verified verbatim at both anchors; comment-vs-code defect) · **(a) fires unconditionally on every resolved illness, all sites** · tier 3 · R20 wave 2
- (a) **date_concluded**: `hedley_stats.module:~955` emits `!empty($row->field_expected_field_expected_value2) ? date_format(date_create($row->field_date_concluded), 'Y-m-d') : NULL` — and the comment DIRECTLY ABOVE explains that `field_date_concluded` "is not set when illness is resolved", which is why the expected-end date should be used. The code checks value2 in the guard, then formats the NEVER-SET field: `date_create(NULL)` = now, so every concluded illness carries the stats-generation date as its conclusion date. Client `Backend/Dashboard/Decoder.elm:257` `required "date_concluded" (nullable decodeYYYYMMDD)` parses it happily → resolutions bucket into whatever month the cache was rebuilt. Fix: format `$row->field_expected_field_expected_value2` (the value the guard already checks); NULL is also safe here (decoder nullable).
- (b) **birth_date**: same block `:950` + siblings `:1298` (prenatal), `:1581` (SPV), `:1780` (NCD), `:2010` (individual nutrition) — `date_format(date_create($row->field_birth_date_value), 'Y-m-d')` over a LEFT-JOINed possibly-NULL column → missing birth date becomes TODAY, age bucketed as 0. Client `:255` `required "birth_date" decodeYYYYMMDD` is **non-nullable**, so ⚠ the fix must SKIP the row (like the `:2514` group-stats guard) — emitting NULL would convert mangle into stats-batch POISON. Live (ihangane 2026-08-17): 387 persons lack birth_date; 1 acute-illness + 1 nutrition participant reference such persons, so (b) fires today on those two. vhw clean. tip-somalia (2026-08-18): 0 birthdateless persons — clean.
- 📌 **Implementation note 2026-08-20 — a guard I added was redundant (user caught it).** I feared that skipping a birthdateless participant would let the encounter loops auto-vivify `$items[$participant]` as an entry holding only `encounters`, and added `!isset($items[...])` at all 6 assignment sites. **Wrong:** every one of those loops ALREADY opens with `if (empty($items[$row->field_individual_participant])) { continue; }` (comment: this failure once wedged health-centre sync). `empty()` on a missing key is TRUE without notice or auto-vivification, and `!isset($x)` is a strict subset of `empty($x)` — the added guard could never fire. Removed; PR #2122 is the date fix + 5 birthdate skips only. ⭐ Lesson: before adding a guard, read the whole loop body — the protection may already be there, and the existing comment usually says why.
- Live confirmation (ihangane 2026-08-20): 23,639 acute-illness participants, **ALL** with `field_date_concluded` empty; 4,346 carry an expected end date ⇒ 4,346 resolved illnesses currently report the stats-run date.
- Same-family latents in `hedley_ncda` (recorded, not queued): `:728` `anc_visits_dates` items + `:973-979` `administration_dates` format stored dates without the `!empty` guard their siblings got in the #2088 family (`edd_date` `:694-696`, `scheduled_date` `:714-717`); live = 0 empty values in both field tables today; consumer is the scoreboard front-end (external repo, client strictness unverified).
