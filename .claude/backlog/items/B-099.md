<!-- id:B-099 status:READY tier:3 round:16 -->
### B-099 — `hedley_ncda/scripts/generate-data-for-all.php` keyset pagination WITHOUT `propertyOrderBy('nid')` — backfill can silently skip children — ✅ IMPLEMENTED (issue #1975, PR #1977, MERGED; widened to 12 scripts, remaining 8 tracked in #1976) · LOW-MED · one-line house-pattern fix
- `:32-36/:56-62/:90` — `nid > $last` + `range(0,50)` + `$nid = end($ids)` with NO order-by; the exclude_deleted tag adds a LEFT JOIN (`hedley_general.module:360-366`) so MariaDB may return any order → `end($ids)` can jump past unprocessed nids → those persons never get field_ncda_data. EVERY sibling batch script uses `->propertyOrderBy('nid')` (hedley_person/migrate-*, bump-revisions, graduate-participants…); only this one omits it. Live tool (backfills, new-site NCDA enablement). Fix: add the orderBy to `$base_query`.
