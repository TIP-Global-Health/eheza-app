<!-- id:B-032 status:IMPLEMENTED tier:2 round:10 -->
### B-032 — hedley_stats dashboard queries join measurement values with NO deleted filter (module has zero `field_data_field_deleted` references) — ✅ IMPLEMENTED 2026-07-07 (issue #1908, PR #1909, branch `b032-stats-deleted-filter`, base develop @502d1cd73, CI running, Copilot requested). Fix (gate honored — ON clause + shared helper): added `hedley_stats_exclude_deleted_from_join($on, $entity)` near get_base_query; wraps the 6 measurement-encounter FAN joins (ai_encounter :978, prenatal_encounter :1156, ncd_encounter :1305, well_child_encounter :1585, child_scoreboard_encounter :1777, nutrition_encounter :2008) with `AND NOT EXISTS (SELECT 1 FROM {field_data_field_deleted} hsd WHERE hsd.entity_id=<fan>.entity_id AND hsd.field_deleted_value=1)`. Filtering each fan covers all its downstream value joins incl. the `clone $base_query` helper queries (zscore_age/administration_dates/test_result/test_execution_note). SCOPE (verified by reading all 7 builders): encounter-level fields on node.nid (prenatal_diagnoses/ncd_diagnoses/encounter_warnings) already base-filtered; nutrition-GROUP + case-mgmt + family-planning + attendance query the measurement bundle directly (node=measurement, base-filtered); person fields (birth_date/gender) + group-education participants = B-033 (person-side). Verify: php -l + phpcs Drupal+DrupalPractice CLEAN; SQL-level discrimination on real field tables in a rolled-back txn (OLD pulls deleted muac=11, NEW muac=NULL + live nutrition_signs=edema retained + encounter row survives all-deleted); NEW SimpleTest scenario 5 in HedleyStatsCalculation (soft-delete muac under live nutrition encounter → recalc → deleted gone, encounter+live sign retained). Prefix `{field_data_field_deleted}` resolves in ON string (select.inc:1602 appends condition verbatim → prefixTables str_replace at execute). CI fully green incl. test_simpletest_linux (new scenario ran end-to-end in fresh install). COPILOT round addressed (f6bd23e90): parenthesized `($on_condition)` for future OR-precedence safety + guarded `extractFirstStatsEncounter` empty-$data (reset→FALSE); both defensive, no live bug (all 6 callers pass simple equality; test never passes empty data). After that push, e2e_playwright_1 flaked (lab-tech-encounter.spec `page.goto` interrupted by app redirect to #pincode — unrelated to PHP; prior commit green, simpletest passed); empty-commit retrigger (5b578aaac) → all 10 green. FINAL: CI green, Copilot addressed, awaiting user merge.
- Primary node IS filtered (`hedley_stats_get_base_query:3484` → excluding-deleted builder) but every measurement-side leftJoin chain is raw: e.g. AI case-mgmt `:978` `leftJoin('field_data_field_acute_illness_encounter','ai_encounter', … = node.nid)` then value joins on `ai_encounter.entity_id` (send_to_hc :981, fever_period, …); same pattern for prenatal/NCD/SPV/child-scoreboard/nutrition builders. `grep -c field_data_field_deleted hedley_stats.module` = 0.
- Reachability: VBO action `hedley_admin_mark_as_deleted_action` (`hedley_admin.module:13-56`, `'triggers'=>['any']`, works on any bundle with field_deleted) lets admins soft-delete an individual measurement while its encounter stays live; `delete-duplicate-measurements.php` does the same in bulk. Stats recalc re-runs the SAME unfiltered query → deleted values re-enter every rebuild, forever.
- Fix: shared helper to fold `(del.field_deleted_value IS NULL OR = 0)` into the measurement-side JOIN (must be in ON clause to preserve LEFT JOIN), applied across the ~7 stats data builders. Severity MED (deleted/erroneous measurements keep feeding dashboards — precisely the values admins removed).
