<!-- id:B-422 status:IMPLEMENTED tier:1 round:35 -->
### B-422 — HIV program Diagnostics: correcting a saved Positive result to "test not run" still stores Positive, and the participant is never closed — ✅ IMPLEMENTED 2026-09-23 (issue #2282, PR #2283) · CONFIRMED (scout run-verified through the real `Pages.HIV.Activity.Update.update`, with a control; orchestrator read hydration, both clearing lambdas, the dialog handler and the close check) · LOW-MED · tier 1 (care-pathway rule) · R35 unit 2 · DEPLOYED, latent (0 live records carry a result)
- **Mechanism.** Hydration hard-codes the dirty flag — the only field in the function that does not copy `form.xDirty`:
  ```elm
  -- client/src/elm/Pages/HIV/Activity/Utils.elm:328-332
                  , testResult =
                      maybeValueConsideringIsDirtyField form.testResultDirty
                          form.testResult
                          value.testResult
                  , testResultDirty = False
  ```
  The update hydrates before every handler and stores the result (`Update.elm:42-43`, `SetEndEncounterDialogState` `:166-174`). So a clear (`testResult = Nothing, testResultDirty = True`, View.elm:299-300 and :404-405) survives one message; the next stores `dirty = False` and every later hydration refills the saved result.
- **Path.** When `runHIVTest == Just False` the Save button is `SetEndEncounterDialogState True Nothing` (View.elm:212-218) — always one extra handler. On confirm, `SaveDiagnostics` rebuilds the value with the refilled Positive, and its close check reads the refilled form:
  ```elm
  -- client/src/elm/Pages/HIV/Activity/Update.elm:216-219
                                          if
                                              not positiveResultRecorded
                                                  && (diagnosticsForm.resultPositive == Just False)
                                                  && (diagnosticsForm.testResult /= Just TestPositive)
  ```
  → `CloseHIVSession HIVOutcomeNotDiagnosed` never fires; stored `signs = {}` + `testResult = Positive`; the nurse returns to the encounter page, and Medication/Next Steps stay expected.
- **Scout run:** saved `{HIVTestRun, date, Positive}` → View's `runHIVTest` lambda False → `SetEndEncounterDialogState True Nothing` → `SaveDiagnostics`: stored `Just TestPositive` (expected `Nothing`), 2 app messages (expected 3). Control without the dialog step: `Nothing`, 3 messages.
- ⚠ **Refutes `archive/rounds-1-9-ledger.md:144`**, which called clearing-to-Nothing "not UI-reachable". Two View lambdas do it.
- **Live (orchestrator, 2026-09-21):** `hiv_diagnostics` ihangane 5, vhw 25, uvl 0 — **none carries a test result**, so no stored instance. A correction path; rare.
- **Fix shape (first guess):** `testResultDirty = form.testResultDirty`, matching every sibling field and TB's copy.
- ✅ **IMPLEMENTED 2026-09-23 — issue #2282, PR #2283, branch `B-422-hiv-diagnostics-withdrawn-result`, base `develop`.** Built with the three pits from #2275's review checked first: (a) raw vs merged form — not present, HIV `update` already merges before every handler; (b) repeat tap — WOULD have bitten once the flag survives, so `setResultPositive` / `setRunHIVTest` (top-level in Utils, replacing the View lambdas) leave the form alone on a repeated answer; (c) data left behind — `toDiagnosticsValue` saves `testResult` only when `runHIVTest == Just True`. Plus the hydration flag (`testResultDirty = form.testResultDirty`) and the save now builds from the merged `diagnosticsForm` and the close check reads `value.testResult` (one reading; covers old records with a result but no run). ⚠ The entry's "TB's copy" is wrong: TB diagnostics has no test result. First HIV test file `Pages/HIV/Activity/Test.elm`, 7 cases through the real `update` and message sequence (answer → end-encounter dialog → save); each of the 4 changes stubbed alone fails ≥1 case. Hydration alone was NOT pinned by the entry's scenario (the save rule masks it) — pinned by "No then Yes asks for the result again". Full elm-test 3,239, elm make, elm-review (fresh elm-stuff; one REAL unused import in the new test, fixed), comment check green. Worktree released. **Review not yet requested.**
- ✅ **CI fully green on `721c5cb41` (all 11 checks incl. simpletest + 3 e2e), 2026-09-23.** Review running.
- **Review round 1 (medium, 2026-09-23, on `721c5cb41`): no findings.** One gap noted, not a bug: `positiveResultDate` is not tied to the test run the way `testResult` now is; both setters clear it and no saved record is known to carry a stale date. Nothing posted to the PR. **Awaiting merge.**
