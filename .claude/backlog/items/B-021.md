<!-- id:B-021 status:READY tier:4 round: -->
### B-021 — Unbatched soft-delete cascade can time out (CORRECTED: availability only, atomic rollback) — READY · CONFIRMED-mechanism, downgrade to LOW-MED (verification sweep 2026-07-06)
- Cascade real: `hedley_person.module:1210-1219` hook → :1296-1342 synchronous recursive node_saves (person→participants→encounters→measurements), each full-price (stats/reports/ncda hooks + AQ enqueues). Triggers: VBO mark-as-deleted (skip_confirmation=1), backoffice edit form; drush is timeout-immune; Elm client NEVER sets deleted=true (grep-verified).
- ❌ REFUTED "half-deleted tree": whole cascade runs inside the initiating node_save's db_transaction (nested saves = savepoints, same connection) → mid-tree PHP kill rolls back EVERYTHING atomically, incl. AQ enqueues. Safely retryable, zero residue.
- ❌ REFUTED "high-volume patient": presave guard (:1159-1202) blocks person-level delete when person has measurements (`field_deleted->set(FALSE)` + error message) → person cascades only for measurement-less persons (small trees).
- Residual CONFIRMED issue: individual_participant / encounter / group-session deletes (no size guard) with years of encounters×measurements = hundreds of node_saves → web request killed (~120s) EVERY attempt → the delete is simply impossible via web UI (works via drush). Availability/operability, not integrity. Fix if wanted: Batch API chunking from VBO/form paths, or size-threshold guard like :1159-1202. (R4-2 restore-cascade still PARKED — do not fold in.)
