<!-- id:B-170 status:CLOSED tier:3 round:19 -->
### B-170 — Download-lane POISON RECORD: `obstetric_history_step2` missing `obstetric_history` fails the client decoder — ✅ CLOSED 2026-08-13, ALREADY FIXED AND DEPLOYED (no code item remains; evidence folded into [[B-168]]) · Rollbar item #53, 140 occ / 114 IPs / 25 devices over 2 years, latest 23 days ago · was HIGH-MED
- Rollbar #53 "Http.BadPayload: Problem with the value at json.data.batch[N]" — worked-example payload (occurrence #465099846413, device "Kagogo Village CHW Pascasie June 27th 2025", Chrome 148, 2026-07-21): node uuid `8a3b4762-7039-5b90-bbb5-612c5ad63bce` nid 5582007 vid 10688414, type `obstetric_history_step2`, carries `obstetric_history_step2: ["none"]`, `previous_delivery: ["none"]`, `c_sections: "-1"` etc. — but **NO `obstetric_history` key** — decoder fails: "Expecting an OBJECT with a field named `obstetric_history`".
- ⛔ **The empty-list hypothesis is only HALF the story — verified in code 2026-08-13.** It explains the PRE-2026-07 occurrences (a server `multiFields` entry is omitted when the field is empty, and the old client `required` it). The CURRENT mechanism is a deliberate coordinated removal: **client `1dac72459` (2026-05-18)** dropped `required "obstetric_history"` from `decodeObstetricHistoryStep2`, **server `f620da93c` (same day)** dropped `field_obstetric_history` from the step2 REST `multiFields`, and **both deployed 2026-07-07** (`e52219e15`, client build `efbff4534`). The deployed server now NEVER sends that key for step2.
- **⇒ Nothing to fix IN THE CODE.** A current client is tolerant; develop and live are both correct on both sides. ✅ **Open question SETTLED 2026-08-17 (R20 wave 1) — the stale-build inference is now a PROOF, by error-string impossibility rather than by build version:** `git grep 'required "obstetric_history"' origin/main -- client/src/elm/` = **zero hits** (the only references are `optional`, Decoder.elm:137-138/:2424), and `optional` never demands a key — so the deployed build CANNOT emit "Expecting an OBJECT with a field named `obstetric_history`". Every post-2026-07-07 occurrence is a pre-18-May build by construction; the "current client failing a decode it should tolerate" alternative is REFUTED. Corroborating: the trickle STOPPED — latest occurrence 2026-07-21 (occ #465099846413, device "Kagogo Village CHW Pascasie", one of the known July jam list), silent for ~4 weeks as of 2026-08-17. ⭐ **The planned check itself was impossible, durable fact: Rollbar `client.javascript.code_version` is hardcoded `'1.0'` in EVERY deployed build** — the dynamic `code_version: data.version` wiring (app.js:1766) arrived in `05a5cc74b` (2026-07-26, develop-only, NOT in main), so Rollbar occurrences will only start carrying real build shas after the next release. Until then, discriminate builds by which code could produce the message, not by code_version. (Sync telemetry URLs carry `db_version=32&base_revision=…` — schema version only, also not a build id.) ⚠ **The July deploy WIDENED the exposure for any lagging device**: a device on a pre-18-May build now fails on EVERY step2 record it downloads, not just ones with empty step-1 signs. Matches the timeline — latest occurrence ~2026-07-21, after the deploy.
- ✅ **Wholesale-batch claim CONFIRMED in code:** `SyncManager/Decoder.elm:432` and `:445` decode as `required "batch" (list <| decodeBackendAuthorityEntity …)`; Elm's `list` is all-or-nothing, so one undecodable record kills the whole batch and the lane retries the same `base_revision` → permanent jam for that device.
- ⛔ **Near-miss, new durable rule:** the local `server/.pantheon-ihangane` **WORKING TREE was checked out at 2026-05-11** and still lists `field_obstetric_history` — reading it gives the WRONG answer. The deployed truth is `origin/master` in that checkout (`git show origin/master:<path>`). The read-the-deployed-code rule needs a second clause: **confirm the deployed checkout is itself current.**
- 🅿 **Generalized sweep DECLINED by user 2026-08-13: "No sweep." Do not re-pitch.** (Scoped for the record: 123 REST plugins declare 148 distinct `multiFields`, omitted when empty, against 166 `required` fields in `Backend/Measurement/Decoder.elm` alone — every pairing is a latent poison record of the B-108 class. An audit, not a diff.)
- 📌 Unexamined leftover from the payload: `c_sections: "-1"` sentinel.
- Impact: the download BATCH containing such a record fails decode WHOLESALE; the lane retries from the same base_revision → **downloads for that shard jam until data/code changes** (worked-example device errored at base_revision 10687990 against poison vid 10688414). 114 distinct IPs have hit this over 2 years; ongoing (latest 23 days ago). Level `debug` in Rollbar — easy to miss.
- Fix candidates (small, after verifying which side): client decoder `optional`-with-default for `obstetric_history` (the B-108 rule — nullable-tolerant decoders for synced data); or server: emit empty list instead of omitting. Plus a live SWEEP: count obstetric_history_step2 rows with no step-1 signs (the standing poison population), and grep the decoders for OTHER `required` fields a server can omit when empty — this is a CLASS, not one record.
