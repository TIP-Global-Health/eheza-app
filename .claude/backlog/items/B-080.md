<!-- id:B-080 status:READY tier:4 round:16 -->
### B-080 — `hedley_device_assign_shards` education_session edit branch leaves `$shards` undefined (PHP notice) — READY · CONFIRMED · trivial
- `hedley_device.module:307-316` sets `$shards` only when `empty($node->original)`; clinic case pre-inits `$shards = [];` at :251. Fix: mirror the init.
