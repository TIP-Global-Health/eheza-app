<!-- id:B-302 status:READY tier:4 round:27 -->
### B-302 — Stock Management (CHW village): the computed data cache is keyed by health center but holds one village's figures — a CHW covering two villages can be shown the previous village's stock — READY · CONFIRMED-mechanism · LATENT (live: **0** multi-village CHW accounts on tip-somalia, 2026-09-01) · LOW · tier 4 · R27 unit 5
- **Mechanism.** `villageStockManagementData` is keyed by `HealthCenterId` alone but computed for one `fetchVillageId` (`Backend/Update.elm:1265-1290`); `SetVillage` resets only `dashboardPage` (`App/Update.elm:1281-1314`). A CHW with ≥2 villages under one HC who switches village and re-enters within 5 minutes gets the cached `Success` for the old village. Self-heals on the next sync marking or the forget.
- **Fix shape (guess).** Key by (HC, village), or invalidate in `SetVillage`.
