<!-- id:G-01 status:READY tier: round:16 -->
### G-01 — Photo-encoder dispatch guard (CI script) — READY · S · SOLID
- `SyncManager/Encoder.elm:82-176`: `replacePhotoWithFileId` dispatch lists 7 photo-bearing types (Person :112 avatar-conditional, Photo :128, NutritionPhoto :134, PrenatalPhoto :140, WellChildPhoto :146, StockUpdate signature :152, FamilyNutritionPhoto :158) then a `_ ->` WILDCARD at :164 that defeats exhaustiveness — a new photo-bearing type compiles and silently uploads a broken photo link (incident-backed class, see [[photo-upload-fileid-encoder-wiring]]). Ground truth: `ImageUrl` fields in `Backend/Measurement/Model.elm` :97/:366/:912/:2366/:3257/:3279 + `Person.avatarUrl` (`Backend/Person/Model.elm:16`, seed exception). Currently 7/7 match. Deliverable: `ci-scripts/check_photo_dispatch.py` — scan Model.elm for ImageUrl-bearing aliases → assert each has an explicit case before the wildcard.
