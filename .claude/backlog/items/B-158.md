<!-- id:B-158 status:READY tier:2 round:19 -->
### B-158 — 🅿 **PARKED (user, 2026-08-11)** — presented as the next Tier 2 item and declined without a stated reason; ⛔ do NOT re-pitch. The mechanism stands (below) and nothing about it was refuted, so if storage-pressure data loss ever surfaces in the field, this is the first place to look. — was READY · CONFIRMED (chain self-verified verbatim + agent node-harness run) · MED (HIGH mechanism, rare trigger) · sync-fidelity · R19 Unit 2
- `client/src/js/nodes.js:388-404` (postNode; :298-314 patchNode identical): `changeTable.add(change).catch(→ retry .catch(→ resolve(400)).then(→ resolve(200))).then(→ resolve(200))` — the inner `.then` fires on the RESOLVED 400 and unconditionally replaces it with the 200 `response`; the 400 is dead code on every path. Self-verified by reading the verbatim chain; agent proved by running the extracted chain in node (double-failure → `{"status":200}`). The comment "we log error with Rollbar" is FALSE — no logging call exists on either path.
- Trigger: `changeTable.add` failing twice inside the committed transaction — realistic cause QuotaExceededError under storage pressure, which this codebase treats as field-real (app.js:668-675 storage-full BulkPut failures observed live; the whole `lastSaveError` machinery exists for it). Because the error is CAUGHT, Dexie does not abort the transaction → the entity write to nodes/shards COMMITS while the change row does not.
- Impact: Elm gets 200 + entity; record visible in UI, no nodeChanges/shardChanges row → **never uploaded, forever** — silent clinical-data divergence invisible to sync counters (they count change rows). Does NOT self-heal (unlike B-148's lanes).
- Fix shape (small): `changeTable.add(change).catch(() => changeTable.add(change)).then(() => response, () => reject400)` + route the error into the existing dbErrors/logByRollbar mechanism (make the comment true). Same fix ×2 sites. NOT the declined sync-poison class — this is a caught-error path that lies, not back-off hardening.
