<!-- id:B-014 status:READY tier:4 round: -->
### B-014 — Remaining UTC residuals (R2-4 class, grouped) — READY · CONFIRMED a+b, ❌ c REFUTED (verification sweep 2026-07-06, self-verified)
- (a) CONFIRMED: `app.js:1550-1555` `date_measured: today.toISOString().split('T')[0]` (UTC) on the WhatsApp screenshot record → off-by-one 00:00–01:59 CAT / 00:00–02:59 EAT. Fix: format from local components or pass Elm's zone-aware currentDate through the port (pattern: `b393e68d0`/#1832, `fromLocalDateTime zone`).
- (b) CONFIRMED: `Pages/PinCode/View.elm` resilience/wellbeing reminders — `resolveTomorrow8AmUTC` (:502-505, `Time.Extra.ceiling Day Time.utc` IS zone-dependent) → daily "drink water" reminder fires 10:00 local instead of 08:00; `:383 Time.toHour Time.utc >= 10` → "take a break" eligibility 12:00 local instead of 10:00. Fix: thread the `zone` already available in the same view (:42, used at :209).
- (c) ❌ REFUTED: `Pages/MessagingCenter/Update.elm:266/:305` + `Pages/PinCode/Update.elm:45` — justinmimbs/time-extra `add Hour/Second` IGNORES the zone param (pure ms arithmetic) → "in X hours" reminders correct in every TZ; Time.utc there is cosmetic. Readability cleanup at most — do not count as a defect.
- Group (a)+(b) into one LOW PR.
