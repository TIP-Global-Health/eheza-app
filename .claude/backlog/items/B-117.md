<!-- id:B-117 status:IMPLEMENTED tier:2 round: -->
### B-117 — Patient merge never re-points acute_illness_trace_contact.field_referred_person — ✅ IMPLEMENTED 2026-08-03 (issue #2025, PR #2026, with B-115)
- Fix: `_consolidate_patient_trace_contact_referrals()`. No shard handling: trace-contact shards derive from `field_person`, which the measurement sweep already re-points. **0 rows on live — the bundle IS a measurement type, so its field_person IS re-pointed; only field_referred_person dangles)
- The one case where the merge SEES the node (it is a measurement) but re-points only `field_person`, leaving the second person reference behind.
