<!-- id:B-290 status:IMPLEMENTED tier:3 round:26 -->
### B-290 — NCD Social History: the drinks-per-week answer is never saved — the encoder writes the cigarettes count into the `beverages_per_week` key — ✅ IMPLEMENTED · CONFIRMED (encoder line, introducing commit, live signature verified) · MED · tier 3 · R26 unit 7 · ⭐ DEPLOYED since the release carrying `c9610431b` (2024-01-18); identical on `origin/main:4204`
- **Mechanism.** `Backend/Measurement/Encoder.elm:4204-4205`: `++ encodeNullable "beverages_per_week" value.cigarettesPerWeek int` / `++ encodeNullable "cigarettes_per_week" value.cigarettesPerWeek int`. The model has two fields (`Model.elm:2854-2855`), the decoder reads them separately, the server stores them separately (`HedleyRestfulNCDSocialHistory.class.php:17-19`, `field_beverages_per_week` / `field_cigarettes_per_week`, integer, cardinality 1). Only the encoder crosses them. `git show c9610431b` ("Introduce and make use of encodeNullable utils") shows the pre-refactor code encoded `value.beveragesPerWeek` — a copy-paste slip in the refactor.
- **Reachability.** NCD encounter → Social History: `Pages/NCD/Activity/Utils.elm:609-627` renders the per-week number input for alcohol, `:490-496` builds the value with `beveragesPerWeek = form.beveragesPerWeek`, `SaveSocialHistory` → this encoder → `/api/sync` upload. Re-opening the form backfills `beveragesPerWeek` from the SAVED value (`:463`), so the nurse sees the cigarette count (or nothing) under the alcohol question.
- **Live signature (`ihangane.live`, 2026-08-30).** `ncd_social_history`: 4,544 nodes; **2,458 created since 2024-01-18**. Among all nodes only 4 have the two counts differing, the last on **2023-09-13** — four months before the refactor, and none since; 235 have beverages without cigarettes and 7 the reverse (both impossible after the refactor, so all pre-refactor). The alcohol count of every drinker recorded in the last 19 months is gone from the server and unrecoverable (the device sent the wrong number; revisions hold the same wrong number).
- **Impact.** Both sites. No server-side reader of the field today beyond the REST plugin (grep of `hedley_*`), so the damage is the stored record and the edit form until a report picks it up.
- **Fix shape (guess).** `value.beveragesPerWeek` on line 4204 (one word), plus a round-trip test for `NCDSocialHistoryValue` — the shape B-230 established. No repair possible for the lost values.
- **Lost values, sized:** **88** social-history records since 2024-01-18 carry the `drink-alcohol` sign — those are the drinkers whose per-week count was replaced by the cigarette count (or null). `ihangane.live` only; `vhw` not queried.
- ✅ **IMPLEMENTED 2026-08-31 — issue #2173, PR #2174**, branch `B-290-social-history-beverages` off `d08e108ba`. One word on `Encoder.elm:4204`, exactly as the entry's fix shape said.
- 📏 **The entry's open sizing question is CLOSED: `vhw` and `uvl` are both ZERO** drinkers recorded since 2024-01-18 (`ncd_social_history` joined to `field_ncd_social_history_signs = 'drink-alcohol'`, `created >= 2024-01-18`). So the loss is **Rwanda-only and bounded at the 88 `ihangane` records** the entry already sized. `tip-somalia` not queried (NCD not in use there).
- ✅ **REGRESSION TEST ADDED, discrimination-proven.** Folded into `Backend/Measurement/Test.elm` beside `preeclampsiaInFamilyEncodeTest`, which is the exact precedent — encode a value, decode one field back out. ⚠ **The fixture must give the two counts DIFFERENT values (3 and 7); with equal counts the test passes against the bug and proves nothing** — that is the whole trap of a crossed-field defect, and it is stated in a comment above the test. Verified by restoring the buggy line and re-running: `beverages_per_week carries the drinks count` fails, the cigarettes case still passes (3031/1).
- 📌 **`encodeNCDSocialHistoryValue` had to be exposed** from `Backend.Measurement.Encoder` for the test, inserted in alphabetical position; `encodeMedicalHistoryValue` was exposed for the same reason when its test was written, so this follows the established precedent rather than setting one.
- **Verified.** `elm-format --validate` clean · `elm make src/elm/Main.elm` exit 0 · `elm-test` **3,032 passed / 0 failed** · `elm-review` *I found no errors!* on a cleared `elm-stuff`.

