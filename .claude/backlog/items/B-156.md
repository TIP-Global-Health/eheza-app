<!-- id:B-156 status:READY tier:4 round:19 -->
### B-156 — Fallback fileId 5002 (cache-evicted photo/screenshot unstick) is environment-coupled: the file exists ONLY on ihangane — READY · CONFIRMED (write path traced end-to-end; live-measured 2026-08-11) · LOW · tier 4
- Resolves the R18 constants-sweep pending live check #3. `file_managed` fid 5002: **ihangane = `image-file`** (application/octet-stream, status 1); **vhw + tip-somalia = NO row**; eheza-site = different application (D11), N/A.
- **Mechanism on a site without the file (traced, not run):** `app.js:821/:919` stamps `fileId: 5002` when a photo/screenshot blob is missing from Cache Storage; the entity change then uploads with `photo`/`screenshot` = 5002; `HedleyRestfulSync::handleChanges` passes the int through untouched (not a UUID → plain `$data[$key] = $value`), restful `propertyValuesPreprocessFile` (`RestfulEntityBase.php:969-976`) wraps it as `['fid' => 5002, 'display' => TRUE]`, and the wrapper set + programmatic save store the **DANGLING fid** — this is the same code path real just-uploaded fids ride daily, so **NO exception and NO upload wedge** (the sync try/catch at `HedleyRestfulSync.class.php:524` never fires). Read side LEFT JOINs `file_managed` (`HedleyRestfulPhotos:40`), so the photo is silently absent for other devices. Downstream adjacency: a vhw `whatsapp_record` carrying screenshot 5002 would hit `send-messages.php`'s unguarded `file_load` (B-152(c)).
- **Measured: the fallback has fired exactly 2× in product history** — `field_data_field_photo` fid=5002: ihangane **2**, vhw **0**, tip-somalia **0**; `field_data_field_screenshot`: **0** on all sites. Near-inert in practice; the weigh-usage rule caps this at a tiny fix.
- Fix shape (pick ONE, small): create the placeholder file row on the other sites (ops, no code); or resolve the placeholder per site at runtime (lookup by filename via a backend-exposed value, larger than it sounds — the client is offline at substitution time); or drop the entity-side substitution and mark the change row failed-permanent with telemetry. New sites will inherit the gap either way until the constant stops being a hardcoded fid.
