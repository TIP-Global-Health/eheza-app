<!-- id:B-192 status:READY tier:3 round:21 -->
### B-192 — Sync: hard-deleting an individual_participant silently DROPS its encounters from sync — 3 live orphans (one written POST-deploy), plus a latent empty-batch download stall shape — READY · CONFIRMED (all 3 hops orchestrator-verified + count re-run) · tier 3 · R21 wave A
- Data: 3 encounters reference deleted participants — `acute_illness_encounter` 2954118 (2023-05-29), `child_scoreboard_encounter` 5154121 (2025-09-23), ⚠ `well_child_encounter` 5586075 (**2026-07-08 — one day after the current deploy; the deletion path is CURRENT**, not legacy).
- Mechanism: `HedleyRestfulIndividualEncounter.class.php:90` joins the participant uuid with require=TRUE → `innerJoin` (`hedley_general.module:~477`) → the encounter row VANISHES from the download batch instead of failing decode. Client cursor: `SyncManager/Update.elm:~286-292` — on an empty batch `lastFetchedRevisionId` falls back to the CURRENT cursor (verified verbatim: `Maybe.withDefault currentZipper.lastFetchedRevisionId`), so if the dropped row is the NEWEST revision in a quiet HC's shard while `revisionCount > 0`, the device re-fetches an empty batch forever (self-heals on the shard's next write). All 3 current rows have 8k-405k newer in-shard revisions → NO live jam today; the 3 encounters simply never reach any client (their measurements arrive with a tolerated null encounter ref).
- All 16 inner-join drop sites enumerated in `scratchpad/r21-waveA-checks.md`; only this one has live violations.
- Fix direction: server — participant/person delete cascade should delete (or `deleted`-mark) the participant's encounters; data repair shape = delete/mark the 3 rows. Durable client hardening (bigger): advance the cursor on empty batches, which needs the server to return the max scanned vid. ⚠ Do NOT propose making the join optional — a null participant on the STRICT `required "individual_participant"` decoder would convert silent-drop into batch poison.
