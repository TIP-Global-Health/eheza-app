<!-- id:B-239 status:IMPLEMENTED tier:3 round:25 -->
### B-239 — Prenatal recurrent labs: the Partner HIV follow-up form has no Save button, so the answers can never be recorded and the encounter can never be ended — ✅ IMPLEMENTED · CONFIRMED (anchors verified; not reproduced in a browser) · MED-HIGH · tier 3 · R25 unit 2
- **Mechanism.** In `viewLabResultFollowUpsContent` (`Pages/Prenatal/RecurrentActivity/View.elm:~713-790`), `formHtmlAndTasks` renders three tasks — `TaskHIVTest`, `TaskSyphilisTest` and `TaskPartnerHIVTest` (:734-737) — but the `saveMsg` case below (:769-780) handles only `TaskHIVTest` and `TaskSyphilisTest` and drops `TaskPartnerHIVTest` into `_ -> Nothing`. `Maybe.map (\msg -> viewSaveAction …) saveMsg |> Maybe.withDefault emptyNode` then draws nothing. ⚠ Do not confuse this with the **other** `saveMsg` in the same file (:338-341), which belongs to the ordinary Lab Results content and *does* handle `TaskPartnerHIVTest` — that is the sibling that was updated. `git show 80fa884d4` added the follow-ups render arm and left this save arm untouched.
- **Reachability.** Partner HIV test ordered at the initial phase without an immediate result → lab tech enters `Positive` in the recurrent encounter → `toPartnerHIVResultValue` with `isLabTech = True` (`Measurement/Utils.elm:8193-8202`) writes `hivSigns = {PrenatalHIVSignPendingInput}` → `Backend/Update.elm:9128` adds `TestPartnerHIV` to `labsResults.testsWithFollowUp`. The nurse then finds the task hidden in Lab Results (`expectLaboratoryResultTask` needs `isLabTech || followUpWasNotScheduled`) and shown in Labs Results Follow Ups (`expectLaboratoryResultFollowUpsTask TaskPartnerHIVTest`, `RecurrentActivity/Utils.elm:322-329` = follow-up scheduled + result positive), rendering "Is the partner taking ARVs?" with no way to save it.
- **Impact.** `hivSigns` stays `{PrenatalHIVSignPendingInput}` forever, so `laboratoryResultFollowUpsTaskCompleted` (`Utils.elm:281-282`) is permanently False → the activity never completes → `pendingActivities` is never empty → `Pages/Prenatal/RecurrentEncounter/View.elm` only ever offers "Leave Encounter", never **End Encounter**, so `ConcludeEncounter` never runs and the pregnancy stays in the labs-review queue. Every reader of the partner signs (`resolveDiscordantPartnershipSet` `Pages/Prenatal/Utils.elm:1829-1834`; `partnerSurpressedViralLoadByPartnerHIVTest` :4523-4535; `ProgressReport/View.elm:1004`) sees nothing. All sites.
- **Fix shape (guess).** Add `TaskPartnerHIVTest -> SavePartnerHIVResult personId measurements.partnerHIVTest nextTask |> Just` to that `saveMsg` case. `SavePartnerHIVResult` already exists in the recurrent `Msg` and its `update` branch already stores and navigates, so the fix is one arm.
- ✅ **IMPLEMENTED 2026-08-30 — issue #2167, PR #2168**, branch `B-239-partner-hiv-followup-save` off `092a0506a`. One arm added to the follow-ups `saveMsg`, placed after `TaskSyphilisTest` so the save case reads in the same order as the render case above it in the same function. The entry's fix shape was right as written.
- **Verified at every layer the answer travels through, not just the missing arm.** `SavePartnerHIVResult` in `RecurrentActivity/Update.elm:931` stores via `saveMeasurementMsgs` and sequences `generateLabResultsMsgs nextTask`, so navigation is already handled. The nurse path through `toPartnerHIVResultValue` (`Measurement/Utils.elm:8181`) takes the `isLabTech = False` branch and builds `hivSigns` from the two form fields, replacing the lab tech's `{PrenatalHIVSignPendingInput}` — which is exactly what `laboratoryResultFollowUpsTaskCompleted` (`RecurrentActivity/Utils.elm:281-282`) tests. ⭐ **The "answered No" branch was the one worth checking and it is safe:** the viral-load question is only asked when `partnerTakingARV == Just True`, so a No leaves `partnerSurpressedViralLoad` at `Nothing` — but `ifNullableTrue` (`Pages/Utils.elm:1306-1309`) returns `Just EverySet.empty` for `Nothing`, never `Nothing`, so `Maybe.Extra.combine` always succeeds and `ifEverySetEmpty NoPrenatalHIVSign` yields `{NoPrenatalHIVSign}`. The task completes either way.
- 📖 **Spec trail read before building:** `git log -S` → `21e93154c` → PR #652 → issues #623/#624/#625 (the Partner HIV lab as a sub-activity, moved out of Partner Information); `80fa884d4` → PR #1353 → #1349. Neither contradicts the fix — the clinical questions are already implemented and unchanged here; what was missing was only the ability to save them.
- ⚠ **No regression test, and the site is not testable as written:** `saveMsg` is a `let` binding inside `viewLabResultFollowUpsContent`, which returns `List (Html Msg)`, and there is no `Pages/Prenatal/RecurrentActivity/Test.elm`. Same class as [[B-124]] — a task/save computation living inside a view function. Verified instead by `elm make src/elm/Main.elm` (Success, 548 modules), `elm-test` **3,030 passed / 0 failed**, `elm-review` *I found no errors!* on a cleared cache, and the layer-by-layer read above.
- 🔍 **Reviewed (medium) 2026-08-30 — no correctness findings.** It independently traced the same chain and reached the same conclusion on the "partner taking ARV = No" branch (`ifNullableTrue` → `Just EverySet.empty` → `{NoPrenatalHIVSign}`, task completes), and added one fact worth keeping: **`isLabTech` is necessarily `False` on this screen** because `getAllActivities` gives `LabsResultsFollowUps` to nurses only — so the pending-input branch of `toPartnerHIVResultValue` is unreachable here by construction, not just by the caller's argument. Its one note, posted inline: the new branch sits after `TaskSyphilisTest`, against the repo's alphabetical case-branch rule. Left as placed, deliberately — it matches the `formHtmlAndTasks` render block above it in the same function, and the other `saveMsg` in this file (`:340`) is not alphabetical either, so the save case and the render case now read in one order.

