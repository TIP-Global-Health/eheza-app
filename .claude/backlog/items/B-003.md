<!-- id:B-003 status:IMPLEMENTED tier:2 round: -->
### B-003 — Subsequent-encounter double-tap still duplicates encounters (R9-3 scope-out) — ✅ IMPLEMENTED 2026-07-13 (issue #1931, PR #1932, branch `b003-guard-inflight-creates`, base develop @8e19e166a, commit 9b8648644, Copilot requested, CI running). **User WIDENED the scope** (asked "should we guard any entity created on the device?"): guard now covers ALL 15 previously-unguarded creates in Backend/Update.elm — the 10 Post*Encounter + PostPerson (duplicate PATIENT registration — worst of the set) + PostRelationship + PostPmtctParticipant + PostSession + PostEducationSession — via one shared `Backend.Utils.isPostInFlight key dict` (Loading-only blocks), and #1881's 2 hand-rolled guards were refactored onto it. NEW test module `Backend/Utils/Test.elm` (6 tests); DISCRIMINATION: neutering the predicate to `False` fails exactly the 2 "blocks" tests, the 4 "allowed" tests still pass. elm make 548, elm-test 2806, elm-format + elm-review (COLD) clean.
- ⚠ MEASUREMENT SAVES DECLINED-FOR-NOW (do not re-pitch as a bug): the 169 `Save*` handlers in Backend/*/Update.elm each have a `saveX : WebData ()` slot so they're guardable, BUT their pages dispatch `SetActivePage` in the SAME update as the save (Pages/Nutrition/Activity/Update.elm:93 pattern) → duplicate window is ONE FRAME, not a round-trip, and many carry `valueId` → PATCH not create. Needs its own evidence before touching.
- 📌 Testability ceiling (state honestly in any future PR): the handlers' only observable difference on double-tap is the returned `Cmd`, and Cmds are OPAQUE in Elm → a handler-level unit test CANNOT distinguish 1 POST from 2. Test the predicate; the wiring rides on the compiler + diff review.
- `client/src/elm/Backend/Update.elm`: the 10 per-program `Post*Encounter` handlers (keyed by participant). #1881 guarded only the 2 participant-create handlers (`PostIndividualEncounterParticipant`/`PostFamilyEncounterParticipant`, keyed by person).
- Mechanism: double-tapping a subsequent-encounter button re-POSTs before `Loading` lands in views → duplicate encounter within an existing episode.
- Fix: same guard pattern as #1881 — `if Dict.get key model.postX |> Maybe.map RemoteData.isLoading |> withDefault False then noop` in each handler.
