<!-- id:B-078 status:READY tier:4 round:16 -->
### B-078 — NCDA scorecard: `continue` on empty Vitamin A skips the MUAC/stunting block in the same loop — READY · CONFIRMED-latent · S · masked-latent (independently re-found + fully traced by the R16 ncda audit)
- `hedley_ncda.module:803-807` `$items = field_get_items(…'field_receive_option'); if (!$items) { continue; }` with MUAC (:821-835) + stunting (:836-852) BELOW it in the loop. Fix: `if ($items) { foreach … }` so execution falls through to MUAC/stunting.
- Full masking trace (R16): vitamin-A question exists only where `atHealthCenter=False` = ONLY child_scoreboard_ncda (`Pages/ChildScoreboard/Activity/View.elm:132`; Nutrition:480/WellChild:2165/group:3766 are True); on the scoreboard the UniversalInterventions step always shows (`Measurement/Utils.elm:8961-8979`) and the answer is a mandatory task (`Measurement/View.elm:2874`) with save gated on all steps (:2516-2530); muac/stunting fields (2023-10-05, 22bad354b) postdate receive_option (2023-09-28, 28fc9ef36) so no historical node lacks it. Bites if any future client change makes vitamin-A optional → CHW malnutrition/stunting silently dropped from district dashboards. Also noted: the `field_ncda_signs` continue at :723-725 is dead (signs always contain ≥ `none`, `ifEverySetEmpty NoNCDASigns`, `Measurement/Utils.elm:8430`).
