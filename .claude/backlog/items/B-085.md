<!-- id:B-085 status:READY tier:4 round:16 -->
### B-085 — Person shards not recomputed on health-center change — READY · CONFIRMED · S · defense-in-depth (unreachable via official client)
- `hedley_device.module:227-230` short-circuits when `field_shards` populated; `HedleyRestfulSync.class.php:442-447` deliberately does NOT ignore `health_center` for person → HC-moved person keeps old shards. Fix: recompute when `field_health_center` changed (compare `$node->original`).
