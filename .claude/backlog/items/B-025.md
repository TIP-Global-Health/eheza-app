<!-- id:B-025 status:IMPLEMENTED tier:1 round:10 -->
### B-025 — Upload lanes wedge the WHOLE sync cycle permanently when the JS handler dies without replying — ✅ IMPLEMENTED 2026-07-05 (issue #1888, PR #1889, branch `b025-upload-reply-guarantee`, base develop @3cd2676a5, CI running, Copilot requested). Fix: appended `.catch` to all 5 upload IIFEs in app.js — Photo/Screenshot → `{tag:'Error',error:'UploadError',reason}`, General/WhatsApp/Authority → `{entities:[],remaining:0}` (self-healing). node --check clean + forced-throw discrimination check (old=0 replies/wedge, new=1 decodable reply, happy path single reply). JS-only, no Elm change. FOLLOW-UP 2026-07-05 (Copilot review, CONFIRMED valid): Photo/Screenshot lanes ran per-row work in an un-awaited `result.forEach(async …)` → callback rejections (incl. the quota `.modify`, the realistic B-025 trigger) bypassed the new catch. Fixed in 2e9926d17 (`await Promise.all(result.map(…))` in both lanes); General/WhatsApp/Authority were already fully awaited/covered. [HIGH REVIEW 2026-07-20: no findings — limit(1)+empty-guard ⇒ exactly one reply; catch shape `{tag:Error,error:UploadError,reason}` = the pre-existing inner fetch-catch shape, decodes via Decoder ['data','tag'/'error'/'reason'] → RemoteData.Failure(UploadError); determineSyncStatus Failure branch rotates→bounded-retry→SyncIdle, not a wedge. General/WhatsApp/Authority `{entities:[],remaining:0,error}` self-heals; extra `error` field ignored by decoder.]
- JS: `app.js` `IndexDbQueryUploadPhoto` (~:715-818) / `IndexDbQueryUploadScreenshot` (~:820+) are bare `(async () => {…})();` with try/catch ONLY around `fetch` and `response.json()`. Un-protected rejection points that exit with NO port reply: opening Dexie query, `caches.open`, `JSON.parse(data)`, `parseInt(json.data[0].id)` (TypeError on 2xx w/o `data[0]`), and `…photoUploadChanges.modify(changes)` (Dexie write — rejects on quota; fires AFTER successful upload). General/Authority/WhatsApp upload branches have the same bare-IIFE pattern.
- Elm: `SyncManager/Update.elm:1126-1130` `SyncUploadPhoto … if RemoteData.isLoading webData then noChange` — NO timeout; downloads DO have one (`downloadRequestTimeout`, :104-106, :666-668 — proves the pattern exists). `determineSyncStatus` rotates only on Success/Failure. Photo upload is stage 1 of the cycle → uploads AND downloads halt until page reload.
- DISTINCT from the declined poison class: those retried forever (self-healing); this never retries again (permanent wedge until reload). Fix is JS-side reply guarantee, not batch-skipping: wrap each branch body so every exit calls `sendIndexedDbFetchResult(queryType, {tag:'Error', error:'UploadError', reason:String(e)})` (decoders already accept it, `SyncManager/Decoder.elm:131-150`); optionally add an Elm upload timeout mirroring :104-106. Severity HIGH-mechanism / MED-trigger (quota-pressure `modify` failure is realistic — same conditions as merged R2-2).
