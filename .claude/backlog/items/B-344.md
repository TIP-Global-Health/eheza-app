<!-- id:B-344 status:IMPLEMENTED tier:1 round:30 -->
### B-344 — Prenatal HIV (lab tech): a positive HIV result entered by the lab tech leaves "pending" in the follow-up answers, and `hivProgramAtHC` reads pending as "no HIV programme at this health centre" — Next Steps offers TDF3TC + Dolutegravir before the nurse is asked, and whatever is dispensed stays recorded after she answers Yes and the pathway switches to a referral — ✅ IMPLEMENTED 2026-09-15 (issue #2252, PR #2253, branch `B-344-hiv-program-pending-answer`, base develop @b4a499481, commit d91ac1e95; `hivProgramAtHC` now returns Maybe Bool — Nothing while the lab-tech pending marker is present; the medication set requires `Just False`, the ARV referrals at both phases and `referredToHIVProgramPreviously` require `Just True` — four callers, not the three the entry named; the completion report's PHP copy skips the HIV set on `pending-input`; 3 elm-test cases in `RecurrentActivity/Test.elm` (pending one fails on develop), new lab-tech e2e in `lab-tech-encounter.spec.ts` — not run locally, CI is its first run; phpcs Drupal+DrupalPractice clean, elm-review clean) · CONFIRMED (the lab-tech writer, the reader, the medication set and the ungated Next Steps expectation read by the orchestrator; scout scratch elm-test before/after the answer) · MED · tier 1 (diagnosis rule — care pathway) · R30 unit 2 · the unguarded sibling of B-323's partner-test guard; all sites; never fired so far (B-240's sizing: 0 lab-entered positive HIV results on any site)
- **Mechanism.** The lab tech's save stores the pending marker as the whole sign set:
  ```elm
  -- client/src/elm/Measurement/Utils.elm:8203-8212 (toHIVTestValue…, lab-tech branch)
                  hivSigns =
                      if isLabTech then
                          ...
                          Just <| EverySet.singleton PrenatalHIVSignPendingInput
  ```
  and the programme reader treats any set without `HIVProgramHC` as "no programme":
  ```elm
  -- client/src/elm/Pages/Prenatal/Utils.elm:2957-2962
  hivProgramAtHC measurements =
      getMeasurementValueFunc measurements.hivTest
          |> Maybe.andThen .hivSigns
          |> Maybe.map (EverySet.member HIVProgramHC)
          |> Maybe.withDefault False
  -- :1810-1816 (resolveRequiredMedicationsSet → resolveHIVPositiveSet)
              if
                  (hivDiagnosed && not hivProgramHC)
                      || patientReportedNoMedicineRecievedFromPMTCT assembled.measurements
              then
                  Just ( Translate.MedicationDistributionHelperHIV, [ TDF3TC, Dolutegravir ], [] )
  ```
  Recurrent Next Steps is expected whenever its task list is non-empty (`Pages/Prenatal/RecurrentActivity/Utils.elm` `expectActivity … RecurrentNextSteps`), not after `LabsResultsFollowUps` completes — the same window B-323's review found for PrEP. `hivSignsAnswered` (`Measurement/Utils.elm:8719`) exists and is used for the partner test; this reader does not use it. `referredToHIVProgramPreviously` (`Pages/Prenatal/Activity/Utils.elm:1039-1055`) reads the same marker at later encounters.
- **Reachability.** Lab tech saves a positive HIV result → `DiagnosisHIVRecurrentPhase` → nurse opens the recurrent encounter; Next Steps is available before the follow-up "Is she in the HIV programme at the HC?".
- **Impact (scout, run).** Pending: required medications `[[TDF3TC, Dolutegravir]]`, Next Steps `[HealthEducation, MedicationDistribution]`, follow-ups `[TaskHIVTest]`. After answering Yes: no medications, Next Steps `[HealthEducation, SendToHC]`. ARVs dispensed in the window remain on the record while the pathway says refer to the programme.
- **Backlog de-dup.** B-323 (partner test's pending marker, IMPLEMENTED), B-240, B-320; symptom not recorded. Server copies of the medication/ARV-referral rules treat the marker the same as the client (scout).
- **Fix shape (guess).** Treat a pending sign set as unanswered in `hivProgramAtHC` (and `referredToHIVProgramPreviously`), so the HIV medication set waits for the answer — e.g. via `hivSignsAnswered`. Decide with B-336's landing, which touches the same completion surface.
- **Review round 1 (2026-09-15, medium):** 6 findings, all fixed in `22261e498` (e2e: back to the encounter page before `completeRecurrentNextSteps`; recurrent referral icon is `next-steps-send-to-hc`, not `next-steps-referral`; `openLabResultsEntryFromCaseManagement` helper replaces four inline copies; test fixtures `withDiagnoses` + `hivTestValueWith`; reader as one `case`, callers `== Just True/False`; doc comment names the no-signs shape). CI's first e2e run failed at the first of these, after the medication-absent assertion had passed on the real app. Phase-1 dedup of the spec left out on purpose.
