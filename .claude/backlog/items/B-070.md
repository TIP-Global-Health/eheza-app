<!-- id:B-070 status:IMPLEMENTED tier:3 round:15 -->
### B-070 — AcuteIllness `fromCovidTestingValue` mis-reconstructs pregnant/positive COVID results on edit — ✅ IMPLEMENTED 2026-07-16 (issue #1953, PR #1954, branch `b070-covid-form-reconstruct`, base develop @15275c54f, commit 7458ec578, Copilot requested). Fix: `testPerformed = not (member [UnableToRun, UnableToRunAndPregnant])`, `isPregnant = Just (member [PositiveAndPregnant, UnableToRunAndPregnant])`. **STRONGER than backlog headline (round-trip verified via toCovidTestingValue):** OLD code corrupted on re-save for TWO variants — RapidTestUnableToRunAndPregnant→Nothing (lost) AND RapidTestPositive→RapidTestPositiveAndPregnant (non-pregnant positive silently flagged pregnant). All 6 variants now round-trip. NEW test `covidTestingRoundTripTest` in existing Pages/AcuteIllness/Activity/Test.elm (drives public toCovidTestingValueWithDefault + emptyCovidTestingForm round-trip); DISCRIMINATION: reverting fails exactly the 2 changed variants, 3 guards pass. elm make 548, elm-test 2835, elm-format + elm-review (cold) clean. Local /code-review medium: no findings. SIDE-FINDING (pre-existing, NOT fixed): RapidTestIndeterminate also fails round-trip (→Nothing) — form has no indeterminate field; file separately IF the covid form offers that option.
- `Pages/AcuteIllness/Activity/Utils.elm:1881` `testPerformed = value.result /= RapidTestUnableToRun |> Just` — for the `RapidTestUnableToRunAndPregnant` variant this is `Just True` (should be False); `:1897 isPregnant = Just <| rapidTestPositive value.result` → `Just False` (should be True, `rapidTestPositive` is False for the unable variant); `testPositive` → `_ -> Nothing`. `toCovidTestingValue:1955-1956` DOES persist `RapidTestUnableToRunAndPregnant` (testPerformed False + isPregnant True), round-trips through encoder :2160 / decoder :3101.
- Trigger: nurse AI, covid-suspect, fertile woman, test NOT performed, pregnant=Yes (the pregnancy question is required in the not-performed branch). On re-entering: shows "Test performed = Yes" with a blank demanded result + pregnancy=No; Save gated on task-count not `isJust measurements.covidTesting`, so answering the bogus result overwrites the correct record. NO diagnosis/completion impact (engine reads saved value directly). The malaria sibling `fromMalariaTestingValue:1560` special-cases `RapidTestPositiveAndPregnant` — covid omits the analogue.
- Fix: `testPerformed = not (List.member value.result [RapidTestUnableToRun, RapidTestUnableToRunAndPregnant]) |> Just`; `isPregnant = Just (List.member value.result [RapidTestPositiveAndPregnant, RapidTestUnableToRunAndPregnant])`. Severity LOW-MED (narrow trigger, edit-corruption risk).
