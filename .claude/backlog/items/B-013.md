<!-- id:B-013 status:READY tier:4 round: -->
### B-013 — Pairing-code consume wipes code before token mint, no transaction — READY · CONFIRMED (verification sweep 2026-07-06, self-verified)
- `HedleyRestfulPairingCode.class.php:101-121` order verified: wipe `field_pairing_code->set('')` + `$wrapper->save()` (COMMITTED) → then EFQ token lookup + entity_delete_multiple + generateAccessToken + viewEntity (all throw-capable). No db_transaction in the method (contrast HedleyRestfulSync :430 which wraps its loop). Retry impossible: lookup `fieldCondition('field_pairing_code','value',$code)` no longer matches → `RestfulBadRequestException('Invalid pairing code.')`. Recovery only via super_user_mode (hedley_device.module:50-65 blocks re-setting a code on paired devices otherwise).
- Fix: wrap :101-121 in db_transaction(), or reorder mint-token-first-then-wipe inside the transaction.
