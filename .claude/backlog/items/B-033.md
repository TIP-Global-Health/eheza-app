<!-- id:B-033 status:IMPLEMENTED tier:2 round:10 -->
### B-033 — Group-education stats count soft-deleted persons and ship their names to the dashboard — ✅ IMPLEMENTED 2026-07-13 (issue #1935, PR #1936, branch `b033-education-deleted-participants`, base develop @8e19e166a, commit 8ebcc6c1f, Copilot requested, CI running). Fix: reused the B-032 helper `hedley_stats_exclude_deleted_from_join('pp.entity_id = node.nid', 'pp.field_participating_patients_target_id')` on the participants leftJoin (ON-clause, so the session survives when one attendee is deleted); also generalized the helper's docblock (it said "measurement" throughout — persons via a reference field are a 2nd use class). Regression test = scenario 6 folded INTO the existing `testStatsCalculations()` method (NOT a new test method — B-042's Copilot lesson) + 2 helpers. DISCRIMINATION end-to-end vs the real function: OLD counted 505,506 and shipped the deleted person's name; NEW counts 505 only, no name. Test verified to FAIL against unpatched code before shipping.
- 📌 SimpleTest fixture gotchas (cost 2 rounds; both surfaced by running the fixture through drush BEFORE CI): `field_scheduled_date` is a DATE RANGE wanting a FORMATTED string — `->set(['value' => date('Y-m-d', $ts), 'value2' => ...])`, NOT a timestamp (wrapper throws, then MySQL rejects); `field_education_topics` is `list_text` with allowed_values keys ('malaria', 'tuberculosis', …), NOT an integer.
- 📌 Local `node_delete()` of fixtures is BLOCKED by `hedley_admin_node_delete()` ("Content deletion is forbidden!") → wrap cleanup in `variable_set('hedley_super_user_mode', 1)` … `variable_del(...)`; restore the variable to its prior state (it was UNSET on this dev DB, so `vdel`, not `vset 0`).
- RESIDUAL (not fixed, worth its own issue if wanted): the session node still REFERENCES the deleted person; only stats ignore them. A `_consolidate_education_session_content_execute` handler re-pointing `field_participating_patients` on merge would fix the root.
- `hedley_stats.module:2272-2274` GROUP_CONCATs `field_participating_patients` target ids with no person-deleted check; `:2297+` explodes and `node_load_multiple` → `hedley_stats_add_patient_details` emits name/gender for every id, deleted or not.
- Reachable via the LIVE duplicate-merge flow: `hedley_patient.drush.inc:228` marks the duplicate person `field_deleted=TRUE` (deletable because attendance is session-side — `hedley_general_get_person_measurements:167-182` counts only `field_person`-referenced measurement bundles, so the presave guard doesn't block) and NO consolidation handler re-points `field_participating_patients` (grep: zero hits in hedley_patient/hedley_person).
- Fix: skip loaded nodes with `field_deleted` TRUE and drop them from the emitted array (they're node_load'ed anyway); optionally add a `_consolidate_education_session_content_execute` to re-point attendance on merge. Severity MED-LOW.
