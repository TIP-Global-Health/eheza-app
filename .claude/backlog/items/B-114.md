<!-- id:B-114 status:IMPLEMENTED tier:2 round: -->
### B-114 — Patient merge never re-points education_session.field_participating_patients (ROOT CAUSE of B-033) — ✅ IMPLEMENTED 2026-07-15 (issue #1947, PR #1948, branch `b114-merge-transfer-education-sessions`, base=**b113 branch** (stacked on #1946, retarget to develop after it merges), commit 6a0291afc, Copilot requested). Fix: new `_consolidate_patient_education_sessions($original,$duplicate)` called from execute() next to the relationship transfer; replaces duplicate→original in the multi-value field_participating_patients, dropping the dup entry when original already attended. NO shard handling (education_session shards are village-based, assigned only on create — verified in hedley_device_assign_shards). Also extracted shared `_hedley_patient_content_referencing_person($person, $fields, $bundle)` (local /code-review medium flagged the same bundle-filter duplication Copilot caught on B-113). DISCRIMINATION (drush, real fn): transfer/dedup/no-op all correct + relationship path re-verified through the refactored helper + OLD-path proof (session keeps deleted D). Test cases 7-9 folded into HedleyPatientConsolidation's single method (method renamed testRelationshipsAreTransferredOnMerge→testContentIsTransferredOnMerge). Local review: medium (touches only hedley_patient, no high-risk paths).
- 📌 NOTE for B-116: fold into HedleyPatientConsolidation same way (extra assertions in the ONE method), and reuse `_hedley_patient_content_referencing_person` for the field_newborn lookup.
- Sessions keep referencing the merged-away person. PR #1936 made the STATS ignore soft-deleted attendees, but the reference itself remains, and other consumers (the Elm client receives the session with the participant UUID over sync) do not necessarily filter.
- Fix: `_consolidate_education_session_content_execute()` — re-point `field_participating_patients` entries from duplicate to original, deduplicating (the original may already attend the same session).
