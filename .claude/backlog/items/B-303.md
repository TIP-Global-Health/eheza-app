<!-- id:B-303 status:IMPLEMENTED tier:3 round:27 -->
### B-303 — Translate: clinical-content translation errors batch — wrong dosage, wrong instruction, collapsed screening options, wrong threshold (6 sites, all objectively wrong vs their own English) — ✅ IMPLEMENTED (issue #2181, PR #2183, 2026-09-01) · was READY · CONFIRMED (every anchor + introducing commit re-verified by orchestrator) · MED · tier 3 · R27 unit 6 · one mechanical PR; where no correct translation exists, `Nothing` (English fallback) is the objective fix
All are the coverage map's allowed exception classes (wrong number/unit, wrong language, copied neighbor) — NOT the declined translation-quality fill. Sites: Kinyarwanda renders on Rwanda only, Kirundi on Burundi, Somali on Somalia (`App/View.elm:306-319`).
1. **TB follow-up End Month 2 (`Translate.elm:27013-27018`)**: `kirundi` holds ENGLISH text that is also the WRONG guidance — verbatim the EndMonth5 entry's English ("…retest after 15 days.") where this stage's English says "Continuation phase." Introduced `76039fe06` (diff re-verified: EndMonth5/6 got real Kirundi, EndMonth2 got the next row's English). Consumer: `Pages/Tuberculosis/Activity/Utils.elm:284`. Fix: `Nothing` (falls back to the correct English) unless a real translation is authored.
2. **Carvedilol dose ×2 (`:26726-26740`)**: `kinyarwanda` says "Karuvedilole (5.25mg)" in BOTH `TreatmentHypertensionAddCarvedilol` and `…AddAmlodipine` where English/Kirundi say 6.25mg (drug label entry `:20770` confirms). Introduced `c5550279a`. Consumer: `Pages/Prenatal/Activity/Utils.elm:1100` (patient-facing treatment recap).
3. **Aluminium Hydroxide (`:20500-20505`)**: `kinyarwanda` "ikinini 1 inshuro ku munsi mu minsi 7" — the "3" of "3x a day" is missing (reads as once/[?] daily); Kirundi has "incuro 3". Introduced `13555ff15`. Consumers: `Measurement/View.elm:2395`, `Pages/Prenatal/Utils.elm:1070`.
4. **Captopril (`:20583-20588`)**: `kirundi` appends "mu minsi 5" — a 5-day course no other language has, on an antihypertensive with no duration in English. Introduced `d37506810`.
5. **EPDS depression screening options 1 & 2 (`:18872-18884`)**: both render `kirundi = "Si vyinshi cane ubu"` — two differently-SCORED answers of a screening instrument are indistinguishable on Burundi. Introduced `bb56a123d`; a later spelling fix preserved the collapse. Consumer: `Pages/Prenatal/Activity/View.elm:1852`.
6. **ECD milestone (`:6275-6281`)**: `somali` says "1 ilbiriqsi" (1 second) where the milestone is standing on one foot up to **5** seconds — a 5× stricter developmental threshold. Introduced `a48ef4c39`. Consumer: `Pages/WellChild/Activity/View.elm:1699`.
- **Fix shape (guess).** Correct the numbers in place (2,3,4,6 — mechanical), differentiate 5 or `Nothing` the second option, `Nothing` 1. Each fix's oracle is the entry's own English.
- **Implementation (2026-09-01, issue #2181, PR #2183, branch `B-303-B-304-translate-corrections`, base `d8c4a9d3f`, one PR with B-304).** 1 → `Nothing`; 2 (×2), 3, 6 corrected in place against the English; 4 dropped the invented "mu minsi 5" (the string is a paste of `AdministerParacetamolHelper`, whose English genuinely says 5 days — that entry untouched); 5 → Option2 `Nothing` (git shows the two options were introduced already collapsed, so no translation exists to restore). Verified: all 16 anchors re-verified verbatim pre-edit, `elm make` 548 modules, 3,039 tests, fresh-clone `elm-review` clean.
