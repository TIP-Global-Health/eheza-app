<!-- id:B-172 status:READY tier:4 round:20 -->
### B-172 — Dashboard decoder: dict KEYS parsed with a defaulting parser — a key-format change collapses buckets silently instead of failing loudly — READY · CONFIRMED-mechanism (both hops verified twice: agent + orchestrator; default branches DEAD today, producer verified) · LOW latent-but-severe-if-triggered · tier 4 · hardening
- (a) `Backend/Dashboard/Decoder.elm:113` `legacyDictToDict (String.toInt >> Maybe.withDefault 1)` in `decodeNutritionValueDict` — producer `hedley_stats.module:3172-3245` emits keys `1..13` (13 = "over a year ago"), so the default is dead TODAY; any key-format change collapses all 13 buckets into key 1 (`Dict.fromList` last-wins) = 11+ months of case-management z-scores silently gone. This is the exact trap that nearly shipped in B-163/#2090 (recorded there as a ⛔ note; this item is the hardening half).
- (b) Same file `:216-238` `programTypeFromString`: unknown strings → `ProgramUnknown`, used as a dict KEY via `legacyDictToDict` (`:151`, `:206`) — two unknown program types collapse last-wins and their stats vanish; dead until a new clinic type is added, which is precisely when it fires.
- (c) Minor same-class note: `:282` `sequence_number (decodeWithFallback 1 decodeInt)` — a parse failure mis-orders acute-illness encounter pairing (`Pages/Report/Utils.elm:208-217`, `Pages/GlobalCaseManagement/View.elm:628-631`).
- Fix direction: parse keys via `andThen` that FAILS on unparseable input (the `decodePatientsDetails` `:487` filterMap at least refuses to invent keys; failing loudly is better — a decode failure surfaces, a collapsed dict does not).
- Sweep verdict (durable): this is the ONLY live withDefault-on-parser site with aggregation blast radius in client + server/elm; all other `String.toInt/toFloat >> withDefault` sites are UI/display-side (declined class) or absence-handling on typed Maybes. server/elm same-class latents (no action, emitter is lockstep + bundle-gated): `Backend/Completion/Decoder.elm:119-129` + `Backend/Reports/Decoder.elm:803-819` `filterMap` silently DROP unknown activity/vaccine tokens (skews completion %); `Reports/Decoder.elm:598-608/:727-732/:791-800` numeric chunks fall back to "not measured" while dates fail loudly.
