<!-- id:B-141 status:IMPLEMENTED tier:1 round:18 -->
### B-141 — ✅ IMPLEMENTED 2026-08-06 (issue #2048, PR #2049 stacked on #2047 — **retarget to develop after it merges**, branch `b141-php-rwanda-ipv-second-dose`, commit 7faedd26c) — PHP engine missed Rwanda IPV-2
- **Built in the safe order the warning below demanded:** extract the duplicated block FIRST → `hedley_reports_resolve_expected_immunisations()` (name pattern `well_child_%s_immunisation` vs `child_scoreboard_%s_iz` is the only difference between the two call sites) → then `$site` on `get_last_dose_for_vaccine` + the Rwanda IPV dose-2 rule beside the OPV one → then label the `'+0 days'` placeholder. 56 insertions / 52 deletions.
- ⭐ **DISCRIMINATION TECHNIQUE WORTH REUSING for PHP module functions: extract the real functions into a standalone harness and run them with host `php`** — no Drupal, no DDEV, no DB. Grabbed `resolve_expected_immunisations` + `get_last_dose_for_vaccine` + `get_interval_for_vaccine` verbatim into `scratchpad/b141_harness.php` with a reconstruction of the naive fix, and ran both. Result table (Rwanda child, IPV-1 at 14 weeks): naive fix expects IPV-2 at 15/20/30/40 weeks; this PR only at 40. Burundi unaffected; Rwanda with both doses not expected. **This is real execution of the shipped code, not reasoning about it.**
- **Verification:** php -l · phpcs Drupal + DrupalPractice clean · the harness above. No test (nothing in the repo covers this engine; a guard = new SimpleTest class + full install).
- 📌 The DTP-SA gate here reads `dtp dose-3` — do NOT "fix" it toward the Elm copies' OPV-3; it is the EVIDENCE for B-055, which is clinician-gated.

Original entry:
- ⛔ **THE STATED FIX IS UNSAFE AS WRITTEN — read before touching (found 2026-08-06, user pulled on the client's comment).** `hedley_reports_get_interval_for_vaccine()` returns **`'+0 days'` for ipv**, exactly as the client's `getIntervalForVaccine` does — and the client says why in a comment the PHP never copied: *"So far, there was only single IPV dose. Since #1426, at Rwanda site, we got second dose scheduled on the latter between age of 36 weeks, and 4 weeks after first dose was administered. This requirement is not reflected here. Instead, it's defined as special case at appropriate spots in code."* So making `case 'ipv'` return `dose-2` on Rwanda WITHOUT porting the special case schedules IPV-2 at `last_dose_date + 0 days`, and `if ($start_date_obj >= $next_dose_date_obj)` then expects it **the same day dose 1 was given** → every Rwandan child with IPV-1 counted incomplete. Today's bug INFLATES completion; the naive fix would deflate it for the whole IPV-1 population — strictly worse.
- ⚠ **The next-dose block is DUPLICATED INSIDE THE MODULE**: `hedley_reports_get_last_dose_for_vaccine()` is called at **:5815 AND :6463**, each followed by its own byte-identical 13-line copy of the interval + OPV-special-case + `$expected[]` logic. **Extract that block into one function BEFORE adding the IPV rule**, or the fix creates two more copies. (PHP needs the rule at 2 sites; the server Elm needs it at 2 different ones — `nextVaccinationDataForVaccine` and `initialVaccinationDateByBirthDate`. PHP has no initial-date equivalent: a child with no doses is simply marked expected, `:5802-5810`.)
- 📌 **Sequence: do B-131 (server Elm) FIRST**, then cite it here rather than re-deriving the rule. Copy count for this single rule today: client Elm (correct), server Elm (wrong), PHP ×2 (wrong).
- `hedley_reports_get_last_dose_for_vaccine()` (`hedley_reports.module:5895-5916`): `case 'ipv': return 'dose-1'`, no site param, no Rwanda IPV-2 rule anywhere in the module. Client: IPV last dose = `VaccineDoseSecond` on SiteRwanda (`Measurement/Utils.elm:9749-9755`, scheduling :9531-9569/:9633-9651). PHP engine written 2024-09-06 (5dde29a14); client changed 2025-02-23 (acd16dada); PHP never updated.
- Mechanism: once dose-1 exists the loop (`hedley_reports.module:5817`) skips IPV → `well_child_ipv_immunisation` never expected again while the app still offers dose 2 → Rwanda well-child completion % overstated for every child due IPV-2.
- **THIRD copy of the vaccination engine, second missed-IPV-2 instance** (server/elm = B-131b). The three engines (client Elm / server Elm / PHP) now have a documented drift triangle — when fixing, fix BOTH copies against the client and consider a comment cross-referencing the three sites.
