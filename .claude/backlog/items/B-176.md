<!-- id:B-176 status:READY tier:4 round:20 -->
### B-176 — Contact tracing: a comma typed in a contact's name or phone silently deletes traced contacts from the record — READY · CONFIRMED-mechanism (orchestrator-verified both anchors) · latent (live: 0 commas stored today, one keystroke away) · tier 4 · R20 wave 2
- Server: `HedleyRestfulActivityBase.class.php:130` `GROUP_CONCAT(DISTINCT ...)` with the DEFAULT comma separator + `:156` `explode(',', ...)`, applied to `field_contacts_trace_data` (`HedleyRestfulAcuteIllnessContactsTracing`) — the ONLY free-text multiField; all others carry enum machine-names that cannot contain commas. Stored values are `[&]`-joined records (id, names, gender, phone, date).
- Client: `Backend/Measurement/Decoder.elm:4117-4132` — each element must `String.split "[&]"` into EXACTLY 6 parts; comma-split fragments fail the match and `decodeListDroppingUnknown` drops them silently. No batch poison — the traced contact just vanishes from every other device.
- Fix: `GROUP_CONCAT(... SEPARATOR '|')` (any char impossible in the data) for this field + explode on the same; or escape commas at write time.
- Live query run: `field_contacts_trace_data_value like '%,%'` → 0 rows (ihangane).
