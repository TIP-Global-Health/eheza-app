<!-- id:B-098 status:IMPLEMENTED tier:2 round:16 -->
### B-098 — NCDA AQ worker `node_save($person)` lost-update window — concurrent person edits silently reverted — ✅ IMPLEMENTED 2026-07-12 (issue #1929, PR #1930, branch `b098-ncda-lost-update`, base develop @2c0322070, commit 71e866a30, Copilot requested, CI running). Fix exactly as recorded: `$person = node_load($person->nid, NULL, TRUE);` + `if (!$person) return FALSE;` immediately before the `field_ncda_data` assignment at :987 (inside `calculate_aggregated_data_for_person`, so both callers — AQ worker + generate-data-for-all.php — get it). Anchors held (:154 worker load, :987-989 write-back). Verification: php -l + phpcs Drupal & DrupalPractice clean; DISCRIMINATION on the REAL function in ddev (person nid 64, concurrent name edit committed between load and save): OLD reverted the edit to "Yvan" (lost update), NEW preserved it, both still wrote field_ncda_data. Harness technique worth reusing for PHP items: extract the patched function from the worktree with `sed -n 'START,ENDp'`, rename it, prepend `<?php`, `ddev exec "cat > /tmp/x.php" < local` to get it into the container (scratchpad is NOT mounted), then `ddev drush php-script` calls old (real, main-tree) vs new (renamed copy) side by side. No SimpleTest added (hedley_ncda has no test class; a new one = a full extra CI install, no natural existing home).
- `hedley_ncda.module:153-155` worker `node_load($person_id)` → full scorecard assembly (hundreds of node loads, ~100ms–seconds) on the STALE in-memory copy → `:987-989` `$person->field_ncda_data[…] = $encoded; node_save($person)` writes ALL fields from the stale object. Any person update committing in the window (device sync PATCH name/birthdate/geo, admin edit) is clobbered; the revision bump then propagates the reverted person to all devices — nurse's correction disappears with no error. Recalc queued on every NCDA-related measurement → frequent windows. Distinct from B-058 (queue-level dedup clobber; this is node-level lost update at save).
- Fix (small, verified shape): re-load fresh immediately before mutation — `$person = node_load($person->nid, NULL, TRUE);` right before the field assignment + save (shrinks window to microseconds). Atomic single-field write in D7 = FIX-TOO-BIG, don't.
