<!-- id:B-393 status:READY tier:3 round:34 -->
### B-393 — E2E suite: the Nutrition NCDA test has run in no CI job since 2026-03-15, because the job-1 exclusion `NCD` matches inside its title's `NCDA` — READY · CONFIRMED (orchestrator reproduced the whole partition with `playwright test --list`) · MED · tier 3 · R34 unit 1
- **Mechanism.** CI job 1 runs two commands, the second excluding tests by title:
  ```yaml
  -- .circleci/config.yml (e2e_playwright_1, "Run E2E tests")
  command: cd client && export TZ=UTC && npx playwright test prenatal tuberculosis && npx playwright test --grep-invert "Well Child|NCD|HIV|Tuberculosis|Child Scoreboard|Prenatal|Family Nutrition|Group Nutrition|Group Education|Stock Management|Admin Reports|Bulk Photo Fetch"
  ```
  The alternative `NCD` is an unanchored regex, so it matches the substring inside `NCDA`:
  ```ts
  -- client/e2e/nutrition-encounter.spec.ts:36
    test('complete normal encounter with NCDA and verify backend sync', async ({
  ```
  Job 2 selects by path token (`well-child ncd hiv child-scoreboard family-nutrition group-session education-session stock-management sync-bulk-photos`) and `nutrition-encounter.spec.ts` matches none of them; job 3 runs only `reporting`. So the test is excluded from job 1 and selected by no other job. Its two siblings in the same file do run, in job 1's grep-invert leg.
- **Evidence (orchestrator, measured not reasoned).** Ran `playwright test --list` once per CI command and diffed against the full list: job1a 11, job1b 25, job2 27, job3 1 — union **64 of 65** tests, zero tests in two jobs, and exactly one in none:
  ```
  [chromium] › nutrition-encounter.spec.ts:36:7 › Nurse: Individual Nutrition Encounter › complete normal encounter with NCDA and verify backend sync
  ```
  ⚠ The measurement is not poisoned by the path trap ([[e2e-ci-partition-trap]]): the absolute path used here (`/var/www/html/ihangane/client/e2e/…`) contains none of the nine job-2 tokens, as CircleCI's `/home/circleci/project/` does not.
- **Since when.** `4c9248767` (**2026-03-15**, "Add NCDA coverage to Nutrition E2E tests") gave the test its current title; before that it read `complete normal encounter and verify backend sync` and did run. **1,846 commits have landed since.**
- **Impact.** The suite is the only functional release gate, and this test's assertions have not executed for six months: the `nutrition_ncda` node created by the nurse-at-health-centre NCDA flow, the height/weight/MUAC values for a 10-month-old nurse encounter, `diagnosisAppeared === false` for normal nutrition signs, and the progress-report check. Mitigating: the `completeNCDA` helper itself is still exercised by `reporting.spec.ts` in job 3, so the helper is not rotting — what is lost is this test's own assertions and the guarantee that it still passes at all.
- ⚠ **Affects planned work:** TH-05 is READY and is written entirely about this test, so that work would harden a test nothing runs. Check this before building it.
- **Fix shape (guess).** Two independent halves: (a) get the test running — anchor the invert alternatives or add `nutrition-encounter` to job 2's path list, whichever the pipeline owner prefers; (b) the standing guard, already queued as G-04, which would have caught this in March. Re-run the full matrix afterwards either way.
