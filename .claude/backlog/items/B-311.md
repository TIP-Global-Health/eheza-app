<!-- id:B-311 status:READY tier:2 round:28 -->
### B-311 — Aggregated NCDA scoreboard: the "born in the last 3 years" cutoff is evaluated at request time, so every historical column (Nov 2023 onward) silently loses the children who were under 2 back then — about half the cohort today, more each day — READY · CONFIRMED (query + year selector read; cutoff blamed; live-sized) · MED-HIGH · tier 2 · R28 unit 5 · ⭐ DEPLOYED (ihangane only — the `ncda` site)
- **Mechanism.** `hedley_ncda_generate_results_data_for_restful` (`hedley_ncda.module:347`, `:367-371`): `$three_years_ago = date('Y-m-d', strtotime('-3 years'))` … `->condition("$bd.field_birth_date_value", $three_years_ago, '>')` under the comment "Since we show data starting Nov 23, it's enough to select only those born during the past 3 years" — true when written (`8e817f9fb`, 2024-03-06: −3 years was 2021-03, before any child under 2 in Nov 2023), preserved verbatim by the 2026-05 direct-SQL rewrite (`8faac649d`). The consuming app is not a last-12-months view: `server/elm/src/Pages/Utils.elm:75` `minYear = 2023`, `Pages/Scoreboard/View.elm:1136-1160` pages back to Nov 2023. Every pane derives denominator (`childrenUnder2`, `View.elm:74-97`) and numerators from `data.records` = this query's result.
- **Today (2026-09-03) the cutoff is 2023-09-03.** For the Jan 2024 column the under-2 cohort is born 2022-02..2024-01; births 2022-02..2023-09 are gone — ~19 of 24 birth-months. For Jan 2025, ~7 of 24. The values of the 2024 and 2025 views change every day as children age past the cutoff — the same "a historical cell changes as the viewing date advances" invariant B-128 used as its proof.
- **Live scale (ihangane, 2026-09-03):** **61,044** persons carry a scoreboard blob and were born after 2021-11-01 (could appear in some displayed column); only **31,842** pass today's cutoff. 29,202 children (48%) are absent from every column, historical ones included.
- **Reachability.** Data Manager / admin → `admin/reports/aggregated-ncda/<province>/<district>[/…]` → Elm POSTs `/api/reports-data` `app_type=scoreboard` (`Backend/Components/Sync.elm:71-88`) → `HedleyRestfulReportsData::getData()` → this function. Current-year columns are correct (a child under 2 now was born < 2 years ago).
- **Fix shape (guess).** Anchor the lower bound on the fixed launch date (born after Nov 2023 − 2 years = `'2021-11-01'`), or have the app pass the selected year and filter `year − 2 .. year`. Count and data queries share `$build`, so one change covers both. Statistics only; no stored data affected.
