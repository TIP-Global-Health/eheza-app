<!-- id:B-221 status:IMPLEMENTED tier:1 round:23 -->
### B-221 — Prenatal: saving the Partner HIV test never re-runs the diagnosis assessment, so a discordant-partnership diagnosis reached through it is not persisted — CONFIRMED (orchestrator-read the branch vs its 9 siblings) · MED · tier 1 · both sites · R23 unit 7 — ✅ IMPLEMENTED 2026-09-06 (issue #2203, PR #2204, branch `B-221-partner-hiv-assessment`, base develop @f151cfef7)
- **Mechanism.** `Backend/Update.elm:3114-3126` `[ PrenatalPartnerHIVTestRevision _ data ]` calls only `processRevisionAndUpdatePrenatalLabsResults`. Every sibling single-revision lab branch (HIV `:3094`, HIVPCR `:3128`, Syphilis `:3148`, HepB `:3175`, Malaria `:3198`, Urine `:3230`, BloodGpRs `:3250`, Hemoglobin `:3273`, RBS `:3293`) additionally calls `processRevisionAndAssessPrenatal data.encounterId False` and appends its messages. But `generatePrenatalDiagnosesForNurse` reads `measurements.partnerHIVTest` (`Pages/Prenatal/Activity/Utils.elm:2320`, feeding `DiagnosisDiscordantPartnership{Initial,Recurrent}Phase`). History (agent-traced): branch written `c8a70a32d` (2024-01) when the partner test fed no diagnosis; partner-driven logic arrived `d3bb86fa8` (2024-10) and the branch was not updated.
- **Reachability.** Nurse saves the partner test (`Pages/Prenatal/Activity/Update.elm:~3191`; recurrent `RecurrentActivity/Update.elm:~937/~1829`) → SW returns the single revision → this branch. Lab tasks are freely ordered, so "partner test saved last (or alone)" is ordinary.
- **Impact.** `encounter.diagnoses` misses the discordant-partnership diagnosis. `resolveDiscordantPartnershipSet` (`Pages/Prenatal/Utils.elm:1821-1822`) gates the TDF3TC/PrEP medication set on `diagnosed diagnosis assembled` (= `EverySet.member … encounter.diagnoses`) → guidance not offered unless a later assessing revision fires in the same encounter. `generatePrenatalRecurrentPhaseCompletedMsgs` (`:~9226`) recomputes locally, so the app can hold Next Steps pending while the Next Steps page (built from the stale persisted set) shows nothing to do. Self-heals only on a later assessing revision.
- **Fix shape (first guess):** mirror the HIV-test branch — keep the labs-results update and add `processRevisionAndAssessPrenatal data.encounterId False`, concatenating both message lists.
- ⬆ **Re-tiered 3 → 1 on 2026-09-03 (user rule):** a defect in how a diagnosis or its care pathway is determined is tier 1 regardless of how many patients hit it. Severity label unchanged — it describes blast radius, the tier describes priority.
- ✅ **IMPLEMENTED 2026-09-06 — issue #2203, PR #2204, branch `B-221-partner-hiv-assessment`, base develop @f151cfef7.** Fix as the entry's shape: the `[ PrenatalPartnerHIVTestRevision _ data ]` branch now mirrors the HIV branch (labs-results update + `processRevisionAndAssessPrenatal data.encounterId False`, messages concatenated). Discrimination test is an e2e case in `prenatal-encounter-nurse.spec.ts` (HIV negative at point of care with the partner question answered No, then Partner HIV known-positive / not on ARVs saved LAST, then Next Steps must show the Medication task with "TDF + 3TC"); the helper `completeLaboratoryNurse` gained a `discordantPartnership` option that saves the partner tab after every other tab. Pushed as two commits so CI proves fail-first: `9a143760e` (test only) — **e2e_playwright_1 FAILED as intended** (CircleCI job 29589: 1 failed on "Next Steps should offer medication", the other 8 passed, so the flow up to Next Steps is sound) — then `18295f218` (fix). Verified locally: `elm make Main` (548 modules), elm-review clean, Playwright `--list` parses the spec. ⚠ Read while building: the HIV form still asks "Is partner known to be HIV positive?" on a negative result whenever the partner test is unrecorded (`hivResultFollowUpInputsAndTasks`, gated on `resolvePartnerHIVTestResult == TestIndeterminate`), so the defect only bites when the partner tab is saved after the HIV tab — which is the ordinary order, Partner HIV being the FIRST lab tab but answered "No / not performed" and revisited. The QA comment on issue #1349 (2024-10-20, "There is no Next steps activity … no Medical Diagnosis") was never answered and reads as this defect seen and lost. Sibling **B-237** (phase attribution, same diagnosis, one line below) becomes reachable more often once this ships — build it next, separately.
- 🔎 **Reviewed 2026-09-06 (`/code-review medium`, user-launched): no correctness findings, five quality findings, all posted inline on PR #2204 and resolved.** (1) the fix is the eleventh hand-copied lab branch — a `let`-local `processPrenatalLabRevision` would collapse all ten — **LEFT AS IS by the user** (*"1 - leave as is"*); not filed as a follow-up. (2)-(5) fixed in `6dc30c3f0`: Partner HIV handled inside the tab loop (order built once, partner index moved to the end) with `answerYesNo`; `dismissWarningPopup` docblock placement; spec uses `clickSubTaskTab`; helper option is now `hivResult?: 'positive' | 'negative'` instead of `hivPositive` (two existing callers updated). ⚠ Process lesson recorded the same day: the fail-first proof was run through CI (two ~30-min cycles) because the local e2e run needs the shared main tree; the runbook asks for a LOCAL stash-and-rerun and treats sibling-mirroring fixes as mechanical — see the user's question and my answer in the session; rule change pending the user's choice.
