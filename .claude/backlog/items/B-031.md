<!-- id:B-031 status:IMPLEMENTED tier:3 round:10 -->
### B-031 — NCD creatinine/BUN/ALT/AST results: clear-goes-stale (missing dirty flags; lipid panel in the SAME file has them) — ✅ IMPLEMENTED 2026-07-19 (issue #1959, PR #1960, branch `b031-ncd-lab-result-dirty-flags`, base develop @c9d08268a, commit cf62c63c9, Copilot requested). Fix mirrors the lipid panel in the SAME 3 files: added `creatinineResultDirty`/`bunResultDirty` + `altResultDirty`/`astResultDirty` to the two form aliases (empty forms converted positional→record-literal), set `…Dirty = True` in the 4 Set handlers (`SetCreatinineResult`/`SetBUNResult`/`SetAltResult`/`SetAstResult`, RecurrentActivity/Update), and swapped `or`→`maybeValueConsideringIsDirtyField form.XDirty …` in `creatinineResultFormWithDefault`/`liverFunctionResultFormWithDefault` (Measurement/Utils ~7871/7904). executionDate/executionNote stay bare `or` (matches lipid). Reachability CONFIRMED: initial NCD offers all 3 on `recurrentTestRequired 12`, recurrent shows result form on `wasTestPerformed .creatinineTest/.liverFunctionTest` — no flag/diagnosis gate. All symbols already exposed+app-used (no new exports → NoUnused.Exports unaffected). 7 NEW tests in existing Measurement/Test.elm (aggregated into `all`); DISCRIMINATION: revert logic (keep flags) fails exactly the 4 cleared-stays-cleared (leaking Just 1.2/15/30/25), guards pass. elm make Main OK, elm-test 2845, elm-format+elm-review cold clean. Local /code-review high: no findings. COMPLETES the R9-4 class (#1883) — the two forms R9-4 wrongly excluded as 'separate design'.
- `Measurement/Utils.elm:7843-7847` (`creatinineResult/bunResult = or form.X value.X`) and :7878-7879 (alt/ast) vs lipid sibling :7930 (`maybeValueConsideringIsDirtyField form.totalCholesterolResultDirty …`). Handlers in `Pages/NCD/RecurrentActivity/Update.elm` (~:304/:321/:349/:366) set no Dirty.
- Completes the R9-4 class (R9-4's agent wrongly ruled these out as "separate design"). Consider folding into open PR #1883 if it hasn't merged by implementation time. Severity LOW (visible snap-back; value can't be retracted).
