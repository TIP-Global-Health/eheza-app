<!-- id:B-149 status:IMPLEMENTED tier:1 round:18 -->
### B-149 — ✅ IMPLEMENTED 2026-08-04 (issue #2035, PR #2036, branch `b149-low-birth-weight-threshold`, base develop @c37f2fe3c, commit 44f976879) — aggregated scoreboard LBW threshold 2000 → 2500 g
- ⭐ **PROVENANCE SETTLED — do NOT re-litigate.** The user asked whether 2.0 vs 2.5 might be intentional (individual vs aggregate). Traced it: commit `7a50180ca` → PR #709 → issues #708/#710. **#543** (jeanette-pelizzon, 2022-11-14) states the requirement: *"If the weight is below 2500 grams the child is underweight. There should be the RED callout next to it. In NCDA, this means that the scorecard will update accordingly."* **#687** (same author, 2023-04-17) defines the aggregated row as *"children born this month/year that had a birth weight in the red"* — i.e. defers to #543's threshold. The 2000 came from an ANALYSIS COMMENT on #708 by anvmn ("low birth weight is considered to be bellow 2kg") with no upstream source. ⇒ 2500 is correct on both sides. **Lesson: for any client↔server constant divergence, `gh api .../commits/<sha>/pulls` → PR body → linked issues + their COMMENTS finds the requirement; the issue BODY is often just a screenshot.**
- **Fix (user decisions 2026-08-04: one PR, floor only / NO upper bound, no SimpleTest):** PHP threshold → 2500 via new `HEDLEY_NCDA_LOW_BIRTH_WEIGHT_THRESHOLD`; unusable weights (`< HEDLEY_NCDA_MINIMAL_PLAUSIBLE_BIRTH_WEIGHT` = 300) now leave the key UNSET = unknown, instead of computing FALSE. Client mirrored: `resolveBornUnderweight` in `Pages/WellChild/ProgressReport/View.elm` returns `Nothing` below `getInputConstraintsBirthWeight.minVal`. ⚠ **The floor is NOT a stopgap** (user correction): #2008 can only convert the sub-10 kg-scaled band; **10–300 is unrecoverable and must stay dropped forever**.
- **De-duplicated the constant** (this is what let the surfaces drift): `2500` was at 3 client sites + `2000` at 1 backend site → now `Measurement.Utils.lowBirthWeightThreshold` (scorecard + red-callout check) and the PHP define, each commented with #543/#687 and a pointer to the other side.
- **Verification:** elm make 548 · elm-test 2968 · elm-format clean · elm-review COLD clean · php -l + phpcs Drupal/DrupalPractice clean. NO behavioural test: the client logic is a `let` binding inside a view (same ceiling as B-124/#2003), and hedley_ncda aggregation has NO test class at all (a guard = new SimpleTest class = full install). The 300 floor IS pinned indirectly by `birthWeightOutsideConstraintsTest` (299 outside / 300 inside).
- ⚠ **DEPLOY STEP REQUIRED — `low_birth_weight` is PRECOMPUTED into `person.field_ncda_data`** by the AQ worker; the scoreboard only reads the stored blob. Nothing changes on live until `drush scr .../hedley_ncda/scripts/generate-data-for-all.php` runs PER SITE after deploy. In the PR body + must go on the release checklist.
- 📌 Visible change: for the ~1,817 kg-entered children the "Born Underweight" row DISAPPEARS from the scorecard instead of reading "Yes". They stay in the newborns denominator (birth-date based), excluded from the numerator.
- 🔎 **REVIEW ROUND (`/code-review medium`, 2026-08-04) — 4 findings, 2 acted on, 2 declined.** ⚠ The review agent had NO `ReportFindings` tool, so it posted NOTHING to GitHub and returned text inline; I posted all four as inline PR comments by hand (`gh api .../pulls/N/comments -X POST` with the FULL 40-char SHA, `-F line=`, `side=RIGHT`; a line outside a diff hunk is rejected, so anchor to the nearest in-hunk line and SAY SO in the body). Findings: (1) no backfill → already documented; added the regeneration note to the constant's comment instead; `hook_update_N` declined (drush-script convention). (2) client/PHP fallback divergence → FIXED in `14eac8d16` (fallback now gates on `< MINIMAL_PLAUSIBLE` not `empty()`). (3) one-sided gate (no maxVal) → **mechanism REFUTED**: the range check BLOCKS the save on both paths (`Pages/WellChild/Activity/Update.elm:211-219` "Nothing is saved until they are within range"; `Measurement/View.elm:2541-2557` diverts the NCDA save button on every step) — the reviewer's evidence was a unit test of the predicate, not proof of persistence. (4) no test → declined per user scoping.
- ⚠ **I OVERSTATED finding 2 and had to correct it publicly.** Called it a live regression; the user pointed out birth weight is taken ONCE, and the code agrees: `showNCDAQuestionsByNewbornExam` (`Measurement/Utils.elm:466-475`) shows the questionnaire's birth-weight input ONLY when the newborn exam has none → **the two sources are MUTUALLY EXCLUSIVE by construction**, so the divergence is LATENT, not reachable. Correction posted to the thread. **Lesson: before calling a two-source divergence live, check whether the second source can exist at all — the form gate decides that, not the resolution code.** (Same fact voids the "$ncdas order vs List.head" note — with one weight ever recorded, both pick the same one.)
- 📌 Live impact query written but BLOCKED by the permission classifier (`ddev exec` denied; `ddev terminus-auth` itself worked). Script left at `.git/b149.sh` in the main tree — run `ddev exec bash .git/b149.sh <site>` for the birth-weight distribution per site, then delete it.

Original entry:
- `hedley_ncda.module:530` `$birth_weight < 2000` (verified) vs client `Pages/WellChild/ProgressReport/View.elm:1762/:1783` `< 2500` (verified; WHO definition, client-side since 2022-11-28/41fbc30d8; PHP wrong since hedley_ncda's creation). Consumer: aggregated scoreboard LBW % (`server/elm .../Scoreboard/View.elm` ~:209/:241-250). Babies 2000-2499 g — most LBW per WHO — show "born underweight: Yes" in the app but are missing from the aggregated count. ⚠ SAME FIELD as B-122's kg-corruption: fix the threshold AND note the count stays garbage until #2008's data repair (1,817 kg-entered records are all < 2000 too — the two indicators interact; a `>= 300` sanity floor per the new gram constant would exclude the corrupt rows from BOTH).
- Same Unit-2 report, smaller siblings (record here, one entry): **(b) MODERATE** — CHW ChildScoreboard NCDA-held weights feed NO z-score (no presave computes for NCDA `field_weight`; aggregation reads only weight-bundle `field_zscore_age`, `hedley_ncda.module:566-580/:822-860`) → every CS-encounter weight is invisible to aggregated underweight. **(c)** `hedley_reports.module:3122/:3126` MUAC `<` vs `<=` = the PHP sibling of B-138 (same #1718 authorship date; `:4493` in the SAME module uses `<=` — fix together). **(d) LOW** — client scorecard pane `< -3`/`< -2` vs everything else `<=` (boundary-only, clinician-gated class). **(e) LATENT** — `hedley_ncda.module:804-806` `continue` on empty field_receive_option would skip MUAC/stunting accounting if a form change ever makes vitamin-A optional (harmless today, traced).
- Unit-1 counterpart is a keeper CLEAR: **z-score engines client-Elm vs PHP are behaviorally equivalent and tables CONTENT-verified identical (18,458 entries)** — all server reports ride on a correct engine.
