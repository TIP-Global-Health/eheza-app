<!-- id:B-101 status:READY tier:4 round:16 -->
### B-101 — NCDA AQ worker returns SUCCESS for nonexistent/hard-deleted person — READY · CONFIRMED · LOW · trivial
- `hedley_ncda.module:154` no FALSE check after node_load → `:440` notice + early `return FALSE` → worker `:164-170` unconditionally reports ADVANCEDQUEUE_STATUS_SUCCESS "data has been calculated". Fix: `if (!$person) { return FAILURE }` after load.
