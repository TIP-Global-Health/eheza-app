<!-- id:B-200 status:IMPLEMENTED tier:3 round:21 -->
### B-200 — ✅ IMPLEMENTED 2026-08-20 (issue #2123, PR #2124, range 20-600 per user) — Blood glucose: mg/dL field takes mmol/L-typed glucometer readings ungated — a diabetic 11.1 mmol/L (=200 mg/dL) can NEVER trip the diagnosis threshold — CONFIRMED-structural (bimodality + no gate verified) · ⚠ CLINICIAN-GATED for the band choice · tier 3 · ongoing 2022→2026 · R21 wave C
- Client interprets `sugar_count` as mg/dL: `diabetesBySugarCount` (Backend/Measurement/Utils.elm:3654) flags >126 fasting / ≥200 non-fasting; input labeled mg/dL; NO constraint constant exists (verified — glucose absent from every getInputConstraints* and from `RangedMeasurement`), not even on develop/#2007.
- Data: **392/11,697 prenatal + 38/603 NCD** `sugar_count` values are <20 — glucometer mmol/L readings typed into the mg/dL field, every year 2022→2026. Those rows can never satisfy >126/≥200 ⇒ gestational-diabetes/diabetes flags structurally missed on them.
- Fix on clinician sign-off: a min constraint (e.g. ≥20 mg/dL) blocks the unit confusion at entry; band choice is the clinicians'.
- 🔧 **Implementation 2026-08-20 (PR #2124).** `MeasurementBloodGlucose` added to `RangedMeasurement`; `getInputConstraintsBloodGlucose = { minVal = 20, maxVal = 600 }`; new site-free helper `bloodGlucoseOutOfRange : Maybe Float -> List RangedMeasurement` (the established `heightOutOfRange`/`muacOutOfRange` all demand `Site`, but two of the five update functions have none and glucose has no site variation — demanding Site would be false coupling). ⚠ **Scope surprise: FIVE entry points, not one** — Prenatal/Activity, Prenatal/RecurrentActivity `update` AND its `updateLabsHistory` sub-updater (lab-tech labs history, separate `LabResultsData`), NCD/Activity, NCD/RecurrentActivity. Only Prenatal/Activity had an out-of-range popup; the other four needed one built. The domain-neutral `Measurement.View.measurementOutOfRangePopup` (used by Nutrition/ChildScoreboard) is the right renderer — do NOT import prenatal `WarningPopupType` into NCD. `site` had to be threaded into `Pages.NCD.RecurrentActivity.View.view` and `viewLabsHistory` (+ 2 App/View call sites). 5 unit tests, discrimination-verified (widening the range fails exactly 2).
- 📊 **Range research 2026-08-20 (live ihangane, 12,322 values).** Distribution: <1 = 24 (zeros) · 1-9.9 = 17 · **10-14.9 = 384** · 15-19.9 = 27 · 20-29.9 = 20 · 30-59.9 = 10 · 60-69.9 = 190 · 70-99 = 8,107 · 100-126 = 3,137 · 127-199 = 344 · 200-399 = 49 · 400-600 = 2 · **>600 = 11** (incl. 1000, 1118, 12312.1). The 10-15 cluster is the unit confusion and the values prove it: 12 (×45), 13 (×42), 14 (×35), 12.5, 12.8, 13.8, 12.6, 12.9, 13.1 — one-decimal display = mmol/L, and 11-15 mmol/L = 198-270 mg/dL, the diabetic-range readings worth recording. **Recommended band: 20-600 mg/dL** — floor sits below every meter's reportable range (~11-600; ADA Level 2 hypo is <54 mg/dL, so 20 never rejects a real emergency reading), ceiling is the meter maximum. Would catch 452 rows at the bottom + 11 at the top. ⚠ Cannot catch mmol/L values ≥20 (the 20 rows in 20-29.9 are genuinely ambiguous: mmol/L severe hyperglycaemia vs mg/dL near-fatal hypoglycaemia) — only a unit selector or detection heuristic would, still a product decision. Sources: ADA Standards of Care 2025 §6; FDA blood-glucose monitoring guidance. (Detection-side heuristics — treating <20 as mmol/L and converting — are a product decision, not proposed.)
