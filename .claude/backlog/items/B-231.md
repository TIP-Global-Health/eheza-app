<!-- id:B-231 status:IMPLEMENTED tier:3 round:24 -->
### B-231 — Case Management TB pane: a patient with a TB follow-up newer than their acute-illness TB-suspect follow-up disappears from the pane — ✅ IMPLEMENTED · CONFIRMED (anchor verified; scout ran an elm-test probe: both output dicts empty) · MED-HIGH · tier 3 · R24 unit 3
- **Mechanism.** `Pages/GlobalCaseManagement/Utils.elm:363-391`, the fold merging TB-encounter follow-ups with AI TB-suspect follow-ups keyed by person. When the person is in both: if the AI item is newer it REPLACES the TB item (`Dict.insert … accum`); otherwise the branch returns `accum` UNCHANGED — the TB item is never inserted — while the AI item is removed from the second dict in both branches. The person ends up in neither `forExisting` nor `forNew`. The comment says "no matter if it's used or dropped" about the AI item; the code also drops the TB one.
- **Reachability.** `View.elm:153-168` `tuberculosisPane` (CHW, `tuberculosis_management` on — both live sites per B-191). The pair is open at once (a) in the offline window between a CHW recording a TB follow-up and `hedley_case_management_resolve_tuberculosis_follow_ups()` (`hedley_case_management.module:390-430`) resolving the AI row server-side and that resolution syncing back — hours to days on a village tablet; (b) persistently when the AI TB-suspect follow-up uploads AFTER the TB node (the trigger resolves rows with `nid <` the new node, so a late-uploaded AI row resolves the TB row instead and stays open).
- **Impact.** The TB patient under follow-up — exactly who the pane is for — is the one hidden. No site split.
- **Fix shape (guess).** `else` branch → `Dict.insert (participantId, personId) item accum`. A 40-line probe test (one open TB item Jun 10 on a loaded TB encounter, one AI TB-suspect item Jun 1, currentDate Jun 15 → expect the TB key in `forExisting`) fails today and pins it.
- ✅ **IMPLEMENTED 2026-08-31 — issue #2171, PR #2172**, branch `B-231-tb-pane-drops-patient` off `fa079571d`. The entry's fix shape was right verbatim: `else` → `Dict.insert ( participantId, personId ) item accum`. The stale comment was extended to name both outcomes, since describing only the replace case is how a reader confirms the `else` and moves on.
- ⭐ **Why the defect survived review — three `else accum` branches within ~90 lines, and TWO ARE CORRECT.** `:296` (dedupe of acute-illness items, newest per person) and the HIV pane's `:450` both return `accum` correctly, because there the item is **already in** `accum` and returning it unchanged keeps it. At `:378` the fold walks a *second* dict, so the item is not in `accum` yet and returning it drops the record. Identical two lines, opposite meaning, decided entirely by whether the compared value is already accumulated. ⭐ The fixed `else` is now textually identical to the `Maybe.withDefault` branch below it — the tell that it was always the same decision ("keep the TB item").
- 📏 **Live measurement (terminus, 2026-08-31): ZERO patients hidden right now** on all three sites where `tuberculosis_management` is enabled — `ihangane`, `vhw` and `uvl` (verified by `vget`; the variable is absent on `tip-somalia`). Query: persons with an unresolved `tuberculosis_follow_up` dated at or after an unresolved `acute_illness_follow_up` carrying `field_acute_illness_diagnosis = 'tuberculosis-suspect'`, both `field_date_concluded IS NULL` and not `field_deleted`. ⚠ **Do not read that as "never happens":** window (a) is transient by construction, so a point-in-time query cannot see a state that opens and closes between syncs. The follow-up count — how often the pairing occurs at all, resolved or not — was **blocked by the auto-mode classifier** and is still unmeasured.
- ✅ **REGRESSION TEST ADDED, and discrimination-proven** — the first item in this stretch with a testable seam. `generateTuberculosisFollowUps` is exposed, `emptyModelIndexedDb` exists, and `Pages/GlobalCaseManagement/Test.elm` already existed (its `filterResolvedFollowUps` block), so three cases folded into an existing module: TB newer, same-day tie (falls to TB), and AI newer. **Reverted the fix and re-ran: the first two FAIL, the third passes** — it pins the branch that was already right, so an over-correction that inverted the comparison would be caught too. 3,033 tests pass with the fix.
- **Verified.** `elm-format --validate` clean · `elm make src/elm/Main.elm` → *Success! Compiled 548 modules* · `elm-test` **3,033 passed / 0 failed** (3,030 before) · `elm-review` *I found no errors!* on a cleared `elm-stuff`.
- ⏭ **B-233 is adjacent and still READY** — starting an encounter from an AI-derived TB entry in this same pane creates a duplicate participant. Separate defect, deliberately not folded in.
- 🔍 **Reviewed (medium) 2026-08-31 — no findings.** Three things it verified that I had not: **no double-listing** (the kept item still `Dict.remove`s the person from the acute-illness dict, so they cannot also appear in the second "new participant" dict at `View.elm:157-166` — the obvious risk of inserting in both branches, and it does not materialise); the **empty `personName`** on the TB-derived item is harmless because `fillPersonName` (`Utils.elm:476`) overwrites it from `db.people` for both dicts before the view; and the **remaining drop paths** (no `encounterId`, or an encounter not yet in `db.tuberculosisEncounters`) are pre-existing and benign — there the acute-illness item survives in the second dict, so the patient still shows.
- ✅ **Its one minor observation is fixed in `f3cedd841`:** the third test asserted only `dateMeasured`, not that `encounterId` was carried over — the other half of that branch's contract, and the whole reason it is a record update rather than a plain insert (the acute-illness item is converted with `encounterId = Nothing`, so without the graft the pane has no encounter to open). Now asserts the pair. ⭐ **Discrimination-checked rather than assumed:** removing the `| encounterId = item.encounterId` graft makes this test — and only this test — fail (3032/1), then restored.

