<!-- id:B-168 status:UNKNOWN tier:3 round:19 -->
### B-168 — EDD trickle (user investigation 2026-08-13) — ⚠ **CONCLUSION REVERSED same day: the deployed FIXED build still drops EDDs. Read THIS block; the original diagnosis below it is WRONG.** — 👁 MONITORING (gate re-pointed 2026-08-24). ✅ **The deploy gate is MET: PR #2086's merge commit `caa405b52` is in `origin/main`, release 1.18.1 shipped 2026-08-23, and the `ihangane` Pantheon head is `a03f5086f` 2026-08-23 15:40.** The diagnostic is now live. Waiting on: **the first `TriggerRollbar IndexedDB` event carrying an action label (`set-edd-date`, `set-newborn`, or one of the 4 close actions)** in the Rwanda Rollbar project. Ends when one arrives — its cache-state string picks the branch (`failure` names the failing read, `loading` = lost reply, `missing` = eviction, `not-asked` = never fetched) — or when ~2 weeks pass with none, which would itself be evidence (the no-op branch is not the drop site after all). ⚠ Two lags before expecting anything: a device only emits once it activates the new bundle (SW update, which these tablets rarely take promptly — the B-150 lesson), and the event rides the offline `dbErrors` lane that drains at the NEXT sync. Two checks remain available and are the user's: chrome://inspect a dropper tablet for the stuck-vs-never-queued PATCH row, and the Rollbar build-version read on item #53.
- **⛔ The old-build diagnosis below was REFUTED in two steps.** (1) The user surfaced `field_version` on device users (written by `/api/report-state` from the RUNNING app at every sync; overwritten in place — no history, no timestamp): ALL 26 active droppers report **efbff4534**, the fixed build. I defended the old-build story via the lagging-indicator argument — version shows the UPLOADING build; creation happens offline earlier; updates download online but activate at the NEXT launch; and crucially **sync is incremental JSON that succeeds on links where the large atomic precache download fails repeatedly** (that asymmetry is real and durable — keep it). (2) **The ops fact that ended it: device 4556 was PREPARED on July 10, after the July-7 deploy** — so its 2/2 dropped datings (both clinically Aug 7, two app sessions) ran on the FIXED build. The June fix does not cover some real workflow.
- **Constraints any mechanism must now fit (all code-verified at efbff4534):** ONE `sw.post` site + ONE response handler for participant creation (the fix's cache-insert covers every dispatcher — Prenatal/Nutrition/NCD/HIV/TB/AI/CS Participant pages AND GlobalCaseManagement) ⇒ NOT a missed creation path; every LMP save dispatches SetEddDate (single dispatcher, same appMsgs list — and the LMP always arrives) ⇒ the drop is the `updateIndividualEncounterParticipant` no-op: `individualParticipants` entry not Success at dating-save time; deterministic per device/workflow (droppers 2/2, 3/3 — setters, incl. CHW village device 4542, always work); the participant PATCH is never queued (no second device revision server-side). Leading suspect class: restart between creation and dating + the page fetch landing Failure that never retries (fetch-failure caching), or an eviction/overwrite of the dict entry — UNFOUND, needs a dedicated trace of the CHW registration→dating flow.
- **RETRACTED ops advice: refreshing/updating tablets will NOT stop the trickle** — droppers already run the fixed build. Keep the nightly script (load-bearing). **The now-clearly-justified fix is server-side belt-and-braces: compute EDD in the LMP insert/presave hook** (populate-edd.php's rule at write time) — ends the symptom for every build and workflow, independent of the client mechanism. The client mechanism is still worth finding (the same no-op idiom may drop OTHER participant updates — outcome, newborn linking).
- Forward evidence still worth watching: any new empty-EDD pregnancy from 4556/3929/3976 with clinical dates after Aug 11-12 re-confirms fixed-build guilt; the script's daily output is the monitor.
- **ROOT-CAUSE HUNT SESSION 2026-08-13 (continued) — everything workflow-shaped ELIMINATED BY EXPERIMENT; hypothesis now environmental:**
  - E2E reproductions on the current build (all PASSED = not the mechanism): canonical CHW register→date flow; app RESTART between creation and dating; BATCH registration (two women interleaved); field-sequence replay (shells + canonical flow after). Local desktop Chromium cannot reproduce the drop.
  - **Killer fact: device 3976's entire Aug-11 batch is NINE nodes — one pristine canonical first encounter (person → participant → encounter → LMP → 4 more), nothing before it — and the EDD still dropped.** The failing flow is IDENTICAL to the passing one; the differentiator is the DEVICE, not the workflow. (Server nids within an upload batch preserve client-side creation order — the batch IS a replay log; durable technique.)
  - **NEW CONFIRMED side-finding from the replay logs: duplicate pregnancy shells** — 5604393 + 5604395 are BOTH antenatal participants for the SAME person 5528577, created by device 4556 in one batch, each with an empty auto-encounter. The deployed #1881 guard only blocks in-flight duplicates; something made the app NOT SEE the first pregnancy on the second tap. Same smell as the EDD drop: **a local read not seeing a local write.** (These shells also pollute the 4,609 NULL-EDD standing pool the script scans; B-140's zero-measurement class is adjacent.)
  - Verified NOT the mechanism (static, at efbff4534): single POST site + handler w/ cache-insert; single LMP dispatcher pairing SetEddDate; participant encoder emits `deleted` (view()'s strict `=== false` filter passes); encoder↔decoder round-trips incl. no-vid local rows; revision helper benign (`Dict.update`, removes only on deleted); 5-min forget can never hit current-page wants (re-stamp precedes partition in the same update; Tick every 10s keeps the cycle alive); upload lane has NO per-uuid dedup (all rows sent, localId order within shard).
  - **UNIFYING HYPOTHESIS (open): device-level IndexedDB reads failing/returning-empty after tablet sleep/wake on old Chrome/WebView builds** — first IDB reads after resume fail → by-person query returns [] → participant page offers CREATE again (→ shells) AND/OR the participant GET caches Failure → `shouldFetch` never retries (NotAsked-only gate) → SetEddDate no-ops for the rest of the session (→ EDD drop). One cause explains both symptoms, per-device determinism (per-device Chrome version), and desktop non-reproducibility.
  - **Decisive next checks (user-side): (1) ROLLBAR — browser/Chrome versions of events from dropper devices (4556/3976/3929/4583) vs setters (4542/4573/4591), plus any decoder-failure / patchFull-failure / triggerRollbarOnFailure events from them Aug 7-12; (2) chrome://inspect a dropper tablet — `shardChanges` for an affected uuid (PATCH row present-but-stuck vs never-queued halves the hypothesis space), Chrome version, IDB errors in console.** (3) Hardening that doubles as diagnosis: make `updateIndividualEncounterParticipant`'s silent no-op branch report to Rollbar — one small PR turns every future field drop into a stack-traced, versioned, device-attributed event.
  - **ROLLBAR CHECKS RUN 2026-08-13 (user-granted access, Site-Rwanda project):** 30-day window (post-deploy) holds only 10 items — **ZERO IndexedDB/InvalidState/Abort errors, ZERO participant-fetch or patch failures ⇒ the drop is silent at fleet scale (the fetch handler also reports nothing on Failure — silence ≠ IDB-hypothesis refuted, but nothing supports it either)**. Browsers seen on CHW tablets: Chrome Mobile 148/149/151 — MODERN, weakening (not killing — no dropper-device event was found to sample) the old-Chrome-IDB variant. Fleet-state discoveries: (a) devices in YEARS-DEEP download crawls — "Kinyonzo Village CHW Diogene" advanced base_revision 3.9M→4.6M (≈2023→2024; head is 10.7M) in 10 min of back-to-back GETs with intermittent Failed-to-fetch (#113, 34 occ/30 IPs — likely RevisionIdAuthorityAdd from-zero re-downloads or re-pairings); (b) SW-update download failures CONFIRMED (#7, 3 devices) — the sync-succeeds-while-update-fails asymmetry is real; (c) THREE Elm-runtime-KILLING uncaught exceptions on CHW tablets: #97 `str.charCodeAt` in `$elm$core$String$toInt` mid-person-registration (Chrome 149, "Akanyiramugarura Village Console"), #91 `str.trim` (3 occ), #117 `undefined.$` vdom corruption — an uncaught exception stops the Elm app dead until reload (candidate cause of abandoned half-done encounters, NOT of the selective EDD drop — same-cycle msgs die together); (d) telemetry shows devices FLAPPING through #pincode mid-session (repeated pincode↔page bounces within seconds); (e) sync-phase field: 5 of 26 droppers last reported "sync-start" (incl. top-dropper 3929) vs 0 of 3 setters — weak, not decisive. **NEW STANDING FACT: Rollbar person.id = the device user name; telemetry per occurrence includes the sync URLs with base_revision — device sync state is reconstructible from Rollbar occurrences.** Poison-record finding spun out as B-170.
  - ✅ **DIAGNOSTIC SHIPPED 2026-08-13 (issue #2085, PR #2086, branch `b168-report-dropped-participant-updates`, base develop @24a6adf88, commit 9ffccc7b5) — the silent no-op now reports.** `updateIndividualEncounterParticipant` receives the FULL cache state (`Maybe (WebData ...)` — Backend.Update no longer collapses via toMaybe) + an action label per message (`set-edd-date`, `set-newborn`, 4× close); the drop branch emits `TriggerRollbar IndexedDB (Plain ...)` (offline-safe dbErrors lane, drains at next sync) with action, participant uuid, and cache state (`missing`/`not-asked`/`loading`/`failure - <err>` via `viewErrorForRollbar`). The cache-state string splits the remaining hypothesis space: failure names the failing read, loading = lost reply, missing = eviction. Patch path untouched. Verification: elm make 548 · elm-test 2,974 · elm-format clean · elm-review on committed state via clone clean. +66/−17, 2 files. ⚠ Built in a scratchpad WORKTREE off origin/develop (main tree was OCCUPIED by `b166-notification-reschedule` — a parallel session; the develop-in-the-main-tree rule yields when the tree is taken); worktree released after push per the standing rule. User decision 2026-08-13: NO server-side EDD hook for now — the Jenkins script stays the safety net. Awaiting PR review/approval per hard rule 3.

### B-168 (ORIGINAL DIAGNOSIS — WRONG; kept only for the verified sub-facts and method) — "pre-fix builds + no forced update"
- **Question asked:** populate-edd.php (nightly Jenkins) still finds LMP-bearing pregnancies with empty EDD (~2/day) although the fix (PR #1789, commits 05e7fffea + 9485cee1b, 2026-06-08 — client-only: SetEddDate silently no-ops when the participant isn't in `individualParticipants`; fix caches it on creation) was deployed 2026-07-07.
- **Evidence chain (each step verified):** (1) fix is client-Elm only, no server guard; (2) deployed bundle CONTAINS it — `app/Main.js` at Pantheon origin/master embeds `build:"efbff4534"` (= the #1765 release merge, descendant of the fix; deploy commit e52219e15 2026-07-07); (3) trickle rate UNCHANGED across the deploy (~8-9/wk June = July–Aug; weekly uid-0-revision-at-23h series); (4) behavior is **BINARY PER DEVICE**, same days, same CHW flow: droppers 4551/4552/4556/4566/4583 script-fixed on EVERY LMP-bearing pregnancy (single create-revision, then uid-0 at 23:54), setters 4542 (a CHW village device!)/4573/4591 client-set on EVERY one (POST+PATCH same-second device revisions) — kills "residual race in current build" and "CHW-flow-specific bug"; two builds in the field; (5) droppers and setters BOTH exist inside the July 8–10 mass CHW onboarding (≈60 devices) ⇒ **pairing date ≠ install date** — tablets provisioned before Jul 7 carry the pre-fix cached PWA; (6) `db_version` is the ONLY update-forcing mechanism (`validateDbVersion` rejects < HEDLEY_RESTFUL_CLIENT_SIDE_INDEXEDDB_SCHEMA_VERSION) and it is **32 on both sides since 2026-02-19 — pre-fix, not bumped at the release** ⇒ pre-fix builds sync forever; SW auto-update needs online navigation + accepting the waiting worker, which always-on offline CHW tablets rarely do (B-150 lesson confirmed at fleet scale).
- **The Aug-11 pair from the user's run:** participant 5604403 / LMP 5604405 ← uid 4556 ("akasedogo Village Sandrine 10th July 2026 Robot", 2/2 dropper); 5604768 / 5604770 ← uid 3976 ("Kamunana Village CHW Consolle June 27th 2025 Robot", old device). Both CHW; both created 2026-08-11; script set EDD 23:54.
- **Dropper fleet since deploy: ~27 distinct device uids** (top 3929=8, 4528=3, 4583=3; list query = participants with uid-0 revision at HOUR=23 since 1783382400, grouped by n.uid). Ops action: map uids→user names, refresh those tablets (online + close/reopen + accept update). The trickle self-resolves per device on refresh.
- 🔗 **B-170 folded in here 2026-08-13. ⚠ CORRECTED 2026-08-16: it is NOT evidence for the stale-fleet story — that story was refuted for the EDD trickle the same day (all 26 droppers report `efbff4534`).** B-170 is an INDEPENDENT poison-record incident; its own "the erroring devices must be on old builds" step is an INFERENCE, not a measurement, and it inherits the same doubt `field_version` cast on B-168. **Cheap decisive check: Rollbar attaches build version per occurrence — read it on item #53's post-July-7 occurrences.** If they report `efbff4534`, then a CURRENT client is failing a decode it should tolerate, which is a new and more serious finding than B-170 was closed as. What B-170 contributes regardless: Rollbar #53 (140 occ / 114 IPs / 25 devices / 2 years, latest ~2026-07-21): pre-18-May client builds `required "obstetric_history"` on `obstetric_history_step2`; client `1dac72459` + server `f620da93c` removed it in lockstep on 2026-05-18, deployed 2026-07-07. Current clients are fine — **stale ones now fail on EVERY step2 record**, and because the batch decodes as `required "batch" (list …)` (all-or-nothing) the device's downloads jam permanently at that `base_revision`.
- ⇒ **What survives the correction:** a coordinated client+server field removal shipped in July (client `1dac72459` + server `f620da93c`), and ANY client that lags such a change jams its download lane wholesale. That risk is real for the September release independent of who is lagging today — it is an argument for the `db_version` lever, but NOT proof that lagging clients exist right now. Settle it with the Rollbar build-version read above before treating it as either.
- **Systemic options (decision, not picked):** (a) bump the db_version gate at every release carrying client fixes — the proven forcing lever, but blocks un-updated devices from syncing until refreshed (queued data waits; client shows must-update); (b) small server-side belt-and-braces: set EDD in an LMP insert/presave hook (moves the script's rule into the write path — ends dependence on client builds for THIS field); (c) keep the nightly script running regardless until fleet convergence (it is load-bearing).
- 📌 Method keepers: revision-authorship (uid-0-at-23:5x vs device-authored) cleanly separates script-set from client-set EDDs — but plain uid-0 revisions on participants are CONTAMINATED by another bulk writer (~1,500 post-deploy across ~215 devices; not identified, not the EDD script — filter by the 23h window). Deployed client build is identifiable from `app/Main.js`'s embedded `build:"<sha>"` at the Pantheon remote. The February-2026 trickle drop (~25/wk → ~8/wk) predates the March deploy and is NOT explained by this investigation — noted, not chased.
- ⚠ Related latent (recorded, separate): HedleyRestfulSync passes empty date values through un-translated (`!empty($value)` gate at :471) — a participant patchFull carrying `expected_date_concluded: null` from a stale-cached client would WIPE a server-set EDD (B-150-class; plausibly the ORIGINAL "EDD turning empty" mechanism pre-dating the script). Not today's trickle (affected participants have NO second device revision), but worth a guard when (b) is considered.
