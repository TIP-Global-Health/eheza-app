<!-- id:B-038 status:UNKNOWN tier:3 round:11 -->
### B-038 — No handling for Dexie open/upgrade failure (VersionError on bundle rollback, quota-aborted upgrade) → device silently stops persisting sync — 🅿 **PARKED by user 2026-08-17 ("park B-038", no reason given) — do NOT re-pitch.** Mechanism stays CONFIRMED below so it is not re-found and re-filed.
- `app.js` Dexie setup (:10-386): zero `on('blocked')`/`versionchange`/`VersionError`/explicit `open().catch` (grep = 0). Rollback across a dbVersion bump → every page-context op rejects (DatabaseClosedError wrapping VersionError) forever; quota-aborted index-building upgrade (e.g. v30 `*national_id_number` rebuild over all of `shards`) retries and re-fails on every open. SW still serves reads (dynamic schema-less open) so the device LOOKS fine while every `sendSyncedDataToIndexDb` bulkPut fails.
- Fix: explicit `dbSync.open().catch` at startup — VersionError → blocking "update required" screen + Rollbar; quota → existing storage-full surface; add `on('blocked')` → Rollbar. Severity LOW-MED (needs rollback-across-bump or quota-mid-upgrade).
