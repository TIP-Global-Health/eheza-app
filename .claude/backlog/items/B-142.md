<!-- id:B-142 status:UNKNOWN tier:3 round:18 -->
### B-142 — hedley_admin demographics/outcome report scripts use 2022-frozen vocabularies (2 parts) — ❌ **CLOSED 2026-08-17 — LIVENESS GATE ANSWERED "These scripts are not in use" (user). MOOT, do NOT fix.** ⛔ Do not re-pitch either half; both mechanisms below are CONFIRMED but the reports are not run. · was MED / LOW-MED
- ⭐ **THE LESSON: a live Robo wrapper is NOT evidence a script is run.** My liveness case was (i) both scripts have Robo wrappers and (ii) both survived #2024's purge of nine dead scripts from the same family — and I read that as "somebody kept these deliberately". **Wrong: the user confirmed 2026-08-17 they are not in use.** ⇒ The `hedley_admin/scripts` declined class now has FOUR closures (R5-4, R5-5, B-109, B-142) and **zero** live members. For anything in that family, ASK FIRST and treat repo-side signals (wrappers, survival of a purge, recent commits) as worthless.
- 🗑 Optional follow-up, NOT proposed: the dead surface is `generate-demographics-report.php` (551 lines), `generate-closed-pregnancies-report.php` (34), `bootstrap-demographics-report.SQL`, `closed-pregnancies-outcome-completed-pregnancies.SQL`, and the three Robo tasks `reportDemographics` (`server/RoboFile.php:429`), `reportDemographicsHc` (`:439`), `reportPregnancy` (`:466`) — the same shape #2024 already did for nine siblings.
- **Liveness evidence gathered before the answer (R19, 2026-08-11) — kept only to show why it was misleading:** Both scripts have live Robo wrappers — `RoboFile.php:429 reportDemographics()` (execs `generate-demographics-report.php`) and `:466 reportPregnancy()` (execs `generate-closed-pregnancies-report.php`) — and BOTH survived #2024's purge that deleted nine dead scripts and the `reportNutrition` Robo task from the same family. Run model note: these tasks exec LOCAL drush against `server/www`, i.e. they are run against a pulled prod DB copy (ties into B-145's clone-PHI concern). Final gate = user confirmation that the team still actually runs them; asked 2026-08-11.
- (a) **"Impacted patients" omits ACHI groups**: `bootstrap-demographics-report.SQL:172` `field_group_type_value IN ('pmtct','fbf','sorwathe','chw')` — `achi` existed 16 months before the SQL was written (02b93dc27 2020-11 vs 626ca4ec5 2022-03) and the SAME reports count ACHI in encounter totals (generate-demographics-report.php:510-546) — so it's an omission, not policy. People served mainly via ACHI groups drop out of the ≥2-encounter "impacted" threshold.
- (b) **Closed-pregnancies outcome report shows non-pregnancy outcomes**: `closed-pregnancies-outcome-completed-pregnancies.SQL:7` blacklists only `referred-to-hc`/`illness-resolved`, but field_outcome carries 4 vocabularies — `patient-died`/`lost-to-follow-up`/`moved-out-of-ca`/`other` (AI, pre-dated the SQL) and `not-dignosed`/`hiv-not-dignosed` (TB/HIV, post-date it) leak in as spurious rows inviting misread (e.g. `patient-died` under pregnancy outcomes). Fix = whitelist the 5 pregnancy outcomes instead of blacklisting.

- 📌 **B-055 scenarios, from the #2047 review (2026-08-06) — concrete, and Burundi-only now.** The gate is `initialVaccinationDateByBirthDate`'s `VaccineDTPStandalone` branch, which reads `VaccineOPV / VaccineDoseThird`. (A) A Burundi child with OPV 1-3 but only TWO DTP doses gets DTP-SA scheduled at 18 months and reads OFF TRACK though it is not due. (B) A Burundi child with all three DTP doses but NO third OPV dose hits the `Maybe.withDefault (Date.add Years 999 birthDate)` branch and reads ON TRACK indefinitely. ⚠ After PR #2047 this affects **Burundi only** — Rwanda no longer reaches the branch at all. ⛔ Still clinician-gated, and the same lookup exists in the CLIENT, so fixing one copy alone re-introduces drift: both must change together.
- **Unit-5 enum sweep also UPGRADES B-055**: the client's DTP-SA gating reads OPV-3 (`Measurement/Utils.elm:9504-9505`, comment says "All 3 doses of DTP") while the LATER-written PHP engine checks `dtp dose-3` (`hedley_reports.module:5779`) — an authoritative second implementation matching the comment's intent. B-055 is now evidence-backed client-bug rather than ambiguous; clinician sign-off still needed to change scheduling behavior, but present the PHP evidence. (Also = B-131c's server/elm copy.)
