<!-- id:B-169 status:IMPLEMENTED tier:2 round:19 -->
### B-169 — CircleCI config declares `version: 2`, which stops compiling at the end-of-life date — ALL CI dies, and the date lands on the release month — ✅ IMPLEMENTED 2026-08-13 (issue #2083, PR #2084, ALL 10 JOBS GREEN under 2.1 incl. the full gated e2e+simpletest tier) · CONFIRMED (banner observed by user on the live workflow page 2026-08-13; config read at `.circleci/config.yml:1`) · HIGH as a release risk, TRIVIAL as a diff · release-prep
- **What CircleCI says** (banner on every workflow page + https://discuss.circleci.com/t/breaking-changes-config-compilation-updates-july-17-2026/54719): 2.0 config is end-of-life; from **2026-09-21** a pipeline still declaring `version: 2` **fails to compile** — i.e. no job runs at all, on every branch including develop and main. ⚠ **The release is expected ~September**, so this can turn CI off in the middle of the release window.
- Our config: `version: 2` at `:1` and the obsolete nested `workflows: version: 2` at `:4`. 256 lines, 10 jobs, and **zero 2.1-only features** (no orbs, executors, commands, parameters, matrix).
- **All 7 documented breaking changes checked against our file — every one is clean:** no heredoc `<<` anywhere in the config (2.1 reads `<<` as a pipeline expression); job names are snake_case only (no colons/parens); no job-level `branches:` filters; no kebab-case `working-directory`; no `<< pipeline.parameters.* >>`; `resource_class: large` is already a top-level job key at `:98/:152/:206` (sibling of `machine:`, not nested); `CIRCLE_COMPARE_URL` appears nowhere in `.circleci/` or `ci-scripts/`.
- **Fix: change `version: 2` → `version: 2.1`** (and drop the nested `workflows: version:` line, which 2.1 ignores). That is the whole migration for this repo.
- ⛔ **Cannot be validated locally — there is no `circleci` CLI on the host or in ddev**, and `circleci config validate --next` is the documented check. The only real proof is a pipeline run on a branch. ✅ **Proof obtained:** pipeline 6613 ran the identical 10-job set under 2.1 and every job passed, so the migration is behaviour-neutral, not merely syntactically accepted.
- 📌 Pre-push re-checks worth repeating if this file is ever restructured: YAML parses; all 10 jobs defined AND referenced; no workflow reference to a missing job; no orphaned job definitions; `resource_class` never nested under `machine:`.
- 📌 Untested hypothesis worth 30 seconds once it lands: the workflow page currently offers **no "rerun failed jobs" button** (user, 2026-08-13), which forces a full 10-job empty-commit push to retry one flaky job. Check whether 2.1 restores it.
- Sibling of the still-open B-157 (e)/(f)/(g) CI items — same file family, but this one is the only one with an external deadline.
