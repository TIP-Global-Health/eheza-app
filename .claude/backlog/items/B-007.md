<!-- id:B-007 status:REFUTED tier:4 round: -->
### B-007 — SW stale IndexedDB handles — SPLIT VERDICT (verification sweep 2026-07-06): statistics.js half ❌ REFUTED; nodes.js half CONFIRMED · LOW
- ❌ statistics.js VersionError claim REFUTED: both SW Dexie instances are DYNAMIC-mode (zero `.version()` declarations in statistics.js/nodes.js — grep-verified) → open() issues no version number → VersionError structurally impossible; Dexie 3.2.7's versionchange handler closes the SW connection during app.js's upgrade and statistics.js:75 re-opens per GET; dashboard Failure refetches every 20s (Backend/Fetch.elm:34-41). Adding the verno guard there is consistency-only.
- ✅ nodes.js half CONFIRMED (was the R11 refinement): `nodes.js:55-68` on verno mismatch re-inits `new Dexie('sync')` WITHOUT `.close()` (zero occurrences in file → one leaked connection per request in the window) and never RE-CHECKS verno after open → serves against the old schema; queries needing new-version-only indexes reject SchemaError → 500 → Elm Failure, and isNotAsked-guarded endpoints (e.g. stock mgmt, Backend/Fetch.elm:420-433) latch the Failure until reload. Window: new SW active + app.js hasn't opened the DB yet (no eager open; first sync port op is up to syncSpeed.idle=10min away, while /sw/nodes GETs start immediately). Self-heals on app.js's upgrade (versionchange closes leaked connections) or SW idle-termination; permanent only if new SW + old app.js coexist, which controllerchange→reload precludes.
- Fix: nodes.js — `dbSync.close()` before re-init + post-open `verno < dbVerno → 503 "DB upgrading"` (not 500, so Elm doesn't latch); statistics.js — add the verno guard for consistency; ROOT CAUSE: eager `dbSync.open()` at app.js startup forces the upgrade before Elm's first /sw requests. Constants all 32: sw.js:34, app.js:386, hedley_restful.module:8.

### B-007 (UPDATED by R11) — SW DB-handle guards: statistics.js has NO verno check at all; nodes.js checks but then proceeds against a stale schema and leaks a connection per mismatch pass
- `nodes.js:55-68`: on `verno != dbVerno` it re-inits (`new Dexie('sync')` — previous instance never `close()`d) via dynamic open and does NOT re-check verno after → queries needing v32-only indexes (`[type+family_participant]` :1107/:1114) throw SchemaError → 500 to Elm in the window after each version activation (app.js only upgrades on its first port op). Self-heals post-upgrade; permanent under rollback (pairs with B-038). `statistics.js:47`: `if (dbSync === null)` only.
- Fix: `close()` before re-init; after reopen, `verno < dbVerno` → 503 "DB upgrading"; add verno check to statistics.js. Severity LOW (transient 500s each deploy activation).
