<!-- id:B-357 status:READY tier:4 round:31 -->
### B-357 — Lab results pane: a "known positive" answer is always dated TODAY and so always ranked newest, and in the history view its result cell is blank — the NCD pregnancy row keeps showing "Known Positive" after a later negative test, contradicting the engine — READY · CONFIRMED (the dating line, the blank translation and the history cell path read by the orchestrator; scout scratch render; deployed) · LOW · tier 4 · R31 unit 6 · DEPLOYED; the NCD pregnancy exposure grows with B-289 (merged, un-deployed: pregnancy test offered at every NCD encounter); ihangane 6 live `known-as-positive` NCD pregnancy records, 0 elsewhere
- **Mechanism.**
  ```elm
  -- client/src/elm/Pages/Report/View.elm:616-630
          getTestResultsKnownAsPositive getValueFunc getResultFunc =
              getValueFunc data
                  |> List.filterMap
                      (\value ->
                          if value.executionNote == TestNoteKnownAsPositive then
                              Just ( currentDate, Just TestNotPerformedKnownAsPositive )
                          ...
                  |> List.sortWith sortTuplesByDateDesc
  -- :554-557 (translateTestReport)
          TestNotPerformedKnownAsPositive ->
              -- We don't need this, since this result is displayed
              -- using viewKnownAsPositiveResult funciton.
              ""
  ```
  The comment holds only for the summary row; the history view's `viewCustomEntry` (`:1195-1199`) maps the result through `resultToStringFunc` directly, so each known-positive line renders an empty result cell. Used at `:860` (HIV), `:869` (Hepatitis B), `:882` (NCD pregnancy).
- **Reachability / impact.** NCD: "known to be pregnant" in January + negative test in October → the row shows `<today> — Known Positive`, while `patientIsPregnantAtEncounter` (`Pages/NCD/Utils.elm:980-1022`) says not pregnant (later negative wins; a positive expires after 9 months). Two or more known-pregnant answers open a history of blank lines all dated today. Prenatal HIV / Hepatitis B: asked once (a known positive stops the test being offered), so only the date is wrong. Scout scratch render confirmed both.
- **Backlog de-dup.** B-289 (implemented — the engine side), B-343 (partner HIV report line, different mechanism). Not recorded.
- **Fix shape (guess).** Date the entry from the measurement's `dateMeasured` (or the encounter start date, as the NCD engine does) and give `TestNotPerformedKnownAsPositive` a real label in the history view.
