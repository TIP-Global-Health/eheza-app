<!-- id:B-187 status:READY tier:3 round:20 -->
### B-187 — Prenatal completion: medication-distribution port drifts ×6 (preeclampsia continuation, Mebendazole-on-missing-questionnaire, Aspirin sets absent, anemia carve-out, HIV-signs default, postpartum scope) — READY · (a)-(d) CONFIRMED, (e)-(f) PLAUSIBLE · tier 3 · same Completion-usage gate · R20 wave 4
- (a) **Continuous-hypertension function excludes the wrong preeclampsia population** (`..._expected_for_continues_hypertension_treatment` PHP:9594-9664, orchestrator-verified): early-returns FALSE on preeclampsia diagnosed PREVIOUSLY (:9597-9604); client excludes preeclampsia diagnosed at the CURRENT encounter and CONTINUES treatment for previous moderate preeclampsia (A:692-720; U:1344-1346, :222-247). That early return makes the function's second use-case (:9632-9661) DEAD CODE — and its BP condition is INVERTED anyway (returns TRUE on `sys>=180 || dia>=110`; client requires BP BELOW the hospitalization threshold). Net: under-expects `x` for previously-moderate-preeclampsia patients, over-expects for current-preeclampsia-on-prior-hypertension. Live today tiny (moderate-preeclampsia-recurrent 2, -initial 0 on rwanda). Fix: replace the early return with current-encounter exclusions; port branch 2 with the BP condition un-inverted.
- (b) **Mebendazole set expected when the medication questionnaire is ABSENT** (:9515-9533, orchestrator-verified): missing `medication` node ⇒ `$prescribed=FALSE` ⇒ TRUE; client requires the questionnaire answered (`Maybe.withDefault False`, U:1874-1893). Bites first nurse encounters at EGA≥24 (late first ANC visits — common). Over-expects `x`.
- (c) **Aspirin (risk-of-preeclampsia) sets missing entirely** (orchestrator-verified: grep `risk-of-preeclampsia` = 0 hits in the module): client triggers on `high-risk-of-preeclampsia-initial`/`-recurrent` + `moderate-risk-of-preeclampsia` (U:1851-1868, :1958-1959, :1966). Live: 12 moderate-risk, 0 high-risk (recent feature, will grow). Under-expects `x`.
- (d) **Moderate anemia given an adverse-event carve-out the client lacks** (:9438-9463): PHP skips when `field_anemia_treatment` has `adverse-events-hospitalization`; client expects unconditionally (A:505-512, RA:363). Small subset of 847 live anemia diagnoses.
- (e) PLAUSIBLE: HIV med-set — PHP requires non-empty `field_hiv_signs` (:9395-9406) before "no HIV program at HC"; client defaults absent hivSigns to no-program ⇒ set required (U:2954-2959). (f) PLAUSIBLE: malaria (:9337-9359) + continuous-hypertension (:9362-9364) triggers also run for nurse-POSTPARTUM; client's postpartum branch has neither (A:517-534).
- 📌 Genre notes, no items: phase-flattened emergency gate (PHP suppresses both activities on the COMBINED emergency list :9087-9091/:9297-9301; client gates per phase) — same genre as the recorded mandatory-gate flattening; completed-side = "node exists" (:10814-10831) vs client's per-diagnosis sub-task requirements — over-counts completion for `r`/`x`. Gray, era-correct, not filed: HIV education for legacy records without `test_prerequisites`.
- ⭐ AUDITED-CLEAN (durable — the port is otherwise faithful, string-for-string vs the encoder): all diagnosis lists (emergency 18+3, hypertension 4, preeclampsia 4+4 incl. -ega-37+, diabetes 4, HIV 4, malaria 6, mental-health 4, syphilis 4+neurosyphilis pair, anemia phase strings), the ENTIRE hypertension treatment-update matrix (methyldopa ladder/carvedilol/amlodipine recommendation thresholds, BP grading 140/160/180 + 90/100/110 with max(), previous-diagnosis gate), latest-treatment reverse scan, diagnosed_previously semantics, EGA floor(days/7), all symptom-education wire names, PMTCT no-medicine values, vitamin-A/discordant-partnership/gonorrhea/trichomonas sets, mebendazole previous-prescription checks. NOT checked: hospital-referral port (token `4`, :9679+), CHW health-ed port, launch-date historical accuracy.
