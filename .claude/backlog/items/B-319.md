<!-- id:B-319 status:READY tier:1 round:28 -->
### B-319 — Prenatal labs (lab technician): a blood-smear order opens pre-answered "test will NOT be performed today" with the nurse's RDT reason, 2/2 complete, and one Save silently closes the order without a result — ✅ IMPLEMENTED 2026-09-09 (issue #2223, PR #2224, branch `B-319-blood-smear-order-precancelled`, base `c9cce14f1`) · CONFIRMED (initial value → result hydration → lab-tech view → save → labs bookkeeping all read; live-counted) · MED · tier 1 · R28 unit 2 · ⭐ DEPLOYED (all sites)
- **Mechanism.** The nurse's initial-phase path "RDT not run (reason), blood smear taken, results later" stores `executionNote = <why-not note>` and `bloodSmearResult = BloodSmearPendingInput` (`toMalariaTestValue`, `client/src/elm/Measurement/Utils.elm:2688-2710`). At the result phase `malariaResultFormWithDefault` (`:7514-7540`) hydrates `runConfirmedByLabTech = or form.runConfirmedByLabTech (resolveRunConfirmedByLabTechFromValue value)`, and that resolver (`:7982-7991`) answers `Just False` for any why-not note — it reads the RDT's reason as the lab tech's "No". For `isLabTech`, `malariaResultFormAndTasks` (`:4508-4530`) then renders `contentAndTasksLaboratoryResultConfirmation` ("Test will be performed today?" = No + the why-not select prefilled, `:4013-4065`), counts 2/2, and hides the result section (`if not isLabTech || form.runConfirmedByLabTech == Just True`). Saving as-is keeps the why-not note and writes `bloodSmearResult = BloodSmearNotTaken` (hydration maps `PendingInput` → `Nothing`, `:7528-7533`); `Backend/Update.elm:2187-2197` routes every lab-tech save through `generatePrenatalLabsResultsAddedMsgs`, which marks `TestMalaria` completed and drops it from the queue. Reopened, `bloodSmearTaken` is now False so the form offers an RDT result select for a test never run as an RDT.
- **Why malaria alone.** Every other test whose note is a why-not never reaches the result phase (`testPrerequisites = Nothing` → `expectUniversalTestResultTask` False); the pending smear is scheduled as `TestNoteRunToday` (`Backend/Update.elm:3206-3207`) and is the one path that carries a why-not note into the result phase.
- **Reachability.** Nurse → Prenatal → Laboratory → Malaria → performed today? No + reason → blood smear taken? Yes → immediate result? No → Save. Lab tech → Case Management → that encounter → Malaria. Nurse path (`isLabTech` False) is unaffected. No site split.
- **Live (ihangane, 2026-09-03).** `prenatal_malaria_test`: **40** orders currently `pending-input` (all with a why-not note — each shows the prefilled cancellation to whichever lab tech opens it); 151 completed smears (`negative`/`+` with why-not notes) show lab techs do work the flow. A cancelled order writes `not-taken` + why-not — the same signature as an ordinary "RDT not done" record (≈16,600 of those) — so past accidents cannot be counted.
- **Fix shape (guess).** In `malariaResultFormWithDefault`, derive `runConfirmedByLabTech` as `Nothing` when `bloodSmearResult == BloodSmearPendingInput` (the why-not belongs to the RDT, not to the lab tech), or give the smear order a distinct execution note.
- ⬆ **Re-tiered 3 → 1 on 2026-09-03 (user rule):** a defect in how a diagnosis or its care pathway is determined is tier 1 regardless of how many patients hit it. Severity label unchanged — it describes blast radius, the tier describes priority.

- ✅ **Built 2026-09-09.** The fix is the entry's guess, kept in `malariaResultFormWithDefault`
  rather than in `resolveRunConfirmedByLabTechFromValue`: the resolver is shared by ten tests
  through a `{ v | executionNote : TestExecutionNote }` extensible record, and `bloodSmearResult`
  exists only on the malaria value. All three readers of the malaria result form
  (`Pages/Prenatal/RecurrentActivity/View.elm` twice, and `Update.elm` via
  `toMalariaResultValueWithDefault`) funnel through that one function, so the list of readers is
  covered by the one edit.
- **Verified both ways against the running app** (main tree borrowed with the user's approval,
  returned to `develop` afterwards). elm-test: 5 new cases in `Measurement/Test.elm`, one of which
  fails against pre-fix `Utils.elm` (`Just False` where `Nothing` is expected). E2E: the assertions
  were folded into the existing `lab-tech-encounter.spec.ts` test; it **fails** on the pre-fix
  bundle (`.form-input.yes-no.test-performed input.checked` → expected 0, received 1) and passes
  on the fixed one. The served bundle was read out of `Main.js` before each run.
- ⚠ **The trap this nearly walked into twice.** `completeLabResults` clicks "Yes" on
  `.form-input.yes-no.test-performed` unconditionally, which *corrects* the pre-answered No on the
  pre-fix build as well — so the backend assertions (`field_blood_smear_result` = `negative`,
  note = `run-confirmed-by-lab-tech`) are guards, not discriminators. The discriminator is the block
  that reads the tab **before** the helper touches it. And the first version of that block asserted
  `label.active`: `viewCustomBoolInput` marks the selection on the `input`
  (`classList [ ( "checked", isChecked ) ]`), not the label, so it would have been count-0 on both
  code paths.
- **E2E scaffolding added:** `completeLaboratoryNurseForLab` gains `bloodSmearAtLab` (no spec
  created a pending smear order before), and `queryMalariaTest` reads the note and smear result off
  the backend. The person → pregnancy → encounters PHP preamble in `e2e/helpers/common.ts` was
  extracted into `pregnancyEncountersPhp` rather than copied a fourth time — it was already
  duplicated three times, byte-identical.
- **Reviewed 2026-09-09 (medium) — three findings, all fixed in `4b9948d18`, all three threads answered.**
  ⭐ **The headline finding's suggested fix was wrong, and only running it showed that.**
  `laboratoryResultTaskCompleted`'s malaria branch hand-rolls its own completion check and omits
  the `testNotPerformedByWhyNotAtExecutionNote` clause that the shared `testResultsCompleted`
  gives every other test, so a declined malaria test left the tab incomplete and
  `activityCompleted`'s `List.all` never let the Lab Results activity close. But adding that
  clause **alone** also matches the PENDING order — a smear ordered at the lab carries the
  nurse's why-not note too — so it reports an unread smear complete: the same class of bug this
  item exists to fix, moved one layer down. The landed fix returns `False` for
  `BloodSmearPendingInput` first. Three cases in `Pages/Prenatal/RecurrentActivity/Test.elm`
  separate the three candidate implementations, and **both** failures were run: pre-fix fails
  "declined → complete", the clause-alone version fails "ordered → not complete".
- ⛔ **Lesson: a review's suggested fix is a hypothesis, not a patch.** Write the test that
  separates the candidates and run every candidate against it, including the reviewer's.
- The two lesser findings: `value.bloodSmearResult == BloodSmearPendingInput` was evaluated twice
  in one `let` (now bound as `smearOrderedAtLab`); and the e2e `bloodSmearDone` guard was set
  before the order was actually placed. The latter is fixed at the save rather than at the
  immediate-result answer, because ⚠ **Elm's `customButton` renders a disabled Save with a
  `disabled` CLASS and no `onClick`, not the `disabled` attribute** — so Playwright sees it,
  clicks it, nothing happens, and the tab is still counted as saved. The locator is now
  `button.ui.fluid.primary.button:not(.disabled)`. That pattern appears ~10 times in
  `e2e/helpers/prenatal.ts`; only the one in `completeLaboratoryNurseForLab` was scoped.
- ✅ **Re-verified at `4b9948d18`:** 3118 elm-tests, `elm make` 548 modules, elm-review and
  elm-format clean, and the lab-tech e2e passes against a served bundle confirmed to carry both
  review fixes.
- 📌 **Nobody has looked at this in a browser.** Every claim about what the lab technician sees is
  read off the Elm view code and Playwright locator counts. The pre-fix failure screenshot
  Playwright saved was discarded by the later passing runs.
- **Reviewed round 2 (2026-09-09) — three findings; two fixed in `5a044bf12`, one split out as issue #2225.**
  ⭐⭐ **The HIGH one was a real regression this PR would have made the default, on a diagnosis
  path — and it was found only because the round-1 fix pulled a new file into the diff that
  round 1 had never seen.** A lab technician answering "Yes" makes `toMalariaResultValue`
  overwrite the note with `TestNoteRunConfirmedByLabTech`; `positiveMalariaTest`'s smear branch
  demanded one of the NURSE's five why-not notes, so their positive smear diagnosed nothing.
  `positiveMalariaTest` drives FIVE diagnoses — Malaria, MalariaWithAnemia,
  MalariaWithSevereAnemia and, **inverted**, ModerateAnemia and SevereAnemia — so a
  smear-positive anemic patient was actively rerouted to plain anemia.
- **The issue decided the fix.** `958bf916fc` wrote that branch for **#631**: *"they need a
  different way to diagnose malaria"* — the smear IS the diagnosis, and the note list was a
  Feb-2023 mechanism for identifying the smear pathway when only the nurse could leave a
  why-not note. `TestNoteRunConfirmedByLabTech` arrived 14 months later with **#1031**, a spec
  about documenting a lab tech's own refusal that never contemplated the diagnosis reading that
  field. So branch 2 now tests the smear result alone. ⛔ The reviewer's other option (preserve
  the nurse's note through the lab tech's save) was declined: it fights this PR's round trip —
  on reopen that note reads back as the lab tech having answered No.
- **Live sizing (ihangane, note × smear):** 9 positive smears exist, all under why-not notes, so
  all nine diagnosed correctly; the lab-tech "Yes" path is already in use (10 records) but has
  **never yet met a positive smear** (`run-confirmed-by-lab-tech` + `+` = 0), so the defect has
  not fired. Widening checked for the opposite error: `run-today` never pairs with a smear
  result (8,092 records, all `not-taken`), so no existing record loses a diagnosis.
- 6 new cases in `Pages/Prenatal/Activity/Test.elm`; **3 fail against `develop`** (both
  lab-tech-confirmed positives and the anemia misrouting), 3 pass on both as guards.
- ➕ **Issue #2225 filed** for round 2's third finding (pre-existing, deliberately not fixed
  here): a malaria test nobody performed is still asked for a RAPID-TEST result, because
  `expectLaboratoryResultTask` asks only `wasTestPerformed` and `bloodSmearTaken` is False for
  `BloodSmearNotTaken`. Needs backlog triage.
- ⚠ **`elm-review` reported a phantom unused `Backend.Measurement.Model exposing (..)`** after
  the edit; `rm -rf client/elm-stuff` cleared it. The documented trap, hit again.
- 📌 **Manual QA (2026-09-09) predates this commit** — it covered the lab tech's screen with a
  NEGATIVE smear, which is exactly why it could not catch this. The positive-smear diagnosis
  path is covered by elm-test only.
- **Reviewed round 3 (2026-09-09) — four findings, ALL pre-existing; none fixed here, all four
  answered on the PR.** ⭐ Round 3 found nothing wrong with the PR itself — the value of the
  round was three new backlog items in the same neighbourhood.
  - ➕ **#2226 (tier 1 — wrong diagnosis)**: a smear the lab tech DECLINES and then corrects is
    recorded as a rapid test. `bloodSmearTaken` is recomputed from the SAVED value alone, and
    `BloodSmearNotTaken` is not in its list, so the decline destroys the "a smear was ordered"
    fact; the correction is offered `standardTestResultInputsAndTasks`, and the resulting
    `testResult` + `TestNoteRunConfirmedByLabTech` satisfies branch 1 of `positiveMalariaTest`
    ⇒ malaria diagnosed from a rapid test nobody ran. ⚠ **Identical before and after this PR** —
    for `isLabTech` the result section was always hidden unless the answer is Yes.
  - ➕ **#2227**: the progress report'"'"'s blood-smear row demands `bloodSmearResultNotSet`
    (`PendingInput`/`NotTaken`), so **no read smear has ever appeared**, by any route, nurse-entered
    included. ⭐ The reviewer framed this as the lab-tech note breaking the first conjunct; the real
    defect is wider and pre-existing, and the condition is inverted against **#632** (*"It will only
    show up on the progress report if it has been run"*). Reading the issue is what corrected the
    framing.
  - #2225 got the "green in the task bar, dead in the form" detail appended (round 3 rediscovered
    round 2'"'"'s third finding from the other side).
  - The fourth (the lab tech'"'"'s "No" overwriting the nurse'"'"'s reason in the shared `executionNote`)
    is a data-fidelity note with no live misread — recorded on the PR as the same underlying
    problem as #2226: one field answering two questions.
- ⛔ **Lesson on review rounds:** rounds 1→2 each found a real defect in the PR; round 3 found only
  pre-existing neighbours. That is the signal the surface is clean — take it as the stopping point,
  and convert the findings to issues rather than growing the branch.
- **Merged `develop` in (`44b691584`, 2026-09-10)** after #2222 landed; #2224 is MERGEABLE/CLEAN again, and the stacked #2228 / #2229 stayed clean. One conflicted file, `client/e2e/helpers/common.ts`, and it was a real clash of two refactors of the same code: this branch extracted the person → pregnancy → encounter-ids PHP prelude into `pregnancyEncountersPhp` and used it in four queries, while develop generalised one of those four (`queryPrenatalDiagnoses` → `queryEncounterDiagnoses`, + `queryNCDDiagnoses`, + `minEncounters`) so its participant lookup had to stay parameterised by encounter type. ⭐ **Resolved by synthesis, not by picking a side:** the prelude became `participantEncountersPhp(personNameB64, encounterType)` — three pregnancy callers pass `'antenatal'`, the generic one passes its parameter — so develop's generalisation lands without reinstating the duplication this branch removed. Error string unified to develop's `'No participant found'` (log-only; nothing asserts on it, and `queryPregnancyEdd` keeps its own).
  - Verified: expansion is byte-for-byte develop's inline PHP (the helper's trailing newline puts `if (empty($encounters))` on its own line exactly as before) · `playwright test --list` loads all 23 spec files, 60 tests · elm make Success · **elm-test 3,159 pass** · elm-format clean · elm-review clean.
  - ⚠ **elm-review first reported 4 phantom "unused import" errors** in the three NCD files the merge took from develop — all `exposing (..)` wildcards, all clean an hour earlier on B-289. `rm -rf client/elm-stuff` → no errors. The memory note on this is exact; the stale cache was the other session's.
  - ⚠ Pushed with `[ci skip]` per the repo default, so #2224's checks still show the run from `5a044bf12`.
