<!-- id:B-109 status:REFUTED tier:3 round:16 -->
### B-109 — export-patients.php clobbers the child's name with the parent's + children header/row column mismatch — ❌ MOOT 2026-08-03 (the script was deleted)
- `hedley_migrate/scripts/export-patients.php` was removed as unused in PR #2024 (#1976), user-confirmed. The defect went with the file; nothing to fix. Original analysis kept below only as a record.
- (a) `:85` child's `resolve_name` result reused at `:184` for the adult → `:190-191` write the ADULT's names as the child's First/Last Name columns; children WITHOUT a relationship keep their own names → silent, inconsistent corruption feeding the patient-import utility. Fix: distinct variables. (b) `:294` children captions = 19 columns vs rows of 18 values (`:188-207`, no group value) → header-index consumers misalign the tail. Fix: drop the `Group Name` caption or append ''.
