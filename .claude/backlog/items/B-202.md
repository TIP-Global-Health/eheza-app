<!-- id:B-202 status:IMPLEMENTED tier: round:22 -->
### B-202 — client duplication batch: 16 merge items in 5 area PRs, ~320 app lines (+~200 tests/minors), ZERO missed-fix drift — ✅ IMPLEMENTED 2026-08-19 (all 5 PRs; #2108, #2111, #2112, #2113, #2114 — reviewed, findings fixed, open) · CONFIRMED (orchestrator spot-verified viewVaccinationOverview byte-identity, the Measurement/View label pair, and the resolveNextStep wrappers) · R22
- **PR-1 Prenatal** ✅ IMPLEMENTED 2026-08-19 (**PR #2111** r22-client-prenatal-dedup, all 5 items, zero drift, all gates green; ⚠ DEVIATION: viewVaccinationOverview landed in `Pages.Prenatal.Activity.Utils` NOT Pages/Prenatal/Utils — the prescribed target creates an import cycle via generateFutureVaccinationsDataByProgress; disclosed in PR body. Consequence: PR-2 no longer overlaps PR-1 and can base on develop): isNurseEncounter (survivor Backend/PrenatalEncounter/Utils:128, repoint Dashboard/Utils:840 via .encounterType; ⛔ Dashboard/View local let is SPV — keep); viewVaccinationOverview 56L byte-identical ×2 → Pages/Prenatal/Utils; viewMarkers (Prenatal copy of ZScore/View:69 — delete; NCD's colored variant KEEP); frame (FamNut copy of Prenatal Svg — delete; NCD white-fill variant KEEP); latestVaccinationDataForVaccine → generalize Measurement/Utils:9627 key type.
- **PR-2 shared Pages utils** ✅ IMPLEMENTED 2026-08-19 (**PR #2114** r22-client-shared-utils-dedup, all 4 items, zero drift, gates green; bonus: the nonReferralReason getter previously ran TWICE per caller — now computed once in a let, matching the repeated-expression rule): viewPhotoThumb (Pages/Utils survivor); viewPaneHeading+viewPersonInfoPane (NCD copies deleted; WellChild PR View is the de-facto shared home, 4 modules already import it); nonReferralReasonSection 31L byte-identical NCD/Prenatal → Pages/Utils taking currentValue (each module keeps its 4L form-getter); filterPreviousEncountersDataToDate via extensible record.
- **PR-3 Backend** ✅ IMPLEMENTED 2026-08-19 (**PR #2108**, branch r22-client-backend-dedup, net −127 lines, all 5 items, zero drift, all gates green incl. cold-clone elm-review; item-5 note: NutritionEncounterDataItem shares the fields but is decode-only — no encoder to unify): resolveIndividual{Nutrition,WellChild,ChildScoreboard}Values ×3 byte-identical → one generic (12 internal callers, 0 external); getParticipantEncountersByEncounterType generalize participant-id type; encodeIndividual/FamilyEncounterParticipantParams → one encodePeopleParams (same file); isDailyEncounterActive extensible record; Dashboard/Encoder zscore/muac let-block ×3 → helper.
- **PR-4 module-local** ✅ IMPLEMENTED 2026-08-19 (**PR #2112** r22-client-local-dedup, +26/-123, all 4 items, zero drift, gates green; item-4 generalization: the extracted viewFamilyMemberDetails takes the links markup as an argument — viewFamilyMemberLinks is module-specific per Model/Msg): viewHealthEducationLabel ≡ viewActionTakenLabel (Measurement/View same file — delete first, 1 caller; the 5-arg AI namesake is different, untouched); resolveNextStepFirst/SubsequentEncounter → one resolveNextStep (callers Backend/Update:9894/10004); closeEncounterMsgs ChildScoreboard ×2 (expose from Encounter/Update, imports verified acyclic); FamilyNutrition member-details 53L block ×2 → helper.
- **PR-5 test fixtures** ✅ IMPLEMENTED 2026-08-19 (**PR #2113** r22-client-test-fixtures, +194/-341; new `src/elm/TestFixtures.elm`; per-suite differences preserved via record-update overrides with the EXACT original values — Prenatal 1990 birthDate, per-suite dummyDates, encounter types; NCD urineProteinValue + Prenatal urineDipstickValueWith kept local, behaviorally different; test count identical 2978→2978; one config touch: TestFixtures added to NoUnused.Exports ignore list per the existing four-file 'exposed only for the unit tests' precedent): shared test-fixtures module for testPerson ×2(+3 near), wrapMeasurement ×3, dummyParticipant ×2, vitals/urine value builders.
- **DRIFT: none** — all 8 most-suspicious 0.9x pairs diffed DELIBERATE (CHW-vs-nurse suggestDoseToday, Initial/Recurrent phase splits, HIV↔TB type parallels, NCD chart styling). **KEEP-LIST (durable):** Dashboard SPV isNurseEncounter; calculatePercentage; emergencyReferalRequired + resolveRequiredReferralFacilities + toHealthEducationValueWithDefault Prenatal pairs (phase-different innards — textually identical wrappers MUST NOT merge); resolvePreviousMaybeValue (nurse-only by design); legacy-vs-universal lab form pairs; Fetch one-line delegations (App/Fetch convention); Clinical vs IndividualEncounterTypes skeleton; Prenatal Svg chart-constant pairs.
- **FAMILY dismissals (durable):** HIV↔TB parallel program (~25 groups — unifiable only by program parametrization = restructuring, out of scope); per-program to*Value pipelines; within-client vaccination-engine bindings (shared engine already in Measurement/Utils; cross-app = B-131/B-055 recorded); per-program Update boilerplate clones (234L/213L/188L/166L…); encounter-page view/fetch scaffolds; decoder/empty-record families; universal lab-test parallel sections. Noise: 4 doc-comment false positives (it/need/rely/searches).
- Coverage frontier: Pass-1b pairs 0.75-0.89 untriaged; 20-30L same-file clones spot-checked only (SyncManager zipper blocks, Prenatal BMI-alert branches, ReportToWhatsAppDialog/Session 22L — noted as optional local extractions).
