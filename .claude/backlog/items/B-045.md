<!-- id:B-045 status:IMPLEMENTED tier:2 round:11 -->
### B-045 — Five photo classes + 2 single-ref classes inner-join on an OPTIONAL field → rows silently dropped from sync (exact siblings of B-011 stock_update signature) — ✅ IMPLEMENTED 2026-07-08 (issue #1914, PR #1915, branch `b045-b011-leftjoin-optional`, base develop @502d1cd73, CI running, Copilot requested). Closes B-045 photo half + B-011 in one PR. Fix: 6 mechanical innerJoin→leftJoin on file_managed (5 photo classes on field_photo :40/:47×4 + HedleyRestfulStockUpdate:124 on field_signature), mirroring HedleyRestfulPeople:120. SAFE no-other-change: post-processing already null-guards + client decodePhoto=decodeStringWithDefault "" (no poison). BOTH single-ref classes DROPPED (participant-consent retired; family-encounter Elm-guaranteed non-Maybe + sole writer — see scope note above). Verify: php -l + phpcs Drupal+DrupalPractice CLEAN (6 files); SQL demo on real tables (rolled-back txn) — dangling-fid nutrition_photo dropped by innerJoin, retained by leftJoin, valid-fid returned by both. NO new test (one-word change, People living reference, decoder-tolerant) — consistent with the CI-budget lesson [[ci-simpletest-time-budget]].
- `innerJoin('file_managed','f','f.fid = field_photo.field_photo_fid')` with `field_photo` required=0 in: `HedleyRestfulPhotos` (:40), `HedleyRestfulNutritionPhotos` (:47), `HedleyRestfulPrenatalPhotos` (:47), `HedleyRestfulWellChildPhoto` (:47), `HedleyRestfulFamilyNutritionPhoto` (:47). Correct sibling: `HedleyRestfulPeople.class.php:120` uses `leftJoin('file_managed')`. Also latent: `HedleyRestfulParticipantsConsent` (:38/:41, field_participant_form uuid inner-joined, required=0) and `HedleyRestfulFamilyEncounter` (:57, field_family_participant uuid inner-joined, required=0).
- Photo nodes with NULL fid or a dangling fid (admin file deletion, file-usage cleanup, bad clone/migration) vanish from the sharded feed → device misses the photo measurement; newest-revision case sticks the shard sync. App always attaches a photo today → latent, needs a dangling fid / non-app path.
- Fix: `leftJoin` mirroring HedleyRestfulPeople + NULL-tolerant post-processing/decoder (or require the ref fields for the 2 single-ref classes). BUNDLE with B-011 into one innerJoin-on-optional-field PR. Severity LOW-MED.
- SCOPE (Opus re-verified 2026-07-08): Sub-pattern A = 6 file_managed innerJoins (5 photo classes + StockUpdate/B-011) → clean innerJoin→leftJoin; PROVEN SAFE — post-processing already null-guards (`if (!empty($item->photo) && !empty($item->uri))`, identical to HedleyRestfulPeople:120) AND client `decodePhoto`=`field "photo" (decodeStringWithDefault "")` + the photo dict is `optional` (Decoder.elm:116/1305) → no poison. No test needed (People is the living reference). Sub-pattern B = uuid-innerJoin-on-optional-ref (B-044 shape): ⛔ **ParticipantsConsent DROPPED — participant-consent feature NOT IN USE anymore (user 2026-07-08), ignore all its issues**; HedleyRestfulFamilyEncounter:57 (field_family_participant) also DROPPED — verified 2026-07-08 it can NEVER be unset: Elm type `participant : FamilyEncounterParticipantId` is non-Maybe + only constructor requires it + encoder emits unconditionally (FamilyNutritionEncounter/Encoder.elm:21), Elm is the SOLE writer (no migrate/PHP create — grep-confirmed), and the join's target uuid survives soft-delete (no hard delete). So the innerJoin never drops → no live bug, no realistic trigger (unlike photos where file_managed CAN be deleted). ⇒ **B-045 actionable scope = ONLY Sub-pattern A: the 6-join leftJoin bundle (5 photos + StockUpdate/B-011)**.
