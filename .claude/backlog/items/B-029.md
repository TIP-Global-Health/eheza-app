<!-- id:B-029 status:IMPLEMENTED tier:2 round:10 -->
### B-029 — WellChild nutrition-assessment saves accept out-of-range height/weight (feeds z-scores); same shared forms are constraint-gated everywhere else — ✅ IMPLEMENTED 2026-07-08 (issue #1916, PR #1917, branch `b029-nutrition-constraints`, base develop @502d1cd73, head 4dbbfe819, **ALL 10 CI CHECKS GREEN**, Copilot round addressed). Anchors held (View.elm:911-912 disabled, saves :892/:898/:909).
- COPILOT ROUND (1 comment, VALID + substantive): test label said "85 kg" while asserting on 850 — and **85 kg IS inside the 0.5–200 kg range**, so the 8.5→85 decimal-slip example used in the issue/PR bodies to motivate the fix is NOT actually caught. Fixed in 4dbbfe819 (relabel + companion test pinning `outsideConstraints getInputConstraintsWeight (Just 85) == False`); issue+PR bodies corrected via `gh api -X PATCH` and given a "Scope of the guard" note. LESSON: when a fix's motivating example is a number, CHECK IT AGAINST THE ACTUAL CONSTANT — the guard's real reach may be narrower than the story.
- **SCOPE GREW: AcuteIllness `Pages/AcuteIllness/Activity/View.elm` MUAC (save gate ~:820) is un-gated too** — renders the same shared `viewMuacForm` (:773). Fixed in the same commit. (Un-gated set = WellChild {height,weight,MUAC} + AcuteIllness {MUAC}.)
- Fix shipped: new `Measurement/Utils.elm` helpers `outsideConstraints : FloatInputConstraints -> Maybe Float -> Bool` + `muacOutsideConstraints : Site -> Maybe Float -> Bool` (applies `muacValueForSite` FIRST — MUAC stored in cm, Burundi constraints in mm; forgetting this breaks every Burundi save). All 4 pre-existing gates (Nutrition height/MUAC/weight, FamilyNutrition MUAC) routed through them (dedup). New pure gates `nutritionAssessmentSaveDisabled` (WellChild Utils) + `physicalExamSaveDisabled` (AcuteIllness Utils). notTaken/skippedForms leg preserved (`heightFormWithDefault` hydrates skipped → `measurementNotTaken = Just True` → Save enabled). HeadCircumference left alone (NO `getInputConstraints*` exists for it anywhere — needs a clinical range; NEW BACKLOG CANDIDATE).
- NEW CANDIDATES surfaced while implementing (not yet queued, both clinician-gated): (a) **head circumference has NO `getInputConstraints*` anywhere** yet feeds `zScoreHeadCircumferenceForAge` — needs a clinical range before it can be gated; (b) **weight constraints 0.5–200 kg are too wide to catch a child's dropped decimal** (8.5→85 passes; range is shared with adult encounters) — age-aware constraints would need clinician sign-off.
- Verify: elm make Main exit 0; elm-format clean; **full-project elm-review 0 errors**; elm-test 2789 pass (2788 pre-Copilot-fix). **Discrimination ×3 (all confirmed):** (A) gates reverted to `tasksIncomplete` → 6 range tests fail, notTaken/completeness tests still pass; (B) drop `muacValueForSite` in `muacOutsideConstraints` → 3 Burundi tests fail; (C) `outsideConstraints` unset→False → 4 unset tests fail. Restored → 2788/0.
- ⚠ **TOOLING GOTCHA (new, cost ~15 min):** the main tree is on `upgrade-elm-0.19.2`, so its `client/node_modules` (symlinked into the worktree per runbook) holds **elm-test 0.19.2-0**, which refuses develop's `elm-explorations/test 1.2.2`. Workaround: `npx --yes elm-test@0.19.1-revision6 "src/elm/**/Test.elm"` from the worktree (host `elm` 0.19.1 is on PATH; no `node_modules/.bin/elm` exists, so the compiler resolves correctly). elm-review 2.13.5 matches both branches — safe. See [[elm-version-must-match-compiler-exactly]].
- MINOR (inert, recorded not fixed): `Pages/WellChild/Activity/Utils.elm` (~:424) and `Pages/AcuteIllness/Activity/Utils.elm` (~:349) hardcode `SiteRwanda` when calling `muacFormInputsAndTasks` for the task-count path. Harmless — only the discarded `inputs` half is site-dependent; the returned tasks list is not.

ORIGINAL ENTRY (verified 2026-07-06):
- WellChild: `Pages/WellChild/Activity/View.elm:911-912` `disabled = tasksCompleted /= totalTasks` — file has ZERO `withinConstraints` (grep-verified). Save msgs :892/:909.
- Sibling convention: `Pages/Nutrition/Activity/View.elm:255-263` (height) and :289-301 (MUAC) disable save via `withinConstraints getInputConstraints…`; group forms block too (`Measurement/View.elm` ~:383-393). Constraints in `Measurement/Utils.elm` (`getInputConstraintsHeight/Weight`).
- NOTE at implementation: WellChild's MUAC task is likely un-gated too (zero withinConstraints in file) — cover height+weight+MUAC. Out-of-range flows into `zScoreLengthHeightForAge`/`zScoreWeightForAge`/WFH, nutrition assessment, growth charts. Severity MED.
