<!-- id:B-375 status:IMPLEMENTED tier:3 round:32 -->
### B-375 — Patient merge: the acute-illness step looks participants up by `'acute_illness'` while the stored value is `'acute-illness'`, so no participant moves, and marking the duplicate deleted cascades through its participants and encounters to soft-delete the measurements just moved to the original — ✅ IMPLEMENTED 2026-09-17 (issue #2263, PR #2264, branch `B-375-merge-acute-illness-lookup`, base develop @332874f44, commit 28a13340e; both lookups read `HEDLEY_PATIENT_ENCOUNTER_TYPE_VALUE_MAPPING['acute_illness_encounter']`; consolidation test gains case 19 (acute-illness participant + encounter + vitals stay live on the original after a full merge) plus a `createAcuteIllnessEncounter` helper — runs in CI only; fail-first check run locally via drush in a rolled-back transaction with the same fixture shape: buggy → participant DELETED on duplicate, encounter DELETED, vitals DELETED; fixed lookup finds 1 → all three live on the original) · CONFIRMED (both literals, the mapping constant, the lookup query and the three cascade branches read by the orchestrator; scout ran `_consolidate_patients_execute` locally in a rolled-back transaction with a discrimination run) · MED · tier 3 · R32 unit 5 · DEPLOYED since `2d6b08d53` (2020-11), all sites; latent — 0 fingerprint rows on ihangane/vhw/uvl
- **Mechanism.**
  ```php
  // server/hedley/modules/custom/hedley_patient/hedley_patient.drush.inc:8-9
  const HEDLEY_PATIENT_ENCOUNTER_TYPE_VALUE_MAPPING = [
    'acute_illness_encounter' => 'acute-illness',
  // :1300 (validate) and :1327 (execute)
    $participants_duplicate = hedley_person_individual_participants_for_person($duplicate, ['acute_illness'], 'DESC');
  ```
  `hedley_person_individual_participants_for_person` filters `field_encounter_type_value IN ($types)` (`hedley_person.module:1358-1371`); the allowed value is `'acute-illness'` (`hedley_schedule.features.field_base.inc:238`). The execute step then re-points the measurements (`_associate_content_by_field($measurements, 'field_person', $original)`) but not the participants. Deleting the duplicate cascades (`hedley_person.module:~1282-1314`): person → `hedley_person_content_affected_by_deletion` (its participants) → each participant's encounters → each encounter's measurements via `field_acute_illness_encounter`, regardless of `field_person`.
- **Evidence.** Scout local run: buggy — `participant: deleted=true field_person=<dup>`, `encounter: deleted=true`, `measurement: deleted=true field_person=<orig>`; discrimination — `['acute_illness']` finds 0 participants, `['acute-illness']` finds 1, and moving it leaves participant/encounter/measurement live. Live fingerprint (AI measurement whose person differs from its participant's person): 0 on ihangane, vhw, uvl (inverse join returns 254,926 on ihangane, so the query shape works). The test class has no acute-illness case. The only visible hint is the preview line "Duplicate got 0 acute_illness Participants".
- **Reachability.** Admin form `admin/content/consolidate-patients` (`hedley_patient.module:24-32`, live since 2023-09 per scout) or `drush consolidate-patients`, for any duplicate with an acute-illness participant; validation uses the same wrong literal, so nothing blocks.
- **Impact.** The original patient's acute-illness history (encounters, diagnoses, follow-ups, case-management items) disappears from every device; participants stay attached to the deleted duplicate and B-218 means un-deleting does not restore them.
- **Backlog de-dup.** B-068 (relationships — done), B-217 (original == duplicate), B-218, B-219. Not recorded.
- **Fix shape (guess).** Use `HEDLEY_PATIENT_ENCOUNTER_TYPE_VALUE_MAPPING['acute_illness_encounter']` in validate and execute; add a merge test case.
