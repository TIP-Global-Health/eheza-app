<!-- id:B-122 status:UNKNOWN tier:1 round:16 -->
### B-122 — ✅ INPUT-GATE HALF DELIVERED (issue #1981 CLOSED 2026-07-27; gram-scaled `getInputConstraintsBirthWeight` shipped with the range-gate programme, verified on develop 2026-08-03/R17). **Remaining half = the DATA repair (~1,817 recoverable kg records) = issue #2008, blocked on customer decision** — tracked in the handoff, not here. · ⬖ SPLIT (input-gate half delivered; remainder tracked in the handoff)
- `hedley_ncda.module` (~:529): `$data['low_birth_weight'] = $birth_weight < 2000;`. `field_birth_weight` is **grams** (`WeightInGrm`, `Translate.Grams`), input at `Measurement/View.elm:3665` (`birthWeightInputsAndTasks`) with NO range check → any kg value is trivially < 2000.
- **LIVE NUMBERS (14,048 records, ihangane.live):** under 10 (kg entered, falsely flagged) = **1,817**; 10-499 (implausible, falsely flagged) = 165; 500-1999 (genuinely low) = **229**; 2000+ = 11,837. ⇒ **of 2,211 flagged, only 229 genuine — ~90% false, over-reporting ~9×.** Also 145 records >10,000 g, max **350,022** (=350kg). Column mean 3,029 confirms grams.
- **Proof they're kg:** most common sub-10 values are 3.0 (298), 2.8 (146), 3.2 (133), 3.5 (127), 2.9 (113), 2.5 (103) — textbook newborn distribution in kg.
- ⚠️ **DO NOT reuse `getInputConstraintsWeight` ({0.5, 200} KILOGRAMS)** — wiring it here REJECTS every genuine newborn (3000 > 200) while ACCEPTING all 1,817 wrong kg entries (3.0 is inside 0.5-200). Locks in the corruption AND blocks correct data. Needs its own gram-scaled constant (~500-6000). This is the concrete instance of the B-029 lesson: CHECK THE MOTIVATING NUMBER AGAINST THE ACTUAL CONSTANT.
- Consider whether the field should ACCEPT kg and convert, since nurses demonstrably enter kg 13% of the time — product decision, not just validation.
- **Second half = data cleanup decision:** ~1,817 sub-10 are mechanically recoverable (×1000, but confirm intent first); the 165 (10-499) and 145 (>10,000) are NOT recoverable. Until corrected, any report using `low_birth_weight` is unreliable.
- No birth-weight records on Burundi (vhw.live) yet — Rwanda-only today, but the same unguarded input ships to both sites.
