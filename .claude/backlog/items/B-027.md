<!-- id:B-027 status:PARKED tier:2 round:10 -->
### B-027 — WhatsApp screenshot pipeline: several failure paths emit no completion event → nurse trapped behind a Close-less modal — 🅿 PARKED by user 2026-07-13 (verbatim: "Never got complaint about issue described at B-027. Park it.") — do NOT re-pitch. Mechanism stays CONFIRMED; details below if ever revived.
- JS: `app.js:1470` `waitForElement('report-content', …)` gives up SILENTLY (`:1824-1826 if (tryCount == 0) return;`); `makeProgressReportScreenshot` (:1474+) bare IIFE — `caches.open`/`html2canvas` un-caught; in `canvas.toBlob` callback `formData.set('file', blob, imageName)` sits BEFORE the try — `toBlob` yields null for oversized canvases (code inflates height +500) and `formData.set('file', null, name)` throws TypeError. Only success/failure paths reaching `reportProgressReportScreenshotResult` dispatch `screenshotcomplete`.
- Elm: `Components/ReportToWhatsAppDialog/View.elm` `viewExecutionResult`: `maybeResult == Nothing → (Translate.EmptyString, emptyNode)` — NO Close action, content hidden, modal active; only escape is the event that never fires.
- Fix: every JS exit path must call `reportProgressReportScreenshotResult('failure')` (incl. a give-up callback for waitForElement + null-blob check); Elm: render Close even while result is Nothing. Severity MED (feature-flagged `report_to_whatsapp`, live in prod).
