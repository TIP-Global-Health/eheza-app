<!-- id:B-232 status:IMPLEMENTED tier:2 round:24 -->
### B-232 — Case Management immunization pane: a child is listed only on the day the follow-up was scheduled, never afterwards — the pane is effectively empty — ✅ IMPLEMENTED 2026-09-02 (issue #2190, PR #2191, branch `B-232-immunization-followup-pane`, base develop @5a7257e27) · CONFIRMED (anchor verified; scout probe: 1 item in, 0 entries out) · HIGH · tier 2 · R24 unit 3
- **Mechanism.** `Pages/GlobalCaseManagement/View.elm:1560-1591` `generateImmunizationFollowUpEntryData` is a copy of the nutrition version (:442-469; the stale `-- No Home Visit encounter found.` comment at :1589 is the tell). It takes the last non-NewbornExam Well Child encounter with `startDate < limitDate` and keeps the entry only if `Date.compare encounter.startDate item.dateMeasured == LT`. For nutrition the acting encounter (home visit) differs from the scheduling one, so "acting encounter older than the follow-up" is meaningful. For immunization BOTH are Well Child encounters: the encounter that saved the `nextVisit` measurement (`asapImmunisationDate`, `Utils.elm:220-243`) has `startDate == item.dateMeasured` (saved with `currentDate`, `Backend/WellChildEncounter/Update.elm:~391`), so `LT` is false and the entry is suppressed. It survives only on the scheduling day itself (that encounter is excluded by `< limitDate` because `viewImmunizationPane` :1534-1538 passes `currentDate`, not `Date.add Days 1 currentDate` like the five sibling panes :417/:539/:677/:804/:941) — i.e. while the CHW is still with the child — and while fetches are in flight (entries flash, then vanish).
- **Reachability.** CHW → Case Management → immunization pane (:146-151); data fully fetched (`Fetch.elm:60-65, 78-81`) — entries are computed then filtered away. Both sites, no flag.
- **Impact.** Children who left a well-child visit with immunisations owed are never listed for the CHW to chase. ⚠ Long-standing; if the user says the pane is not relied on, re-tier — but as coded the feature does not work.
- **Fix shape (guess).** Suppress only when a Well Child encounter STARTED AFTER the follow-up (`GT`), and pass tomorrow as the limit date like the siblings. Check whether `Pages/Dashboard/Utils.elm:1113-1140`, which shares the nutrition/AI/prenatal entry generators, needs an immunization counterpart (it does not read immunization today). Fixing the `LT` alone would expose the second defect: an entry stays live after today's visit and tapping it opens a second Well Child encounter the same day.
- 📌 Scout note, not raised: `viewImmunizationFollowUpEntry` (:1600-1605) labels ≤30 days overdue as `DueThisMonth` (green) instead of using `followUpDueOptionByDate` like every other pane — possibly deliberate.
- ✅ **IMPLEMENTED 2026-09-02 — issue #2190, PR #2191, commit `0d23c377a`.** Both halves, one file: `viewImmunizationPane` now computes `limitDate = Date.add Days 1 currentDate` like its five siblings, and the suppression test is `== GT` (branches swapped) so the scheduling encounter, which starts on `item.dateMeasured`, no longer counts as having answered the follow-up. The stale `-- No Home Visit encounter found.` comment is corrected. `generateImmunizationFollowUpEntries` is now exposed, joining the three sibling generators already exported for `Pages/Dashboard/Utils.elm`.
- 📌 **A fact the entry did not have, and it makes the pane's failure total:** `NextVisitValue.resolutionDate` is **never written anywhere in the codebase** — `emptyNextVisitForm` is all-`Nothing` and no `Msg` sets it — so `filterResolvedFollowUps` always returns `True` for immunization and the encounter comparison is the ONLY gate that can show or retire an entry. There is no second path.
- ⚠ **The entry's "fixing the `LT` alone would expose a second defect" warning does NOT hold.** `startFollowUpEncounterWellChild` (`Pages/GlobalCaseManagement/Update.elm:141-150`) posts a new encounter unconditionally, and the entry is ALREADY visible on the scheduling day today, so the same-day double-encounter risk is pre-existing and unchanged by this fix. Day-0 behaviour is identical before and after; only day-1-onward changes. Not a blocker; worth its own item if the user wants the guard.
- 📌 Entry's open question answered: `Pages/Dashboard/Utils.elm:1163-1205` wires only the nutrition, acute-illness and prenatal generators — no immunization counterpart to keep in step.
- 🅿 `viewImmunizationFollowUpEntry`'s hand-rolled `DueThisMonth`/`OverDue` split (instead of `followUpDueOptionByDate`) was raised and **left as is by user decision 2026-09-02**.
- **Verified:** elm-format clean · `elm make src/elm/Main.elm` Success (548 modules on the full build) · elm-test 3,044 pass · elm-review on the COMMITTED state via clone: no errors. **Discrimination test done:** 5 new cases added to the existing `Pages/GlobalCaseManagement/Test.elm` (which already reaches this module graph via `Pages/Dashboard/Test.elm` → `Pages/Dashboard/Utils.elm`, so no new OOM exposure); reverting the comparison fails 2 of them ("lists the child on the day the follow up was scheduled", "still lists the child on later days"). ⚠ **The limit-date half is NOT unit-covered** — it lives in the view function, which the test cannot call; it is verified by inspection against the five identical siblings.
- ⛔ Trap paid for here: `import Backend.IndividualEncounterParticipant.Model exposing (IndividualEncounterType(..))` collides with `Backend.TuberculosisEncounter.Model.TuberculosisEncounter` in that test file — the encounter-type variants share names with the encounter record types. Import it qualified.
- ✅ **Reviewed 2026-09-02 (`/code-review medium`) — no correctness findings, CI fully green (all 10 checks incl. simpletest + 3 e2e).** The reviewer's own mutation of `== GT` fails 3 tests, so the logic is discriminated. ⭐ **It also measured the CI OOM risk of importing a `Pages/**/View.elm` into a test module: 3.47 GB peak RSS vs 3.37 GB on develop, ~3%. Not a concern — this seam is safe to reuse.**
- 📌 **Two LOW findings raised, both posted inline on #2191, both LEFT AS IS by user decision 2026-09-02** (*"leave #2191 as is and file a new item"*):
  1. Resolution is day-granular, so a SECOND Well Child encounter on the day the follow-up was scheduled never clears the entry. Real — but reachable only through the Case Management tap itself, which is the one creation path with no same-day guard (the participant page has two: `resolveActiveEncounter` navigates into today's open encounter, `disableAction` blocks when one completed today). ➔ **split out as [[B-307]]**, which closes this case as a side effect.
  2. The pane's own `limitDate` line is untested. ⚠ **The reviewer's suggested remedy does NOT close it** — `run` calls the generator directly with a limit it computes itself, so no test case can observe a mutation to that line (their own mutation experiment showed all 3,044 stayed green). Only extracting a shared `followUpsLimitDate` helper used by both the pane and the test would, and the same three-line expression is copy-pasted into all six panes (`View.elm:411/:535/:672/:800/:937/:1536`) — a cleanup for its own PR.
- 🚢 **PR #2191 MERGED 2026-09-03** (`87320b4d5`), after a rebase onto `develop` that resolved two `Test.elm` conflicts against the B-307 merge.
