<!-- id:B-039 status:READY tier:4 round:11 -->
### B-039 — 13 residual strict-false `deleted` filters (incl. `view()` — the GET-by-uuid path for EVERY type) + a countQuery double-filter trap — READY · CONFIRMED · mechanical
- `nodes.js:424` (`view()`: `item.deleted === false`), :915/:918 (index() default, loose `==`), :1052/:1058, :1088/:1094, :1109/:1115, :1136/:1142/:1148 — rows with `deleted: undefined` are invisible; the accepted convention (R8-3/#1875) is `!== true`. No proven live vintage (all current encoders emit `deleted`; v13 backfilled) → hardening, not demonstrated loss.
- Also :911-918: `countQuery = query.clone()` is discarded — `countQuery = query.and(…)` chains off the already-filtered query (double filter; harmless today only because count() runs before offset/limit). Fix: mechanical `!== true` conversion ×13 + `countQuery = countQuery.and(…)`. Severity LOW.
