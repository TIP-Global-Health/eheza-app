<!-- id:B-368 status:READY tier:4 round:31 -->
### B-368 — Dashboard minors ×3: nurse "Total High Risk Pregnancies" counts a woman twice; CHW home "Acute Illness Diagnosed" includes nurse encounters; HPV doses never reach the immunisation figures (latent) — READY · CONFIRMED at the anchors by the orchestrator · LOW · tier 4 · R31 unit 2 · DEPLOYED
- **(a) High-risk total double-counts.**
  ```elm
  -- client/src/elm/Pages/Dashboard/View.elm:1346-1357
          totalHighRiskPregnancies =
              newlyDiagnosesSyphilisPregnancies
                  ++ ...
                  |> List.map .identifier
                  |> EverySet.fromList
                  |> EverySet.size
                  |> (+) currentlyPregnantWithDangerSigns
  ```
  A woman newly diagnosed this month whose latest encounter has danger signs is counted in both terms. Over-count only; not measured live. Fix: union the danger-sign pregnancies' identifiers into the same set.
- **(b) CHW home card counts nurse encounters (medium confidence — intended meaning undocumented).** `View.elm:259-263` `encountersForSelectedMonth = getEncountersForSelectedMonth dateLastDayOfSelectedMonth assembled.acuteIllnessData` with no role filter, while `e71c0c846` (2023-12) added a CHW-only filter to the Acute Illness page (`View.elm:821-836`) and the sibling "Mothers in ANC" card passes `isChw = True` (`:266-267`). Scout: roughly 500 nurse vs 2,000 CHW non-none diagnoses a year on ihangane. Fix: same CHW filter — confirm intent first.
- **(c) HPV never emitted — latent.** `HEDLEY_ACTIVITY_WELL_CHILD_IMMUNISATION_BUNDLES` (`server/hedley/modules/custom/hedley_activity/hedley_activity.module:130-142`) deliberately omits HPV ("used only by NCDA"), but `hedley_stats.module:1713-1714` reuses it, while the client decodes HPV (`Backend/Dashboard/Decoder.elm:414`) and adds an HPV entry for Rwanda girls (`Pages/Dashboard/Utils.elm:1859-1865`) — girls 12+ would read "Behind" on the immunisation donut. 0 HPV nodes on ihangane/vhw/uvl today. Fix: a stats-specific bundle list including HPV (not the shared NCDA constant).
