<!-- id:B-136 status:READY tier:4 round:17 -->
### B-136 — Dead-constructor deletion batch from the R17 NoUnused run (~550-650 lines) — READY · PROVEN per-item (classification agent, spot-verified) · LOW · one PR (or split by feature)
- ⛔ **FIVE FALSE POSITIVES — DO NOT DELETE:** `Activity/Model.elm` `ContributingFactors`/`FollowUp`/`FamilyPlanning`/`Lactation`/`ParticipantConsent` are CONSTRUCTED in live code (`Activity/Utils.elm` activityFromString/nextStepsActivities/getAllMotherActivities; `Measurement/View.elm`). The one-off rule misattributes them: **elm-review-unused bug — a same-named `type alias` imported via `exposing (..)` (from Backend.Measurement.Model) shadows ModuleNameLookupTable resolution** (agent minimal-repro'd; worth reporting upstream). Deleting these kills live group activities.
- **Deletable (keep-lists verified — measurement types/decoders/`BackendAuthority*` sync entities all STAY so old records decode):**
  - COVID-era batch (~150-250 lines): Backend/AcuteIllnessEncounter `SaveCall114/SaveExposure/SaveHCContact/SaveIsolation/SaveTravelHistory` + HandleSaved branches + save* model fields; `Pages/AcuteIllness/Activity` `SetHealthEducation` (~:1071-1086) + orphaned `IsolationForm/HCContactForm/Call114Form/ExposureData` records (Model.elm ~:270/:335-358); `Types.elm` `ExposureExposure/ExposureTravel` (module exposes ExposureTask WITHOUT (..) — unconstructable). Removal commits: 70b8a97c5 + 33d987ade (Feb 2025).
  - Counseling pair (~40-60): `SelectCounselingTopic/SaveCounselingSession` + `HandleSaveCounselingSession` + counseling form field. ⚠ counseling sync plugin/decoder/endpoint LIVE — keep.
  - Aspirin trio (~70): page branches (`Pages/Prenatal/Activity/Update.elm` ~:1697-1737) + `aspirinForm` + backend `SaveAspirin/HandleSavedAspirin/saveAspirin` (`Backend/PrenatalEncounter/Update.elm` ~:155-163). Dropped by e5d422e95 (2025-07).
  - DownloadPhotos modes (~130-150): `DownloadPhotosBatch/DownloadPhotosNone` + `DownloadPhotosBatchRec` + ~12 branch clusters + `Pages/Device/View.elm:133-157` + `downloadPhotosBatchSize` flags plumbing (+ JS `photoDownloadBatchSize`). Mode hardcoded `DownloadPhotosAll` since 0d758f49b (2020-10); remaining "constructions" are self-reconstructions inside dead branches. NOT flag-gated (flags value written, never read).
  - Vestigial tab variants (~30): `Reports` ×3 (AcuteIllness/FamilyNutrition/WellChild Encounter), `Scorecard` (ChildScoreboard) — tabs render as nav links comparing `== Reports` (always False); the visible tab STAYS, only the impossible state goes.
  - Small fry: `DialogEndSession` + page `CloseSession` msg (~20, replaced by direct redirect 15b1710d6; Backend `CloseSession` stays — used by Pages/Participants), `CaseManagementContactsTracing` (~10, both branches documented dead ends), `NoECDStatus` (~10 incl. Translate id), `NoOp` ×2 (Prenatal backend + TB progress report, 4 lines each), `ExpectMale` (~5), `FetchFamilyEncounterParticipants`+`ForPeople` (~45 — judgment call: Feb-2026 forward-built scaffolding for family follow-ups that don't exist yet; ask before deleting).
- `ParticipantConsent` deserves a separate product conversation (activity commented out of getAllMotherActivities years ago; URL-only reachable) — NOT part of this batch.
- Also surfaced, not analyzed: `Restful/Endpoint.elm` `ReadOnly/ReadWrite/Open` (vendored dir, ignored by the standard config).
