<!-- id:B-008 status:IMPLEMENTED tier:1 round: -->
### B-008 — IndexedDB save-failure wedges download lane in `Loading` — ✅ IMPLEMENTED 2026-07-06 (issue #1902, PR #1903, branch `b008-download-lane-save-failure`, base develop @91fb401fb, CI running, Copilot requested). Fix: Option A (user choice) — failure branch parks the matching lane (table+timestamp guard) to SyncIdle, no completion dispatch, lastSaveError/banner unchanged; incident report DROPPED (backend field_incident_type is an allowed_values list — would need field-base change). 4 new tests in SyncManager/Test.elm (2 parking = discriminating vs old code, 2 guards); elm make Success (548), elm-review clean, elm-test 2756. SIDE FINDING recorded below in minors: Elm encodeSyncIncident sends "file-upload" but backend allowed_values are "files-upload"/"content-upload" — off-list value stored (no validation on programmatic save), label missing in admin UI; cosmetic. [HIGH REVIEW 2026-07-20: no findings — adversarially checked the 3rd download lane SyncDownloadAuthorityDashboardStats: REFUTED as a gap — stats completes on FETCH-success (Update:457 syncStatus=webData) with fire-and-forget save (timestamp=""), and IndexDbSaveSuccess `_ ->` no-ops AuthorityStats, so it CANNOT wedge on save-failure. Fix covers exactly the 2 save-dependent lanes (Authority/General); triple guard (table+syncStatus+timestamp) correct; correctly does NOT dispatch FetchedDataSavedHandle (no cursor-advance/data-loss); 4 tests (2 park-discriminating, 2 guards). CONFLICT-RESOLVED 2026-07-20: merged origin/develop (merge commit 77ea50c58), only SyncManager/Test.elm conflicted (b008 4 park tests vs B-050 3 pageAllowsBackgroundRefresh tests both appended to `all`) → kept both; Update.elm auto-merged (parking fix intact, confirmed by park tests passing). elm make 0, elm-test 2860, elm-format+elm-review clean, CI watching.]
- Handler: `SyncManager/Update.elm:2332-2343` `SavedAtIndexDbHandle … IndexDbSaveFailure` sets ONLY `lastSaveError` (R2-2 banner), no syncStatus change/no retry — lane completion reachable ONLY via save success (`Backend*FetchedDataSavedHandle`, :2291-2327). Lane set Loading at :163-171; HTTP-success handler leaves it Loading pending save round-trip.
- Loop: 30s timeout (`downloadRequestTimeout = (12000+3000)*2`, Model.elm:770-772; guard Update.elm:112-118) re-issues with `lastFetchedRevisionId` which only advances in the save-SUCCESS handler → SAME batch re-fetched every 30s forever. `determineSyncStatus` (Utils.elm:236-286) rotates downloads only on Success+empty; `_ -> noChange` for Loading/Failure. Timestamp guard (:224-229) blocks stale acks. JS always replies with distinguishable reason (app.js bulkPut catch → indexDbSaveErrorFromReason → StorageFull/Other). General lane structurally identical (:671-676/:766).
- Observable: storage-full → red banner (App/View.elm:206-214) + authority stuck "Downloading" + bandwidth burned re-downloading same batch every 30s; photos/stats lanes + SyncIdle never reached.
- Fix design (agent-traced touch points): in the failure branch map `indexDbSaveResult.table`+timestamp to the wedged lane; StorageFull → `syncStatus = SyncIdle` (retry pointless until space freed; banner already shown); Other → reset lane to NotAsked with bounded retry counter (mirror `SyncUploadPhoto Int` errorsCount pattern); optionally add Failure rotation cases in determineSyncStatus + SyncReportIncident for repeated failures. Do NOT dispatch `*FetchedDataSavedHandle` (would advance revision cursor = data loss).
