<!-- id:B-175 status:READY tier:3 round:20 -->
### B-175 — Dashboard stats: a person with no gender makes the WHOLE health-centre statistics payload undecodable — devices' dashboards never load — READY · CONFIRMED-mechanism (client hops orchestrator-verified; live near-miss measured) · latent TODAY only by coincidence · tier 3 · R20 wave 2
- Server: NULL gender passes through raw at `hedley_stats.module:1581` (SPV), `:1780` (NCD), `:2499→2526` group children-beneficiaries (the `:2514` guard checks ONLY birth_date; same shape `:2577-2604`, `:2786-2817`). Client: SIX `required "gender" decodeGender` sites (`Backend/Dashboard/Decoder.elm:96/:158/:169/:377/:422/:500`); `decodeGender` starts with `string`, so JSON `null` fails the decoder → `decodeDashboardStatsRaw` fails → the stats download batch (`SyncManager/Decoder.elm:445` `required "batch"`) rejects wholesale; the device refetches the same cached blob forever. Main measurement sync unaffected (separate lane).
- Live (ihangane 2026-08-17): 3 person nodes lack gender; 9 are pmtct_participant children + 1 has an individual_participant. **Not poisoning today only because**: all 9 group children ALSO lack birth_date (so the `:2514` birth-date guard skips them) and the lone individual participant is nutrition-type, whose path (`:2010`) doesn't emit gender. One genderless child WITH a birth date in a group — or with an SPV/NCD participant — poisons that HC's stats. vhw: 0 genderless referenced persons. tip-somalia (2026-08-18): 0 genderless persons at all — zero exposure.
- Fix: server-side skip rows with NULL gender exactly like the birth-date guard (client-side alternative: `decodeWithFallback` at the six sites — but a fabricated gender skews stats; skipping is honest).
