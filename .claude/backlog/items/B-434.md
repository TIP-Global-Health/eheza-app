<!-- id:B-434 status:READY tier:4 round:24 -->
### B-434 — Case Management HIV positive-result entry: a tap creates a second HIV participant when the patient already has an open one — READY · CONFIRMED (split out of [[B-233]] 2026-09-24; anchors read at `8c3e29072`) · LOW · tier 4
- ⬖ **Split out of [[B-233]]**, whose TB half shipped as PR #2287; the user took the TB fix only.
- **Mechanism.** `generateHIVFollowUpEntryData` (`Pages/GlobalCaseManagement/View.elm`, dummy branch) resolves the person's HIV participants to date-check their last encounter, then builds `HIVFollowUpEntry Nothing personId item True`; `startFollowUpEncounterHIV` (`Update.elm`) posts a new participant for `Nothing`.
- **When.** The patient has an OPEN HIV participant with no `hiv_follow_up` (every encounter left before Next Steps, `Pages/HIV/Activity/Utils.elm` `TaskFollowUp -> True`) and all encounters older than a later positive HIV test from another programme. One extra participant per positive result — no loop: the new encounter hides the entry.
- **Live (ihangane, 2026-09-24):** 57 people with one HIV participant, 1 with three.
- **Fix shape (first guess):** reuse the open HIV participant in `startFollowUpEncounterHIV`, mirroring the TB reuse in PR #2287; a closed ("not diagnosed") participant correctly gets a new one.
