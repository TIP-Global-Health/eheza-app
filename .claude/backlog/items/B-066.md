<!-- id:B-066 status:IMPLEMENTED tier:2 round:14 -->
### B-066 — FamilyNutrition: caregiven (`MyCaregiven`) children are never fetched → they silently vanish from the encounter — ✅ IMPLEMENTED 2026-07-09 (issue #1921, PR #1922, branch `b066-famnut-caregiven`, base develop @1ca6a4662, commit 3bdefb540, CI running, Copilot requested).
- Mechanism CONFIRMED end-to-end incl. the two-layer fetch chain: `Pages/FamilyNutrition/Encounter/Fetch.elm` (takes encounterId) resolves `participant.person` and passes it into `Backend/FamilyNutritionEncounter/Fetch.elm fetch` — SAME person key the consumer `generateAssembledData` reads relationships for. So the one-filter fix is correct.
- Fix = DEDUP (B-048 pattern): the fetch filter `== MyChild` and the consumer filter `[MyChild, MyCaregiven]` had drifted. Extracted `isChildRelation : MyRelatedBy -> Bool` into Backend/Relationship/Utils (exhaustive `case` — MyChild/MyCaregiven True, MyParent/MyCaregiver False; forces a decision on future ctors), routed BOTH sites through it. Other `== MyChild` sites (PatientRecord, Prenatal/DemographicsReport) are DIFFERENT contexts — deliberately NOT touched.
- Verify: elm make Main 548 exit 0; elm-format clean; **full-project elm-review 0** (ran COLD to avoid the stale-cache artifact from B-048); elm-test 2765 pass (4 new in NEW `Backend/Relationship/Test.elm`). Discrimination: mutate `isChildRelation MyCaregiven` → False (the old bug) → fails exactly that test; restored → 2765/0.
- REACHABILITY caveat surfaced to user (they said implement): needs caregiver-headed FamNut family (MyCaregiven relation) + child not already in db.people; masked if FamNut participants are always biological parents; family_nutrition is feature-flagged.

ORIGINAL ENTRY (verified 2026-07-06):
- `Backend/FamilyNutritionEncounter/Fetch.elm:23` filters relationships to `.relatedBy == MyChild` only before `FetchPerson`, but the consumer `Pages/FamilyNutrition/Encounter/Utils.elm:67` `generateAssembledData` builds `children` from BOTH `[MyChild, MyCaregiven]`. `FetchRelationshipsForPerson` loads only relationship rows, not the related persons (explicit FetchPerson required, Backend/Update.elm:1441). So a caregiver-headed family's cared-for child never enters db.people → the consumer's `List.filterMap (Dict.get … db.people)` drops it → the child never appears as a selectable family member, its Aheza/MUAC/Photo can't be recorded, and auto-advance skips it.
- Fix: match the consumer — `List.filter (.relatedBy >> (\r -> List.member r [MyChild, MyCaregiven]))` in the Fetch. Severity MED (data-entry gap for caregiver-headed families; masked if the child is already in db.people from prior nav, or if FamNut participants are always biological parents).
