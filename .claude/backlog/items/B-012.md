<!-- id:B-012 status:READY tier:4 round: -->
### B-012 — Device title→robot-user collision — READY · CONFIRMED (verification sweep 2026-07-06, self-verified)
- `hedley_device.module:80-98`: robot resolved by `user_load_by_name($node->title . ' Robot')`; presave (:22-45) enforces uniqueness ONLY on field_uuid + field_pairing_code, no title check anywhere (no hook_node_validate in any custom module) → two same-titled devices share one robot uid. Pairing deletes ALL tokens for that uid (`HedleyRestfulPairingCode.class.php:105-116` propertyCondition('uid')+entity_delete_multiple) → pairing device B silently invalidates device A. Devices created via free-text admin UI title — nothing prevents duplicates operationally.
- Fix: enforce title uniqueness in hedley_device_node_presave alongside the existing checks, or key the robot on field_uuid.
